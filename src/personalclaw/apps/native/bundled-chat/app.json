{
  "name": "bundled-chat",
  "version": "0.1.0",
  "displayName": "Bundled offline model",
  "description": "The small Apache-2.0 chat model that ships with PersonalClaw so a fresh install can hold a conversation before any provider is bound. Runs on your machine with no account, no API key and no network. It is deliberately tiny, so it is only ever the floor: bind a real model in Settings → Models and this stops being used.",
  "icon": "HardDriveDownload",
  "author": "PersonalClaw",
  "tags": [
    "model",
    "llm",
    "local",
    "offline",
    "bundled"
  ],
  "provider": {
    "type": "model",
    "providerType": "bundled-chat",
    "implementation": "provider:create_provider",
    "multiInstance": false,
    "capabilities": [
      "chat"
    ],
    "settingsSchema": {
      "type": "object",
      "properties": {
        "offer_as_fallback": {
          "type": "boolean",
          "default": true,
          "x-meta": {
            "label": "Answer when nothing else is bound",
            "help": "On by default: with no provider configured, this model answers chat so a fresh install is not a dead end. Turn it off to go back to the calm “set up a model” state instead."
          }
        },
        "max_output_tokens": {
          "type": "integer",
          "minimum": 1,
          "default": 320,
          "x-meta": {
            "label": "Maximum reply length",
            "help": "Tokens. This model runs on the CPU at roughly 5–50 tokens per second, so a long cap means a long wait. A reply that reaches it stops mid-sentence and is marked as cut off. It is never more than half the prompt budget, because the reply has to fit in that window too."
          }
        },
        "context_tokens": {
          "type": "integer",
          "minimum": 1,
          "maximum": 8192,
          "default": 4096,
          "x-meta": {
            "label": "Prompt budget",
            "help": "Tokens the model reads at once: the conversation plus room for its reply. The weight can take up to 8192, but time and memory both grow with it. The oldest turns are dropped first, and a single message too long to fit is refused with the limit and how to fix it. Lowering this speeds up replies and refuses shorter messages.",
            "tags": [
              "advanced"
            ]
          }
        },
        "temperature": {
          "type": "number",
          "minimum": 0,
          "default": 0,
          "x-meta": {
            "label": "Temperature",
            "help": "0 is greedy decoding, which is the most coherent setting at this model size. Above 0 samples with nucleus (top-p) selection.",
            "tags": [
              "advanced"
            ]
          }
        },
        "top_p": {
          "type": "number",
          "minimum": 0,
          "default": 0.95,
          "x-meta": {
            "label": "Top-p",
            "help": "Nucleus sampling mass. Ignored when temperature is 0.",
            "tags": [
              "advanced"
            ]
          }
        },
        "repeat_penalty": {
          "type": "number",
          "minimum": 0,
          "default": 1.12,
          "x-meta": {
            "label": "Repetition penalty",
            "help": "Small models loop without this. 1.0 disables it.",
            "tags": [
              "advanced"
            ]
          }
        }
      }
    }
  },
  "native": true
}
