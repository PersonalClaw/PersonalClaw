<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MRI-2` — Six-row curated read-only tool table + arg validation + fencing meta-test

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MCP-READONLY-INBOUND](../plans/MCP-READONLY-INBOUND.md) (`MRI`, plan status `done`) |
| **Atom table** | [`../atomic/MRI.md`](../atomic/MRI.md) |
| **Execution position** | #361 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 — Tool table + validation (T2.1-T2.5)

## Done when

tools/list advertises memory_recall, knowledge_search, tasks_list, task_get, sessions_search, status with memory-vs-knowledge boundary sentences; each returns real fenced (<untrusted_content source=inbound:mcp:...>) data; incognito-suppressed memories never appear (gate located as per-session and shown not to apply); sessions_search redaction test green (seeded credential/token-like string never in output); unknown args -> JSON-RPC -32602, wrong types refused, out-of-range limits clamp; meta-test iterating real TOOLS fails if any tool skips wrap_result; arg-coverage test asserts every registered tool is covered.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MRI-1`](MRI-1.md) | intra-repo | `done` |

### Unblocks (3)

[`MRI-3`](MRI-3.md) (`done`), [`MRI-4`](MRI-4.md) (`done`), [`MRI-5`](MRI-5.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 11 (MRI) — read from the live module

**Code evidence:**

- personalclaw.inbound.tools.TOOLS imported at runtime holds EXACTLY 6 entries, matching the atom's 'six-row curated read-only tool table'
- part of the 146-passed inbound run, which includes argument validation

**Notes:** Confirmed by importing and counting rather than by reading a docstring — the same technique that settled AG-13's fourteen knobs. A curated table is only as good as its curation being real, and six is six.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `T2.1`, `T2.5`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:52` — | `memory_recall(query, limit≤20)` | `MemoryService.recall_with_provenance` | honors temporary/incognito restrictions (reuse the recall API's gate — verify location T2.1); memory.db = harness mechanics (stated in description) |
- `T2.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:96` — | `memory_recall` | `{query:str, limit:int≤20}` | `MemoryService.recall_with_provenance` | honors temporary/incognito restriction (T2.1 verifies the gate location) |
- `T2.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:132` — | T2.1 | Verify the memory-restriction gate location (temporary/incognito) on the recall path; wire `memory_recall` through it; result via `_wrap_result` | `src/personalclaw/inbound/tools.py` (+ Execution log note on gate location) | incognito-fixture session's suppressed memories never appear in…
- `T2.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:242` — ### 2026-07-28 — Session 2 (T2.1–T2.5, V2 partial): DONE
- `T2.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:249` — **T2.1 — the memory restriction gate: located, and it does NOT apply here.** The plan
- `T2.5` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:136` — | T2.5 | `_wrap_result` fencing helper + a meta-test: every registered tool's output passes through it (iterate table, assert fenced markers present) | `inbound/tools.py`, tests | meta-test fails if a new tool skips fencing (fixture-verified) |
- `T2.5` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:234` — untrusted-data fencing even by accident. T2.5's meta-test still lands with the
- `T2.5` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:242` — ### 2026-07-28 — Session 2 (T2.1–T2.5, V2 partial): DONE
- `T2.5` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:277` — **T2.5 fencing meta-test** iterates the REAL `TOOLS` table rather than a fixture list,
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:12` — **Status:** IN PROGRESS — Session 1 (transport + auth + caps + audit) shipped 2026-07-28; **Session 2
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:24` — said Session 2 was not started). Deepened 2026-07-18 with code recon (initial PROPOSED 2026-07-18;
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:128` — ### Session 2 — Tool table + validation
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:157` — auth, caps, audit and the kill switch are all provable on their own, so Session 2's
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:230` — **Deferred to Session 2, by design:** the five tools. `TOOLS` is empty and
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:233` — text and the dispatcher does the wrapping — so a Session 2 tool cannot skip
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:242` — ### 2026-07-28 — Session 2 (T2.1–T2.5, V2 partial): DONE
- `Session 2` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:412` — **The headline: a stock MCP client could not connect at all.** Session 2's V2 note said "the

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
