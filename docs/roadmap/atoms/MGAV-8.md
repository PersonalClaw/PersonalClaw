<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MGAV-8` — Memory slots: bounded always-injected registers + reflection append hook + self-model

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MEMORY-GRAPH-AND-VAULT](../plans/MEMORY-GRAPH-AND-VAULT.md) (`MGAV`, plan status `done`) |
| **Atom table** | [`../atomic/MGAV.md`](../atomic/MGAV.md) |
| **Execution position** | #352 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1379 |

## Intent — what this is for

§6 Memory Slots, §6.1 the self-model slot; Session 5 (backend + slot primitive)

## Done when

slot.<name> prefix added to _BUILTIN_PREFIXES with per-slot size cap enforced at put (over-cap append fails loudly as a trim proposal); lazy built-ins (persona/preferences/pending_items/self_notes/glossary(workspace)/self_model); one bounded Slots block injected in build_session_context adjacent to persona/USER PROFILE; after_turn_review append-only hook (WAL, undo, human-tombstone respected); self-model promotes a line to a behavioral principle only after >=3 reinforcements

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`MGAV-9`](MGAV-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `3b1ab8201` 2026-08-15 — feat(memory): MGAV-8 memory slots with caps, lazy built-ins and reinforced self-model

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:918` — callers**. `memory_slots` (`MGAV-8`) had no editor at all, making a register the plan calls
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:931` — over-cap append is a **409 carrying the trim proposal**, not a 400 with a message: `MGAV-8`'s

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§6`, `§6.1`, `Session 5`. Those tokens appear in its source plan at:

- `§6` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:28` — - **NEW-15** — readable Markdown vault (Obsidian-compatible, wikilinked, edits flow back, three-type split, symlink + raw/ capture, starter seeding, static export) → §5; memory citations in chat + admit-ignorance → §5.4; memory slots (named size-capped always-injected registers) + per-project glo…
- `§6` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:44` — - **Memory record kinds are key-prefix-inferred** (`memory_record.py:_kind_from_key` L310: `lesson.*`, `user.procedural.*`, `user.persona.*`, `user.commitment.*`) — there is **no kind column**. New record classes in this plan (slots §6, claims §4.2) follow the same convention: new key prefixes + …
- `§6` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:207` — slots/<name>.md     # §6 registers — THE primary slot editor
- `§6` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:245` — - **Built-ins** (created lazily, all optional): `persona`, `preferences`, `pending_items`, `self_notes`, `glossary` (the per-project domain-glossary variant — scope `workspace` via the existing `MemoryScope` axis, riding memory's cwd-partitioning), `self_model` (§6.1).
- `§6` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:288` — | `after_turn_review.py` | **EXTENDED (append-only)** | slot reflection hook (§6); eligibility gates untouched (LEARNING-FLYWHEEL owns their evolution) |
- `§6.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:245` — - **Built-ins** (created lazily, all optional): `persona`, `preferences`, `pending_items`, `self_notes`, `glossary` (the per-project domain-glossary variant — scope `workspace` via the existing `MemoryScope` axis, riding memory's cwd-partitioning), `self_model` (§6.1).
- `Session 5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:335` — - **Session 5 — slots + FE + wiring:** `slot.*` prefix + caps + built-ins + reflection append hook + self-model promotion logic, MemoryPanel tabs (Slots editor, entity browser, lint/stats surfaces, settings), MemoryGraph upgrade + HTML export, full FOUR-point config wiring, as-a-user validation s…
- `Session 5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:907` — ### 2026-08-16 — Session 5 (§7.1–7.2 FE + config wiring): PARTIAL (`MGAV-9`)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
