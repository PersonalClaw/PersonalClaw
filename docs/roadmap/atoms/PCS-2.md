<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PCS-2` — §C3 date-line relocation with truncation-immunity (F2 fix)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROMPT-CACHE-SUBSTRATE](../plans/PROMPT-CACHE-SUBSTRATE.md) (`PCS`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PCS.md`](../atomic/PCS.md) |
| **Execution position** | #410 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##788 |

## Intent — what this is for

Session 1 / T1.2; §C3 (prefix stability)

## Done when

[CURRENT DATE] moved to the end of the assembled block in BOTH the is_custom and normal branches, appended AFTER the _MAX_CONTEXT_CHARS truncation step; date text byte-identical; explicit test proves an oversized context still ends with the date line; test_context.py:698-707 and the test_context_engine.py:45 fixture updated; two assemblies ~1 min apart are byte-identical up to the final date line.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `555babe03` 2026-08-06 — feat(llm): PCS-1 wire-order repair — stable prefix leads, volatile note at tail (F1) (#882)
- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:983` — personalclaw.manifest_reference` and amended into the DAS-6a commit; cascade-rebased CATO-1 + PCS-2.
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:284` — - **PCS-2 DONE — [CURRENT DATE] moved after truncation (§C3).** `context.py::build_session_context`
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:290` — text byte-identical; date now always the final block, exactly once. **Scope:** PCS-2 only (the

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:234` — | T1.2 | §C3 date relocation **incl. the truncation fix**: move `[CURRENT DATE]` to the end of the assembled block in BOTH branches, appended AFTER the `_MAX_CONTEXT_CHARS` truncation; update `tests/test_context.py:698-707` and the `test_context_engine.py:45` fixture; comment states both reasons …
- `Session 1` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:229` — ### Session 1 — Wire-order repair + prefix stability (no marker; pays off on its own)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
