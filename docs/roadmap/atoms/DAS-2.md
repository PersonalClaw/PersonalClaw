<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DAS-2` — Deterministic shard format + manifest + `backup validate` (Session 2a)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DURABILITY-AND-SYNC](../plans/DURABILITY-AND-SYNC.md) (`DAS`, plan status `done`) |
| **Atom table** | [`../atomic/DAS.md`](../atomic/DAS.md) |
| **Execution position** | #140 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2 Deterministic Shard Format; Implementation Order Session 2 (format half)

## Done when

durability/shards.py export_shards() writes canonical byte-identical JSONL (sorted keys/rows, year-sharded append streams, part-split, content-addressed blob dir) + manifest.json (schema version, machine_id, per-shard bytes/rows/sha256); `personalclaw backup export [--incremental]` / `backup validate` CLI with non-zero exit; determinism + corruption detection proven; ~28 tests green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DAS-1`](DAS-1.md) | intra-repo | `done` |

### Unblocks (2)

[`DAS-3`](DAS-3.md) (`done`), [`DAS-6`](DAS-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1521` — (DAS-2's contract, with byte-determinism tests on it), not this atom's. **What shipped instead:**

### Its plan's own record, via the scope references (16 line(s))

This atom's scope cites `§2`, `Session 2`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:28` — - **NEW-4** — durable state backup + multi-machine sync core (JSONL shards + SHA manifest + validate; pull→merge-import→export-union→push; boot-started scheduled self-backup with rolling backups + restore endpoints; git-versioned memory snapshots; encrypted-intents crypto for machine exchange) → …
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:115` — Multi-machine sync = the shard representation (§2) + a dumb shared store + a deterministic merge. **No PersonalClaw server anywhere**: the shared store only ever needs "write these objects" and "list/read objects" — insert-only semantics (lifeGLANCE's zero-server-logic doctrine, re-grounded onto …
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:184` — - `POST /api/durability/export` — full or per-domain (memory / knowledge / work / automation / platform / config) shard export as a zip (inventory-driven; `secret ∪ derived` excluded exactly as `EXPORT_EXCLUDE` does today), with the §2 manifest inside. "Give me everything PersonalClaw knows about…
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:237` — - **Session 2 — Shard exporter + manifest + validate + snapshot service.** §2 format (canonical JSONL, year shards, part-split, tombstone write-path hook, blob dir for knowledge files); `backup validate`; boot-started service with nightly tar + hourly dirty-entry shard export + hourly git commit …
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:282` — - **Auto-detect default (sharpens :650):** restoring onto a non-empty home (any non-derived inventory entry present — not just `memory.db`) → propose merge, print the plan, require confirmation; `--replace` stays the explicit wipe path and keeps the `_is_gateway_running` refusal + `pre-restore-<t…
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:303` — - [2026-07-28][S2a] DONE (§2 — the shard format half of Session 2): `durability/shards.py`. (a) **Deterministic export** — `export_shards()` writes canonical JSONL (sorted keys, compact separators, LF, UTF-8) one directory per inventory entry, rows sorted by id; entity dirs → `entities.jsonl`, si…
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1515` — atom row both say the export is a *shard* zip with the §2 manifest inside. Measured why that cannot
- `§2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1520` — a bag of anonymous blobs, unrestorable by name. Adding a tree index is a §2 shard-FORMAT change
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:12` — **Status:** IN PROGRESS — Session 1 (inventory + audit) and Session 2 (2a shards / 2b service / 2c
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:237` — - **Session 2 — Shard exporter + manifest + validate + snapshot service.** §2 format (canonical JSONL, year shards, part-split, tombstone write-path hook, blob dir for knowledge files); `backup validate`; boot-started service with nightly tar + hourly dirty-entry shard export + hourly git commit …
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:287` — Folded into **Session 2** (which already owns the restore endpoints + `backup validate`); the merge-plan UI confirm lands with Session 5's archive browser. Session count stays 5.
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:303` — - [2026-07-28][S2a] DONE (§2 — the shard format half of Session 2): `durability/shards.py`. (a) **Deterministic export** — `export_shards()` writes canonical JSONL (sorted keys, compact separators, LF, UTF-8) one directory per inventory entry, rows sorted by id; entity dirs → `entities.jsonl`, si…
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:310` — ## Execution log — Session 2b (scheduled snapshot service)
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:390` — ## Execution log — Session 2c (the backups settings surface)
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:451` — ## Execution log — Session 2d (the declared merge with no executor)
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:504` — ## Execution log — Session 2e (the restore side of the gap closure)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
