<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2TAS-9` — FE surfacing surfaces: surfacingMeta.ts, composer chips, validated deep-links, templates-list freshness/scope/pack rendering

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-TASKS-SOPS](../plans/WORKFLOWS-V2-TASKS-SOPS.md) (`WF2TAS`, plan status `done`) |
| **Atom table** | [`../atomic/WF2TAS.md`](../atomic/WF2TAS.md) |
| **Execution position** | #631 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§7 Surfacing UX (R15) — composer chips, validated deep-links, templates list freshness gradient/scope states/pack proposals

## Done when

surfacingMeta.ts mirrors workflowMeta discipline reading backend-computed state; off def gets no chip, suggest gets run affordance; deep-link params allowlisted against declared inputs with reported rejections; list degrades not blanks; validated as-a-user against live gateway with zero console messages (34 FE tests, S61c)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2TAS-8`](WF2TAS-8.md) | intra-repo | `done` |

### Unblocks (1)

[`WF2TAS-11`](WF2TAS-11.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `§7`, `R15`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:40` — - **TASK-R15** → §7 Surfacing UX (composer chips, validated deep-links)
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:51` — 5. The FE DagView node-level **Approve/Deny (`onApprove`/`onDeny` + `awaiting` state) is a declared, UNWIRED extension point** (`web/src/pages/tasks/DagView.tsx`) — §4's ConfirmationRequest is the missing backend seam and §7 wires it.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:409` — - One entity means ONE inbox surface (dashboard ActionCenter / "Needs you") and ONE autonomy policy instead of three bespoke flows — and it is exactly the backend seam the FE DagView's unwired `onApprove`/`onDeny` extension point has been waiting for (§7).
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:569` — - **Session 3 — ConfirmationRequest + gates:** the unified durable record with atomic single-use resolution + auto-resume; `require_hitl`; needs-input queue semantics; DagView Approve/Deny wiring; guardrails/postconditions def sections + verbatim injection; per-stage mute + step-scoped approval m…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:797` — `onApprove`/`onDeny` is the declared extension point this session supplies the backend for (§7 wires
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:911` — surfaces to §7/session 7, and the fields land with the def-model session. The semantic channel
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:980` — belong with §7's wiring session; the decision rules they implement are complete and tested here.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1092` — The frontend half of §7's surfacing UX, driven against a live gateway rather than asserted.
- `R15` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:40` — - **TASK-R15** → §7 Surfacing UX (composer chips, validated deep-links)
- `R15` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:507` — ### Surfacing UX (R15)
- `R15` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:556` — 2. **Surfacing regression risk** — three channels could over-fire. Mitigation: `off`-by-default for new defs, negative triggers, once-daily cadence throttle, fingerprint runs only on directory attach, trigger-accuracy CI per bundled template, visible chips (R15) so a bad match is one toggle away …
- `R15` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:573` — - **Session 7 — UX + validation:** composer chips + validated deep-links + checklist edit UX (R15); config four-point wiring; end-to-end validation as-a-user: run a checklist workflow → tasks appear in board → gate pauses on a ConfirmationRequest → resolve in DagView → run auto-resumes → verified…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
