<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-5` — Loops emit the ledger (closes the flywheel's loop blind spot)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #616 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1339 |

## Intent — what this is for

Measured: `learning/` has ZERO references to `loop.store` or `get_findings`. The five loop kinds — where the long-horizon autonomous work actually runs today — generate no learning signal at all, so LangChain's fourth loop ("the return arrow reaches inside and updates the agent loop directly", 2026-06-16) closes over workflow runs only. Depends on `WF2LOO-16` because a loop cycle cannot emit `judge_verdict` until the third verdict vocabulary speaks the contract; emitting `CycleVerdict`'s shape would add a fifth dialect to the ledger rather than closing the gap.

## Done when

Loop cycles emit to the `PP-4` ledger: a cycle becomes `step_started`/`step_completed`, a supervisor assessment becomes `judge_verdict` in the reconciled vocabulary, a stall becomes `breaker_trip`, a reap becomes `watcher_reaped`. `loop/store.py`'s findings/verdicts files become projections over the ledger rather than a second store (clean break — no dual write). Verified by driving a real loop and reading its trajectory back from the ledger alone, and by `learning/mining.py` returning a non-empty result for a loop run where it previously returned nothing — the acceptance bar is the flywheel producing a proposal from loop evidence, not merely the rows existing.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-4`](PP-4.md) | intra-repo | `done` |
| [`WF2LOO-16`](WF2LOO-16.md) | intra-repo | `done` |

### Unblocks (1)

[`PP-16`](PP-16.md) (`todo`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `fee109c66` 2026-09-06 — feat(workflows): the run side gets its two ledger rails — findings and verdict/ROI (PP-16 seam 4)
- `5ceb84248` 2026-08-27 — refactor(loop): PP-16 retire loops.total_cycles, a cache of a shipped projection
- `94711166d` 2026-08-15 — docs(roadmap): flip PP-2/PP-5/PP-10/PP-14 to done (batch cherry-pick lost their plans[] edit)
- `70f939eb6` 2026-08-15 — feat(loops): PP-5 emit the ledger and project findings/verdicts over it
- `fee85e575` 2026-08-14 — refactor(ledger): PP-4 extract the ledger as a platform primitive

### Named by atom id in the roadmap prose (21)

- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:227` — `loop/watchdog.py:401 _complete`, which already calls `_capture_loop_end` (`:464`, PP-5) — that
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:236` — `_capture_loop_end` and **before** the `"complete"` publish — a sibling of the PP-5 hook, not a
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:647` — PP-5 has since landed, so a loop IS a ledger producer (`loop/journal.py` — "the SECOND producer
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:655` — PP-5 follow-on work in the loop engine, not this atom's fence, and inventing a number to fill
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:670` — "~$X this run" — on the reasoning that session 2 had deferred it to "PP-5 follow-on work" and PP-5
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:671` — has since landed. PP-5 landing was necessary but not sufficient, and the measurement below is the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:96` — One vocabulary unlocks seven findings at once: loop→flywheel coverage (`PP-5`), replay as a real verb
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:139` — PP-4 ─┬─→ PP-5 (+WF2LOO-16) ─────────────┐
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:153` — **The timing argument, which is the part that needs an owner decision.** `PP-4`, `PP-5`, `PP-9` and
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:273` — verdict/ROI rail are PROJECTIONS over the `PP-5` ledger, which already carries `step_completed`,
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:379` — (`store.py:1000`), which `PP-5` already ships; `ledger.reader.run_totals` already returns
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:537` — (PP-5), same kinds, same reader, so the flywheel already sees loop runs; *one attention path* —
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:695` — import that no import-time probe would see) and reds naming the file and line. `PP-5` is the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:710` — §2's "one vocabulary" is the reason: `PP-5` has loop cycles emitting `step_started`,
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:713` — forbidden direction) or fork the vocabulary into the fifth dialect `PP-5` exists to prevent. All
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1127` — - **2026-08-15 — `PP-5` DONE.** The loop engine is the SECOND producer of the ledger.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1144` — - **2026-08-15 — DEVIATION (`PP-5`): the worker's finding FILE stays; it is the ingest source, not a
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1155` — - **2026-08-15 — DISCOVERY (`PP-5`): `step_started` is journal-only, not a durable ledger kind.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1165` — - **2026-08-15 — `PP-5` proof.** Falsified three times, each target line read first and restored from
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1519` — **Not a dependency problem.** `PP-16` is READY: every non-`EXT` dep (`PP-15`, `PP-5`, `PP-13`) is
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1960` — computes `len(get_findings(cid))`, `get_findings` is a pure projection over `step_completed` from `PP-5`,

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
