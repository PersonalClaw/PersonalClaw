<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2TAS-2` — Verified done + enforcement: engine-owned criterion, actor matrix, cascade-fail, stuck-work sweep

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-TASKS-SOPS](../plans/WORKFLOWS-V2-TASKS-SOPS.md) (`WF2TAS`, plan status `done`) |
| **Atom table** | [`../atomic/WF2TAS.md`](../atomic/WF2TAS.md) |
| **Execution position** | #624 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1 Verified Done (R2), Projection Enforcement three-actor matrix (R5-rest), Cascade-Fail Propagation (R17); §5 diagnostics

## Done when

workflows/verified_done.py: engine-owned criterion execution over loop/gates tristate, pass-state gating, weighted acceptance schema, three-actor transition matrix, binding-graph cascade with debounced notify, stuck-work sweep, idempotent timing (70 tests, S56)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2TAS-1`](WF2TAS-1.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:loop/gates run_verify_command tristate + audit_bash_command screen` | cross-plan (EXT) | **unresolved** |

### Unblocks (2)

[`WF2TAS-11`](WF2TAS-11.md) (`done`), [`WF2TAS-6`](WF2TAS-6.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 7 (WF2TAS) — DRIVEN in a real browser

**Code evidence:**

- part of the 392-passed WF2TAS run

**Driven in the UI:** The verified-done criterion is USER-AUTHORABLE and per-condition, observed live: the new-task form carries an 'Add a completion condition' field, and adding one immediately renders it as its own control — `button "Mark done: the completion condition is observable"`. So each criterion is independently markable rather than the task carrying one blanket done flag.

**Notes:** This is the strongest single observation of the cycle. The atom pairs an engine-owned criterion with enforcement; I could not drive the ENFORCEMENT (that needs a run to enforce against), but the criterion is real, authorable and individually resolvable in the product, which is the part a user meets. The form also exposes prerequisites, steps, and three distinct note kinds (note / research note / execution note) — a richer task model than the atom's title implies.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (32 line(s))

This atom's scope cites `§1`, `§5`, `R2`, `R5`, `R17`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:26` — - **TASK-R1** → §1 State Projection (typed block/status taxonomy + card previews)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:27` — - **TASK-R2** → §1 Verified Done (done_criterion, evidence, engine-owned transitions, diagnostics sweep, clean-exit template, granularity lint)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:30` — - **TASK-R5** → §1 Projection Enforcement (API-rejected writes, actor matrix, fingerprint dedup, fan-out caps, idempotent recompute)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:37` — - **TASK-R12** → §1 Task Body Contract + §6 Context Bundles
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:42` — - **TASK-R17** → §1/§5 Cascade-Fail Propagation
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:258` — `requirements` preflight: a def whose requirements are unmet **fails at suggestion time naming the missing item** instead of surfacing a suggestion that dies mid-run. The preflight uses the real availability seam that already exists: provider bundles may export module-level `availability() -> (bo…
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:422` — - Stages carry `enabled: bool` — muted = skipped-but-visible; projects as `skipped` per §1 (not removed), letting users trim a checklist instance (e.g., mute the staging gate for a hotfix) without structural edits.
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:453` — | Manual block (external reason) | `gate{kind: approval, prompt: "Blocked: {reason}"}` — pauses projection per §1 |
- `§5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:35` — - **TASK-R10** → §5 Frontier/Next Projections, Evented Unblock, TTL'd Leases
- `§5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:42` — - **TASK-R17** → §1/§5 Cascade-Fail Propagation
- `§5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:99` — - **Stuck-work diagnostics sweep**: a periodic pass flags `in_progress` without node heartbeat >20min, `open`-and-ready unclaimed >1h, and done-without-evidence — surfaced as a Tasks-board strip and as Run Ledger findings (WORKFLOWS-V2 §5). The sweep also auto-releases expired leases (§5).
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:27` — - **TASK-R2** → §1 Verified Done (done_criterion, evidence, engine-owned transitions, diagnostics sweep, clean-exit template, granularity lint)
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:52` — 6. The Task model **already has** `exit_criteria` with `can_mark_complete` requiring all criteria complete (tasks/models.py:224) — `done_criterion` (R2) extends this existing seam rather than inventing a parallel one.
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:92` — ### Verified Done (R2)
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:108` — - **Three-actor transition matrix** (CORE precedent, R5 batch-5): allowed transitions are per-ACTOR (engine / user / agent), covering standalone tasks too. The agent (via `task_update` tool) may only move tasks to propose states (`blocked(needs_input)`, review); claim states (`in_progress` by eng…
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:125` — - The granularity lint (R2) enforces sizing at SOP-migration time.
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:198` — - **Granularity lint** (R2): flag steps that aren't one-session-completable or lack any verification.
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:463` — - **TTL'd lease claims**: an executing session/subagent takes an exclusive TTL'd lease (≤1h, renew/release) on a task before working it. Acquire is compare-and-swap — implemented as an fcntl-locked read-modify-write on the task's JSON file (the `single_flight`/flock pattern, since per-entity JSON…
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:504` — - **Stuck-work strip** (R2): the diagnostics sweep's findings render as a dismissible strip above the board.
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:30` — - **TASK-R5** → §1 Projection Enforcement (API-rejected writes, actor matrix, fingerprint dedup, fan-out caps, idempotent recompute)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:103` — ### Projection Enforcement (R5)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:108` — - **Three-actor transition matrix** (CORE precedent, R5 batch-5): allowed transitions are per-ACTOR (engine / user / agent), covering standalone tasks too. The agent (via `task_update` tool) may only move tasks to propose states (`blocked(needs_input)`, review); claim states (`in_progress` by eng…
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:142` — fingerprint: str = "" # sha1(source_ref or title+body)[:16] — dedup key (R5)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:555` — 1. **Per-file JSON, no transactions** — materialization dedup and lease CAS rely on flock + atomic_write discipline, not transactions. Mitigation: fingerprint-lookup-before-create, `single_flight` around lease acquire, projection-as-idempotent-recompute (R5) so any race resolves on the next rebuild.
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:566` — - **7 sessions** (after Workflows v2 Slices 0-4) — was 3; the approved high-priority set (R1/R2/R3/R4/R5/R6) adds ~1 session, the medium/low set adds ~3 more.
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:567` — - **Session 1 — Projection core:** `workflow_binding` + new Task fields (`blocked_kind`, `preview`, `done_criterion`, `evidence`, `attempts`, `fingerprint`) in tasks/models.py; `TaskStatus.SKIPPED`; auto-materialization with fingerprint dedup + fan-out caps; typed state projection table (R1, R5-c…
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:568` — - **Session 2 — Verified done + enforcement:** engine-owned criterion execution (reuse `run_verify_command`), pass-state gating, completion records; three-actor transition matrix + managed-write rejection on the task façade; cascade-fail propagation + debounced notify; stuck-work diagnostics swee…
- `R17` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:42` — - **TASK-R17** → §1/§5 Cascade-Fail Propagation
- `R17` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:80` — | upstream node failed/cancelled | `blocked` | `upstream_failed` | Cascade, see below (R17) |
- `R17` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:115` — ### Cascade-Fail Propagation (R17)
- `R17` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:461` — - **Evented auto-unblock for standalone tasks**: `status=done` on a task emits an event that unblocks dependents with `requires`/BLOCKS edges (today only workflow-bound tasks get this via `frontier()`). Failure of a blocker cascades `blocked(kind=dependency_failed)` with the blocker's failure rea…
- `R17` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:568` — - **Session 2 — Verified done + enforcement:** engine-owned criterion execution (reuse `run_verify_command`), pass-state gating, completion records; three-actor transition matrix + managed-write rejection on the task façade; cascade-fail propagation + debounced notify; stuck-work diagnostics swee…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
