<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-9` — Headless CLI mode — `personalclaw run` one-shot scripted turns

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #222 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2013 |

## Intent — what this is for

§9.5 (Headless CLI Mode, grok-build learning): run subcommand + 3 formatters, transient-gateway bootstrap, inbound:cli: headless classification, --allow grant flag, CI recipe docs. Session 7.

## Done when

`personalclaw run -p "<prompt>" [--format plain|json|streaming-json] [--agent] [--model] [--session] [--cwd] [--allow]` executes one turn against the local gateway, auto-starting a transient gateway (reusing the doctor readiness probe) when none runs, exits 0/nonzero per turn success; plain=final text, json=one {result,session,turns,tool_calls,tokens,duration_ms} doc, streaming-json=NDJSON of the same WS envelope frames (chat_chunk/tool_call/chat_done); inbound:cli: prefix inherits the §2.3 headless SafetyProfile (read-only defaults; --allow write grant printed to stderr); budgets ride SpendMeter scope_key=cli; --session opts into a persistent named session (default stateless); CI smoke-test recipe in docs + shell-script and GitHub-Action validation.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EA-1`](EA-1.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:headless SafetyProfile classification` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 40 (EA) — ARCC's SSRF guidance found a real hole; fixed in PR #2790

**Code evidence:**

- 🔑 DRIVEN TWICE against the real CLI. `personalclaw run -p … --format json --port 10011` returned exactly the document the clause specifies — {result, session, turns, tool_calls, tokens, duration_ms} — with session 'inbound:cli:f3b58c24', so the §2.3 headless prefix is real
- 🔑 THE READ-ONLY STANCE IS ANNOUNCED, NOT ASSUMED: every run prints 'read-only — session inbound:cli:<id> denies every non-read-only tool. Pass --allow to grant writes.' The user learns the profile and its widening flag before anything runs
- the exit contract holds under a real failure: with no model bound the run exits 1 and prints a three-line WHAT/WHY/FIX naming the remediation ('add a model provider in Settings → Providers, then bind chat to it') — and it does so identically under --format plain AND --format json
- --port is present (not in the clause, but it is what let this be driven against a non-default gateway), alongside --agent/--model/--session/--cwd/--allow/--timeout
- test_cli_run.py green inside the 143
- 333 across the EA seam/dialect/bridge/capture/replay/a2a suites; +132 capture incl. the new redirect suite; +143 cli_run + inbound_mcp; +28 channel-inbound chokepoint

**Driven in the UI:** Driven as a CLI rather than in a browser, which is the surface this atom IS.

**Notes:** 🪤 A FINDING COLLAPSED HERE TOO, and the process note is worth more than the finding. I first read the json run as exiting 0 with an empty result — an inconsistency with plain's exit 1 that would have been a real defect — because I piped combined output through tail and saw only the JSON tail while the diagnostic went to stderr. Capturing the exit code explicitly showed BOTH formats exit 1 with the same diagnostic. Withdrawn. Seventh collapsed finding of this campaign, and the first caused by my own output handling rather than by a misread of the code.

## Recorded history

### Commits (3)

- `361d281c0` 2026-08-24 — docs(roadmap): cite the batch PR on EA-9
- `524cf7d5c` 2026-08-24 — docs(roadmap): EA-9 done — headless run shipped (PR #2011)
- `2bd69cdcd` 2026-08-24 — feat(cli): EA-9 headless `personalclaw run` one-shot scripted turns

### Named by atom id in the roadmap prose (21)

- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:867` — ## Execution log — `EA-9` (§9.5 Headless CLI mode: `personalclaw run`)
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:869` — - [2026-08-24][EA-9] ✅ **DONE — `src/personalclaw/cli_run.py` + a top-level `run` subcommand.** Driven end
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:876` — - [2026-08-24][EA-9] **NOT an extension of `chat -m`, deliberately.** `cli_chat._chat` builds a provider
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:881` — - [2026-08-24][EA-9] 🔴 **The read-only rail shipped INERT first, and only driving it found that.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:889` — - [2026-08-24][EA-9] **DEVIATION — read-only is enforced by TASK MODE, not `SafetyProfile.tool_grants`.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:897` — - [2026-08-24][EA-9] 🔴 **OWNER DECISION SURFACED — a read-only headless turn on an ACP agent is
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:909` — - [2026-08-24][EA-9] 🔴 **A measured classification gap, fixed: `dashboard:inbound:cli:<id>` classified
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:917` — - [2026-08-24][EA-9] **DEVIATION — `inbound:` went in `policy._EXTRA_UNATTENDED_PREFIXES`, not
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:924` — - [2026-08-24][EA-9] **DEVIATION — `SpendMeter` has no `scope_key`.** §9.5 says "budgets ride SpendMeter
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:933` — - [2026-08-24][EA-9] 🔴 **A 2s liveness probe misread a live gateway as absent — and that is
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:942` — - [2026-08-24][EA-9] **DEVIATION — auth rides `?token=`, not `Authorization: Bearer`.** `token_auth` reads
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:946` — - [2026-08-24][EA-9] **DEVIATION — `"tokens"` reads the dashboard-wrapped ledger key.** No WS frame
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:950` — - [2026-08-24][EA-9] **The readiness probe was NOT reused, because there is nothing reusable.** §9.5 says
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:956` — - [2026-08-24][EA-9] **CI recipe + validation shipped:** `scripts/ci_smoke_run.sh` (drives all three
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:961` — - [2026-08-24][EA-9] **Transcript containment: nothing escaped.** The drive left transcripts under the
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:969` — - [2026-08-24][EA-9] ✅ **ATOM FLIPPED `done` (PR #2011).** Integration re-ran the gate on the branch
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:979` — **The flip is justified against the criterion, not the surrounding surface:** `EA-9`'s `done_when`
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1128` — - [2026-08-25][EA-2] **DEVIATION — `inbound:` was NOT added to `session._STATELESS_PREFIXES`.** EA-9's
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1133` — transcript AND the provider resume id. The guardrails half of the clause needed no work — EA-9 already
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1143` — `chat_handlers._run_chat_scoped` (added by EA-9) reads segment 1 of an `inbound:` key as the SpendMeter
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1215` — not by the literal mechanism: EA-9's ruling stands (that list is the provider resume/pool axis and

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§9.5`, `Session 7`. Those tokens appear in its source plan at:

- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:17` — harness, and §9.5's headless `personalclaw run` (the existing `run` subcommand is `subagent spawn
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:256` — - **Session 7 — headless CLI mode (§9.5, grok-build learning):** `personalclaw run` subcommand with plain/json/streaming-json formatters, transient-gateway bootstrap, `inbound:cli:` headless-profile classification, `--allow` grant flag, CI recipe docs, shell-script + GitHub-Action validation.
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:867` — ## Execution log — `EA-9` (§9.5 Headless CLI mode: `personalclaw run`)
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:878` — attribution. It is a provider smoke test. §9.5 says "against the local gateway", so `run` is a *client*
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:890` — §9.5 says the run "inherits the §2.3 headless SafetyProfile (read-only defaults)". `tool_grants` has
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:920` — `inbound:` there would contradict §9.5's own `--session` clause, which exists so a named headless
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:924` — - [2026-08-24][EA-9] **DEVIATION — `SpendMeter` has no `scope_key`.** §9.5 says "budgets ride SpendMeter
- `§9.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:950` — - [2026-08-24][EA-9] **The readiness probe was NOT reused, because there is nothing reusable.** §9.5 says
- `Session 7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:195` — - **Session (+1, appended as Session 7):** the `run` subcommand + three formatters + transient-gateway bootstrap + headless profile classification + `--allow` grant flag; CI smoke-test recipe in docs (`personalclaw run -p "..." --format json | jq .result`); as-a-user validation from a shell scrip…
- `Session 7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:256` — - **Session 7 — headless CLI mode (§9.5, grok-build learning):** `personalclaw run` subcommand with plain/json/streaming-json formatters, transient-gateway bootstrap, `inbound:cli:` headless-profile classification, `--allow` grant flag, CI recipe docs, shell-script + GitHub-Action validation.
- `Session 7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:258` — Sessions 1-3 are NEW-10's core and ship value alone; 4-5 are NEW-20; 6 completes both. Session 4 can land before 2-3 if capture is wanted early (it depends only on Session 1). Session 7 depends on Session 1's headless-profile classification but can otherwise land any time after it.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
