<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `IA2-7` — Adoption-sweep frontend: mount InvestigateButton across all surfaces + per-kind prompts/agents

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INVESTIGATE-ANYWHERE](../plans/INVESTIGATE-ANYWHERE.md) (`IA2`, plan status `done`) |
| **Atom table** | [`../atomic/IA2.md`](../atomic/IA2.md) |
| **Execution position** | #292 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

S2 T2.3+T2.4 + V2 — FE sweep, opening prompts/suggested agents, validation

## Done when

9 FE mounts of the ONE shared primitive (notifications, TaskDetail, ScheduleDetail runs, LoopCockpit cycle, KnowledgeDetail, MemoryPanel records+lessons, DoctorPanel cards, AuditPanel rows); grep gate clean (zero bespoke variants); per-kind opening prompts read naturally, agents conservative (empty unless clearly better); two dead-back-link route bugs fixed and locked by test_back_links_use_real_frontend_routes; V2 walk of all 13 kinds passes; full lint+test+web gate green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`IA2-4`](IA2-4.md) | intra-repo | `done` |
| [`IA2-6`](IA2-6.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.3`, `T2.4`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:116` — | T2.3 | Frontend sweep: drop `InvestigateButton` on InboxPage/InboxDetail, NotificationsPage, TaskBoard/TasksListPage/DagView/TaskDetail, ScheduleDetail run rows, LifecycleDetail/TriggersListPage run rows, LoopCockpitPage findings/cycles, KnowledgeListPage/KnowledgeDetail, MemoryPanel records+le…
- `T2.4` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:117` — | T2.4 | Per-kind opening prompts + suggested agents where an obvious specialist exists (e.g. failures → the session default; knowledge → default); keep suggestions conservative — `""` unless clearly better | `investigate.py` resolver bodies | prompts read naturally per kind; no agent is forced |
- `T2.4` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:141` — - [2026-07-28][S2] DONE: the adoption sweep (T2.1-T2.4, V2). **11 new resolvers → 13 kinds**: `notification`, `task`, `schedule_run`, `trigger_run`, `loop_cycle`, `knowledge_item`, `memory_record`, `memory_lesson`, `doctor_finding`, `crash_report`, `audit_event` (+ the S1 pair). **Nine FE mounts*…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
