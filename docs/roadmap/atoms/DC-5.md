<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DC-5` — S4: Native notifications as a plan-42 rules target

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-CAPABILITIES](../plans/DESKTOP-CAPABILITIES.md) (`DC`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DC.md`](../atomic/DC.md) |
| **Execution position** | #152 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, and the remaining legs are HARDWARE, not code. The native target was declared-but-inert on three layers at once and all three are now wired: notification_rules.TARGETS carried `native` with the comment 'accepted and persisted but inert', its only consumer being a metadata annotation nothing read; desktop/main.js imported Electron's Notification solely for isSupported(); and the frontend listed native in INERT_TARGETS so a user could tick a checkbox that did nothing. native_delivery(rule, capability) is now one pure function with three outcomes, wired at the single delivery choke point on the immediate path only, consulting `available` rather than `granted` because macOS never reports notification authorization. UNOBSERVED: whether macOS actually draws the banner and whether a real click fires the handler - Electron was never launched, so those are proven against a recording double. Pairs with DC-1's dmg and DC-3/DC-4/DCU-3's identical on-device remainders: ONE launched signed shell closes four plans' V-clauses at once. |

## Intent — what this is for

Session 4 — Presence + platforms, T4.2 (native notifications target); Integration points ('native' notification target)

## Done when

a notification rule with target `native` fires an Electron OS Notification when the desktop shell is connected and the tap focuses the relevant surface; falls back to dashboard toasts when the shell is not connected.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DC-2`](DC-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 52 (DC) — the Security panel driven in a browser tab; contextIsolation and the inverted updater rail both falsified

**Code evidence:**

- 🔑 THE INERT-CONTROL DEFECT WAS ON THREE LAYERS AT ONCE AND ALL THREE ARE NOW WIRED: the target existed in `notification_rules.TARGETS` marked 'accepted and persisted but inert', the shell imported Electron's Notification only for `isSupported()`, and the frontend listed native in `INERT_TARGETS` so a user could tick a checkbox that did nothing. That last one is the worst form of the class — a control that looks like consent and does nothing
- `native_delivery(rule, capability)` is one pure function with three outcomes and it HAS A NON-TEST CALLER, checked rather than assumed: `dashboard/state.py:1470`, at the single immediate-path delivery choke point
- 🔑 THE FALLBACK IS THE ABSENCE OF AN ADDITION, NOT A SECOND PATH: 'The dashboard delivery that runs anyway IS the fallback — the caller does not choose a different path, it just does not raise an OS notification.' There is no fallback branch to get wrong
- 🔑 IT CONSULTS `available` RATHER THAN `granted`, ON A MEASURED PLATFORM FACT: 'macOS never reports notification authorization, so a `granted` check here would mean refusing to deliver on the one platform that cannot answer.' A fail-direction decision made from behaviour rather than from convention
- 🔑 A MONOTONICITY ARGUMENT, THE SECOND IN TWO CYCLES: it 'never promotes dashboard off the note and never runs for never/badge/digest' because 'native is an interruption, and the three quieter modes have already said not to interrupt'. A target cannot escalate past the user's stated preference
- the vacuity property is stated in the function itself: a rule that did not name native returns None, so 'a rule that did not ask for the desktop cannot get it, however the shell is configured'
- the unavailable outcome carries a REASON, because 'I asked for native and got a bell instead' needs an answer and 'a UI that cannot say why reads as a broken toggle'
- test_desktop_seam + test_desktop_install_kind 37/37; desktop node suite 358/358 across 75 suites (falsified: one contextIsolation flag flipped reds exactly 1 of 358; adding electron-updater to package.json reds exactly the inverted rail)

**Driven in the UI:** Not drivable: raising a real OS notification and tapping it to focus a surface needs the packaged shell. The panel half was observed from the browser as 'not connected', which is the same registry field this function consults.

**Notes:** Partial because the remaining legs are the operating system's, not the code's. The decision function is the best-reasoned small function in this plan: three outcomes, each one a distinct product experience, and a documented reason for every field it does and does not read.

## Recorded history

### Commits (2)

- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `1a9ff0c33` 2026-08-27 — feat(desktop): DC-5 make the native notification target dispatch

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:134` — **2026-09-02 — DC-5 validated done (impl pre-landed).** `native` is a LIVE plan-42 rules target (`notification_rules.py` cites DC-5; `native_delivery` when the shell is connected, dashboard-toast fallback otherwise). `desktop/test/nativeNotifications.test.js` 17/17.
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:196` — ### Session 5 — `DC-5` native notifications as a plan-42 rules target (2026-08-27)
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:208` — - **REMAINING (one unobservable leg + a genuinely unrunnable one).** Electron was never launched, so **"macOS draws a banner"** and **"clicking it fires the `click` handler"** are proven only against a recording double — the same class of remainder DC-2/DC-3/DC-4 each carry, and it pairs with DC-…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:222` — - **REMAINING (unobservable here, and it is the clause's own verb).** The `done_when` says the toggle "**survives reboot**". Nothing in this repo can observe that: it needs a signed `.app` bundle (DC-1), a real macOS login session, and a machine restart. Everything up to the OS boundary is driven…

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T4.2`, `Session 4`. Those tokens appear in its source plan at:

- `T4.2` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:110` — | T4.2 | Native notifications as a plan-42 target: `native` target routed to Electron Notification when the desktop shell is connected; falls back to dashboard toasts otherwise | notification target registration (plan 42's rules engine), shell handler | a rule with target `native` fires an OS not…
- `Session 4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:105` — ### Session 4 — Presence + platforms
- `Session 4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:165` — ### Session 4 — Presence (T4.1 + T4.3, 2026-08-17)
- `Session 4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:184` — ### Session 4 remainder — the orphan clause, actually measured (2026-08-25)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
