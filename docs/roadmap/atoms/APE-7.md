<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `APE-7` — Update surfacing: catalog.updates_available() + card/nav badges + kind-registered notification

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [APP-PLATFORM-EVOLUTION](../plans/APP-PLATFORM-EVOLUTION.md) (`APE`, plan status `done`) |
| **Atom table** | [`../atomic/APE.md`](../atomic/APE.md) |
| **Execution position** | #46 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment 2026-07-26 T2.4 (update-available surfacing; folds into Session 2)

## Done when

bump a local source's version -> installed-card badge + Store nav count + ONE notification; re-view -> no re-nag (dedup by name+latest_version); zero polling processes added

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:INBOX-NOTIF-UNIFICATION:kind registry + emit_attention_item (dual-honesty: plain notify before its gate is ON)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `bd83e55c8` 2026-08-08 — feat(apps): APE-7 surface installed-app updates (card badge + Store nav count + notification) (#929)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.4` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:122` — | T2.4 | `updates_available()` + badges + kind-registered notification (dedup by version; no new scheduler) | `apps/catalog.py`, `dashboard/handlers/apps.py`, Store/installed cards, kind registration | bump a local source's version → badge + one notification; re-view → no re-nag; zero polling pro…
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:79` — ### Session 2 — Quality bar + native evolution
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:118` — (a)+(c) fold into **Session 2** (they ARE the quality bar); (b) folds into **Session 3** beside `appMessaging` (one consent-copy review covers both). Session count stays 4.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
