<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DC-2` — S2: Typed capability bridge + gateway seam + Settings panel + app perm

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-CAPABILITIES](../plans/DESKTOP-CAPABILITIES.md) (`DC`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DC.md`](../atomic/DC.md) |
| **Execution position** | #149 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1286 |

## Intent — what this is for

Session 2 — Capability bridge (T2.1-T2.4, V2); Contracts C1 (preload contextBridge), C2 (gateway desktop route), C3 (app manifest desktop perm)

## Done when

window.pclawDesktop.capabilities probe/request/state works per-capability with unit tests over the state machine and contextIsolation asserted (renderer cannot reach ipc channels outside the namespace); shell POSTs a capability manifest to a new loopback /api/desktop/register (per-session shell_token; misuse -> 403 + SEL) and GET /api/desktop/state reflects availability (absent/'not connected' in a browser tab); Settings -> Security -> Desktop capabilities panel renders truthful per-capability state with request buttons; a fixture app with desktop:['native_notifications'] is enforced like api/events (missing cap -> 403 + SEL capability_denied) and shown on the install consent surface. (Owner task 2 approves the consent copy.)

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (3)

[`DC-3`](DC-3.md) (`done`), [`DC-4`](DC-4.md) (`done`), [`DC-5`](DC-5.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 52 (DC) — the Security panel driven in a browser tab; contextIsolation and the inverted updater rail both falsified

**Code evidence:**

- 🔑 ARCC'S CSRF/TOKEN GUIDANCE APPLIES TO THIS LOOPBACK REGISTRATION ENDPOINT AND ALL FOUR OF ITS ASKS ARE MET. Per-session token: `shell_token`, rotated on re-registration. Validated on receipt: every shell write path runs `_require_loopback` then `_require_shell_token`. Constant-time comparison: `hmac.compare_digest` for the local secret at `desktop.py:122` AND inside `DesktopRegistry.verify`, which short-circuits an empty token before comparing. No leakage: the register response is 'the only place the token ever appears', `_deny` 'never echoes' it, and `test_registry_never_exposes_the_token` pins that
- 🔑 IT FAILS CLOSED WHERE FAILING OPEN WOULD HAVE BEEN EASIER, AND SAYS SO: with no local secret minted the endpoint returns 503 rather than falling back to loopback-only, 'which would let any local process register'. The convenient degradation is named and refused
- 🔑 THE CHECK ORDER IS ITSELF A CONTROL: `test_non_loopback_register_is_refused_before_the_credential` — a remote caller never reaches the secret comparison, so the endpoint is not an oracle for it
- 🔑 AUDIT IS FAIL-OPEN, AUTHORIZATION IS FAIL-CLOSED, and the split is stated at the exception handler: 'audit must never change the security decision'. A broken SEL sink cannot become an outage, and cannot become a bypass either
- 🔑 FALSIFIED THE ISOLATION RAIL: flipping one `contextIsolation: true` to false in `desktop/main.js` reddened exactly one of 358 tests. Reverted; 358/358. The test's own comment is why the rail exists — 'Nothing in the runtime would fail loudly if someone [changed it]' — which is the precise definition of a regression that needs a test rather than a reviewer
- the manifest normalizer fails closed on garbage, drops unknown capability names, and `test_normalize_forces_requestable_false_when_unavailable` — so the UI cannot offer a Request button for something the OS cannot give
- test_desktop_seam + test_desktop_install_kind 37/37; desktop node suite 358/358 across 75 suites (falsified: one contextIsolation flag flipped reds exactly 1 of 358; adding electron-updater to package.json reds exactly the inverted rail)

**Driven in the UI:** 🔑 DRIVEN IN A REAL BROWSER TAB, which is the exact condition the clause names. Settings → Security → 'Desktop capabilities' renders 'Desktop app not connected' plus the reason: 'You are viewing this in a browser tab. Native capabilities — microphone, notifications, the menu-bar item — exist only while the PersonalClaw desktop app is running, so there is nothing to show or grant here.' Not an error, not an empty list of dead buttons — a stated reason.

**Notes:** 🔑 THE SECTION'S OWN COPY IS THE CAMPAIGN'S CONTAINMENT PRINCIPLE IN A NEW FORM — the authority is ELSEWHERE: 'Each one is granted by macOS, not by PersonalClaw — revoking it in System Settings takes effect immediately, and NOTHING HERE CAN GRANT ITSELF.' A permissions panel that cannot grant is a permissions panel that cannot be tricked into granting.

## Recorded history

### Commits (3)

- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `b52343f13` 2026-08-14 — test(settings): complete both Security panel mocks after DC-2 and SH-10 met
- `a6ef5e339` 2026-08-13 — feat(desktop): DC-2 expose native capabilities through one typed bridge and a loopback gateway seam

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:149` — - **REMAINING (V2 + owner task 2).** Electron was never launched during this session, so V2's on-device walk-through — real TCC dialogs, "exactly once per grant" on a device, the grant→push→panel loop through the actual shell — is unproven; the state machine's once-only property is proven by unit…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:167` — - **DECISION (extend the existing tray; move its logic OUT of the glue).** T4.1 says "verify what `Tray` wiring already exists — extend, don't duplicate", and it did exist: DC-2/DC-3 left a static six-item menu built inline in `main.js` plus a `setTitle` capture indicator. So `createTray()` is de…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:208` — - **REMAINING (one unobservable leg + a genuinely unrunnable one).** Electron was never launched, so **"macOS draws a banner"** and **"clicking it fires the `click` handler"** are proven only against a recording double — the same class of remainder DC-2/DC-3/DC-4 each carry, and it pairs with DC-…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:222` — - **REMAINING (unobservable here, and it is the clause's own verb).** The `done_when` says the toggle "**survives reboot**". Nothing in this repo can observe that: it needs a signed `.app` bundle (DC-1), a real macOS login session, and a machine restart. Everything up to the OS boundary is driven…

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `T2.1`, `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:90` — | T2.1 | Bridge API in preload (typed, namespaced, per-capability probe/request/state; deny-by-default) + main-process handlers (TCC state via `systemPreferences.getMediaAccessStatus` etc.) | `desktop/preload.js`, `desktop/main.js` (+ split `desktop/capabilities.js`), desktop tests with injected …
- `T2.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:142` — - **DONE T2.1** — `desktop/capabilities.js` (new, split from main.js): 6 capabilities x 5 grant states over injected OS handles. `probe()` never throws (an OS failure degrades to `unavailable` + reason); `request()` prompts ONLY on the `not-determined` → granted/denied transition and returns `pro…
- `T2.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:149` — - **REMAINING (V2 + owner task 2).** Electron was never launched during this session, so V2's on-device walk-through — real TCC dialogs, "exactly once per grant" on a device, the grant→push→panel loop through the actual shell — is unproven; the state machine's once-only property is proven by unit…
- `T2.4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:93` — | T2.4 | App-facing permission: manifest `desktop: [caps]` parsed + enforced (gateway mediates app calls to bridge-backed routes exactly like `api` prefixes); consent surface shows it at install | `apps/manifest.py`, `apps/permissions.py`, install consent UI | fixture app with `desktop: ["native_…
- `T2.4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:145` — - **DONE T2.4** — App-manifest `desktop: [caps]`: `Permissions` (+`to_dict`/`from_dict`) → `PermissionChecker.can_use_desktop` → the desktop handler → `AppPermissionsWire` → `PermissionList`. EXACT-match only, no `*` wildcard (the vocabulary is closed, and "everything native this host can do" is …
- `T2.4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:146` — - **DEVIATION (T2.4 first call site).** The task's "fixture app with `desktop: ["native_notifications"]` can fire one" cannot land in S2: no bridge-backed notification route exists until S4/plan-42 registers the `native` target. Enforcement therefore lands on the gateway-mediated capability READ …
- `T2.4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:149` — - **REMAINING (V2 + owner task 2).** Electron was never launched during this session, so V2's on-device walk-through — real TCC dialogs, "exactly once per grant" on a device, the grant→push→panel loop through the actual shell — is unproven; the state machine's once-only property is proven by unit…
- `T2.4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:198` — - **CONFIRMED (the audit's premise, measured before writing anything).** `native` was declared and inert on **both** sides. Backend: `notification_rules.TARGETS` carried it with the comment "accepted and persisted but inert until MOBILE-COMPANION / DESKTOP-CAPABILITIES land", and a census of ever…
- `Session 2` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:86` — ### Session 2 — Capability bridge
- `Session 2` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:138` — ### Session 2 — Capability bridge (2026-08-13)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
