<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-7` — Provider apps declare openai/anthropic pythonDependencies (apps-repo half of T1.4)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #184 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #feature-provider-sdk-deps |

## Intent — what this is for

Session 1 T1.4 cross-repo half; §2.6 app-manifest dependencies

## Done when

the 12 provider apps that build OpenAIProvider/AnthropicProvider declare the matching SDK in manifest dependencies.pythonDependencies; all 38 manifests parse + round-trip stable; per-bundle app tests green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-2`](DIST-2.md) | intra-repo | `done` |
| `EXT:PROVIDER-BOUNDARY-COMPLETION:manifest pip-step installs the declared deps into the shared venv` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `T1.4`, `§2.6`, `Session 1`. Those tokens appear in its source plan at:

- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:152` — | T1.4 | LLM-SDK demotion **(conditional on plan 32 T2.1 outcome — read its Execution log first; if pip-step unsupported, log DEVIATION and skip to T1.5)**: move `openai`, `anthropic` from `dependencies` to their existing extras; branded apps declare the dep via the verified mechanism; sharpen la…
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:219` — - **VERIFY (S1 partial)** — Built the wheel locally (`python -m build --wheel`) and inspected METADATA: 52 `Requires-Dist` (20 core + extras), 5 `Project-URL`, SPA `index.html` present in the wheel. `make lint` fully green (black/isort/flake8/mypy — 451 source files, mypy clean). `tests/test_vers…
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:224` — - **PREMISE CONFIRMED (T1.4 precondition)** — read PROVIDER-BOUNDARY-COMPLETION Execution log: T2.1 DONE — `apps/app_manager.py::_install_python_deps` (L142, called from install L365 + update L529) DOES pip-install manifest `dependencies.pythonDependencies` into the shared venv. The app pip-step …
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:225` — - **DONE T1.4 (core-repo half)** — Demoted `openai` and `anthropic` out of core `[project].dependencies`; they remain as the `[openai]` / `[anthropic]` extras. Added `src/personalclaw/_sdk_deps.py` with `require_sdk(module, extra, feature=None)` → returns the module or raises `MissingSDKError(Imp…
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:227` — - **CROSS-REPO ACTION (T1.4, apps repo `PersonalClawApps` — maintainer hand-applies)** — the branded OpenAI/Anthropic provider apps must declare their SDK so the installer pulls it. In each app's `app.json` (`manifest.json`), add under the top-level `dependencies` object (create it if absent), mi…
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:285` — - **DONE T2.2 (bootstrap `install.sh` — content produced)** — Wrote the actual bootstrap script at `deploy/website/install.sh` (staged in-repo for the website repo, plan 36): POSIX sh (`set -eu`, validated with `sh -n` AND `dash -n`), OS/arch detect, `uv` presence check → official `astral.sh/uv/i…
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:297` — | T1.4 | `71db09f` | Demote `openai`/`anthropic` to extras; `_sdk_deps.require_sdk`/`MissingSDKError` at the 4 lazy-import sites |
- `T1.4` `docs/roadmap/plans/DISTRIBUTION.md:324` — - **WHEEL RE-VERIFICATION (soul guardrail)** — After all S1–S4 code changes (T1.4 dep demotion, T4.x updater modules), rebuilt the wheel and re-ran `scripts/verify_wheel.py` end-to-end: **PASS** — SPA packaged, installs from the wheel alone into a scratch venv, `personalclaw gateway --test-mode` …
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:145` — ### Session 1 — Packaging correctness
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:213` — ### Session 1 — Packaging correctness
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:222` — ### Session 1 (cont.) — Cycle 2
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:238` — ### Session 1 (cont.) — Cycle 3

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
