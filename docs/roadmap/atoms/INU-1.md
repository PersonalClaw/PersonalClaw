<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `INU-1` — Kind registry + rules engine (S1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INBOX-NOTIFICATIONS-UNIFICATION](../plans/INBOX-NOTIFICATIONS-UNIFICATION.md) (`INU`, plan status `done`) |
| **Atom table** | [`../atomic/INU.md`](../atomic/INU.md) |
| **Execution position** | #293 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##111 |

## Intent — what this is for

Session 1 — Kind registry + rules engine (T1.1-T1.4, V1); C1/C2/C3

## Done when

notification_kinds.py registry (frozen (source,kind,label,default_mode,default_severity)) covers every notify() emitter's kind; ~10/26 emitters migrated to typed constants (no bare-string kinds); notification_rules.py load/validate + resolve_rule with fail-open defaults + evaluation wired into notify() as THE delivery path (never/badge/immediate/digest); digest queue writer with 2x trim; guarded GET/PUT /api/notifications/rules; regression test proves absent rules file reproduces today's behavior for every kind

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (5)

[`INU-2`](INU-2.md) (`done`), [`INU-3`](INU-3.md) (`done`), [`INU-4`](INU-4.md) (`done`), [`INU-5`](INU-5.md) (`done`), [`INU-6`](INU-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T1.1`, `T1.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:97` — Every one of the ~10 `notify()` emitters passes a registered `(source, kind)`. The inventory of current kinds is built in T1.1 (grep + Execution-log table) so none is missed.
- `T1.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:168` — | T1.1 | `notification_kinds.py`: registry dataclass + registrations covering every existing `notify()` call site's kind (enumerate by grep; record the inventory in the Execution log); constants exported | create `src/personalclaw/notification_kinds.py`, tests | every current emitter's kind strin…
- `T1.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:301` — - 2026-07-30 — **DONE (Session 1: T1.1–T1.4).** Typed kind registry
- `T1.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:320` — **T1.1 inventory (26 call sites, 11 flat kinds).** Built by AST walk, not grep — **two
- `T1.4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:171` — | T1.4 | Digest queue writer (append-only JSONL, trim at 2× cap) | `notification_rules.py` | queue writes covered by test; trim proven at the boundary |
- `T1.4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:301` — - 2026-07-30 — **DONE (Session 1: T1.1–T1.4).** Typed kind registry
- `T1.4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:308` — legacy path, byte-identical notes)", T1.4 for a `lifecycle/gates.py` registration, and S5's
- `Session 1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:164` — ### Session 1 — Kind registry + rules engine (the rules path replaces the ad-hoc one)
- `Session 1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:301` — - 2026-07-30 — **DONE (Session 1: T1.1–T1.4).** Typed kind registry

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
