<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `ET-1` — `personalclaw app new` scaffold: provider-registry-derived type table + per-type generators + conformance test

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ECOSYSTEM-TOOLING](../plans/ECOSYSTEM-TOOLING.md) (`ET`, plan status `in_progress`) |
| **Atom table** | [`../atomic/ET.md`](../atomic/ET.md) |
| **Execution position** | #258 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1656 |

## Intent — what this is for

Session 1 (T1.1, T1.2, T1.3, V1); C1 — Scaffold (src/personalclaw/cli_app_new.py, wired via §3.10 subparser)

## Done when

`personalclaw app new --list-types` prints a type table derived at runtime from the provider registry (adding an upstream capability type appears without editing the generator); for EVERY type, generate→`pytest <dir>` passes→local-source install succeeds→provider registers (scripted test loop); each type emits valid app.json (incl. plan-32 cli.*/loggerRoots), a minimal ABC-implementing provider stub, passing stub-based test_provider.py, README, MIT LICENSE; tests/test_app_scaffold.py generates each type into tmp and runs apps-repo checks so scaffold drift = red; V1 timed stranger-shaped `search`-app run recorded (<30 min target)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:PROVIDER-BOUNDARY-COMPLETION:manifest cli.*/loggerRoots fields the scaffold emits` | cross-plan (EXT) | **unresolved** |
| `EXT:PLATFORM-LEGIBILITY:manifest self-description enabling the runtime-derived type table` | cross-plan (EXT) | **unresolved** |
| `EXT:CI-RELEASE-ENGINEERING:apps-repo CI the generated output must pass as-generated` | cross-plan (EXT) | **unresolved** |

### Unblocks (3)

[`ET-2`](ET-2.md) (`done`), [`ET-6`](ET-6.md) (`done`), [`ET-7`](ET-7.md) (`blocked`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 61 (ET) — the scaffold type table run live; the registry sync run live, which reversed a finding

**Code evidence:**

- 🔑 I RAN IT: `personalclaw app new --list-types` prints `Provider types (19) — derived at runtime from the provider registry`, with each type's SDK contract beside it and duck-typed stubs marked as such rather than silently listed
- 🔑 THE ANTI-DRIFT PROPERTY IS THE WHOLE POINT AND IT IS STRUCTURAL: the table is derived from the registry, so 'adding an upstream capability type appears without editing the generator'. A hand-maintained type list is the standard way a scaffold falls one capability behind the platform
- the table also reports how many providers of each type are REGISTERED, so a reader can tell an available type from a used one — zero across the board on a bare core, which is the correct answer here
- 174 tests green across the scaffold and registry-validation suites
- app-scaffold + registry-validation suites 174/174; `app new --list-types` prints 19 provider types live; the site's registry sync run live returns `registry absent-at-pin, 0 listing(s)`

**Driven in the UI:** Not a browser surface: a CLI table, which was run rather than read.

**Notes:** This is the atom the CHANNEL-EXPANSION audit already leaned on — its `channel` template needed no registration because the type table is derived. Two plans' worth of correctness comes from one design decision.

## Recorded history

### Commits (5)

- `281d693b1` 2026-09-06 — docs(roadmap): execute the 2026-08-28 cycle ruling, and mint the atom for a seam nobody adopted
- `d81bbb8cf` 2026-09-06 — docs(ecosystem): ship the app bounty wants-list, machine-checked
- `b2578e62c` 2026-08-18 — chore(roadmap): cite the landing batch #1656 on EIAT-3, WF2LEA-10 and ET-1
- `c0178228a` 2026-08-17 — chore(roadmap): mark EIAT-3, WF2LEA-10 and ET-1 done with their findings
- `e26ba3055` 2026-08-17 — feat(cli): ET-1 personalclaw app new scaffold with registry-derived types

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:602` — was `ET-7`'s own output (circular), and the scaffold half shipped from `ET-1`, which `ET-7` already
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:607` — the scaffold is `ET-1`'s, and the wants-list itself landed under ECOSYSTEM-TOOLING's own
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:208` — `app_cli.py`. Step 1 printed **19** types, not the 18 in ET-1's entry above — the quickstart
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:229` — - **2026-08-17 — DONE (`ET-1`): `personalclaw app new` scaffold with a registry-derived type table** (#1553).
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:245` — - **2026-08-17 — V1 + DISCOVERY (`ET-1`): the generated README's own install snippet did not work.**
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:255` — section was created with `ET-1`'s entry rather than the entry being filed elsewhere.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.1`, `T1.2`, `T1.3`, `§3.10`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:68` — | T1.1 | Type table derived from the provider registry (enumerate capability types + their ABC/module mapping programmatically; record the mapping source in the Execution log) | `src/personalclaw/cli_app_new.py` (new), wired via `cli.py` subparser pattern | `personalclaw app new --list-types` pri…
- `T1.2` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:69` — | T1.2 | Generators per type: manifest (+plan-32 fields), provider stub implementing the type's contract minimally, passing stub-based `test_provider.py`, README, LICENSE | `cli_app_new.py` + `src/personalclaw/templates/app/` data files | for EVERY type: generate → `pytest <dir>` passes → local-s…
- `T1.3` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:70` — | T1.3 | Generated-output CI conformance: a core test generates each type into tmp and runs the apps-repo checks (manifest validate, sdk-boundary, tests) against it | `tests/test_app_scaffold.py` | scaffold drift = red test |
- `§3.10` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:39` — ### C1 — Scaffold (`src/personalclaw/cli_app_new.py`, wired via §3.10 subparser)
- `Session 1` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:64` — ### Session 1 — Scaffold + template

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
