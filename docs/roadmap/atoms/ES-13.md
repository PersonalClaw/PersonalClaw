<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `ES-13` — Trust record: per-scope trust ledger feeding autonomy.py rung decisions

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EVALUATION-SUBSTRATE](../plans/EVALUATION-SUBSTRATE.md) (`ES`, plan status `done`) |
| **Atom table** | [`../atomic/ES.md`](../atomic/ES.md) |
| **Execution position** | #252 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§4.2 trust record (decomposed from ES-8 per owner ruling 2026-08-27 Q2/11); size M

## Done when

a per-scope trust record is persisted (one record per template/scope) capturing the inputs guardrails/autonomy.py's resolve_rung already consumes; resolve_rung reads the record when deciding a scope's rung; NO second maturity/L3 rung vocabulary is minted (a rail asserts guardrails/autonomy.py is the only rung dialect); the record round-trips to disk and survives restart

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`ES-1`](ES-1.md) | intra-repo | `done` |
| [`ES-5`](ES-5.md) | intra-repo | `done` |
| [`ES-7`](ES-7.md) | intra-repo | `done` |

### Unblocks (2)

[`ES-14`](ES-14.md) (`done`), [`ES-16`](ES-16.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 16 (ES) — DRIVEN in a real browser

**Code evidence:**

- guardrails/trust_record.py:1-22 — one JSON record per action-type scope capturing exactly what resolve_rung already consumes, written through from grant_rung/demote and read back on every decision
- autonomy.py:58 imports it, :550 resolve_rung consults is_revoked and clamps to the floor 'even if the store lost its demotion entry', :842 record_grant, :878 record_demotion — a live read on the decision path, not a write-only ledger
- THE RAIL IS A REAL RAIL: tests/test_guardrails_trust_record.py:150 test_module_mints_no_rung_vocabulary inspects the module SOURCE for foreign rung words and asserts the validator accepts exactly autonomy.RUNGS while REJECTING 'unattended'
- fail-safe reads have their own four tests: unknown rung on disk, corrupt record, key mismatch, absent record — each reads as absent rather than licensing anything

**Notes:** The direction asymmetry is stated and correct: 'Write failures never block a grant (the grant is the user's decision; the ledger is its receipt). Read failures never license anything.' That is the fail-open/fail-closed convention applied per direction rather than per module, which is the harder and right version.

## Recorded history

### Commits (4)

- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `01fae0425` 2026-09-04 — feat(workflows): ES-16 human-attention accounting with decay and demotion signal
- `00eda6a29` 2026-09-04 — feat(guardrails): ES-13 per-scope trust record feeding rung decisions
- `b7c7ad574` 2026-09-04 — docs(roadmap): flip MC-7/DC-3/DC-4/APE-6 + ES-8 decomposition

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1929` — ## Execution log — ES-8 (trust-graduation ladder) — decomposed into ES-13..16
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1931` — - **[2026-09-04] ES-8 done-by-decomposition per owner ruling 2026-08-27 (question 2 of 11).** The ruling narrowed the trust-graduation gate to inputs that exist — ONE rung vocabulary, `guardrails/autonomy.py` (`rung_rank`/`rung_state`/`grant_rung`/`resolve_rung`), rather than minting a second mat…
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1933` — ## Execution log — ES-13 (Trust record: per-scope trust ledger feeding autonomy.py rung decisions)
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1935` — - [2026-09-04][ES-13] LANDED on main; atom flipped ✅. Merged via core PR #2416 (merge commit 00eda6a29). `guardrails/trust_record.py` persists one durable JSON record per action-type scope under `~/.personalclaw/evals/trust/`: `grant_rung` writes the grant receipt (rung, grant time, evidence wind…
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:510` — catalog's id grammar is numeric (`[A-Z0-9]+-\d+` — the same reason ES-8 decomposed to ES-13..16).

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `§4.2`. Those tokens appear in its source plan at:

- `§4.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:400` — - **Loop 3 — live quality (field):** derived metrics, computed by query, stored nowhere new (the §4.4 discipline): per-template/per-action-type 👍/👎 rate + edit-before-approve rate from plan 58's FEEDBACK-SIGNAL store, plus approval/rejection/undo rates from the earned-autonomy ledger. Rendered BE…
- `§4.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:410` — | E3 | Loop 3 derived field metrics (plan-58 records + autonomy ledger + SEL outcomes, query-computed) rendered beside lab results on the Learning tab; `lab_field_divergence` flag feeding §4.2 demotion (extends Session 4; gated on plan 58 S1) | ledger query module, Learning-page tab, trust-record…
- `§4.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1931` — - **[2026-09-04] ES-8 done-by-decomposition per owner ruling 2026-08-27 (question 2 of 11).** The ruling narrowed the trust-graduation gate to inputs that exist — ONE rung vocabulary, `guardrails/autonomy.py` (`rung_rank`/`rung_state`/`grant_rung`/`resolve_rung`), rather than minting a second mat…
- `§4.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1947` — - [2026-09-05][ES-15] LANDED on main; atom flipped ✅. Merged via core PR #2453 (merge commit a96e7776f). The revocation half of the §4.4 ladder — the promotion half already shipped (`propose_promotions` files the graduation proposal on sustained success; a human click grants). This lands the fail…
- `§4.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1951` — - **[2026-09-05] IMPLEMENTATION LANDED on a feature branch; atom stays ⬜ for the owner's dag.json flip.** `evals/field_metrics.py` is E3's ledger query module: one `SubjectRow` per subject — lab score (Loop 1: the newest `results.tsv` `template_ab` row, pin and all) | gate status (Loop 2: the new…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
