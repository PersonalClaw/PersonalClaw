<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2TAS-10` — Lease write path + confirmation resolve endpoint + task-projection events on both channels

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-TASKS-SOPS](../plans/WORKFLOWS-V2-TASKS-SOPS.md) (`WF2TAS`, plan status `done`) |
| **Atom table** | [`../atomic/WF2TAS.md`](../atomic/WF2TAS.md) |
| **Execution position** | #632 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§5 TTL'd lease claims write path (R10); §4 ConfirmationRequest resolve (R6); §7/§8 items 4/11 engine events + FE stream union

## Done when

sidecar lease file with single_flight CAS (0 multi-winner across 8 processes); POST /runs/{id}/confirm riding resume_run with verb→boolean; five ledger kinds + SSE events (task_materialized/confirmation_pending/confirmation_resolved/task_verified/cascade_blocked) with FE WORKFLOW_LIFECYCLE union guard tests (S61d + S61e, 34+20 py + 6 FE tests)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2TAS-3`](WF2TAS-3.md) | intra-repo | `done` |
| [`WF2TAS-6`](WF2TAS-6.md) | intra-repo | `done` |

### Unblocks (1)

[`WF2TAS-11`](WF2TAS-11.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 7 (WF2TAS) — observed

**Code evidence:**

- tests/test_workflows_task_write.py and tests/test_tasks_api.py pass (part of the 392-passed run)

**Notes:** PARTIAL: the lease write path and the confirmation-resolve endpoint are engine seams. The task-projection half is the same one WF2TAS-1 leaves open for want of a run.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (34 line(s))

This atom's scope cites `§5`, `§4`, `§7`, `§8`, `R10`, `R6`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:35` — - **TASK-R10** → §5 Frontier/Next Projections, Evented Unblock, TTL'd Leases
- `§5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:42` — - **TASK-R17** → §1/§5 Cascade-Fail Propagation
- `§5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:99` — - **Stuck-work diagnostics sweep**: a periodic pass flags `in_progress` without node heartbeat >20min, `open`-and-ready unclaimed >1h, and done-without-evidence — surfaced as a Tasks-board strip and as Run Ledger findings (WORKFLOWS-V2 §5). The sweep also auto-releases expired leases (§5).
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:31` — - **TASK-R6** → §4 ConfirmationRequest (one durable typed record, atomic single-use resolution, require_hitl)
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:34` — - **TASK-R9** → §4 Guardrails & Postconditions (Stop rules, non_negotiable_rules, posture, regression-appendix loop — propose-only)
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:38` — - **TASK-R13** → §4 Per-Stage Mute, Observed Tool Profiles, Step-Scoped Approval Memory
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:51` — 5. The FE DagView node-level **Approve/Deny (`onApprove`/`onDeny` + `awaiting` state) is a declared, UNWIRED extension point** (`web/src/pages/tasks/DagView.tsx`) — §4's ConfirmationRequest is the missing backend seam and §7 wires it.
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:76` — | `waiting` (gate / needs input) | `blocked` | `needs_input` | Routed to the needs-input inbox (§4 ConfirmationRequest); >24h lingering → re-notify via `state.notify()` and flip `blocked_kind` to `escalated` |
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1008` — boolean `True`, matching §4's `require_hitl` rule.
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1245` — tell a reject from an expiry auto-reject — the exact distinction §4's per-type expiry policy exists
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1423` — text. §4 gives a destructive confirmation a different expiry policy (auto-reject) and forbids muting
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:40` — - **TASK-R15** → §7 Surfacing UX (composer chips, validated deep-links)
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:51` — 5. The FE DagView node-level **Approve/Deny (`onApprove`/`onDeny` + `awaiting` state) is a declared, UNWIRED extension point** (`web/src/pages/tasks/DagView.tsx`) — §4's ConfirmationRequest is the missing backend seam and §7 wires it.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:409` — - One entity means ONE inbox surface (dashboard ActionCenter / "Needs you") and ONE autonomy policy instead of three bespoke flows — and it is exactly the backend seam the FE DagView's unwired `onApprove`/`onDeny` extension point has been waiting for (§7).
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:569` — - **Session 3 — ConfirmationRequest + gates:** the unified durable record with atomic single-use resolution + auto-resume; `require_hitl`; needs-input queue semantics; DagView Approve/Deny wiring; guardrails/postconditions def sections + verbatim injection; per-stage mute + step-scoped approval m…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:797` — `onApprove`/`onDeny` is the declared extension point this session supplies the backend for (§7 wires
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:911` — surfaces to §7/session 7, and the fields land with the def-model session. The semantic channel
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:980` — belong with §7's wiring session; the decision rules they implement are complete and tested here.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1092` — The frontend half of §7's surfacing UX, driven against a live gateway rather than asserted.
- `§8` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:32` — - **TASK-R7** → §2 Hand-Off Edges + §8 Seed Template Library
- `§8` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:100` — - **Clean-exit checklist** (build passes / tests pass / progress recorded / no stale artifacts / startup works) ships as a bundled checklist template (§8 item 8).
- `§8` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:730` — template belongs with the bundled-template work in §8; the granularity lint belongs with SOP
- `§8` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1494` — **The config four points — plus the fifth nobody writes down.** §8 names four `WorkflowsConfig`
- `§8` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1504` — **DEVIATION — `match_threshold` was NOT re-added.** §8's recon (correction #1) states it exists at
- `R10` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:35` — - **TASK-R10** → §5 Frontier/Next Projections, Evented Unblock, TTL'd Leases
- `R10` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:157` — Every engine mutation of a materialized Task emits a state-change signal so the FE board updates without polling drift — **adapted to PClaw's real live-channel architecture** (recon: fe-surfaces): dashboard WS envelopes are refetch SIGNALS, not payloads, so the engine emits a `tasks`-kind refresh…
- `R10` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:161` — Each WorkflowRun auto-provisions one TaskList (named `"{workflow_name} run #{run_id[:6]}"`). All materialized tasks land in this list. The existing Tasks UI scoped by project shows them naturally. TaskLists support **shared handles**: workflow runs AND ad-hoc sessions can attach to the same list …
- `R10` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:457` — ### Frontier/Next Projections, Evented Unblock, Leases (R10)
- `R10` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:572` — - **Session 6 — Pool + templates:** frontier/next projections + evented unblock + TTL'd leases + write-time acyclicity; task lifecycle events on the hook bus; seed template library (franklioxygen imports, audit-and-file triage flavor, clean-exit checklist) + hand-off edges; blueprint sessions (R1…
- `R6` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:31` — - **TASK-R6** → §4 ConfirmationRequest (one durable typed record, atomic single-use resolution, require_hitl)
- `R6` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:383` — ### ConfirmationRequest: ONE Durable Typed Record (R6)
- `R6` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:503` — - **DagView Approve/Deny gets wired** (R6): the declared-but-unwired `onApprove`/`onDeny` + `awaiting` node state in `web/src/pages/tasks/DagView.tsx` binds to ConfirmationRequest resolve endpoints — gate nodes render `awaiting` (already pulse-styled), approve/deny resolves the record and the run…
- `R6` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:566` — - **7 sessions** (after Workflows v2 Slices 0-4) — was 3; the approved high-priority set (R1/R2/R3/R4/R5/R6) adds ~1 session, the medium/low set adds ~3 more.
- `R6` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:569` — - **Session 3 — ConfirmationRequest + gates:** the unified durable record with atomic single-use resolution + auto-resume; `require_hitl`; needs-input queue semantics; DagView Approve/Deny wiring; guardrails/postconditions def sections + verbatim injection; per-stage mute + step-scoped approval m…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
