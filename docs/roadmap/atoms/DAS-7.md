<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DAS-7` — Conflict handling — records + review queue + propose-only LLM merge (Session 3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DURABILITY-AND-SYNC](../plans/DURABILITY-AND-SYNC.md) (`DAS`, plan status `done`) |
| **Atom table** | [`../atomic/DAS.md`](../atomic/DAS.md) |
| **Execution position** | #145 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1375 |

## Intent — what this is for

§4.2 Conflict handling; §7 (no auto-applied LLM merges); Implementation Order Session 3

## Done when

sha-divergence on the same entity id with both sides edited since the common ancestor (ancestor sha in registry) produces a conflict record in a review queue; a background one_shot_completion(use_case="background") pass drafts a proposed merge + rationale surfaced as needs-review, never auto-applied; local version stays authoritative and both versions persist in the shared store until resolved; memory-domain conflicts route to the memory review surface and knowledge-domain to the knowledge UI; criterion 5 met (offline same-task edit yields a conflict-review item, applies nothing until accepted)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DAS-6`](DAS-6.md) | intra-repo | `done` |

### Unblocks (1)

[`DAS-10`](DAS-10.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 12 (DAS) — observed

**Code evidence:**

- tests/test_durability_conflicts.py and tests/test_durability_conflict_review.py pass (part of the 561-passed run)
- tests/test_durability_error_copy.py also passes — a rail on the wording of the errors, not just their occurrence

**Notes:** The propose-only posture is the safety property here (a conflict becomes a reviewable record rather than an automatic overwrite), and it has both a records rail and a review-queue rail. The separate error-copy rail is unusual and good: it means the message a user reads during a conflict is itself under test.

## Recorded history

### Commits (1)

- `5d882eaca` 2026-08-15 — feat(durability): DAS-7 sync conflict review queue + proposed merge

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1467` — atoms (DAS-7+/DAS-9), not part of the Session-3 sync program this completes. **Gates:** `make lint`
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1590` — no screen, so DAS-7's queue is still unreadable by a user; criterion 9's *second* half (memory and

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `§4.2`, `§7`, `Session 3`. Those tokens appear in its source plan at:

- `§4.2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1595` — ## Execution log — DAS-10b (§4.2 conflict review queue + §4.3 sync config FE) — closes DAS-10
- `§7` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:28` — - **NEW-4** — durable state backup + multi-machine sync core (JSONL shards + SHA manifest + validate; pull→merge-import→export-union→push; boot-started scheduled self-backup with rolling backups + restore endpoints; git-versioned memory snapshots; encrypted-intents crypto for machine exchange) → …
- `Session 3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:238` — - **Session 3 — Sync core + `git-sync` + `dir-sync`.** `sync` type (manifest + handler together), domain registry, `sdk/sync.py`; pull→merge-import→export-union→push with the registry/CAS, staleness window, outbox + cursor rules; deterministic merges + tombstones; conflict records + propose-only …
- `Session 3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:910` — is Session 3's `pull → merge-import → export-union → push`, not a gap in this one; noted here so it
- `Session 3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1440` — criterion-4 over a real git repo/folder — the last piece of Session 3). **Gates:** `make lint`

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
