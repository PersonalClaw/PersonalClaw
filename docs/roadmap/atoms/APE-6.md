<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `APE-6` — Migrate Minutes + Growth backend+UI apps to the current design system

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [APP-PLATFORM-EVOLUTION](../plans/APP-PLATFORM-EVOLUTION.md) (`APE`, plan status `done`) |
| **Atom table** | [`../atomic/APE.md`](../atomic/APE.md) |
| **Execution position** | #51 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL 2026-08-26 (PersonalClawApps#54, rebase-merged f3963408) - BOTH apps are now migrated and token-lint is 0 for each (growth 0; minutes 9 -> 0, with all six copied style constants deleted, spacing/radius tokenized, type moved to the data-type role layer, and every prop passed to Button/Surface audited against their real signatures - 9 and 2 used, ZERO unknown, which matters because neither forwards ...rest). Minutes now declares quality {tested:true, designSystem:'v2', a11y:false}, so APE-4's verifier re-checks its designSystem claim on every push. THE ONE UNMET CLAUSE is the done_when's 'look native (SCREENSHOT check)': neither minutes/ui nor growth/ui has node_modules, so no bundle can be built offline and the host cannot mount either page for a visual comparison. The committed rail covers this as far as static evidence can - test_built_bundle_imports_only_specifiers_the_host_resolves checks the real artifact when one exists and asserts its own absence otherwise rather than silently passing. Clears with npm ci in both ui/ dirs plus a gateway run with PERSONALCLAW_FIRST_PARTY_APPS_DIR pointed at the apps clone. |

## Intent — what this is for

Session 2 T2.3 (apps repo minutes/ui, growth/ui)

## Done when

both apps pass token-lint and look native (screenshot check) using tokens + shell primitives via the UI SDK

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`APE-11`](APE-11.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `b7c7ad574` 2026-09-04 — docs(roadmap): flip MC-7/DC-3/DC-4/APE-6 + ES-8 decomposition
- `aa53b1655` 2026-08-26 — docs(roadmap): AS-7 done via the apps rails batch; APE-6 records the screenshot remainder
- `90b51ad38` 2026-08-26 — docs(roadmap): APE-4 done via the apps quality batch; APE-6 + AS-7 recorded PARTIAL

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:152` — - [2026-09-04][S2 · atom `APE-6`] **DONE (cross-lane; evidence PersonalClawApps#54) — Minutes + Growth migrated to the design system.** Both apps render on `@personalclaw/app-sdk/ui` primitives + tokens with token-lint 0 each (growth 0; minutes 9 → 0 — six copied style constants deleted, spacing/…
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:269` — the honest value for both is `"legacy"` until `APE-6` lands; that is the atom's real-world red, not just
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:721` — Flipping APE-11 does NOT complete this plan — `APE-4` and `APE-6` remain `todo`.
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:891` — - [2026-08-26][APE-6] **PARTIAL — stays `todo`.** The criterion says **both** apps; only **Growth** was

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:85` — | T2.3 | Migrate Minutes + Growth backend+UI apps to the current design system (tokens + shell primitives via UI SDK) — coordinates with plan 51 | apps repo: `minutes/ui`, `growth/ui` | both apps pass the token-lint + look native (screenshot check) |
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:79` — ### Session 2 — Quality bar + native evolution
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:118` — (a)+(c) fold into **Session 2** (they ARE the quality bar); (b) folds into **Session 3** beside `appMessaging` (one consent-copy review covers both). Session count stays 4.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
