<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-13` — Similarity edges: write-time kNN over chunk vectors, item-pair rollup, canonical ordering + chunk-pair provenance

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #315 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1822 |

## Intent — what this is for

Capability-gap amendment (2026-08-19)

## Done when

an item-similarity edge table exists with UNIQUE(source_item_id,target_item_id), ON DELETE CASCADE on both legs so edge cleanup on item delete is schema-enforced, a similarity score, and the winning source/target chunk indices stored as provenance so an edge can explain itself; after an item's chunks embed, a kNN pass over the existing chunk vector index fetches a multiple of top-K candidates per chunk, filters at a configured cosine floor, collapses chunk pairs to item pairs keeping the MAX (the same roll-up rule the vector arm already uses), truncates to top-K per item and upserts with canonical (min,max) id ordering; recomputing one item must NOT delete an edge another item's pass created — a union or canonical-source-only delete, proven by a test that recomputes A and asserts an edge B-A survives; a GLOBAL per-item degree cap is enforced, not merely a per-pass truncation, so inbound edges cannot accumulate unbounded; the related-items endpoint is served from this table with a real threshold, replacing the unthresholded shared-entity COUNT it uses today; the edge pass runs on the KL-14 maintenance host, never inline on the write path; the cosine floor, top-K and degree cap round-trip through config

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-11`](KL-11.md) | intra-repo | `done` |
| [`KL-14`](KL-14.md) | intra-repo | `done` |

### Unblocks (1)

[`KL-17`](KL-17.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 5 (KL) — observed

**Code evidence:**

- covered by the 234-passed KL run

**Notes:** PARTIAL: write-time kNN over chunk vectors needs both vectors and more than one item. Blocked twice over.

## Recorded history

### Commits (3)

- `2c21d2056` 2026-08-20 — docs(roadmap): cite the landing PR for KL-13
- `583e643fb` 2026-08-20 — feat(knowledge): KL-13 similarity edges on the maintenance host
- `0377b537e` 2026-08-19 — docs(roadmap): add capability-gap amendment atoms to KL, WF2KNO and LMMV

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1039` — **Two facts for whoever takes KL-13 (which this unblocks once clause 7 closes):** `source_seen`
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1217` — **Two traps for whoever takes KL-13 (now unblocked and in the ready frontier):**
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1223` — - [2026-08-20][KL-13] **DONE — similarity edges, on KL-14's host, behind a real floor.** Unblocked by

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
