<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-6` — Turn-level surface — tokens + cost on the 'Turn complete' line

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #102 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2(a) turn-level; C3 — Surfaces (Turn line); Session 2 T2.1. NOTE: cache fragment is the optional PROMPT-CACHE-SUBSTRATE seam — renders only when cache tokens are non-zero, absent otherwise (non-blocking, degrades gracefully)

## Done when

The existing 'Turn complete' render shows real USD + in/out tokens for a priced turn; shows 'unpriced' (never $0.00) for a model with no price row; the cache fragment renders only when cache tokens are non-zero (absent when PROMPT-CACHE hasn't landed); backend line composer + FE renderer both updated; tests green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-2`](CATO-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:221` — (CATO-5) + surfaces (CATO-6..8) remain, so no user-visible readout yet → no CHANGELOG. **Gates:**
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:236` — session header / Usage panel = CATO-6..8) are later atoms, so no user-visible readout yet → no
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:241` — - **CATO-6 DONE — the turn-complete cost readout (S2, first user-visible surface).** The existing
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:290` — read API (CATO-5) → turn / session / account surfaces (CATO-6/7/8). Every soul guardrail held (one

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:142` — | T2.1 | Turn-line extension: tokens + cost on the existing "Turn complete" render; "unpriced" instead of `$0.00`; cache fragment rendered only when cache tokens are non-zero | `dashboard/chat_runner.py` (the line's composer) + the FE renderer, tests | a priced turn shows real USD; an unpriced tu…
- `Session 2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:138` — ### Session 2 — The surfaces

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
