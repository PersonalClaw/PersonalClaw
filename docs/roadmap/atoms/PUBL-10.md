<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PUBL-10` — Publicize once the DISCOVERABILITY-LAUNCH gate is green

| | |
|---|---|
| **Status** | `blocked` |
| **Plan** | [PUBLICATION](../plans/PUBLICATION.md) (`PUBL`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PUBL.md`](../atomic/PUBL.md) |
| **Execution position** | #506 of 684 in dependency order |
| **Startability** | not startable |
| **Blocked reason** | OWNER-ONLY, and it was never ext-gated (correction 2026-09-07). Its own scope already said 'Status line (owner call)'. The done_when is 'public launch announced' — a Show HN / r/selfhosted / r/LocalLLaMA post — and DL-11 (Owner: P0 launch-gate clearance sign-off before any public posting) is the real gate. Re-pointed there; the ext ref previously mis-resolved to DL-8, which shipped none of the three things its label named. ALL THREE NAMED PREREQUISITES ARE SATISFIED AND WERE VERIFIED LIVE RATHER THAN CITED (2026-09-06/07): (1) SITE LIVE — curl -I https://personalclaw.dev/ returns HTTP/2 200, server: Vercel, 42460 bytes. Shipped by DL-1, whose own done_when says 'over HTTPS via Pages'; it is on Vercel now, so the clause is met by a different mechanism than the one written down. (2) REAL SCREENSHOTS — /_astro/chat.CfK76HXe_5Kjsb.webp is 1600x1000 and was rendered and inspected, not just fetched: a genuine dashboard with a lived-in multi-turn thread, a tool-use disclosure ('Worked through 4 steps · knowledge_search, knowledge_get') and real UUIDs. Not a mockup. Shipped by DL-3 / PUBL-4 under marketing/screenshots.manifest.json. (3) INSTALL ONE-LINER WORKING — actually executed: curl -fsSL https://personalclaw.dev/install | sh with UV_TOOL_DIR/UV_TOOL_BIN_DIR redirected to a throwaway dir installed the tool and `personalclaw --version` reported personalclaw 0.1.3; the served bytes are SHA-identical to the site repo's public/install. Shipped by DISTRIBUTION. RECORDED CAVEAT for anyone auditing through this row: DL-1 is done with NO evidence field at all — absent, not empty — so two of these verdicts route through an unevidenced atom. Both were established against code and live output instead of against DL-1's record. See also #2560: the install one-liner has no CI rail at all, and core's staged copy has already drifted from the bytes we serve, so 'verified working' currently rests on a manual run. |

## Intent — what this is for

Session 1 §9; Status line (owner call)

## Done when

public launch announced after DISCOVERABILITY-LAUNCH S1-3 gate is met — marketing site live, real screenshots, install one-liner working

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PUBL-6`](PUBL-6.md) | intra-repo | `done` |
| `EXT:DISCOVERABILITY-LAUNCH:P0 launch-gate clearance sign-off before any public posting (owner)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `ccb4541b8` 2026-09-06 — docs(roadmap): flip EI-6, and fix four gates that were wrong about why they were gated

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:384` — (`CA-9`, `CE-9`, `DCU-3`, `DL-7`, `EI-2`, `ET-7`, `LV-7`, `PEP-3`, `PEP-11`, `PUBL-10`,

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `§9`, `Session 1`. Those tokens appear in its source plan at:

- `Session 1` `docs/roadmap/plans/PUBLICATION.md:55` — ## Session 1 — Release (manual, ~1 session)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
