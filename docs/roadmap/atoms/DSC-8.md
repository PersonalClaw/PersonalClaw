<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-8` — Raw-button consolidation + shared-primitive extraction (SquareIconButton, GraphZoomControls, FormFooter, AddItemButton, QuietButton, SelectionPill, InlineError, Centered, VariableRow) + mega-page triage (T2.2/T2.3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #209 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 T2.2/T2.3; Execution log cy4, cy16, cy37–cy50 (item-1 mega-page per-role triage; migrate-the-idiom-not-the-page)

## Done when

raw-button count 420→~314 via byte-identical primitive extraction (SquareIconButton +danger tone, GraphZoomControls, VariableRow, FormFooter, AddItemButton, QuietButton, SelectionPill, InlineError, Centered) and per-role mega-page triage (labelled→Button, round→IconButton, dense-square→SquareIconButton; semantic rows stay raw); each ratcheted down in lockstep; visual/live-validated; pattern-gallery entries added.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-3`](DSC-3.md) | intra-repo | `done` |
| [`DSC-2`](DSC-2.md) | intra-repo | `done` |

### Unblocks (3)

[`DSC-10`](DSC-10.md) (`done`), [`DSC-12`](DSC-12.md) (`done`), [`DSC-9`](DSC-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 10 (DSC) — first-hand, from working inside it

**Code evidence:**

- part of the 1636-passed ui run
- FIRST-HAND: the raw-button consolidation is the family my silent-toggle work sat in. I measured its live population element-scoped — 67 toggles, 58 announcing, 7 name-flips, 2 genuinely silent — and closed the two real ones (ContentSurface's Export menu, and the Nudge disclosures in LoopCockpitPage and DesignCockpitPage)
- the shared-primitive extraction is observable in the same work: HeaderControl, IconButton, SquareIconButton, FilterChip and TileButton all take a state prop and bind it to aria-pressed, with SquareIconButton and HeaderControl deferring to ariaExpanded when present

**Notes:** Confirmed with a caveat I should state because it cuts against me: the shared primitives are real and the consolidation holds, but the CENSUS that measured it was wrong about 6 of its 7 named entries when I found it — it used a fixed byte-window instead of reading the element, so four announcing call sites scored as silent and both real defects were in files it never named. I fixed the census as part of that work (brace-depth element parsing). So DSC-8's consolidation is confirmed; the measurement that reported on it needed repair, and the repair is in.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (37 line(s))

This atom's scope cites `T2.2`, `T2.3`, `Session 2`, `cy4`, `cy16`, `cy37`, `cy50`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:69` — | T2.2 | Migrate bespoke chrome → shell primitives; consolidate duplicate components into the canonical primitive | outlier pages, `web/src/ui/` | duplicates removed; pages use shared primitives; suite + build green |
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:118` — - **DONE (T2.2, pure-conformance consolidation — no visual change):** the verbatim-duplicated unread accent rail (`boxShadow: inset 2px 0 0 0 ${tone}`) and tinted icon-chip background (`color-mix(in srgb, ${tone} 16%, transparent)`) lived in both `NotificationsPage.tsx` (Row, 3 sites) and `Notifi…
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:193` — - **DONE (T2.2 — CodeCockpit button wave 1):** migrated the highest-risk mega-page's raw chrome to `Button`/`IconButton`, **51→23 raw** `<button>`. Closed real gaps the migration surfaced in the primitives themselves (`Button.tsx`, `IconButton.tsx`) so the page's variants map onto the canonical A…
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:194` — - **DONE (T2.2 — ChatPage button wave 1):** `ChatPage.tsx` **34→19 raw** `<button>`.
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:390` — ### 2026-07-24 — cycle 39 (T2.2: extract the duplicated `GraphZoomControls` overlay — byte-identical, autonomous) — no taste call
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:395` — - **Evidence:** typecheck clean (no orphaned imports); vitest **122/122** (incl. `primitiveAdoption.test.ts` against 344 + the 2 new GraphZoomControls tests); build green; visual **64/64 with ZERO baseline changes** (both graph overlays are gated behind non-empty data — `nodes.length === 0` early…
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:405` — ### 2026-07-24 — cycle 41 (T2.2: consolidate the duplicated `VariableRow` prompt-variable editor — owner-authorized, "use your judgement") — canonical-copy decision made
- `T2.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:411` — - **Evidence:** typecheck clean (after the self-corrected `PromptVariable` restore); vitest **126/126** (incl. `primitiveAdoption.test.ts` against 340/163 + the 4 new VariableRow tests; one self-fixed test-authoring bug — the sanitize test first used `input.value=`+raw native event, which React's…
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:70` — | T2.3 | Add any genuinely-missing primitive to the system ONCE (if the audit found a real gap) with a pattern-gallery entry | `web/src/ui/`, `docs/design/patterns.md` | new primitive documented + adopted where needed; not a one-off |
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:101` — - **DISCOVERY:** There is **no shared form-field primitive** — the one genuine system gap (T2.3): a `TextField`/`Field` in `web/src/ui/` + `patterns.md` entry, to land before migrating the 206 raw inputs.
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:124` — ### 2026-07-22 — S2 cycle 5 (T2.3: the missing form-field primitive + pattern gallery)
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:125` — - **DONE (T2.3 — the one genuine system gap):** added `web/src/ui/TextField.tsx` — `TextField` (single-line) + `TextArea` (multi-line) with `size` (sm/md/lg → h-8/9/10), `surface` (container/high/base), `mono`, and `leadingIcon` variants. It is the CANONICAL EXTRACTION of the exact input shape ~2…
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:290` — ### 2026-07-23 — S2 cycle 30 (T2.3: codify the standard-field size/surface scale)
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:292` — - **DONE (T2.3 — the scale, byte-identical):** `TextInput` now carries a principled **size** (sm/md/lg → h-8/h-9/h-10) × **surface** (container/high/base) scale via `FIELD_SIZE`/`FIELD_SURFACE` tables + a shared `INPUT_BASE` invariant-chrome const. Defaults (lg/container/0.9375rem) reproduce the …
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:303` — - **DONE (T2.3 adopter — first cluster):** migrated the **7** raw `<input>` in `AddToolServerModal` (`pages/tools/ToolsPage.tsx`) — mcp branch (name/command/args) + openai branch (name/endpoint/apiKey/toolFilter) — to `<TextInput size="md" surface="high" [mono] [type="password"]>`. The `mcpInputC…
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:316` — - **DONE (T2.3 adopters — 6 bare leading-icon filters):** migrated to `<TextInput … leadingIcon={<Search size={14} />}>`:
- `Session 2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:64` — ### Session 2 — Token + primitive hardening
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:433` — - **Discovery → decision (pivot, surfaced not blocked):** that add-button idiom (`inline-flex items-center gap-1.5 rounded-md bg-surface-container px-m h-9 text-on-surface-var text-[0.8125rem] hover:bg-surface-high transition-colors`) is duplicated **byte-for-byte across 5 static `<button>` sites…
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:448` — - **Context:** cycle 45's NEXT put LoopCockpitPage (~11 raw `<button>`) next in the item-1 mega-page queue. Reading it in full surfaced the same scope discovery that reshaped cy44: **LoopCockpitPage is already ~82% migrated** — it imports and uses `IconButton`, `Button`, `HeaderActions`/`HeaderCo…
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:449` — - **Discovery → decision (the clean slice = the idiom, not the page):** that Download button's className (`inline-flex items-center gap-1 rounded-md px-2 h-7 text-[0.75rem] text-on-surface-low hover:bg-surface-high hover:text-on-surface`) is **verbatim-duplicated across 4 static `<button>` sites …
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:451` — - **Ratchet: `rawButton` 327 → 323 (−4), same commit.** Four literal `<button>` removed (LoopCockpitPage −1, ArtifactViewer −2, FileViewer −1; the new primitive lives under `ui/`, scanner-exempt). `primitiveAdoption.test.ts` green against the new 323 confirms **exactly** four removed. `rawInput`/…
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:457` — - **Discovery → decision (the clean slice = the reorder-stepper idiom across ALL its remaining sites):** the reorder up/down stepper is a **genuine 3-site cross-file idiom**, and `SquareIconButton`'s own doc names "reorder up/down" as its purpose. After cycle 45, `WorkflowForm` already uses `Squa…
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:461` — - **Evidence:** typecheck clean (both new imports resolve; `ChevronUp`/`ChevronDown` still used in both files); vitest **136/136** (ratchet green at 319/163; `SquareIconButton.test.tsx` 7 tests unchanged); build green (chunk-size warning pre-existing); visual **64/64 with ZERO baseline movement**…
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:465` — - **Context:** cycle 47's NEXT put ChatPage (~19 raw `<button>`, the largest mega-page reached so far) next in the item-1 queue. Reading it in full — plus its lines 188/296/1868/1876/1885/2117/2182/2255/2328/2411/2492/2611/2622/2659/2709/3009/3027/3100/3189 — surfaced the by-now-familiar scope tr…
- `cy4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:466` — - **Discovery → decision (the clean slice = the `SelectionPill` idiom across BOTH its sites):** the floating "act on the current text selection" pill is a **genuine 2-site cross-file idiom** whose `<button>` className is **duplicated character-for-character across two files** — `ChatPage.tsx`'s `…
- `cy16` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:652` — ### 2026-07-24 — cy16 DONE (S2 raw-input migration wave 4 — the dense modal/side-panel `<textarea>` cluster converges onto `TextArea`, which grows a lockstep `size` axis; owner-authorized under the full-authority grant)
- `cy16` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:661` — - **Validation (drove the running dev gateway):** bundled Playwright against `127.0.0.1:10000`. Created a throwaway native agent (`cy16-validate-tmp`) via the real `POST /api/agents` (native agents mount `RoutingNotesEditor`; the 6 built-ins are reserved and render only the model editor), opened …
- `cy16` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:665` — - **Why in-scope now:** rotates from the S2 raw-input/raw-button lanes (both drained of *byte-identical* clusters — cy16's Why + cy15's NEXT established the survivors all move pixels or need primitive growth first) to the **S3 T3.1** interaction-pattern lane, which is explicitly "standardize sele…
- `cy37` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:489` — - **NEXT (item 1 mega-page queue CLOSED — every mega-page triaged):** ChatPage (cy48), ProjectsSection (cy49), and CodeCockpitPage (cy50) are done; the three largest mixed-role pages are triaged and their clean slices extracted (SelectionPill, InlineError, Centered). **No mega-pages remain.** Wha…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:489` — - **NEXT (item 1 mega-page queue CLOSED — every mega-page triaged):** ChatPage (cy48), ProjectsSection (cy49), and CodeCockpitPage (cy50) are done; the three largest mixed-role pages are triaged and their clean slices extracted (SelectionPill, InlineError, Centered). **No mega-pages remain.** Wha…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:491` — ### 2026-07-24 — OWNER TASTE-CALL RULINGS (the cy50 batch, decided after visual inspection of the real-CSS comparison screenshots) — the taste/pixel lane is now unblocked
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:492` — - **Context:** the cy50 "batch of taste/pixel decisions" was surfaced to the owner as a set of faithful comparison screenshots (real compiled-CSS harness, dark+light, every divergent variant beside its shared-primitive reference, labelled file:line). Owner inspected all groups ("each of these loo…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:517` — - **Context:** the Cancel/Done idiom is *already* overwhelmingly converged onto `Button variant="ghost"` (a full-tree grep shows ~30 sites already using it). G6's ruling targets only the **two remaining divergent surface-filled pills** the cy50 screenshots surfaced: `FilterMenu:83` "Done" and `Ta…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:524` — - **Context:** the retry idiom is *already* largely converged onto `Button` — the cy50 audit named CodeCockpitPage:2904/2997 (quiet inline `variant="ghost" size="xs" text-primary`) and CodeCockpitPage:471 (filled `variant="secondary"`) as the two established precedents. G3's ruling targets the **…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:536` — - **Reconciliation (ruling estimated "11 sites" → 16 instances across 14 files):** the ruling's "11" was an estimate from the cy50 audit; the actual hand-rolled idiom appears 16× (three files carry two instances each: CodeCockpitPage's queue-remove pair, AppsSection's two empty-state branches). A…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:553` — - **Context + scope (the authoritative 5-site family):** G1's ruling (line 479) converges the row-action/remove-X family onto **`SquareIconButton tone="danger"`** — "idle ink-low, hover tints red, no fill; the restrained treatment … the variants (hover-reveal danger / always-visible danger / alwa…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:573` — - **NEXT (batch cycle 9/9 → G9):** fold `ChatPage` ErrorLine (:2633) + `FilesSection` strip (:351) + `MoveErrorBanner` (`TasksListPage:428`) into the `InlineError` primitive, re-tone warn→danger, and extend `InlineError`'s API (a `tone`? an `animated`? a non-dismissible multi-line `items-start` m…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
