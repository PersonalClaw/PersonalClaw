<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MRI-1` — Inbound substrate + fail-closed mount (auth exemption, auth.py, config round-trip, caps+audit, JSON-RPC transport, CLI token)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MCP-READONLY-INBOUND](../plans/MCP-READONLY-INBOUND.md) (`MRI`, plan status `done`) |
| **Atom table** | [`../atomic/MRI.md`](../atomic/MRI.md) |
| **Execution position** | #360 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 — Substrate + mount (T1.1-T1.6, V1)

## Done when

With no token: gateway boots, refusal log names the failing condition, POST /mcp 404s and every other route still auth-enforced (existing auth tests green). With token+flag ON: initialize round-trips via curl; test_config_roundtrip green with corrupt-enabled reading disabled; burst of 26 -> 20x200 then 429 with Retry-After>=1; kill-switch flip unmounts within one config read (no restart); bad bearer -> 401 mirrored to SEL; inbound_audit.jsonl populates; token stored 0600 and prints once, rotation invalidates old.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`MRI-2`](MRI-2.md) (`done`), [`MRI-3`](MRI-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/MCP-READONLY-INBOUND.md:529` — connection, not just one call. HTTP-level rate limiting is spec-permitted, and MRI-1's

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `T1.1`, `T1.6`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:109` — - **Calls:** the hooks-endpoint auth-exemption mechanism (T1.1 locates it in `token_auth.py`/`server.py` — extend, don't fork), `fence_untrusted`, `sel()`, `save_credential`, the §3.9 read paths, `config_dir`/`atomic_write`.
- `T1.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:120` — | T1.1 | Locate the hooks-endpoint auth-exemption mechanism (`token_auth.py` + `server.py` wiring for `/api/hooks/agent`); extend it to `/mcp` with an in-code comment citing this plan; record the mechanism in the Execution log | `src/personalclaw/dashboard/token_auth.py`, `dashboard/server.py` | …
- `T1.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:154` — ### 2026-07-28 — Session 1 (T1.1–T1.6, V1): DONE
- `T1.1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:160` — **T1.1 — auth exemption mechanism (as instructed: extended, not forked).** The
- `T1.6` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:125` — | T1.6 | CLI `personalclaw inbound token create mcp` (+ `--rotate`); docs stub in `docs/reference/cli.md` | `src/personalclaw/cli.py` | token printed once; stored 0600; rotation invalidates the old |
- `T1.6` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:154` — ### 2026-07-28 — Session 1 (T1.1–T1.6, V1): DONE
- `T1.6` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:203` — **T1.6 — CLI.** `personalclaw inbound token create|show mcp [--rotate]`. The token
- `Session 1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:12` — **Status:** IN PROGRESS — Session 1 (transport + auth + caps + audit) shipped 2026-07-28; **Session 2
- `Session 1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:116` — ### Session 1 — Substrate + mount
- `Session 1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:154` — ### 2026-07-28 — Session 1 (T1.1–T1.6, V1): DONE
- `Session 1` `docs/roadmap/plans/MCP-READONLY-INBOUND.md:156` — The substrate landed with an empty tool table, as Session 1 intends: transport,

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
