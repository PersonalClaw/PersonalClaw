<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2UNI-3` — Stage contracts + parameterization: resolve_unfilled_inputs/template_types + extraction contract + done-means lint + preflight + decision typing

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-UNIVERSAL-PLANNING](../plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md) (`WF2UNI`, plan status `in_progress`) |
| **Atom table** | [`../atomic/WF2UNI.md`](../atomic/WF2UNI.md) |
| **Execution position** | #637 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##181 |

## Intent — what this is for

Implementation Effort Session 3; Stage Contracts (UP-R3); Template Parameterization (UP-R8); Approval as an Autonomy Mode-Switch (UP-R16 blocking/open); From-Scratch Generation (preflight)

## Done when

workflows/contracts.py provides resolve_unfilled_inputs(), template_types(), the extraction contract, per-stage done-means contracts with the minimal-triple lint (with the two measured exemptions), and mechanical blocking-vs-open decision typing; workflows/preflight.py emits the preflight step; all wired into workflow_plan's template path as the review surface.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2UNI-2`](WF2UNI-2.md) | intra-repo | `done` |

### Unblocks (1)

[`WF2UNI-4`](WF2UNI-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `Session 3`, `R3`, `R8`, `R16`. Those tokens appear in its source plan at:

- `Session 3` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:594` — - **Session 3 — Contracts + parameterization:** per-stage done-means contracts + validation lint + preflight synthesis + altitude rule; `resolve_unfilled_inputs()` + extraction contract; triage-first convention + escalate-and-reclassify op; blocking/open decision typing.
- `R3` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:30` — - **UP-R3** — Per-stage done-means contracts, preflight, stopping-condition triple, planner altitude rule → *Stage Contracts*
- `R3` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:78` — 5. **Validation + review** — stage-contract lint (UP-R3), risk/autonomy annotation (UP-R4/R6), streaming multi-view review (UP-R7).
- `R3` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:169` — ### Stage Contracts (UP-R3)
- `R3` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:205` — All bundled multi-stage templates adopt the triage-first convention (UP-R11) and per-stage contracts (UP-R3).
- `R3` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:753` — lint (UP-R3), and blocking-vs-open decision typing (UP-R16). Wired into `workflow_plan`'s template
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:35` — - **UP-R8** — Derived parameter schemas + extraction contract → *Template Parameterization*
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:230` — ### Template Parameterization (UP-R8)
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:579` — 2. **Tiered matching in the engine:** `defs.py` gains `match_template(intent, catalog) -> (candidates ranked, reason strings)` implementing T1-T5; the T4 tier reuses the old surfacing system's embedding+keyword machinery with its REAL parameters (`workflows.match_threshold` 0.62 default + keyword…
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:752` — `template_types()` + the extraction contract (UP-R8), per-stage done-means contracts with their
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:43` — - **UP-R16** — Blocking vs non-blocking decision typing, Open Decisions on run summary → *Approval as an Autonomy Mode-Switch*
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:343` — ### Approval as an Autonomy Mode-Switch (UP-R4 + UP-R16)
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:356` — 10. **Blocking vs non-blocking decisions (UP-R16).** The planner types every decision node: **blocking needs-input** (pauses the run, enters the needs-input inbox) vs **non-blocking open-decision** (never pauses; lands as a structured "Open Decisions" section on the completed run's summary, answe…
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:753` — lint (UP-R3), and blocking-vs-open decision typing (UP-R16). Wired into `workflow_plan`'s template

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
