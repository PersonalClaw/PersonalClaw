<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-4` — personalclaw-client packaging metadata + a client CI job

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #181 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #13e46fd |

## Intent — what this is for

Session 2 T2.3; a stable published surface

## Done when

client wheel METADATA correct (name/urls/classifiers/markdown README); `pip install personalclaw-client` works; the new client CI job builds the wheel and runs its 18-test suite green in a fresh venv

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:CI-RELEASE:client CI job wiring + separate `release-client` publish environment` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`DIST-10`](DIST-10.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 19 (DIST) — wheel built and booted on this machine

**Code evidence:**

- personalclaw-client 0.1.3 is LIVE on PyPI (read from the registry's own JSON API)

**Driven in the UI:** Not applicable — a client wheel has no UI.

**Notes:** Partial deliberately. The publish half is confirmed against the live index, but the atom's other clauses — the client wheel's METADATA being correct and 'the new client CI job builds the wheel and runs its 18-test suite green in a fresh venv' — are about a CI job and a sibling package I did not build or run here. Nothing suggests they are wrong; I simply did not observe them.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/DISTRIBUTION.md:162` — | T2.3 | Publish `personalclaw-client` to PyPI (name per owner task 1); wire into apps/client CI as a pinned dep where used | `packages/personalclaw-client-py/pyproject.toml` (name/urls check), release workflow addition | `pip install personalclaw-client` works; its tests green in CI |
- `T2.3` `docs/roadmap/plans/DISTRIBUTION.md:247` — - **DONE T2.3 (personalclaw-client packaging + CI)** — Enriched `packages/personalclaw-client-py/pyproject.toml`: added `readme`, `license` (MIT), `authors`, `keywords`, full `classifiers` (py3.9–3.13, OS-Independent, AsyncIO), `[project.urls]` (Homepage/Documentation/Source/Issues — placed AFTER…
- `T2.3` `docs/roadmap/plans/DISTRIBUTION.md:299` — | T2.3 | `13e46fd` | `personalclaw-client` packaging metadata + a `client` CI job |
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:156` — ### Session 2 — PyPI + bootstrap + client
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:245` — ### Session 2 — Cycle 4

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
