<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-10` — First real PyPI publish of core + client; verify uv tool / pipx on a clean machine (T2.1, owner)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #187 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #PyPI 0.1.0–0.1.3 (owner-triggered) |

## Intent — what this is for

Session 2 T2.1; Owner task 1

## Done when

PyPI carries `personalclaw` + `personalclaw-client` (live through 0.1.3); `uv tool install personalclaw` and `pipx install personalclaw` both yield a working CLI on a clean machine

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-1`](DIST-1.md) | intra-repo | `done` |
| [`DIST-3`](DIST-3.md) | intra-repo | `done` |
| [`DIST-4`](DIST-4.md) | intra-repo | `done` |
| `EXT:CI-RELEASE:release.yml publishes core (env release) + client (env release-client)` | cross-plan (EXT) | **unresolved** |

### Unblocks (2)

[`DIST-11`](DIST-11.md) (`todo`), [`DIST-12`](DIST-12.md) (`todo`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:152` — | T1.4 | LLM-SDK demotion **(conditional on plan 32 T2.1 outcome — read its Execution log first; if pip-step unsupported, log DEVIATION and skip to T1.5)**: move `openai`, `anthropic` from `dependencies` to their existing extras; branded apps declare the dep via the verified mechanism; sharpen la…
- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:160` — | T2.1 | First real PyPI publish via release pipeline (owner triggers the environment approval); verify `uv tool install personalclaw` + `pipx install personalclaw` on a clean machine | — (pipeline exists) | both commands yield a working `personalclaw` on PATH |
- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:224` — - **PREMISE CONFIRMED (T1.4 precondition)** — read PROVIDER-BOUNDARY-COMPLETION Execution log: T2.1 DONE — `apps/app_manager.py::_install_python_deps` (L142, called from install L365 + update L529) DOES pip-install manifest `dependencies.pythonDependencies` into the shared venv. The app pip-step …
- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:250` — - **T2.1 / T2.2 (owner + website repo):** T2.1 (first real PyPI publish + `uv tool install` / `pipx` verify on a clean machine) is an owner real-world step — owner states publishing is handled (pending publisher registered 2026-07-20, env `release`). T2.2 (`install.sh` bootstrap) lives in the per…
- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:285` — - **DONE T2.2 (bootstrap `install.sh` — content produced)** — Wrote the actual bootstrap script at `deploy/website/install.sh` (staged in-repo for the website repo, plan 36): POSIX sh (`set -eu`, validated with `sh -n` AND `dash -n`), OS/arch detect, `uv` presence check → official `astral.sh/uv/i…
- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:314` — **OWNER real-world steps remaining (no core-repo code):** T2.1 (first PyPI publish via `release.yml` env `release` + verify `uv tool`/`pipx` on a clean machine); V1 (clean-VM/empty-container wheel install → onboarding → first chat, Node absent); V2 (follow the new getting-started on a clean machi…
- `T2.1` `docs/roadmap/plans/DISTRIBUTION.md:360` — machine): T2.1 first PyPI publish; V1–V4 clean-machine walkthroughs. The sibling-repo changes
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:156` — ### Session 2 — PyPI + bootstrap + client
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:245` — ### Session 2 — Cycle 4

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
