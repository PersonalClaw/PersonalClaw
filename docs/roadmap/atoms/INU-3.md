<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `INU-3` — Settings unification + alert-fields backfill (S3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INBOX-NOTIFICATIONS-UNIFICATION](../plans/INBOX-NOTIFICATIONS-UNIFICATION.md) (`INU`, plan status `done`) |
| **Atom table** | [`../atomic/INU.md`](../atomic/INU.md) |
| **Execution position** | #295 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##113 |

## Intent — what this is for

Session 3 — Settings unification (frontend) + alert-fields backfill (T3.1-T3.3, V3)

## Done when

notifications settings page with global gate + source-by-kind rules matrix (row per registered kind, grouped by source) + digest schedule field, edits persist and take effect without restart; idempotent data-inspection backfill projects inbox.json alert_keywords/alert_on_name_mention onto channel message/mention rule conditions then deletes those fields + PUT guards + inbox alert UI in the same change (re-run is a no-op, PUT to removed field 400s); docs/architecture/inbox-channels.md Notifications section rewritten to the rules model

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`INU-1`](INU-1.md) | intra-repo | `done` |

### Unblocks (1)

[`INU-6`](INU-6.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 3 (INU) — observed

**Code evidence:**

- the unified entry point exists and is reachable: an 'Inbox settings' control renders in the inbox header (observed in the live DOM, not inferred from code)
- the INU suite covering settings/backfill passes as part of the 218 tests run this cycle

**Notes:** PARTIAL: I confirmed the control is present but did NOT open it, so the unification claim (one settings surface rather than several) and the alert-fields backfill are unvalidated by driving. No defect implied — an unexercised clause.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `T3.1`, `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:188` — | T3.1 | Notifications settings page: global gate section (existing controls) + rules matrix (source×kind grid: mode selector, targets, conditions editor) + digest schedule field; served from the rules store | `web/src/pages/settings/` (new panel beside MemoryPanel pattern), rules PUT routes | ma…
- `T3.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:231` — - **C2 rule schema gains one field** (additive): `"<source>/<kind>": {mode, targets, conditions, "verify": false}` — the T3.1 rules matrix renders the toggle only for verifiable kinds.
- `T3.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:368` — - 2026-07-30 — **DONE (Session 3: T3.1–T3.3).** The notifications settings page gains the
- `T3.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:372` — **T3.1 — the matrix.** `NotificationRulesMatrix` renders one row per REGISTERED kind
- `T3.3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:190` — | T3.3 | Docs: `docs/architecture/inbox-channels.md` Notifications section rewritten to the rules model (legacy paragraphs deleted, not marked); configuration reference updated | the two docs | docs match shipped behavior; no paragraph describes the removed alert fields |
- `T3.3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:368` — - 2026-07-30 — **DONE (Session 3: T3.1–T3.3).** The notifications settings page gains the
- `Session 3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:184` — ### Session 3 — Settings unification (frontend) + the alert-fields backfill
- `Session 3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:368` — - 2026-07-30 — **DONE (Session 3: T3.1–T3.3).** The notifications settings page gains the

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
