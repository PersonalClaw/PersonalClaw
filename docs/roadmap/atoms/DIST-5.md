<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-5` — Docs restructure: getting-started uv-first install matrix + extras table; CONTRIBUTING framed as contributor path

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #182 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2b880d7 |

## Intent — what this is for

Design §A; Session 2 T2.4

## Done when

a stranger following docs/guides/getting-started.md never runs Node or git; §A install matrix + extras guidance table present; venv/`make web-build` path moved to CONTRIBUTING dev-setup

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-1`](DIST-1.md) | intra-repo | `done` |

### Unblocks (1)

[`DIST-11`](DIST-11.md) (`todo`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 19 (DIST) — wheel built and booted on this machine

**Code evidence:**

- docs/guides/getting-started.md and docs/guides/containers.md both exist

**Notes:** The atom's clause is a STRANGER test — 'a stranger following docs/guides/getting-started.md never runs Node or git'. That is stranger-validation work, the same category as MRI-4's guide, and it cannot be settled by reading the file I would be grading. The install matrix and extras table are checkable and present; the claim about what a first-time reader ends up running is not.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:154` — | V1 | Validation: clean VM/container with NO Node: install the wheel → onboarding → first chat. Update `docs/guides/getting-started.md` ONLY after this passes (uv-first rewrite is T2.4) | — | walkthrough passes; Execution log records the environment used |
- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:163` — | T2.4 | Docs restructure: getting-started leads with uv-tool + bootstrap + compose matrix (§A table); venv/`make web-build` path moves to CONTRIBUTING dev-setup; extras guidance table added | `docs/guides/getting-started.md`, `CONTRIBUTING.md` | a stranger following getting-started never runs No…
- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:243` — - S1 now has T1.1–T1.5 implemented + locally verified. V1 (clean-VM/empty-container wheel install → onboarding → first chat, Node absent throughout) remains an owner real-world step; the getting-started uv-first rewrite is deferred to T2.4 per the plan.
- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:251` — - **DONE T2.4 (docs restructure)** — Rewrote `docs/guides/getting-started.md` to lead with the §A install matrix: **uv tool** (recommended, brings its own Python 3.12), the `curl … | sh` bootstrap one-liner, pipx, pip, and Docker Compose — with the git-checkout path redirected to CONTRIBUTING. Dr…
- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:253` — - Owner note (plan owner task 4): this changes the project's public "how do I run this" answer (uv-first, contributor path demoted) — flagged for owner approval; implemented per T2.4 since it's the assigned task and the wheel/serve contract is proven locally (T1.5). V2 (follow the new getting-sta…
- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:258` — - **DONE T3.2 (container guide)** — Created `docs/guides/containers.md`: services table (gateway + web), ports (3000→HTTPS 3443 app, gateway 10000 loopback), the `personalclaw_home` volume + `down` vs `down -v` warning, the `.env` pattern (image tag / port / bind host / auth mode / api key), gett…
- `T2.4` `docs/roadmap/plans/DISTRIBUTION.md:300` — | T2.4 | `2b880d7` | getting-started uv-first install matrix + extras table; CONTRIBUTING framed as contributor path |
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:156` — ### Session 2 — PyPI + bootstrap + client
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:245` — ### Session 2 — Cycle 4

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
