<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-16` — A Loop becomes a WorkflowRun (retire the second work-unit noun)

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #620 of 684 in dependency order |
| **Startability** | **startable** — every intra-repo dependency is done and nothing gates it |
| **Blocked reason** | PARTIAL — seam 4's LEDGER-RAILS third is closed by PR #2565; the other two thirds are not, and one of them is held by the owner. Do not read this as seam 4 done. WHAT LANDED: four pure projections in introspection.py (findings_rail, verdict_rail, rail_coverage, rail_totals) in the module whose stated doctrine is 'pure functions over event lists', one service.ledger_rails(run_id) doing a single ledger read for both rails, its own route GET /api/workflows/runs/{run_id}/ledger-rails (deliberately NOT a field on introspect, which pays for a cross-run scan this does not need), and LedgerRailsPanel.tsx behind a Rails toggle. No store, no new event kind, no state invented or retired — so the homeless-column census is UNCHANGED at 33, re-measured rather than inherited (loops 41, runs 26, shared 8; the count in this atom's previous reason was one revision stale). THE PLAN'S OWN CLAUSE IS WRONG AND THIS IS THE CORRECTION: it says the PP-5 ledger carries step_completed, judge_verdict, breaker_trip and watcher_reaped 'for both nouns'. Measured, loop/journal.py::breaker_trip is the ONLY writer in the tree and the workflow engine has no breaker to trip. So the rail reports breaker coverage as `null`, never `0` — 'the breaker never tripped' and 'nothing here can trip a breaker' are different claims and only the first is an observation. RAIL_PRODUCERS declares the asymmetry and an AST scan checks that declaration against the workflow package's real emitters IN BOTH DIRECTIONS, so an engine that grows a breaker reds the table instead of leaving a null where a real zero now belongs. ABSENT-IS-NOT-ZERO was the load-bearing design, and this was the eighth place the class surfaced: a run-shaped step_completed always carries cost_usd, so 0.0 on a free local model is a REAL observation and is reported as 0.0; a loop-shaped step_completed carries no cost or token key at all, because loop money lives in usage/turns.jsonl. Since PP-16 retires the loop noun ONTO the run noun, loop-shaped rows will flow through this exact projection — and a rail defaulting the missing key to zero would report '$0.00, 0 tokens' for work that really cost money, on the one surface a user opens to find out what it cost. Read through _carried rather than hard-coded, and railed in both directions. Same discipline on the ROI axis: marginal_value and quality_score are reported as absent and NAMED in absent_scores rather than zeroed. A REAL LEAK WAS FOUND BY ITS OWN MUTATION TESTING, worth recording because it argues for the practice: rail_totals lifts a verdict's own word into a verdicts_by_word KEY, and totals were computed from raw rows BEFORE row-level redaction — so free text in a verdict word reached the payload un-redacted through the aggregate while the row beside it was clean. Fixed by redacting then aggregating. Mutation testing also caught one vacuous test (it asserted the WRITE path's redaction while claiming to assert the read path, and stayed green with read-side redact deleted) and one unrailed leg. 20/20 mutations caught after the fixes. MEASURED REACHABILITY, before -> after: 0 of the 4 named kinds had its own payload reachable from any run-side projection (service.introspect was the only one, and its timeline row drops output_ref, provider, retries and degraded_reason); all 4 are reachable now, two as rails and two via the coverage row. 26 run routes existed and none was named findings/verdict/roi/rails — /runs/{id}/review is a different noun entirely (review_finding, code-review comments against a live diff). REMAINING, FIVE UNITS NAMED BY DELIVERABLE rather than by letter (neither 4g nor 4h is resurrected; those were undefined — see #2536). Note the plan BUNDLES the first two and they are separable: (1) A RUN-LEVEL DOCUMENT DELIVERABLE — CLOSED 2026-09-07 (PR #2635, merged 3456ebdf3, 19/19 CI green). GET /api/workflows/runs/{run_id}/deliverable, proven by RESOLVING A CONCRETE PATH through a built aiohttp app and asserting the handler identity — not a canonical-path census and not grep — plus a real TestClient GET and a 404 for an unknown run. THE KIND->FILENAME MAPPING IS DERIVED, NOT SPELLED: nothing in workflows/deliverable.py names a document, and a test PARSES ITS AST and fails on any REPORT.md / MONITOR_LOG.md / DESIGN.md / RESEARCH.md literal outside a docstring. The table is built by walking loop_aliases FORWARD over every (kind, variant) pair and asking that kind's own deliverable_name() — the same call loop/store.read_deliverable and loop/watchdog._deliverable_file make — so loop_aliases' one-way rule holds: it never asks which kind a template was, it asks which templates the kinds produce and inverts its own forward answer. Derived, not asserted: goal/open_ended REPORT.md, goal/monitor MONITOR_LOG.md, design DESIGN.md, research RESEARCH.md, and goal/verifiable + code-project + general-project produce NOTHING. loop/store.py gained LOG_NAME and DELIVERABLE_FALLBACKS so the run side imports the log's filename rather than spelling FINDINGS.md twice — the FILE half only; the row half is untouched and is unit (4). ABSENCE GETS FIVE NAMED REASONS, exported as ABSENT_COPY so a test asserts the rendered sentence: template_unknown (26 of 33 bundled templates are not loop kinds — UNKNOWN, not none), kind_has_no_document (finished, nothing missing), not_written, no_root, unreadable. `content` is null and never "", with the OPPOSITE direction railed — a file someone really wrote nothing into reports present:true, content:"". AND AN HONESTY NOTE THE MEASUREMENT FORCED: no bundled template names its kind's document anywhere in its spec, so the panel says "this run's template never names REPORT.md, so nothing has asked the worker to write one. Waiting will not produce it" — measured per run against the run's own spec, with a test that RE-TAKES the measurement so the claim cannot rot. No money and no ROI axis on the payload, with tests asserting the ABSENCE per #2566 and the JudgeVerdict gap. 14 mutations applied, 14 caught. COUPLING TO UNITS (2)-(5): NONE — one adjacency was hit and deliberately not crossed, since Loop.kind_config['primary_deliverable'] lets a loop user name the file and no bundled template declares an equivalent input; the name is derived from the template only and the missing override is NAMED in the payload's derivation rather than invented as a run-side input. ONE FINDING BEYOND THE UNIT, filed as #2637: security.redact_credentials is QUADRATIC IN UNBROKEN-TOKEN LENGTH and flat in document length, so A BYTE CEILING DOES NOT BOUND THIS READ'S COST — 512 KB of prose is 0.04s and 512 KB of one token is ~111s, measured. Clipped at this read path (any run of >512 non-space chars replaced WHOLE, because half a credential is still half a credential, before redaction, counted and shown); the shared redactor is untouched and needs an owner ruling. FOUR UNITS REMAIN, so this atom does NOT flip. (2) A STEPWISE PLAN-WALKTHROUGH SURFACE FOR A RUN — the six loop routes plan-session, plan/start, plan/approve, plan/comment, plan/edit, plan/retry over planning.session.PlanSession. held on an owner ruling and not startable before it: 4e's doctrine note is the blocker, because a loop provisions one TaskList PER PHASE and seeds tasks into it while a run materializes one task per node into NO list, so parity needs either a run-side filing step or explicit acceptance that a run's derived map is empty until a user files. (3) KIND-SPECIFIC INTAKE AND CANVASES — classify, validate, grill-tree, queue, design/tokens. Belongs WITH the store retirement per the plan's own argument: once the kind IS the template, a design loop's token canvas is a template's surface, so building it first builds it twice. (4) THE `loops` ROW RETIREMENT ITSELF — 33 destinations for the 33 homeless columns plus deleting loop/store.py's row half. EIGHT await an owner ruling: name, provider_agent, strategy_id, strategy_config, auto_teardown_on_complete, deadline_secs, stop_reason, tasks_project_id. Seam 4's tail, not a cockpit item. (5) ONE STATUS VOCABULARY — LoopStatus 12 vs RunStatus 8, with 7 loop-only (intake, planning, review, ready, stagnant, blocked, stopped) and 3 run-only (draft, cancelled, escalated) per STATUS_VOCABULARY_DELTA. One decision per orphan. A PEER of seam 4 rather than part of it — this atom's own reason already called it 'its own out-of-scope seam'. AN UNSCOPED DIRECTION, worth minting and currently nowhere: TEN RUN ROUTES HAVE NO LOOP EQUIVALENT — fork, rewind, run-from, continuations, introspect, per-node inspect, outbox, review, review/triage, drop. 'One cockpit contract' is a UNION, and PP-16 only ever scoped the loop->run direction. TWO FURTHER FINDINGS. Issue #2566: ledger.reader.run_totals reports cost_usd 0.0 and tokens 0 for a LOOP, because LoopJournal.cycle writes no money keys at all (loop money lives in usage/turns.jsonl via loop_spend) — latent today because nothing reads a loop's money keys, and aimed straight at the retirement; introspection.RunStats.cost_usd has the same shape. Not fixed in #2565 because three packages consume that contract and changing it needs a ruling. And a fidelity gap the retirement will hit, recorded as a design question rather than filed as a bug: the run controller ledgers only verdict/status/evidence and keeps the rich JudgeVerdict in the node OUTPUT, so marginal_value and quality_score — the axes the loop's RoiRail plots — are unreachable from a run's ledger. #2565's rails report them absent and NAME them, which is the correct scope, but a run-side ROI rail can never match the loop's until the controller ledgers them. MARKER NOTE: naming a sub-unit's owner ruling with the OWNER_GATE_RE spelling moved this whole atom into gated_frontier, which is wrong — unit (1) is agent-closable today. atom_gates() reads blocked_reason as one blob and cannot tell an atom that IS held by the owner from one that MENTIONS an owner gate, so the spelling is avoided above. |

## Intent — what this is for

The capstone. `materialize.py` already proves the direction ("tasks as a projection of run state") and `containers.py` is the Work board projection; loops are the holdout, with their own row, status enum, store, watchdog and adoption path. Consequence today: restart-adoption is implemented twice (`loop/manager.reap_orphaned_loops` and `workflows/watchdog` adoption), park-on-human twice, budget twice, cancel twice — so every future feature touching work units costs 2x. LOOPS-EVOLUTION always intended kinds to become templates; this is the half it did not name, the NOUN change rather than the brain change.

## Done when

A Loop is a `WorkflowRun` carrying a `SupervisorPolicy`; the five kinds are bundled templates plus policies, so the domain intelligence lives in the policy and the supervisor stops being pluggable Python. One status vocabulary, one adoption/reaping path, one attention path, one ledger, one projection to tasks, one cockpit contract. `loop/store.py`'s parallel row is retired (clean break under the pre-1.0 banner; release notes advise `personalclaw snapshot`). Verified as a user: each of the five kinds is driven end-to-end through the unified path with its cockpit intact, a kill mid-run is adopted by the single watchdog, and the flywheel produces a proposal from a loop run's ledger. Explicitly NOT in scope: `triggers/` stays a separate scheduler — it answers whether to START, and folding it in would put wall-clock into a pure core.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-15`](PP-15.md) | intra-repo | `done` |
| [`PP-5`](PP-5.md) | intra-repo | `done` |
| [`PP-13`](PP-13.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- 🔴 STATUS IS `todo` AND THE ATOM SAYS SO IN CAPITALS: 'PARTIAL — seam 4's LEDGER-RAILS third is closed by PR #2565; the other two thirds are not, and one of them is held by the owner. DO NOT READ THIS AS SEAM 4 DONE'. Nine `test_pp16_*` suites exist and are green, which is the landed prep rather than the atom
- 🔑 WHAT LANDED IS DESCRIBED IN TERMS OF WHAT IT DID **NOT** ADD, which is the honest way to report a partial: 'four pure projections in introspection.py … in the module whose stated doctrine is "pure functions over event lists", one service read for both rails, its own route … and a panel behind a toggle. NO STORE, NO NEW EVENT KIND, NO STATE INVENTED OR RETIRED'
- 🔑 AND THE CENSUS WAS RE-MEASURED RATHER THAN INHERITED: 'the homeless-column census is UNCHANGED at 33, RE-MEASURED rather than inherited (loops 41, runs 26, shared 8; the count in this atom's previous reason was ONE REVISION STALE)'. Correcting your own previous count is this audit's discipline applied by the plan to itself
- 🔑 THE ROUTE PLACEMENT CARRIES ITS REASON: a dedicated `GET …/ledger-rails` 'deliberately NOT a field on introspect, which pays for a cross-run scan this does not need'
- 🪤 AND THE ATOM OVERTURNS ITS OWN PLAN TEXT — its reason opens a passage headed that the plan's own clause is WRONG. A plan atom correcting the plan is the same move PHF-13 made against PHF-12's premise
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not driven: the unified path's five kinds need real runs, and the atom is explicitly incomplete.

**Notes:** Partial, and the encoding is the familiar limitation: the status vocabulary is done/todo/blocked only, so a multi-seam atom with one third of one seam closed has no accurate value to carry — `todo` plus a reason that refuses to be misread is the closest available. This is the plan's clean-break atom and the roadmap's own guidance says it gets more expensive the longer it waits.

## Recorded history

### Commits (28)

- `deb5bb7b4` 2026-09-07 — docs(roadmap): record PP-16 unit (1) closed, and the redactor finding it surfaced
- `3456ebdf3` 2026-09-07 — feat(workflows): a run has a document deliverable, and every absence is named (PP-16 unit 1)
- `b9409e6c0` 2026-09-07 — docs(roadmap): WF2UNI-14 named the wrong blocker, and its scope would falsify a done atom
- `ccb4541b8` 2026-09-06 — docs(roadmap): flip EI-6, and fix four gates that were wrong about why they were gated
- `bfb9fb098` 2026-09-06 — fix(workflows): redact the ledger rails BEFORE aggregating, and rail both legs (PP-16 seam 4)
- `fee109c66` 2026-09-06 — feat(workflows): the run side gets its two ledger rails — findings and verdict/ROI (PP-16 seam 4)
- `daa3b999b` 2026-09-06 — docs(roadmap): flip SH-12 and DCU-4; stop the frontier claiming owner work is startable
- `1d59de333` 2026-09-06 — docs(roadmap): PP-16 4g/4h are undefined; record 4f and re-measure seam 4
- `42656856b` 2026-09-05 — feat(workflows): per-run policy-overrides editor, prelaunch-gated (PP-16 seam 4f)
- `90aa345cd` 2026-09-05 — docs(roadmap): flip MC-9 + EA-7 done (EXTERNAL-ACCESS completes 9/9); PP-16 4d/4e exec-log
- `95a00d2f9` 2026-09-05 — feat(workflows): PP-16 seam 4d — sparse per-run SupervisorPolicy overlay (RULING 2)
- `08ff520c9` 2026-09-05 — feat(workflows): PP-16 seam 4e — the run-side plural tasks projection
- `d21b9df08` 2026-09-05 — docs(roadmap): flip ES-15 done; PP-16 seam-4 log catches up (4a/4b done, 4c in review)
- `7f33b5082` 2026-09-05 — refactor(workflows): PP-16 seam 4c — retire the inert WorkflowRun.task_list_id
- `9df4c3320` 2026-09-05 — refactor(loop): PP-16 seam 4b — split the row store and the per-loop file store
- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `5ceb84248` 2026-08-27 — refactor(loop): PP-16 retire loops.total_cycles, a cache of a shipped projection
- `fb6abb6c6` 2026-08-26 — feat(workflows): PP-16 retire the pluggable loop supervisor for a declared policy
- `bf2f10ddf` 2026-08-26 — feat(workflows): PP-16 one boot-adoption path for both work-unit nouns
- `e2e97cc8b` 2026-08-25 — docs(roadmap): PP-16 BLOCKED — record the measured scope and the owner decision it needs
- `9829f2d48` 2026-08-23 — refactor(loop): PP-16 one status vocabulary — terminality derived from a shared lifecycle phase
- `08ecb1422` 2026-08-23 — refactor(tasks): PP-16 converge the loop and run task-status vocabularies
- `465c88584` 2026-08-23 — test(workflows): PP-16 census which of the six one-X clauses converged
- `6b1061943` 2026-08-22 — docs(roadmap): PP-16 BLOCKED — retiring the inert run task_list_id is an owner call
- `204f3a8f7` 2026-08-22 — fix(loops): a wedged intake/planning loop can be stopped, not just deleted
- `d66900cd6` 2026-08-20 — fix(loops): PP-16 one action-guard vocabulary — a blocked loop can be resumed again
- `e768c58f7` 2026-08-18 — feat(loops): PP-16 one loop-status vocabulary + the Loop-to-WorkflowRun field map (#1705)
- `97c9e0249` 2026-08-18 — docs(roadmap): flip PP-15 done — its work landed, its status did not (#1654)

### Named by atom id in the roadmap prose (49)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:304` — poll beside `max_cycles`; store migration + FAILED-resume un-end clears both; the PP-16
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:462` — route. Nothing was invented, reshaped, or wrapped — `PP-16` is unifying Loops into WorkflowRuns
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1828` — see it.** Found while root-causing a CI-only `real-home rail FAILED` on `PP-16`'s branch (#2111).
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1946` — calling `AppConfig.load()` at import time. That fix is also only on the PP-16 branch. `mcp_core.py`
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1958` — leg will red **silently at merge** unless re-pointed. PP-16's `mcp_core._api_base()` change remains
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:3` — **Status:** IN PROGRESS — 15 of 16 atoms shipped (see `## Execution log`). `PP-16` (a Loop becomes a WorkflowRun) is the only atom not yet done. 16 atoms in [`../atomic/PP.md`](../atomic/PP.md).
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:121` — ## §5 One work-unit noun (`PP-14`…`PP-16`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:133` — retry-with-a-hint it is today. `PP-16` is the noun change.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:144` — WF2LOO-16 ─→ PP-14 ─→ PP-15 ─────────────┴─→ PP-16
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:154` — `PP-16` are **state-shape changes** (class B). Today they are plain clean breaks: no gate, no dual
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:186` — * **A fifth verdict vocabulary**, which is the default outcome of doing `PP-11` or `PP-16` before
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:207` — - **2026-09-06 — `PP-16` sub-seam 4f DONE (recorded late), and `4g`/`4h` ARE UNDEFINED — a roadmap
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:223` — `PP-16`'s `blocked_reason`). Neither names a surface, a `done_when` or a dependency. A session
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:308` — - **2026-09-05 — `PP-16` sub-seams 4d + 4e DONE: the sparse policy overlay (RULING 2) and the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:333` — - **2026-09-05 — `PP-16` sub-seam 4c DONE: `WorkflowRun.task_list_id` retired under OWNER
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:352` — - **2026-09-05 — `PP-16` sub-seam 4b DONE: the two stores sharing `loop/store.py` are SPLIT (core
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:369` — - **2026-08-27 — `PP-16` sub-seam 4a DONE: `loops.total_cycles` retired. Atom stays `todo`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:403` — EXISTS` cannot drop a column from an existing table and PP-16 4a deliberately adds no DROP path: a
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:459` — - **2026-08-20 — `PP-16` slice DONE: one action-guard vocabulary (the backend mirror the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:527` — **Still open on PP-16, unchanged by this slice** (the noun change itself, one adoption/reaping
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:534` — - **2026-08-18 — `PP-16` PARTIAL (atom stays `todo`).** The capstone is multi-session; two slices
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:550` — - **2026-08-18 — `PP-16` step 1 DONE: the field map.** `workflows/loop_run_map.py` maps all **39**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:562` — - **2026-08-18 — `PP-16` OWNER DECISIONS the field map surfaced (recorded, not taken).** (1) **A run
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:575` — - **2026-08-18 — `PP-16` DONE-NOW: one loop-status vocabulary (the frontend half of "one status
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:599` — - **2026-08-18 — `PP-16` UNMET, with the census evidence (why no more was started).** Doctrine is
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1153` — different atom (`PP-16`'s "a Loop becomes a WorkflowRun"), not this one.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1431` — - **2026-08-22 — `PP-16` slice DONE: no non-terminal status is actionless. Atom stays `todo`.** The
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1468` — **Still open on `PP-16`** (unchanged by this slice): the noun change itself, one adoption/reaping
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1477` — - **2026-08-22 — `PP-16` BLOCKED on an owner decision: retiring the inert `WorkflowRun.task_list_id`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1499` — PP-16 lands; a new homeless field is an owner decision, not a detail."* Removing an inert
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1517` — ### `PP-16` — BLOCKED (E6 scope pressure), 2026-08-25 — needs an owner scope decision
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1519` — **Not a dependency problem.** `PP-16` is READY: every non-`EXT` dep (`PP-15`, `PP-5`, `PP-13`) is
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1545` — **What would clear it.** An owner scope decision splitting `PP-16` into sequenced sub-atoms with
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1563` — ### `PP-16` — 2026-08-26, slice DONE: one adoption/reaping path. Atom stays `todo`.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1565` — **The 2026-08-25 `BLOCKED` above asked for an owner scope decision splitting `PP-16` into sequenced
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1660` — `test_workflows_hardening.py`, `test_structural_baseline.py`, both `PP-16` rails, the field map,
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1704` — **Still open on `PP-16` — the four clauses that keep the atom `todo`** (unchanged by this slice):
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1723` — ### `PP-16` — 2026-08-26, CI follow-up: the real-home rail failure was PRE-EXISTING, not this slice
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1817` — budget/attention/stagnation coverage. That is `PP-16`'s still-open **pluggable supervisor** seam,
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1825` — including the new rail, both `PP-16` rails, `test_aap9_project_stamping` (the collection-time
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1839` — ### `PP-16` — PARTIAL (seam 3 of 6: the pluggable supervisor became a policy), 2026-08-26
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1841` — **Owner scope decision applied.** The 2026-08-25 `BLOCKED` asked for `PP-16` to be split into the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1847` — `_rearm_running` / `_rekick_planning`. **`PP-16` stays `todo`** — a PARTIAL atom does.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1914` — **Still open on `PP-16`** — four clauses, unchanged by this seam: one status vocabulary
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1932` — - **[2026-08-27] OWNER RULING — `PP-16`'s six-way decomposition is APPROVED, and three seams have now shipped under
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1942` — ### `PP-16` seam 4 — MEASURED, then split. Two owner rulings recorded. 2026-08-27
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1056` — multi-session by measurement (`PP-16`'s remainder is 9,045 lines across 43 importing files).
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:1779` — direction `PP-16` will take when it unifies the two work-unit paths, and
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:1294` — retirement) fires only after the loop drain, which is `PP-16`'s store seam: `planning/` still has live importers and

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
