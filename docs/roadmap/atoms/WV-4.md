<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-4` — Slices 2+3 — outcome model + engine-owned completion + resilience/budgets/timeouts + effect ledger + write-scope + termination + secrets

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #673 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Slice 2 + Slice 3

## Done when

extended outcome states, verification ladder + required_artifacts + fresh-judge + closed verdict enum, mutation-hint retries + circuit breaker + soft budgets + baseline_check, two-knob timeouts; effect ledger idempotency keys + BYOI teardown, v2 run-workflow provider in ALLOWED_HOOK_PROVIDERS, fs-diff scope_violation, sticky CANCEL + workflow_audit, {{secret:KEY}} + RedactingSink

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-3`](WV-3.md) | intra-repo | `done` |

### Unblocks (3)

[`WV-13`](WV-13.md) (`done`), [`WV-14`](WV-14.md) (`done`), [`WV-6`](WV-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `Slice 2`, `Slice 3`. Those tokens appear in its source plan at:

- `Slice 2` `docs/roadmap/plans/WORKFLOWS-V2.md:703` — ### Slice 2: Outcome Model + Engine-Owned Completion + Resilience (est. 3 sessions)
- `Slice 2` `docs/roadmap/plans/WORKFLOWS-V2.md:1179` — (Slice 4). Retry currently re-runs a fresh attempt; mutation-hint injection is Slice 2.
- `Slice 3` `docs/roadmap/plans/WORKFLOWS-V2.md:631` — - `validation.py`: remove `_WORKFLOW_SCOPES`, all `WORKFLOW_*_SCHEMA`, workflow entries in `MCP_CORE_SCHEMAS`, `"run-workflow"` in `ALLOWED_HOOK_PROVIDERS` (re-added for the v2 run-workflow action in Slice 3)
- `Slice 3` `docs/roadmap/plans/WORKFLOWS-V2.md:711` — ### Slice 3: Side Effects + Scope + Termination + Secrets (est. 2 sessions)
- `Slice 3` `docs/roadmap/plans/WORKFLOWS-V2.md:1178` — refusal rather than a silent skip); the effect ledger (Slice 3); mid-flight mutation

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
