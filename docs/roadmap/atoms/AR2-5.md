<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AR2-5` — RoutingChip component: WS-driven pill, Route→setSessionAgent+toast, dismiss, FEEDBACK-SIGNAL double-write

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-ROUTING](../plans/AGENT-ROUTING.md) (`AR2`, plan status `done`) |
| **Atom table** | [`../atomic/AR2.md`](../atomic/AR2.md) |
| **Execution position** | #66 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #feature-agent-routing |

## Intent — what this is for

Design S2 (the chip); Task breakdown Session 2 T2.1; Risks (FEEDBACK-SIGNAL coordination)

## Done when

chip renders non-blockingly on routing_suggestion for the open session, Route re-targets the session via existing switch path, dismiss suppresses, both actions double-write feedback (routing_suggestion target / routing_pair producer), auto-clears on send/switch, typecheck + vitest green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AR2-3`](AR2-3.md) | intra-repo | `done` |
| [`AR2-4`](AR2-4.md) | intra-repo | `done` |
| `EXT:FEEDBACK-SIGNAL:routing_suggestion target-kind + routing_pair producer-kind vocabulary and record_feedback API (plan 58, shipped 2026-07-27)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 36 (AR2) — DRIVEN in a real browser

**Code evidence:**

- web/src/pages/chat/RoutingChip.tsx exists among the chat surface's 54 modules, and the frontend chat suite passes 300/300 (measured last cycle, same tree)
- the double-write the clause requires is the FEEDBACK-SIGNAL contract — one signal on the suggestion target, one on the routing pair — so an accept and a dismiss both become training data rather than only the accept
- test_agent_routing 15/15; test_config_roundtrip 17/17

**Driven in the UI:** Not driven: the chip renders on a routing_suggestion broadcast, which needs a matching send against a bound model. No chip could be produced in this home.

**Notes:** Partial for the drive. The design point worth recording is that BOTH actions write feedback. A suggester that only learns from accepts sees a biased sample — every ignored or dismissed suggestion is the more informative half, because it is the case the classifier got wrong.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/AGENT-ROUTING.md:132` — | T2.1 | `RoutingChip` component (the `SessionSkillsReview` pill pattern): renders on `routing_suggestion` for the open session, "Route" → `api.setSessionAgent` + toast, ✕ → dismiss endpoint; auto-clears on next send / agent switch; honors `prefers-reduced-motion` via existing MotionConfig | `web…
- `Session 2` `docs/roadmap/plans/AGENT-ROUTING.md:128` — ### Session 2 — Chip UI + authoring + suppression UX

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
