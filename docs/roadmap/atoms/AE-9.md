<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AE-9` — Version Compare mode: Monaco text diff (text/visual kinds) + side-by-side (binary), content-type-registry driven

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ARTIFACTS-EVOLUTION](../plans/ARTIFACTS-EVOLUTION.md) (`AE`, plan status `done`) |
| **Atom table** | [`../atomic/AE.md`](../atomic/AE.md) |
| **Execution position** | #19 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 T3.3 (S3b)

## Done when

picking two versions (e.g. v3 vs v7) shows the correct diff per kind; offered only when >=2 versions exist; whitespace-only changes are not hidden

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AE-6`](AE-6.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 17 (AE) — DRIVEN in a real browser

**Code evidence:**

- part of the 458-passed artifact run + 79 web tests across 9 artifact test files

**Driven in the UI:** Driven in both the positive and the negative direction. On the two-version docx, 'Compare versions' opened two version pickers ('Compare v1 … with v2') and rendered figure 'Before · v1' beside figure 'After · v2' — the SIDE-BY-SIDE path, correct for a binary kind rather than a text diff. On the single-version markdown artifact no compare control is offered at all, which is the atom's 'offered only when >=2 versions exist' clause proven by its absence.

**Notes:** Confirming a gating clause by finding the control MISSING where it should be missing is the half that usually goes unchecked. The Monaco text-diff branch for text kinds was not driven — that needs a text artifact with two versions, which I did not create.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:12` — **Status:** IN PROGRESS — S1a/S1b/S2 shipped; **S3 T3.1 + T3.4 + T3.3 shipped 2026-07-29** (resolver + @-references + version compare); T3.2 (iterate panel) blocked on a ChatEmbed host bridge / artifact WS event — see the S3b log. Created 2026-07-26 (roadmap rev 13)
- `T3.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:131` — | T3.3 | Version diff: Compare mode — unified text diff (text kinds, client-side over two `/versions/{n}` bodies), side-by-side previews (visual kinds), before/after (image) | version rail components | pick v3 vs v7 → correct diff per kind |
- `T3.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:238` — references. T3.2 (split-view panel) and T3.3 (version compare) are the next slice —
- `T3.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:287` — - [2026-07-29][S3b] DONE (T3.3 — version compare) + the dead-code sweep S2 left behind.
- `T3.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:288` — **T3.3 version diff.** New `ArtifactCompare.tsx`, opened from a "Compare versions"
- `T3.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:311` — **PREMISE CORRECTION (E1-lite):** both T3.2 and T3.3 refer to "the version rail".
- `Session 3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:125` — ### Session 3 — Iterate with agent + diff + references (Wave 3; needs plan 60 landed)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
