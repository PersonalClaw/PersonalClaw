<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2UNI-14` — Retire legacy planning modules after loop drain (split from WF2UNI-12 per ruling)

| | |
|---|---|
| **Status** | `blocked` |
| **Plan** | [WORKFLOWS-V2-UNIVERSAL-PLANNING](../plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md) (`WF2UNI`, plan status `in_progress`) |
| **Atom table** | [`../atomic/WF2UNI.md`](../atomic/WF2UNI.md) |
| **Execution position** | #648 of 684 in dependency order |
| **Startability** | not startable |
| **Blocked reason** | BLOCKED, correctly — but this reason previously named the wrong blocker, and the correction matters because the old wording invites exactly the wrong action (2026-09-07). IT IS NOT 'AFTER WF2LOO'. All 18 WF2LOO atoms are `done`, and that is NOT the drain: WF2LOO delivered LOOPS-EVOLUTION Phase 1 (coexistence) and Phase 2 (feature parity). The drain is Phase 3 (deprecation) into Phase 4 (retirement endgame), and neither is a WF2LOO atom at all. THE CODE SEAM IS PP-16, still `todo` — specifically its remaining units, a stepwise plan-walkthrough surface for a run and the kind-specific intake/canvases, which are the REPLACEMENTS for plan_walkthrough / planning.session and the two classifiers. The 2026-08-27 owner ruling that created this atom says so in as many words: '12b fires only after the loop drain, which is PP-16's store seam.' I read 18/18 WF2LOO as 'the drain happened' and was wrong; acting on that reading would have deleted nine live modules. MEASURED, not inferred: FOURTEEN registered routes still resolve through these nine modules, proven at runtime by building an aiohttp Application and resolving each route rather than by grep — eight loop routes plus six /api/chat/sessions/{s}/plan* routes. The handler bodies call the modules AT REQUEST TIME, not merely at import: plan/approve to PS.approve_step, plan/edit to PS.edit_artifact, plan/start to pw.advance_plan to planning/runner.run_planner_pass. `POST /api/loops` create is still LIVE AND UNGATED with no deprecation gate, which is the cleanest proof Phase 3 has not started. The frontend is mounted, not dead: App.tsx mounts LoopsSection and CodeSection, and LoopComposer calls classifyULoop into loop/classify.py. And the replacement has ZERO reach — workflows.intent has 0 consumers under loop/ or dashboard/. loop/plan_walkthrough.py was last touched 2026-09-05 BY PP-16's own seam work: under active development as part of the atom that must land first, not being retired. SCOPE DEFECT, NOT JUST A SCHEDULE ONE — this is the finding worth acting on. `planning/session.py` is OUT OF THIS ATOM'S REACH REGARDLESS OF THE DRAIN. CC-8 (`done`, in the `done` CHAT-CRAFT plan) shipped chat plan mode ON TOP OF IT on 2026-08-19, a month after the WF2UNI plan was written and two days after this atom was first blocked, and CC-8's own `done_when` NAMES THE MODULE: 'a composer affordance opens the EXISTING planning/session.py walkthrough ... the no-execute guarantee is enforced by the existing plan task-mode gate'. So satisfying WF2UNI-14 as written would FALSIFY A COMPLETED ATOM'S done_when and remove a read-only-planning safety gate a user reaches from the main chat page today. That surface is NOT a loop, so no amount of loop drain licenses deleting it. WF2UNI-12's title conflated the dead plan_memory chat plan-FORMAT (retired in 12a) with the live CC-8 chat plan GATE, and 14 inherited planning/session.py from scope that predates CC-8 shipping on top of it. OWNER CHOICE, deliberately not made here because it DROPS a deliverable rather than making an impossible measurement honest: either narrow this atom to the seven loop/ modules plus planning/runner.py, or add an explicit predecessor that re-homes chat plan mode first. CLAUSE 2 IS UNSATISFIABLE AND WAS ALREADY DROPPED FOR 12a BY THE SAME RULING, then copied forward here verbatim: 'their example intents already seeding the eval fixtures' has nothing to harvest — measured, loop/classify.py and loop/code_classify.py contain NO example collection under any name, and intake_rigor / clarifying_questions / success_criteria / kind_config appear 0 times under evals/. The 2026-08-27 ruling records the drop for 12; 14's text never got the same edit. TEST MATERIAL AT RISK if this is ever rushed: 13 test files reference these modules or their routes, 58 tests in the six dedicated suites alone, and tests/test_usage_reachable_purposes.py names plan_walkthrough's planner session as a reachable spend purpose — a ratchet that treats it as live. ONLY TWO OF THE FIFTEEN IMPORTERS ARE ACTUALLY LEGACY: loop/manager.py and loop/watchdog.py, named by Phase 4. Even at the endgame Phase 4 rule 4 is 'block new loop CREATION only; keep read/cancel/cockpit forever — retirement is non-destructive by construction', so the read side survives. Everything else is a post-cutover REPLACEMENT surface (handlers/loop_routes.py registered 'in place of' the legacy routes), a live non-loop feature (dashboard/chat_plan.py), a kept module (planning/scratchpad.py, WF2UNI-9), or under active PP-16 work (loop/files.py). TWO SAFE PRE-SLICES exist today with zero behaviour change, if the atom should be shrunk before the drain: (1) decouple planning/__init__.py's 11 eager re-exports, which currently mean importing only planning.scratchpad leaves planning.session in sys.modules — proven dynamically — and which is what makes this a package-structure change rather than file deletions; (2) re-home the shared private helpers, since code_classify._parse_obj is imported cross-module by four *_plan_briefs.py files and agents/native/sdlc_tools.py reaches into code_classify._normalize_plan, with three near-duplicate _parse_obj implementations in grill.py, classify.py and code_classify.py. DEP MODELLING: its recorded dep WF2UNI-12 is `done` and models nothing, so the real dependency lived only in this prose. A resolved_edges entry PP-16 -> WF2UNI-14 is added alongside this correction so the graph carries it. |

## Intent — what this is for

The 12b residue of WF2UNI-12, split per owner ruling 2026-08-27 (question 6): deletion of the legacy planning/loop modules once loops drain onto workflow templates.

## Done when

planning/ (runner.py, session.py), loop/plan_walkthrough.py, loop/*_plan_briefs.py, and loop/classify.py + loop/code_classify.py are deleted as loops drain, with their example intents already seeding the eval fixtures.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2UNI-12`](WF2UNI-12.md) | intra-repo | `done` |
| `EXT:PROMPT-PACKS:PP-16 loop-to-WorkflowRun store seam (the Phase 3->4 drain)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 21 (WF2UNI) — code + tests observed on this machine

**Code evidence:**

- dag.json records this atom as `blocked` — the first blocked atom in this audit — and the code AGREES: all four named modules still exist (planning/runner.py, planning/session.py, loop/plan_walkthrough.py, loop/classify.py), which is what 'not yet deleted' looks like
- its declared blocker is genuinely open: PP-16 ('A Loop becomes a WorkflowRun') is `todo` in the same catalogue, so the Phase-3-to-4 drain this atom waits on has not happened

**Notes:** Confirmed as a correctly-recorded BLOCKED atom rather than as delivered work, which is the only thing an audit can confirm about work nobody has started. Two things had to hold together and both do: the deletions have not happened, and the dependency that would license them is still open. A stale blocker — one whose dependency had since landed — would have been the finding here; there isn't one.

## Recorded history

### Commits (3)

- `b9409e6c0` 2026-09-07 — docs(roadmap): WF2UNI-14 named the wrong blocker, and its scope would falsify a done atom
- `ad06a3cff` 2026-09-07 — feat(roadmap): make the dashboard say WHY an atom is not moving
- `ba1986f61` 2026-09-02 — docs(roadmap): flip five Wave-A atoms done; three plans complete; file WF2UNI-14

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1931` — - **[2026-09-04] ES-8 done-by-decomposition per owner ruling 2026-08-27 (question 2 of 11).** The ruling narrowed the trust-graduation gate to inputs that exist — ONE rung vocabulary, `guardrails/autonomy.py` (`rung_rank`/`rung_state`/`grant_rung`/`resolve_rung`), rather than minting a second mat…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:631` — - **[2026-09-02][WF2UNI-12] DONE (12a scope per owner ruling 2026-08-27) — plan_format retired; 12b residue filed as `WF2UNI-14`.**
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:637` — `WF2UNI-14`, blocked on the Phase-4 loop drain.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
