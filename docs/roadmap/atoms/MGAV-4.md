<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MGAV-4` — Knowledge-side deterministic alias pre-pass in the ingestion entities stage

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MEMORY-GRAPH-AND-VAULT](../plans/MEMORY-GRAPH-AND-VAULT.md) (`MGAV`, plan status `done`) |
| **Atom table** | [`../atomic/MGAV.md`](../atomic/MGAV.md) |
| **Execution position** | #348 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##118 |

## Intent — what this is for

§1.3 the alias index — knowledge-side pre-pass (knowledge/alias_prepass.py, knowledge/pipeline/runner.py)

## Done when

link_known_entities runs before the LLM EntityExtractor and unconditionally (incl. pool is None), using the same AliasIndex as memory, adding INSERT-OR-IGNORE mentions for already-known entities; snapshots (name,type,context) across clear_item_entities so a model-present ingest keeps deterministic links; bounded by MAX_INDEXED_ENTITIES/MAX_MENTIONS_PER_ITEM; validated no-model ingest links a declared-alias mention

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MGAV-1`](MGAV-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 4 (MGAV) — observed

**Code evidence:**

- tests/test_knowledge_vault.py passes (part of the 246-passed run)

**Notes:** PARTIAL: the alias pre-pass sits in the knowledge INGESTION entry path, so exercising it means ingesting a document. Not done this cycle.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `§1.3`. Those tokens appear in its source plan at:

- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:13` — Session 2 COMPLETE (recall arm 2026-07-28 + push reflex 2026-07-30); **the §1.3 knowledge-side alias
- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:38` — **Memory vs Knowledge boundary (user directive — load-bearing for this whole plan):** KNOWLEDGE = the user's personal items (documents, files, photos, notes; future providers: Google Drive, Google Photos). MEMORY = the harness's own internal mechanics (facts/facets/episodic/procedural/lessons abo…
- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:95` — 1. **Mention matching** against the alias index (§1.3): word-boundary matches of entity names, aliases, and @handles in the record's value/text. No LLM, no regex authored per entity — one compiled Aho-Corasick-style pass over the alias set (rebuilt on alias change, cached in-process).
- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:289` — | knowledge.db `entities`/`entity_relations`/`mentions` + `HybridRetriever` graph arm | **KEPT — enriched only** | deterministic alias pre-pass in the pipeline entities stage (§1.3); zero schema change |
- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:513` — pre-pass (§1.3's knowledge side), and the volunteer-stats table — the rest of Session
- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:609` — **Still NOT in this session** (the rest of §1.3 / later sessions): the knowledge-pipeline alias
- `§1.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:613` — - 2026-07-30 — **DONE (§1.3 remainder: the knowledge-side alias pre-pass).** The piece

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
