<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CRE-3` — Apps-repo CI + core rails mount (S2)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CI-RELEASE-ENGINEERING](../plans/CI-RELEASE-ENGINEERING.md) (`CRE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CRE.md`](../atomic/CRE.md) |
| **Execution position** | #133 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Sessions S2; Task breakdown Session 2 T2.1-T2.4/V2; Workflow set — apps repo; Contracts C1 rails job

## Done when

apps-repo .github/workflows/ci.yml has manifest-validate (all app.json parse via core apps/manifest.py), tests (core installed from git, vendor SDKs uninstalled, per-bundle to avoid basename collisions), boundary (SDK-only import lint); scripts/validate_manifests.py + check_sdk_boundary.py exist; core ci.yml rails job mounts the plan-32 residue sweep unguarded; corrupting a manifest or adding a core-internal app import turns the respective job red

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CRE-2`](CRE-2.md) | intra-repo | `done` |
| `EXT:PROVIDER-BOUNDARY-COMPLETION:residue rail test test_provider_boundary_residue` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.1`, `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:111` — | T2.1 | Manifest-validate script: iterate `*/app.json`, parse via core `apps/manifest.py` `from_dict`, fail listing offenders | apps repo: `scripts/validate_manifests.py`, workflow job | corrupting a fixture manifest on a branch turns the job red |
- `T2.4` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:114` — | T2.4 | Mount PROVIDER-BOUNDARY-COMPLETION's residue-sweep rail test as first-class in core `ci.yml` rails job (no guard) | `.github/workflows/ci.yml` | rails job runs the residue sweep unguarded |
- `Session 2` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:107` — ### Session 2 — Apps CI + rails

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
