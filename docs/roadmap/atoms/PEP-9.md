<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PEP-9` — React artifact build path

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PRODUCT-EXPERIENCE-PARITY](../plans/PRODUCT-EXPERIENCE-PARITY.md) (`PEP`, plan status `done`) |
| **Atom table** | [`../atomic/PEP.md`](../atomic/PEP.md) |
| **Execution position** | #426 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2078 |

## Intent — what this is for

Add a React build path for webapp artifacts: build once in a sandboxed, resource-limited spawn and store the emitted static bundle as the artifact's files (build-once-serve-static; no per-artifact dev server), served through the PEP-8 static route. A build failure surfaces a legible WHAT/WHY/FIX error rather than a hang. This is the unbounded-spawn hazard, so it must ride the shared resource-ceiling build-spawn profile and must not begin before that profile exists.

## Done when

A small React artifact builds and serves as static files through the deploy route and is interactable in-app; a build failure is legible, not a hang.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PEP-8`](PEP-8.md) | intra-repo | `done` |
| `EXT:EXECUTION-ISOLATION:resource-limited-build-spawn` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `3bbec41f1` 2026-08-25 — docs(roadmap): PEP-9 done — React artifact build path (PR #2078)
- `7e83a68de` 2026-08-25 — feat(artifacts): PEP-9 React artifact build path

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:845` — - [2026-08-25][PEP-9] DONE: SP5.3 — the React artifact build path. `artifacts/build.py` bundles a
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:856` — - [2026-08-25][PEP-9] DONE: the ceiling. The one spawn goes through
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:867` — - [2026-08-25][PEP-9] DISCOVERY: **a bounded build timeout needs `start_new_session`, not
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:875` — - [2026-08-25][PEP-9] DISCOVERY: **`--bundle` alone does not make the OUTPUT network-free.**
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:885` — - [2026-08-25][PEP-9] DEVIATION: SP5.1's `webapp` artifact kind is NOT added. `react` is
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:893` — - [2026-08-25][PEP-9] DEVIATION: the serve route's single-body fallback ("a one-file
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:900` — - [2026-08-25][PEP-9] DISCOVERY: no frontend change was needed for the failure to reach the
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:904` — - [2026-08-26][PEP-9] ✅ **ATOM FLIPPED `done` (PR #2078).** Integration re-gated on `origin/main` rather

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
