<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DCU-2` — Target policy, input-target screen, SEL-audit gate

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-COMPUTER-USE](../plans/DESKTOP-COMPUTER-USE.md) (`DCU`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DCU.md`](../atomic/DCU.md) |
| **Execution position** | #154 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

policy.py (§3.3-3.4): a self-plus-operator target-app allowlist via check_app, and a secure/password-field and sensitive-text screen via check_input_target run before any type/set-value; plus gate.py where require_computer_use records every attempt to the security event log (records, does not decide).

## Done when

Driving a non-allowlisted app refuses; typing or set-value into a secure/password field refuses; every attempt, allowed or refused, produces a SEL record.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:AUTONOMY-GUARDRAILS:SEL audit + safety profile` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`DCU-4`](DCU-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `775f47b8d` 2026-08-28 — docs(roadmap): rule three cross-plan blockers, one of them a fail-open
- `f4aa88da0` 2026-08-25 — fix(tools): adjudicate an atom only on entries it is the subject of
- `903cef2f1` 2026-08-25 — docs(roadmap): flip DCU-2 done — DCU-4 landing is the caller its gate was missing
- `8b4ca7b09` 2026-08-24 — test(computer-use): DCU-2 census the three screens' production call sites
- `22f28ad64` 2026-08-23 — feat(computer-use): DCU-2 target policy, secure-field screen and SEL audit

### Named by atom id in the roadmap prose (12)

- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:257` — - **2026-08-23 — `DCU-2` COMPLETE (all three clauses). Atom stays `todo` only because this code is
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:335` — `{"version":1,"enabled":true,"apps":["Mail"]}` must refuse with *"does not enforce"*. `DCU-2` enforcing
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:340` — - **2026-08-24 — `DCU-2` AUDIT: the deliverable IS on `main` (commit `22f28ad6`), so the entry above's
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:342` — rather than rebuilt: a branch carrying the DCU-2 commit rebased onto `main` reported *"dropping … patch
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:404` — `POST /api/computer-use/dispatch`, and the `mcp_core` aggregation entry. **`DCU-2`'s three screens now
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:415` — every other clause is completable without it and because the alternative — leaving `DCU-2`'s screens
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:433` — **RESOLVING THE TENSION `DCU-1` AND `DCU-2` BOTH LEFT OPEN.** `DCU-1`'s keystone rail binds every
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:449` — **GREEN**, which is exactly the single-"a row exists"-assertion failure `DCU-2`'s audit predicted.
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:506` — fully consumed**: `DCU-2` shipped `gate.require_computer_use` inert, and it now fires on every attempt
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:559` — - **[2026-08-25][`DCU-2`] DONE — its gate was inertness, and `DCU-4` landing IS the caller.** The atom's
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:561` — still reads blocked. The entry immediately below it rules otherwise: *"**`DCU-2`'s three screens now fire
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:808` — out-of-band enable document (agent-unwritable, absent = fail closed) and `DCU-2` shipped the target policy

### Its plan's own record, via the scope references (0 line(s))

Its scope cites `§3.3`, but no line of its source plan carries any of them.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
