<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2LOO-3` — Author the bundled loop templates (5 of 8)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-LOOPS-EVOLUTION](../plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md) (`WF2LOO`, plan status `done`) |
| **Atom table** | [`../atomic/WF2LOO.md`](../atomic/WF2LOO.md) |
| **Execution position** | #602 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Per-Kind Template Designs (goal-pursuit, general-project, design-project, diagnose); Implementation Effort Session 3

## Done when

Five bundled templates ship with runtime_hints + judge contract wired: goal-pursuit-open-ended, goal-pursuit-verifiable, general-project, design-project, diagnose-run; convention suite extended 6->11 templates; 132 structural integration tests green. Premise-mismatch corrections recorded: {{defaults.runtime_hints.*}} binding, gate_kind, and until-config forms do not exist in the engine, so rubrics are inlined into judge prompts with a test asserting every declared criterion reaches a judge.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2LOO-1`](WF2LOO-1.md) | intra-repo | `done` |

### Unblocks (5)

[`WF2LOO-10`](WF2LOO-10.md) (`done`), [`WF2LOO-14`](WF2LOO-14.md) (`done`), [`WF2LOO-5`](WF2LOO-5.md) (`done`), [`WF2LOO-6`](WF2LOO-6.md) (`done`), [`WF2LOO-9`](WF2LOO-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `fc02ccac6` 2026-09-06 — docs(roadmap): mint AR-9 council precursor; AR-1's dead ext gate becomes an owner call

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:1445` — caller, so as DATA the rubric is inert. As PROSE it does arrive: `WF2LOO-3`'s log claims "a test
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:1570` — **DEVIATION — the atom is filed as `WF2LOO-14` with a dep on `WF2LOO-3` only.** `WF2LOO-7`'s

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `Session 3`. Those tokens appear in its source plan at:

- `Session 3` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:832` — - **Session 3 — Author the 8 template YAML specs** (R5 code-project restructure, R11 design/test/TDD content, R12 diagnose, R15 monitor self-scheduling incl. the trigger tools module + bounds config through the four wiring points) + integration tests that run each through the engine; verify `unti…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
