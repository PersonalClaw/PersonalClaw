<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `ES-1` — Shared eval substrate: store, experiment-matrix runner, subprocess isolation fix, config + SEL wiring

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EVALUATION-SUBSTRATE](../plans/EVALUATION-SUBSTRATE.md) (`ES`, plan status `done`) |
| **Atom table** | [`../atomic/ES.md`](../atomic/ES.md) |
| **Execution position** | #241 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1.1 evals store, §1.2 experiment-matrix runner, §1.3 isolation fix; §10 EvalsConfig 4-point wiring + snapshot/portability listing + SEL hooks

## Done when

matrix runner (MatrixSpec/run_matrix) executes a scenario in a spawned child process with the PERSONALCLAW_WORKSPACE override in the child only (parent env never mutated); budget preflight, three-state passed/failed/verifier_absent aggregates, and per-cell artifact retention land under ~/.personalclaw/evals/matrices/; EvalsConfig round-trips through loader dataclass/load()/to_dict()/_EDITABLE_CONFIG; the evals/ store joins snapshot VALID_COMPONENTS/CORE_FILES and portability export (locked/ excluded); snapshot/restore round-trips it

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (10)

[`ES-10`](ES-10.md) (`done`), [`ES-11`](ES-11.md) (`done`), [`ES-13`](ES-13.md) (`done`), [`ES-17`](ES-17.md) (`done`), [`ES-2`](ES-2.md) (`done`), [`ES-3`](ES-3.md) (`done`), [`ES-4`](ES-4.md) (`done`), [`ES-5`](ES-5.md) (`done`), [`ES-7`](ES-7.md) (`done`), [`ES-8`](ES-8.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 16 (ES) — DRIVEN in a real browser

**Code evidence:**

- evals/runner.py:171-181 states the isolation contract and its reason: env construction is 'a BUILD rather than a copy' from a measured name allowlist, with PERSONALCLAW_WORKSPACE/PERSONALCLAW_HOME set on that fresh dict 'so both overrides exist in the child and nowhere else and the parent's own env is never touched'
- runner.py:180-182 argues the same decision from VALIDITY, not only security: an os.environ.copy() 'would hand a cell every credential the launching shell exported — a provider key a study never declared is a provider a study never declared, and a benchmark that reached one by accident could not say which model produced its result'
- evals/matrix.py contains no os.environ mutation at all; MatrixSpec/run_matrix + the three-state passed/failed/verifier_absent aggregate live there
- part of the 628-passed eval run (tests/ -k 'evals or eval_harness', 1 unrelated skip)

**Notes:** The best single piece of reasoning in this plan. One decision serves the credential boundary and the experiment's validity at once, and the docstring makes the second argument rather than assuming the first is enough.

## Recorded history

### Commits (5)

- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `a5821b589` 2026-09-06 — docs(roadmap): mint ES-17 — an eval cell cannot reach a bound provider; LV-7's premise was measured false
- `00eda6a29` 2026-09-04 — feat(guardrails): ES-13 per-scope trust record feeding rung decisions
- `b7c7ad574` 2026-09-04 — docs(roadmap): flip MC-7/DC-3/DC-4/APE-6 + ES-8 decomposition
- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (13)

- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:412` — ## Execution log — ES-1a (pure evals substrate: store + matrix types + EvalsConfig) — ES-1 split
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:414` — - **ES-1 DECOMPOSED (tick-14 design verdict); ES-1a DONE.** ES-1 split into 1a (pure store +
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:416` — child-process matrix runner + SpendMeter preflight + SEL). Design verified ES-1 startable now with
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:420` — - **ES-1a shipped:** new `evals/` package — `matrix.py` (the shared TYPES: frozen `MatrixSpec` with
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:433` — surface (the FE tab is a later atom) → no CHANGELOG. **Remaining:** ES-1b (child-process `run_matrix`
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:439` — ## Execution log — ES-1b (child-process matrix runner) — ES-1 COMPLETE
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:441` — - **ES-1b DONE; ES-1 (evals store + experiment-matrix runner + isolation fix) COMPLETE.**
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:442` — `evals/runner.py::run_matrix` composes ES-1a's types + store: expand axes cartesian product ×
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1715` — have existed since ES-1 for exactly this shape.
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1931` — - **[2026-09-04] ES-8 done-by-decomposition per owner ruling 2026-08-27 (question 2 of 11).** The ruling narrowed the trust-graduation gate to inputs that exist — ONE rung vocabulary, `guardrails/autonomy.py` (`rung_rank`/`rung_state`/`grant_rung`/`resolve_rung`), rather than minting a second mat…
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1980` — `ES-1`, and LV-6 §7 G1 already ruled that LV must not grow a second lever beside it — the same
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:211` — - **DISCOVERY (adjacent, not acted on) — `ES-1`'s atom row reads `todo` while its named machinery is
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:214` — exist, and `ES-2` (which depends on `ES-1`) is already `✅`. The genuinely missing piece is the

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `§1.1`, `§1.2`, `§1.3`, `§10`. Those tokens appear in its source plan at:

- `§1.1` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:100` — - Cells execute **sequentially** (single-user machine; also sidesteps any residual env-mutation concern) with per-cell wall-clock timeouts and a cost preflight against the guardrails `SpendMeter` (AUTONOMY-GUARDRAILS §1.1) when present — the matrix runner is a *client* of the budget floor, never …
- `§1.1` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:328` — - **Stores:** `~/.personalclaw/evals/` (§1.1) — `atomic_write` JSON + append-only TSV; added to `snapshot.py` `VALID_COMPONENTS`/`CORE_FILES` and `portability.py` export (locked/ excluded from export). Fixtures/qrels/results are **harness mechanics**: nothing here writes to `memory.db` or `knowle…
- `§1.1` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:385` — - **Loop 1 — scenario evals (lab):** grow `eval/scenarios/` into `~/.personalclaw/evals/scenarios/` (joins the §1.1 store), a versioned library over seeded fixture homes (the `tests_fixtures/` seed mechanism `--seed` already ships; scenarios reference a fixture home by name so runs are reproducib…
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:173` — 2. **Run a small fixed benchmark** through the matrix runner (§1.2): the component's owning template(s) replayed over their harvested suites with the component ON vs OFF (`arm_mask` axis). Component toggling is a config/spec **overlay applied only inside the child process** — the live template/co…
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:258` — The harness is a §1.2 matrix consumer (`scorer: "qrels"`, axes = arm_mask × k), so its reports land in the same `matrices/` + registry-table UI as everything else — one place to look, per amendment 1b's "shared between harness-ablation and local-model validation."
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:267` — - **Matrix**: fixtures × model tiers (every model bound or bindable to `eval_judge`/`background`, local tiers included) × iteration counts (`judge_samples` 1/3/5) — full per-run artifact retention (amendment 1a), run through §1.2.
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:290` — `loop` node wrapping: **propose** (subagent or BYO runner emits a candidate edit + LEARN-R16 change manifest with `predicted_fixes`/`risk_tasks`) → **diff + scope-check** (the engine's `allowed_write_paths` snapshot/diff → `scope_violation` terminal state — WORKFLOWS-V2 §write-scope, already spec…
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:317` — | Experiment-matrix machinery | **ONE runner** (§1.2) shared by §2/§3/§5/§6/§7 and available to LOCAL-MODEL-MANAGER-V2 validation ("does the local 20b handle this template or time out" — the MetaHarness result class) |
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:398` — Executed via the §1.2 matrix runner in §1.3's subprocess isolation (unchanged); results persist as artifacts under `matrices/` + a `results.tsv` row carrying the pin — "did anything change" is a pin-diff query. This extends §3.2's model fingerprint from a watchdog key to a universal run attribute.
- `§1.2` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:989` — that is a benchmark rather than a scenario cannot satisfy. So the harness is a §1.2 *consumer* the way
- `§1.3` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:45` — **The re-open, stated honestly.** LEARNING-FLYWHEEL §3.4 demoted the eval regression gate as "CI/CD machinery sized for a team," betting that GateOK + canary revert + the harvested suite cover the risk. The research corpus (17 sources) converges on the counterargument: **self-improvement without …
- `§1.3` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:310` — | Flywheel §3.4 contingent eval gate | **RE-OPENED, re-sized** — not team CI: harvested inputs, k≈5 paired runs, child-process isolation (§1.3 discharges §3.4's named precondition). GateOK/canary/harvested-suite (LEARN-R2) stay as the always-on cheap tier; studies are the deliberate instrument ab…
- `§1.3` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:398` — Executed via the §1.2 matrix runner in §1.3's subprocess isolation (unchanged); results persist as artifacts under `matrices/` + a `results.tsv` row carrying the pin — "did anything change" is a pin-diff query. This extends §3.2's model fingerprint from a watchdog key to a universal run attribute.
- `§1.3` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:446` — entrypoint running ONE cell via the reused `EvalRunner`. **§1.3 isolation:** each cell spawns with
- `§1.3` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:520` — `EvalRunner.run_scenario` mutating `PERSONALCLAW_WORKSPACE` in the calling process (§1.3). A judge
- `§10` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:400` — - **Loop 3 — live quality (field):** derived metrics, computed by query, stored nowhere new (the §4.4 discipline): per-template/per-action-type 👍/👎 rate + edit-before-approve rate from plan 58's FEEDBACK-SIGNAL store, plus approval/rejection/undo rates from the earned-autonomy ledger. Rendered BE…
- `§10` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1246` — asks for, but the FE half of §10 is untouched by this atom. (iv) The template's propose stage needs a

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
