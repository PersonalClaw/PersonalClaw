<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-12` — Add `Lease` and `Dwell`/`MetricGate` admission policies

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #470 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1556 |

## Intent — what this is for

With the `PP-11` seam in place, the two capabilities that live in the other schedulers become expressible in the frontier. Leases exist only in `pool.py`, so "cap this fan-out because each item holds a rate-limited endpoint" is inexpressible today — lane caps protect the engine and `max_concurrency` protects the run's shape, but neither models a resource an item HOLDS. Dwell/metric gating exists only in `loop/tick.evaluate` and is consumed by exactly one kind (`kinds/sdlc.py:_tick_decide`), so a workflow loop cannot bake a step or roll back on a regressed metric.

## Done when

`Lease(ttl)` reuses `pool.py`'s proven compare-and-swap decision functions rather than a second lease implementation (S57 measured `unlink`-based single-use failing 36 of 40 races, and a lease that loses a race is worse than no lease), and `Dwell`/`MetricGate` reuse `loop/tick.StepConfig`'s parsed thresholds. Both are additive: a spec declaring neither behaves exactly as before, proven by re-running `PP-11`'s golden frontier file. Verified by a fan-out that genuinely serializes on a leased resource across a gateway restart, and by a metric regression rolling a step back inside a workflow run.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-11`](PP-11.md) | intra-repo | `done` |

### Unblocks (2)

[`PP-13`](PP-13.md) (`done`), [`PP-15`](PP-15.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- 🔑 CAPABILITY LANDS 'ON THE SEAM ALREADY PROVEN INERT' — the two-step is the point: PP-11 is verifiable precisely because it adds nothing, and PP-12 is safe precisely because it lands on something verified
- the three new policies are the capabilities the four old schedulers each held alone: `Lease` (exclusive occupancy of a named external resource — the pool's), `Dwell` (a bake floor) and `MetricGate` (a metric gate that rolls a step back — the loop's)
- 🔴 'Both REUSE the proven implementation rather than' reimplementing it, so the loop's gate semantics do not fork when they move behind the interface
- the admission suites are green inside the 244
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not a browser surface.

**Notes:** A lease is the one policy here with a correctness rather than a pacing role — two runs holding one external resource is corruption, not slowness — which is why it belongs in admission rather than in a retry.

## Recorded history

### Commits (4)

- `89b1acf7b` 2026-08-17 — chore(roadmap): cite the landing batch #1556 on PP-12
- `d69a20a66` 2026-08-14 — feat(workflows): PP-12 add Lease, Dwell and MetricGate admission policies
- `5387e5980` 2026-08-15 — docs(roadmap): record PP-12 BLOCKED — the lease's guarantee is an owner decision
- `7042fb288` 2026-08-14 — refactor(workflows): PP-11 compose admission from an ordered policy list

### Named by atom id in the roadmap prose (14)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:72` — | Task-pool leases | TASKS-SOPS `WF2TAS-6`/`-10` (done) | **Reused**: `PP-12` adopts the shipped compare-and-swap lease decision functions as an admission policy; `PP-13` retires only the duplicate projection. |
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:118` — cannot be verified. `PP-12` adds `Lease` and `Dwell`/`MetricGate` on the proven seam. `PP-13` retires
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:143` — └─→ PP-11 ─→ PP-12 ─→ PP-13 ───────┤
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:889` — by minimum is the only rule an added policy cannot loosen, which is the property that lets `PP-12`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:937` — `MetricGate` are `PP-12`, and `pool.py`'s private projection is `PP-13` — this atom only makes the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:947` — the seam has the PROPERTY `PP-12` will rely on. Neither alone is sufficient, which is worth knowing
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:948` — before `PP-12` appends `Lease` to the list.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:964` — `personalclaw/sdk/`, so no app can be depending on it. `PP-12` is the entry that will have
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1202` — - [PP-12] **BLOCKED — owner decision needed on what guarantee `Lease` provides.** An implementation of
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1213` — it is neither a contention flake nor a rebase regression. The branch's `dag.json`/`PP.md` flip PP-12 to
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1235` — - [PP-12] **DONE — ruled OPTION (1), and the diagnosis above was measured against a MUTATED tree.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1286` — flipped to PP-12-done while the atom itself still read `todo`** — the pair reds
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1298` — (`acquire`, `Lease`, the TTL constants — `PP-12`), plus two comment-only mentions in `triggers/`.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1372` — 4 skips `PP-12` recorded). The 13 relocated projection tests were rewritten against the core, not

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
