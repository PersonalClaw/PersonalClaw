<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-2` — S2 read-state + favorited display and filters

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #303 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 — T2.1 (read_state + favorited); Design §S2

## Done when

favorite gets its own star glyph (distinct from pin), a reading badge marks in-progress, read items dim; Reading/Unread/Read/Favorites filter chips with live counts appear only when the state is present; reader has a read-state cycle + favorite toggle routed through the non-touching curation endpoints (updated_at verified unchanged); pin has no retrieval weighting so favorite-vs-pin distinctness holds by inspection

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-1`](KL-1.md) | intra-repo | `done` |

### Unblocks (1)

[`KL-8`](KL-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:13` — COMPLETE** — T2.1 curation display/filters, T2.2 tags taxonomy, T2.3 bulk ops, all 2026-07-29.
- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:94` — | T2.1 | read_state + favorited store API + routes; item rows/reader show state; toggles persist | `knowledge/store.py`, handlers, frontend | mark unread→reading→read; favorite distinct from pin (verify weighting unaffected) |
- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:298` — **NOT done: T2.1's remainder and T2.2.** T2.1 is ~85% shipped by S1 — what's left is
- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:321` — - 2026-07-29 — **DONE (S2: T2.1 remainder).** S1 shipped the store API, routes and
- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:350` — **Premise correction carried from the audit:** the plan's T2.1 done-when says "verify
- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:365` — **T2.1 is now complete.** Remaining in S2: **T2.2 (tags taxonomy)**, which still needs
- `T2.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:468` — **S2 is now COMPLETE** (T2.1 + T2.2 + T2.3). Remaining in the plan: Session 3
- `Session 2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:12` — **Status:** IN PROGRESS — Session 1 (collections + item curation) shipped 2026-07-29; **Session 2
- `Session 2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:90` — ### Session 2 — Curation + taxonomy + bulk

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
