<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-6` — Bootstrap install.sh (uv-first, --container flag) + host at website /install

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #183 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #5bc4c98 |

## Intent — what this is for

Design §E; Session 2 T2.2 (cross-repo deliverable)

## Done when

`sh -n`/`dash -n` clean; `sh install.sh` on bare ubuntu+macos reaches a working CLI; `--container` prints the compose snippet; idempotent re-run upgrades; served as text/plain at personalclaw.dev/install

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:DISCOVERABILITY:plan 36 owns the website repo that hosts /install` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/DISTRIBUTION.md:161` — | T2.2 | Bootstrap `install.sh`: POSIX sh, `set -eu`; OS/arch detect; uv presence check → official installer if absent; `uv tool install personalclaw`; print next steps + offer `personalclaw setup`; `--container` prints compose snippet; idempotent | website repo: `public/install` (plan 36 owns th…
- `T2.2` `docs/roadmap/plans/DISTRIBUTION.md:250` — - **T2.1 / T2.2 (owner + website repo):** T2.1 (first real PyPI publish + `uv tool install` / `pipx` verify on a clean machine) is an owner real-world step — owner states publishing is handled (pending publisher registered 2026-07-20, env `release`). T2.2 (`install.sh` bootstrap) lives in the per…
- `T2.2` `docs/roadmap/plans/DISTRIBUTION.md:285` — - **DONE T2.2 (bootstrap `install.sh` — content produced)** — Wrote the actual bootstrap script at `deploy/website/install.sh` (staged in-repo for the website repo, plan 36): POSIX sh (`set -eu`, validated with `sh -n` AND `dash -n`), OS/arch detect, `uv` presence check → official `astral.sh/uv/i…
- `T2.2` `docs/roadmap/plans/DISTRIBUTION.md:308` — | T2.2 | `5bc4c98` | `deploy/website/install.sh` bootstrap + cross-repo instruction README |
- `T2.2` `docs/roadmap/plans/DISTRIBUTION.md:332` — The two T2.2 / T1.4 sibling-repo deliverables that closeout had left staged (they land
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:156` — ### Session 2 — PyPI + bootstrap + client
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:245` — ### Session 2 — Cycle 4

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
