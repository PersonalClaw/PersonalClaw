<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-5` — T2.1 Suggested organization: deterministic→LLM folder/tag proposals via inbox proposal contract

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #536 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 (T2.1); Design §S2 (suggested organization), Integration points (emit_attention_item kind=proposal)

## Done when

An untagged session gets a sensible folder/tag proposal from deterministic heuristics (title keywords, workspace dir, channel origin) with an LLM suggestion only when ambiguous, surfaced as an inbox proposal item (ItemKind.PROPOSAL) or an inline accept chip; accept applies, ignore leaves it; never auto-applies. New session_organize.py + frontend chip.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:INBOX-NOTIFICATIONS-UNIFICATION:emit_attention_item(kind=proposal)/ItemKind.PROPOSAL/InboxItem.item_kind (S1-S5 landed 2026-07-30 — edge now satisfied)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 24 (SM) — code, tests and the history page driven

**Code evidence:**

- the proposal path is deterministic-first — title keywords, workspace dir, channel origin — with 'an LLM suggestion only when ambiguous', so the common case costs no model call
- 167 tests pass across the session-search / export / share / template / retention / price-key / tool-name modules, plus 37 in the archived-integrity, resurrection-audit and sessions-search suites

**Driven in the UI:** Not driven: a folder/tag proposal needs an untagged session to propose about.

**Notes:** Partial. The deterministic-then-LLM ordering is the same cost posture ES-10's capture and WF2UNI-9's triage took: spend a model call only where a rule genuinely cannot decide. Routing the proposal through the inbox rather than applying it silently is the other half — a suggestion the user accepts is not the same as an automatic reorganisation.

## Recorded history

### Commits (2)

- `6a606c078` 2026-08-12 — docs(roadmap): correct three stale gates and record why no atom is startable (#1193)
- `e116fd8ed` 2026-08-10 — feat(sessions): SM-5 suggested folder/tag organization (#1041)

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:16` — **T2.1 (suggested organization) shipped 2026-08-11** as atom SM-5 — the plan's last unbuilt task. Its
- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:218` — **↳ RESOLVED 2026-08-11 by `SM-5`** — Inbox-Unification landed the contract this
- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:337` — 2026-08-11 as `SM-5`; the DONE entry is below in this same log ("suggested organization ships,
- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:345` — - [2026-08-11][SM-5 / T2.1] DONE: suggested organization ships, and this plan has no unbuilt task left. Organization was all-manual, so sessions accumulated with neither folder nor tag and stopped being findable — the gap this plan's own Context names as "organization is all-manual".
- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:361` — **Never auto-published — proven, and the proof was checked for teeth.** `share_session` has exactly one call site: the explicit `POST /api/chat/sessions/{session}/share` handler. `test_share_has_exactly_one_call_site` is an AST census over every `.py` under `src/personalclaw`, counting both the a…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:1325` — **Verification notes worth keeping.** (a) A narrow test selection hid 5 real reds: the first targeted run passed at 245, and adding the workflow-tool/manifest suites surfaced them. Worse, a nonexistent path (`tests/test_mcp_workflows.py`) made pytest report `collected 0 items` — a zero that reads…

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:16` — **T2.1 (suggested organization) shipped 2026-08-11** as atom SM-5 — the plan's last unbuilt task. Its
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:85` — | T2.1 | Suggested organization: deterministic heuristics → LLM only if ambiguous → propose (accept chip or inbox proposal); never auto-applies | new `session_organize.py`, frontend chip | an untagged session gets a sensible folder/tag *proposal*; accept applies, ignore leaves it |
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:211` — - **DEVIATION — T2.1 (suggested organization) NOT taken; it is BLOCKED.** Its spec
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:217` — that plan lands. (Note `chat_retag.py` already covers much of T2.1's user value.)
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:219` — waited on, and T2.1 shipped. This entry stays as the record of why it waited; it is
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:332` — ~~**The plan is now COMPLETE except T2.1 (suggested organization)**, which stays BLOCKED on
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:336` — **SUPERSEDED — corrected 2026-08-12. The plan is COMPLETE, full stop.** T2.1 shipped on
- `T2.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:341` — this file still described T2.1 as unbuilt — including the S2 DEVIATION above, which was
- `Session 2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:13` — 2026-07-28; Session 2 T2.2/T2.3 (bulk ops + lifecycle + auto-archive) shipped 2026-07-29; Session 3
- `Session 2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:81` — ### Session 2 — Smart organization + bulk + auto-archive
- `Session 2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:157` — matched terms marked. Windowing is deferred to Session 2 with a measurement first —
- `Session 2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:159` — adding virtualization to an un-profiled list is speculative work, and Session 2

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
