<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SH-1` — Keychain credential backend selector behind save_credential/read, keyring optional extra, headless fail-closed to .env 0600, doctor reports active backend

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SECURITY-HARDENING](../plans/SECURITY-HARDENING.md) (`SH`, plan status `in_progress`) |
| **Atom table** | [`../atomic/SH.md`](../atomic/SH.md) |
| **Execution position** | #511 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1419 |

## Intent — what this is for

Session 1 — OS keychain credential storage / T1.1; Design S1; Contracts C1 (credential backend selector)

## Done when

reads are backend-transparent; headless fixture (no keyring) falls back to .env 0600 with a doctor warning (never plaintext-elsewhere); doctor reports the active backend; unit tests cover both backends; keyring added as optional extra in pyproject.toml

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`SH-2`](SH-2.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `eabfce171` 2026-08-28 — feat(secrets): EI-10 secrets vault — presence-only API + Settings surface
- `a15d5bfba` 2026-08-28 — docs(roadmap): strike two EXT deps that pointed at work the atoms already own
- `df8cdb5e7` 2026-08-20 — docs(roadmap): SH-2 is unauthorized, not blocked — and a late amendment lands nowhere
- `acd00884a` 2026-08-15 — build(deps): lock the SH-1 keychain extra so --locked installs resolve
- `e767f4bc2` 2026-08-15 — feat(security): SH-1 OS keychain credential backend with fail-closed .env fallback

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1316` — `security.credential_keychain` config (`SH-1`, done), `{{secret:KEY}}` resolution
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1325` — **RULED: strike the dep.** `EI-10` owns the per-project rows, layered over the keychain backend `SH-1`
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1372` — (SH-1), the `.env` 0600 floor, the union read, and the inventory's `secret=True` projection into
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:162` — SH-1's reads are the union of both stores with the **keychain preferred**. So the mismatch path
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:174` — real and is the persisted user opt-in — the plan asked for that in T1.3 and SH-1 explicitly
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:417` — ### 2026-08-15 — SH-1 (T1.1 / Design S1 / Contract C1) credential backend selector — DONE
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:419` — - **[SH-1] DONE:** C1 lands in `config/loader.py`: `CredentialBackend = Literal["keychain",
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:497` — diff is SH-1 `todo`→`done`, the SH ready-frontier moving to SH-2, and SH `done` 3→4 / `todo` 6→5.
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1060` — difference and hides an option. Verified: `SH-1` is `done`, `SH-2` has no unmet non-EXT dep, and

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T1.1`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/SECURITY-HARDENING.md:65` — | T1.1 | Keyring backend behind the credential API: `save_credential`/read gain a backend selector (`keychain` | `dotenv`), `keyring` as an optional extra; **absent secret service → fall back to `.env` 0600 with a doctor warning (never plaintext-elsewhere)** | `src/personalclaw/config/loader.py`,…
- `T1.1` `docs/roadmap/plans/SECURITY-HARDENING.md:417` — ### 2026-08-15 — SH-1 (T1.1 / Design S1 / Contract C1) credential backend selector — DONE
- `T1.1` `docs/roadmap/plans/SECURITY-HARDENING.md:449` — 1. **No config field; the opt-in is `PERSONALCLAW_CREDENTIAL_BACKEND`.** T1.1's sibling T1.2/T1.3
- `Session 1` `docs/roadmap/plans/SECURITY-HARDENING.md:61` — ### Session 1 — OS keychain credential storage

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
