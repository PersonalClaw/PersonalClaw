<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PL-1` — /api/manifest self-description endpoint + drift test

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-LEGIBILITY](../plans/PLATFORM-LEGIBILITY.md) (`PL`, plan status `done`) |
| **Atom table** | [`../atomic/PL.md`](../atomic/PL.md) |
| **Execution position** | #453 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1 ("/api/manifest — the self-description endpoint"), §1.1 sections table, §1.2 drift test, §1.3 typed-envelope discipline; §9 Session 1

## Done when

GET /api/manifest returns {apiVersion,tools,routes,app_surfaces,providers} generated from list_all_tools()+route table+extension registry; test_api_manifest_drift.py reds when a tool/route ships without a TOOL_META/exclusion entry; canonical-route leak regression green; FE api.manifest() typed

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:WORKFLOWS-V2:embed workflow_manifest under tools[].workflow_manifest_ref when WF2-R12 lands (federate, not rebuild)` | cross-plan (EXT) | **unresolved** |

### Unblocks (6)

[`PL-2`](PL-2.md) (`done`), [`PL-3`](PL-3.md) (`done`), [`PL-4`](PL-4.md) (`done`), [`PL-5`](PL-5.md) (`done`), [`PL-6`](PL-6.md) (`done`), [`PL-7`](PL-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `§1`, `§1.1`, `§1.2`, `§1.3`, `§9`, `Session 1`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:26` — - **NEW-13 core (a)** (`/api/manifest` generated from actual registries + drift test; typed doc objects for UI primitives; token registry behind `ui_search`/`ui_get`) → §1, §5. Sources: `meta-astryx` (capability manifest from Commander metadata + `manifest.test.mjs` CI drift test; typed `.doc.mjs…
- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:42` — - **Tool schemas are typed but hand-scattered:** `validation.py` holds ~40 `ToolSchema` constants; `mcp_core.py:_list_tools()` (line 112) hand-writes tool dicts for `skill_invoke`/`skill_search`/`hook_register`. These are inputs to §1, and the drift test is what keeps them honest.
- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:107` — - **Action-provider errors:** `ActionResult` (`action_providers/base.py:ActionResult`) gains the same optional field; the three dispatch seams (`hooks.py`, `gateway.py`, `event_triggers.py`) wrap uncaught provider exceptions into a generic envelope so **app-contributed providers inherit it withou…
- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:157` — - **Drift closes the audit loop:** a startup/`/update` check compares the app's *declared* routes against the backend's live route table (probe the app's `/health`-style introspection or match on first proxy 404) and files a warning notification for undeclared or dead-declared routes — the manife…
- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:172` — - **Telemetry inputs (all existing or free by-products):** `skills/.usage.json` (`SkillUsageStore`), a new per-tool invocation counter (same sidecar pattern, `~/.personalclaw/tool_usage.json`, incremented at the `list_all_tools()`-fed invoke path — best-effort like skill usage), and the §1 manife…
- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:203` — - **Session 1 — manifest + drift (§1):** `/api/manifest` generator over `list_all_tools()` + route table + extension registry; `manifest_meta.py` response-type/examples map; `test_api_manifest_drift.py`; FE `api.manifest()`.
- `§1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:244` — ### 2026-07-24 — S1 (§1) manifest + drift — DONE
- `§1.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:107` — - **Action-provider errors:** `ActionResult` (`action_providers/base.py:ActionResult`) gains the same optional field; the three dispatch seams (`hooks.py`, `gateway.py`, `event_triggers.py`) wrap uncaught provider exceptions into a generic envelope so **app-contributed providers inherit it withou…
- `§9` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `Session 1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:203` — - **Session 1 — manifest + drift (§1):** `/api/manifest` generator over `list_all_tools()` + route table + extension registry; `manifest_meta.py` response-type/examples map; `test_api_manifest_drift.py`; FE `api.manifest()`.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
