<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AP-2` — Import core: inspect-without-write, leaves-first commit, rollback, ref-integrity lint, scan

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-PACKS](../plans/AGENT-PACKS.md) (`AP`, plan status `done`) |
| **Atom table** | [`../atomic/AP.md`](../atomic/AP.md) |
| **Execution position** | #35 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #915 |

## Intent — what this is for

§3.1 Pipeline; §3.2 Referential-integrity linter; §3.5 Trust scan-everything-tier-by-origin; §8 skills path (PackMarketplace→install_guarded). Session 2. Success criteria 2, 3.

## Done when

import runs inspect (dry-run, no writes) then quarantine→integrity recompute→packs/lint.py ref-integrity lint→leaves-first commit with per-write journal at packs/.installing/<id>.json; a DANGEROUS-pattern skill is refused regardless of consent and WARNING needs explicit consent; fault-injected mid-import unwinds every journaled write to byte-identical pre-import state; skills commit through PackMarketplace→install_guarded producing .pclaw-lock.json; fresh-id rewriting on parsed objects (never raw bytes); triggers land disabled and config_subset staged; every step SEL-audited.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AP-1`](AP-1.md) | intra-repo | `done` |

### Unblocks (2)

[`AP-3`](AP-3.md) (`done`), [`AP-4`](AP-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `9d2a520d3` 2026-08-16 — feat(packs): AP-4 pack kinds — Domain OS packs, rosters, prompt cards, one-link
- `57e77db23` 2026-08-08 — feat(packs): AP-2 pack import core — inspect-without-write, journaled leaves-first commit, trust scan (#915)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:380` — `AP-6` rows (one ⬜, one ✅ each) and shows `AP-1`/`AP-2`/`AP-3` as ⬜ despite being done. Both
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:393` — **DISCOVERY — §4.2's stated seam is wrong against code, and AP-2 already handled it.** §4.2 says
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:399` — *intent* is reachable, just not through one store. AP-2 already committed pack agents to the
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:426` — AP-2 shape, found only because AP-4's done_when demands the exact ref be named.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§3.1`, `§3.2`, `§3.5`, `§8`, `Session 2`. Those tokens appear in its source plan at:

- `§3.1` `docs/roadmap/plans/AGENT-PACKS.md:216` — - A match **proposes** — a card in the project hub / pack store ("This looks like a Terraform project — the `infra-ops` pack matches, 0.9 confidence, here's what it would install") with the full §3.1 inspect report behind it. Never auto-installs, never auto-enables. Rejections are remembered per …
- `§3.1` `docs/roadmap/plans/AGENT-PACKS.md:257` — | **AUTOMATION-SUBSTRATE** (approved) | Pack triggers target whichever store exists (dual-write shim until `triggers.json`); always disabled on import; its never-throw trigger validation is the model for §3.1's lenient component parse. |
- `§3.1` `docs/roadmap/plans/AGENT-PACKS.md:269` — - **Session 2 — import core (§3.1-3.2, §3.5):** inspect-without-write; quarantine + integrity recompute; referential-integrity linter; leaves-first commit with write-journal rollback; fresh-id rewriting; `PackMarketplace` adapter through `install_guarded`; trigger/config staging semantics; SEL wi…
- `§3.2` `docs/roadmap/plans/AGENT-PACKS.md:126` — → referential-integrity lint (§3.2) → supply-chain scan (§3.5)
- `§3.2` `docs/roadmap/plans/AGENT-PACKS.md:168` — `agents/catalog.json` (the agency-agents divisions.json shape: `{slug, name, description, label, icon, color, activation}`) + one persona markdown per agent + optional scenario runbooks with **staged rosters** (`activation: always | phase-N | as-needed`). Import path honors the real seam: AgentDe…
- `§3.2` `docs/roadmap/plans/AGENT-PACKS.md:225` — - **Action providers:** this plan adds **none**, so `ALLOWED_HOOK_PROVIDERS` (`src/personalclaw/validation.py`) is unchanged — restated because pack-shipped *triggers* reference action providers by name, and the importer's lint (§3.2) validates every referenced provider name against that frozense…
- `§3.5` `docs/roadmap/plans/AGENT-PACKS.md:126` — → referential-integrity lint (§3.2) → supply-chain scan (§3.5)
- `§3.5` `docs/roadmap/plans/AGENT-PACKS.md:269` — - **Session 2 — import core (§3.1-3.2, §3.5):** inspect-without-write; quarantine + integrity recompute; referential-integrity linter; leaves-first commit with write-journal rollback; fresh-id rewriting; `PackMarketplace` adapter through `install_guarded`; trigger/config staging semantics; SEL wi…
- `§8` `docs/roadmap/plans/AGENT-PACKS.md:133` — - **Lock stamping:** every installed component gets the flywheel's `{source, computedHash}` lock; skills additionally get their standard `.pclaw-lock.json` because they commit **through `install_guarded`'s file-writer path** (`install_skill_files`, which re-scans per file — defense-in-depth inher…
- `§8` `docs/roadmap/plans/AGENT-PACKS.md:206` — - Catalog list lives in config (`packs.skill_catalogs: list[{name, url, kind}]` — four wiring points, §8); the Skills store UI gains a source filter + per-source counts. Search is client-side over the fetched index (personal-scale; no search service).
- `§8` `docs/roadmap/plans/AGENT-PACKS.md:245` — | Config | `config.json` → `packs` section | `PacksConfig` | four wiring points (§8) |
- `§8` `docs/roadmap/plans/AGENT-PACKS.md:254` — | **DURABILITY-AND-SYNC** (approved) | Orthogonal: backup/sync of your own state vs sharing with others. Pack stores register in its inventory (§8). Its shard format is NOT the pack format (shards are lossless self-state; packs are redacted compositions). |
- `§8` `docs/roadmap/plans/AGENT-PACKS.md:258` — | **AUTONOMY-GUARDRAILS** (approved) | Shared scan rules (§2.2), guard-flag parse tenet (§8 config), profiles/budgets governing any pack-shipped trigger the user enables. |
- `§8` `docs/roadmap/plans/AGENT-PACKS.md:290` — | Silent config drop (four-wiring-points gotcha) | Explicit checklist §8; schema reachability tests enforce `_meta`; list-element `_meta` per the ProjectionRuleConfig precedent |
- `Session 2` `docs/roadmap/plans/AGENT-PACKS.md:269` — - **Session 2 — import core (§3.1-3.2, §3.5):** inspect-without-write; quarantine + integrity recompute; referential-integrity linter; leaves-first commit with write-journal rollback; fresh-id rewriting; `PackMarketplace` adapter through `install_guarded`; trigger/config staging semantics; SEL wi…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
