<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AG-1` — Model-call chokepoint core (§2): ModelCallGuard, breaker, timeout, audit, output_type

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AUTONOMY-GUARDRAILS](../plans/AUTONOMY-GUARDRAILS.md) (`AG`, plan status `done`) |
| **Atom table** | [`../atomic/AG.md`](../atomic/AG.md) |
| **Execution position** | #21 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2.1 seam at provider_bridge return; §2.3 per-provider three-state breaker + hard timeout; §2.4 output_type on one_shot_completion + structured_output capability-dispatch hook; §2 attempt-level model_calls.jsonl audit; migrate top parse_llm_json call sites; judge bounded-reasoning field (Session 1)

## Done when

ModelCallGuard wraps the bridge-resolved reasoning-use-case provider; breaker/hard-timeout/JSONL audit live; one_shot_completion(output_type=…) returns typed data via targeted retry with zero silent None at migrated sites; full gate green (7820 passed) — DONE per Execution log 2026-07-25

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (3)

[`AG-2`](AG-2.md) (`done`), [`AG-4`](AG-4.md) (`done`), [`AG-9`](AG-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 9 (AG) — observed

**Code evidence:**

- tests/test_guardrails_model_call.py passes (part of 333 tests passing across eleven AG modules)

**Notes:** The model-call chokepoint and breaker are pre-model-call machinery, so their own suite is the right instrument. Confirmed on that rather than on the plan's log.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:520` — `AG-1`…`AG-13` are all `done` in `dag.json`, so Autonomy-Guardrails does not gate this.
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:316` — Dep (guardrails `model_calls.jsonl` + `ModelCallGuard` seam, AG-1) verified SHIPPED; the seam

### Its plan's own record, via the scope references (19 line(s))

This atom's scope cites `§2.1`, `§2.3`, `§2.4`, `§2`, `Session 1`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:312` — typed output. Interactive chat stream untouched (out of scope v1, per §2.1). Clean
- `§2.3` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:128` — scan (PII/secret, §2.2) → circuit-breaker check (§2.3) → meter (§1.1) →
- `§2.4` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:129` — call with hard timeout → output-contract validation (§2.4) →
- `§2.4` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:375` — DECLARATION deferred to a coordinated apps-repo change.** §2.4/§8 name "ollama +
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:12` — **Status:** IN PROGRESS — Sessions 1-4 landed 2026-07-25 (see `## Execution log`): the §2 model-call
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:13` — chokepoint, §1.1 budgets, §2.2 scan, §1.2 denylist, §1.3 incident kill switch, §1.4
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:14` — DISABLE_LIVE_WRITES and §2.5 provider health are all wired to real callers and surfaced in
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:43` — - **NEW-2** (Model-Call Control Chokepoint: metering, per-provider circuit breaker, hard timeout, failure-mode-classified targeted retry, ordered fallback with degraded provenance, attempt-level JSONL audit; `structured_output` capability + `output_type` on `one_shot_completion` with capability-d…
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:44` — - **NEW-2 amendments5** (composable secret/PII scan wrapper, WARN/REDACT/BLOCK, at the model-call seam) → §2.2.
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:55` — 2. **The Model-Call Control Chokepoint (§2)** — the LLM twin of `net.fetch`: one guard wrapping every background/tool LLM call (`one_shot_completion`, `eval/judge.py:LLMJudge`, planners, synthesizers) with scan → breaker → meter → call → validate → targeted retry → fallback → audit.
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:57` — **Soul guardrail:** this is a *personal* safety floor — one user, one gateway, config files plus one policy check per seam. No ops console, no RBAC, no fleet dashboards. The provider health view (§2.5) is a Settings panel derived from files already on disk, not telemetry infrastructure.
- `§2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:78` — - **What exists today (build on, don't reinvent):** wall-clock ceilings already exist per cron job — `timeout_secs` (default 1800, clamp 1..86400, `schedule.py:_execute_with_timeout` L1241) plus the reaper (`start_reaper` L550, killpg escalation). These are KEPT and become the `max_wall_secs` enf…
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:259` — - **Session 1 — the chokepoint core (§2):** `ModelCallGuard` at the bridge seam; hard timeout; per-provider breaker; attempt-level JSONL audit; `output_type` on `one_shot_completion` with capability dispatch (`structured_output` capability declared for ollama + OpenAI-wire branded apps); migrate …
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:264` — Each session ships independently; Session 1 alone is a Wave-0 win (typed outputs + fail-fast on provider outages).
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:309` — ### 2026-07-25 — Session 1 (§2 chokepoint core) — DONE
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:384` — core provider can declare) and matches the plan's own framing that "Session 1 alone is
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:418` — +18 new; the only 2 reds are the pre-existing subagent SEL xdist flake from Session 1,
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1167` — **DISCOVERY — the "capability-dispatch hook" Session 1's DEVIATION says core shipped does not
- `Session 1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1361` — Session 1 (model-call chokepoint), Session 2 (budgets + scan + config), Session 3 (denylist

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
