<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-8` — Slices 9+10+11 — templates + conventions pack + advanced constructs + context-lifecycle (partial) + validation/hardening

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #677 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Slice 9 + Slice 10 + Slice 11

## Done when

6 bundled templates + macros + Finding record + shared prompt blocks + template-lint; foreach pipeline/loop until_dry/subworkflow nesting; session:fresh handoffs + carryover buckets + decision records (offloading+compaction deferred); timeout-fires + active-edge + journal-replay regression harnesses green; S147/S148 stall-window + allow_failure template-config fixes landed

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-7`](WV-7.md) | intra-repo | `done` |

### Unblocks (2)

[`WV-11`](WV-11.md) (`done`), [`WV-12`](WV-12.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `Slice 9`, `Slice 10`, `Slice 11`. Those tokens appear in its source plan at:

- `Slice 9` `docs/roadmap/plans/WORKFLOWS-V2.md:754` — ### Slice 9: Templates + Conventions Pack (est. 2 sessions)
- `Slice 10` `docs/roadmap/plans/WORKFLOWS-V2.md:760` — ### Slice 10: Advanced Constructs + Context Lifecycle (est. 2 sessions)
- `Slice 10` `docs/roadmap/plans/WORKFLOWS-V2.md:1177` — consumes them until Slice 6/7); `subworkflow` execution (Slice 10 — it returns a typed
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:30` — - **WF2-R5** (degraded completion, typed error taxonomy, two-knob timeouts, foreach per-item policy + amendments/batch-5 timeout tests) → §1 Node Outcome Model, §2 Timeouts, §8 Slice 11
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:36` — - **WF2-R11** (event dedup key, snapshot-then-subscribe, replay harness, workflow_observe + event-fold law + batch-5 StateMonitor coalescing) → §5 Event Pipeline, §4 tools, §8 Slice 11
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:296` — **Active-edge convergence gating (WF2-R18):** convergence (join) nodes with multiple predecessors wait only on predecessors with an **ACTIVE edge** — an edge along an actually-taken execution path. On conditional routing (`branch`, gate routing), only the taken path's outgoing edges become active…
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:362` — Two knobs, not one: `timeout_total` (max wall clock) + `timeout_stall` (max window with NO progress events — a long operation is fine, silence is not). Local-model-backed action providers get separate warm-up vs execute budgets (minutes-long cold starts look identical to hangs under one number). …
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:520` — - **Replay regression harness** (Slice 11): recorded JSONL traces for required scenarios (happy-path, restart-during-run, rewind-during-stream, network-degraded); compute duplicate_event_rate / event_fanout_ratio / order-violations / p95 offline; CI-gated against a checked-in baseline with hard +…
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:765` — ### Slice 11: Validation + Hardening (est. 2 sessions)
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:800` — | Timeouts that never actually fire (documented in the wild) | "Timeout fires" regression pair as Slice 11 acceptance criteria (WF2-R5 batch-5) |
- `Slice 11` `docs/roadmap/plans/WORKFLOWS-V2.md:849` — - Acceptance sharpening (Slice 11): edit node X mid-run → confirm cascade → every completed node OUTSIDE the binding closure re-completes via `step_cached` with zero model calls; a node INSIDE the closure re-executes. Asserted from the ledger, not logs.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
