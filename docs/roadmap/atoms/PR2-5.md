<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR2-5` — Health-scored self-remediation engine (deficit scoring, capped plan) on the heartbeat

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-RESILIENCE](../plans/PLATFORM-RESILIENCE.md) (`PR2`, plan status `done`) |
| **Atom table** | [`../atomic/PR2.md`](../atomic/PR2.md) |
| **Execution position** | #484 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§4 Health-Scored Self-Remediation Engine (Wave 3)

## Done when

resilience/remediation.py deficit scoring with max_reachable ceilings + reachable-exclusion; dependency-ordered plan (after: edges) stops at target_score / max_cost_usd / exhausted; judgment lane charges guardrails SpendMeter under run_key doctor; per-job success-only cooldown; doctor/remediation.jsonl ledger + doctor/jobs.json; runs as ONE adaptive-cadence heartbeat job; Doctor Maintenance section renders score + recent runs (criterion #6 score-raise + ledger).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PR2-1`](PR2-1.md) | intra-repo | `done` |
| [`PR2-2`](PR2-2.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:SpendMeter + model_calls.jsonl cost audit for the judgment lane` | cross-plan (EXT) | **unresolved** |

### Unblocks (2)

[`PR2-11`](PR2-11.md) (`done`), [`PR2-8`](PR2-8.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 8 (PR2) — observed

**Code evidence:**

- tests/test_resilience_remediation.py and tests/test_resilience_remediation_trigger.py pass (part of the 152-passed run)

**Notes:** PARTIAL: the health-scored self-remediation engine scores deficits and acts on them. This home HAS real deficits (no model, no embeddings, no STT), so it is a good future target — but observing the engine act means watching it run on a cadence, not clicking anything.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/PLATFORM-RESILIENCE.md:1249` — unattended since PR2-5 and a lower floor would stop live maintenance rather than harden it.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§4`. Those tokens appear in its source plan at:

- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:14` — **Wave:** split — §1-§3 (doctor probes + read-only surface) and §5 (degraded contract) are Wave 0/1 invariants (every existing surface touches models; offline behavior must be designed before the engine multiplies unattended runs); §6 (mid-turn handling) is Wave 1, independent; §4 (remediation en…
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:15` — **Depends on:** nothing for §1-§3/§5/§6 (Wave-0-compatible). §4 depends on AUTONOMY-GUARDRAILS (SpendMeter + model-call audit for cost caps) and prefers AUTOMATION-SUBSTRATE (runs as an adaptive-cadence trigger once triggers.json exists; hangs off the heartbeat until then).
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:22` — - **NEW-18** (Doctor: tiered probes process → socket → cheap RPC → per-capability; capability-degraded-is-never-core-failure doctrine; confirm-gated auto-fixes — stale cache, symlink repair, orphan pruning; per-provider selftest endpoints; GBrain health-scored self-remediation — deficit score, de…
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:25` — - **NEW-18 amendment c** (dry-run mode for triggers/automations: would-execute description without touching any wired system, sibling of the surfacing simulator on the same trust/debug surface) → §3.3, honoring the approved **AUTO-R15** dry-fire smoke gate + `automation_run(dry_run?)` (WORKFLOWS-…
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:37` — - **Maintenance is N independent tick-modulo jobs:** heartbeat (`heartbeat.py`, hard 60s) runs FTS rebuild every 15 ticks, daily history/SEL prune + skill-curator aging; inbox runs its own 6h maintenance pass; `verify_skill_integrity`/`run_aging` are callable seams with no scheduled caller verifi…
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:137` — **Overlap honored:** the execution machinery is APPROVED — `automation_run(id, dry_run?)`, the dry-fire smoke gate button, and the T9 recon rule (dry-run against bash/run-script/webhook is *refused and recorded as a preview*; only run-prompt/run-workflow truly dry-run, `ActionProvider.supports_dr…
- `§4` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:159` — Healthy (≥95) → sleep 60 min; degraded → 5-min tick with targeted jobs. Once AUTOMATION-SUBSTRATE lands, the engine IS one trigger (adaptive clock kind, `created_by: system`) on the Automations page — fulfilling §4.1 there ("optionally run as ONE health-scored maintenance trigger… replacing N ind…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
