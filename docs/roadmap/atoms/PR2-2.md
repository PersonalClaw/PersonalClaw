<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR2-2` — Platform-wide no-model degraded contract, registry, chip + honesty lint

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-RESILIENCE](../plans/PLATFORM-RESILIENCE.md) (`PR2`, plan status `done`) |
| **Atom table** | [`../atomic/PR2.md`](../atomic/PR2.md) |
| **Execution position** | #481 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§5 Platform-Wide No-Model Degraded Mode

## Done when

resilience/degraded.py DegradedContract registry + evaluate(); GET /api/resilience/degraded; shell degraded chip + down/recovery transition notifications; lint test asserts every one_shot_completion call-site maps to a registered contract; ResilienceConfig guard flags (doctor_enabled, degraded_indicator) round-trip through the 5-point config contract.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PR2-1`](PR2-1.md) | intra-repo | `done` |

### Unblocks (4)

[`PR2-3`](PR2-3.md) (`done`), [`PR2-4`](PR2-4.md) (`done`), [`PR2-5`](PR2-5.md) (`done`), [`PR2-9`](PR2-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§5`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:14` — **Wave:** split — §1-§3 (doctor probes + read-only surface) and §5 (degraded contract) are Wave 0/1 invariants (every existing surface touches models; offline behavior must be designed before the engine multiplies unattended runs); §6 (mid-turn handling) is Wave 1, independent; §4 (remediation en…
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:15` — **Depends on:** nothing for §1-§3/§5/§6 (Wave-0-compatible). §4 depends on AUTONOMY-GUARDRAILS (SpendMeter + model-call audit for cost caps) and prefers AUTOMATION-SUBSTRATE (runs as an adaptive-cadence trigger once triggers.json exists; hangs off the heartbeat until then).
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:26` — - **NEW-21** (platform-wide no-model degraded mode: per-surface declared fallback contract — deterministic fallbacks + pending-enrichment queues that drain when a provider returns + visible degraded indicator) → §5. Sources: `knowledge-forge`, `moss-audio`. The knowledge instance is ALREADY APPRO…
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:87` — This doctrine also feeds §5: a capability whose probe fails flips that capability's degraded contract on (one signal source, two consumers).
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:147` — `health_score = 100 − Σ weighted deficits`, computed from **measured problems only** (never guesses): stale/heuristic-stamped embeddings and knowledge entries (§5's pending-enrichment backlogs), FTS/faiss desyncs, orphan counts (§2's probe outputs), staging backlog (§3.2), failed-run backlog, ski…
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:286` — - **Guard flags:** `doctor_enabled` and `degraded_indicator` are guard-class — they parse per the AUTONOMY-GUARDRAILS §5 fail-safe tenet (missing/unknown ⇒ enabled; safe dataclass defaults).
- `§5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:299` — | **WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS** KNOW-R17 (heuristic floor), degradation ladder (§2.2 there) | The knowledge instance of §5 — registered, not re-specified. The generalized contract is this plan's remainder |

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
