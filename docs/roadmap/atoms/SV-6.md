<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-6` — Workflow-run half of resume-audit: byte-equal frontier reconstruction from the event-fold

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #549 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2.4 (resume-audit workflow-run half; resume_audit.py exports only audit_loop today)

## Done when

resume-audit kills and resumes a persisted workflow run from disk alone and verifies the journal replay reconstructs frontier state byte-equal to the pre-kill snapshot (Success Criterion #5, workflow half)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SV-4`](SV-4.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:event-fold law + frontier snapshot contract` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `64aa3088e` 2026-08-09 — chore(roadmap): reconcile dag.json — CE-2/SV-6/WF2WOR-8 merged → done (#932)
- `2727ac6c6` 2026-08-08 — test(harness): SV-6 workflow-run resume-audit — byte-equal frontier reconstruction (#925)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:18` — DONE) and the workflow-run half of resume-audit (`resume_audit.py::audit_workflow_run`, SV-6, DONE)
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:283` — - [2026-08-08][SV-6] DONE: workflow-run half of resume-audit — byte-equal frontier reconstruction from the event-fold (§2.4, **Success Criterion #5 workflow half met**). `harness/resume_audit.py::audit_workflow_run(run_id)` (beside `audit_loop`) reconstructs a persisted run from DISK ALONE — a fr…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:284` — - [2026-08-08][SV-6] DEVIATION (naming): the "Done when" says "resume-audit kills and resumes" — the existing CLI verb `resume-audit` is the LOOP audit, so the workflow half is exposed as a sibling verb `workflow-resume-audit <run_id>` rather than overloading `resume-audit` (which takes a `loop_i…

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `§2.4`. Those tokens appear in its source plan at:

- `§2.4` `docs/roadmap/plans/SELF-VERIFICATION.md:228` — - **Session 4 — baselines + journal gate + riders (§2.3, §2.4, §4.1)** *(Wave 1, co-scheduled with WF2 Slice 2)*: baselines.json + hard/drift compare + missing-scenario-fails; record `workflow-journal-projection` + `rewind-during-stream` against the young engine and **gate the journal format on t…
- `§2.4` `docs/roadmap/plans/SELF-VERIFICATION.md:282` — - [2026-07-26][S4] BLOCKED (E-none — expected dependency, not a defect): the Workflows-v2-gated deliverables are correctly NOT built this session because the engine + journal do not exist yet: (1) replay scenarios `workflow-journal-projection` + `rewind-during-stream` (§2.3 required set) — need W…
- `§2.4` `docs/roadmap/plans/SELF-VERIFICATION.md:283` — - [2026-08-08][SV-6] DONE: workflow-run half of resume-audit — byte-equal frontier reconstruction from the event-fold (§2.4, **Success Criterion #5 workflow half met**). `harness/resume_audit.py::audit_workflow_run(run_id)` (beside `audit_loop`) reconstructs a persisted run from DISK ALONE — a fr…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
