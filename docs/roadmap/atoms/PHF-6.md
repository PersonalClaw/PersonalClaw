<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-6` — Inert-control inventory baseline (writer/reader ratchet)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #443 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##879 |

## Intent — what this is for

Add tests/test_inert_surface_baseline.py plus a committed inert-surface-baseline.json recording, for each declared surface (config keys, enum members, registered kinds/runtimes, _EDITABLE_CONFIG entries, SDK exports), whether a writer and a reader exist. This targets the recurring defect where something is declared and nothing on the other side of the seam consumes or produces it while hand-built test state hides it. Per-file counters may only shrink; ship at the measured population (not zero, since a never-run gate given teeth at zero is an outage) with a doc line forbidding raising a number to go green. Drive the top offenders down one file per commit, each commit proving the writer now exists. SafetyProfile is expected to appear and is left for PHF-8 to fix.

## Done when

The current inert population is measured and committed; adding a declared-but-unread surface reds CI; each cleanup commit shrinks a counter with a test proving the writer now exists; the forbidden-to-raise doc line is present.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PHF-5`](PHF-5.md) | intra-repo | `done` |

### Unblocks (1)

[`PHF-12`](PHF-12.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `917ef60f8` 2026-08-12 — feat(workflows): WV-14 make `on_overlap: queue` queue instead of starting a concurrent run (#1130)
- `546949220` 2026-08-06 — test(hardening): PHF-10 docs-lint + plan-hygiene gate (shrink-only ratchet) (#880)
- `3293c213f` 2026-08-06 — test(hardening): PHF-6 inert-surface baseline (writer/reader shrink-only ratchet) (#879)

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:535` — - [2026-08-12][PHF-12] DEVIATION: `PHF-6`'s docstring instructs "do not 'fix' any surface this
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:832` — defect class `PHF-6`'s census exists to catch. `grep -rn xdist_group tests/` stays empty by
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:971` — Every number below is a MEASUREMENT of the tree, never an aspiration — `PHF-6`'s
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1080` — a user-visible surface (`PHF-6`'s precedent). No `web/` change.
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1228` — tool plus its baseline is not a user-visible surface — `PHF-6`'s precedent). No `web/` change.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
