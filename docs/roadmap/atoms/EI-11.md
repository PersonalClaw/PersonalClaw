<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EI-11` — Security docs correction — credential-hiding vs confinement (D0, land first)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXECUTION-ISOLATION](../plans/EXECUTION-ISOLATION.md) (`EI`, plan status `done`) |
| **Atom table** | [`../atomic/EI.md`](../atomic/EI.md) |
| **Execution position** | #233 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##750 |

## Intent — what this is for

Amendment 2026-07-29 (b) + task D0 — docs-only, dependency-free: correct the website boundary diagram/copy, qualify the guardrails claim to unattended work, qualify the desktop-platform claim, add a "what the sandbox does and does not do" section to docs/architecture/security.md

## Done when

no public surface claims confinement the code does not provide; the guardrails claim names its unattended scope (reusing the Settings panel wording); the desktop claim matches CI reality; security.md has the explicit sandbox does/doesn't section

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 65 (EI) — the spawn-site census falsified (an unmapped spawn named to file:line); the runner provenance rows driven in a real browser

**Code evidence:**

- 🔑 THE CORRECTION LANDED IN THE CODE'S OWN WORDS, WHICH IS WHERE IT MATTERS MOST: `sandbox.py`'s docstring says it 'HIDES sensitive credential paths … using platform-native isolation' and that it 'falls back gracefully to NO SANDBOX when the OS mechanism is unavailable (logged as warning)'. Credential-hiding is claimed; confinement is not
- the threat model matches — the OS child sandbox is entered as a 'credential-env denylist so secrets never reach a sandboxed child', not as containment
- 🏅 AND THE APP-NETWORK CLAIM IS THE SHARPEST DISCLOSURE IN THE PROJECT. `limitations.md` puts the network claim OUTSIDE the enforced-permission list, labelled advisory: 'PersonalClaw does not confine an app's outbound traffic: this app's code can reach the network either way. THE DECLARATION IS DISCLOSURE, NOT CONTAINMENT.' It is shown 'whether or not the app declares `network`, so an app that declares `network: false` is NOT presented as one the platform has blocked'
- 🔴 that last clause is the absent-vs-declared-false class in its consent form: a `false` that the platform does not enforce must not read as a `false` the platform imposed
- the docker provider's egress comment is the same discipline inside the code — only `off` is claimed at the container layer, and the finer tiers name where enforcement actually lives
- 358 passed / 3 skipped across 17 sandbox, runner, vault, checkpoint and triage suites; 16/16 on the runner-health frontend suite; the Secrets vault and Agent-defaults pages driven on :10011

**Driven in the UI:** The app-consent wording was verified against the enforcement docs; the Store's rendering of it was driven in the APE cycle.

**Notes:** 🏅 A plan atom whose entire deliverable is REMOVING overclaims is unusual and it is the one that makes the other eleven trustworthy. Everything else here confines something; this one makes sure the docs only say what those eleven actually do.

## Recorded history

### Commits (2)

- `0cd9396f7` 2026-08-22 — docs(roadmap): correct EI D0's status — security.md already had the section
- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:22` — security claim is a live defect". So atom `EI-11` (D0) was flipped `done` while one of its two named
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:25` — approval/policy gate and hidden credential paths rather than an OS jail). `EI-11` is genuinely complete
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1005` — - **2026-08-22 — `D0` / atom `EI-11`: the claim was corrected on the public surface, and this plan's own
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1012` — **So `EI-11` read `done` in `dag.json` while one of its two named surfaces was unmet** — exactly what a
- **source plan** `docs/roadmap/plans/PLATFORM-REACH.md:174` — - [2026-08-10][PR-5 / A2.2+A2.3] DONE: support matrix with real proof tokens + `[models]` per-arch reality. `docs/guides/platforms.md` already existed (created by EI-11 security docs) and already had a matrix, but its Proof column was prose ("CI test + release smoke") — not the required `CI:<job>…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
