<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2TAS-12` — Retire the guidance-persistence Lifecycle enum; rail the def→SurfacingMeta conversion point

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-TASKS-SOPS](../plans/WORKFLOWS-V2-TASKS-SOPS.md) (`WF2TAS`, plan status `done`) |
| **Atom table** | [`../atomic/WF2TAS.md`](../atomic/WF2TAS.md) |
| **Execution position** | #634 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1144 |

## Intent — what this is for

§2 Surfacing discipline: the declared-only `Lifecycle` field on `SurfacingMeta` (WF2TAS-4) measured against the live surfacing path (WF2TAS-7/-8), and the adapter seam that would have to carry it

## Done when

Measured: `render_passive`/`may_suggest` have zero production callers and nothing in src/ reads `agent_digest`, so the passive channel `lifecycle` paced does not exist (corroborated by FS-6's note and test_learning_ambient's "unbuilt producer" seam test); `DefMetadata` had no `lifecycle` field, so no authored def could write it and `meta_from_def` could not carry it — three members, zero observable difference. `Lifecycle` + `SurfacingMeta.lifecycle` + its to_dict/from_dict keys DELETED (clean break, not a documented TODO); a stale `lifecycle:` key on disk still loads because from_dict names what it reads. New rail in test_workflows_def_surfacing_fields.py asserts `meta_from_def` names every field shared by `SurfacingMeta` and `DefMetadata` (7 today, vacuity-guarded, source-read via AST because a dropped field arrives as a legal default) plus a proof-it-can-fail test; inert-surface-baseline.json regenerated on the legitimate shrink (surfacing.py entry removed, enum 19→18, inert 146→145). Guidance aggressiveness unchanged: nothing surfaces passively today, so no def surfaces more often

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2TAS-4`](WF2TAS-4.md) | intra-repo | `done` |
| [`WF2TAS-7`](WF2TAS-7.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `917ef60f8` 2026-08-12 — feat(workflows): WV-14 make `on_overlap: queue` queue instead of starting a concurrent run (#1130)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1551` — ### 2026-08-12 — WF2TAS-12 (retire the guidance-persistence `Lifecycle`) DONE — PR PENDING
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1590` — recorded in `docs/roadmap/atomic/WF2TAS.md` under `WF2TAS-12`.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§2`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:28` — - **TASK-R3** → §2 Surfacing Discipline (opt-in enum, trigger phrases, negative triggers, trigger CI, reachability doctor, registry-first doctrine)
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:29` — - **TASK-R4** → §2 Surfacing Metadata & Injection Contract (summary/when_to_use, preconditions, freedom_level, digest tier, one-source-two-wrappers, overlays, portable markdown + git-sync provider)
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:32` — - **TASK-R7** → §2 Hand-Off Edges + §8 Seed Template Library
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:33` — - **TASK-R8** → §2 Cadence Surfacing Channel (cadence_days, overdue escalation via create-task)
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:36` — - **TASK-R11** → §2 Parameter Pre-Fill Contract + Requirements Preflight
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:39` — - **TASK-R14** → §2 Composition-Direction Lint (checklist ⊃ SOP, acyclic refs)
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:41` — - **TASK-R16** → §2 Blueprint Sessions (third surfacing mode)
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:43` — - **TASK-R18** → §2 Layered Scope Resolution & Shadowing (per-stage overlays)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
