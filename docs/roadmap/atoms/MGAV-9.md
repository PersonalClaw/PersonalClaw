<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MGAV-9` — FE surfaces: MemoryPanel tabs, MemoryGraph viz + HTML export, full config wiring + as-a-user validation

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MEMORY-GRAPH-AND-VAULT](../plans/MEMORY-GRAPH-AND-VAULT.md) (`MGAV`, plan status `done`) |
| **Atom table** | [`../atomic/MGAV.md`](../atomic/MGAV.md) |
| **Execution position** | #353 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1715 |

## Intent — what this is for

§7.1 MemoryPanel extensions, §7.2 graph visualization + export, Provider & Config Plug-in Map (FOUR-point wiring); Session 5 (FE + wiring)

## Done when

studio tab gains Slots editor + entity browser + proposed-entity accept queue; inspect tab shows per-record backlinks + evidence tags (citation deep-link target); settings tab exposes vault mode/path, push toggle+min-confidence, slot caps, topology toggle via _EDITABLE_CONFIG PATCH; MemoryGraph.tsx renders entities colored by Louvain community with typed/provenance/min-confidence filtering + side drawer, plus GET /api/memory/graph/export self-contained HTML; all remaining MemoryConfig fields survive the four-point round trip (schema tests) and toggle live; end-to-end write→link→recall→volunteer→edit-vault→undo validation sweep passes with graph_enabled:false / foreign provider degrading cleanly

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MGAV-5`](MGAV-5.md) | intra-repo | `done` |
| [`MGAV-6`](MGAV-6.md) | intra-repo | `done` |
| [`MGAV-8`](MGAV-8.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `52a466441` 2026-08-18 — docs(roadmap): MEMORY-GRAPH-AND-VAULT is done, its status field said otherwise
- `42339ba45` 2026-08-18 — chore(roadmap): cite the landing batch #1715 on MGAV-9
- `831669c98` 2026-08-18 — test(memory): MGAV-9 end-to-end write-link-recall-volunteer-vault-undo sweep
- `a04145bc3` 2026-08-16 — feat(memory): MGAV-9 slots editor, entity graph canvas + HTML export, config wiring

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:800` — surfaced in `/api/memory/settings`; `config-baseline.json` regenerated. FE controls are MGAV-9's
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:886` — MGAV-9 owns the settings surface.
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:907` — ### 2026-08-16 — Session 5 (§7.1–7.2 FE + config wiring): PARTIAL (`MGAV-9`)
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:912` — `docs/roadmap/atomic/MGAV.md` under `MGAV-9`; this entry records what changed and what it proved.
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:992` — three legs of clause 7 remain, which is why `MGAV-9` stays `todo`.
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:994` — ### 2026-08-18 — Session 6 (clause 7: the end-to-end sweep): DONE (`MGAV-9`)

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§7.1`, `§7.2`, `Session 5`. Those tokens appear in its source plan at:

- `§7.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:320` — - **Config — the FOUR wiring points** (recon: persistence-security gotcha #1) for every new `MemoryConfig` field — `graph_enabled` (default true), `graph_topology_in_context` (false), `push_context_enabled` (false, opt-in), `push_context_min_confidence` (0.7), `push_context_max_items` (3), `vault…
- `§7.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:907` — ### 2026-08-16 — Session 5 (§7.1–7.2 FE + config wiring): PARTIAL (`MGAV-9`)
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:27` — - **NEW-3** — typed entity graph over memory (zero-LLM write-time linking, alias table, backlinks, edge vocabulary `same_topic/temporal_proximity/references/same_project`, graph recall arm, orphans lint) → §1, §2; ambient push-context reflex + volunteered-vs-used stat → §3; Louvain community topo…
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:159` — On the consolidation maintenance cadence (the existing post-step slot in `history.py`, NOT a new loop): run Louvain community detection (fixed seed, deterministic — llm-wiki-agent's `seed=42` discipline) over `mem_links`, write `community` into `mem_link_stats`, and materialize a **compact topolo…
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:235` — - **Static-site export**: `personalclaw memory vault export --html` — nice-to-have CLI, lowest priority, shares §7.2's renderer.
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:743` — reads, what §7.2's viz can draw) plus a `conflict_keep_both` WAL event (what survives with the
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:884` — shares §7.2's renderer) and the reveal/copy-symlink affordance are not built. `index.md` stays
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:924` — drops (which is exactly what §7.2's filters need, so this is a sibling query, not a fork). An
- `§7.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:1059` — §7.2 canvas is permanently "unclustered" on such an install — a live reader of an unwritten key.
- `Session 5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:335` — - **Session 5 — slots + FE + wiring:** `slot.*` prefix + caps + built-ins + reflection append hook + self-model promotion logic, MemoryPanel tabs (Slots editor, entity browser, lint/stats surfaces, settings), MemoryGraph upgrade + HTML export, full FOUR-point config wiring, as-a-user validation s…
- `Session 5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:907` — ### 2026-08-16 — Session 5 (§7.1–7.2 FE + config wiring): PARTIAL (`MGAV-9`)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
