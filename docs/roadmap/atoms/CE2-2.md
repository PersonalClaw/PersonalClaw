<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE2-2` — Type-routed compressors (JSON crusher, AST code) + three-layer rule overlay + prose compressor

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CONTEXT-ECONOMY](../plans/CONTEXT-ECONOMY.md) (`CE2`, plan status `done`) |
| **Atom table** | [`../atomic/CE2.md`](../atomic/CE2.md) |
| **Execution position** | #125 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2.1 JSON crusher, §2.2 AST-aware code content type, §2.3 three-layer rule overlay (builtin/user/project + rule ops v2), §2.4 background-only prose-model compressor; Session 2

## Done when

SC#3 met: .personalclaw/projection_rules.json reroutes to the log projector beating user layer + sniff, bad regex skipped+logged never raising; 829K JSON crushes to ~379 chars with raw_ref; code content type + builtin rule pack (rules_builtin.json) ship; prose compressor locked out of sync dispatch by test; log [2026-07-26][S2] DONE, gate green (8083 passed)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CE2-1`](CE2-1.md) | intra-repo | `done` |

### Unblocks (1)

[`CE2-3`](CE2-3.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 25 (CE2) — DRIVEN in a real browser

**Code evidence:**

- tests/test_tool_output_projection.py covers the type routing by SHAPE rather than by name: infer_diff, infer_json_must_parse, infer_test_output, infer_csv, infer_generic_for_prose
- the projections are asserted on what they PRESERVE, which is the only thing that matters for a compressor: log projection 'keeps error lines from the middle', diff projection 'has a stat summary', json projection 'shows shape not midcut', test projection 'keeps failures'
- a small result passes through untouched and an uncapped one passes through — the two no-op paths are pinned so compression cannot creep into cases that do not need it
- 386 tests pass across the projection / compression / codegraph / tool-group modules (1 unrelated skip)

**Notes:** Partial: the atom's headline measurement (829K of JSON crushing to about 379 characters with a raw_ref) is a specific number I did not reproduce, and the three-layer rule overlay's precedence — a project file beating the user layer beating the sniffer — needs a real overlay to exercise. What IS confirmed is the shape of the tests, and 'shows shape not midcut' is the right assertion: a mid-cut of 829K of JSON is unparseable, so preserving structure rather than a prefix is the whole trick.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§2.1`, `§2.2`, `§2.3`, `§2.4`, `Session 2`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:218` — - **Session 2 — type-routed compressors + rule overlay (§2.1-2.4):** JSON crusher upgrade; `code` content type + AST/regex outliner; builtin rule pack + project rule layer + rule ops v2 with one interpreter + PATCH-boundary validation; prose-model compressor module (background-only, deterministic…
- `§2.1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:294` — - [2026-07-26][S2] DONE: type-routed compressors + three-layer rule overlay + rule ops v2 + prose compressor (§2.1-2.4). (a) **JSON crusher** — `_project_json` now folds arrays via `_fold_array`: per-field schema inferred from a bounded 50-item sample (names, types, numeric ranges, null counts), …
- `§2.2` `docs/roadmap/plans/CONTEXT-ECONOMY.md:191` — - **Scope (deliberately light):** a per-workspace index of definitions (functions/classes/methods with file:line), import edges, and a reverse-reference map — built with tree-sitter (Python/TS/JS/Rust/Go first; the AST outliner from §2.2 shares the parser install). NOT a full LSP; no type inference.
- `§2.2` `docs/roadmap/plans/CONTEXT-ECONOMY.md:223` — - **Session 6 — codebase graph (§5.5, grok-build learning):** tree-sitter indexer (shares §2.2's parser install) + SQLite store + mtime invalidation; `code_map` tool registered as a group; SDLC planning-context module summary; `@`-mention centrality ranking; as-a-user validation on a multi-module…
- `§2.3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:205` — - **Config — the FOUR wiring points** for every new field: (a) dataclass field with `_meta(label, help)` on `ToolsConfig` (loader.py:1410) — new fields: `bg_compress_enabled`, `bg_compress_idle_days`, `groups_enabled`, `group_defaults`, plus `ProjectionRuleConfig` gaining the §2.3 op fields (each…
- `§2.4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:30` — - **LEARN-R12** (approved, WORKFLOWS-V2-LEARNING-FLYWHEEL §1/§2.4): the `model_invoked: bool` axis already removes command-like skills from the surfacing embeddings and INDEX — the SKILLS-side context-budget reclaim is that plan's job and is only referenced here. This plan owns the orthogonal TOO…
- `§2.4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:74` — The projector table grows from shaping-by-elision to true type-routed compression. All hot-path compressors stay **deterministic and synchronous** (the tool-dispatch path cannot await an LLM); the prose-model compressor is background-only (§2.4). Every compressor keeps `project_and_retain`'s inva…
- `§2.4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:122` — - **Topic segmentation + attention-weighted summarization** (`agent-zero`): segment the transcript by embedding-drift boundaries (active embedding provider; deterministic turn-count fallback when no embedder is bound — degraded mode is designed, not accidental), then compress per-topic with atten…
- `§2.4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:298` — - [2026-07-27][S3] DONE: continuous background compression service (§3, §4). (a) **Shared segmenter** — new `context_segmentation.py` (`segment_messages`, pure, no config/provider graph): embedding-drift boundaries when an `embed_fn` is bound (adjacent user-turn cosine < `DEFAULT_DRIFT_THRESHOLD=…
- `Session 2` `docs/roadmap/plans/CONTEXT-ECONOMY.md:218` — - **Session 2 — type-routed compressors + rule overlay (§2.1-2.4):** JSON crusher upgrade; `code` content type + AST/regex outliner; builtin rule pack + project rule layer + rule ops v2 with one interpreter + PATCH-boundary validation; prose-model compressor module (background-only, deterministic…
- `Session 2` `docs/roadmap/plans/CONTEXT-ECONOMY.md:225` — Sessions 1-3 (NEW-16) and 4-5 (NEW-22) are independent tracks; either alone is a Wave-0 win. Session 6 depends only on Session 2's tree-sitter install (or ships its own) and is otherwise independent. The engine consumers (§2.5b node outputs, §5.4 per-template groups) activate when WORKFLOWS-V2 Sl…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
