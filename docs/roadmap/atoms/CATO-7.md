<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-7` — Session total on the chat session header/detail

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #103 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2(b) session-level; C3 — Surfaces (Session total); Session 2 T2.2

## Done when

The chat session header/detail renders '$X · N tokens' by reading rollup scoped to the session key; a multi-turn session's reported total matches the sum of its turn lines

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-5`](CATO-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:251` — → **CHANGELOG entry added.** Scope: turn readout only — session-header (CATO-7) + Usage panel
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:256` — - **CATO-7 DONE — the session cost header (S2 session-level surface).** Added a `session_key` filter
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:279` — group_by}` options shape (+ fixed CATO-7's call site) and added the `UsageAgg` type. **Wired the

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:143` — | T2.2 | Session total on the chat session header/detail (reads `rollup` scoped to the session key) | `web/src/pages/ChatPage.tsx` (or the session detail component), `web/src/lib/api.ts` | a multi-turn session reports a total matching the sum of its turn lines |
- `Session 2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:138` — ### Session 2 — The surfaces

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
