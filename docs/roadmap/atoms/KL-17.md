<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-17` — S4 knowledge graph: embedding-projected layout, server-side edge sparsification, collision-checked labels, ego view from the document

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #318 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1826 |

## Intent — what this is for

Capability-gap amendment (2026-08-19)

## Done when

node positions are a deterministic 2-D projection of item embeddings computed server-side (power iteration with deflation, fixed-seed init so layouts are reproducible, normalized output, and mixed-dimension vectors placed at the origin rather than dropped), served with the graph payload and memoized with a debounced invalidation — so on-screen distance carries semantic meaning and the layout is stable across sessions, replacing the insertion-order concentric rings whose centre means 'highest inbound count' above the truncation cap and 'alphabetically first' below it; the payload keeps every node and thins EDGES instead of truncating nodes at a node cap (which loses data) — a similarity floor plus a top-K-per-node keep, with clusters above a minimum size labelled from their dominant tags; labels are placed by a measure-then-filter pass with a rendered-size floor, a shared placed-rect list and larger nodes winning slots on collision, replacing the degree/zoom threshold; the edge weight already on the wire and drawn nowhere is encoded on two channels (colour and width) since width alone is unreliable under a viewBox; a focused ego view reachable from an item's reading rail expands neighbours by hop depth, reusing the memory graph's existing lit-set BFS and its filter/drawer vocabulary rather than inventing a second one; both canvases keep the shared zoom control; the graph-mark contrast rail is widened from one hardcoded path to a derived census of every file rendering graph marks, with a vacuity assertion that the census is non-empty; the graph is reachable as a route so it is axe-scanned and snapshotted

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-13`](KL-13.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `b66f8cf0a` 2026-08-20 — docs(roadmap): cite the landing PR for KL-17
- `9e14538b6` 2026-08-20 — feat(knowledge): KL-17 graph navigation — projected layout, edge thinning, ego view
- `0377b537e` 2026-08-19 — docs(roadmap): add capability-gap amendment atoms to KL, WF2KNO and LMMV

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:857` — `FRONTEND_PATHS` list the script is not in. (This also re-confirms `KL-17`'s finding that
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1224` — KL-11 + KL-14; unblocks KL-17. Built as four fenced parallel slices (store/pass · config · endpoint ·
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1323` — **Two traps for whoever takes KL-17** (now unblocked, in the ready frontier): a document's own chunks
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1440` — **`KL-17` remains the only KL atom this unblocks-adjacent work leaves open besides `KL-8`.**
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1442` — - [2026-08-20][KL-17] **DONE — graph navigation: a projected layout, edge thinning, two-channel
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1654` — inert control (KL-17 already owns the graph UI).

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
