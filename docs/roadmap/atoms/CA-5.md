<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CA-5` — S2 discovery: optional mDNS advertiser + client resolver + guide

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COMPANION-APPS](../plans/COMPANION-APPS.md) (`CA`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CA.md`](../atomic/CA.md) |
| **Execution position** | #92 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1506 |

## Intent — what this is for

Session 2 T2.2, T2.3 + V2; Contracts C3; new companion/discovery.py (advertise _personalclaw._tcp with token-free TXT only when bound beyond loopback + discovery_enabled) + client resolver + new docs/guides/companion-apps.md fallback section

## Done when

a resolver on the LAN finds the instance by name and can begin pairing; loopback-only gateway is a no-op + log; TXT record asserted to carry no token/content; disabling discovery leaves manual-URL + QR pairing working (degradable, Success Criterion 5)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CA-4`](CA-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `ff3b394e4` 2026-08-16 — feat(companion): CA-5 optional LAN discovery over the standard library

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:147` — `127.0.0.1` (this is the same origin surface as the CA-5 rough edge in the Execution log);
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:268` — - [2026-08-16][CA-5] DONE: S2 discovery — stdlib mDNS/DNS-SD advertiser + client resolver
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:277` — - [2026-08-16][CA-5] DEVIATION: the done_when's "QR pairing" clause is vacuous — no QR surface
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:306` — the same origin surface as the CA-5 DISCOVERY below), and `pair/complete`'s `device_name` is
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:330` — - [2026-08-16][CA-5] DISCOVERY (not fixed): redeeming a pairing code from a browser reached **by
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:391` — - [2026-08-18][CA-1] DISCOVERY (not fixed, blocks CA-2): the CA-5 rough edge above is **still
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:457` — this reads as an absent one, and pairing still completes. This closes CA-5's vacuity DEVIATION above:

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.2`, `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/COMPANION-APPS.md:199` — | T2.2 | Optional mDNS advertiser (C3): advertise `_personalclaw._tcp` with a token-free TXT record only when bound beyond loopback + `discovery_enabled`; clean start/stop with the gateway | `companion/discovery.py` (new) | a resolver on the LAN finds the instance by name; loopback-only gateway →…
- `T2.3` `docs/roadmap/plans/COMPANION-APPS.md:200` — | T2.3 | Client resolver helper + graceful "no discovery → type the URL" fallback documented for wrappers | `companion/discovery.py`, `docs/guides/companion-apps.md` (new) | resolver returns candidates on a LAN fixture; fallback path documented |
- `Session 2` `docs/roadmap/plans/COMPANION-APPS.md:194` — ### Session 2 — Local-network discovery

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
