<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-7` — S4 Capacitor shell wrapping the served companion route

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #341 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, verified 2026-08-27 - the shell is COMPLETE and the gap is a toolchain, so do NOT schedule an implementation slot. mobile/ ships 14 tracked files wrapping the SERVED route (COMPANION_ROUTE='#/companion'); the gateway URL is deliberately RUNTIME state with capacitor.config.json OMITTING server.url/hostname (a rail asserts their absence) because baking it means one store build per owner; the device session needs no new contract since the shell hands the WebView to the gateway's own /pair page so the httponly cookie lands in the jar the companion reads (a rail asserts no fetch(...pair/complete...) exists under mobile/ and that ?token= stays forbidden); safe areas use two mechanisms for two documents against a measured premise (web/src has ZERO env(safe-area-inset-*)); no forked UI, enforced three ways. It is the FIRST consumer of web/src/lib/endpoints.ts, which previously had zero production importers. SUB-GAP logged for MC-8: rows this shell creates carry device_session_ref:'' and it is never filled, because pair/complete returns device_id/name/kind/expires_in and not the nonce. UNVERIFIABLE-BY-CODE: mobile/ios and mobile/android are deliberately gitignored (generated by `cap add`; a committed copy forks the template), so nothing in the repo could be built and no code change closes this. SMALLEST REMAINING UNIT: an Xcode + CocoaPods + Android SDK pass on a Mac - npm ci from the root, npm run add:ios/add:android, install to a simulator, enter a gateway address, confirm live #/companion renders. Neither native safe-area key can be proven by any test in this repo. |

## Intent — what this is for

Sessions 4-6 Wrapper tier — T4.1 Capacitor shell

## Done when

A Capacitor shell (new mobile/ dir; repo-location decision recorded) wraps the served companion URL with config for gateway URL + device session and native safe-areas, no forked UI; builds for iOS+Android and renders the live companion.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MC-3`](MC-3.md) | intra-repo | `done` |
| [`MC-2`](MC-2.md) | intra-repo | `done` |

### Unblocks (3)

[`MC-10`](MC-10.md) (`done`), [`MC-8`](MC-8.md) (`done`), [`MC-9`](MC-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `b7c7ad574` 2026-09-04 — docs(roadmap): flip MC-7/DC-3/DC-4/APE-6 + ES-8 decomposition
- `5b4b03f87` 2026-08-27 — feat(companion): MC-7 wrap the served companion route in a Capacitor shell

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:215` — **2026-09-04 — MC-8 validated done (owner-residual device gate).** The QR pairing screen is code-complete on main: the in-repo QR model-2 encoder `web/src/lib/qr.ts` (header names MC-8 as the atom that made the build-vs-install call) is RS-syndrome-verified by `web/src/lib/qr.test.ts`; `PairingQr…
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:217` — **2026-09-04 — MC-7 validated done (owner-residual toolchain gate).** The S4 Capacitor shell is code-complete on main: `mobile/` ships 14 tracked files wrapping the served `#/companion` route and is the first production consumer of `web/src/lib/endpoints.ts`; `capacitor.config.json` omits `server…
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:730` — does not exist yet** — that is `MC-7` (the Capacitor wrapper), still `todo` — and a camera scan needs
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:740` — ### 2026-08-27 — `MC-7` (S4 T4.1) — **PARTIAL**: the shell is complete; both native builds are environment-gated
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:917` — **Rebase note (2026-08-27).** This atom was authored against a base that predated `MC-6`, `MC-7`,

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T4.1`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/MOBILE-COMPANION.md:137` — | T4.1 | Capacitor shell: wraps the served companion URL, native safe-areas, no forked UI. Connection state is COMPANION-APPS §C1's endpoint registry (`{active, endpoints[]}` per plan 54 S3/T3.3, already shipped in `web/src/lib/endpoints.ts`) — **not** a single stored gateway URL and not a second…
- `T4.1` `docs/roadmap/plans/MOBILE-COMPANION.md:740` — ### 2026-08-27 — `MC-7` (S4 T4.1) — **PARTIAL**: the shell is complete; both native builds are environment-gated
- `T4.1` `docs/roadmap/plans/MOBILE-COMPANION.md:770` — consumers explicitly: *"what desktop (T4.1) and mobile import so that neither re-decides the key

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
