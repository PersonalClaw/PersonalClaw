<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-2` — S2 device-session consumption + Devices list in Settings

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #336 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 T2.3/T2.4 + C1 Device token (SUPERSEDED — consumes COMPANION-APPS §C1/C2 device sessions on REMOTE-USER-AUTH's durable store, not token_auth.py)

## Done when

A roaming-IP phone keeps its device session valid per the plan-54 contract; the Settings > Devices list renders name/minted/last-seen and Revoke; revoking kills the device session on the next request. No new claim added to token_auth.py — the device session comes from plan 54.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:COMPANION-APPS:device-session model + unified pairing contract` | cross-plan (EXT) | **unresolved** |
| `EXT:REMOTE-USER-AUTH:durable session store (auth/sessions.json)` | cross-plan (EXT) | **unresolved** |

### Unblocks (3)

[`MC-5`](MC-5.md) (`done`), [`MC-7`](MC-7.md) (`done`), [`MC-8`](MC-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `41fc975ac` 2026-08-25 — docs(roadmap): flip MC-2 done — the device-session consumers are live
- `739890ce8` 2026-08-25 — test(devices): MC-2 device-session consumption rails at the HTTP boundary

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:129` — `device` claim, no unbinding, no weakened invariant, so no E4. It is what `MC-2`'s "a
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:296` — `token_auth.py` change. This is what `MC-2`'s "roaming-IP phone keeps its device session
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:54` — > atom `MC-2`) *consume* plan 54 §C1/C2 and touch `token_auth.py` **not at all** — E4 if a task
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:387` — ### 2026-08-25 — `MC-2` (S2 T2.3 + T2.4) — **DONE**, and it was a rail job, not a build job
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:396` — `token_auth.py:221` on both authorized paths. So MC-2's UI clause was already MET by `CA-2` (merged
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:443` — growing a second. It is **not** part of `MC-2`'s `done_when` (all four clauses above are met without
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:731` — a second physical device, which is the same gate `MC-2`/`CA-2` already carry. What was driven is

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `T2.3`, `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/MOBILE-COMPANION.md:53` — > What this plan still owns: the **phone UI + push**. Its device-auth tasks (T2.3/T2.4 below,
- `T2.3` `docs/roadmap/plans/MOBILE-COMPANION.md:111` — | T2.3 | Consume plan 54's device session: pair/enroll the phone per COMPANION-APPS §C2 and prove a roaming-IP phone keeps its session (it rides the cookie, which skips the IP check per §C1's transport constraint) — **no new claim, no `token_auth.py` change; E4 if it appears to need one** | compa…
- `T2.3` `docs/roadmap/plans/MOBILE-COMPANION.md:153` — - **IP-bound tokens vs roaming phones** is the one real unknown (T2.3 resolves it surgically); worst case the device claim mints unbound tokens with shorter TTL + SEL visibility — still within the existing model.
- `T2.3` `docs/roadmap/plans/MOBILE-COMPANION.md:170` — - **S2 — Approvals-first companion:** T2.1 narrows to the Approvals section only (full-context cards); T2.2 narrows to the approve/reject wiring; T2.3/T2.4 (device sessions/devices list — already superseded to plan 54's contract) unchanged.
- `T2.3` `docs/roadmap/plans/MOBILE-COMPANION.md:387` — ### 2026-08-25 — `MC-2` (S2 T2.3 + T2.4) — **DONE**, and it was a rail job, not a build job
- `T2.3` `docs/roadmap/plans/MOBILE-COMPANION.md:407` — - **T2.3 roaming IP.** Pair a device, then hit a protected route with its cookie from `203.0.113.7`
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:53` — > What this plan still owns: the **phone UI + push**. Its device-auth tasks (T2.3/T2.4 below,
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:112` — | T2.4 | Devices list: consumed from COMPANION-APPS T1.2 (Settings → Devices — name, kind, minted, last-seen, issuer + revoke). This plan builds **no second list**; it links to that one from the companion surface | companion nav/link only | a revoke performed in plan 54's panel kills the device s…
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:170` — - **S2 — Approvals-first companion:** T2.1 narrows to the Approvals section only (full-context cards); T2.2 narrows to the approve/reject wiring; T2.3/T2.4 (device sessions/devices list — already superseded to plan 54's contract) unchanged.
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:387` — ### 2026-08-25 — `MC-2` (S2 T2.3 + T2.4) — **DONE**, and it was a rail job, not a build job
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:398` — `devicesPanel.test.tsx:90` asserts `/^Paired \d+[mhd] ago/`. **No second list was built** (T2.4's
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:413` — - **T2.4 revocation.** `CA-2` asserts revoke through `validate_token`; a phone never calls
- `T2.4` `docs/roadmap/plans/MOBILE-COMPANION.md:441` — **Unmet, and deliberately left:** T2.4's *Files* column also asks for a "companion nav/link only" —
- `Session 2` `docs/roadmap/plans/MOBILE-COMPANION.md:105` — ### Session 2 — Companion view (PWA part 1)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
