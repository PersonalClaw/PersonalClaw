<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-1` — S1 audit: drift reporter + a11y static scan + ranked audit doc (T1.1/T1.2/V1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #202 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 — The audit (map only): T1.1 token-lint reporter, T1.2 primitive-adoption/a11y/parity scan → docs/design/consistency-audit.md; V1

## Done when

web/src/design/consistencyAudit.report.ts (scanDrift/rankFiles + scanA11y) + consistencyAudit.test.ts emit docs/design/consistency-audit.{json,md} ranked worst-first; V1 spot-check of 5 findings holds; vitest green; no fixes made (map-only). Recorded the dominant drift is primitive-adoption (420 raw <button> + 206 raw inputs), token layer near-clean.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`DSC-3`](DSC-3.md) (`done`), [`DSC-5`](DSC-5.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 10 (DSC) — observed

**Code evidence:**

- the drift reporter and a11y static scan are real modules with real rails: web/src/design/consistencyAudit.report.ts plus consistencyAudit.test.ts, consistencyAudit.noRepoWrites.test.ts and consistencyAudit.generate.test.ts
- part of 841 passing tests across 65 design-rail files

**Notes:** The noRepoWrites sibling is the detail worth naming: the audit has a rail asserting it does NOT write to the repo during a plain test run, which is a discipline most audit tooling lacks.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T1.1`, `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:60` — | T1.1 | Extend token-lint into a reporter: emit every hardcoded design value in `web/src` grouped by file + type, without failing the build | `web/src/design/tokenLint.test.ts` (+ a report script) | report lists all drift with file:line; counts by category |
- `T1.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:98` — - **DONE (T1.1):** Built the drift reporter — `web/src/design/consistencyAudit.report.ts` (`scanDrift()` inventories color/spacing/radius/shadow/duration + a primitive-adoption scan, grouped by file:line; `rankFiles()` weighted worst-first) and `web/src/design/consistencyAudit.test.ts` (runs the …
- `T1.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:61` — | T1.2 | Primitive-adoption + a11y + parity scan → the ranked audit doc | `docs/design/consistency-audit.md`, scan scripts | audit ranks the worst offenders (likely ChatPage/CodeCockpit/loop cockpits); each entry actionable |
- `T1.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:99` — - **DONE (T1.2):** Authored `docs/design/consistency-audit.md` — the ranked drift map from measured data. V1 spot-check: 5 findings verified against code (all hold).
- `T1.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:106` — - **DONE (T1.2, a11y portion):** Extended the reporter with `scanA11y()` (reduced-motion + focus-visible coverage) and wired it into the JSON + audit doc. Replaced the "pending" a11y placeholder with measured coverage.
- `Session 1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:56` — ### Session 1 — The audit (map only)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
