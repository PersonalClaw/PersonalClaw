<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AE-3` — Artifacts get their own top-level #/artifacts route; Files artifacts tab deleted; legacy #/files/<slug> redirect

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ARTIFACTS-EVOLUTION](../plans/ARTIFACTS-EVOLUTION.md) (`AE`, plan status `done`) |
| **Atom table** | [`../atomic/AE.md`](../atomic/AE.md) |
| **Execution position** | #13 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 T1.3; Contracts C3 (Routes + library surface)

## Done when

Files shows only file roots; an old #/files/<slug> link lands on #/artifacts/<slug>; the relocated viewer works byte-equivalently at its new home

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`AE-4`](AE-4.md) (`done`), [`AE-6`](AE-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:112` — | T1.3 | Route split: `#/artifacts[/<slug>]` top-level route + nav entry; `FilesSection` drops the artifacts tab (delete `pages/files/artifacts/` after relocation); `#/files/<slug>` hash-redirect; skill/docs prose updated to the real route | `web/src/app/App.tsx`, nav shell, `web/src/pages/files/…
- `T1.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:170` — - [2026-07-27][S1b] DEFERRED (T1.3, route split): moving artifacts to their own
- `T1.3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:179` — - [2026-07-27][S1b] DONE (T1.3, route split): artifacts are their own top-level surface.
- `Session 1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:106` — ### Session 1 — Entity split + store hardening

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
