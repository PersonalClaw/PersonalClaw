<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `ES-4` — Judge benchmark harness → tier-recommendation table

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EVALUATION-SUBSTRATE](../plans/EVALUATION-SUBSTRATE.md) (`ES`, plan status `done`) |
| **Atom table** | [`../atomic/ES.md`](../atomic/ES.md) |
| **Execution position** | #244 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§6 (generalizes loop/instrument.py:probe_judge across tiers)

## Done when

fixture set (real judged runs, deliberately-bad null probes, forbidden-success-mode cases) runs through the matrix over fixtures × judge tiers × judge_samples 1/3/5; the tier-recommendation table shows agreement-with-known-verdict, strong-vs-null separation, position-swap flip rate, cost and wall time per (rubric-class × tier × samples) with honest failure-mode notes, rendered on a Settings/Learning panel; rebinding a judge to the cheapest adequate tier is one user action on the Models panel

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`ES-1`](ES-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `1286891ab` 2026-08-18 — feat(evals): ES-4 judge benchmark harness → tier-recommendation table (#1502)

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:462` — ## Execution log — ES-4 (judge benchmark harness → tier-recommendation table) — §6 COMPLETE
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:464` — - **ES-4 DONE.** `evals/judge_bench.py` (+ the shipped `evals/benchmarks/judge/starter.json`)
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:581` — template arm. There is also **no invocation surface**: the sibling precedent is explicit — ES-4's
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:853` — `GET /api/evals/ablation` shipped with **no frontend consumer**, unlike ES-4's sibling
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:990` — ES-4's judge bench is — shared `expand_cells`/`aggregate`/`aggregate_by`, the same `matrices/<id>/` sinks,
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1035` — ES-4's `list_bench_runs` claimed every `matrices/<id>/` that had a `table.json` — a working proxy for "is
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1036` — a judge bench" only while ES-4 was the only writer of one. ES-3 is a second writer, so the newest
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1042` — a prefix rule stranded ES-4's own runs, which is the same bug with the roles swapped. An unstamped table
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1706` — {name: scenario_sha256}>)` — ES-4's move for a fixture SET. `RunPin.is_complete()` is a

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§6`. Those tokens appear in its source plan at:

- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:14` — **Wave:** 3-4 — the study registry, retrieval harness, and judge benchmark (§3, §5, §6) are v2-independent and can front-run; template studies and the trust ladder (§2, §4) consume the WORKFLOWS-V2 Run Ledger (Slices 0-3) and LEARNING-FLYWHEEL's proposal queue (steps 3+), so they land after the f…
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:15` — **Depends on:** WORKFLOWS-V2 (Run Ledger acceptance criteria, §5 event table) for §2/§4/§8; WORKFLOWS-V2-LEARNING-FLYWHEEL (proposal queue, GateOK, change manifests, maturity levels) as the machinery this plan feeds evidence into; AUTONOMY-GUARDRAILS §2 (`model_calls.jsonl` attempt audit) for §7'…
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:24` — - **NEW-11 amendment 1** (judge benchmark harness — fixed inputs × model tiers × iteration counts → tier-recommendation table with honest failure-mode notes; experiment-matrix runner shared between harness-ablation and local-model validation; model bake-off from production-sampled real inputs; bu…
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:33` — | **LOOP-R3** — rubric convergence contract (fixed 0-2 dimensions, evidence citation, `judge_samples: N` median, judge-verdict ledger + hardening loop) | LOOPS-EVOLUTION Runtime Hints + Migration Checklist | The rubric contract is the *format*; §2.3's blinded judging and §6's judge benchmark *pin…
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:50` — - A judge-calibration probe EXISTS: `loop/instrument.py:probe_judge` (strong-vs-null separation ≥1.5) — §6 generalizes exactly this shape across model tiers instead of inventing a new one.
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:58` — **One sentence of architecture:** everything below is *one small store* (`~/.personalclaw/evals/`), *one shared runner* (§1), and *five consumers of it* (§2 studies, §3 ablation, §5 retrieval, §6 judge benchmark, §7 bake-off) — plus the trust ladder (§4) that turns their outputs into autonomy gra…
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:75` — benchmarks/<name>/         # fixture sets: retrieval corpora+qrels (§5), judge fixtures (§6), ablation sets (§3)
- `§6` `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:152` — - **Agreement checks**: (a) each pair is judged with `judge_samples: 3` median (the approved LOOP-R3 mechanism, reused verbatim); (b) each pair is additionally judged **position-swapped** (A/B then B/A) — a pair whose verdict flips with position is recorded as `tie/no-signal`, not counted for eit…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
