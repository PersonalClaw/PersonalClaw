<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-5` — Canonical form-field family: ui/forms convergence + size/surface scale + NumberField (T2.3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #206 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 T2.3 (missing primitive) + Owner task 2 (two-patterns→one); Execution log cy5, cy17, cy28–cy30, cy35

## Done when

dead ui/TextField removed; the adopted formControls generics relocated to ui/forms.tsx (Field a11y wiring + TextInput/TextArea/DateInput/Select/ChipInput/NumberField) with 24 importers repointed; principled size×surface scale byte-identical at comfortable density (density-correct px-m); forms.test.tsx locks the scale + accessible-name logic; ratchet rawInput drawn down in lockstep.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-1`](DSC-1.md) | intra-repo | `done` |
| [`DSC-2`](DSC-2.md) | intra-repo | `done` |

### Unblocks (1)

[`DSC-6`](DSC-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (23 line(s))

This atom's scope cites `T2.3`, `Session 2`, `cy5`, `cy17`, `cy28`, `cy30`, `cy35`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:70` — | T2.3 | Add any genuinely-missing primitive to the system ONCE (if the audit found a real gap) with a pattern-gallery entry | `web/src/ui/`, `docs/design/patterns.md` | new primitive documented + adopted where needed; not a one-off |
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:101` — - **DISCOVERY:** There is **no shared form-field primitive** — the one genuine system gap (T2.3): a `TextField`/`Field` in `web/src/ui/` + `patterns.md` entry, to land before migrating the 206 raw inputs.
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:124` — ### 2026-07-22 — S2 cycle 5 (T2.3: the missing form-field primitive + pattern gallery)
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:125` — - **DONE (T2.3 — the one genuine system gap):** added `web/src/ui/TextField.tsx` — `TextField` (single-line) + `TextArea` (multi-line) with `size` (sm/md/lg → h-8/9/10), `surface` (container/high/base), `mono`, and `leadingIcon` variants. It is the CANONICAL EXTRACTION of the exact input shape ~2…
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:290` — ### 2026-07-23 — S2 cycle 30 (T2.3: codify the standard-field size/surface scale)
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:292` — - **DONE (T2.3 — the scale, byte-identical):** `TextInput` now carries a principled **size** (sm/md/lg → h-8/h-9/h-10) × **surface** (container/high/base) scale via `FIELD_SIZE`/`FIELD_SURFACE` tables + a shared `INPUT_BASE` invariant-chrome const. Defaults (lg/container/0.9375rem) reproduce the …
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:303` — - **DONE (T2.3 adopter — first cluster):** migrated the **7** raw `<input>` in `AddToolServerModal` (`pages/tools/ToolsPage.tsx`) — mcp branch (name/command/args) + openai branch (name/endpoint/apiKey/toolFilter) — to `<TextInput size="md" surface="high" [mono] [type="password"]>`. The `mcpInputC…
- `T2.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:316` — - **DONE (T2.3 adopters — 6 bare leading-icon filters):** migrated to `<TextInput … leadingIcon={<Search size={14} />}>`:
- `Session 2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:64` — ### Session 2 — Token + primitive hardening
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:489` — - **NEXT (item 1 mega-page queue CLOSED — every mega-page triaged):** ChatPage (cy48), ProjectsSection (cy49), and CodeCockpitPage (cy50) are done; the three largest mixed-role pages are triaged and their clean slices extracted (SelectionPill, InlineError, Centered). **No mega-pages remain.** Wha…
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:491` — ### 2026-07-24 — OWNER TASTE-CALL RULINGS (the cy50 batch, decided after visual inspection of the real-CSS comparison screenshots) — the taste/pixel lane is now unblocked
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:492` — - **Context:** the cy50 "batch of taste/pixel decisions" was surfaced to the owner as a set of faithful comparison screenshots (real compiled-CSS harness, dark+light, every divergent variant beside its shared-primitive reference, labelled file:line). Owner inspected all groups ("each of these loo…
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:505` — ### 2026-07-24 — cy51 DONE (G1 B′ token normalization — warm-up, batch cycle 1/9)
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:513` — - **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` with a real drift reduction — `LoopCockpitPage` raw-button count `10 → 9` (score `22 → 20`) from the G7 conversion. Unlike cy51 (pure timestamp churn, correctly reverted), this is …
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:517` — - **Context:** the Cancel/Done idiom is *already* overwhelmingly converged onto `Button variant="ghost"` (a full-tree grep shows ~30 sites already using it). G6's ruling targets only the **two remaining divergent surface-filled pills** the cy50 screenshots surfaced: `FilterMenu:83` "Done" and `Ta…
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:524` — - **Context:** the retry idiom is *already* largely converged onto `Button` — the cy50 audit named CodeCockpitPage:2904/2997 (quiet inline `variant="ghost" size="xs" text-primary`) and CodeCockpitPage:471 (filled `variant="secondary"`) as the two established precedents. G3's ruling targets the **…
- `cy5` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:531` — ### 2026-07-24 — cy5 DONE (G4 text-link idiom — extract the `<TextLink>` primitive + migrate every hand-rolled `text-primary hover:underline` site — batch cycle 5/9)
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:664` — ### 2026-07-24 — cy17 DONE (S3 T3.1 interaction-pattern standardization — the last two hand-rolled page-empties converge onto the shared `EmptyState` primitive; owner-authorized under the full-authority grant)
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:672` — - **Docs:** `patterns.md` — replaced the line-163 "should adopt … as they're touched" TODO-note with a **cy17 convergence record** (both sites now on `EmptyState`, markup locked by the test) + an explicit "remaining centered blocks are load-error / not-found / filtered / slot — don't force them o…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:677` — - **Why a no-code cycle is the honest move (not a stop, not a manufactured edit):** the autonomous-loop-continuity directive says don't stop while authorized work remains — but the soul guardrail ("consistency, not redesign; add a shared primitive *only when the system genuinely lacks a pattern*"…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:682` — - **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:712` — - **Live validation — driven in a real browser, in BOTH themes, and cy17's theme caveat is now retired.** Served this worktree's frontend on Vite `:3100` proxied to a gateway on `:10000` with an ISOLATED `PERSONALCLAW_HOME` (deleted after) and `AUTH_MODE=none`, then drove it with the bundled Play…
- `cy35` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:485` — - **Discovery → the clean slice the triage exposed (verbatim component dedup, GraphZoomControls/cy38 class):** sweeping the page's helpers surfaced that its `Centered` layout wrapper (`function Centered({children}) { return <div className="flex h-full items-center justify-center">{children}</div>…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
