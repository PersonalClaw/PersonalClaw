<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DAS-9` — Workspace time-travel — adaptive-debounce git + rollback/revert/preview + panel (Session 5, §5)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DURABILITY-AND-SYNC](../plans/DURABILITY-AND-SYNC.md) (`DAS`, plan status `done`) |
| **Atom table** | [`../atomic/DAS.md`](../atomic/DAS.md) |
| **Execution position** | #147 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1836 |

## Intent — what this is for

§5 Workspace Time-Travel (NEW-4.a); §3 deferred hourly git commit of the memory tree; Implementation Order Session 5 (time-travel half)

## Done when

state-history git repos cover config.json/entity_settings, skills/, the memory markdown tree (workspace/memory, _ext, plan_memory), prompts/+prompt_snippets, projects/<id>/context; adaptive-debounce commits (10s→0, serialized per root) hooked at the atomic_write post-write seam; hourly git commit of the memory tree/shards lands (the deferred §3 piece); rollback (hard reset, prior HEAD preserved in service refs) vs revert (reverse-merge, loud overlap failure) with a mandatory preview; secrets gitignored yet preserved across rollbacks; Settings → Durability → Time Travel panel (timeline, diff preview, rollback/revert, what-changed-while-I-slept filter); criterion 6 met; time-travel never syncs

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `9e114b4a5` 2026-08-20 — docs(roadmap): cite PR #1836 for DAS-9
- `014f54f05` 2026-08-20 — feat(durability): DAS-9 workspace time-travel — per-file restore, background surface, live-driven
- `fed353986` 2026-08-18 — feat(durability): DAS-9 workspace time-travel engine + atomic_write post-write seam (#1723)

### Named by atom id in the roadmap prose (19)

- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1467` — atoms (DAS-7+/DAS-9), not part of the Session-3 sync program this completes. **Gates:** `make lint`
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2139` — ## Execution log — DAS-9 (§5 workspace time-travel) — **PARTIAL**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2141` — - [2026-08-18][DAS-9] **DONE — the engine, the seam, the debounce, rollback/revert/preview, the
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2154` — - [2026-08-18][DAS-9] **DESIGN — the repo is a separate git DIR, never a `.git` in the user's tree.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2166` — - [2026-08-18][DAS-9] **BOTH HALVES OF THE SECRET CLAIM ARE PROVEN, and they are proven differently.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2176` — - [2026-08-18][DAS-9] **"NEVER SYNCS" IS PROVEN BY BUILDING THE ENTRY SET, not by reading a list.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2187` — - [2026-08-18][DAS-9] **THE DEBOUNCE IS DRIVEN BY AN INJECTED CLOCK, never by sleeping.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2201` — - [2026-08-18][DAS-9] **THE MANDATORY PREVIEW IS SERVER-ENFORCED, not a frontend convention.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2210` — - [2026-08-18][DAS-9] **BUG FOUND AND FIXED IN MY OWN CODE: `str.splitlines()` eats `\x1e`.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2218` — - [2026-08-18][DAS-9] **DISCOVERY — an isolated home does NOT confine the workspace, and this plan's
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2229` — - [2026-08-18][DAS-9] **UNMET, deliberately, with evidence.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2246` — - [2026-08-18][DAS-9] **Gates:** `make lint` exit 0 (black 1792 files, isort, flake8, mypy 921
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2260` — - [2026-08-20][DAS-9] **DONE — the five items recorded UNMET on 2026-08-18 are resolved.** Two were
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2266` — - [2026-08-20][DAS-9] **(2) `SURFACE_BACKGROUND` has a producer.** Before: the only
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2275` — - [2026-08-20][DAS-9] **(3) Per-file restore.** `preview_rollback`/`preview_revert`/`preview`/
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2288` — - [2026-08-20][DAS-9] **(5) Driven through a live gateway — and it found a bug the rails could not.**
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2302` — - [2026-08-20][DAS-9] **A limit worth recording, because two falsifications found it and a future
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2311` — - [2026-08-20][DAS-9] **Gates:** `make lint` clean (mypy, 941 source files) · 236 passed across the
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1007` — dodge the counter), **durable-write 5** (DAS-9's `mkstemp`+`rename` bypass, in 4 files).

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§5`, `§3`, `Session 5`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:29` — - **NEW-4.a** — workspace-level adaptive-debounce git auto-commits on config/skills/memory/projects + rollback/revert/preview UI panel → §5
- `§5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:240` — - **Session 5 — Time-travel + portability endpoints + FE.** §5 adaptive-debounce repos + rollback/revert/preview; §6 export/import/archive endpoints; Settings → Durability panel (status card, archive browser, sync config via the standard `/api/providers/...` routes, time-travel timeline, conflict…
- `§5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:381` — Independent of the schedule loop; belongs with the §5 time-travel work that owns the
- `§5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2139` — ## Execution log — DAS-9 (§5 workspace time-travel) — **PARTIAL**
- `§5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:2230` — (1) **`plan_memory/`** from the §5 root list **does not exist in the codebase** — `grep -rn
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:28` — - **NEW-4** — durable state backup + multi-machine sync core (JSONL shards + SHA manifest + validate; pull→merge-import→export-union→push; boot-started scheduled self-backup with rolling backups + restore endpoints; git-versioned memory snapshots; encrypted-intents crypto for machine exchange) → …
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:94` — - **`personalclaw backup validate`:** manifest well-formed → every shard exists → bytes/rows/sha match → every row parses → sqlite dumps re-import cleanly into a scratch DB. Non-zero exit for CI/cron use. This is also the restore-drill core (§3).
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:170` — - **Roots:** `config.json` (+ entity_settings), `skills/`, the memory markdown workspace (`workspace/memory`, `workspace/_ext/`, `plan_memory/`), `prompts/` + `prompt_snippets/`, and `projects/<id>/context/`. Each root is (or shares) a local git repo under the `state-history` umbrella (§3).
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:186` — - `GET /api/durability/archive` + `POST /api/durability/archive/{id}/restore` — the snapshot list/restore pair (§3) exposed as the archive browser: date, size, per-domain row counts (from the manifest), validate status from the last drill.
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:207` — | `snapshot.py` | **ABSORBED** (kept as the tar engine) | `VALID_COMPONENTS`/`CORE_FILES` re-derived from the inventory (§1); gains `everything`; sqlite-backup-API applied to ALL `*.db` discovered (fixes the live `knowledge.db` raw-copy hazard); WAL checkpoint, `_data_filter`, atomic tar rename, …
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:280` — - **Inventory-driven merge (extends §1/§3, no new mechanism):** `restore --mode merge` iterates `durability/inventory.py` entries and dispatches on the already-specified `merge` field — `union_by_id` (entity dirs: copy rows/files whose id is absent), `sqlite_attach_ignore` (memory/knowledge DBs —…
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:282` — - **Auto-detect default (sharpens :650):** restoring onto a non-empty home (any non-derived inventory entry present — not just `memory.db`) → propose merge, print the plan, require confirmation; `--replace` stays the explicit wipe path and keeps the `_is_gateway_running` refusal + `pre-restore-<t…
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:302` — - [2026-07-28][S1] Remaining: S2 (shard exporter + manifest + `backup validate` + boot-started snapshot service + restore drills) and S3 (sync core + `git-sync`/`dir-sync`). §3's rolling retention tiers were NOT built this session — the existing `--keep N` remains; the tiered N-nightly/M-weekly/Y…
- `Session 5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:240` — - **Session 5 — Time-travel + portability endpoints + FE.** §5 adaptive-debounce repos + rollback/revert/preview; §6 export/import/archive endpoints; Settings → Durability panel (status card, archive browser, sync config via the standard `/api/providers/...` routes, time-travel timeline, conflict…
- `Session 5` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:287` — Folded into **Session 2** (which already owns the restore endpoints + `backup validate`); the merge-plan UI confirm lands with Session 5's archive browser. Session count stays 5.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
