<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-7` — Slice 8 — live chat widget + event pipeline (dedup/fold/coalesce) incl. WF2-A1 step_cached emission

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #676 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Slice 8 + Amendment WF2-A1

## Done when

WorkflowProgressCard mirrors SdlcProgressCard; dedup keys + deterministic event ids + event-fold law + epoch-tagged supersede + ~25ms coalescing + result_omitted spill; FE lifecycle-union registration + backend⊆FE-union test; typed ask-payload renderer; step_cached ledger event + cached flag on workflow_node_done

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-6`](WV-6.md) | intra-repo | `done` |

### Unblocks (3)

[`WV-10`](WV-10.md) (`done`), [`WV-8`](WV-8.md) (`done`), [`WV-9`](WV-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `Slice 8`. Those tokens appear in its source plan at:

- `Slice 8` `docs/roadmap/plans/WORKFLOWS-V2.md:747` — ### Slice 8: Live Chat Widget + Event Pipeline (est. 3 sessions)
- `Slice 8` `docs/roadmap/plans/WORKFLOWS-V2.md:851` — **Lands in:** Slice 1 (`step_cached` emission — journal.py already computes the hit), Slice 7 (inspect endpoint + drawer), Slice 8 (widget badge), Slice 11 (regression pair). No new slice; honest estimate 31 → **32 sessions** (+1 spread across Slices 7/8).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
