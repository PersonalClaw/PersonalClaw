<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2TAS-11` — Engine call sites + Task write + verified-done/confirmation emission + DagView composition + config four-point (+fifth) wiring

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-TASKS-SOPS](../plans/WORKFLOWS-V2-TASKS-SOPS.md) (`WF2TAS`, plan status `done`) |
| **Atom table** | [`../atomic/WF2TAS.md`](../atomic/WF2TAS.md) |
| **Execution position** | #633 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1 Materialization Flow call site + Task write (ENGINE actor); §7 DagView Approve/Deny wiring + checklist edit UX; §9 Config four points + settings.py resolvers

## Done when

RunController projects at node settle (correct id/TaskSpec keys), writes the managed Task via engine actor with reject_write guard, executes done_criterion (passed+unrunnable tristate) and emits confirmation-gate events; WorkflowRunDetail renders DagView with wired Approve/Deny; four WorkflowsConfig fields + fifth settings.py resolver point wired and validated as-a-user; checklist checked-locks-drag + two-stage delete (S61f–S61k, ~120 tests); DEVIATION: match_threshold not re-added

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2TAS-1`](WF2TAS-1.md) | intra-repo | `done` |
| [`WF2TAS-2`](WF2TAS-2.md) | intra-repo | `done` |
| [`WF2TAS-3`](WF2TAS-3.md) | intra-repo | `done` |
| [`WF2TAS-9`](WF2TAS-9.md) | intra-repo | `done` |
| [`WF2TAS-10`](WF2TAS-10.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:RunController tick/completion path + _publish stream` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `§1`, `§7`, `§9`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:26` — - **TASK-R1** → §1 State Projection (typed block/status taxonomy + card previews)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:27` — - **TASK-R2** → §1 Verified Done (done_criterion, evidence, engine-owned transitions, diagnostics sweep, clean-exit template, granularity lint)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:30` — - **TASK-R5** → §1 Projection Enforcement (API-rejected writes, actor matrix, fingerprint dedup, fan-out caps, idempotent recompute)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:37` — - **TASK-R12** → §1 Task Body Contract + §6 Context Bundles
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:42` — - **TASK-R17** → §1/§5 Cascade-Fail Propagation
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:258` — `requirements` preflight: a def whose requirements are unmet **fails at suggestion time naming the missing item** instead of surfacing a suggestion that dies mid-run. The preflight uses the real availability seam that already exists: provider bundles may export module-level `availability() -> (bo…
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:422` — - Stages carry `enabled: bool` — muted = skipped-but-visible; projects as `skipped` per §1 (not removed), letting users trim a checklist instance (e.g., mute the staging gate for a hotfix) without structural edits.
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:453` — | Manual block (external reason) | `gate{kind: approval, prompt: "Blocked: {reason}"}` — pauses projection per §1 |
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:40` — - **TASK-R15** → §7 Surfacing UX (composer chips, validated deep-links)
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:51` — 5. The FE DagView node-level **Approve/Deny (`onApprove`/`onDeny` + `awaiting` state) is a declared, UNWIRED extension point** (`web/src/pages/tasks/DagView.tsx`) — §4's ConfirmationRequest is the missing backend seam and §7 wires it.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:409` — - One entity means ONE inbox surface (dashboard ActionCenter / "Needs you") and ONE autonomy policy instead of three bespoke flows — and it is exactly the backend seam the FE DagView's unwired `onApprove`/`onDeny` extension point has been waiting for (§7).
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:569` — - **Session 3 — ConfirmationRequest + gates:** the unified durable record with atomic single-use resolution + auto-resume; `require_hitl`; needs-input queue semantics; DagView Approve/Deny wiring; guardrails/postconditions def sections + verbatim injection; per-stage mute + step-scoped approval m…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:797` — `onApprove`/`onDeny` is the declared extension point this session supplies the backend for (§7 wires
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:911` — surfaces to §7/session 7, and the fields land with the def-model session. The semantic channel
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:980` — belong with §7's wiring session; the decision rules they implement are complete and tested here.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:1092` — The frontend half of §7's surfacing UX, driven against a live gateway rather than asserted.
- `§9` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:112` — - **Fan-out caps**: a `foreach` body materializes at most ~20 child tasks before collapsing to one parent task with a progress counter; auto-generated task trees respect a depth cap (default 3, max 5). Configurable via `workflows.max_materialized_per_foreach` (§9 config wiring).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
