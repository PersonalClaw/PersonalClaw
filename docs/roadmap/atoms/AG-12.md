<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AG-12` — Restore the §1.2 denylist at the third dispatch seam (gateway._fire_store_trigger)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AUTONOMY-GUARDRAILS](../plans/AUTONOMY-GUARDRAILS.md) (`AG`, plan status `done`) |
| **Atom table** | [`../atomic/AG.md`](../atomic/AG.md) |
| **Execution position** | #32 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1216 |

## Intent — what this is for

§1.2 enforcement placement — the third of the three dispatch seams. §1.2 names it `gateway.py` (`_run_action_job`), which retired with `ScheduleService` (S112); the successor `_fire_store_trigger` kept the kill switch and gained AG-7's rung routing but never re-established the denylist. Measured `enforce_action` per seam file: hooks.py 1, event_triggers.py 1, gateway.py 0 — while gateway is the dispatch path for every clock, file, webhook and chained trigger.

## Done when

`_fire_store_trigger` calls `enforce_action` before `provider.execute` and a blocked decision short-circuits without executing; the refusal is observable (enforce_action's SEL row + needs_human notification, plus a `skipped_gate` Runs-history row naming the matched rule); a test drives a REAL trigger fire and asserts the provider was never reached plus the allowed inverse; `session_key` is threaded so the run's SafetyProfile.denylist_extra/path_allowlist layer as at the other two seams; the chokepoint rail asserts all three execution seams carry the denylist so a fourth seam or another retirement cannot drop it again — DONE 2026-08-13 (18921 passed + 3 pre-existing worktree-only harness_validate failures; 15 new tests). Population measured before enforcing: the only real store available (dev-home) held 1 trigger (`notification-digest`, empty config) → 0 blocked; a 24-config corpus covering every shipped provider blocked 4, of which 3 are credential-exfil/sensitive-path refusals and 1 (`rm -rf /tmp/...`) is a plausible cleanup command already refused at the other two seams AND by the agent's own bash tool (same `BUILTIN_DENIED_COMMAND_PATTERNS`). So this adds no new policy — it removes the one seam that was exempt from the existing one.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AG-3`](AG-3.md) | intra-repo | `done` |
| [`AG-7`](AG-7.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 9 (AG) — observed

**Code evidence:**

- tests/test_baseline_denylist_integrity.py passes (part of the 333-passed run)

**Notes:** A RESTORATION claim at a specific seam — the third dispatch seam regaining the denylist. Like WF2TAS-12's retirement, this is the decidable-by-looking class, and its integrity rail passes.

## Recorded history

### Commits (1)

- `5b46fdcd4` 2026-08-13 — feat(guardrails): AG-6 earn autonomy per action type, lose it on one rejection (#1197)

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:861` — ### 2026-08-13 — Atom AG-12 (§1.2 denylist at the THIRD dispatch seam) — DONE

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§1.2`. Those tokens appear in its source plan at:

- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:13` — chokepoint, §1.1 budgets, §2.2 scan, §1.2 denylist, §1.3 incident kill switch, §1.4
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:246` — - **Action providers:** unchanged contract (`action_providers/base.py:ActionProvider`); the denylist is enforced at the three dispatch seams (§1.2), so app-contributed providers inherit it. Any NEW action provider still MUST be added to `ALLOWED_HOOK_PROVIDERS` (`src/personalclaw/validation.py`) …
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:261` — - **Session 3 — the floor (§1.2-§1.4, §5):** denylist + `check_action` at the three dispatch seams + `sdk.guardrails`; incident kill switch (flag + seam checks + endpoints + CLI); `DISABLE_LIVE_WRITES` honored + auto-set in conftest; `guard_flag` helper + safe-default schema test; SEL wiring thro…
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:480` — **Remaining for later sessions:** §1.2 denylist + `check_action` at the 3 dispatch seams,
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:485` — ### 2026-07-25 — Session 3 (§1.2 denylist + §1.3 incident + §1.4 DISABLE_LIVE_WRITES + §5) — DONE
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:861` — ### 2026-08-13 — Atom AG-12 (§1.2 denylist at the THIRD dispatch seam) — DONE
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:863` — **PROVENANCE — the gate was lost in a retirement, not never built.** §1.2 (line 96) declares the
- `§1.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:938` — joins it: `DENYLIST_SEAMS` names the three §1.2 seams and requires `enforce_action` in each, found

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
