<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-12` — Convenience channels: Homebrew tap + Nix flake (S5, post-launch, out of scope for this loop)

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #189 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`owner`) |
| **Blocked reason** | BLOCKED-OWNER: acceptance requires `brew install personalclaw/tap/personalclaw` on a CLEAN MAC plus a new personalclaw/tap repository (outward-facing repo creation, owner-only), and `nix run .#personalclaw` — verified `nix` is ABSENT on this machine, so the nix half cannot be verified here either. |

## Intent — what this is for

Design §A (Homebrew/Nix row); Session 5 T5.1–T5.2, V5; Owner task 3

## Done when

`brew install personalclaw/tap/personalclaw` works on a clean mac; `nix run .#personalclaw -- --version` prints the version; per-release smoke checklists recorded; README matrix updated

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-10`](DIST-10.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 19 (DIST) — wheel built and booted on this machine

**Code evidence:**

- dag.json records `todo`, and the atom's own title says '(S5, post-launch, out of scope for this loop)'
- no Homebrew tap or Nix flake exists in the repo, which MATCHES that status

**Notes:** Confirmed as a correctly-recorded TODO rather than as delivered work — the code state and the recorded state agree, which is the only thing an audit can confirm about an atom nobody has started. Listing it as done anywhere would have been the contradiction.

## Recorded history

### Commits (1)

- `3f0845f9d` 2026-08-11 — feat(learning): WF2LEA-11 retroactive run→skill promotion — proposal only, never a write (#1086)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/DISTRIBUTION.md:383` — - **BLOCKED `DIST-12`** (Homebrew tap + Nix flake) — **owner-only acceptance, with evidence.** The
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1043` — not re-derive it. All four owner-priority areas hold nothing startable: `DIST-11`/`DIST-12`/`CRE-7`

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T5.1`, `T5.2`, `Session 5`. Those tokens appear in its source plan at:

- `T5.1` `docs/roadmap/plans/DISTRIBUTION.md:190` — | T5.1 | Homebrew tap: `personalclaw/homebrew-tap` repo, formula installing via `uv tool` (or brew python) pinned to the latest release; per-release bump automation in release.yml | tap repo, `release.yml` | `brew install personalclaw/tap/personalclaw` works on a clean mac |
- `T5.2` `docs/roadmap/plans/DISTRIBUTION.md:191` — | T5.2 | Nix flake: package + `nix run` app output; flake check in full.yml (best-effort, report-only) | `flake.nix`, `flake.lock` | `nix run .#personalclaw -- --version` prints the version |
- `Session 5` `docs/roadmap/plans/DISTRIBUTION.md:186` — ### Session 5 — Convenience channels (post-launch)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
