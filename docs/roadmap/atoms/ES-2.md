<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `ES-2` — RunPin + versioned scenario library migration (amendment E1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EVALUATION-SUBSTRATE](../plans/EVALUATION-SUBSTRATE.md) (`ES`, plan status `done`) |
| **Atom table** | [`../atomic/ES.md`](../atomic/ES.md) |
| **Execution position** | #242 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1359 |

## Intent — what this is for

Amendment 2026-07-26 E1 — evals/pinning.py RunPin, Loop-1 scenario library, results.tsv pin

## Done when

eval/scenarios/*.json migrate to versioned ~/.personalclaw/evals/scenarios/ over named seeded fixture homes; every matrix/study/gate run persists a RunPin (scenario_sha256, model_fingerprint, prompt_pack_sha256, config_snapshot_ref); re-running a scenario after a model rebind yields a different fingerprint row with the same scenario hash, and a run without a pin cannot be written to results.tsv

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`ES-1`](ES-1.md) | intra-repo | `done` |

### Unblocks (1)

[`ES-6`](ES-6.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 16 (ES) — DRIVEN in a real browser

**Code evidence:**

- evals/pinning.py:12-29 defines all four RunPin fields the atom names — scenario_sha256 (canonical-JSON hash), model_fingerprint (per-use-case Provider:model), prompt_pack_sha256 (over the RESOLVED pack), config_snapshot_ref (over the relevant AppConfig subset)
- pinning.py:24-26 keeps a cell's own fingerprint as a SECOND fact under its own name (cell_model_fingerprint) rather than overwriting the operator-level one
- tests/test_evals_pinning.py green in the 628-passed run

**Notes:** The two-facts-not-one-overwrite choice at :24 is what makes a rebind diff readable later: the cell's binding and the operator's binding are different claims and are stored as such.

## Recorded history

### Commits (1)

- `d94c48582` 2026-08-15 — feat(evals): ES-2 versioned scenarios + run pins

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:435` — STUDIES concern for ES-2/ES-5, not here). **Gates:** `make lint` clean (718 files);
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:455` — FTS5-degrade-test fix). **Remaining EVALUATION-SUBSTRATE:** ES-2+ (RunPin, judge bench, studies,
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:214` — exist, and `ES-2` (which depends on `ES-1`) is already `✅`. The genuinely missing piece is the

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
