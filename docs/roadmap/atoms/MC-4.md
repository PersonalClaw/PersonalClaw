<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-4` — S3 PWA installability: manifest + service worker (app-shell precache, /api never cached)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #338 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1429 |

## Intent — what this is for

Session 3 T3.1 + C2 service-worker rule (network-first; explicit no-cache for /api/*)

## Done when

Lighthouse installability passes with manifest (claw-mark icons, standalone, start_url #/companion) and sw.ts precaching app-shell only; API responses are never served from cache (verified with offline toggle).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MC-3`](MC-3.md) | intra-repo | `done` |

### Unblocks (2)

[`MC-5`](MC-5.md) (`done`), [`MC-6`](MC-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `b3123d8fe` 2026-08-15 — feat(web): MC-4 PWA installability with an API-never-cached service worker

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:288` — manifest/service worker is `MC-4`; push and `#/companion?approval=<id>` are `MC-5`; the
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:303` — ### 2026-08-15 — `MC-4` (S3 T3.1 manifest + service worker) — **DONE**

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T3.1`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/MOBILE-COMPANION.md:119` — | T3.1 | Manifest (icons from the claw mark, standalone display, start_url `#/companion`) + service worker (app-shell precache ONLY; network-first for everything; explicit no-cache for `/api/`) | `web/public/manifest.webmanifest`, `web/src/sw.ts`, vite wiring | Lighthouse installability passes; A…
- `T3.1` `docs/roadmap/plans/MOBILE-COMPANION.md:138` — | T4.2 | QR pairing screen **and endpoint switching**, both consumed from plan 54: Settings → Devices → "Pair phone" renders a QR of COMPANION-APPS §C2's `{pairing_url, code}` → app scans → `pair/complete` → device session (§C1); the paired gateway then becomes a §C1 registry entry (`device_sessi…
- `T3.1` `docs/roadmap/plans/MOBILE-COMPANION.md:171` — - **S3 — Push-to-approval (milestone 1 completes here):** T3.1 (manifest/SW) + T3.2 (web push) + tap-through deep-link to the specific approval; V3 is the milestone validation.
- `T3.1` `docs/roadmap/plans/MOBILE-COMPANION.md:303` — ### 2026-08-15 — `MC-4` (S3 T3.1 manifest + service worker) — **DONE**
- `Session 3` `docs/roadmap/plans/MOBILE-COMPANION.md:115` — ### Session 3 — Installability + push (PWA part 2)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
