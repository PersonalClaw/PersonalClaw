<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `IA2-2` — POST /api/investigate endpoint: resolve → create session → set ask mode → stage envelope

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INVESTIGATE-ANYWHERE](../plans/INVESTIGATE-ANYWHERE.md) (`IA2`, plan status `done`) |
| **Atom table** | [`../atomic/IA2.md`](../atomic/IA2.md) |
| **Execution position** | #287 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

S1 T1.2 / Contracts C2 — the endpoint (§2.2 error envelope)

## Done when

endpoint round-trips returning {session_key, context}; 404 unknown_entity / 400 unknown_kind per §2.2; task mode set via existing set_task_mode path; envelope staged on session._investigate_ctx (mirrored to session meta); SEL log_api_access(operation=investigate_open) present

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`IA2-1`](IA2-1.md) | intra-repo | `done` |

### Unblocks (1)

[`IA2-4`](IA2-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.2`, `§2.2`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:104` — | T1.2 | `POST /api/investigate`: resolve → create session → set `ask` task mode → stage envelope → return session_key; §2.2 error envelope; SEL `investigate_open` | `dashboard/` handler + route registration (`server.py`), `investigate.py` | endpoint round-trips; 404/400 shapes per §2.2; SEL entr…
- `T1.2` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:133` — - [2026-07-27][S1] DONE: the primitive (T1.1-T1.5). **T1.1** `src/personalclaw/investigate.py`: `InvestigateContext` envelope + resolver registry (`register_investigate_resolver`/`resolve`; unknown KIND raises KeyError → the route's 400, unknown ENTITY returns None → 404), snapshot cap 8KB with a…
- `§2.2` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:63` — ### C2 — The endpoint (§2.2 error envelope)
- `§2.2` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:104` — | T1.2 | `POST /api/investigate`: resolve → create session → set `ask` task mode → stage envelope → return session_key; §2.2 error envelope; SEL `investigate_open` | `dashboard/` handler + route registration (`server.py`), `investigate.py` | endpoint round-trips; 404/400 shapes per §2.2; SEL entr…
- `§2.2` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:133` — - [2026-07-27][S1] DONE: the primitive (T1.1-T1.5). **T1.1** `src/personalclaw/investigate.py`: `InvestigateContext` envelope + resolver registry (`register_investigate_resolver`/`resolve`; unknown KIND raises KeyError → the route's 400, unknown ENTITY returns None → 404), snapshot cap 8KB with a…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
