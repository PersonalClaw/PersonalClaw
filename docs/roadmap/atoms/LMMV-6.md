<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `LMMV-6` — Session 5a — Subscription-credential model providers (credential_source resolver + one reference app)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [LOCAL-MODEL-MANAGER-V2](../plans/LOCAL-MODEL-MANAGER-V2.md) (`LMMV`, plan status `in_progress`) |
| **Atom table** | [`../atomic/LMMV.md`](../atomic/LMMV.md) |
| **Execution position** | #325 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§8 Subscription-Credential Model Providers (am.d)

## Done when

BrandedProviderSpec (sdk/provider_helpers.py) gains credential_source; _factory's credential order becomes entry.credential → options.api_key → subscription-source resolver → spec.api_key_env → anon placeholder; the resolver reads the named agent CLI's own credential store read-only (e.g. claude-code OAuth/keychain); not-logged-in fails soft/typed via providers/loader.py availability() reporting (False, 'sign in with `claude login` first') so the extensions list greys it out with the reason; ONE reference model-provider app ships (PersonalClawApps) riding CLI auth with no separate API key, sessions/models/catalogs flowing through the normal branded-app path (no agent runtime involved). NOTE CORE HALF LANDED, atom stays todo: the credential_source field, the five-hop _factory order, the read-only resolver (llm/subscription_credentials.py) and the availability() probe providers/loader.py derives from a declared credential_source are all in with tests (tests/test_subscription_credentials.py). ONLY the reference app remains, and it is a PersonalClawApps deliverable — a separate repository, unreachable from core (git ls-files apps/ is 0 here) — so the mechanism is production-INERT until one app registers a SubscriptionSource. Its exact app.json + provider.py contract is written out in the plan's Execution log entry for this session.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`LMMV-7`](LMMV-7.md) (`blocked`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 23 (LMMV) — code and tests observed on this machine

**Code evidence:**

- MEASURED: credential_source present in llm/branded_specs.py (the BrandedProviderSpec home), llm/subscription_credentials.py and providers/loader.py — three live sites, not one declaration
- 309 tests pass across the local-model / fit / sidecar / download modules (1 unrelated skip)

**Notes:** Same status mismatch as LMMV-2/3, resolved to done on the code. The clause's ordered credential cascade (entry → options → subscription resolver → spec env var → anon placeholder) ends in a PLACEHOLDER rather than an error, which is the same fail-to-unauthenticated posture ES-17's cell provider took: a missing credential degrades to 'can only reach an anonymous endpoint' instead of throwing.

**Follow-ups filed:**

- issue #2763 (LMMV-4 recorded done with none of its artifacts present; three sibling status mismatches resolved)

## Recorded history

### Commits (4)

- `7a015ce7e` 2026-08-19 — docs(roadmap): close LMMV-6 + ET-2 + EI-5, and make the atomic tables tell the truth (#1738)
- `97c9e0249` 2026-08-18 — docs(roadmap): flip PP-15 done — its work landed, its status did not (#1654)
- `d38abd7e6` 2026-08-17 — test(llm): LMMV-6 pin the whole five-hop subscription credential order
- `6a1e90589` 2026-08-16 — feat(local-models): LMMV-6 subscription-credential provider resolver

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:750` — - 2026-08-16 — **PARTIAL (LMMV-6 / Session 5a — Subscription-credential model providers). The
- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:852` — - 2026-08-17 — **DISCOVERY (LMMV-6 / Session 5a follow-up). The five per-hop precedence
- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:902` — - **2026-08-19 — `LMMV-6` CLOSED (`todo` → `done`), on the clause that kept it open.** The atom's
- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:916` — `dag.json` in three ways, and nothing could see it.** While closing `LMMV-6` I found its row

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `§8`. Those tokens appear in its source plan at:

- `§8` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:36` — - **NEW-8 am.(d)** — subscription-credential model providers (reuse Claude Code/Codex/Copilot auth) → §8
- `§8` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:778` — 3. **No refresh, ever — a DEVIATION from §8's wording.** §8 says the adapter reads the
- `§8` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:792` — **Core ships ZERO vendor rows (provider boundary).** §8's example is a specific agent CLI,

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
