<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-3` — MANIFEST.in grafts web/dist into the sdist so wheel-from-sdist carries the SPA

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #180 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #fix-sdist-bundles-spa |

## Intent — what this is for

Contract C4 wheel contract; Execution log 'Release dry-run — RELEASE-BLOCKING BUG'

## Done when

`python -m build` (no args, the release command) then verify_wheel.py PASSES — the wheel built from the sdist carries the SPA; guarded by tests/test_sdist_bundles_spa.py; must land before the first tag push

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-1`](DIST-1.md) | intra-repo | `done` |

### Unblocks (1)

[`DIST-10`](DIST-10.md) (`done`)

### ⚠️ In a dependency cycle

This atom sits in a cross-plan cycle, so no execution order can satisfy it as declared — the cycle has to be broken by a scope decision:

- `CRE-4 → DIST-3 → DIST-1 → CRE-4`

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `2f0dffd37` 2026-09-07 — feat(roadmap): rank the owner's queue by what each atom actually unblocks
- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `281d693b1` 2026-09-06 — docs(roadmap): execute the 2026-08-28 cycle ruling, and mint the atom for a seam nobody adopted

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:494` — **The other cycle is harmless and needs nothing:** `CRE-4 → DIST-3 → DIST-1 → CRE-4` is formed the same

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
