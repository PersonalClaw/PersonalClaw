<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2WOR-1` — Project umbrella + Work board: /work endpoint, lease files, hub FE

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-WORK-CONTAINERS](../plans/WORKFLOWS-V2-WORK-CONTAINERS.md) (`WF2WOR`, plan status `done`) |
| **Atom table** | [`../atomic/WF2WOR.md`](../atomic/WF2WOR.md) |
| **Execution position** | #649 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##892 |

## Intent — what this is for

§1 (Project extensions), §5.2 (truthful run lifecycle), §6.1 (Work tab + local-first rendering), Migration Order steps 1 & 4

## Done when

Success Criterion 1: GET /api/projects/{id}/work is registered (register_unified_loop_routes pattern) over containers.py's projection with per-section isolation; the hub Work tab shows runs+legacy loops+tasks in one state-grouped board (needs-input pinned, queued/suspended/claimed), truthful across a gateway kill; claim leases ride the concurrency.single_flight flock; overview auto-revises on run completion via the controller completion hook

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (4)

[`WF2WOR-2`](WF2WOR-2.md) (`done`), [`WF2WOR-4`](WF2WOR-4.md) (`done`), [`WF2WOR-6`](WF2WOR-6.md) (`done`), [`WF2WOR-7`](WF2WOR-7.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 66 (WF2WOR) — the archive extractor's path safety falsified and found unasserted (fixed in #2825); the Work board driven in a real browser

**Code evidence:**

- all three routes are registered, read off the router rather than the plan: `GET /api/projects/{project_id}/work`, plus `/work/claim` and `/work/release` (`tasks/hierarchy_handlers.py:997-999`)
- 🔑 I DROVE THE BOARD. A project detail page renders `Work · 0` with an ON-RAMP empty state — 'No work here yet — use New above to launch a loop, or start a chat' — so a count sits in the section header and an empty board reads as empty rather than as a board that failed to load
- the board is local-first + stale-while-revalidate on the same seam as the rest of the page ('the board paints from cache, then swaps in the fresh projection when it lands'), so a slow projection does not blank a surface that already had content
- 🔴 AND A LOAD FAILURE IS NOT A DELETION — observed directly: a bad project id renders 'Couldn't load your project', with the source recording why ('`error` was previously discarded, so a 500 fell through to `!project` and the page claimed the project was DELETED. A failed read is not a deletion')
- 555 passed across the containers, needs-input, export, container-workspace, publish, filedrop, introspection, fan-out, worktree and memory-locality suites; the Work board and a project detail page driven on :10011

**Driven in the UI:** Driven on :10011: the project list, a project's detail page with its Work section, and the load-failure surface.

**Notes:** Confirmed on the endpoint plus the rendered board. The state-GROUPED columns with real runs, legacy loops and tasks in them were not observed — this home has no work items in a project — so the grouping is unit-shaped here.

## Recorded history

### Commits (3)

- `ccb4541b8` 2026-09-06 — docs(roadmap): flip EI-6, and fix four gates that were wrong about why they were gated
- `e800cd5a0` 2026-08-11 — fix(workflows): don't let a stale process fail a run it cannot even load (#1053)
- `8d24ba0a2` 2026-08-07 — feat(workflows): WF2WOR-1 project Work board — /work route, claim leases, boot sweep, completion hook (#892)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:968` — no-claim-lease gap already owned by WF2WOR-1; recorded here because it is what made the clock half
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1469` — was **satisfied all along**, shipped by `WF2WOR-1` rather than by any plan literally named

### Its plan's own record, via the scope references (20 line(s))

This atom's scope cites `§1`, `§5.2`, `§6.1`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:41` — - **WORK-R8** — claim-before-work leases + wayfinder sections (decisions / fog / out-of-scope) → §1.5, §6.1
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:44` — - **WORK-R11** — project living overview + charter/instructions injection into all project sessions → §1.2
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:45` — - **WORK-R12** — /work bulk-data semantics + local-first hub rendering + rebuild-projections repair → §1.4, §6.1
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:47` — - **WORK-R14** — project-scoped memory locality + knowledge project tagging with provenance fencing (adapted to the real cwd-partition seam) → §1.6
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:48` — - **WORK-R15** — project export/import contract (validated ZIP, integrity, path safety) → §1.7
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:74` — ├─ TaskList/Task[]        (existing; runs materialize tasks per TASKS-SOPS; TTL'd claim leases §1.5)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:77` — ├─ overview.md + brief    (living overview revised on run completion; both injected into project sessions §1.2)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:79` — └─ wayfinder ledgers      (decisions-so-far / not-yet-specified / out-of-scope §1.5)
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:40` — - **WORK-R7** — truthful run-state lifecycle (queued/zombie/lost/suspended) + live run adoption + run-id-keyed streaming → §5.2, §6.3
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:67` — ├─ WorkflowRun[]          (project_id required; journaled; forked branches; truthful lifecycle §5.2)
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:229` — - **Work**: state-grouped rows — *Needs input* pinned first, then Working / Queued / Suspended / Ready for review / Done (the Jules/Claude-agents board anatomy + §5.2's truthful states). Rows = runs + legacy loops + tasks, with cheap-model one-line summaries, §1.5 claim badges, and §4.1 `origin: …
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:302` — 1. Project threading (`project_id` + `/work` endpoint **with per-section isolation** + brief/context bindings) — lands with v2 Slice 1 (already an acceptance criterion). Queued-before-slot + zombie/suspended/lost lifecycle (§5.2) land here too — they are engine-record semantics.
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:462` — - **`queued` is derived from a record with no `started_at`.** That is what §5.2's record-before-slot
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1372` — **BEHAVIOUR CHANGE, measured not assumed.** Declaring a workspace changes what a crash-survivor batch does at boot. `stamp_run` records `worktree_path` for EVERY isolated mode and `worktrees.substrate_for` reports anything with a recorded path as isolated, so a compiled batch now takes PAUSED / `…
- `§6.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:34` — - **WORK-R1** — NeedsInputItem contract + journal-event reification + resume handshake + owner binding + OS-projectable payload → §6.1, §5
- `§6.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:41` — - **WORK-R8** — claim-before-work leases + wayfinder sections (decisions / fog / out-of-scope) → §1.5, §6.1
- `§6.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:45` — - **WORK-R12** — /work bulk-data semantics + local-first hub rendering + rebuild-projections repair → §1.4, §6.1
- `§6.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:126` — 3. **Evidence bundles + terminal handoff report (R4).** An evidence bundle is an **Artifact composition**: one schema-versioned manifest Artifact `{per-file kind, name, size, sha256, optional expiry}` grouping screenshots/video/logs/metadata a run produced. The cockpit renders a **Proof section**…
- `§6.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:305` — 4. Project hub Work tab (read-only board over runs + loops + tasks, local-first rendering, claim badges) + NeedsInputItem journal-event reification + cross-project inbox (§6.1).
- `§6.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:837` — - **NOT DONE:** journal-event reification (§6.1 asks for needs-input/approval as first-class journal

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
