<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AP-4` — Pack kinds: agent/roster packs, prompt-card importer, bundled Domain OS packs, one-link serialization

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-PACKS](../plans/AGENT-PACKS.md) (`AP`, plan status `done`) |
| **Atom table** | [`../atomic/AP.md`](../atomic/AP.md) |
| **Execution position** | #37 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1506 |

## Intent — what this is for

§4.1 Domain OS packs; §4.2 Agent/roster packs; §4.3 Prompt-card importer; §4.4 / §2.3 One-link setup export. Session 4. Success criteria 1, 4. Soft: WORK-R16 roster slug refs, LEARNING-FLYWHEEL {source,computedHash} locks.

## Done when

Personal CFO + Health OS bundled first-party packs export→wipe→import on a fresh PERSONALCLAW_HOME with skills locked, template runnable, digest trigger DISABLED, connector configure-or-substitute prompt, and setup interview binding a folder; roster pack imports rendering persona markdown into config agents{} with every runbook/catalog slug lint-resolved (a broken slug blocks import naming the exact unresolved ref) and only the always tier one-click-deploys; prompt-card importer fences pasted input, maps to typed PromptTemplate/WorkflowDef/AgentDefinition through the proposal review flow; one-link JSON serialization imports through the same §3 pipeline.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AP-2`](AP-2.md) | intra-repo | `done` |
| [`AP-3`](AP-3.md) | intra-repo | `done` |

### Unblocks (1)

[`AP-7`](AP-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `76b773ce6` 2026-08-16 — chore(reference): re-render the agent reference for AP-4's routes
- `9d2a520d3` 2026-08-16 — feat(packs): AP-4 pack kinds — Domain OS packs, rosters, prompt cards, one-link

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:335` — connector, so it gets its own two-home round-trip test rather than joining AP-4's parametrized
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:383` — ### 2026-08-16 — `AP-4` DONE (pack kinds: Domain OS, roster, prompt-card, one-link)
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:400` — AgentDefinition store; AP-4 builds on that rather than adding a second write.
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:426` — AP-2 shape, found only because AP-4's done_when demands the exact ref be named.
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:437` — instead of only existing. Nothing in AP-4 dereferences a bound folder for file access — the pack's
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:457` — **Not done here (unchanged scope):** no pack store / detail / export FE — AP-7 owns it. AP-4's

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§4.1`, `§4.2`, `§4.3`, `§4.4`, `§2.3`, `Session 4`, `R16`. Those tokens appear in its source plan at:

- `§4.2` `docs/roadmap/plans/AGENT-PACKS.md:23` — - **NEW-12 amendment (a)** (agent-pack/roster-pack installs: catalog JSON + persona markdown + optional scenario runbooks with staged rosters, slug-resolution-checked on install, one-click team deploy) → §4.2, source `agency-agents` (divisions.json / runbooks.json / activation staging).
- `§4.2` `docs/roadmap/plans/AGENT-PACKS.md:36` — - **The entities a pack composes live in knowable stores:** workflow templates at `~/.personalclaw/workflows/<name>/` today (WORKFLOWS-V2 moves them to versioned `templates/<slug>/vN.json` — the exporter reads whichever exists); prompts at `~/.personalclaw/prompts/*.yaml` (`PromptTemplate`); agen…
- `§4.2` `docs/roadmap/plans/AGENT-PACKS.md:54` — ├── agents/<slug>.md             # persona markdown (frontmatter + body → AgentDefinition, §4.2)
- `§4.2` `docs/roadmap/plans/AGENT-PACKS.md:393` — **DISCOVERY — §4.2's stated seam is wrong against code, and AP-2 already handled it.** §4.2 says
- `§4.3` `docs/roadmap/plans/AGENT-PACKS.md:255` — | **LEARNING-FLYWHEEL** (approved) | Its `{source, computedHash}` lock on imported templates is the drift primitive pack updates rely on (§1 pack_owned); its proposal queue is the review surface the prompt-card importer (§4.3) and fingerprint proposals (§7) feed. Packs are "the sharing channel fo…
- `§4.4` `docs/roadmap/plans/AGENT-PACKS.md:24` — - **NEW-12 amendment (b)** (one-link setup export/import: skills+prompts+workflow templates+app configs, never secrets, single shareable manifest with per-resource hashes) → §4.4, sources `different-ai-openwork`, `tryfriday`.
- `§2.3` `docs/roadmap/plans/AGENT-PACKS.md:176` — §2.3 — same importer, JSON serialization, per-resource hashes.
- `Session 4` `docs/roadmap/plans/AGENT-PACKS.md:271` — - **Session 4 — pack kinds (§4):** agent/roster pack import (catalog.json + persona→`agents{}` rendering + slug lint + staged-roster surfacing); prompt-card importer (fenced input, typed output, proposal review); author + ship Personal CFO and Health OS bundled packs; one-link serialization.
- `R16` `docs/roadmap/plans/AGENT-PACKS.md:98` — | template → agent slug | WORK-R16 roster slugs in template `agent:` fields (WORK-CONTAINERS §3: templates reference agents by slug, drift-checked) |
- `R16` `docs/roadmap/plans/AGENT-PACKS.md:138` — `packs/lint.py` — the check.py pattern, run at export AND import: every `depends_on` resolves to a bundled component or a `requirements` row; every agent slug referenced by a template resolves (the WORK-R16 drift check applied to pack scope); every skill mentioned in template/agent prose (backtic…
- `R16` `docs/roadmap/plans/AGENT-PACKS.md:168` — `agents/catalog.json` (the agency-agents divisions.json shape: `{slug, name, description, label, icon, color, activation}`) + one persona markdown per agent + optional scenario runbooks with **staged rosters** (`activation: always | phase-N | as-needed`). Import path honors the real seam: AgentDe…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
