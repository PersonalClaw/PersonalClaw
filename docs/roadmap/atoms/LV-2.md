<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `LV-2` — S2 legibility: per-run 'used N skills you approved' chip + session learned-chips with tap-through

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [LEARNING-VISIBILITY](../plans/LEARNING-VISIBILITY.md) (`LV`, plan status `in_progress`) |
| **Atom table** | [`../atomic/LV.md`](../atomic/LV.md) |
| **Execution position** | #329 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 T2.1 (plumb loaded_skills+usage as additive meta on existing turn/loop events) and T2.2 (after-turn capture chips: facets/lessons/proposals)

## Done when

run/loop panel shows 'used N skills' chip with names on hover and zero new WS/SSE channels; a correction in chat yields a visible learned-chip within the session whose tap lands on the right approve/edit surface

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`LV-1`](LV-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 62 (LV) — the fence falsified at the call site (a self-satisfying assertion, fixed in #2819); the benchmark preflight run live

**Code evidence:**

- 🔑 THE 'ZERO NEW WS/SSE CHANNELS' CLAUSE IS ASSERTED DIRECTLY, which almost never happens for a negative requirement: `test_no_new_ws_channel_was_introduced` plus `test_a_turn_carrying_skills_used_broadcasts_only_known_events`. The chip rides the SAME persisted meta and is invisible to the WS
- one label helper for two surfaces — chat (`ChatPage`) and the loop cockpit (`LoopCockpitPage`) both import `skillsUsedLabel`/`skillsUsedTitle`, so 'used N skills' cannot mean two things in two places
- 🔑 THE HOVER LIST IS IN THE ALLOCATOR'S OWN ADMISSION ORDER, 'never re-sorted here, which would invent a ranking the backend never stated' — and a `reduced` load is marked 'summary only', because 'presenting a summary-only load as a full one is the one thing this chip must not do'. Same no-paraphrase rule ET-8 applies to permissions, applied to a provenance chip
- 🔴 ABSENT, NOT ZERO, AGAIN: `test_no_skills_omits_the_key_entirely` — the meta key is dropped rather than set to an empty list, so 'no skills loaded' and 'not recorded' stay distinguishable. `test_a_later_turn_does_not_inherit_an_earlier_turns_skills` covers the stale-carry
- `test_the_three_origins_are_distinct_and_closed` — the learned-chip origin vocabulary is closed, not an open string
- the seven LV suites + the ladder suite 140/140; benchmark + verdict suites 73/73; `learning_benchmark.py --preflight` run live: task set v2, all 10 tasks runnable; Skills and Learning pages driven in a real browser on :10011

**Driven in the UI:** The chip needs a turn that loaded a skill, which needs a bound model — not drivable in this home. The two surfaces that render it were read, and the shared helpers are unit-covered.

**Notes:** The `title`-is-not-an-accessible-name call is made consistently here and in LV-3's block, with the visible label carrying the same information. A hover-only affordance would have been the easy version.

## Recorded history

### Commits (8)

- `129dbe11c` 2026-08-26 — feat(learning): LV-2 make the learned chip reach its approve surface in one tap
- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `084a42175` 2026-08-25 — docs(roadmap): give SV-9 and ES-5 machine-readable blocked_reasons
- `4f9fddf91` 2026-08-25 — docs(roadmap): park LV-2 on its owner taste call so the census stops re-dispatching it
- `827fcbdd1` 2026-08-25 — docs(roadmap): cite the batch PR on DFE-4
- `5d4250f25` 2026-08-24 — test(learning): LV-2 pin the loop half's two silent seams + log the atom
- `c09a0e7d2` 2026-08-24 — feat(learning): LV-2 skills-used chip + tappable learned chips
- `22f536467` 2026-08-24 — feat(learning): LV-2 skills-used meta + learned-chip origin discriminator

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:289` — - **DISCOVERY (adjacent, not acted on) — the S2 chip has no channel yet.** `LV-2` will need the
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:298` — ## Execution log — LV-2 (S2 legibility: skills-used chip + tappable learned chips)
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:320` — **zero** times in the LV-2 diff, and the rail's own vacuity floor held (49/49 call sites matched),
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:360` — one disclosure. That is pre-existing ledger behavior, not something LV-2 introduced, so it was left
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:363` — - **DISCOVERY (2026-08-25) — LV-2 was re-briefed by an execution tick and found already built.**
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:368` — a census can read. Fixed by giving LV-2 an explicit `blocked_reason` in `dag.json` naming the open
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:386` — - [2026-08-26][LV-2] **OWNER RULING — the link must be reachable WITHOUT the user opening the
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:394` — - [2026-08-26][LV-2] **DONE — one tap now opens the ledger AND lands focus on the approve/edit
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:408` — - [2026-08-26][LV-2] **DEVIATION — the extraction was the only way to assert the behaviour at the
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:416` — - [2026-08-26][LV-2] **DISCOVERY — three of the atom's four citations had drifted**, measured
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:422` — - [2026-08-26][LV-2] **DEVIATION — a GLOBAL design ratchet had to follow the moved file.**
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:428` — - [2026-08-26][LV-2] **Falsification (four mutations on the LIVE file, each grep-confirmed applied,
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:439` — - [2026-08-26][LV-2] **Gate:** `make lint` green (black 2138 files, isort, flake8, mypy 1054 source
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:445` — - [2026-08-26][LV-2] **ATOM COMPLETE — both `done_when` clauses met; `LV.md` row flipped to ✅.**
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:449` — the now-stale `BLOCKED-OWNER (2026-08-25)` `blocked_reason` for `LV-2`** — that file is fenced to
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:518` — - [2026-08-25][LV-3] **Design vocabulary:** reused LV-2's non-interactive chip idiom (`<div title>`,
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:861` — - [2026-08-26][LV-2] **OWNER RULING — the taste call is decided: the tap target must be reachable
- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:870` — fails when the disclosure stays closed. No backend change and no new WS/SSE channel. `LV-2` stays

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.1`, `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/LEARNING-VISIBILITY.md:76` — | T2.1 | Plumb per-run loaded-skills + usage into the frontend event stream (additive meta on existing turn/run events — locate the tool-result meta path; no new event channel) | runner event emission site, `web` chat/loop panels | run panel shows "used N skills" chip with names; zero new WS/SSE …
- `T2.1` `docs/roadmap/plans/LEARNING-VISIBILITY.md:305` — - **T2.1 DONE, and it took the DISCOVERY at the end of the LV-1 log seriously.** That note warned
- `T2.2` `docs/roadmap/plans/LEARNING-VISIBILITY.md:77` — | T2.2 | Session learned-chips: after-turn captures (facets/lessons/proposals) render as chips with tap-through to the relevant approve/edit surface | after-turn result plumb-through, `web/src/pages/chat/` components | a correction in chat yields a visible chip within the session; tap lands on th…
- `Session 2` `docs/roadmap/plans/LEARNING-VISIBILITY.md:72` — ### Session 2 — "What I learned" legibility
- `Session 2` `docs/roadmap/plans/LEARNING-VISIBILITY.md:120` — **Lands in:** extends **Session 2** ("What I learned" legibility — this is its long-horizon sibling; T2.3's digest block already builds half the gather). Count stays **4 sessions**; S2 grows by ~half a session, recorded honestly.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
