<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PL-3` — pclaw-api first-party skill + offline reference + 5-task eval gate

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-LEGIBILITY](../plans/PLATFORM-LEGIBILITY.md) (`PL`, plan status `done`) |
| **Atom table** | [`../atomic/PL.md`](../atomic/PL.md) |
| **Execution position** | #455 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§3 ("First-party PersonalClaw skill + offline reference"), §3.1 artifacts, §3.2 acceptance bar; §9 Session 3

## Done when

skills/bundled/pclaw-api/SKILL.md + personalclaw.reference/ subpackage rendered at build time from the §1 manifest generator (one source, two renderings); doctor --paths resolves reference dir; test_agent_reference.py byte-compares checked-in vs fresh render; eval_pclaw_api_battery.py checked in; with/without eval scored 5/5 first-try, 0 silent misses (bar ≥4/5) vs 1-2/5 baseline

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PL-1`](PL-1.md) | intra-repo | `done` |
| [`PL-2`](PL-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `§3`, `§3.1`, `§3.2`, `§9`, `Session 3`. Those tokens appear in its source plan at:

- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:27` — - **NEW-13 core (b)** (first-party SKILL.md + offline API/tool-signature reference in the distribution; with/without eval as acceptance bar) → §3. Source: `quarkdown` (skill resolved via `doctor get install-dir`, orient-then-drill, exact-signature lookups, mandatory verify loop, published eval 4/…
- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:48` — - **A bundled self-description skill exists but is prose:** `skills/bundled/pclaw-features/SKILL.md` describes capabilities channel-neutrally with NO exact signatures — exactly the "0/5 without exact-signature reference" failure Quarkdown measured. §3 upgrades it.
- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:50` — - **`personalclaw doctor` exists** (`cli.py:214`) — the natural anchor for install-dir/docs-path resolution (§3), mirroring Quarkdown's `doctor get install-dir`.
- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:205` — - **Session 3 — first-party skill + reference (§3):** `pclaw-api` SKILL.md; build-time `docs/agent-reference/` rendering from the manifest generator; `doctor --paths`; the 5-task with/without eval — **session doesn't close below 4/5 + 0 silent misses.**
- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:365` — ### 2026-07-25 — S3 (§3) `pclaw-api` skill + offline reference + eval gate — DONE
- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `§3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:416` — literal `docs/agent-reference/`.** Repo-root `docs/` does not ship in a wheel, and §3.1's whole
- `§3.1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:416` — literal `docs/agent-reference/`.** Repo-root `docs/` does not ship in a wheel, and §3.1's whole
- `§3.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `§9` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `Session 3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:205` — - **Session 3 — first-party skill + reference (§3):** `pclaw-api` SKILL.md; build-time `docs/agent-reference/` rendering from the manifest generator; `doctor --paths`; the 5-task with/without eval — **session doesn't close below 4/5 + 0 silent misses.**
- `Session 3` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
