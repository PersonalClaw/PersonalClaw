<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `OO-6` — Maintainer release runbook (T3.1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [OSS-OPERATIONS](../plans/OSS-OPERATIONS.md) (`OO`, plan status `done`) |
| **Atom table** | [`../atomic/OO.md`](../atomic/OO.md) |
| **Execution position** | #383 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 — Governance floor (T3.1: docs/maintainers/release-runbook.md — tag → environment approval → post-release verification checklist) + V3 (walk it against the shipped v0.1.3)

## Done when

a person with org access can cut a release from the doc alone; documents all release.yml jobs, both protected environments, the six version surfaces, the annotated-tag dereference trap, and the '## [X.Y.Z]' CHANGELOG-heading requirement; validated against v0.1.3 (GHCR pull step flagged as the one hand-confirm item)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:CI-RELEASE:release.yml jobs + protected environments (release, release-client)` | cross-plan (EXT) | **unresolved** |
| `EXT:DISTRIBUTION:install one-liner + compose pull for the post-release verification checklist` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T3.1`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/OSS-OPERATIONS.md:83` — | T3.1 | Release runbook: tag → environment approval → post-release verification checklist (install one-liner, compose pull, changelog panel) | create `docs/maintainers/release-runbook.md` | a person with org access can cut a release from the doc alone |
- `T3.1` `docs/roadmap/plans/OSS-OPERATIONS.md:86` — | V3 | Validation: a release can be cut from T3.1's runbook alone (walk it against the v0.1.3 release as the reference run) | — | recorded |
- `T3.1` `docs/roadmap/plans/OSS-OPERATIONS.md:129` — draft, T3.1 release runbook, T3.3 roadmap-intake wiring.
- `T3.1` `docs/roadmap/plans/OSS-OPERATIONS.md:209` — - **[2026-07-31][T3.1] DONE — `docs/maintainers/release-runbook.md`, written from `release.yml` and
- `Session 3` `docs/roadmap/plans/OSS-OPERATIONS.md:79` — ### Session 3 — Governance floor

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
