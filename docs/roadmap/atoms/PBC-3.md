<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PBC-3` — App-registered logger-root aggregation; delete constants.APP_LOGGER_ROOTS

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROVIDER-BOUNDARY-COMPLETION](../plans/PROVIDER-BOUNDARY-COMPLETION.md) (`PBC`, plan status `done`) |
| **Atom table** | [`../atomic/PBC.md`](../atomic/PBC.md) |
| **Execution position** | #404 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1c879ec |

## Intent — what this is for

S1 T1.4; Design D (app-registered logger roots)

## Done when

catalog.installed_logger_roots() reads installed+enabled manifests without importing app code; cli.py log setup and dashboard/handlers/updates.py log-level handler consume ('personalclaw', *installed_logger_roots()) with graceful () when apps dir absent; grep for APP_LOGGER_ROOTS in src/ is empty; test_app_catalog.py P13-P16 pass

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PBC-1`](PBC-1.md) | intra-repo | `done` |

### Unblocks (2)

[`PBC-4`](PBC-4.md) (`done`), [`PBC-7`](PBC-7.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 38 (PBC) — DRIVEN via the real doctor CLI + a falsified residue rail

**Code evidence:**

- MEASURED, catalog.py:1132 installed_logger_roots() — reads list_apps()'s SCANNED MANIFEST DICT, and the docstring says the load-bearing part out loud: 'Manifest-only … no app import/exec'. Enabled apps only, de-duped preserving first-seen order
- 🔑 THE ABSENT-APPS-DIR PATH IS EXPLICIT, NOT INCIDENTAL: `if not apps_dir().is_dir(): return ()` with 'so callers degrade to just personalclaw' — the fresh-install case the clause names
- both consumers present: cli.py:1339-1355 log setup and dashboard/handlers/updates.py:854-856 log-level handler, the latter as ('personalclaw', *installed_logger_roots()) exactly as the clause requires
- test_app_catalog.py green inside the 141
- test_app_cli + test_sdk_cli + test_provider_boundary_residue + test_app_manifest + test_app_catalog 141/141

**Notes:** 🔑 THE done_when's LITERAL CHECK IS FALSE WHILE ITS INTENT HOLDS, and the distinction matters. It says 'grep for APP_LOGGER_ROOTS in src/ is empty'. It is NOT empty: cli.py:1341-1355 carries a LOCAL variable named _APP_LOGGER_ROOTS. But that local holds the RUNTIME RESULT of installed_logger_roots() — it is the replacement, not the residue; constants.APP_LOGGER_ROOTS itself is gone. A grep-shaped acceptance clause reading as failed while the deliverable is complete is the same near-miss shape this audit hit on ChatPage's findOpen, and it is worth noting that the plan's own rail (PBC-7) does not use a bare grep for exactly this reason — it pairs the sweep with a machine-checked keeps table so a judgment can be recorded where the pattern cannot express one.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T1.4`. Those tokens appear in its source plan at:

- `T1.4` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:127` — - **Coordination:** DISTRIBUTION T1.4 (LLM-SDK demotion) depends on T2.1's pip-step finding — read this plan's Execution log first.
- `T1.4` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:138` — | T1.4 | Logger-root aggregation: `installed_logger_roots() -> tuple[str,...]` reading installed+enabled manifests (no app code import); swap consumers `cli.py:750-762` and `dashboard/handlers/updates.py:686-692` to `("personalclaw", *installed_logger_roots())`; delete `constants.APP_LOGGER_ROOTS…
- `T1.4` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:167` — - [2026-07-20][T1.4] DONE: `catalog.installed_logger_roots()`; consumers swapped; `constants.APP_LOGGER_ROOTS` deleted; `tests/test_app_catalog.py` P13-P16. commit `1c879ec`.
- `T1.4` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:174` — - [2026-07-20][HANDOFF] Executed via a code-kind goal loop (052ae197) that produced T1.1/T1.2a/T1.4 across parallel git worktrees, then stalled on a worktree-merge collision (worker also committed to the base branch → divergent history). Per owner authorization, the maintainer took over: consolid…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
