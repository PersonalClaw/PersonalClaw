<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EI-9` — Reviewer-comment triage primitive (line-anchored findings → accepted-subset dispatch)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXECUTION-ISOLATION](../plans/EXECUTION-ISOLATION.md) (`EI`, plan status `done`) |
| **Atom table** | [`../atomic/EI.md`](../atomic/EI.md) |
| **Execution position** | #231 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2094 |

## Intent — what this is for

§7: consume the approved WORKFLOWS-V2 Canonical Finding record + auto_fixable flag; diff-anchored triage panel with anchor validation against the actual diff; accepted subset auto-dispatched to the originating session (native resume per §3 capability matrix); rejections feed the flywheel calibration; §11 Session 7

## Done when

SC9: a workflow review stage emits line-anchored findings; the triage panel validates anchors against the real diff; the user accepts 2 of 5; the accepted pair auto-dispatches to the originating worker which applies them; rejected findings land in the calibration record; nothing was auto-written without acceptance

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EI-5`](EI-5.md) | intra-repo | `done` |
| `EXT:WORK-CONTAINERS:cockpit diff panel to extend` | cross-plan (EXT) | **unresolved** |
| `EXT:LEARNING-FLYWHEEL:calibration record for rejections` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `3915489df` 2026-08-26 — docs(roadmap): EI-9 done (PR #2094)
- `ab730d83a` 2026-08-26 — feat(review): EI-9 line-anchored review-finding triage primitive

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1030` — - **2026-08-26 — `EI-9` (§7 reviewer-comment triage): built, with the two EXT deps resolved against what
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1080` — - **2026-08-26 — `EI-9` DONE (PR #2094).** SC9 closed end to end: a review stage emits line-anchored

### Its plan's own record, via the scope references (16 line(s))

This atom's scope cites `§7`, `§3`, `§11`, `Session 7`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:41` — - **NEW-14 amendments (batch 3)** — reviewer-comment triage primitive (line-anchored diff comments → human-accepted subset → auto-dispatched follow-ups to the originating session) → §7; Memoh's container-direction confirmation + hosting external ACP agents behind a controlled tools proxy → §3/§5 …
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:61` — **Soul guardrail:** your machine, your installed CLIs, your Docker/Lima if you have them — graceful degradation to `none` when you don't. No fleet, no k8s, no remote deploy. The runner catalog is a folder of markdown+JSON files, not a service registry. Learning stays propose-don't-write (§7's tri…
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:109` — - **NOT an action provider** — nothing here touches `ALLOWED_HOOK_PROVIDERS` (src/personalclaw/validation.py) for §1 (§4 and §7 DO add action providers; see there).
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:269` — **Memory vs Knowledge boundary:** this plan touches neither store. Sandboxes, runner catalogs, checkpoints, and the vault are harness mechanics (files under `~/.personalclaw/` + OS keychain), not memory entries and not knowledge items. The §7 rejection records feed LEARNING-FLYWHEEL's propose que…
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:278` — | In-cockpit diff panel + reintegration verbs + localhost preview | approved in WORK-CONTAINERS §4.1 — §7's triage panel *extends* it |
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:1030` — - **2026-08-26 — `EI-9` (§7 reviewer-comment triage): built, with the two EXT deps resolved against what
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:1050` — ["steering_queue"]` → `RunController._consume_steering` at the iteration boundary. That IS §7's
- `§7` `docs/roadmap/plans/EXECUTION-ISOLATION.md:1052` — §7's "no resume capability → fresh session with the handoff brief" is **DEVIATED**: a terminal run
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:40` — - **NEW-14 amendments (batch 2)** — (a) Lima VM tier (shell/file ops only, LLM host-side, cached availability probe, host↔guest path translation, explicit degradation) → §2; (b) pluggable SandboxProvider registry (bind-mount/isolated taxonomy + 6-method handle, none+docker built-ins, consumed by …
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:41` — - **NEW-14 amendments (batch 3)** — reviewer-comment triage primitive (line-anchored diff comments → human-accepted subset → auto-dispatched follow-ups to the originating session) → §7; Memoh's container-direction confirmation + hosting external ACP agents behind a controlled tools proxy → §3/§5 …
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:86` — safety_profile: str            # AUTONOMY-GUARDRAILS §3 profile name
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:172` — - Target selection: a DIFFERENT runner from the catalog (§3) than the one that stalled — filtered by health evidence + required capabilities; the user's binding order breaks ties.
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:188` — `prepare` renders runner-specific instructions (Claude Code wants different framing than Gemini CLI — the dialect knows); `collect` normalizes into one result record consumed identically by loops/gates/UI. Backends: one per cataloged runner (built from the §3 catalog), plus a `subagent` backend (…
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:198` — - **Deterministic names** derived from identity, not randomness: `pclaw-<project_id>-<run_or_loop_id>-<session_slug>` (same `_tmux_session_name` sanitization, terminal.py:96) — so a restarted gateway *recomputes* the name and **reattaches instead of reaping** (emdash's exact recipe). Applies to: …
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:208` — - **Policy at the host end:** the shim's tool surface is the sandbox spec's safety profile (AUTONOMY-GUARDRAILS §3) — a research-class sandbox gets read-only tools; every call SEL-audited under the owning session key. This is ALSO how hosted external ACP runners (§3) inside sandboxes reach PClaw …
- `§3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:237` — - **Dispatch:** the accepted subset auto-dispatches as **follow-up instructions to the ORIGINATING session** (the worker that produced the diff — resumed via its runner's native resume per the §3 capability matrix; no resume capability → fresh session with the handoff brief). `auto_fixable: true`…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
