<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AE-7` — artifact investigate resolver (agent task mode, fenced current content, slug-naming opening prompt)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ARTIFACTS-EVOLUTION](../plans/ARTIFACTS-EVOLUTION.md) (`AE`, plan status `done`) |
| **Atom table** | [`../atomic/AE.md`](../atomic/AE.md) |
| **Execution position** | #17 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 T3.1; Contracts C4 (consumes plan 60 registry)

## Done when

POST /api/investigate {kind:artifact} produces a session whose first turn carries the fenced artifact content (v-current, not stale) with agent mode + #/artifacts/<slug> back-link

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:INVESTIGATE-ANYWHERE:register_investigate_resolver registry primitive` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`AE-10`](AE-10.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:381` — INVESTIGATE-ANYWHERE (`POST /api/investigate {kind:'artifact'}` — AE-7's resolver) and
- **source plan** `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:404` — AE-7's slug-naming prompt in **Agent** mode, artifact left / chat right at 1440px. Then

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T3.1`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:12` — **Status:** IN PROGRESS — S1a/S1b/S2 shipped; **S3 T3.1 + T3.4 + T3.3 shipped 2026-07-29** (resolver + @-references + version compare); T3.2 (iterate panel) blocked on a ChatEmbed host bridge / artifact WS event — see the S3b log. Created 2026-07-26 (roadmap rev 13)
- `T3.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:17` — **Consequence of T3.2 being blocked (audited 2026-08-04):** T3.1's `artifact` investigate resolver
- `T3.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:129` — | T3.1 | `artifact` investigate resolver (envelope per C4: content capped + fenced downstream, task mode `agent`, back-link, slug-naming opening prompt) registered against plan 60's registry | `artifacts/` boot registration, tests | "Iterate with agent" produces a session whose first turn carries…
- `T3.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:237` — - 2026-07-29 — **DONE (S3, T3.1 + T3.4).** The iterate *entry point* and @-artifact chat
- `T3.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:241` — - **T3.1 resolver.** `_resolve_artifact` in `investigate.py`, registered as `artifact`.
- `Session 3` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:125` — ### Session 3 — Iterate with agent + diff + references (Wave 3; needs plan 60 landed)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
