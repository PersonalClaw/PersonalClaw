<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EI-4` — Lima VM tier app + apps-sandbox (#71) + terminal sandbox picker

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXECUTION-ISOLATION](../plans/EXECUTION-ISOLATION.md) (`EI`, plan status `done`) |
| **Atom table** | [`../atomic/EI.md`](../atomic/EI.md) |
| **Execution position** | #226 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2 Lima VM Tier (apps/lima-sandbox isolated kind, cached availability probe, host↔guest path translation, degradation dialog); §1.3(3) Terminal per-session sandbox picker; §1.3(4) apps sandbox un-deferred (backend.sandbox manifest field + permissions.network→egress_tier / permissions.storage→allowed_write_paths); §11 Session 3

## Done when

SC3: with apps/lima-sandbox installed + instance running, a terminal opened "inside the run's sandbox" executes in the VM with correct path translation; stopping the instance flips the provider to greyed-out-with-reason within one probe TTL; an interactive request gets the path-guard-only dialog; app backends launch through backend.sandbox

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EI-1`](EI-1.md) | intra-repo | `done` |
| [`EI-2`](EI-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 65 (EI) — the spawn-site census falsified (an unmapped spawn named to file:line); the runner provenance rows driven in a real browser

**Code evidence:**

- `sandbox_providers/lima.py` ships with its suite; `tests/test_terminal_sandbox_picker.py` covers the picker; the probe suite covers the greyed-out-with-reason flip
- 🔴 THE VM HALF WAS NOT OBSERVED: the interactive case skips with 'limactl / Running instance unavailable'. So the path-translation and in-VM execution clauses are unit-shaped here, not driven
- 358 passed / 3 skipped across 17 sandbox, runner, vault, checkpoint and triage suites; 16/16 on the runner-health frontend suite; the Secrets vault and Agent-defaults pages driven on :10011

**Driven in the UI:** Not driven: no Lima instance on this host.

**Notes:** Partial for the same honest reason as EI-2 — a VM tier cannot be observed without a VM. The provider-unavailable path IS observed, and it is the one a user without Lima actually meets.

## Recorded history

### Commits (3)

- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `5b156d829` 2026-09-03 — feat(sandbox): EI-4 terminal sandbox picker + manifest egress mapping
- `0dad4130e` 2026-09-03 — feat(sandbox): EI-4 lima sandbox provider with egress-tier manifest mapping

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1143` — tiers are `EI-2`/`EI-4`, both `todo`. `SandboxHandle` is a 3-method contract (`argv`/`exec`/
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1177` — `copy_file_in` in `EI-2`/`EI-4`.
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1433` — ## Execution log — EI-4 (Lima VM tier app + apps-sandbox + terminal sandbox picker) — validated done

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§2`, `§1.3`, `§11`, `Session 3`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:40` — - **NEW-14 amendments (batch 2)** — (a) Lima VM tier (shell/file ops only, LLM host-side, cached availability probe, host↔guest path translation, explicit degradation) → §2; (b) pluggable SandboxProvider registry (bind-mount/isolated taxonomy + 6-method handle, none+docker built-ins, consumed by …
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:77` — def is_available(self) -> tuple[bool, str]: ...      # cached probe (§2.2)
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:107` — - **Built-ins register at boot** like channel transports' webui: a `register_default_sandbox_providers()` call in `dashboard/server.py` startup registers `none` + `docker` — they are core-native, not apps. `lima` ships as a **first-party app** (`apps/lima-sandbox`, §2) because it carries a real e…
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:410` — [PLATFORM-HARDENING-FLOORS](PLATFORM-HARDENING-FLOORS.md): §2.5 extends the same allowlist shape
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:412` — `bash_provider.py:65`'s `_scrub_env` is the allowlist shape); and §2.1 closes the **other** half of
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:415` — `app_permission_middleware` entirely. `D1` and §2.1 are complementary: `D1` limits what a backend
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:416` — *receives*, §2.1 establishes *who may call it*.
- `§2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:592` — `apps/permissions.py`'s `can_use_network` docstring, `docs/security/limitations.md` §2 (which quotes
- `§1.3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:277` — | WORK-R3 workspace block / WORK-R20 container mode / WORK-R8 leases / WORK-R19 secrets store | **APPROVED elsewhere — consumed, not rebuilt.** §1.3 adds `sandbox:` beside the workspace block; WORK-R20 re-expresses as `sandbox: docker` capability; §3.1(5) reuses the R8 lease convention; §8 is UX-…
- `§1.3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:285` — | apps sandbox task #71 | **un-deferred** — becomes §1.3(4), consuming the same substrate |
- `Session 3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:362` — **Lands in:** Session 1 (ceilings module + `SandboxSpec.ceilings` + `none` provider + the native-bash/action-provider/subagent seams + the audit test) and Session 2 (docker/lima mapping + cgroup tier); remaining seams (apps backend, MCP, ACP, cron script, loop) adopt in Session 3. Count 7 → **8 s…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
