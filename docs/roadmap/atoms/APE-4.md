<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `APE-4` — quality manifest block + Store card rendering + first-party CI verification

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [APP-PLATFORM-EVOLUTION](../plans/APP-PLATFORM-EVOLUTION.md) (`APE`, plan status `done`) |
| **Atom table** | [`../atomic/APE.md`](../atomic/APE.md) |
| **Execution position** | #44 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #53 |

## Intent — what this is for

Session 2 T2.1 + V2; Contract C1 (quality block)

## Done when

a dishonest first-party quality declaration turns apps-repo CI red (tested=CI green, designSystem=token-lint pass, a11y=axe pass); Store cards render honest badges

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:DESIGN-SYSTEM-CONSISTENCY:token-lint + axe a11y verification` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 55 (APE) — the read-only shared handle driven directly; the wildcard disclosure falsified

**Code evidence:**

- the quality-enforcement suite is green inside the 113, and the atom's own framing is the strongest thing about it: 'a DISHONEST first-party quality declaration turns apps-repo CI red'. The declaration is not trusted, it is checked against the thing it claims (tests green, token-lint pass, axe pass)
- 🔑 THAT IS THE SAME SHAPE AS PLATFORM-REACH'S PROOF TOKENS ONE PLAN OVER: a badge is only worth rendering if something can contradict it. A `quality` block nobody verifies is a self-assigned grade
- app messaging + platform events + background contract + quality enforcement 113/113; consent/card/fix-with-AI frontend suites 32/32 (falsified: literalising a wildcard target reds exactly the two pattern tests)

**Driven in the UI:** Not drivable: Store cards need a bound Store source. The badge rendering is covered by the card suites in the 32.

**Notes:** Worth noting the direction of the gate — it fires in the APPS repo, where the declaration lives, rather than in core. The claim is policed where it is made.

## Recorded history

### Commits (3)

- `90b51ad38` 2026-08-26 — docs(roadmap): APE-4 done via the apps quality batch; APE-6 + AS-7 recorded PARTIAL
- `cc8831afe` 2026-08-24 — docs(roadmap): cite the batch PR on the three atoms this train lands
- `2e0a74446` 2026-08-24 — feat(apps): APE-4 quality manifest block + honesty verifier + Store badges

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:750` — no `quality` block, so APE-4's verifier does not reach it — the bundle is not claiming a bar it
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:787` — APE-4's verifier exit 1 naming the bundle and axis; breaking the badge makes it exit 1 on
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:156` — install errors, absent stays absent on the wire — the `quality` pattern from `APE-4`); two SDK subpaths
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:210` — - [2026-08-24][S2 · atom `APE-4`] **PARTIAL — the core half is complete and railed; the apps-repo CI job is
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:721` — Flipping APE-11 does NOT complete this plan — `APE-4` and `APE-6` remain `todo`.
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:872` — - [2026-08-26][APE-4] **DONE.** Both halves of the `done_when` are now on `main`, in two different
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:893` — bundle in the repo declaring `quality {tested: true, designSystem: "v2", a11y: false}`, and APE-4's

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:83` — | T2.1 | `quality` manifest block + Store card rendering; CI check verifies first-party self-declarations (tested=CI green, designSystem=token-lint pass, a11y=axe pass) | `apps/manifest.py`, Store card, apps-repo CI | dishonest first-party declaration turns apps CI red |
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:79` — ### Session 2 — Quality bar + native evolution
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:118` — (a)+(c) fold into **Session 2** (they ARE the quality bar); (b) folds into **Session 3** beside `appMessaging` (one consent-copy review covers both). Session count stays 4.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
