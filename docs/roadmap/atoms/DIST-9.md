<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-9` — Install-kind-aware self-update: detect_install_kind, tag-driven check, per-kind apply, per-kind Updates panel (S4, clean break)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #186 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #ded75c0, aac5a84, 880131d, b22cc8f, a65d2fd |

## Intent — what this is for

Design §C; Contracts C1, C2, C5; Session 4 T4.1–T4.5

## Done when

detect_install_kind() classifies git/pip/container/desktop; C2 wire-shape conformance test locked (Tier-S); git rides release tags with a dev_mode override, pip does `pip install -U` + graceful re-exec (no web build), container/desktop return structured instructions; per-kind Updates panel + POST /api/update/dev-mode; dashboard.update_dev_mode config round-trips; CHANGELOG entry recording the clean-break DEVIATION (no update_kind_aware gate) and `snapshot` advice

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-8`](DIST-8.md) | intra-repo | `done` |

### Unblocks (2)

[`DIST-11`](DIST-11.md) (`todo`), [`DIST-13`](DIST-13.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `T4.1`, `T4.5`, `Session 4`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/DISTRIBUTION.md:179` — | T4.1 | `detect_install_kind()` per Design §C (git/.git probe via `_resolve_project_dir`, container env, desktop env, else pip) + unit tests for all four | `src/personalclaw/dashboard/handlers/updates.py` (or new `updates_kind.py` beside it), tests | four fixtures classify correctly |
- `T4.1` `docs/roadmap/plans/DISTRIBUTION.md:257` — - **DONE T3.1 (container install-kind env)** — Added `PERSONALCLAW_INSTALL_KIND=container` to both Dockerfiles: `deploy/docker/Dockerfile.backend` runtime `ENV` block (the gateway process that runs `detect_install_kind()` — S4 T4.1) and `deploy/docker/Dockerfile.web` (nginx front-end sets it too …
- `T4.1` `docs/roadmap/plans/DISTRIBUTION.md:266` — - **DONE T4.1 (`detect_install_kind`)** — New module `src/personalclaw/dashboard/handlers/updates_kind.py`: `detect_install_kind() -> InstallKind` (`git|pip|container|desktop`) per C1 resolution order — env `PERSONALCLAW_INSTALL_KIND` in {container,desktop} wins first (case-insensitive, junk igno…
- `T4.1` `docs/roadmap/plans/DISTRIBUTION.md:281` — - S4 tasks T4.1–T4.5 are implemented + validated. **V4** (owner per-kind real-world walkthroughs) is the remaining owner step. S5 (Homebrew/Nix) is OUT of scope per the brief — STOPPING at S4.
- `T4.1` `docs/roadmap/plans/DISTRIBUTION.md:303` — | T4.1 | `ded75c0` | `updates_kind.detect_install_kind()` (C1) + tests |
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:183` — | T4.5 | Register gate `update_kind_aware` (class B, plan DISTRIBUTION, removal = one release after default-on) + CHANGELOG entry; default ON for fresh installs, existing installs flip via migration-less config default | `src/personalclaw/lifecycle/gates.py` registration site, `CHANGELOG.md` | ga…
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:265` — - **DEVIATION (C5 / T4.5 gate — recorded per brief):** the plan wired S4 behind a `lifecycle.gates` gate `update_kind_aware` (class B). Per the owner decision (2026-07-20) S4 is taken as a **plain clean break**: there is NO `lifecycle/gates.py` machinery in the tree (verified — only `loop/gates.p…
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:268` — - Remaining S4: T4.2 (tag-driven latest-release check w/ ETag cache, offline-tolerant, git adds commits-behind), T4.3 (per-kind apply), T4.4 (frontend Updates panel), T4.5 (CHANGELOG + `dashboard.update_dev_mode` config round-trip). V4 = owner per-kind walkthroughs.
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:271` — - Remaining S4: T4.3 (per-kind apply: git=pipeline gated on new tag + `update_dev_mode` override; pip=`pip install -U personalclaw==<tag>` into `sys.prefix` → graceful re-exec, no web build; container/desktop=instructions; keep the 409 guard), T4.4 (frontend Updates panel per-kind), T4.5 (CHANGEL…
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:272` — - **DONE T4.3 (per-kind apply) + T4.5 config round-trip** — Wired `GET /api/update/check` to merge the C2 `build_update_status` payload (all kinds get the tag-driven `update_available`; legacy git changelog-diff fields kept for the current panel; adds `update_dev_mode`). `POST /api/update` now br…
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:273` — - **T4.5 config round-trip (done here, needed by T4.3):** added `DashboardConfig.update_dev_mode: bool = False` with `_meta` (title/description), wired into `from_dict` (`dashboard_data.get("update_dev_mode", False)`) and serialized via the existing `asdict(self.dashboard)` in `to_dict`. Verified…
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:275` — - Remaining S4: T4.4 (frontend Updates panel: per-kind states — instructions view for container, dev-mode toggle for git, unchanged progress stream — locate via the existing `update_progress` consumer + the `update_dev_mode` config control), T4.5 CHANGELOG entry (the config field is done; the CHA…
- `T4.5` `docs/roadmap/plans/DISTRIBUTION.md:278` — - Remaining S4: T4.5 CHANGELOG entry for the class-B/S self-update behavior change (the `update_dev_mode` config field + per-kind apply are done; the CHANGELOG note + release-note `snapshot` advice remain). V4 = owner per-kind walkthroughs (git one-tag-behind updates; pip venv one-version-behind …
- `Session 4` `docs/roadmap/plans/DISTRIBUTION.md:175` — ### Session 4 — Self-update generalization (class B, gate `update_kind_aware`)
- `Session 4` `docs/roadmap/plans/DISTRIBUTION.md:263` — ### Session 4 — Cycle 8 (self-update generalization — CLEAN BREAK)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
