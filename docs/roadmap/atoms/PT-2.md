<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PT-2` — S2: soundCues synth + master toggle (default OFF) + cue wiring at the three cue points

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PERSONALITY-THEMES](../plans/PERSONALITY-THEMES.md) (`PT`, plan status `done`) |
| **Atom table** | [`../atomic/PT.md`](../atomic/PT.md) |
| **Execution position** | #492 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1440 |

## Intent — what this is for

Design 'S2 — Sound cues ...' (soundCues.ts); Task breakdown Session 2 T2.1; Contract C2 (CueRecipe/playCue)

## Done when

web/src/design/soundCues.ts lazily creates one gesture-gated AudioContext and plays the closed cue set (turn_complete, approval_needed, error); a master toggle (default OFF) added to the Personality picker; cues wired at turn-settled (ChatPage), approval-requested (useApprovalToasts), and error toast (Toaster); no sound ever plays with toggle off, under prefers-reduced-motion, or when document.hidden (tests mock the media query); CI grep confirms zero audio files shipped in the bundle

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PT-1`](PT-1.md) | intra-repo | `done` |

### Unblocks (2)

[`PT-5`](PT-5.md) (`done`), [`PT-6`](PT-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `32bf078b7` 2026-08-16 — feat(web): PT-5 claw-arcade proof and personality closed-map rails
- `737750ce8` 2026-08-16 — feat(web): PT-2 synthesised sound cues, default off

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:26` — **S2's four build tasks have ALL landed** — T2.1 (sound cues, atom `PT-2`): `design/soundCues.ts`
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:175` — - [2026-08-16][S2/T2.1 · atom `PT-2`] DONE (sound cues — synthesised, default OFF). (a) **`web/src/design/soundCues.ts`** — three earcons built from oscillator + gain envelopes on the context's own timeline (`turn_complete` a rising major third, `approval_needed` two rising pulses, `error` a fall…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:186` — - [2026-08-16][S2/T2.2] DISCOVERY (adjacent, deliberately left): PT-5's row asks `personalityA11y.test.ts` to go red on a dangling `shellElement`/`errorTreatment` id. PT-4 already added the treatment half, so this atom added the symmetric `shellElement` assertion — the registry's own structural g…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:187` — - [2026-08-16][S2/T2.4 · atom `PT-5`] DONE (both proofs finished + the registry's structural rails). (a) **`behavior.dials`** is the expressiveness preset the plan's Integration-points line asked for ("`runtime` dials for expressiveness presets"), and it reaches `runtime` the long way ON PURPOSE:…

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:26` — **S2's four build tasks have ALL landed** — T2.1 (sound cues, atom `PT-2`): `design/soundCues.ts`
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:38` — 2026-08-16 (it still claimed T2.1 was missing two atoms after it landed). Created 2026-07-26 (roadmap rev 12; owner ask:
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:143` — | T2.1 | `soundCues.ts` synth (C2) + master toggle (default OFF) in the Personality picker + cue wiring at the three cue points (turn settled / approval requested / error toast) behind the toggle, reduced-motion, and `document.hidden` gates | `web/src/design/soundCues.ts` (new), `web/src/pages/Ch…
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:175` — - [2026-08-16][S2/T2.1 · atom `PT-2`] DONE (sound cues — synthesised, default OFF). (a) **`web/src/design/soundCues.ts`** — three earcons built from oscillator + gain envelopes on the context's own timeline (`turn_complete` a rising major third, `approval_needed` two rising pulses, `error` a fall…
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:176` — - [2026-08-16][S2/T2.1] Proofs. **Each suppressor is falsified ALONE.** The three tests share one audible baseline (cues on, reduced-motion clear, tab visible, context primed by a real gesture — with a vacuity assertion that the baseline actually built a context) and each breaks exactly one condi…
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:177` — - [2026-08-16][S2/T2.1] DISCOVERY (measured, fixed here): `armCueAudio()` armed its primer BEFORE asking whether the platform has Web Audio at all, so on a browser without it the shell held a `pointerdown` + `keydown` listener for the life of the page, retrying a construction that could never suc…
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:178` — - [2026-08-16][S2/T2.1] DISCOVERY (pre-existing, NOT part of this diff): the committed `docs/design/consistency-audit.json` is stale on `main`. A vitest run regenerates it to `filesScanned` **476** / `localFocusVisibleFiles` **32**, while the committed copy claims 470 / 30. Measured on the PARENT…
- `T2.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:179` — - [2026-08-16][S2/T2.1] Gate: web typecheck green · FULL `npm test --workspace web` **276 files / 2772 tests passed (91.8s)**, zero failures (+3 files / +43 tests over the parent) · `npm run build --workspace web` green (504 SPA assets, 0 audio) · `tests/test_roadmap_dag_derived.py` 9 passed and …
- `Session 2` `docs/roadmap/plans/PERSONALITY-THEMES.md:139` — ### Session 2 — Sound cues + shell element + error treatment + proofs polished

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
