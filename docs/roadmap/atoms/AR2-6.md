<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AR2-6` — Authoring fields: Specialty + Routing hints in AgentForm.tsx

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-ROUTING](../plans/AGENT-ROUTING.md) (`AR2`, plan status `done`) |
| **Atom table** | [`../atomic/AR2.md`](../atomic/AR2.md) |
| **Execution position** | #67 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #feature-agent-routing |

## Intent — what this is for

Design S1 (authoring surface); Task breakdown Session 2 T2.2 (form fields)

## Done when

Specialty + Routing-hints fields (with comma-separated-utterances hint text) round-trip through agent create/edit via draft/empty/toDraft/payload + SavedAgent type

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AR2-1`](AR2-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 36 (AR2) — DRIVEN in a real browser

**Code evidence:**

- web/src/pages/agents/AgentForm.tsx exists and is the authoring surface the clause names; the fields ride the existing draft/empty/toDraft/payload plumbing plus the SavedAgent type rather than a parallel form path
- the underlying fields round-trip on both agent layers (AR2-1, confirmed above), which is the half a form cannot fake
- test_agent_routing 15/15; test_config_roundtrip 17/17

**Driven in the UI:** Not driven: creating or editing an agent through the form was not exercised this cycle.

**Notes:** Partial for the drive only. The clause's own detail — comma-separated-utterances hint text — is the part that matters for a field whose FORMAT is load-bearing: route_hints is scored per phrase by the classifier, so a user who types a sentence instead of comma-separated utterances gets worse matching, and the hint is what prevents that rather than a validator.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/AGENT-ROUTING.md:18` — **One logged DEVIATION:** T2.2's muted-state row + Unmute affordance on the agent detail page is
- `T2.2` `docs/roadmap/plans/AGENT-ROUTING.md:133` — | T2.2 | Authoring surface: `Specialty` + `Routing hints` fields in `AgentForm.tsx` (with the "comma-separated example utterances" hint text); muted-state row + Unmute on the agent detail page | `web/src/pages/agents/AgentForm.tsx`, `web/src/pages/agents/AgentDetail.tsx` | fields round-trip throu…
- `T2.2` `docs/roadmap/plans/AGENT-ROUTING.md:179` — - [2026-07-27][S2] DEVIATION (deferred, small): the plan's T2.2 "muted-state row + Unmute on the
- `Session 2` `docs/roadmap/plans/AGENT-ROUTING.md:128` — ### Session 2 — Chip UI + authoring + suppression UX

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
