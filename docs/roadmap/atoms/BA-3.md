<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-3` — Browse loop + BrowseActionProvider contract + provider-fidelity wiring (ALLOWED_HOOK_PROVIDERS, fencing, loop guards, budget)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #81 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§7 Browse Loop Architecture (7.1 form-submission verification, 7.2 loop guards); §8.3 Workflow action-node dispatch; §9 Provider-Fidelity Wiring; Session 3

## Done when

BrowseActionProvider implements the ActionProvider ABC and 'browse' is added to ALLOWED_HOOK_PROVIDERS (src/personalclaw/validation.py); a workflow action node invokes browse by name and the definition is accepted and dispatched through the standard seams inheriting denylist/budget/profile; a multi-step task completes within max_steps (default 20) with each page fenced via fence_untrusted; SUBMIT triggers outcome verification; budget/step exhaustion parks cleanly into needs-input with notes preserved — SC 2, SC 6, SC 8

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`BA-2`](BA-2.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2-AUTOMATION-SUBSTRATE:action-node dispatch contract` | cross-plan (EXT) | **unresolved** |
| `EXT:AUTONOMY-GUARDRAILS:model-call budget chokepoint (SpendMeter) + denylist dispatch seams` | cross-plan (EXT) | **unresolved** |

### Unblocks (4)

[`BA-4`](BA-4.md) (`done`), [`BA-5`](BA-5.md) (`done`), [`BA-6`](BA-6.md) (`done`), [`BA-7`](BA-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `01eb3d1b5` 2026-08-26 — docs(roadmap): BA-3 records its open owner question, status unchanged
- `b9a73a752` 2026-08-25 — feat(browse): BA-3 browse loop + BrowseActionProvider + provider-fidelity wiring

### Named by atom id in the roadmap prose (13)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:582` — **`GatedCdpSession` still has no non-test importer** — BA-3 is its consumer by design, so clause 1 is
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:611` — lifecycle/launcher) is unbuilt and `GatedCdpSession` has no production consumer yet (BA-3's by design) —
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:683` — - **2026-08-25 — `BA-3` COMPLETE (all five clauses). Atom stays `todo` only because this code is
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:695` — `origin/main` (`2e394588`, no BA-3 code), together with its neighbour
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:742` — form. Re-homing to the apps repo is an SDK-export decision, not a BA-3 one.
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:786` — - [2026-08-26][BA-3] **OWNER RULING — `BA-3` flips to `done` on its `done_when` as written.** The
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:798` — and **not** a condition on `BA-3`: it belongs with `BA-5`'s as-a-user validation, where the same
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:892` — (the same limitation `BA-2`'s and `BA-3`'s entries record, and `BA-3`'s ruling Q2 already filed
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:918` — **Corrected citation:** `BA-3`'s row in `docs/roadmap/atomic/BA.md` still cites
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:920` — **`validation.py:812`** — the same drift `BA-3`'s own log recorded and corrected in the plan, left
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:921` — uncorrected in the atom row. Not edited here (it is `BA-3`'s row, and `dag.json` carries the same
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:931` — **Note on this plan's shape:** it has **no `## Execution log` heading**. `BA-2`, `BA-3` and `BA-7`
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1051` — real-browser gap is already recorded by `BA-2` and `BA-3`.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§7`, `§8.3`, `§9`, `Session 3`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/BROWSE-AUTOMATION.md:259` — - Each SUBMIT triggers outcome verification (§7.1)
- `§7` `docs/roadmap/plans/BROWSE-AUTOMATION.md:379` — Sentinel vocabulary (§2) migrates from positional numbers to refs (`CLICK <ref>`, `TYPE <ref>(value)`) — a re-snapshot after dynamic DOM change no longer invalidates the agent's plan (the numbered-list TOCTOU that positional-index approaches all hit). Screenshots are captured per step for verific…
- `§7` `docs/roadmap/plans/BROWSE-AUTOMATION.md:684` — unmerged**; flip it when the PR lands. Landed: `browse/loop.py` (the §7 loop), `browse/page.py`
- `§7` `docs/roadmap/plans/BROWSE-AUTOMATION.md:721` — **Clause 4 — SUBMIT verification (§7.1).** A second, separate model call, with the post-submit page
- `§9` `docs/roadmap/plans/BROWSE-AUTOMATION.md:686` — the §9 wiring and `apps/native/browse-action/app.json`. 30 new tests, `make lint` clean,
- `§9` `docs/roadmap/plans/BROWSE-AUTOMATION.md:687` — `gate_report.py` 6/6, probe sweep 16 with 0 introduced. §9's wiring list is INCOMPLETE as written:
- `§9` `docs/roadmap/plans/BROWSE-AUTOMATION.md:737` — 1. **Core placement, not the app bundle §9 describes.** §9 says `apps/browse-action/app.json`, a
- `Session 3` `docs/roadmap/plans/BROWSE-AUTOMATION.md:321` — - **Session 3 — browse loop + action provider contract:** the full browse loop (navigate, extract, prompt, parse, execute, repeat); `BrowseActionProvider` implementing `ActionProvider` ABC; `ALLOWED_HOOK_PROVIDERS` addition; form submission with outcome verification; stuck detection; visited-URL …

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
