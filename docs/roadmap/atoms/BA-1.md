<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-1` — Text-only page extraction + compression module (stable ElementRefs, screenshot-as-path, sentinel parser)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #79 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #913 |

## Intent — what this is for

§1 Text-Only Page Representation (1.1 extraction pipeline, 1.2 Links DSL, 1.3 Forms DSL); §2 Sentinel Action Vocabulary; Session 1; Amendment 2026-07-26 (a) Context compression layer + task A1

## Done when

Standalone browse/extraction.py + browse/compress.py return a structured page representation (text <=4000 chars, Links DSL, Forms DSL) from fixture HTML; a re-snapshot after DOM mutation preserves ElementRefs for unchanged elements; ref-based sentinels (CLICK <ref> / TYPE <ref>(value)) parse correctly; a 100K-token DOM enters context as <1K tokens + a screenshot path with no base64 in any rendered prompt (regression-tested) — SC 1, task A1

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`BA-2`](BA-2.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `14529480c` 2026-08-08 — feat(ambient): AS-1 composable home — dashboard-as-views registry, Overview preset, pin + propose, AmbientConfig (#910)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:513` — **DISCOVERY (pre-existing BA-1, outside this atom): `browse/__init__.py`'s docstring claim "No network,
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:738` — first-party app. BA-1 and BA-2 already landed `browse/` in core, and an app bundle may only import

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `§1`, `§2`, `Session 1`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:305` — - **Action dispatch:** inherits denylist enforcement at the three dispatch seams (`hooks.py`, `gateway.py`, `event_triggers.py`) — the browse provider's execute() is called after `check_action` passes (AUTONOMY-GUARDRAILS §1.2).
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:356` — **The three make-or-break enrichments + the actuator pattern.** Design evidence: browse agents live or die on context cost (~20x reduction from compressed outlines + screenshots-as-paths; unusable without), on the user being able to *watch* an unattended browse, and on auth that never transits th…
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:360` — - **(a) Context compression layer** — an in-gateway module between the browser driver and the agent, formalizing §1 into a stable contract:
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:367` — text: str                    # §1.1 pipeline output, ≤4000 chars
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:445` — - **Prompt injection reaches a privileged surface.** A page the agent reads can attempt to steer it while it holds an authenticated session. Existing controls apply (`fence_untrusted` on all page content, the §1 compression layer meaning raw DOM never enters context) and must not be weakened for …
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:572` — §10's Session-2 line also promises "browser lifecycle management (launch with persistent profile, CDP
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:610` — `done_when` clauses are met and proven live, but BA-2's declared *scope* (§4 Stealth Stack + §10 browser
- `§1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:677` — `done_when`, or wait for §4 Stealth Stack + §10 launcher) is unchanged and still the owner's; this pass
- `§2` `docs/roadmap/plans/BROWSE-AUTOMATION.md:275` — - **Budget integration:** each LLM call within the loop charges through the model-call chokepoint (AUTONOMY-GUARDRAILS §2); budget exhaustion parks the run.
- `§2` `docs/roadmap/plans/BROWSE-AUTOMATION.md:379` — Sentinel vocabulary (§2) migrates from positional numbers to refs (`CLICK <ref>`, `TYPE <ref>(value)`) — a re-snapshot after dynamic DOM change no longer invalidates the agent's plan (the numbered-list TOCTOU that positional-index approaches all hit). Screenshots are captured per step for verific…
- `§2` `docs/roadmap/plans/BROWSE-AUTOMATION.md:426` — 5. Every grant, navigation, action, and revocation is **SEL-audited** (§2.3 conventions; new event types lowercase snake, e.g. `browser_grant`, `browser_revoked`).
- `Session 1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:319` — - **Session 1 — page extraction + action parsing:** markdownify + sentence filter + size cap; links/forms DSL extraction; sentinel action parser; unit tests with fixture HTML pages (static, form-heavy, JS-rendered snapshots). Output: a standalone `browse/extraction.py` module that takes HTML and …
- `Session 1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:386` — (a) restructures Session 1 (extraction was already there; stable refs + screenshot-path discipline join it). (b), (c)-sharpening, and (d) are new surface + persistence work: one added **Session 5**. Honest count ~4 → **~5**.
- `Session 1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:390` — | A1 | Stable `ElementRef` contract + ref-based sentinels + screenshot-as-path discipline folded into the extraction module (extends Session 1) | `browse/compress.py`, `browse/extraction.py`, sentinel parser, fixture tests | a re-snapshot after DOM mutation preserves refs for unchanged elements; …

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
