<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `INU-4` — Fold the proposal surfaces (S4)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INBOX-NOTIFICATIONS-UNIFICATION](../plans/INBOX-NOTIFICATIONS-UNIFICATION.md) (`INU`, plan status `done`) |
| **Atom table** | [`../atomic/INU.md`](../atomic/INU.md) |
| **Execution position** | #296 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##114 |

## Intent — what this is for

Session 4 — Fold the proposal surfaces (Wave 2) (T4.1-T4.4, V4)

## Done when

skills enqueue() also emits a proposal item (refs.pid); inbox row accept/reject call skills/proposals.accept/reject resolving HANDLED/DISMISSED; idempotent backfill gives each pending proposal without an item exactly one (re-run no-op); skills approval tab cross-links the filtered inbox (single install path); outlived tool approvals mirror an agent_request item after a grace period (asyncio.shield-protected), answering either surface resolves both

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`INU-1`](INU-1.md) | intra-repo | `done` |
| [`INU-2`](INU-2.md) | intra-repo | `done` |

### Unblocks (1)

[`INU-7`](INU-7.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 3 (INU) — observed

**Code evidence:**

- tests/test_inbox_proposals.py and tests/test_inbox_app_proposals.py pass (part of an 86-passed run)

**Notes:** PARTIAL: 'folding the proposal surfaces' is a claim about surfaces a user sees, and the validation home has no proposals to render — nothing generated one, because that needs a model provider or an app emitting them. Code rail observed; the folded UI is unvalidated.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `T4.1`, `T4.4`, `Session 4`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:156` — - **Called by:** every `notify()` emitter (~10 sites, T1.2 migrates them to typed kinds); loop watchdog + gateway autopause → `emit_attention_item(kind="needs_input")`; skills `enqueue()` → `emit_attention_item(kind="proposal")` (T4.1); channel trust (plan 40) → `kind="agent_request"`; LEARNING-F…
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:157` — - **Calls:** `DashboardState.notify` (unchanged choke point, §3.4), `resolve_kind`, `ChannelDelivery.deliver_notification` (channel_dm target), `skills/proposals.accept/reject` (T4.1). (No `gate_enabled`, no migration framework — see *Change discipline*.)
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:197` — | T4.1 | Skills proposals → inbox: `enqueue()` also emits a `proposal` item (refs.pid); accept/reject actions on the inbox row call `skills/proposals.accept/reject`; item resolves HANDLED/DISMISSED accordingly | `skills/proposals.py`, inbox handlers, inbox frontend row actions | proposing → item …
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:260` — **The Proposals contract (owner-approved primitive).** This plan already types `proposal` (C1/C4) and folds the skills queue in at S4 (T4.1) with LEARNING-FLYWHEEL's queue registering as `proposal` from birth — this amendment does NOT re-add the kind. What is missing is a CONTRACT: today a propos…
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:277` — #  {"skill_promotion": {pid}}              — skills/proposals.accept (T4.1 path, now one case of the contract)
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:287` — Extends **Session 4** (which already owns the proposal fold-in): T4.1 becomes the first `apply.skill_promotion` consumer of C6 rather than a bespoke wiring. The app path + batching = new **Session 7** (after S4-6; count 6 → 7, honest). Round 1's Session 6 (verification gate) is untouched; C6 comp…
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:291` — | T7.1 | C6 Proposal dataclass + apply dispatcher (four apply cases through existing dispatchers; typed failure keeps item PENDING); T4.1's skill path re-expressed as `skill_promotion` | `inbox.py` or new `proposals_contract.py`, inbox handlers, tests | each apply case round-trips on fixtures; a …
- `T4.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:429` — - 2026-07-30 — **DONE (Session 4: T4.1–T4.4).** Skill proposals and outlived tool approvals
- `T4.4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:200` — | T4.4 | Session-modal approvals: when a tool-approval prompt outlives its session view (unanswered > TTL or session backgrounded), mirror an `agent_request` item (refs.session, deep-link to the approval); answering either surface resolves both | approval prompt machinery (`chat_runner`/approval …
- `T4.4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:429` — - 2026-07-30 — **DONE (Session 4: T4.1–T4.4).** Skill proposals and outlived tool approvals
- `T4.4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:458` — **T4.4 — approval mirroring, with a grace period.** An approval prompt is session-modal for
- `Session 4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:193` — ### Session 4 — Fold the proposal surfaces (Wave 2)
- `Session 4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:287` — Extends **Session 4** (which already owns the proposal fold-in): T4.1 becomes the first `apply.skill_promotion` consumer of C6 rather than a bespoke wiring. The app path + batching = new **Session 7** (after S4-6; count 6 → 7, honest). Round 1's Session 6 (verification gate) is untouched; C6 comp…
- `Session 4` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:429` — - 2026-07-30 — **DONE (Session 4: T4.1–T4.4).** Skill proposals and outlived tool approvals

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
