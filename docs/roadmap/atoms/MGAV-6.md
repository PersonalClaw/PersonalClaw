<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MGAV-6` — Two-way readable vault: mode config, wikilink projection, sync pass, vault lints, raw capture, seeding, snapshot

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MEMORY-GRAPH-AND-VAULT](../plans/MEMORY-GRAPH-AND-VAULT.md) (`MGAV`, plan status `done`) |
| **Atom table** | [`../atomic/MGAV.md`](../atomic/MGAV.md) |
| **Execution position** | #350 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1451 |

## Intent — what this is for

§5.1 mirror→two-way projection, §5.2 edits flow back, §5.3 structure conventions+lints, §5.5 interop (symlink, raw/ capture, starter seeding, static export, git/snapshot); Session 4

## Done when

memory.vault_mode off|mirror|two_way (back-reads vault_enabled); pages carry frontmatter+source_hash and [[wikilinks]] generated from mem_links with compiled-truth+append-only-timeline shape; on-cadence/on-demand POST /api/memory/vault/sync parses hash-changed pages back through the MemoryService write path (S5 scan, WAL, edit-wins, sync-conflict flag, no data loss); vault lints (backlink symmetry, stale hash, broken links, orphans); raw/ sweep routes files to the knowledge ingest queue only; starter seeding writes only missing/pristine files; snapshot.py adds the vault dir

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MGAV-1`](MGAV-1.md) | intra-repo | `done` |

### Unblocks (1)

[`MGAV-9`](MGAV-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `e25a6ffa0` 2026-08-21 — feat(knowledge): KL-20 project knowledge to plain markdown the owner can edit back
- `69c3b36f0` 2026-08-16 — feat(memory): MGAV-6 two-way readable vault with hash-gated sync

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1714` — - **MGAV-6's four mechanics are REUSED by import, not re-derived.** The atom names two vaults
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1719` — (`absorb → project → collect deletions`, in that order and for the reason MGAV-6 documents),
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1748` — - **A LEDGER (`vault_projections`) replaces MGAV-6's whole-library manifest scan.** Keyed backlog
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:809` — ### 2026-08-16 — Session 4 (§5.1–5.3, §5.5): the two-way readable vault — DONE (`MGAV-6`)
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:811` — - **DONE `MGAV-6`.** `memory_vault.py` became mode-driven. `memory.vault_mode`
- **source plan** `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:991` — none), and edit-vault + undo exercise `MGAV-3`/`MGAV-6` machinery this atom only surfaces. Those

### Its plan's own record, via the scope references (19 line(s))

This atom's scope cites `§5.1`, `§5.2`, `§5.3`, `§5.5`, `Session 4`. Those tokens appear in its source plan at:

- `§5.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:279` — | `memory_vault.py` (read-only mirror) | **EXTENDED** | becomes mode-driven `off|mirror|two_way`; layout+frontmatter conventions §5.1; sync pass §5.2. Legacy `vault_enabled` back-read on load |
- `§5.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:809` — ### 2026-08-16 — Session 4 (§5.1–5.3, §5.5): the two-way readable vault — DONE (`MGAV-6`)
- `§5.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:831` — episodic page (evidence is immutable, §5.1), a lesson's rule/counter-example structure — is
- `§5.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:872` — - **DEVIATION (§5.1 `last_updated`).** Not emitted. `_FM_ORDER` already carries the record's
- `§5.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:279` — | `memory_vault.py` (read-only mirror) | **EXTENDED** | becomes mode-driven `off|mirror|two_way`; layout+frontmatter conventions §5.1; sync pass §5.2. Legacy `vault_enabled` back-read on load |
- `§5.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:785` — for 90 days, not forever. Worth deciding alongside §5.2's no-data-loss guarantee.
- `§5.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:868` — - **DEVIATION (§5.2 supersession chain).** "the store version preserved in the supersession
- `§5.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:203` — index.md            # the always-injected bounded summary table (§5.3)
- `§5.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:286` — | `memory_lint.py` | **EXTENDED** | orphans/phantoms/proposed-entities/vault lints (§2.3, §5.3) |
- `§5.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:853` — hid until §5.3's broken-link check measured the vault against itself; `render_session_hub`
- `§5.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:880` — - **DEVIATION (§5.3 page types).** Only `concept|slot|synthesis|entity|session|tag|index|guide`
- `§5.3` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:881` — are declared. `connection` and `qa` are in §5.3's vocabulary but nothing generates them, and a
- `§5.5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:208` — raw/                # watched capture dir → routed to KNOWLEDGE ingestion (§5.5)
- `§5.5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:809` — ### 2026-08-16 — Session 4 (§5.1–5.3, §5.5): the two-way readable vault — DONE (`MGAV-6`)
- `§5.5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:858` — **Durability (§5.5's snapshot clause).** `memory-vault` is a declared `StateEntry` (tree,
- `§5.5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:875` — - **DEVIATION (§5.5 starter seeding source).** Seeds are the vault's own README rather than an
- `§5.5` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:883` — - **DISCOVERY (out of scope, left open).** §5.5's `--html` static export ("lowest priority",
- `Session 4` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:334` — - **Session 4 — vault two-way:** vault_mode config (+legacy back-read), layout/frontmatter/wikilink generation from `mem_links`, sync pass (hash compare → MemoryService write path → conflict flags), vault lints, `raw/` → knowledge-queue sweep, starter seeding, snapshot.py vault-tree addition, mem…
- `Session 4` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:809` — ### 2026-08-16 — Session 4 (§5.1–5.3, §5.5): the two-way readable vault — DONE (`MGAV-6`)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
