<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-1` — Spec harness core: harness/ package, 3 spec kinds, validate/explain, profiles, AGENT.md

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #544 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1.1 (three spec kinds), §1.2 (validate|explain + profiles), §4.2 (AGENT.md)

## Done when

python -m harness validate passes on seeded 8 rules/3 scenarios/1 task spec; a dangling requiredTests node-id fails validation; AGENT.md exists at repo root covering the two canonical gotchas (Success Criteria #1, #9)

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`SV-2`](SV-2.md) (`done`), [`SV-3`](SV-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `§1.1`, `§1.2`, `§4.2`. Those tokens appear in its source plan at:

- `§1.1` `docs/roadmap/plans/SELF-VERIFICATION.md:225` — - **Session 1 — spec harness core (§1.1, §1.2, §4.2)** *(Wave 0/1-compatible)*: `harness/` layout; three spec schemas + `validate`/`explain`; profile→command mapping; `AGENT.md` written and cross-referenced; first 6-8 rule specs + 3 scenario specs seeded from the known bug classes.
- `§1.1` `docs/roadmap/plans/SELF-VERIFICATION.md:268` — - [2026-07-26][S1] DONE: Spec harness core (§1.1, §1.2, §4.2). Created the repo-inner `harness/` package (`__init__.py`, `__main__.py`, `specs.py`, `validate_refs.py`, `profiles.py`, `cli.py`, `README.md`) beside `src/`/`tests/`/`scripts/` — NOT in the shipped wheel (`pyproject` finds packages on…
- `§1.2` `docs/roadmap/plans/SELF-VERIFICATION.md:67` — profiles.py    # profile → concrete command mapping (§1.2)
- `§1.2` `docs/roadmap/plans/SELF-VERIFICATION.md:225` — - **Session 1 — spec harness core (§1.1, §1.2, §4.2)** *(Wave 0/1-compatible)*: `harness/` layout; three spec schemas + `validate`/`explain`; profile→command mapping; `AGENT.md` written and cross-referenced; first 6-8 rule specs + 3 scenario specs seeded from the known bug classes.
- `§1.2` `docs/roadmap/plans/SELF-VERIFICATION.md:268` — - [2026-07-26][S1] DONE: Spec harness core (§1.1, §1.2, §4.2). Created the repo-inner `harness/` package (`__init__.py`, `__main__.py`, `specs.py`, `validate_refs.py`, `profiles.py`, `cli.py`, `README.md`) beside `src/`/`tests/`/`scripts/` — NOT in the shipped wheel (`pyproject` finds packages on…
- `§4.2` `docs/roadmap/plans/SELF-VERIFICATION.md:33` — - **NEW-17 amendment** (machine-facing repo-gotchas `AGENT.md` checked into the repo: installed-apps sync, static/dist symlink, venv path) → §4.2.
- `§4.2` `docs/roadmap/plans/SELF-VERIFICATION.md:72` — The CLI runs on the repo venv (`.venv/bin/python` at the repo root — the documented interpreter gotcha, now also §4.2 content). ClawX's reference is Node (`pnpm harness …`); PClaw's is Python for zero new toolchain.
- `§4.2` `docs/roadmap/plans/SELF-VERIFICATION.md:163` — 3. **`execute` (stage node, `isolation` + `tools_posture` per WF2)** — a subagent (spawned through the normal `SubagentManager.spawn` engine path, `silent=True`, `__wf_depth` enforced) drives the scenario against the **live local gateway UI** via the Chrome DevTools MCP server bound through the e…
- `§4.2` `docs/roadmap/plans/SELF-VERIFICATION.md:225` — - **Session 1 — spec harness core (§1.1, §1.2, §4.2)** *(Wave 0/1-compatible)*: `harness/` layout; three spec schemas + `validate`/`explain`; profile→command mapping; `AGENT.md` written and cross-referenced; first 6-8 rule specs + 3 scenario specs seeded from the known bug classes.
- `§4.2` `docs/roadmap/plans/SELF-VERIFICATION.md:268` — - [2026-07-26][S1] DONE: Spec harness core (§1.1, §1.2, §4.2). Created the repo-inner `harness/` package (`__init__.py`, `__main__.py`, `specs.py`, `validate_refs.py`, `profiles.py`, `cli.py`, `README.md`) beside `src/`/`tests/`/`scripts/` — NOT in the shipped wheel (`pyproject` finds packages on…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
