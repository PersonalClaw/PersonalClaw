<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-15` — Embedding batch path: provider batching, bounded retry with backoff, adaptive batch bisection

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #316 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1816 |

## Intent — what this is for

Capability-gap amendment (2026-08-19)

## Done when

the ingest and re-embed paths call the embedding provider's batch entry point (already implemented by the shipped model apps and currently unreached from core) instead of one provider call per chunk, and the per-call thread-pool churn is removed; a configured batch size groups chunk texts per call; each call is wrapped in bounded retries with exponential backoff, replacing today's failure-returns-None-and-logs-at-debug path; on a retryable or batch-size-shaped error a batch of more than one splits in half and retries each half recursively until it succeeds or an individual chunk fails, so a provider's undeclared batch ceiling is discovered rather than failing the whole import — this matters precisely because providers are removable app bundles whose limits core cannot know; a fake provider that rejects batches above K proves the bisection converges and that no chunk is silently dropped; batch size and retry budget round-trip through config

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `c589506d6` 2026-08-27 — fix(resilience): the re-embed backfill could not embed anything
- `4b0569043` 2026-08-19 — docs(roadmap): cite the landing PR for KL-15
- `4ecbb8c41` 2026-08-19 — feat(knowledge): KL-15 batched embedding with bounded retry and adaptive bisection
- `0377b537e` 2026-08-19 — docs(roadmap): add capability-gap amendment atoms to KL, WF2KNO and LMMV

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1045` — - [2026-08-20][KL-15] DONE — the embedding batch path, with retry and adaptive bisection. Every
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1276` — deleted (that exact vacuous test shipped in KL-15 and had to be split). `candidate_multiple` is

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
