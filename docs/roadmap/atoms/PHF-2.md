<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-2` — cgroup v2 second enforcement tier

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #439 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, verified 2026-08-27. The mechanism HOLDS and is REACHABLE: cgroup_scope_argv emits systemd-run --user --scope with TasksMax/MemoryMax/MemorySwapMax=0, called from spawn_shim_argv - the live chokepoint imported by 9 production modules - and the scope is the OUTER layer so it contains the NOFILE shim. The probe never raises (total inside a defensive except Exception) and the doctor wiring is VERIFIED NOT INERT (probe id sandbox.cgroup_scopes, Tier.CAPABILITY, registered inside _register_builtin_probes which executes at import time). UNMET as an OBSERVATION: there is no fork-bomb fixture anywhere. test_sandbox_cgroup_scopes.py has zero fork-bomb tests and its only platform gates are skipif(platform != 'darwin'), so they SKIP on CI; no test anywhere execs systemd-run - every hit is an argv assertion or a monkeypatched shutil.which. A Linux CI leg exists (all 22 runs-on lines are ubuntu-latest) but has no test to run, and GitHub runners have no systemd user bus anyway. The macOS warning clause HOLDS BUT CONDITIONALLY: _warn_unenforced_ceilings is latched once and names RLIMIT_AS/NPROC honestly, but it returns EARLY unless max_pids>0 or max_rss_mb>0 and BOTH default to 0 - so a DEFAULT macOS install emits NO warning, while the clause reads unconditional. Recorded caveat at sandbox.py: a readable root controller list does NOT prove pids/memory are DELEGATED, so TasksMax can be inert even where the probe reports available. SMALLEST REMAINING UNIT: a Linux-gated fork-bomb containment test that actually execs systemd-run and skips cleanly when probe_cgroup_scopes() is False, so the clause has a fixture at all. |

## Intent — what this is for

Add an opt-in sandbox.cgroup_scopes tier on Linux as a second enforcement layer above the NOFILE floor: systemd-run --user --scope with TasksMax, MemoryMax, and MemorySwapMax=0. Probe once for a unified cgroup hierarchy plus a systemd user session and add the probe line to the doctor. On macOS, non-systemd, or containers, emit exactly one loud warning naming what is not enforced (pids/RSS) while NOFILE still applies; the probe never raises.

## Done when

On a Linux fixture a fork bomb hits pids.max and dies contained; on macOS one warning states pids/RSS are not enforced; the probe never raises under any environment.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PHF-1`](PHF-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `606612848` 2026-08-25 — fix(tools): scope audit_landed_atoms verdicts to the atom being adjudicated
- `7532ed2c8` 2026-08-22 — feat(sandbox): PHF-2 cgroup v2 enforcement tier, and an honest warning where it cannot apply
- `5b46fdcd4` 2026-08-13 — feat(guardrails): AG-6 earn autonomy per action type, lose it on one rejection (#1197)
- `c5ff4b3e6` 2026-08-07 — feat(security): PHF-1 post-exec resource-ceiling shim + spawn tripwires (#896)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:479` — language implies belong to PHF-2's Linux cgroup tier, not to an rlimit. Separately, this host is
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1335` — - **2026-08-22 — `PHF-2` PARTIAL: the tier ships and the macOS half is measured; the Linux fork-bomb
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:2145` — ## Execution log — PHF-2 (cgroup v2 second enforcement tier) — validated done; plan COMPLETE (15/15)

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
