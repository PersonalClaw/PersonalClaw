<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EIAT-2` — mail-inbox provider app: IMAP inbound, checkpointing, fail-closed allowlist, MIME extraction (S2)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EMAIL-INBOX-AND-TRIGGERS](../plans/EMAIL-INBOX-AND-TRIGGERS.md) (`EIAT`, plan status `done`) |
| **Atom table** | [`../atomic/EIAT.md`](../atomic/EIAT.md) |
| **Execution position** | #236 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 — The mail inbox provider app (T2.1, T2.2, T2.3); Contract C3. New PersonalClawApps/mail-inbox/ app.

## Done when

Polling a real IMAP mailbox surfaces messages as inbox items; a restart neither reprocesses nor skips (UID checkpoint via poll's returned dict, tested) and a duplicate Message-ID is dropped; the sender allowlist is provider-enforced and fail-closed — an unlisted sender and an empty allowlist both surface ZERO messages and zero events (both asserted), startup logs the posture, and SEL mail_sender_rejected fires per rejection; multipart mail extracts text/plain, HTML-only mail is sanitized, a PDF attachment contributes extracted text via the platform's existing document readers; credentials come only from the SDK credential store (never app.json/ProviderSettings); app installs from a local Store source. Validation (V2) requires an owner-provided test mailbox.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EIAT-1`](EIAT-1.md) | intra-repo | `done` |

### Unblocks (3)

[`EIAT-3`](EIAT-3.md) (`done`), [`EIAT-4`](EIAT-4.md) (`done`), [`EIAT-6`](EIAT-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `92420d541` 2026-08-11 — feat(inbox): INU-8 resolve app-contributed inbox sources + make the #47 guard bidirectional (#1090)
- `0a40c2ade` 2026-08-11 — docs(roadmap): add INU-8 — inbox provider-seam resolution, with design + implementation plan (#1089)
- `43fa10e3e` 2026-08-09 — docs(roadmap): CE-4 Discord channel app done (apps#29) (#949)
- `ca6562230` 2026-08-09 — docs(roadmap): EIAT-2 mail-inbox provider app done (apps#27) (#941)

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:297` — - **Not the `mail-inbox` app.** `mail-inbox` (EIAT-2) is a `MessageSourceProvider` — mail as a
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:377` — **NOT a slack-only gap — `mail-inbox` has the same shape.** `mail-inbox/app.json` declares `{"type":"inbox","implementation":"mail_inbox_runtime.provider:create_provider"}` and is subject to the identical dead end, so EIAT-2's provider is equally unreachable. This is a seam defect affecting every…
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:241` — - **2026-08-09 — DONE: EIAT-2** (mail-inbox provider app: IMAP inbound, checkpointing, fail-closed
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:320` — breaking EIAT-2's guardrail. The IMAP password stays credential-store-only and a test asserts zero
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:346` — `inert-surface-baseline.json` had independently censused both as inert. EIAT-2's mail-inbox app
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:542` — CE-8 and EIAT-2 both dead-ended on the same seam and no atom owned the fix; CE-8's own log states it
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:556` — (Slack inbox source, recorded BLOCKED-E1 twice by independent passes) and EIAT-2 (`mail-inbox`
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:600` — **Hand-off:** CE-8 part 1 and EIAT-2 are now unblocked — their app-side adapters were already written
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:605` — CE-8/EIAT-2 — a follow-up row for whoever lands CE-8 part 1.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T2.1`, `T2.2`, `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:185` — | T2.1 | Scaffold `mail-inbox` with `app.json` (minimum permissions), `MessageSourceProvider` impl, SDK-credential wiring; IMAP inbound + UID checkpointing via `poll`'s returned dict + `Message-ID` dedup | `PersonalClawApps/mail-inbox/` | polling a real IMAP mailbox surfaces messages as inbox ite…
- `T2.1` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:243` — - **T2.1 (IMAP + checkpointing):** `MailInboxProvider.poll` returns `(messages, checkpoints)` where
- `T2.2` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:186` — | T2.2 | **Fail-closed sender allowlist** in the provider: no allowlist ⇒ zero messages surfaced; startup logs the posture; SEL `mail_sender_rejected` per rejection | `mail-inbox/provider.py`, `test_provider.py` | an unlisted sender produces no inbox item and no event; an empty allowlist surfaces…
- `T2.2` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:249` — - **T2.2 (fail-closed allowlist + SEL):** the allowlist is provider-enforced with `fnmatch` globs.
- `T2.3` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:187` — | T2.3 | MIME handling: prefer `text/plain`, sanitized-HTML fallback, attachment text via the platform's existing readers; body/subject carried raw into the event (fencing is downstream) | `mail-inbox/provider.py`, tests | multipart mail extracts correctly; an HTML-only mail is sanitized; a PDF a…
- `T2.3` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:255` — - **T2.3 (MIME):** `extract_body` prefers `text/plain`, sanitizes HTML-only mail (drops
- `Session 2` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:181` — ### Session 2 — The mail inbox provider app (apps repo)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
