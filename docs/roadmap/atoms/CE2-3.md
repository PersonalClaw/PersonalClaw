<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE2-3` — Continuous background compression service + shared topic segmenter + prefix-stability locks

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CONTEXT-ECONOMY](../plans/CONTEXT-ECONOMY.md) (`CE2`, plan status `done`) |
| **Atom table** | [`../atomic/CE2.md`](../atomic/CE2.md) |
| **Execution position** | #126 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§3 prefix-stability KV-cache contract (invariants 1-2 unit-locked), §4 continuous background compression service (context_segmentation.py shared with LOOP-R13, attention-weighted per-topic compression, consolidation-cadence wiring, incognito/temporary skip, archive-before-rewrite, kill switch + idle-days knob); Session 3

## Done when

SC#5 (7-day-idle session topic-compressed at consolidation tick, incognito untouched, disable stops within a tick) + SC#6 (byte-stability: same raw → identical previews, at-rest only) met; ToolsConfig.bg_compress_enabled/bg_compress_idle_days wired; log [2026-07-27][S3] DONE, gate green (full suite)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CE2-2`](CE2-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (22 line(s))

This atom's scope cites `§3`, `§4`, `Session 3`, `R13`. Those tokens appear in its source plan at:

- `§3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:25` — - **NEW-16** (reversible context compression: compress-cache-retrieve loop, lossy compressed originals behind unguessable hash markers + retrieval tool with byte/line ranges; type-routed compressors — JSON crusher, AST-aware code, prose model — over subagent transcripts and tool outputs; builtin/…
- `§3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:57` — - **Marker stability**: the recovery hint appended to a preview becomes a pure function of the content, so replayed/compacted transcripts stay byte-identical (prefix stability, §3).
- `§3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:157` — - Takes effect at the **next turn boundary** (§3 prefix corollary); the current turn's in-flight tool calls are unaffected.
- `§3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:219` — - **Session 3 — background compression service (§3, §4):** `context_segmentation.py` (shared with LOOP-R13); attention-weighted per-topic compression; consolidation-cadence wiring with per-tick budget; incognito/temporary skip; archive-before-rewrite reversibility; prefix-stability unit tests (in…
- `§3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:298` — - [2026-07-27][S3] DONE: continuous background compression service (§3, §4). (a) **Shared segmenter** — new `context_segmentation.py` (`segment_messages`, pure, no config/provider graph): embedding-drift boundaries when an `embed_fn` is bound (adjacent user-turn cosine < `DEFAULT_DRIFT_THRESHOLD=…
- `§3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:299` — - [2026-07-27][S4] DONE: dynamic tool-group activation core (§5.1-5.4). (a) **The group model** — new `tool_providers/groups.py`: frozen `ToolGroup` (name/display/instructions/always_on/capability/tools) DERIVED one-per-registered-provider (`group_name_for_provider` shortens registry names to mod…
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:26` — - **NEW-16 amendment** (continuous background compression service over old conversation and loop history using topic segmentation and attention-weighted summarization — the always-on complement to the on-demand loop) → §4, Session 3. Source: `agent-zero` (topic segmentation with attention ratios:…
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:31` — - **WF2-R6 / WF2-R13** (approved, WORKFLOWS-V2 §2 Context Lifecycle + Run Ledger): node output offloading to `runs/<id>/artifacts/` and crystallize-before-prune journal digests are the ENGINE's context lifecycle and stay speced there. This plan supplies the shared compressor library those mechani…
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:32` — - **LOOP-R13** (approved, WORKFLOWS-V2-LOOPS-EVOLUTION, Context-overflow recovery): proactive topic-segmented compression for workflow-template loops is that plan's migration item. §4's topic segmenter is built as the SHARED primitive LOOP-R13 consumes — one segmenter, two callers.
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:95` — An LLM summarizer (`one_shot_completion(use_case="background")` — the `reasoning`-axis resolution path, never the native chat runtime) for long natural-language outputs, used ONLY where latency is already tolerable: the background compression service (§4) and the subagent-result path (§2.5). It i…
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:110` — 3. **Compaction is a declared prefix break**: `rewrite_session` (history.py:682) and the compact hook (`context_compaction.py`) are discrete whole-transcript events — inherently cache-invalidating, and that is fine because they are RARE. The background service (§4) only touches sessions **at rest…
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:219` — - **Session 3 — background compression service (§3, §4):** `context_segmentation.py` (shared with LOOP-R13); attention-weighted per-topic compression; consolidation-cadence wiring with per-tick budget; incognito/temporary skip; archive-before-rewrite reversibility; prefix-stability unit tests (in…
- `§4` `docs/roadmap/plans/CONTEXT-ECONOMY.md:298` — - [2026-07-27][S3] DONE: continuous background compression service (§3, §4). (a) **Shared segmenter** — new `context_segmentation.py` (`segment_messages`, pure, no config/provider graph): embedding-drift boundaries when an `embed_fn` is bound (adjacent user-turn cosine < `DEFAULT_DRIFT_THRESHOLD=…
- `Session 3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:26` — - **NEW-16 amendment** (continuous background compression service over old conversation and loop history using topic segmentation and attention-weighted summarization — the always-on complement to the on-demand loop) → §4, Session 3. Source: `agent-zero` (topic segmentation with attention ratios:…
- `Session 3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:219` — - **Session 3 — background compression service (§3, §4):** `context_segmentation.py` (shared with LOOP-R13); attention-weighted per-topic compression; consolidation-cadence wiring with per-tick budget; incognito/temporary skip; archive-before-rewrite reversibility; prefix-stability unit tests (in…
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:18` — **Companions:** WORKFLOWS-V2 (§2 Context Lifecycle / WF2-R6 output offloading consumes the shared compressor), WORKFLOWS-V2-LOOPS-EVOLUTION (LOOP-R13 topic-segmented compression consumes the same segmenter), WORKFLOWS-V2-LEARNING-FLYWHEEL (LEARN-R12 owns the skills-side context-budget reclaim; th…
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:31` — - **WF2-R6 / WF2-R13** (approved, WORKFLOWS-V2 §2 Context Lifecycle + Run Ledger): node output offloading to `runs/<id>/artifacts/` and crystallize-before-prune journal digests are the ENGINE's context lifecycle and stay speced there. This plan supplies the shared compressor library those mechani…
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:32` — - **LOOP-R13** (approved, WORKFLOWS-V2-LOOPS-EVOLUTION, Context-overflow recovery): proactive topic-segmented compression for workflow-template loops is that plan's migration item. §4's topic segmenter is built as the SHARED primitive LOOP-R13 consumes — one segmenter, two callers.
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:121` — - **Eligibility:** sessions idle > `tools.bg_compress_idle_days` (default 7) whose transcript exceeds a size floor; per-loop file dirs (`config_dir()/loop/<id>/findings/`) of TERMINAL loops past the same idle window. Workflow-run dirs are EXCLUDED — crystallize-before-prune (WF2-R13) owns those.
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:122` — - **Topic segmentation + attention-weighted summarization** (`agent-zero`): segment the transcript by embedding-drift boundaries (active embedding provider; deterministic turn-count fallback when no embedder is bound — degraded mode is designed, not accidental), then compress per-topic with atten…
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:219` — - **Session 3 — background compression service (§3, §4):** `context_segmentation.py` (shared with LOOP-R13); attention-weighted per-topic compression; consolidation-cadence wiring with per-tick budget; incognito/temporary skip; archive-before-rewrite reversibility; prefix-stability unit tests (in…
- `R13` `docs/roadmap/plans/CONTEXT-ECONOMY.md:298` — - [2026-07-27][S3] DONE: continuous background compression service (§3, §4). (a) **Shared segmenter** — new `context_segmentation.py` (`segment_messages`, pure, no config/provider graph): embedding-drift boundaries when an `embed_fn` is bound (adjacent user-turn cosine < `DEFAULT_DRIFT_THRESHOLD=…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
