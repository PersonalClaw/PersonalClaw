<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2LEA-12` — Amendment E1.4: project_context_review → typed proposals into the §2.2 queue

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-LEARNING-FLYWHEEL](../plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md) (`WF2LEA`, plan status `done`) |
| **Atom table** | [`../atomic/WF2LEA.md`](../atomic/WF2LEA.md) |
| **Execution position** | #597 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #908 |

## Intent — what this is for

Amendment (c) self-updating project context with an approval gate; task row E1.4

## Done when

project_context_review reads a conversation/run and emits project_instruction/project_file/project_skill proposals with per-item rationale into the §2.2 queue; nothing written until accepted; accepting applies exactly the accepted items; the decision is recorded so a second review does not re-propose a rejected item; prompt-triggered only

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2LEA-1`](WF2LEA-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 30 (WF2LEA) — DRIVEN in a real browser

**Code evidence:**

- MEASURED: project_context_review.py has TWO non-self importer files
- the three typed proposal kinds (project_instruction / project_file / project_skill) route into the same §2.2 queue, so 'nothing written until accepted' and the decision-memory suppression are the queue's existing properties rather than new ones
- tests/test_learning_project_context_review.py is part of the 951
- 951 tests pass across the 30 learning suites

**Driven in the UI:** Not driven: prompt-triggered only, so it needs a chat turn against a bound model.

**Notes:** Partial for the drive. Same architectural note as the promotion atom: the clause's hardest guarantees (nothing written until accepted, a rejected item does not re-propose) are inherited from the shared queue rather than reimplemented, which is why they are cheap to trust here.

## Recorded history

### Commits (2)

- `ca5acbadc` 2026-08-08 — feat(workflows): WF2UNI-10 plan-review FE — QuestionSlider stepper, streaming multi-view, RUN_LIFECYCLE events, naming (#909)
- `e939e9e4f` 2026-08-08 — feat(learning): WF2LEA-12 project_context_review — typed proposals into the review queue (#908)

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:796` — **CORRECTION to the atom's own text:** the done-when says `Kind` is "a closed 6-value enum". It has 9 values — `PROJECT_INSTRUCTION`/`PROJECT_FILE`/`PROJECT_SKILL` landed since (WF2LEA-12). The blocker was real; only the count was stale.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
