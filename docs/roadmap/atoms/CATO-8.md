<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-8` — Settings → Usage panel with honest-zero / partial-pricing marker

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #104 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2(c) account-level; C3 — Surfaces (Settings→Usage) + frontend rules; C4 (MAY read SpendMeter config, MUST NOT write it); Session 2 T2.3 + T2.4

## Done when

UsagePanel renders Today/7d/30d totals, a by-model table, a by-source table, the cache-savings line (0 when no cache support), and a read-only 'spent $X of your $Y cap' from SpendMeter config; period segmented control state round-trips the URL; a period mixing priced+unpriced models shows a visible 'partial — N unpriced models' marker (never a confidently-complete number); the orphaned SystemAgentStats.input_tokens is wired (no parallel type added); zero primitive-adoption ratchet trips (Button/Segmented/ui-forms only); token-lint + a11y (focus-visible, reduced-motion) pass

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-5`](CATO-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:252` — (CATO-8) remain. **Gates:** `make lint` clean (699 files); `tests/test_usage_ledger.py::TestTurnCompleteLine`
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:265` — panel (CATO-8, account-level) remains. **Gates:** `make lint` clean (699 files); `npm run
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:270` — - **CATO-8 DONE — the account-level Usage panel (S2c). PLAN COMPLETE.** New `web/src/pages/settings/UsagePanel.tsx`

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.3`, `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:144` — | T2.3 | Settings → Usage panel: period segmented control (Today/7d/30d, URL-backed), by-model table, by-source table, cache-savings line, "spent X of Y cap" context; wire the orphaned `SystemAgentStats.input_tokens` rather than adding a parallel type | `web/src/pages/settings/UsagePanel.tsx` (+ …
- `T2.4` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:145` — | T2.4 | Honest-zero + partial-pricing test sweep: a period containing BOTH priced and unpriced models must render the total with a visible "partial — N unpriced models" marker, never a confidently-complete number | tests + the panel | a mixed period is visibly marked partial |
- `Session 2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:138` — ### Session 2 — The surfaces

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
