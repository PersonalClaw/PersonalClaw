<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-3` — CI consistency rails / ratchets: primitive-adoption + inline-font-weight caps + token-lint-strict (C1/T3.4)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #204 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

C1 — The consistency rails (CI-enforced); T3.4 ratchet; Execution log cy6/cy11

## Done when

web/src/design/primitiveAdoption.test.ts + baseline.json ratchet rawButton/rawInput/rawDialog/inlineFontWeight (may only decrease) live in the CI web vitest step alongside tokenLint-strict; a synthetic +1 raw element turns CI red and reverts clean; count-based, no browser needed. (The third C1 rail — axe-per-route — is the deferred DSC-11 tail.)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-1`](DSC-1.md) | intra-repo | `done` |

### Unblocks (4)

[`DSC-11`](DSC-11.md) (`done`), [`DSC-4`](DSC-4.md) (`done`), [`DSC-6`](DSC-6.md) (`done`), [`DSC-8`](DSC-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:731` — **The number: interaction degrades at 500 rows** — the first N where the MEDIAN frame during a sustained scroll exceeds one 16.7 ms frame, i.e. where scrolling stops holding 60 Hz rather than merely dropping the odd frame. First measurable frame loss is at 250 (worst frame exactly 16.7 ms); it co…

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `T3.4`, `cy6`, `cy11`. Those tokens appear in its source plan at:

- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:17` — cost) — and `SH4.3` explicitly mounts this plan's `T3.2`/`T3.4` axe-per-route a11y rail on it. So
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:18` — T3.2/T3.4/V3 are **DEFERRED with a named owner**, not open-ended.
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:80` — | T3.4 | Ratchet: mount token-lint-strict + axe into plan 33 web/rails CI (block regressions) | `.github/workflows/ci.yml` (coordinate with plan 33) | a new hardcoded value or a11y regression turns CI red |
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:131` — ### 2026-07-22 — S2→S3 cycle 6 (T3.4 ratchet: primitive-adoption CI guard)
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:132` — - **DONE (C1 third rail / T3.4):** added `web/src/design/primitiveAdoption.test.ts` + `primitiveAdoption.baseline.json` — a bespoke-chrome ratchet asserting raw `<button>` (baseline 420), raw `<input>/<textarea>/<select>` (206), and ad-hoc dialogs (0) counts may only DECREASE. A NEW raw element t…
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:160` — - **DONE (C1/T3.4 — 4th rail):** added `countInlineFontWeights()` to the reporter and an `inlineFontWeight` cap (baseline 168) to `primitiveAdoption.test.ts` + baseline.json. The inline `font-variation-settings "wght"` count may only shrink — a new inline weight turns the CI web job red, locking …
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:630` — - **NEXT:** S3 interaction/responsive parity tail (T3.3) and the remaining `rawInput`/`rawButton` drawdown where a byte-identical idiom exists. Environment-blocked tail stays honestly undone: the **authenticated axe-per-route CI gate** (T3.2/T3.4) and the **V3 full-app walkthrough** need a seeded…
- `cy6` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:540` — ### 2026-07-24 — cy6 DONE (G2 tonal / primary-fill convergence — the two faithful moves the ruling's geometry/semantics guardrail actually authorizes — batch cycle 6/9)
- `cy6` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:560` — - **Ratchet: `rawButton` 314 → 290 (−24), committed same cycle.** Five buttons removed this cycle (audit per-file: ChatPage 18→16, ProjectsSection 18→17, CodeSection 3→2, CodePlanReview 9→8) drop live to **290**; the `primitiveAdoption` soft-nudge ("live counts well below baseline — ratchet DOWN …
- `cy6` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:569` — - **DONE (`CodePlanReview.tsx` — the authorized layout rewrite):** `PlanCapabilities` rewritten from the `CapChip` flex-wrap chip row to a **`CapRow` vertical list** matching `LoopPlanReview`'s picker: container `flex flex-wrap gap-1.5` → `flex flex-col gap-1.5`; each `<CapChip label icon on sugg…
- `cy6` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy6` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:672` — - **Docs:** `patterns.md` — replaced the line-163 "should adopt … as they're touched" TODO-note with a **cy17 convergence record** (both sites now on `EmptyState`, markup locked by the test) + an explicit "remaining centered blocks are load-error / not-found / filtered / slot — don't force them o…
- `cy11` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:600` — ### 2026-07-24 — cy11 DONE (S2 raw-input migration wave 2 — the submit-a-query idiom converges onto `TextInput`)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
