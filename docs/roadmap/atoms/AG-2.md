<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AG-2` — Budgets + outbound scan + GuardrailsConfig (§1.1, §2.2)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AUTONOMY-GUARDRAILS](../plans/AUTONOMY-GUARDRAILS.md) (`AG`, plan status `done`) |
| **Atom table** | [`../atomic/AG.md`](../atomic/AG.md) |
| **Execution position** | #22 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1.1 SpendMeter + spend.json, budget checks at due-collection / gateway dispatch / mid-run chokepoint / subagent spawn, pause-into-needs-input (extends _maybe_autopause); §2.2 PII/secret scan WARN/REDACT/BLOCK; §6/§7 GuardrailsConfig (BudgetConfig+BreakerConfig+scan_mode) through the four wiring points (Session 2)

## Done when

a per-minute trigger hitting its per-day token/dollar ceiling pauses into needs-input and auto-resumes next day; a secret-shaped payload is blocked at scan (secret_leak, non-retryable); config round-trips — DONE per log 2026-07-25 (7838 passed)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AG-1`](AG-1.md) | intra-repo | `done` |

### Unblocks (3)

[`AG-10`](AG-10.md) (`done`), [`AG-3`](AG-3.md) (`done`), [`AG-4`](AG-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (24 line(s))

This atom's scope cites `§1.1`, `§2.2`, `§6`, `§7`, `Session 2`. Those tokens appear in its source plan at:

- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:13` — chokepoint, §1.1 budgets, §2.2 scan, §1.2 denylist, §1.3 incident kill switch, §1.4
- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:128` — scan (PII/secret, §2.2) → circuit-breaker check (§2.3) → meter (§1.1) →
- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:203` — `SubagentManager.spawn` gains `capability_class: research | mutating` (default `research` for auto-fired spawns). `research` = default-deny on write/execute tools — a declared class, enforced by the tool-approval layer, not by prompt. Existing caps (`max_subagents`, `subagent_max_turns`, the invo…
- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:260` — - **Session 2 — money and meters (§1.1, §2.2):** SpendMeter + `spend.json`; budget checks at due-collection / gateway dispatch / mid-run / subagent spawn; pause-into-needs-input (extending `_maybe_autopause`); PII/secret scan wrapper with WARN/REDACT/BLOCK; `GuardrailsConfig` through all four wir…
- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:413` — ### 2026-07-25 — Session 2 (§1.1 budgets + §2.2 scan + config substrate) — DONE
- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:444` — call (refuses with `BudgetExceededError` when the ceiling is hit — the mid-run §1.1
- `§1.1` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:471` — AUTOMATION-SUBSTRATE.** §1.1 lists a `run` scope and notes per-trigger budgets "become
- `§2.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:13` — chokepoint, §1.1 budgets, §2.2 scan, §1.2 denylist, §1.3 incident kill switch, §1.4
- `§2.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:44` — - **NEW-2 amendments5** (composable secret/PII scan wrapper, WARN/REDACT/BLOCK, at the model-call seam) → §2.2.
- `§2.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:128` — scan (PII/secret, §2.2) → circuit-breaker check (§2.3) → meter (§1.1) →
- `§2.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:260` — - **Session 2 — money and meters (§1.1, §2.2):** SpendMeter + `spend.json`; budget checks at due-collection / gateway dispatch / mid-run / subagent spawn; pause-into-needs-input (extending `_maybe_autopause`); PII/secret scan wrapper with WARN/REDACT/BLOCK; `GuardrailsConfig` through all four wir…
- `§2.2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:413` — ### 2026-07-25 — Session 2 (§1.1 budgets + §2.2 scan + config substrate) — DONE
- `§6` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:40` — - **NEW-1** (Autonomy Guardrail Substrate: run/day/trigger token+dollar+wall-clock ceilings, path/action denylist, incident kill switch, DISABLE_LIVE_WRITES, graduated safety profiles) → §1, §3, §4, data model §6, Slices 2-4.
- `§6` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:330` — In-process state is deliberate for a single-user gateway (§6: "restart resets").
- `§6` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:763` — ### 2026-08-12 — Atom AG-8 / Session 6.1 (§6.1 promotion proposals + the ladder's user surface) — DONE
- `§6` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:768` — handle**. This atom closes all three and is the LAST atom of the rung-ladder track (§5-§6).
- `§7` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:232` — | Guardrails config | `config.json` → `guardrails` section | `GuardrailsConfig` dataclass | Four wiring points (§7) |
- `§7` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:278` — | Silent config drop (the four-wiring-points gotcha) | Explicit checklist in §7; schema reachability tests already enforce (a) and guard-default test added in Session 3 |
- `§7` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:426` — (`warn`|`redact`|`block`). Wired through all FOUR points (§7): dataclass + `_meta`,
- `§7` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1400` — - **Asymmetric by design:** promotion is ALWAYS a user click — eligibility files a *proposal*, never auto-promotes. Demotion is automatic + immediate on ANY rejection, undo, or 👎 for that type, with a cooldown before re-eligibility. Thresholds per-type configurable via a new `guardrails.autonomy`…
- `Session 2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:260` — - **Session 2 — money and meters (§1.1, §2.2):** SpendMeter + `spend.json`; budget checks at due-collection / gateway dispatch / mid-run / subagent spawn; pause-into-needs-input (extending `_maybe_autopause`); PII/secret scan wrapper with WARN/REDACT/BLOCK; `GuardrailsConfig` through all four wir…
- `Session 2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:413` — ### 2026-07-25 — Session 2 (§1.1 budgets + §2.2 scan + config substrate) — DONE
- `Session 2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:476` — amendment forbids. So Session 2 enforces the **day** scope (the real cost guardrail) at
- `Session 2` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1361` — Session 1 (model-call chokepoint), Session 2 (budgets + scan + config), Session 3 (denylist

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
