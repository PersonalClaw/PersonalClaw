<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-8` — A2A gateway (inbound agent card + tasks→WorkflowRun) + a2a-call outbound provider

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #221 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2170 |

## Intent — what this is for

§5 (Dialect 4 A2A): inbound GET /a2a/agent-card + POST /a2a/tasks, a2a_published template flag, apps/a2a-action outbound provider + ALLOWED_HOOK_PROVIDERS. §11 provider-fidelity wiring. Session 6 (A2A half).

## Done when

GET /a2a/agent-card serves a card whose skills are workflow templates with a2a_published:true (default false, per-template toggle in template detail UI); POST /a2a/tasks maps an A2A task onto a WorkflowRun (v2 run-start seam), streams A2A task lifecycle, returns fenced artifacts under headless profile + client budget; card mounts EMPTY until WF2 slices land; apps/a2a-action first-party app (provider type action, entity a2a, ActionProvider factory, webhook-action precedent) whose name is added to ALLOWED_HOOK_PROVIDERS (src/personalclaw/validation.py) or hook create/update rejects it; execute sends one A2A task via net.fetch under CONNECTOR policy + egress_policy_for deny-by-default host allowlist; selectable by all trigger kinds + workflow action nodes once registered (Success Criterion 10).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EA-1`](EA-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 40 (EA) — ARCC's SSRF guidance found a real hole; fixed in PR #2790

**Code evidence:**

- 🔑 PUBLICATION IS OPT-IN AND THE CHECK IS EXACT: a2a.py:327/:372 admits a template as a skill only when metadata.a2a_published 'is exactly True' — `is not True` rejects a truthy string, so a config typo cannot publish a workflow to an external agent network
- MEASURED: the routes mount unconditionally and refuse per request (server.py:483-491, 'three literal paths under /a2a'), with the mount failure logged rather than swallowed
- the clause's own honesty holds — 'card mounts EMPTY until WF2 slices land' — which is why an empty card here is the specified behaviour rather than a gap
- test_inbound_a2a.py green inside the 333
- 333 across the EA seam/dialect/bridge/capture/replay/a2a suites; +132 capture incl. the new redirect suite; +143 cli_run + inbound_mcp; +28 channel-inbound chokepoint

**Notes:** Default-false plus an identity check rather than a truthiness check is the same absent-versus-declared-false discipline this campaign keeps meeting; here the cost of getting it wrong is publishing a workflow to strangers, which is the highest-stakes instance of it so far.

## Recorded history

### Commits (8)

- `a1bcaa41e` 2026-09-02 — docs(roadmap): EA-8 done — both halves verified on main (core #2170, Apps#56)
- `7c094b9df` 2026-09-01 — feat(channels): EA-7 make the sender-trust check unavoidable
- `a30502fa0` 2026-08-27 — feat(external-access): EA-8 accept a2a-call and export its egress policy to the app SDK
- `9fdefe971` 2026-08-28 — feat(actions): net-fetch, a deny-by-default HTTP-egress action provider
- `1bce47fde` 2026-08-26 — docs(roadmap): EA-2 done (PR #2086); EA-8 stays todo with its cross-repo clause
- `4e41be3ba` 2026-08-26 — docs(reference): regenerate the offline reference for the /v1 and /a2a routes
- `1e994498f` 2026-08-25 — feat(inbound): EA-8 A2A agent card + tasks onto a WorkflowRun
- `ecd3f06e8` 2026-08-20 — docs(roadmap): WF2KNO-9 BLOCKED — the egress chokepoint has no atom

### Named by atom id in the roadmap prose (11)

- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:581` — validates-then-fails-at-fire-time shape `EA-8`'s split-across-repos hazard describes, which this very
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:582` — session hit on `EA-8`. Order: (1) core ships `deliver_channel_inbound` — this change; (2) the four
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1213` — - [2026-08-26][EA-2] **Atom flipped `done` (PR #2086).** Landed through a combined batch with EA-8.
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1222` — ## Execution log — `EA-8` (§5 Dialect 4: the A2A agent card + tasks)
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1224` — - [2026-08-25][EA-8] **PARTIAL — the inbound dialect is DONE end to end; the outbound `a2a-call` half is
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1372` — - [2026-08-27][EA-8] **The outbound half is BUILT; the atom is 🟡 pending TWO merges, not one.** Closes the
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1445` — - [2026-09-02][EA-8] **DONE — both merges are on `main`; the atom flips in this commit.** The
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1035` — atoms for a clause naming a dispatchable net/HTTP-egress provider returns only `EA-8` ("A2A gateway
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1095` — `apps/webhook-action`. No atom owned it — the only atom naming an egress provider is `EA-8`, which
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1126` — hits across `src`, `tests` and `docs`), and there is no `EA-8` comment block in that file — `EA-8`
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1132` — provider, not `EA-8`.** None of the 20 registered providers is a network fetch. `EA-8` is the **inbound** A2A

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `§5`, `§11`, `Session 6`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:28` — - **NEW-10 amendment 2** (A2A protocol gateway as a third inbound dialect: expose workflows as A2A-callable agents; let workflow nodes delegate to external A2A agents — same fail-closed bearer/rate-cap/framing discipline) → §5. Source: `agent-zero` (FastA2A both-directions integration).
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:52` — New module `src/personalclaw/inbound/` mounted by the `dashboard/server.py` app factory. All five dialects (§2-§5, §7) register sub-routes on it and inherit the full discipline below; none of them may add a route outside it.
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:219` — - **`a2a-call` action provider** (§5): first-party app `apps/a2a-action` (`type: "action"`, factory returns `ActionProvider`, webhook-action precedent) — **added to `ALLOWED_HOOK_PROVIDERS` (src/personalclaw/validation.py)**, or hook create/update rejects it. Ships a `a2a-action` extension manife…
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:254` — - **Session 6 — A2A + sender trust + hardening sweep (§5, §6):** agent card + tasks→WorkflowRun (if v2 slices landed; else card-empty mount + outbound only), `apps/a2a-action` + `ALLOWED_HOOK_PROVIDERS` entry, `channel_transports/trust.py` + pairing codes + Slack migration, adversarial as-a-user …
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1222` — ## Execution log — `EA-8` (§5 Dialect 4: the A2A agent card + tasks)
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1242` — **The WF2 gate is MOOT, not skipped.** §5 says the card "mounts empty until WF2 Slices 0-3"; those landed
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1252` — 🔴 **DESIGN FINDING — §5's stated egress composition is PERMISSIVE, and the clause contradicts it.** The
- `§5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1260` — is shown to allow. **§5's sentence should be corrected — owner call.**
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:203` — | External-access config | `config.json` → `external_access` section | `ExternalAccessConfig` dataclass | four wiring points (§11) |
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:273` — | Silent config drop (four-wiring-points gotcha) | Explicit checklist in §11; schema reachability tests enforce `_meta`; tokens deliberately outside config.json entirely |
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:312` — ## Execution log — EA-1 (§1 shared inbound access seam + §10 stores + §11 config wiring, Session 1)
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:337` — consult `_EDITABLE_CONFIG` at all. That is §11's stated design (its alternative is "a deliberate
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1404` — **Citation correction.** `validation.py:555` is cited in §5, §11 and the `done_when` row.
- `Session 6` `docs/roadmap/plans/EXTERNAL-ACCESS.md:254` — - **Session 6 — A2A + sender trust + hardening sweep (§5, §6):** agent card + tasks→WorkflowRun (if v2 slices landed; else card-empty mount + outbound only), `apps/a2a-action` + `ALLOWED_HOOK_PROVIDERS` entry, `channel_transports/trust.py` + pairing codes + Slack migration, adversarial as-a-user …

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
