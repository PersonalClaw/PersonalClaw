<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WS-5` — dir-source: signature-diff observer, debounce, archive-on-delete

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WATCHED-SOURCES](../plans/WATCHED-SOURCES.md) (`WS`, plan status `done`) |
| **Atom table** | [`../atomic/WS.md`](../atomic/WS.md) |
| **Execution position** | #665 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1369 |

## Intent — what this is for

§4 Watched Local Directories (am.5): dir-source spec + save-time validate_file_path/sensitive-path/path-cap; dependency-free mtime+size signature-diff poll (not watchdog); debounced incremental re-index; §11 step 3. Reconcile against shipped triggers/file_poll.py; fs_watch.py stays untouched

## Done when

Editing three files in a watched dir within the debounce window re-indexes each exactly once (create->new item, modify->re-enqueue existing item); deleting one archives its item with metadata source_deleted_at and never hard-deletes (SC#5); first pass seeds only (no startup ingestion storm)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WS-2`](WS-2.md) | intra-repo | `done` |

### Unblocks (1)

[`WS-9`](WS-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `ff6f78f67` 2026-08-15 — feat(knowledge): WS-9 Sources UI with paste-URL preview and health
- `8c4f14189` 2026-08-15 — feat(sources): WS-4 cross-source dedup with both attributions
- `e4b92c7da` 2026-08-15 — feat(sources): WS-5 watched-dir debounce + archive on delete

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:589` — WS-5's rule for a watched directory is archive-never-delete, because that file may be back tomorrow
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:529` — create a watched source of ANY kind, so `WS-2`, `WS-4` and `WS-5` are all complete and the feature as a
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:536` — callers**, so WS-2's store, WS-3's five-detector web kind, WS-4's feeds and WS-5's directory
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:553` — re-validation WS-3/WS-5 already do; there is no second copy of the rules in the handler.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:693` — create/modify/delete propagation past the first seeding pass (WS-5 seeds only on pass one and its
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1005` — **CHANGELOG: yes** — and this is the first WS atom that earns one. `WS-2`/`WS-3`/`WS-4`/`WS-5` all

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§4`, `§11`. Those tokens appear in its source plan at:

- `§4` `docs/roadmap/plans/WATCHED-SOURCES.md:19` — seen-set, daily request budget and `net.fetch` routing, driven from the gateway's poll loops. So §2/§3/§4
- `§4` `docs/roadmap/plans/WATCHED-SOURCES.md:31` — - **NEW-5 am.5** — local filesystem directories as a watched-feed kind (debounced incremental re-index on create/modify/delete) → §4
- `§4` `docs/roadmap/plans/WATCHED-SOURCES.md:54` — - **`fs_watch.py` is UI-refresh only.** The config-tree watcher (dependency-free 3s poll, mtime+size signature) publishes SSE to the frontend and "is not a trigger kind." Watched local dirs (§4) get their own observer following the same dependency-free poll-signature pattern — `fs_watch` itself i…
- `§4` `docs/roadmap/plans/WATCHED-SOURCES.md:87` — | Watched local dir | `dir-source` (§4) | core-native |
- `§4` `docs/roadmap/plans/WATCHED-SOURCES.md:165` — Each source's `source_cursors` row holds provider-defined opaque state: `{last_seen_guid, last_published_at, etag, last_modified}` for feeds (conditional GET via ETag/Last-Modified keeps polls nearly free); `{since_ts}` for HN Algolia; `{mtime_signatures}` for dirs (§4). Cursor writes are atomic …
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:321` — **~5 sessions**, mapping 1:1 onto §11:
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
