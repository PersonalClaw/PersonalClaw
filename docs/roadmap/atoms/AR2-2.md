<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AR2-2` — agents/routing.py pure classifier: eligible_candidates + classify (keyword 0.7 → embedding 0.62 + 0.1 margin) + embedding cache with staleness check

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-ROUTING](../plans/AGENT-ROUTING.md) (`AR2`, plan status `done`) |
| **Atom table** | [`../atomic/AR2.md`](../atomic/AR2.md) |
| **Execution position** | #63 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #feature-agent-routing |

## Intent — what this is for

Design S1 (the classifier); Contracts C2; Task breakdown Session 1 T1.2

## Done when

pure-function tests pass: keyword hit, embedding hit, low-confidence None, no-embedder falls back to keyword-only, reserved agents never candidates; classifier never raises

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AR2-1`](AR2-1.md) | intra-repo | `done` |

### Unblocks (2)

[`AR2-3`](AR2-3.md) (`done`), [`AR2-4`](AR2-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/AGENT-ROUTING.md:123` — | T1.2 | `agents/routing.py`: `eligible_candidates`, `classify` (keyword 0.7 → embedding 0.62 + 0.1 margin, `None` when no embed fn), embedding cache on `DashboardState` with `embedding_model` staleness check | `src/personalclaw/agents/routing.py` (new) | pure-function tests: keyword hit, embeddi…
- `Session 1` `docs/roadmap/plans/AGENT-ROUTING.md:118` — ### Session 1 — Metadata + classifier + emission (backend)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
