<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-2` — CDP browser integration + per-page safety-script injection + BROWSE egress policy + redirect re-eval

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #80 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§3 Per-Page Safety Script Injection; §4 Stealth Stack; §6 Egress Chokepoint Integration (6.1 BROWSE policy, 6.2 redirect re-eval, 6.3 headless bypass gap); Session 2

## Done when

Every CDP navigation is pre-flighted through net/guard.py:evaluate against a new BROWSE profile in net/policy.py; a denied host is blocked before Page.navigate fires and the block is recorded in the SEL; the injected safety script makes a test page's fetch()/media.play()/navigator.bluetooth throw or return blocked; client-side redirects re-evaluated per Page.frameNavigated — SC 4, SC 5

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`BA-1`](BA-1.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:net/guard egress chokepoint + HEADLESS safety profile` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`BA-3`](BA-3.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 14 (BA) — DRIVEN in a real browser

**Code evidence:**

- src/personalclaw/net/policy.py:234 BROWSE = EgressPolicy(...) registered at :296; browse/cdp.py pre-flights every navigation through net/guard.py:evaluate
- net/guard.py:74-102 covers the full forbidden set — loopback, RFC-1918, link-local INCLUDING 169.254.0.0/16 (IMDS), ULA fc00::/7, multicast, reserved, unspecified — and :86-88 unwraps IPv4-mapped IPv6 because 'a private v4 hidden in a v6 literal' is the bypass v4-only guards miss
- guard.py:57-65 pinned_ips are 'the already-resolved, validated IPs the client must dial — no second resolution (that is the rebind window)'
- browse/cdp.py:20-22 re-judges a client-side redirect on Page.frameNavigated and TEARS DOWN a denied one; :80 keeps preflight and redirect denials as distinguishable SEL rows
- MEASURED: tests/test_browse_safety_script.py + test_browse_cdp_live.py + test_browse_behavioural_proof_is_reachable.py = 53 passed, ZERO skipped on this machine — a real Chromium opened and the injected script's fetch()/media/bluetooth blocks were proven behaviourally, not structurally

**Notes:** This atom answers ARCC's SSRF Mitigation guidance point for point: deny private/local/metadata (169.254.169.254 named explicitly in both), DNS-rebinding protection by pinning the resolved IPs rather than re-resolving, redirects re-validated rather than followed blindly, and every denial logged to the SEL. It also carries the campaign's best example of a false-green being closed: tests/test_browse_live_legs_run_in_ci.py exists because the behavioural layer used to SKIP on the merge machine ('Measured on a browser-less tree: 20 skipped'), and 'a skipped surface reads exactly like a pass, so the green check said the safety script blocks fetch() while having never opened a browser'. The rail is over the CI WIRING and is derived rather than pinned by job name. I verified the same legs run here: 53 passed, 0 skipped.

## Recorded history

### Commits (6)

- `242a8b253` 2026-09-02 — feat(browse): persisted idempotent scheduled-actuator plans (BA-6)
- `8346a7940` 2026-08-27 — test(browse): BA-2's live legs run on the merge gate instead of skipping
- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `2e394588f` 2026-08-25 — test(browse): BA-2 make the behavioural proof reachable instead of silently skipped
- `3984eb0ed` 2026-08-24 — feat(browse): BA-2 real CDP transport + live-browser proof of the egress gate
- `3961807a0` 2026-08-23 — feat(browse): BA-2 gate every CDP navigation and guard every page in it

### Named by atom id in the roadmap prose (17)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:448` — - **2026-08-23 — `BA-2` COMPLETE (all four clauses, proven against a real browser). Atom stays `todo`
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:517` — - **2026-08-24 — `BA-2` re-driven against a REAL browser. The 2026-08-23 code is all merged**
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:581` — whose `done_when` neither mentions it. BA-2's own `done_when` does not require it and is met.
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:583` — a property of the seam, not yet of a running loop. Worth stating plainly so nobody reads BA-2 as
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:603` — - [2026-08-24][BA-2] **Integration re-verification.** Rebased onto `origin/main` (`9e0f727b`), full gate
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:610` — `done_when` clauses are met and proven live, but BA-2's declared *scope* (§4 Stealth Stack + §10 browser
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:612` — so whether BA-2 flips on `done_when` or waits for the launcher to move to BA-4/a new atom is an **owner
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:615` — - [2026-08-25][BA-2] **DONE (closure pass) — the gap was that the behavioural proof was silently
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:738` — first-party app. BA-1 and BA-2 already landed `browse/` in core, and an app bundle may only import
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:739` — `personalclaw.sdk.*` — which does not export `GatedCdpSession`, the thing BA-2 built for this atom
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:782` — installs no browser (the same limitation BA-2's entry records). Colliding element identities resolve
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:892` — (the same limitation `BA-2`'s and `BA-3`'s entries record, and `BA-3`'s ruling Q2 already filed
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:931` — **Note on this plan's shape:** it has **no `## Execution log` heading**. `BA-2`, `BA-3` and `BA-7`
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1051` — real-browser gap is already recorded by `BA-2` and `BA-3`.
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1061` — - [2026-08-27][BA-2] **The live legs now execute on the MERGE GATE — the false green the closure pass
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1062` — left to the owner is closed.** `BA-2`'s 2026-08-25 entry ended with the gap named and deferred:
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1148` — (flip `BA-2` on `done_when`, or wait for §4 Stealth Stack + §10 launcher) is unchanged.

### Its plan's own record, via the scope references (13 line(s))

This atom's scope cites `§3`, `§4`, `§6`, `Session 2`. Those tokens appear in its source plan at:

- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:525` — one path writes) — only the summary was short. Clause status now: **1, 2, 3, 4, 5 met; §4.2 of the
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:571` — **NOT DONE, and deliberately NOT improvised: §4.2's anti-detection baseline and browser lifecycle.**
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:573` — connection)". There is no launcher in `src/`, so §4.2's `--disable-blink-features=AutomationControlled`,
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:575` — unimplemented. This is an **owner scope question, not an oversight**: §4.1 says the provider "shares the
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:579` — owns no process; the live test launches its own. **Recommendation:** give the launcher + §4.2 a named
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:610` — `done_when` clauses are met and proven live, but BA-2's declared *scope* (§4 Stealth Stack + §10 browser
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:677` — `done_when`, or wait for §4 Stealth Stack + §10 launcher) is unchanged and still the owner's; this pass
- `§4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:788` — **Q1: flip on `done_when`, or wait for the §4 Stealth Stack and §10 launcher before calling browse
- `§6` `docs/roadmap/plans/BROWSE-AUTOMATION.md:430` — - **(e) Egress still applies.** Every navigation the extension performs on our instruction passes the existing guard (§6.1 `net/guard.py::evaluate`) before it is issued. Note the honest limit already documented in code: a real browser does its own DNS and connections, so it **bypasses `net.fetch`…
- `§6` `docs/roadmap/plans/BROWSE-AUTOMATION.md:489` — **§6.3 headless bypass gap — enumerated in the module, not left implicit.** The pre-flight covers the
- `§6` `docs/roadmap/plans/BROWSE-AUTOMATION.md:564` — prevention. The guard's reach here is the DOM, not the socket — §6.3, restated as a test.
- `§6` `docs/roadmap/plans/BROWSE-AUTOMATION.md:666` — **§6.3 headless bypass gap — still OPEN, deliberately, and correctly scoped.** Not a regression and not
- `Session 2` `docs/roadmap/plans/BROWSE-AUTOMATION.md:320` — - **Session 2 — CDP integration + safety injection + egress wiring:** browser lifecycle management (launch with persistent profile, CDP connection); safety script injection via `Page.addScriptToEvaluateOnNewDocument`; navigation with egress pre-flight (`guard.evaluate`); redirect re-evaluation; s…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
