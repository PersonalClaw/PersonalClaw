<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DL-4` — T3.1: build the demo-home seed fixture (core)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISCOVERABILITY-LAUNCH](../plans/DISCOVERABILITY-LAUNCH.md) (`DL`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DL.md`](../atomic/DL.md) |
| **Execution position** | #194 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1602 |

## Intent — what this is for

Session 3 — Launch assets (T3.1)

## Done when

src/personalclaw/tests_fixtures/demo-home/ exists following the empty-fixture layout with believable non-personal data (project, tasks, knowledge docs, memory entries, one loop); `personalclaw gateway --seed demo-home` boots a demo-ready dashboard

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`DL-5`](DL-5.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 58 (DL) — the demo seed booted on a fresh home and driven; the capability matrix read row by row

**Code evidence:**

- 🔑 I BOOTED IT RATHER THAN READ IT: a fresh gateway on an isolated throwaway home with `--seed demo-home --seed-replace` came up HTTP 200 and rendered a demo-ready dashboard, driven in a browser at a desktop viewport
- 🔑 THE SEEDED STATE IS BELIEVABLE AND NON-PERSONAL, observed live: the dashboard greets 'Good morning, Alex' — an invented persona — with '6 tasks ready', one unread notification, and 5 knowledge items (the gateway's own re-index warning names the count)
- 🔑 A PII SWEEP OVER THE FIXTURE FINDS NOTHING: no occurrence of the owner's name, handle or any of their email domains anywhere under the fixture directory. The atom's 'non-personal' clause is the one that would embarrass the project if wrong, and it holds
- the fixture follows the empty-fixture layout the atom requires — `config.json`, `fixture.yaml`, `projects`, `tasks`, `loop`, `workspace` — so it is a peer of the shipped `empty` fixture rather than a special case
- seed suites pass 57/57
- seed suites 57/57; a fresh gateway booted with --seed demo-home on an isolated home and driven in a browser; capabilities matrix 20 rows (9 yes / 7 no / 4 partial); llms.txt 80 lines + llms-full.txt 5417

**Driven in the UI:** 🔑 THE WHOLE ATOM, END TO END: seeded a fresh home, booted the gateway on it, and drove the resulting dashboard. This is the atom in this plan that was most fully verifiable and it verified.

**Notes:** 🪤 A FALSE ATTRIBUTION I CAUGHT BEFORE IT BECAME EVIDENCE. My first boot targeted port 10012, got HTTP 200, and looked like a success — but the log showed 'Port 10012 already in use', so the 200 came from a PRE-EXISTING gateway from an earlier cycle and NOT from my seeded home. Re-ran on a free port. The seed had in fact loaded before the bind failed, which is exactly the kind of partial success that reads as a clean pass if you only check the status code.

## Recorded history

### Commits (4)

- `995e2ef80` 2026-09-02 — docs(roadmap): flip already-built Wave-GA atoms to done + safe reconciliations
- `aea611673` 2026-08-17 — chore(roadmap): cite the landing batch #1602 on DL-4
- `ddde1fd7c` 2026-08-17 — feat(fixtures): DL-4 demo-home seed fixture boots a demo-ready dashboard
- `4b931cc43` 2026-08-16 — feat(seed): DL-4 demo-home fixture + recursive fixture package data

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:140` — ### 2026-08-16 — `DL-4` (T3.1) demo-home seed fixture — **PARTIAL / BLOCKED**
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:182` — `CAPTURE.md` now says. `DL-4` stays `todo` in `dag.json`/`DL.md` because the done_when is not met.
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:206` — ### 2026-08-17 — `DL-4` (T3.1) demo-home seed fixture — **DONE**
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:364` — `src/data/` (three modules today) and `src/pages/`. Compare `DL-4`, the one atom in this

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T3.1`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:24` — `CAPTURE.md`) and the README/SHOWCASE rework landed. Still open: **T3.1's
- `T3.1` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:78` — | T3.1 | Build the demo seed fixture: believable non-personal data (a project, tasks, a knowledge doc set, memory entries, one loop) for screenshot/GIF capture | core repo: `tests_fixtures/demo-home/` (follow existing fixture layout) | `personalclaw gateway --seed demo-home` boots a demo-ready da…
- `T3.1` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:140` — ### 2026-08-16 — `DL-4` (T3.1) demo-home seed fixture — **PARTIAL / BLOCKED**
- `T3.1` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:206` — ### 2026-08-17 — `DL-4` (T3.1) demo-home seed fixture — **DONE**
- `Session 3` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:74` — ### Session 3 — Launch assets

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
