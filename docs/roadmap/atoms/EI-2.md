<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EI-2` — `docker` provider + engine/workspace-block adoption + cgroup v2 tier

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXECUTION-ISOLATION](../plans/EXECUTION-ISOLATION.md) (`EI`, plan status `done`) |
| **Atom table** | [`../atomic/EI.md`](../atomic/EI.md) |
| **Execution position** | #224 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1.1 docker built-in + failure-honesty/downgrade; §1.3(2) code loops & workflow stage nodes (allowed_write_paths/egress_tier/safety_profile threaded into SandboxSpec); §11 Session 2; Amendment 2026-07-26 EI-A2 (cgroup v2 systemd-run transient scope + doctor probe)

## Done when

SC1: a stage `sandbox: docker` runs its process tree in a UID-aligned bind-mount container over its WORK-R3 worktree; a write outside allowed_write_paths is blocked by the boundary; a no-Docker machine parks needs-input with a typed reason (no silent host downgrade for unattended). SC2: a subagent `sandbox: docker` without a grant to ~/.personalclaw/models cannot delete a real local model. Linux fork-bomb child hits pids.max; macOS logs a one-time not-enforced warning

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EI-1`](EI-1.md) | intra-repo | `done` |
| `EXT:WORK-CONTAINERS:WORK-R3 workspace block call sites + WORK-R20 mode:container re-expression` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`EI-4`](EI-4.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 65 (EI) — the spawn-site census falsified (an unmapped spawn named to file:line); the runner provenance rows driven in a real browser

**Code evidence:**

- the argv builder composes real isolation: `--user uid:gid` ('UID alignment WITHOUT chown -R: mount writes are host-user owned'), `--pids-limit`, `--memory`, `--workdir`, and per-path bind mounts; 27 cases green
- 🔑 ONLY WHAT IT ACTUALLY ENFORCES IS CLAIMED — the egress comment is the plan's own doctrine applied to itself: 'only `off` is enforced docker-side (`none`); finer egress tiers are ADVISORY here and belong to the host egress rail (net/policy), recorded as such in SandboxSpec'. The contract's docstring repeats it, so a consumer reading the field is told which tiers a container boundary answers for
- 🔴 THIS HOST IS THE ATOM'S OWN no-Docker FIXTURE and the fallback held: the two container-boundary cases skip with 'docker CLI/daemon unavailable' while the rest pass, which is the clause 'a no-Docker machine parks needs-input with a typed reason (no silent host down…)'
- 358 passed / 3 skipped across 17 sandbox, runner, vault, checkpoint and triage suites; 16/16 on the runner-health frontend suite; the Secrets vault and Agent-defaults pages driven on :10011

**Driven in the UI:** Not driven: no Docker daemon on this host.

**Notes:** Partial on SC1's load-bearing half — 'a write outside allowed_write_paths is blocked by the boundary' needs a real container and was NOT observed. The argv that would produce that boundary was read and unit-tested; the boundary itself was not. Recorded as unobserved rather than inferred, the same treatment SH-2's macOS keychain half got.

## Recorded history

### Commits (9)

- `f063ef7fa` 2026-09-03 — docs(roadmap): flip MC-6b + EI-2 (evidence #2359, #2361)
- `f36673654` 2026-09-03 — feat(sandbox): add docker confinement sandbox provider (EI-2)
- `53a7334ea` 2026-09-03 — docs(roadmap): flip MI-2, DL-8, BA-5 to done post-#2370 + fix EI-3 placeholder
- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips
- `b1cab73b3` 2026-09-03 — docs(roadmap): fold verified dev-lane flips (WF2AUT-12, WF2AUT-14, EI-6 annotation)
- `eabfce171` 2026-08-28 — feat(secrets): EI-10 secrets vault — presence-only API + Settings surface
- `485ef7b6d` 2026-08-26 — feat(agents): EI-6 runner lifecycle leases + durable-session reattach
- `f4aa88da0` 2026-08-25 — fix(tools): adjudicate an atom only on entries it is the subject of
- `18251518f` 2026-08-09 — feat(sandbox): EI-1 SandboxProvider seam + none provider + ResourceCeilings (#933)

### Named by atom id in the roadmap prose (11)

- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:384` — (`CA-9`, `CE-9`, `DCU-3`, `DL-7`, `EI-2`, `ET-7`, `LV-7`, `PEP-3`, `PEP-11`, `PUBL-10`,
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:827` — + a cockpit surface, none of which the checkpoint store touches) and `EI-2`'s docker provider owns the
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:927` — §6.2 asks for both for the docker/lima tier. `EI-2`'s container provider does not exist (the only
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1143` — tiers are `EI-2`/`EI-4`, both `todo`. `SandboxHandle` is a 3-method contract (`argv`/`exec`/
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1177` — `copy_file_in` in `EI-2`/`EI-4`.
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1231` — onto a PTY, and the sandbox-interior shell §5.1 actually specifies belongs to `EI-2`'s isolated
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1232` — provider, which this plan's own log already records as non-existent ("`EI-2`'s container provider
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1366` — is deferred WITH the clause and lands beside `EI-2`'s docker provider.
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1414` — - **[2026-08-27] OWNER RULING — `EI-7`: DEVIATION accepted; the in-sandbox observation moves to `EI-2`'s
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1417` — atom that **owns** the sandbox — `EI-2`'s `docker` provider — not with the second-opinion handoff that merely runs
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1429` — ## Execution log — EI-2 (docker confinement sandbox provider) — validated done

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `§1.1`, `§1.3`, `§11`, `Session 2`. Those tokens appear in its source plan at:

- `§1.1` `docs/roadmap/plans/EXECUTION-ISOLATION.md:129` — - **Explicit degradation dialog:** when a consumer requests `lima` and the probe fails, interactive surfaces show a one-question dialog: *"Lima unavailable (reason) — run with path-guard-only (`none` + seatbelt) or cancel?"* Unattended surfaces park needs-input (§1.1 downgrade rule). "Path-guard-…
- `§1.1` `docs/roadmap/plans/EXECUTION-ISOLATION.md:358` — - New `sandbox_providers/ceilings.py`: `ResourceCeilings{nofile: int = 1024, max_pids: int | None, max_rss_bytes: int | None}` + `ceiling_kwargs(ceilings) -> dict` returning `{"preexec_fn": ...}` (POSIX: `resource.setrlimit(RLIMIT_NOFILE, ...)` in the child; `preexec_fn` is fork-safe here because…
- `§1.3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:277` — | WORK-R3 workspace block / WORK-R20 container mode / WORK-R8 leases / WORK-R19 secrets store | **APPROVED elsewhere — consumed, not rebuilt.** §1.3 adds `sandbox:` beside the workspace block; WORK-R20 re-expresses as `sandbox: docker` capability; §3.1(5) reuses the R8 lease convention; §8 is UX-…
- `§1.3` `docs/roadmap/plans/EXECUTION-ISOLATION.md:285` — | apps sandbox task #71 | **un-deferred** — becomes §1.3(4), consuming the same substrate |
- `Session 2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:362` — **Lands in:** Session 1 (ceilings module + `SandboxSpec.ceilings` + `none` provider + the native-bash/action-provider/subagent seams + the audit test) and Session 2 (docker/lima mapping + cgroup tier); remaining seams (apps backend, MCP, ACP, cron script, loop) adopt in Session 3. Count 7 → **8 s…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
