<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PEP-19` — Ops app (suite 8/9)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PRODUCT-EXPERIENCE-PARITY](../plans/PRODUCT-EXPERIENCE-PARITY.md) (`PEP`, plan status `done`) |
| **Atom table** | [`../atomic/PEP.md`](../atomic/PEP.md) |
| **Execution position** | #436 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #apps#83 + core #2605 |

## Intent — what this is for

On-call first responder: watch alarms/pages, claim, investigate, propose fixes. Largest and most heavily gated: every fix is confirm-gated and rides AUTONOMY-GUARDRAILS (shipped). Build last among 8/9.

## Done when

The ops app ships as its own validated PersonalClawApps PR built to the app-creation contract (app.json, SDK-only imports, minimum permissions, test_provider.py/test_server.py, README, LICENSE), validated by adding it as a local Store source and driving it in the real UI; listed in the Store and recorded in ECOSYSTEM-TOOLING's exemplar list; a simulated alarm is claimed, investigated, and a fix PROPOSED behind an explicit confirm gate — no ungated mutation path exists.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `82580fc88` 2026-09-07 — docs(roadmap): flip PEP-18 and PEP-19 — the UI validation they were blocked on is driven
- `260aa77eb` 2026-09-06 — docs(roadmap): flip PEP-16 — the reinstall clause is measured true, not argued
- `daa3b999b` 2026-09-06 — docs(roadmap): flip SH-12 and DCU-4; stop the frontier claiming owner work is startable
- `5e3c8b680` 2026-09-06 — docs(roadmap): flip PEP-12/13/17/20 done; record why the other five suite atoms are not

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:698` — - **2026-09-06 — `ops` (`PEP-19`, suite 8/9) — a `tool` exemplar for a GATED MUTATION PATH.**
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:994` — uninstall/reinstall, which Deactivate/Activate does not substitute for; and `PEP-18` and `PEP-19` **cannot

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
