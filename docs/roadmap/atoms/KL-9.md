<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-9` — Indexing H1.1+H1.2: chunks table + structural chunker + chunk embedding in ingest (retire 1000-char top-up)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #310 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment task table — H1.1 (chunks table + chunker) and H1.2 (embed chunks in ingest, retire compose_item_text top-up); Amendment Design (a); Owner task 2 (confirm clean break)

## Done when

a chunks table (id,item_id,chunk_index,text,embedding,section,line_start,line_end) and knowledge/chunking.py exist; long markdown/PDF/pptx items chunk on real structural boundaries (headings/slides/sheets) with size fallback + overlap; structureless items chunk by size; chunk section/line_start/line_end are populated; the item row keeps its whole-item embedding; ingest embeds chunks and the compose_item_text 1000-char top-up is deleted (clean break, no dual path); a test retrieves content deep in a long document that fails before the change

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`KL-10`](KL-10.md) (`done`), [`KL-12`](KL-12.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 5 (KL) — observed

**Code evidence:**

- tests/test_knowledge_chunking.py passes (part of the 234-passed run)

**Notes:** PARTIAL: I deliberately wrote the probe document WITH STRUCTURE (an H1, a paragraph, an H2, a second section) so a structural chunker would have shape to work with — but I did not inspect the chunks table, so the chunking itself is observed only through its test.

## Recorded history

### Commits (2)

- `3b69c4e9f` 2026-08-13 — feat(knowledge): KL-10 search chunk vectors and roll them up to items before RRF
- `767700877` 2026-08-09 — feat(knowledge): KL-9 chunks table + structural chunker + chunk embeddings (#931)

### Named by atom id in the roadmap prose (10)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:26` — and superseded by `KL-9`/`KL-10`/`KL-11`/`KL-12`.) Created 2026-07-18 (roadmap rev 10; owner ask: more
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:523` — - [2026-08-09][KL-9] DONE: chunks table + structural chunker + chunk embeddings (H1.1+H1.2). New `knowledge/chunking.py` (`chunk_text()` + `Chunk`); a `chunks` table `(id,item_id,chunk_index,text,embedding,section,line_start,line_end)` in `store._init_schema()` (idempotent CREATE, FK ON DELETE CA…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:524` — - [2026-08-13][KL-10] DONE: the vector arm now searches CHUNK vectors and rolls chunk hits up to their item before RRF (H1.3). `_vector_search` keeps its exact `[(item_id, rank)] | None` return so `_rrf_fuse` and its `k` are untouched (§Risks: do not redesign fusion) — a chunk id can never reach …
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:528` — **DEVIATION (broader than the task line, nothing dropped).** H1.3's done-when says items *lacking* chunks fall back to the whole-item vector. Implemented instead as: the whole-item scan is kept **unchanged** and simply maxed against the chunk scan, so every item consults both signals. The require…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:530` — **DISCOVERY — today's item-level locator is real, and its gap is precisely the semantic case.** The returned shape already carries a P12 locator (`source_type`/`section`/`line_range`/`deep_link`) built by `_attach_locator` from a whole-document query-term scan. So "at least as specific" is NOT tr…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:536` — Vectors throughout the tests are deterministic hand-written BLOBs written straight into `items.embedding`/`chunks.embedding` (no embedding model is involved in any assertion), so the cosine, floor, roll-up, RRF and cliff arithmetic is exactly reproducible; a ranking test that could not embed woul…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:539` — **The index is a CANDIDATE GENERATOR, not a second scorer.** `vec0` narrows the corpus; `_consider` still applies the KL-9 dimension guard, the same cosine, the same `_VECTOR_MIN_SIMILARITY` floor and the same MAX roll-up. So ANN and the exact scan share one scoring implementation and cannot disa…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:630` — **DISCOVERY — H1.1/H1.2 (`KL-9`) is ALREADY on `origin/main`; only H1.3/H1.4 are unmerged.**
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1161` — but unused" since KL-9), so feeding it would leave the backfill blind to every entity named in a
- **design note** `docs/roadmap/design-notes/kl9-chunk-indexing.md:1` — # KL-9 — chunk indexing design

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
