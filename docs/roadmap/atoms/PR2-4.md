<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR2-4` — Confirm-gated fixes + surfacing/memory-pipeline simulators + structured crash capture

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-RESILIENCE](../plans/PLATFORM-RESILIENCE.md) (`PR2`, plan status `done`) |
| **Atom table** | [`../atomic/PR2.md`](../atomic/PR2.md) |
| **Execution position** | #483 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2 Confirm-Gated Auto-Fixes; §3.1 Surfacing simulator; §3.2 Memory-pipeline probe set; §6.5 Structured Crash Capture

## Done when

resilience/fixes.py registry (symlink-repair with shadow-copy backup, orphan-prune, prune-bindings) SEL-audited behind a two-step confirm (400 without confirm); POST /api/doctor/simulate/surfacing returns per-signal breakdown with zero LLM calls (criterion #4 surfacing half); crashes + memory-pipeline Doctor probes; redacted, 20-file-capped crash artifacts; POST /api/model-providers/{name}/selftest.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PR2-1`](PR2-1.md) | intra-repo | `done` |
| [`PR2-2`](PR2-2.md) | intra-repo | `done` |

### Unblocks (2)

[`PR2-7`](PR2-7.md) (`done`), [`PR2-9`](PR2-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/PLATFORM-RESILIENCE.md:1167` — `doctorSimulateSurfacing` shipped in PR2-4 with ZERO frontend consumers — an inert control. This

### Its plan's own record, via the scope references (27 line(s))

This atom's scope cites `§2`, `§3.1`, `§3.2`, `§6.5`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:22` — - **NEW-18** (Doctor: tiered probes process → socket → cheap RPC → per-capability; capability-degraded-is-never-core-failure doctrine; confirm-gated auto-fixes — stale cache, symlink repair, orphan pruning; per-provider selftest endpoints; GBrain health-scored self-remediation — deficit score, de…
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:26` — - **NEW-21** (platform-wide no-model degraded mode: per-surface declared fallback contract — deterministic fallbacks + pending-enrichment queues that drain when a provider returns + visible degraded indicator) → §5. Sources: `knowledge-forge`, `moss-audio`. The knowledge instance is ALREADY APPRO…
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:63` — - **FE:** a **Doctor tab** in Settings via the SUBPAGES registry (`SettingsPage.tsx:66` precedent, next to the existing Diagnostics log-tail, which stays). Grouped capability cards, tier-failed badges, evidence disclosure, fix buttons (§2). The dashboard `SystemHealth` widget gains a one-line doc…
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:74` — | **model-providers** | per config entry: `ModelCatalog.test_connection()` (fail-soft, no session); breaker state + latency percentiles COMPOSED from AUTONOMY-GUARDRAILS §2.5's `GET /api/models/health` (that plan owns the model-call audit; the Doctor renders it, never rebuilds it) |
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:120` — **Overlap honored:** the *scoring machinery* and its evolution (per-arm confidence, slot allocator, surfacing_events measurement, near-miss ledger) are owned by the approved **LEARN-R4/R7/R15** (WORKFLOWS-V2-LEARNING-FLYWHEEL §2.4/§2.5) and the observability panel by **LEARN-R14** (§6 there). Thi…
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:127` — - **no-op vs saved counts** — reading the approved **LEARN-R19** outcome records (`FLUSH_OK` / `FLUSH_ERROR` / proposal IDs — WORKFLOWS-V2-LEARNING-FLYWHEEL §2.1): "7 days of all-FLUSH_OK on an active system" renders as a WARN, exactly the alarm LEARN-R19d specifies;
- `§2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:147` — `health_score = 100 − Σ weighted deficits`, computed from **measured problems only** (never guesses): stale/heuristic-stamped embeddings and knowledge entries (§5's pending-enrichment backlogs), FTS/faiss desyncs, orphan counts (§2's probe outputs), staging backlog (§3.2), failed-run backlog, ski…
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:23` — - **NEW-18 amendment a** (surfacing simulator: dry-run any hypothetical query → per-candidate per-signal score breakdown, tier decision, inclusion/exclusion reason, zero LLM calls) → §3.1.
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:137` — **Overlap honored:** the execution machinery is APPROVED — `automation_run(id, dry_run?)`, the dry-fire smoke gate button, and the T9 recon rule (dry-run against bash/run-script/webhook is *refused and recorded as a preview*; only run-prompt/run-workflow truly dry-run, `ActionProvider.supports_dr…
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:300` — | **WORKFLOWS-V2-LEARNING-FLYWHEEL** LEARN-R4/R7/R14/R15 (surfacing scoring/observability), LEARN-R19 (staging outcomes + cost metering) | §3.1 simulator is a dry-run explain UI over their scorer; §3.2 probes READ LEARN-R19 records; both degrade gracefully pre-flywheel |
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:499` — - [2026-07-25][S4] DONE: Fixes + simulators + crash capture (§2, §3.1, §3.2, §6.5). New
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:511` — returns per-candidate arm scores + inclusion/exclusion reason — the §3.1 simulator, zero
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:1166` — **DISCOVERY / adjacent scope taken deliberately.** The §3.1 surfacing simulator endpoint
- `§3.1` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:1193` — `service.tick` persists the next fire BEFORE dispatch (§3.1 crash safety), so it can only ever see
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:24` — - **NEW-18 amendment b** (memory-pipeline probe set: last capture per source, no-op vs saved counts, staging backlog, per-op LLM cost — silent memory-pipeline death becomes visible) → §3.2.
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:77` — | **memory-pipeline** | the §3.2 probe set |
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:137` — **Overlap honored:** the execution machinery is APPROVED — `automation_run(id, dry_run?)`, the dry-fire smoke gate button, and the T9 recon rule (dry-run against bash/run-script/webhook is *refused and recorded as a preview*; only run-prompt/run-workflow truly dry-run, `ActionProvider.supports_dr…
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:147` — `health_score = 100 − Σ weighted deficits`, computed from **measured problems only** (never guesses): stale/heuristic-stamped embeddings and knowledge entries (§5's pending-enrichment backlogs), FTS/faiss desyncs, orphan counts (§2's probe outputs), staging backlog (§3.2), failed-run backlog, ski…
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:300` — | **WORKFLOWS-V2-LEARNING-FLYWHEEL** LEARN-R4/R7/R14/R15 (surfacing scoring/observability), LEARN-R19 (staging outcomes + cost metering) | §3.1 simulator is a dry-run explain UI over their scorer; §3.2 probes READ LEARN-R19 records; both degrade gracefully pre-flywheel |
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:499` — - [2026-07-25][S4] DONE: Fixes + simulators + crash capture (§2, §3.1, §3.2, §6.5). New
- `§3.2` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:533` — - [2026-07-25][S4] DEVIATION (scope): §3.2 memory-pipeline is the "current-seam version"
- `§6.5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§6.5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:314` — - **Session 4 — fixes + simulators + crash capture (§2, §3, §6.5):** confirm-gated Fix catalog (symlink repair, cache cleanup, orphan pruning, manifest resync) with SEL audit; per-provider selftest endpoint; surfacing simulator (`explain=True` on the scorer + panel); memory-pipeline probe pack (c…
- `§6.5` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:499` — - [2026-07-25][S4] DONE: Fixes + simulators + crash capture (§2, §3.1, §3.2, §6.5). New

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
