<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WS-1` — Source-provider contract + make the dormant knowledge seam real

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WATCHED-SOURCES](../plans/WATCHED-SOURCES.md) (`WS`, plan status `done`) |
| **Atom table** | [`../atomic/WS.md`](../atomic/WS.md) |
| **Execution position** | #661 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1.1 One contract four shapes; §1.3 fixes 1-2-3 (KnowledgeTypeHandler, create_native_provider, search_all non-goal); §Plug-in Map SDK surface; Disposition rows for base.py/registry.py/EntitySeamHandler

## Done when

KnowledgeSourceProvider/SourceItem/SourcePollResult defined and re-exported via sdk/knowledge.py; EntitySeamHandler no-op for `knowledge` replaced by a real KnowledgeTypeHandler (load_factory->register into knowledge_providers.registry); create_native_provider returns a real factory (no longer None); a KnowledgeProvider-shaped fixture registers and appears in list_provider_info as kind:external; test_manifest_types_match_handlers stays green

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`WS-2`](WS-2.md) (`done`), [`WS-8`](WS-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `14529480c` 2026-08-08 — feat(ambient): AS-1 composable home — dashboard-as-views registry, Overview preset, pin + propose, AmbientConfig (#910)
- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:965` — implementation subagents stalled on it). Split, contract-owner-first (the pattern WS-1 used):
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:971` — SAME commit — the #47 rule; parity test green. Applied the same lazy-`deregister` fix as WS-1 (no
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:576` — `KnowledgeTypeHandler` (WS-1) and `ChannelTypeHandler` are the precedent for graduating a type off the
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:348` — - [WS-1] DONE: **§1.1 contract + §1.3 fix 1 (KnowledgeTypeHandler).** Defined the poll contract in
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:422` — ABC** as §1.1's sketch shows. The shipped ABC (WS-1) has neither, and `feed_source`/`dir_source`
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:892` — connector pack is not a new provider type — it rides `knowledge` (already present since WS-1) with

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§1.1`, `§1.3`. Those tokens appear in its source plan at:

- `§1.1` `docs/roadmap/plans/WATCHED-SOURCES.md:348` — - [WS-1] DONE: **§1.1 contract + §1.3 fix 1 (KnowledgeTypeHandler).** Defined the poll contract in
- `§1.1` `docs/roadmap/plans/WATCHED-SOURCES.md:422` — ABC** as §1.1's sketch shows. The shipped ABC (WS-1) has neither, and `feed_source`/`dir_source`
- `§1.1` `docs/roadmap/plans/WATCHED-SOURCES.md:423` — already carry `validate_spec(spec) -> tuple[bool, str]` as a convention — not §1.1's
- `§1.1` `docs/roadmap/plans/WATCHED-SOURCES.md:488` — implementer's correction of §1.1 and of the brief stands; `sanitize_html` reuses
- `§1.3` `docs/roadmap/plans/WATCHED-SOURCES.md:13` — `knowledge/sources/` package, no `sources` table, no feed layer, no `dir-source`, and §1.3's three
- `§1.3` `docs/roadmap/plans/WATCHED-SOURCES.md:50` — - **Retrieval is NOT federated.** The pipeline runs through `personalclaw.knowledge.*` directly; the uber-pool model is: external providers write into THE ONE `items` table (`provider` column = attribution) via `store.create_typed_item(provider=<name>)` + `ingest_queue.enqueue(item_id)`, and cros…
- `§1.3` `docs/roadmap/plans/WATCHED-SOURCES.md:348` — - [WS-1] DONE: **§1.1 contract + §1.3 fix 1 (KnowledgeTypeHandler).** Defined the poll contract in
- `§1.3` `docs/roadmap/plans/WATCHED-SOURCES.md:366` — appears as `kind:external`) is fully met by the real handler. `§1.3 fix 2/3` (search_all non-goal /

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
