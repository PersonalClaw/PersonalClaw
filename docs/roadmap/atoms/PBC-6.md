<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PBC-6` — --slack-only clean-break removal (mapped to --headless)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROVIDER-BOUNDARY-COMPLETION](../plans/PROVIDER-BOUNDARY-COMPLETION.md) (`PBC`, plan status `done`) |
| **Atom table** | [`../atomic/PBC.md`](../atomic/PBC.md) |
| **Execution position** | #407 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #ea63235 |

## Intent — what this is for

S2 T2.3; Design A (flag cleanup #6)

## Done when

--slack-only deleted outright (owner clean-break call, DEVIATION from plan's warn-one-release); dest renamed slack_only->headless at cli.py:232 parser + cli.py:151 consumer (E1 premise: plan said cli_server.py, actually cli.py); docs/reference/cli.md + two tests updated; CHANGELOG entry written

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`PBC-7`](PBC-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.3`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:148` — | T2.3 | `--slack-only`: emit `DeprecationWarning`-style stderr line mapping to `--headless` (removal note in CHANGELOG); keep behavior identical this release | `src/personalclaw/cli_server.py`, `CHANGELOG.md` | flag still works; warning printed once |
- `T2.3` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:171` — - [2026-07-20][T2.3] DEVIATION: `--slack-only` DELETED outright (owner decision 2026-07-20: clean break under the pre-1.0 banner), NOT the plan's warn-one-release. Its dest renamed `slack_only`→`headless`; `docs/reference/cli.md` + two tests updated. commit `ea63235`. Premise note: the flag lived…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
