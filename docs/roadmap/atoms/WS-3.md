<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WS-3` — web-source: five-detector stack, selector configs, escalating fetch, preview+create flow

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WATCHED-SOURCES](../plans/WATCHED-SOURCES.md) (`WS`, plan status `done`) |
| **Atom table** | [`../atomic/WS.md`](../atomic/WS.md) |
| **Execution position** | #663 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1402 |

## Intent — what this is for

§2.1 five detectors; §2.2 declarative selector configs + schema-derived validate_spec; §2.3 outcome-driven escalating fetch (net.fetch tier1 + web/render.py tier2) under one budget; §2.4 preview-then-save; §11 step 2. Reconcile against shipped triggers/web_poll.py extraction/budget rather than re-derive

## Done when

Pasting a real changelog/blog URL yields a correct zero-LLM item preview via auto-detection and a homepage yields the pick-a-listing-page guidance; a manual selector config rescues a JS-lite failure (SC#1); a JS-heavy source succeeds only after render-tier escalation within max_requests with the escalation recorded, and allow_render:false degrades to a 'needs render tier' health status (SC#2)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WS-2`](WS-2.md) | intra-repo | `done` |

### Unblocks (2)

[`WS-8`](WS-8.md) (`done`), [`WS-9`](WS-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `fd3f8c96f` 2026-08-15 — feat(sources): WS-3 web-source detection, selector configs, and escalating fetch
- `14529480c` 2026-08-08 — feat(ambient): AS-1 composable home — dashboard-as-views registry, Overview preset, pin + propose, AmbientConfig (#910)

### Named by atom id in the roadmap prose (17)

- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:374` — - [WS-3] DONE: **§2 web-source — five detectors, selector configs, escalating fetch, preview.**
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:391` — Its per-day `WatchState` budget was NOT lifted: WS-3's budget is per POLL (`budget.max_requests`), a
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:454` — **OPEN (not WS-3's, stated so it is not lost):** `SourcesConfig.daily_request_budget` is still
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:456` — reads it (the provider is not handed `cfg`). WS-3's budget is the per-poll `max_requests` the atom
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:531` — surface; `WS-3` owns the web-source preview+create flow. Announcing feeds now would promise something
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:536` — callers**, so WS-2's store, WS-3's five-detector web kind, WS-4's feeds and WS-5's directory
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:553` — re-validation WS-3/WS-5 already do; there is no second copy of the rules in the handler.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:555` — WS-3 deliberately kept `preview` off the `KnowledgeSourceProvider` ABC (a feed's or a folder's
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:562` — the schema would be exactly the second artifact WS-3's schema-first inversion exists to avoid.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:602` — exactly the two that could not say a retry was coming — the same shape WS-3 fixed for
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:678` — **OBSERVED, not changed (WS-3's decision, stated so it is not lost):** after both fixes landed
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:682` — `needs render tier` verdict even though the page still needs the tier. That is WS-3's
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:695` — **CHANGELOG: yes.** WS-3 and WS-4 both withheld an entry because nobody could reach the feature.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:876` — URL — so this is the CORRECT grammar here, not a smaller one, and it is why WS-3's
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:903` — zero non-test callers exactly as `WS-3` and `WS-4` recorded — so "bundled recipes surface in the
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1005` — **CHANGELOG: yes** — and this is the first WS atom that earns one. `WS-2`/`WS-3`/`WS-4`/`WS-5` all
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1025` — error, success) — the same reasoning that made `next_poll_at` unconditional in WS-3: a poll event

### Its plan's own record, via the scope references (18 line(s))

This atom's scope cites `§2.1`, `§2.2`, `§2.3`, `§2.4`, `§11`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/WATCHED-SOURCES.md:153` — "Watch this URL" (Knowledge section + chat tool): paste URL → `provider.preview(spec)` runs detection once and returns extracted items + which detector won → user sees the item list, tunes detectors/selectors, names the source → save. Zero items → the listing-page guidance (§2.1). Preview is a dr…
- `§2.1` `docs/roadmap/plans/WATCHED-SOURCES.md:375` — `knowledge_providers/web_source.py` runs the five §2.1 detectors as pure functions over ONE
- `§2.1` `docs/roadmap/plans/WATCHED-SOURCES.md:394` — `selector_frequency` runs LAST rather than §2.1's fourth — DEVIATION, recorded: it is the only
- `§2.1` `docs/roadmap/plans/WATCHED-SOURCES.md:396` — outrank a declaration. §2.1's table is otherwise kept in order. (b) Hygiene runs INSIDE the stack
- `§2.1` `docs/roadmap/plans/WATCHED-SOURCES.md:413` — page that rendered plenty of text is the WRONG URL → §2.1's listing-page guidance and NO render
- `§2.1` `docs/roadmap/plans/WATCHED-SOURCES.md:479` — reversing `DETECTOR_ORDER`'s last two entries — this atom's own deviation from §2.1 — reded exactly
- `§2.2` `docs/roadmap/plans/WATCHED-SOURCES.md:397` — loop, per detector: a detector whose every candidate fails the §2.2 floors found NOTHING and the
- `§2.2` `docs/roadmap/plans/WATCHED-SOURCES.md:402` — **DEVIATION (recorded): §2.2's schema is derived the other way round — schema FIRST.** The atom says
- `§2.2` `docs/roadmap/plans/WATCHED-SOURCES.md:560` — of answered with an empty item list that reads like a failure. §2.2's `SPEC_SCHEMA` is deliberately
- `§2.3` `docs/roadmap/plans/WATCHED-SOURCES.md:29` — - **NEW-5** (core) — five-detector auto-source stack + declarative selector configs + preview → §2; outcome-driven escalating fetch under one budget at the `net.fetch` chokepoint → §2.3; guid-keyed novelty gating → §3.3; first-class watched feeds (HN Algolia, GitHub trending, RSS/CSV) with since-…
- `§2.3` `docs/roadmap/plans/WATCHED-SOURCES.md:53` — - **A headless-browser fetch path already exists in core.** `web/render.py` does its own pre-flight `guard.evaluate()` because Playwright bypasses IP pinning. The escalating fetch chain (§2.3) **reuses it** as the escalation tier — no new scrape container ships in core.
- `§2.3` `docs/roadmap/plans/WATCHED-SOURCES.md:104` — budget: dict               # {max_requests: 10, allow_render: false} — per-poll fetch budget (§2.3)
- `§2.4` `docs/roadmap/plans/WATCHED-SOURCES.md:75` — async def preview(self, spec: dict) -> SourcePreview:   # dry extraction for the create flow (§2.4)
- `§2.4` `docs/roadmap/plans/WATCHED-SOURCES.md:430` — tier` into `degraded`), `SourcePreview` is the §2.4 dry run, `HEALTH_OK/DEGRADED/ERROR/NEEDS_RENDER`
- `§2.4` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:321` — **~5 sessions**, mapping 1:1 onto §11:
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
