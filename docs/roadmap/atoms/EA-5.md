<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-5` — External-agent capture proxy (/capture/v1) + telemetry import

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #218 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§7 (Dialect 5 capture proxy): §7.1 dual-wire OpenAI/Anthropic proxy, §7.2 recording + fence-at-ingestion; §8 telemetry import. Session 4.

## Done when

/capture/v1/chat/completions + /capture/v1/messages forward verbatim to a client-record `upstream` ProviderEntry (standard credential order) with passthrough fallback; loopback-only always; SSE piped via a dedicated streaming client that pre-flights guard.evaluate against an operator-visible upstream host allowlist (web/render.py:76 pattern); stream-first, record-async off hot path (asyncio.to_thread), recording failure never fails the forwarded request; capture/<id>.jsonl (0600) turn records + full-content sidecar; read-skill attribution via skill_path_map; redact()→fence_untrusted(source=capture:<client_id>) BEFORE persist; new `capture` staging source in learning.db (records durably even if flywheel steps 1-3 absent — hookup is one adapter); retention prune at capture.retention_days (default 30) on curator tick; `personalclaw capture import <file> --format jsonl|json|sse` + POST /capture/import normalize→redact→fence→stage, idempotent by content hash, malformed-lines skipped-and-counted (Success Criteria 5,6).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EA-1`](EA-1.md) | intra-repo | `done` |
| `EXT:LEARNING-FLYWHEEL:learning.db staging tier + capture_hygiene fence rule (records without it; mining hookup is one adapter)` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`EA-6`](EA-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (14)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `a6c90e488` 2026-08-26 — feat(external-access): EA-5 add POST /capture/import and the upstream allow-list control
- `a0bc0bf91` 2026-08-26 — feat(external-access): EA-5 stage captured turns as the fourth cadence and reach prune() from the tick
- `8047398a8` 2026-08-25 — docs(roadmap): EA-5's upstream clause closed; two others keep it todo
- `9c856f283` 2026-08-25 — feat(inbound): EA-5 forward capture to a client-record upstream ProviderEntry
- `361d281c0` 2026-08-24 — docs(roadmap): cite the batch PR on EA-9
- `adf11620b` 2026-08-24 — test(capture): the bypass suite asserts the typed envelope too
- `cfdba4be9` 2026-08-24 — test(capture): follow the typed envelope and the moved registry
- `e91be0b7b` 2026-08-24 — refactor(llm): move the branded-spec registry and credential ladder into core
- `635819002` 2026-08-24 — docs(roadmap): EA-5 PARTIAL — record the two integration defects and the unmet upstream clause
- `4885728a6` 2026-08-24 — fix(capture): EA-5 make the imported record shape and the proxy routes actually reach their handlers
- `e731ed970` 2026-08-24 — feat(capture): EA-5 telemetry import adapters for unproxyable agents
- `46dfe3216` 2026-08-24 — feat(inbound): EA-5 loopback-only /capture/v1 proxy (transport half)
- `63d2f6ea4` 2026-08-24 — feat(inbound): EA-5 capture session store with ingestion-time hygiene

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:700` — ## Execution log — `EA-5` (§7 capture proxy + §8 telemetry import) — **COMPLETE in code; atom flips with PR #2121**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:702` — - [2026-08-24][EA-5] **Shipped in PR #1988** as three fenced halves plus one integration fix:
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:714` — - [2026-08-24][EA-5] 🔴 **UNMET — "forward to a client-record `upstream` ProviderEntry".**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:721` — - [2026-08-24][EA-5] **TWO DEFECTS THAT ONLY INTEGRATION COULD FIND** — each half's suite was green
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:738` — - [2026-08-24][EA-5] **The overlay had to be INSIDE `record_hash`.** `_build_record` hashes
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:744` — - [2026-08-24][EA-5] **MEASURED — `redact_credentials` has two traps, both now pinned.** It matches
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:751` — - [2026-08-24][EA-5] **Fail-closed choices, stated because the inverse is plausible.** An empty
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:757` — - [2026-08-24][EA-5] **PRE-EXISTING DRIFT, mitigated not ignored:** a flat
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:763` — - [2026-08-24][EA-5] **Gate, re-run by the driver rather than taken on report:** `make lint` clean
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:774` — - [2026-08-24][EA-5] **DISCOVERY (not acted on) — imported turns carry no original timestamps.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:781` — - [2026-08-26][EA-5] ✅ **DONE — the two clauses the last session recorded as DISCOVERY.** Both were
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:857` — - [2026-08-26][EA-5] 🟡 **ATOM READY TO FLIP, NOT FLIPPED.** With this commit, every `done_when`
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:987` — - **[2026-08-25][`EA-5`] The pinned-upstream clause is CLOSED. Atom stays `todo` on two others.** The
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1014` — this work, and it alone keeps `EA-5` `todo`.
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1033` — - **[2026-08-26][`EA-5`] BOTH parked clauses are CLOSED. Atom still `todo` — on two clauses nobody had
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1095` — backend **332 passed, 0 failed** across `test_ea5_capture_import.py` (25, +7 new), the three other EA-5
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1299` — - [2026-08-26][EA-5] **The wire-census ceiling caught a real omission in this commit; satisfied, not
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1463` — - [2026-09-01][EA-6] **DONE.** `learning/replay.py` (new) is the whole atom: case mining over EA-5's

### Its plan's own record, via the scope references (24 line(s))

This atom's scope cites `§7`, `§7.1`, `§7.2`, `§8`, `Session 4`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:29` — - **NEW-20** (external-agent capture proxy: local OpenAI/Anthropic-compatible endpoint other agents on the machine point at as API base URL, recording sessions — turns, tool calls, skill reads, file mutations — into the learning-flywheel capture path with untrusted-content fencing at ingestion; t…
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:52` — New module `src/personalclaw/inbound/` mounted by the `dashboard/server.py` app factory. All five dialects (§2-§5, §7) register sub-routes on it and inherit the full discipline below; none of them may add a route outside it.
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:174` — `personalclaw capture import <file> --format jsonl|json|sse --source <label>` + `POST /capture/import` (capture surface, same bearer): normalizes exported agent logs (Claude Code session JSONL, OpenAI-format request logs, raw SSE event dumps) into the §7.2 session record shape via small per-forma…
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:252` — - **Session 4 — capture proxy + import (§7, §8):** dual-wire proxy with provider-entry upstream resolution + pre-flighted streaming egress, off-hot-path recording, skill_path_map attribution, redact→fence-at-ingestion, staging-tier adapter, retention; telemetry-import adapters + idempotent import.
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:267` — | Prompt injection via captured external-agent content becoming standing instructions | Fence-at-ingestion (§7.2) + flywheel hygiene rule + propose-don't-write: fenced content can only travel the proposal path; success criterion 6 is the adversarial test |
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:700` — ## Execution log — `EA-5` (§7 capture proxy + §8 telemetry import) — **COMPLETE in code; atom flips with PR #2121**
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:717` — the missing binding rather than silently choosing a provider. **Passthrough — §7.1's own documented
- `§7` `docs/roadmap/plans/EXTERNAL-ACCESS.md:723` — `raw["request_body"]` and rebuilt the §7.2 record itself, while the adapters emit §7.2-shaped
- `§7.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:717` — the missing binding rather than silently choosing a provider. **Passthrough — §7.1's own documented
- `§7.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:174` — `personalclaw capture import <file> --format jsonl|json|sse --source <label>` + `POST /capture/import` (capture surface, same bearer): normalizes exported agent logs (Claude Code session JSONL, OpenAI-format request logs, raw SSE event dumps) into the §7.2 session record shape via small per-forma…
- `§7.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:267` — | Prompt injection via captured external-agent content becoming standing instructions | Fence-at-ingestion (§7.2) + flywheel hygiene rule + propose-don't-write: fenced content can only travel the proposal path; success criterion 6 is the adversarial test |
- `§7.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:723` — `raw["request_body"]` and rebuilt the §7.2 record itself, while the adapters emit §7.2-shaped
- `§7.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:728` — `_build_record` as the ONE shaping+screening path, synthesising minimal bodies from the §7.2 text
- `§7.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:775` — `stage_records` stamps import time and ignores the adapter's `ts`, for the §7.2 path as for the
- `§7.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:784` — — §7.2 and the plan's own line 42 ("the capture proxy is a **fourth capture cadence**") /
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:29` — - **NEW-20** (external-agent capture proxy: local OpenAI/Anthropic-compatible endpoint other agents on the machine point at as API base URL, recording sessions — turns, tool calls, skill reads, file mutations — into the learning-flywheel capture path with untrusted-content fencing at ingestion; t…
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:252` — - **Session 4 — capture proxy + import (§7, §8):** dual-wire proxy with provider-entry upstream resolution + pre-flighted streaming egress, off-hot-path recording, skill_path_map attribution, redact→fence-at-ingestion, staging-tier adapter, retention; telemetry-import adapters + idempotent import.
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:700` — ## Execution log — `EA-5` (§7 capture proxy + §8 telemetry import) — **COMPLETE in code; atom flips with PR #2121**
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:727` — record SHAPE was not, and each half picked a defensible reading of §8. Fixed by keeping
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:792` — `record_turn` AND from `stage_records`, i.e. the proxy path and the §8 import path stage through the
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1038` — …)`, i.e. the function the CLI calls. So redaction, fencing, the content-hash ledger and §8's
- `§8` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1068` — `cadence`/`kind` and no capture row is ever written. §8's "records durably even if flywheel steps 1-3
- `Session 4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:252` — - **Session 4 — capture proxy + import (§7, §8):** dual-wire proxy with provider-entry upstream resolution + pre-flighted streaming egress, off-hot-path recording, skill_path_map attribution, redact→fence-at-ingestion, staging-tier adapter, retention; telemetry-import adapters + idempotent import.
- `Session 4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:258` — Sessions 1-3 are NEW-10's core and ship value alone; 4-5 are NEW-20; 6 completes both. Session 4 can land before 2-3 if capture is wanted early (it depends only on Session 1). Session 7 depends on Session 1's headless-profile classification but can otherwise land any time after it.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
