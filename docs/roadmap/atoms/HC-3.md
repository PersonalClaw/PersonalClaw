<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `HC-3` — Best-of-N sampling core (sampling.py) + bundled best-of-n skill

| | |
|---|---|
| **Status** | `blocked` |
| **Plan** | [HARNESS-CRAFT](../plans/HARNESS-CRAFT.md) (`HC`, plan status `in_progress`) |
| **Atom table** | [`../atomic/HC.md`](../atomic/HC.md) |
| **Execution position** | #283 of 684 in dependency order |
| **Startability** | not startable |
| **Blocked reason** | Status `blocked` is CORRECT and the blocker is an OWNER LIVE RUN, re-verified 2026-08-27 - do not re-implement. best_of_n(prompt, n=3, judge_criteria, use_case) is genuinely parallel (create_task per candidate + gather), proven by peak_in_flight==4 AND a wall-clock span assertion so a for-await loop fails both; every sample goes through the wrap_model_call_guard chokepoint; the judge picks deterministically with ties to the lowest index, pinned across opposite completion orders; the outcomes file is bounded; and snapshot exclusion is WIRED (StateEntry(derived=True) -> backup_entries filters not e.derived -> snapshot iterates that). The bundled skill carries the cap, the cost multiplier inside the gate, and a numbering rule that makes 'use #2' fixed. MEASURED FINDING: one_shot_completion had NO temperature parameter, so 'temperature-varied' was unbuildable as written - it was added and threaded; documented caveat, an extended-thinking model DROPS a custom temperature and collapses the ladder to zero spread. UNMET: the live run, which needs a real paid model and spends real money, so no agent may do it. SMALLEST REMAINING UNIT: the owner starts the dev gateway on an isolated home with a real credential, asks for 3 versions and a pick, confirms the gate names 3x cost and that <home>/model_calls.jsonl shows 3 sample calls plus the judge pass, then records it in the plan's Execution log. |
| **PR** | #1392 |

## Intent — what this is for

§2.1 The core: sampling.py helper; §2.2 The skill: skills/bundled/best-of-n/SKILL.md

## Done when

best_of_n(prompt,n,judge_criteria,use_case) fires N temperature-varied parallel one_shot_completion calls through the ModelCallGuard chokepoint, LLMJudge scores each and returns winner+candidates+judgments, and appends a bounded record (ts,n,criteria_digest,winner_idx,score_spread,tokens_total) to ~/.personalclaw/sampling_outcomes.jsonl (snapshot-excluded); skills/bundled/best-of-n/SKILL.md confirms N (cap 5)+criteria naming the N-call cost multiplier (grill ambiguous-trigger precedent), presents winner with collapsible runners-up and a working 'use #2' choice; chat 'give me 3 versions and pick the best' validated end-to-end with all N calls visible in model_calls.jsonl (SC 3, 4).

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`HC-5`](HC-5.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `232c26738` 2026-09-07 — docs(roadmap): record the #2673 dependency audit — one real defect, two cleared
- `a122e0fd1` 2026-09-05 — feat(workflows): v2 best-of-n and check-work templates calling the HC-3/HC-4 cores (HC-5)
- `07cd1e53c` 2026-08-15 — docs(roadmap): restore INU-7's done flip lost to the dag.json merge
- `2f2b8cbdd` 2026-08-15 — feat(harness): HC-3 best-of-N sampling with judge selection

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/HARNESS-CRAFT.md:156` — ActionTypeSpecs, bundled census, frontier golden regenerated purely additively. `HC-3` stays todo
- **source plan** `docs/roadmap/plans/HARNESS-CRAFT.md:313` — - **[2026-08-15][S2] HC-3 IMPLEMENTATION COMPLETE, ATOM BLOCKED on an owner live-run.**

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `§2.1`, `§2.2`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/HARNESS-CRAFT.md:70` — - A `best-of-n` workflow template: fan-out node (N samples) → judge node → select node. This is the engine-native form the roadmap already anticipated ("v2's judge-panel pattern will make this a template"); the template CALLS the §2.1 core rather than reimplementing, so skill and template stay be…
- `§2.1` `docs/roadmap/plans/HARNESS-CRAFT.md:109` — Session 1 is fully independent (Wave 2, or whenever fan-out slowness is observed). Sessions 2-3's skill halves are standalone-shippable; their template halves land with WORKFLOWS-V2 Slice 3+ (Wave 2/3). If v2 slips, the skills ship alone and the templates follow — the §2.1/§3.1 cores are the stab…
- `§2.1` `docs/roadmap/plans/HARNESS-CRAFT.md:122` — | Skill/template drift (two behaviors for one name) | Both halves call the same §2.1/§3.1 core module; templates are thin spec wrappers; a shared test exercises both entry points |

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
