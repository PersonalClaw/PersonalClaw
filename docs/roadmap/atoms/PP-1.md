<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-1` — Refuse a binding whose producer is not ordered before it (WF_UNORDERED_DEP)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #460 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1297 |

## Intent — what this is for

The engine keeps TWO edge lists and nothing checks they agree. Scheduling admission comes ONLY from container order plus sibling-only `needs` (`tick._visit_parallel` gates on `child.needs`; `validator.py:221-230` refuses a `needs` that is not a sibling in the same `parallel`, reason given: "cross-container edges would make the tree a graph and break the frontier's locality"). Data dependencies are a SEPARATE graph: `node_deps` feeds the resume cache's `inputs_hash` (`controller._resolved_inputs`), the stale-inputs check (`controller.py:1471`) and the mutation cascade (`mutations.py:262`) — never admission. `validator._kahn_levels` builds the combined graph and its own docstring says reading its levels as an execution order would be wrong. None of the 46 shipped `WF_*` codes covers the disagreement.

## Done when

A new `WF_UNORDERED_DEP` error proves, for every node and every `node_deps` target, that the producer is terminal before the reader can be admitted — satisfied by being an earlier sibling within an enclosing `sequence`, or by a `needs` chain to it within the same `parallel`; anything else is an error naming the reader, the producer and the missing edge. Computed from the path→node map `validate_node_tree` already walks; ZERO runtime change and zero scheduling change (`tick.py` byte-identical). The bundled-template population is censused BEFORE the code becomes an error — giving a never-run rule teeth against a population that fails it is an outage, not a gate — and any violator is fixed in the same change. Proven able to fail by two probes (delete a `needs` that a binding relies on; add a cross-container binding with no ordering) each reverted by a targeted edit, plus a vacuity floor asserting the rule sees a non-empty dep set on at least one shipped template.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`PP-2`](PP-2.md) (`done`), [`PP-3`](PP-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `29d8080c6` 2026-08-14 — feat(workflows): PP-3 cross-check output_contract against the bindings that read it
- `ff555d51b` 2026-08-14 — feat(workflows): PP-1 refuse a binding whose producer is not ordered before it
- `6d2a7f0bc` 2026-08-14 — docs(roadmap): add Platform-Primitives plan + 20 atoms (rev 17)

### Named by atom id in the roadmap prose (14)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:78` — ## §1 Edges — dataflow declared once, ordering derived (`PP-1`…`PP-3`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:85` — `PP-1` makes the disagreement a typed error. `PP-2` derives ordering from bindings and — because the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:138` — PP-1 ─→ PP-2, PP-3
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:148` — **Startable now:** `PP-1`, `PP-4`, `WF2LOO-16`, `WF2LOO-17`, `WF2LOO-18`.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:150` — **If only three land: `WF2LOO-16`, `PP-1`, `PP-4`.** Verdict, edges, ledger. Cheapest, they unblock
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:168` — `PP-1`, `PP-2`, `PP-3`, `PP-11`, `PP-14` and `PP-15` are pure-logic and can land at any time.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:613` — - **2026-08-14 — `PP-1` DONE.** `WF_UNORDERED_DEP` (the 47th `WF_*` code) refuses a binding whose
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:620` — - **2026-08-14 — `PP-1` design notes.** (a) `dep_ordering_edges` derives every edge from
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:628` — - **2026-08-14 — `PP-1` CENSUS (the atom's precondition, run BEFORE the code became an error).**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:634` — - **2026-08-14 — `PP-1` zero-runtime-change proof.** `src/personalclaw/workflows/tick.py` is
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:640` — - **2026-08-14 — `PP-1` DISCOVERY (process, not code).** The implementing subagent died to a stream
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:781` — the author who described their output least. **Built on `PP-1`'s edge list rather than beside it**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1024` — program's standing rule (`PP-1`, `PP-3`, `WF2LOO-18`): **0 of the 19 bundled templates declare a
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1063` — `PP-1` already guarantees every binding edge in a validated spec is ordered by container structure,

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
