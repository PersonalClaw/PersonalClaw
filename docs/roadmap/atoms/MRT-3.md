<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MRT-3` — Usage/spend read model — fold, /api/usage, Usage UI, monthly recap

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MODEL-ROUTING-TELEMETRY](../plans/MODEL-ROUTING-TELEMETRY.md) (`MRT`, plan status `done`) |
| **Atom table** | [`../atomic/MRT.md`](../atomic/MRT.md) |
| **Execution position** | #367 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1899 |

## Intent — what this is for

Amendment (2026-07-26) — the usage story: T-U1 (usage fold + route), T-U2 (usage UI + per-run cost), T-U3 (monthly recap)

## Done when

routing/usage.py folds usage_stats.json (per-day keyed date->provider:model->purpose with interactive|background|loop|eval|app mapping) matching a hand-computed fixture over 50 audit lines and reproducible after delete via the shared §1.3 rebuild; GET /api/usage?window=&group= returns rows+total+estimated_share; Usage section on Settings->Models renders daily/weekly charts and a run/loop detail shows '~$X this run' via run_totals; usage_recap(month) template renders verbatim-predictable and delivers one digest-mode notification honoring quiet hours/mute via the rules engine + system cron

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MRT-1`](MRT-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 29 (MRT) — DRIVEN in a real browser

**Code evidence:**

- routing/usage.py folds the per-day usage read model and GET /api/usage is registered beside the per-turn ledger routes (dashboard/handlers/usage.py:9)
- 🔑 estimated_share IS CONSERVATIVE BY CONSTRUCTION AND SAYS WHY: a ledger dollar is treated as an estimate because TurnUsage carries no 'estimated' flag — 'over-disclosing an estimate is safe; claiming absent precision is not' — and it is not decoration, since the fold keeps per-cell estimated_dollars so it drops below 1.0 the moment a writer marks a reported cost
- unpriced_calls are structurally 0 dollars, so 'a total containing them is a FLOOR. An unpriced model must never read as "$0 spent"'
- 🔑 THE FOLD EARNS ITS PLACE BESIDE THE LEDGER FOR A MEASURED REASON: refresh() merges the refold OVER the persisted fold 'so days that have aged out of the capped JSONL survive (the ledger trims at 2×50000): per cell it keeps whichever saw more calls, which is correct because trimming can only remove rows from a completed day, never add them'
- DRIVEN: #/settings/usage renders Today/7 days/30 days, cost + tokens + turns totals, and four sections — By day and purpose, By model, By source, Cache savings — each with its own honest empty state
- 🔑 DRIVEN, THE HEADER STATES THREE INVARIANTS THE CODE ENFORCES: the double-counting refusal ('Unattended model calls are recorded in a separate log that cannot be merged with these without double-counting; the "By day and purpose" section states how much is excluded'), the scope boundary ('Observation only: nothing here caps or throttles a turn (that\'s Guardrails)'), and the unpriced rule verbatim
- 441 tests pass across the 15 routing suites; make lint clean

**Driven in the UI:** Drove the page. All figures are zero because no model is bound, so no turn has ever been recorded — the totals and per-section rows are unobserved rather than doubted.

**Notes:** Partial for the monthly recap: usage_recap(month) rendering verbatim-predictably and delivering ONE digest-mode notification honoring quiet hours and mute through the rules engine plus the system cron is not observable without a month of usage and a fired cron. Everything else in the clause is confirmed, and the double-counting sentence is the part worth keeping — a product that refuses to add two numbers because the sum would be wrong, and then tells the user what it left out, is rarer than it should be.

## Recorded history

### Commits (10)

- `ae0dc12c2` 2026-08-23 — feat(routing): MRT-5 learned policy, cost-aware ordering, propose-don't-write
- `3cb4f360b` 2026-08-22 — chore(roadmap): cite DSC-11 and MRT-3 to the batch PR, and close DESIGN-SYSTEM-CONSISTENCY
- `27d7b2ca7` 2026-08-22 — docs(roadmap): MRT-3 done — verify the usage/spend read model against main
- `caaec0140` 2026-08-21 — docs(roadmap): MRT-3 loop spend — the deviation from run_totals
- `6a5108991` 2026-08-21 — feat(usage): MRT-3 a loop's spend, read from the turn ledger by session prefix
- `e422c7f7c` 2026-08-20 — fix(usage): the loop purpose has no writer — stop advertising it as reachable
- `b6d48af6c` 2026-08-18 — feat(usage): MRT-3 monthly usage recap delivery via the rules engine + system cron (#1736)
- `c495fc13a` 2026-08-18 — chore(reference): re-render for the batch union (MRT-3 + TSE-4 on top of CA-1)
- `ac1833be2` 2026-08-18 — feat(routing): MRT-3 usage/spend read model — per-day fold + GET /api/usage
- `b24106a40` 2026-08-16 — docs(roadmap): MRT-3 BLOCKED — the usage story already ships over a different record

### Named by atom id in the roadmap prose (23)

- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:422` — pricing; MRT-3/4/5 (usage read-model, heuristic router, learned policy) are Sessions 2-3. **Gates:**
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:426` — ## Execution log — MRT-3 (usage/spend read model) — BLOCKED, owner scope decision needed
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:428` — - **MRT-3 NOT STARTED — BLOCKED (E1 premise mismatch + E6 scope). No code written; the atom stays
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:431` — COST-AND-TOKEN-OBSERVABILITY over a *different* record. Building MRT-3 as written would put a
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:463` — (`` `$${data.stats.cost_usd.toFixed(4)} this run` ``). MRT-3's `/api/usage?window=&group=` would sit
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:494` — - **THE OWNER DECISION MRT-3 NEEDS: which record is THE spend read model, and how do the two
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:495` — reconcile?** Three coherent answers, none of them "build the atom as written": **(a) retire MRT-3's
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:506` — ## Execution log — MRT-3 (usage/spend read model) — BUILT, resolving the BLOCKED above
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:509` — entry offered three ways out: (a) retire MRT-3's fold and make the amendment additive to CATO,
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:565` — ## Execution log — MRT-3, session 2 (the recap's DELIVERY + the money-honesty defect)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:656` — the surface would be worse than the gap. **So MRT-3 remains PARTIAL on this one clause.**
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:667` — ## Execution log — MRT-3, session 3 (why the last clause is still unmet, measured one layer deeper)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:669` — **MRT-3 REMAINS `todo`.** This session set out to close the one unmet clause — the loop detail's
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:709` — **Shipped instead — the one defect this measurement exposed, in MRT-3's own module.**
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:729` — ## Execution log — MRT-3, session 4 (the `loop` exclusion was hiding real spend)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:801` — `run_totals`. That is an owner shape decision, recorded here rather than guessed. **MRT-3 stays
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:809` — ## Execution log — MRT-3, session 5 (the loop money line, and why it is not `run_totals`)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:811` — **The last unmet clause is CLOSED. MRT-3's `done_when` is now satisfied end to end**, with one
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:896` — ## Execution log — MRT-3 status reconciliation (2026-08-22)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:898` — **DONE — `MRT-3` flips `todo` → `done`.** No code changed. Session 5 above already concluded *"the
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:899` — last unmet clause is CLOSED; MRT-3's `done_when` is now satisfied end to end"*, but the atom was
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:993` — **no join key between the two stores** — the identical gap `MRT-3` recorded for spend, resolved the same
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:420` — it on `api_loop_list` would be N scans per poll" rule (MRT-3). Instead the count is derived at the

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
