<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PL-4` — App legibility — declared skills through the chokepoint + auto-surfaced route tools

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-LEGIBILITY](../plans/PLATFORM-LEGIBILITY.md) (`PL`, plan status `done`) |
| **Atom table** | [`../atomic/PL.md`](../atomic/PL.md) |
| **Execution position** | #456 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§4 ("App legibility — declared skills + auto-surfaced route tools"), §4.1 skill seeding, §4.2 route-table surfacing, §8 wiring; §9 Session 4

## Done when

revived app.json skills field seeds via install_guarded (scan/lock/SEL, DANGEROUS refuses, provenance-keyed removal); backend.routes[] + AppRoutesToolProvider surface app_<name>_<op> tools resynced on enable/disable/update; one call-app-route action added to ALLOWED_HOOK_PROVIDERS; declared-vs-live drift files app.route.drift notification; app_surfaces[] populated in /api/manifest; Growth+Minutes manifests retrofitted in PersonalClawApps as proving pair

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PL-1`](PL-1.md) | intra-repo | `done` |
| [`PL-2`](PL-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 1 (PL) — observed

**Code evidence:**

- tests/test_app_routes.py present and passing (part of an 85-passed run) — covers the backend.routes[] surface and the app_<name>_<op> route tools the atom names

**Notes:** PARTIAL: the atom's proving pair is a Growth + Minutes manifest retrofit in the SEPARATE PersonalClawApps repository, which this audit did not touch, and the install_guarded chokepoint path needs an app actually installed to drive. In-core code and rail observed; the cross-repo half is unvalidated. Worth noting the workspace apps clone is named PersonalClawApps, so the gateway only discovers it via PERSONALCLAW_FIRST_PARTY_APPS_DIR.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (19 line(s))

This atom's scope cites `§4`, `§4.1`, `§4.2`, `§8`, `§9`, `Session 4`. Those tokens appear in its source plan at:

- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:28` — - **NEW-13 core (c)** (app.json-declared skills auto-registering on install; MCPify-style auto-generation of agent tools/action providers from installed app route tables, resynced on `/update`) → §4. Recon fact honored: the manifest-vs-UI dead-path audit precedent becomes a build step, not a memo…
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:45` — - **App-shipped skills have a dead field:** `apps/manifest.py:_KNOWN_FIELDS` lists `skills` as a LEGACY stripped field ("no runtime consumer"). The live precedents to copy are `prompts[]` app-owned seeding (`apps/prompt_seed.py`: idempotent, non-clobbering, removal keyed by the app's own files) a…
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:46` — - **App route tables are invisible:** first-party app backends (e.g. `apps/growth/backend/server.py:419` `r.add_get("/artifacts", …)`) register routes only inside app code, proxied at `/apps/{name}/api/*` (`apps/backend_runtime.py`). Nothing tells an agent they exist — the exact dead-path the aud…
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:66` — | `app_surfaces[]` | installed app manifests' declared route tables (§4.2) | per enabled app: routes + generated tool names |
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:189` — - **Action provider:** exactly one — `call-app-route` (§4.2) — added to `ALLOWED_HOOK_PROVIDERS` (`src/personalclaw/validation.py`) in the same commit that registers it, or hook create/update rejects it. Its rejection message is itself a §2 envelope with the allowed set as `suggestions`.
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:192` — - **Skill installs:** ONLY via `install_guarded` (§4.1) — app-shipped skills inherit quarantine/scan/lock/SEL like any marketplace skill; trust tier from the app's ledger.
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:206` — - **Session 4 — app legibility (§4):** typed `skills` field + `install_guarded` seeding + removal contract; `backend.routes[]` + `AppRoutesToolProvider` + `call-app-route` (+ `ALLOWED_HOOK_PROVIDERS`); resync on enable/disable/`/update`; declared-vs-live drift warning; retrofit Growth + Minutes m…
- `§4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:219` — | App-declared routes lie (declared but dead, or undeclared but live) | The §4.2 declared-vs-live check at enable/update files a warning notification — dishonest manifests are visible, and `agentCallable` tools 404 loudly through the proxy with a §2 envelope |
- `§4.1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:45` — - **App-shipped skills have a dead field:** `apps/manifest.py:_KNOWN_FIELDS` lists `skills` as a LEGACY stripped field ("no runtime consumer"). The live precedents to copy are `prompts[]` app-owned seeding (`apps/prompt_seed.py`: idempotent, non-clobbering, removal keyed by the app's own files) a…
- `§4.1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:192` — - **Skill installs:** ONLY via `install_guarded` (§4.1) — app-shipped skills inherit quarantine/scan/lock/SEL like any marketplace skill; trust tier from the app's ledger.
- `§4.1` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:435` — **§4.1 — app-declared skills, seeded through the chokepoint.** Revived the dead
- `§4.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:46` — - **App route tables are invisible:** first-party app backends (e.g. `apps/growth/backend/server.py:419` `r.add_get("/artifacts", …)`) register routes only inside app code, proxied at `/apps/{name}/api/*` (`apps/backend_runtime.py`). Nothing tells an agent they exist — the exact dead-path the aud…
- `§4.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:66` — | `app_surfaces[]` | installed app manifests' declared route tables (§4.2) | per enabled app: routes + generated tool names |
- `§4.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:189` — - **Action provider:** exactly one — `call-app-route` (§4.2) — added to `ALLOWED_HOOK_PROVIDERS` (`src/personalclaw/validation.py`) in the same commit that registers it, or hook create/update rejects it. Its rejection message is itself a §2 envelope with the allowed set as `suggestions`.
- `§4.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:219` — | App-declared routes lie (declared but dead, or undeclared but live) | The §4.2 declared-vs-live check at enable/update files a warning notification — dishonest manifests are visible, and `agentCallable` tools 404 loudly through the proxy with a §2 envelope |
- `§4.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:453` — **§4.2 — route-table tool surfacing (MCPify, adapted).** One generic provider, never
- `§8` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:225` — | Four-wiring-points silent config drop | Explicit checklist in §8; schema reachability tests enforce (a) |
- `§9` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `Session 4` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:206` — - **Session 4 — app legibility (§4):** typed `skills` field + `install_guarded` seeding + removal contract; `backend.routes[]` + `AppRoutesToolProvider` + `call-app-route` (+ `ALLOWED_HOOK_PROVIDERS`); resync on enable/disable/`/update`; declared-vs-live drift warning; retrofit Growth + Minutes m…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
