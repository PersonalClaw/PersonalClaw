<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-2` — Search endpoint + ChatHistoryPage search box, marked snippets, jump-to-message

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #533 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 (T1.2); Contracts §C1 (search_sessions), Design §S1

## Done when

Search returns ranked sessions; clicking a result opens it scrolled to the match; the page renders the index's best-match snippet with matched terms marked (rendered as parsed parts, never HTML); endpoint reports which path answered (source: index|scan). Shipped 2026-07-28.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SM-1`](SM-1.md) | intra-repo | `done` |

### Unblocks (1)

[`SM-3`](SM-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:77` — | T1.2 | Search endpoint + sidebar search box + result grouping + jump-to-message | chat handlers, `web/src/pages/ChatPage.tsx` sidebar | search returns ranked sessions; clicking a result opens it scrolled to the match |
- `T1.2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:119` — ### 2026-07-28 — Session 1 (T1.1, T1.2 + V1): DONE. T1.3 re-scoped — see below.
- `T1.2` `docs/roadmap/plans/SESSION-MANAGEMENT.md:154` — `/api/sessions/search` with a 300ms debounce — so T1.2's "add a search box" was
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:12` — **Status:** DONE — Session 1 (FTS5 cross-session search + match snippets) shipped
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:72` — ### Session 1 — Cross-session search + scalable sidebar
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:119` — ### 2026-07-28 — Session 1 (T1.1, T1.2 + V1): DONE. T1.3 re-scoped — see below.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
