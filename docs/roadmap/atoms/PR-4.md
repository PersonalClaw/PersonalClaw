<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR-4` — Make multi-arch images release-blocking: per-arch smoke gate (personalclaw --version) in release.yml

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-REACH](../plans/PLATFORM-REACH.md) (`PR`, plan status `done`) |
| **Atom table** | [`../atomic/PR.md`](../atomic/PR.md) |
| **Execution position** | #475 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Task A2.1; Design Track A

## Done when

release.yml gate requires both amd64+arm64 to build AND smoke 'personalclaw --version' in each; a failed arm64 build/smoke fails the release (multi-arch BUILD already present in release.yml images job; this adds the blocking smoke gate)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:CI-RELEASE-ENGINEERING:release.yml images job to gate` | cross-plan (EXT) | **unresolved** |
| `EXT:DISTRIBUTION:published multi-arch images` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `630038898` 2026-08-10 — ci(platform-reach): PR-4 make multi-arch images release-blocking (#1004)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/PLATFORM-REACH.md:133` — line is user-visible). Scope: PR-1 only — the arm64 build matrix (PR-4) + Windows guides (PR-6/7)
- **source plan** `docs/roadmap/plans/PLATFORM-REACH.md:172` — - [2026-08-10][PR-3 / A1.3] DONE: arm64 CI jobs on the non-PR path. Extended `full.yml`'s `matrix` job with two `include` entries — `ubuntu-24.04-arm` and `macos-14`, each on Python 3.13 — rather than crossing arm into the full grid: A1.3 scopes arm to the non-PR path explicitly "for budget", so …
- **source plan** `docs/roadmap/plans/PLATFORM-REACH.md:173` — - [2026-08-10][PR-4 / A2.1] DONE: multi-arch images are release-BLOCKING. `release.yml`'s `images` job already built `linux/amd64,linux/arm64`, but a manifest that merely BUILDS can still ship an arch that cannot start — so this adds a per-arch smoke that runs INSIDE each container: `docker pull …
- **source plan** `docs/roadmap/plans/PLATFORM-REACH.md:174` — - [2026-08-10][PR-5 / A2.2+A2.3] DONE: support matrix with real proof tokens + `[models]` per-arch reality. `docs/guides/platforms.md` already existed (created by EI-11 security docs) and already had a matrix, but its Proof column was prose ("CI test + release smoke") — not the required `CI:<job>…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
