<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MUCV-3` — background axis live: one_shot collapse retarget + _bg/personalclaw-lite factory

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MODEL-USE-CASES-V2](../plans/MODEL-USE-CASES-V2.md) (`MUCV`, plan status `done`) |
| **Atom table** | [`../atomic/MUCV.md`](../atomic/MUCV.md) |
| **Execution position** | #372 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##58 |

## Intent — what this is for

S2 T2.1 (C3; Design S2 background)

## Done when

one_shot_completion collapses 'background'/'ingestion' to the background axis (explicit axes honored, unrecognized -> reasoning); the _bg/personalclaw-lite factory resolves use_case='background' (both _ensure_background and cold-start paths); guard-wrap extends to the axis; binding a cheap model to background moves titles/tags/suggestions/digests/consolidation off flagship chat

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MUCV-1`](MUCV-1.md) | intra-repo | `done` |

### Unblocks (1)

[`MUCV-6`](MUCV-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.1`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:120` — | T2.1 | `background` axis live: `one_shot_completion` collapse targets `"background"` (explicit real axes still honored); `_bg`/personalclaw-lite factory resolves `use_case="background"` (SessionManager background factory + `_fallback_chat_model` walks the background chain first); guard-wrap ext…
- `T2.1` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:147` — - **`reasoning` semantic shrink:** after T2.1/T2.3 move background chores and loop judgments off it, `reasoning` narrows to explicit one-shot judgment calls (web-extract etc.). Verify no consumer is orphaned (the removal doctrine cuts both ways — an axis with no consumer gets removed, not kept fo…
- `T2.1` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:153` — - [2026-07-27][S2] DONE: consumer wiring + call-failure advance (T2.1-T2.5). **background**: `one_shot_completion`'s `"ingestion"` label collapses to `background` (explicit real axes honored; unrecognized → `reasoning`); the `_bg`/personalclaw-lite factory passes `model_axis="background"` (both t…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
