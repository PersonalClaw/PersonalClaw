<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DHT-7` — pptx writer + deck_from_markdown + deck_create tool + pptx kind

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-HANDLING-TOOLS](../plans/DOCUMENT-HANDLING-TOOLS.md) (`DHT`, plan status `done`) |
| **Atom table** | [`../atomic/DHT.md`](../atomic/DHT.md) |
| **Execution position** | #174 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2 (pptx); T2.1

## Done when

pptx_writer.py + deck_from_markdown (#/## slide boundaries, <!-- notes: --> speaker notes) + deck_create tool + pptx artifact kind; a markdown outline becomes a deck re-readable by the existing pptx reader including notes; placeholder-identity regression (title-via-idx not object identity) covered

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DHT-1`](DHT-1.md) | intra-repo | `done` |
| [`DHT-5`](DHT-5.md) | intra-repo | `done` |

### Unblocks (1)

[`DHT-9`](DHT-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.1`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:185` — | T2.1 | `documents/writers/pptx_writer.py` (python-pptx) + `deck_from_markdown` (`#`/`##` slide boundaries, `<!-- notes: -->` → speaker notes); artifact kind `pptx`; tool `deck_create` | `src/personalclaw/documents/writers/pptx_writer.py`, `from_markup.py`, `artifacts/models.py`, `mcp_artifacts.…
- `T2.1` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:290` — - [2026-07-29][S2] **DONE (T2.1–T2.4).** pptx + pdf writers, `deck_create`, and the

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
