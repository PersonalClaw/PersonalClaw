<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-12` — Two-layer context-compaction ladder for LLM-backed nodes

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #681 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2 Context Lifecycle (two-layer context ladder)

## Done when

proactive compaction at ~80% of the bound model window via a cheap summarizer, then error-triggered aggressive re-compaction before failing the node, degrade-to-drop-with-placeholder if the summarizer fails — driven end to end on a long-horizon template

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-3`](WV-3.md) | intra-repo | `done` |
| [`WV-8`](WV-8.md) | intra-repo | `done` |
| `EXT:CONTEXT-ECONOMY:cheap-summarizer/compaction seam (queue records it does not exist yet)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 27 (WV) — DRIVEN in a real browser

**Code evidence:**

- compaction.py exists and :1-10 states the failure it removes concretely: 'Cycle 50 of a loop carries 50 cycles of findings. Before this module the engine sent that prompt as-is and let the provider decide: a node that had done real work died on a context-length error the engine could have measured and avoided one call earlier'
- 🔑 :14-20 BOTH LAYERS, AND LAYER 2 IS JUSTIFIED AS A BACKSTOP RATHER THAN A DUPLICATE: proactive compaction at ~80% of the bound model's window, then error-triggered aggressive re-compaction retried ONCE on a LENGTH rejection specifically — 'Layer 2 exists because layer 1's measurement is an APPROXIMATION … it is the backstop for the cases the estimate gets wrong, not a duplicate of it'
- :22-30 REUSE, NOT REIMPLEMENTATION: all actual compaction is personalclaw.context_compaction.compact, 'the same seam the native agent loop uses', and re-deriving it 'would be a second implementation that drifts from the first' — with the stake named: an orphaned tool-result 'breaks the provider', 'so getting it wrong is a live failure, not a style question'
- :111-149 the degrade path is real and narrow: _placeholder_body states what was removed, reached ONLY when a supplied summarizer RAISED, and the wrapper catches broadly on purpose ('any summarizer failure degrades, never propagates')
- 5238 tests pass across 107 workflow suites (2 skips, both a template legitimately having no work loop)

**Driven in the UI:** Not driven: 'driven end to end on a long-horizon template' needs many model calls, and no model is bound in this home.

**Notes:** The atom's own dependency line is STALE and worth correcting: it names 'EXT:CONTEXT-ECONOMY:cheap-summarizer/compaction seam (queue records it does not exist yet)'. The seam exists — personalclaw.context_compaction.compact — and this module consumes it rather than re-deriving it, which is the stronger outcome. Partial only for the end-to-end drive.

## Recorded history

### Commits (1)

- `922edfd40` 2026-08-11 — feat(workflows): WV-12 two-layer context-compaction ladder for LLM nodes (#1060)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1351` — **FOLLOW-UP (same PR) — the doc-coverage ratchet caught a missing row for `pinned.py`.** `test_workflows_hardening::TestDocumentationAccuracy::test_the_doc_covers_every_module_that_EXISTS` asserts every `workflows/*.py` has a row in `docs/architecture/workflows.md`; the new module had none. A rea…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2.md:1277` — - [2026-08-11][WV-12] DONE — the two-layer compaction ladder for LLM-backed nodes. A long-horizon node previously died on a context-length rejection the engine could have avoided: it sent the prompt as-is and let the provider decide.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§2`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:16` — + cached badge (`workflowFold.ts`'s `cached?` is declared and read by nothing), and §2's
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:26` — - **WF2-R1** (effect ledger + idempotency + per-def dispatch lock w/ lease + on_overlap policy) → §2 Side Effects, §4 tool idempotency, §5 ledger
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:28` — - **WF2-R3** (engine-owned completion, verification ladder, required_artifacts, typed verdicts, fresh-judge) → §2 Engine-Owned Completion
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:29` — - **WF2-R4** (attempt records w/ mutation hints, circuit breaker, soft budgets, pre-charge + baseline check + amendments) → §2 Failure/Retry/Budgets, resolves Open Question 4
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:30` — - **WF2-R5** (degraded completion, typed error taxonomy, two-knob timeouts, foreach per-item policy + amendments/batch-5 timeout tests) → §1 Node Outcome Model, §2 Timeouts, §8 Slice 11
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:31` — - **WF2-R6** (per-iteration session reset w/ journaled handoffs, decision records, output offloading + amendments) → §1 stage fields, §2 Context Lifecycle
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:33` — - **WF2-R8** (capability-dispatched schema enforcement, output_contract for non-LLM nodes + amendments) → §2 Structured Output
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:35` — - **WF2-R10** (sticky cancel intent, terminal-write ownership, protocol-violation blocked state, workflow_audit) → §2 Termination Robustness, §4 tools

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
