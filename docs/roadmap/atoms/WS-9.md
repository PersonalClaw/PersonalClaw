<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WS-9` — Sources UI in the Knowledge section + as-a-user validation

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WATCHED-SOURCES](../plans/WATCHED-SOURCES.md) (`WS`, plan status `done`) |
| **Atom table** | [`../atomic/WS.md`](../atomic/WS.md) |
| **Execution position** | #669 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1421 |

## Intent — what this is for

§2.4 create flow UI; §6.3 'no AI' chip; health rollups (§12 risk row); §11 step 5 (UI + as-a-user validation). Implementation-owns-product tenet: users can find/create/tune/inspect sources

## Done when

Sources UI in the Knowledge section lists all source kinds with health status, drives the paste-URL preview/tune/save create flow, shows the 'no AI' chip on raw sources, and offers listing-page/render-tier remediation affordances; validated as a user driving web/feed/dir sources end-to-end from the frontend

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WS-2`](WS-2.md) | intra-repo | `done` |
| [`WS-3`](WS-3.md) | intra-repo | `done` |
| [`WS-4`](WS-4.md) | intra-repo | `done` |
| [`WS-5`](WS-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `9eb6a6035` 2026-08-15 — chore(roadmap): keep dag.json in its checked-in escaped form
- `ff6f78f67` 2026-08-15 — feat(knowledge): WS-9 Sources UI with paste-URL preview and health
- `fd3f8c96f` 2026-08-15 — feat(sources): WS-3 web-source detection, selector configs, and escalating fetch
- `8c4f14189` 2026-08-15 — feat(sources): WS-4 cross-source dedup with both attributions

### Named by atom id in the roadmap prose (12)

- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:470` — create a watched source of ANY kind. `WS-9` (Sources UI + as-a-user validation) owns that surface and
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:530` — whole is still store-only. `WS-9` (Sources UI in the Knowledge section + as-a-user validation) owns that
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:532` — nobody can reach, so the user-facing entry lands with `WS-9`.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:798` — brief said "WS-9 shipped the Sources UI"; `WS-9` is still `todo` and WS-4's own log records that
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:902` — cut from, `WS-9` was `todo`, `SourceCreatePage.tsx` did not exist, and `store.create_source` had
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:905` — `?url=` for the "already covered?" answer). **Then `WS-9` landed on `origin/main` (`ff6f78f6`)
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:984` — (687 → 688 agent-callable routes, on top of WS-9's four) — the byte-compare rail.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1010` — the paste-URL create-flow UI, which is `WS-9`'s.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1066` — today. **Follow-up: a dedicated kind alongside the Sources UI (`WS-9`/`WS-10`).**
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1091` — (`WS-9`/`WS-10` own the Sources section) and `run_morning_digest` has no bundled clock trigger
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1189` — dedicated kind (WS-9/WS-10), and the `app:` prefix naming wart.

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `§2.4`, `§6.3`, `§12`, `§11`. Those tokens appear in its source plan at:

- `§2.4` `docs/roadmap/plans/WATCHED-SOURCES.md:75` — async def preview(self, spec: dict) -> SourcePreview:   # dry extraction for the create flow (§2.4)
- `§2.4` `docs/roadmap/plans/WATCHED-SOURCES.md:430` — tier` into `degraded`), `SourcePreview` is the §2.4 dry run, `HEALTH_OK/DEGRADED/ERROR/NEEDS_RENDER`
- `§2.4` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:29` — - **NEW-5** (core) — five-detector auto-source stack + declarative selector configs + preview → §2; outcome-driven escalating fetch under one budget at the `net.fetch` chokepoint → §2.3; guid-keyed novelty gating → §3.3; first-class watched feeds (HN Algolia, GitHub trending, RSS/CSV) with since-…
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:102` — enrichment: str            # "full" | "raw"  — §6.3, raw is guaranteed no-LLM
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:495` — - [WS-4] DONE: **§3 feed kinds + §3.3 cross-source identity + §6.3 raw mode.**
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:510` — surviving item's `also_seen_in`. §6.3 raw mode is kept by ABSENCE in both halves: `FeedItemGraph` has
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§12` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:321` — **~5 sessions**, mapping 1:1 onto §11:
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
