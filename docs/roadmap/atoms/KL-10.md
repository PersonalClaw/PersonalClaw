<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-10` — Indexing H1.3: vector arm searches chunks and rolls up to items before RRF

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #311 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1286 |

## Intent — what this is for

Amendment task table — H1.3; Amendment Design (a); Risks (do not redesign fusion)

## Done when

the vector arm queries chunk vectors and rolls chunk hits up to their item before RRF; fusion, cliff-cut, _VECTOR_MIN_SIMILARITY and the returned item shape are unchanged on a fixed corpus; a chunk hit returns an item-shaped result whose section/line_range locator is asserted at least as specific as today's item-level one; items lacking chunks fall back to the whole-item vector (partial-chunk libraries degrade, never return zero)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-9`](KL-9.md) | intra-repo | `done` |

### Unblocks (2)

[`KL-11`](KL-11.md) (`done`), [`KL-12`](KL-12.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `530826e22` 2026-08-14 — feat(knowledge): KL-12 backfill chunks for libraries ingested before chunking
- `2729ec17b` 2026-08-13 — feat(knowledge): KL-11 index chunk vectors with sqlite-vec, fail-soft to the exact scan
- `3b69c4e9f` 2026-08-13 — feat(knowledge): KL-10 search chunk vectors and roll them up to items before RRF

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:26` — and superseded by `KL-9`/`KL-10`/`KL-11`/`KL-12`.) Created 2026-07-18 (roadmap rev 10; owner ask: more
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:524` — - [2026-08-13][KL-10] DONE: the vector arm now searches CHUNK vectors and rolls chunk hits up to their item before RRF (H1.3). `_vector_search` keeps its exact `[(item_id, rank)] | None` return so `_rrf_fuse` and its `k` are untouched (§Risks: do not redesign fusion) — a chunk id can never reach …
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:543` — **MEASURED, at KL-10's own benchmark shape** (300 items × 5 chunks @ 384 dims = 1,800 scoreable rows), through the real `_vector_search`, ANN on vs the extension force-disabled: **40.13 ms → 2.23 ms per query, 18-20x**, at **recall 1.0000 at k=1/5/10/20 and byte-identical ranked lists on 20/20 qu…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:551` — **Recall tolerance: 0.95 stated, 1.0000 asserted.** Justified by the candidate-generator design (shared scoring ⇒ truncation is the only divergence) plus the completeness stop rule, so the tolerance is a floor, never a target — the test asserts `>= 0.95` AND exact equality, so a regression to a m…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:571` — **This atom is what makes H1.3/H1.4 reach real data.** KL-10 put the vector arm on chunk

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
