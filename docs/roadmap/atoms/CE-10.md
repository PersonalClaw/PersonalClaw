<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE-10` — Adopt the trigger-source seam in the shipped channel apps (retire the 0/4 forward obligation)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CHANNEL-EXPANSION](../plans/CHANNEL-EXPANSION.md) (`CE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CE.md`](../atomic/CE.md) |
| **Execution position** | #123 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #apps#90 + core #2576 |

## Intent — what this is for

Gap filled 2026-09-07 — WF2AUT-8's seam shipped with zero adopters and no atom in the dag required one (issue #2557)

## Done when

Each shipped channel app declares a `trigger_source` provider so an `app:<name>:<event>` trigger arms from real inbound traffic rather than from a test fixture; adoption is MEASURED at N/N over the channel apps a rail DISCOVERS rather than against a remembered count, so a regression reds in either direction, and an unparseable manifest, an empty `providers: []`, and a declaration whose implementation does not resolve are each reported as their own state instead of collapsing into a quietly lowered ratio; the conformance kit's completeness advisory stops firing on those apps for a missing TRIGGER source (its inbox arm is CE-8-shaped work for three more vendors and is deliberately out of scope here); and no bespoke event glue is added — the apps reach the bus only through WF2AUT-8's registered TriggerSourceTypeHandler, stated over Python NAMES via AST rather than raw text: `emit_event`, `dispatch_event`, `event_bus` and `register_source` at zero LIVE references, `SourceEvent`/`TriggerSourceProvider` bound only from `personalclaw.sdk.trigger_source`, and no app naming `triggers.json` in code.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CE-8`](CE-8.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (8)

- `5057798b1` 2026-09-07 — docs(roadmap): two reasons described a world that no longer exists
- `f572f6258` 2026-09-07 — docs(ecosystem): a bounty row whose gap closed kept soliciting built work
- `b9409e6c0` 2026-09-07 — docs(roadmap): WF2UNI-14 named the wrong blocker, and its scope would falsify a done atom
- `ad06a3cff` 2026-09-07 — feat(roadmap): make the dashboard say WHY an atom is not moving
- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `9a8325805` 2026-09-06 — feat(ce10): the completeness advisory grows its trigger-source arm
- `ccb4541b8` 2026-09-06 — docs(roadmap): flip EI-6, and fix four gates that were wrong about why they were gated
- `281d693b1` 2026-09-06 — docs(roadmap): execute the 2026-08-28 cycle ruling, and mint the atom for a seam nobody adopted

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:582` — **The second half now HAS an owner atom: `CE-10`, minted 2026-09-07.** It did not when this note
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:588` — [#2557](https://github.com/PersonalClaw/PersonalClaw/issues/2557) and then minted as `CE-10`, whose
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:615` — - **`CE-10` minted — a shipped seam with nobody scheduled to adopt it.** Adoption of `WF2AUT-8`'s
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:620` — - **2026-09-07 — `CE-10` closes the same day it was minted, and its own `done_when` had to be

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
