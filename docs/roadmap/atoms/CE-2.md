<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE-2` — Migrate the Slack app onto the core trust seam with a one-time loud migrate_to_core_trust()

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CHANNEL-EXPANSION](../plans/CHANNEL-EXPANSION.md) (`CE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CE.md`](../atomic/CE.md) |
| **Execution position** | #115 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 — T1.4 (apps repo slack-channel); Design §S1 'Slack migration'

## Done when

slack-channel persist_allowed_user/persist_tracking_channel delegate to core channel_trust; migrate_to_core_trust() moves app-local JSON into the core store once (idempotent, logged, second run no-op per fixture); Slack app tests green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CE-1`](CE-1.md) | intra-repo | `done` |

### Unblocks (2)

[`CE-6`](CE-6.md) (`done`), [`CE-8`](CE-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `64aa3088e` 2026-08-09 — chore(roadmap): reconcile dag.json — CE-2/SV-6/WF2WOR-8 merged → done (#932)
- `17dc1a9f7` 2026-08-07 — feat(channels): CE-1 sender-trust core seam — the channel-trust contract for CE-2..9 (#895)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T1.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:106` — - **Called by:** every channel transport (slack migrates onto it in S1 T1.4; Telegram/Discord/email consume it); plan 24 §3 inherits this seam (does NOT rebuild it).
- `T1.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:120` — | T1.4 | Slack app onto the seam: `persist_allowed_user/tracking_channel` delegate to core trust; one-time loud `migrate_to_core_trust()` moving its JSON into the core store (idempotent, logged) | apps repo: `slack-channel/slack_runtime/allowlist.py`, `settings.py` | slack tests green; migration …
- `T1.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:389` — **FINDING — Slack fails one MUST clause, and the gap is asserted rather than annotated.** Telegram, Discord and email pass in full. Slack fails exactly `[fencing]`: it predates the CE-1 trust seam, still owns `slack_runtime/allowlist.py`, and its runtime never calls `guard_inbound` — **T1.4 of th…
- `Session 1` `docs/roadmap/plans/CHANNEL-EXPANSION.md:113` — ### Session 1 — Trust seam (core)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
