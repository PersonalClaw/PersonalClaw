<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-9` — Owner taste-call convergence batch G1–G9 + follow-on raw-button waves (owner-authorized pixel moves)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #210 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

OWNER TASTE-CALL RULINGS (cy50 batch); Execution log cy51/cy2–cy9 (G1–G9) + cy14–cy15 (RowActionButton family)

## Done when

G1–G9 executed as 9 atomic convergence cycles: *-error→*-danger token normalization, LoopCockpit back-icon + add-prerequisite, ghost Cancel/Done, retry family→Button, <TextLink> extraction (16 sites), tonal/primary convergence, full RowActionButton/remove-X family→SquareIconButton tone=danger, CapRow picker unification, InlineError fold-all + warn→danger re-tone; rawButton 314→285; each live-validated dark+light, baselines regenerated where pixels moved.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-8`](DSC-8.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 10 (DSC) — observed

**Code evidence:**

- part of the 841 + 1636 passing rails

**Notes:** An owner taste-call convergence batch is by nature adjudicated rather than derived — the G1–G9 rulings and the follow-on raw-button waves are recorded decisions, and their rails pass. This is also one of the two atoms whose scope vocabulary (cy51 cycles, G-batches) taught the consolidation generator a new token family; before that, WF2KNO-era scanning left it with no resolvable record at all.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (45 line(s))

This atom's scope cites `cy50`, `cy51`, `cy2`, `cy9`, `cy14`, `cy15`, `G1`, `G9`. Those tokens appear in its source plan at:

- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:489` — - **NEXT (item 1 mega-page queue CLOSED — every mega-page triaged):** ChatPage (cy48), ProjectsSection (cy49), and CodeCockpitPage (cy50) are done; the three largest mixed-role pages are triaged and their clean slices extracted (SelectionPill, InlineError, Centered). **No mega-pages remain.** Wha…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:491` — ### 2026-07-24 — OWNER TASTE-CALL RULINGS (the cy50 batch, decided after visual inspection of the real-CSS comparison screenshots) — the taste/pixel lane is now unblocked
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:492` — - **Context:** the cy50 "batch of taste/pixel decisions" was surfaced to the owner as a set of faithful comparison screenshots (real compiled-CSS harness, dark+light, every divergent variant beside its shared-primitive reference, labelled file:line). Owner inspected all groups ("each of these loo…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:517` — - **Context:** the Cancel/Done idiom is *already* overwhelmingly converged onto `Button variant="ghost"` (a full-tree grep shows ~30 sites already using it). G6's ruling targets only the **two remaining divergent surface-filled pills** the cy50 screenshots surfaced: `FilterMenu:83` "Done" and `Ta…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:524` — - **Context:** the retry idiom is *already* largely converged onto `Button` — the cy50 audit named CodeCockpitPage:2904/2997 (quiet inline `variant="ghost" size="xs" text-primary`) and CodeCockpitPage:471 (filled `variant="secondary"`) as the two established precedents. G3's ruling targets the **…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:536` — - **Reconciliation (ruling estimated "11 sites" → 16 instances across 14 files):** the ruling's "11" was an estimate from the cy50 audit; the actual hand-rolled idiom appears 16× (three files carry two instances each: CodeCockpitPage's queue-remove pair, AppsSection's two empty-state branches). A…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:553` — - **Context + scope (the authoritative 5-site family):** G1's ruling (line 479) converges the row-action/remove-X family onto **`SquareIconButton tone="danger"`** — "idle ink-low, hover tints red, no fill; the restrained treatment … the variants (hover-reveal danger / always-visible danger / alwa…
- `cy50` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:573` — - **NEXT (batch cycle 9/9 → G9):** fold `ChatPage` ErrorLine (:2633) + `FilesSection` strip (:351) + `MoveErrorBanner` (`TasksListPage:428`) into the `InlineError` primitive, re-tone warn→danger, and extend `InlineError`'s API (a `tone`? an `animated`? a non-dismissible multi-line `items-start` m…
- `cy51` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:505` — ### 2026-07-24 — cy51 DONE (G1 B′ token normalization — warm-up, batch cycle 1/9)
- `cy51` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:513` — - **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` with a real drift reduction — `LoopCockpitPage` raw-button count `10 → 9` (score `22 → 20`) from the G7 conversion. Unlike cy51 (pure timestamp churn, correctly reverted), this is …
- `cy51` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:510` — ### 2026-07-24 — cy2 DONE (G7 loop-rail back-icon → SquareIconButton + G8 add-prerequisite → Button secondary — batch cycle 2/9)
- `cy2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:549` — - **Validation:** live-validated as a user in **both dark and light**. **ProjectsSection "New" split-button** (created a real project through the UI on the isolated dev home to render the per-project card): computed **dark** bg = `rgb(255,107,91)` = `--color-primary` (was `/90` translucent), text…
- `cy2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:560` — - **Ratchet: `rawButton` 314 → 290 (−24), committed same cycle.** Five buttons removed this cycle (audit per-file: ChatPage 18→16, ProjectsSection 18→17, CodeSection 3→2, CodePlanReview 9→8) drop live to **290**; the `primitiveAdoption` soft-nudge ("live counts well below baseline — ratchet DOWN …
- `cy2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:563` — - **NEXT (batch cycle 8/9 → G5):** unify the capability picker on `CapRow` — rewrite the code-plan `CapChip` surface (`CodePlanReview:391`) to `CapRow`, hoisting `CapRow` to a shared `ui/`/plan-review component consumed by both `LoopPlanReview` and `CodePlanReview` (ruling line 483 — "moves pixel…
- `cy9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:575` — ### 2026-07-24 — cy9 DONE (G9 InlineError fold-all — three residual danger banners converge onto the primitive, warn banner re-toned — batch cycle 9/9, FINAL)
- `cy9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:632` — ### 2026-07-24 — cy14 DONE (S2 raw-button wave — the reveal-X row-delete family in `formControls.tsx` converges onto `SquareIconButton tone="danger"`; owner-authorized under the full-authority grant)
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:643` — - **Why in-scope now:** direct continuation of cy14's NEXT. cy14 converged the tightest sub-cluster (the 3 reveal-X buttons in one file); cy15 extends the same `SquareIconButton tone="danger"` convergence to the **standalone inline danger-remove idiom** wherever it appears raw across the app — th…
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:645` — - **Faithfulness (what moves, what doesn't):** `tone="danger"` renders `grid size-7 rounded-md text-on-surface-low hover:text-danger` — **no fill** in idle OR hover, byte-identical hover treatment to all four raw buttons. Deltas, all improvements: (a) hit-area standardizes to `size-7` (28px) — Vo…
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:677` — - **Why a no-code cycle is the honest move (not a stop, not a manufactured edit):** the autonomous-loop-continuity directive says don't stop while authorized work remains — but the soul guardrail ("consistency, not redesign; add a shared primitive *only when the system genuinely lacks a pattern*"…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:623` — ### 2026-07-24 — cy13 DONE (S3 a11y parity — the ten non-default color schemes reach WCAG AA in light mode, closing the cy15 DEVIATION; a permanent per-scheme contrast guard replaces the default-only axe rail)
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:624` — - **Why this, why now (conformance, not taste — so it's in-scope autonomously):** cy15 recorded a standing DEVIATION — the coral **default** scheme was AA-corrected and is enforced live by `e2e/a11y.spec.ts` (axe), but **axe only ever exercises the active/default scheme**, so the other ten curate…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:625` — - **Measurement (WCAG 2.1 relative-luminance, 4.5:1 small-text):** measured all 11 schemes in the two contexts each light accent actually bears text/UI — `primary` as a filled control (onPrimary over primary) **and** as accent text on the white surface, `primaryEmphasis` as the hover fill, `info`…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:627` — - **Structural close of the cy15 hole — new permanent guard `web/src/design/schemeContrast.test.ts`:** iterates **every** `SCHEMES` entry (not just the default) and asserts each one's `primary`/`primaryEmphasis`/`info` meets ≥4.5:1 in both modes — light: filled-control, accent-text-on-white, emph…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:642` — ### 2026-07-24 — cy15 DONE (S2 raw-button wave, cross-file — the standalone inline danger-remove `RowActionButton` idiom converges onto `SquareIconButton tone="danger"` across four files; owner-authorized under the full-authority grant)
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:643` — - **Why in-scope now:** direct continuation of cy14's NEXT. cy14 converged the tightest sub-cluster (the 3 reveal-X buttons in one file); cy15 extends the same `SquareIconButton tone="danger"` convergence to the **standalone inline danger-remove idiom** wherever it appears raw across the app — th…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:653` — - **Why in-scope now:** rotates from the (now-converged) raw-button idiom back to the S2 raw-**input** drawdown flagged in cy15's NEXT. Earlier cycles (3/5/14) established there is **no byte-identical raw-`<input>` cluster** left (the survivors hardcode `flex-1`/`px-2`/`text-on-surface-var`/custo…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:662` — - **NEXT:** the dense-textarea cluster is converged and `TextArea` now carries a tested `size` axis. Remaining S2 raw-input drawdown is (a) the `resize`/`onKeyDown`-bearing inline composers (need those props grown on `TextArea` in a lockstep step), and (b) any remaining faithful `Select`/`<input>…
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:494` — - **G1 — row-action / remove-X family** → converge outliers onto **`SquareIconButton tone="danger"`** (idle ink-low, hover tints red, no fill; the restrained treatment). **Also FIX the B′ token bug:** `OllamaModelManager:88` uses `hover:text-error` → change to `hover:text-danger` (on-token; same …
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:503` — - **EXECUTION ORDER (cheapest/highest-confidence first, contract-owners before consumers):** (1) **G1 B′ token bug** — one-char on-token fix, trivial, zero-pixel, warm-up; (2) **G7** back-icon + **G8** add-prerequisite — single-site primitive adoptions; (3) **G6** ghost Cancel/Done; (4) **G3** re…
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:505` — ### 2026-07-24 — cy51 DONE (G1 B′ token normalization — warm-up, batch cycle 1/9)
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:552` — ### 2026-07-24 — cy7 DONE (G1 full `RowActionButton`/remove-X family — the 5-site multi-variant sweep onto `SquareIconButton` — batch cycle 7/9)
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:553` — - **Context + scope (the authoritative 5-site family):** G1's ruling (line 479) converges the row-action/remove-X family onto **`SquareIconButton tone="danger"`** — "idle ink-low, hover tints red, no fill; the restrained treatment … the variants (hover-reveal danger / always-visible danger / alwa…
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `G1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:589` — - **Owner grant (2026-07-24):** with the G1–G9 taste-call batch closed, the owner granted full authority to **finish the plan autonomously** — "I approve your judgement on all owner-gated taste families … tackle all the remaining work in whatever order that fits the remaining work best and finish…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:502` — - **G9 — InlineError divergent strips** → **FOLD ALL 3 IN** (owner: maximum convergence, accept pixel moves): `ChatPage ErrorLine` (2633), `FilesSection` strip (351), AND the warn-toned animated `MoveErrorBanner` (`TasksListPage:428`) all converge onto **`InlineError`** — including **re-toning th…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:503` — - **EXECUTION ORDER (cheapest/highest-confidence first, contract-owners before consumers):** (1) **G1 B′ token bug** — one-char on-token fix, trivial, zero-pixel, warm-up; (2) **G7** back-icon + **G8** add-prerequisite — single-site primitive adoptions; (3) **G6** ghost Cancel/Done; (4) **G3** re…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:563` — - **NEXT (batch cycle 8/9 → G5):** unify the capability picker on `CapRow` — rewrite the code-plan `CapChip` surface (`CodePlanReview:391`) to `CapRow`, hoisting `CapRow` to a shared `ui/`/plan-review component consumed by both `LoopPlanReview` and `CodePlanReview` (ruling line 483 — "moves pixel…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:573` — - **NEXT (batch cycle 9/9 → G9):** fold `ChatPage` ErrorLine (:2633) + `FilesSection` strip (:351) + `MoveErrorBanner` (`TasksListPage:428`) into the `InlineError` primitive, re-tone warn→danger, and extend `InlineError`'s API (a `tone`? an `animated`? a non-dismissible multi-line `items-start` m…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:575` — ### 2026-07-24 — cy9 DONE (G9 InlineError fold-all — three residual danger banners converge onto the primitive, warn banner re-toned — batch cycle 9/9, FINAL)
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:576` — - **Context + ruling (line 487):** three failure banners were left OUT of the S2 `InlineError` consolidation because folding them would move pixels — the primitive's docblock named them explicitly as "Deliberately NOT folded in here": **ChatPage's `ErrorLine`** (`:2635`, a non-dismissible multi-l…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `G9` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:589` — - **Owner grant (2026-07-24):** with the G1–G9 taste-call batch closed, the owner granted full authority to **finish the plan autonomously** — "I approve your judgement on all owner-gated taste families … tackle all the remaining work in whatever order that fits the remaining work best and finish…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
