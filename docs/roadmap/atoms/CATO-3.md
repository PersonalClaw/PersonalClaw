<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-3` — Subagent attribution — replace the discard, add cost fields to SubagentInfo

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #99 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

C2 — Attribution (subagent row); Context §3 (subagents discard the completion event today); Session 1 T1.3

## Done when

The EVENT_COMPLETE discard at subagent.py:1704 is replaced with a record_turn(source='subagent'); SubagentInfo (subagent.py:262) gains input_tokens/output_tokens/cost_usd carried onto the existing completion delivery; a 3-way fan-out yields 3 rows and the parent completion reports each child's cost; tests green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-1`](CATO-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:193` — - **CATO-3 DONE — the subagent write-site (C2, 2 of 4).** The `EVENT_COMPLETE` handler in
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:215` — own EVENT_COMPLETE loop (`source="cli"`). Refactored CATO-2's `_record_turn_usage` and CATO-3's

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.3`, `§3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.3` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:133` — | T1.3 | Wire `subagent`: replace the completion-event discard at `subagent.py:1684-1685`; add `input_tokens`/`output_tokens`/`cost_usd` to `SubagentInfo` (`subagent.py:262`) and carry them onto the existing completion delivery | `src/personalclaw/subagent.py`, tests | a sub-agent run writes a ro…
- `Session 1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:127` — ### Session 1 — The durable ledger

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
