<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR-2` — Fail-actionable FTS5 guards at the 6 FTS5 consumers' store-init (require_fts5 / documented degrade)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-REACH](../plans/PLATFORM-REACH.md) (`PR`, plan status `done`) |
| **Atom table** | [`../atomic/PR.md`](../atomic/PR.md) |
| **Execution position** | #473 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #831 |

## Intent — what this is for

Contracts C1 (require_fts5, remedy text, §2.7 fail-closed-for-capability); Task A1.2

## Done when

each of the 6 FTS5 modules checks sqlite_features().fts5 once at init and raises/degrades with the fixed remedy text; a monkeypatched no-FTS5 probe fixture produces the actionable error/degradation at init, never a mid-query traceback; per-module degrade-vs-raise choice recorded in the Execution log

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PR-1`](PR-1.md) | intra-repo | `done` |

### Unblocks (1)

[`PR-3`](PR-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/PLATFORM-RESILIENCE.md:752` — ## Execution log — PR-2 (FTS5 capability guard at init)
- **source plan** `docs/roadmap/plans/PLATFORM-RESILIENCE.md:754` — - **PR-2 DONE.** Every FTS5-dependent module now checks `sqlite_compat.probe().fts5` ONCE at init and

### Its plan's own record, via the scope references (0 line(s))

Its scope cites `§2.7`, but no line of its source plan carries any of them.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
