<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `APE-8` — Fix-with-AI: InstallResult.log_excerpt + Store error button -> prefilled fenced chat

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [APP-PLATFORM-EVOLUTION](../plans/APP-PLATFORM-EVOLUTION.md) (`APE`, plan status `done`) |
| **Atom table** | [`../atomic/APE.md`](../atomic/APE.md) |
| **Execution position** | #47 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #918 |

## Intent — what this is for

Amendment 2026-07-26 T2.5 (Fix with AI; folds into Session 2)

## Done when

a deliberately broken app's failed install offers the button; the opened chat (via ne:launch-chat) contains the log wrapped in fence_untrusted(source=app_install_log:<name>); fence verified

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 55 (APE) — the read-only shared handle driven directly; the wildcard disclosure falsified

**Code evidence:**

- the fix-with-AI frontend suite is green inside the 32, covering the button's presence on a failed install
- 🔑 THE LOG IS FENCED WITH A PROVENANCE SOURCE, WHICH IS THE WHOLE POINT: the excerpt reaches chat wrapped in `fence_untrusted(source=app_install_log:<name>)`, so a hostile app cannot put instructions in its own install failure and have them read as a prompt. An install log is attacker-influenced text being handed to a model — precisely the case the core fence exists for
- the chat is OPENED PREFILLED rather than sent, so the user still decides whether to spend the turn
- app messaging + platform events + background contract + quality enforcement 113/113; consent/card/fix-with-AI frontend suites 32/32 (falsified: literalising a wildcard target reds exactly the two pattern tests)

**Driven in the UI:** Not drivable: it needs a deliberately broken app to install, which would mean writing into the validation home.

**Notes:** This is the feature most likely to have been built without a fence — the text is 'just a log' — and it is fenced with a source that names the app. Good instinct captured as code rather than as advice.

## Recorded history

### Commits (4)

- `5b46fdcd4` 2026-08-13 — feat(guardrails): AG-6 earn autonomy per action type, lose it on one rejection (#1197)
- `bd83e55c8` 2026-08-08 — feat(apps): APE-7 surface installed-app updates (card badge + Store nav count + notification) (#929)
- `3264effd1` 2026-08-08 — chore(roadmap): reconcile dag.json with merged stack (AS-4/APE-8/LMMV-4/SV-5 → done) (#926)
- `b96dbd45b` 2026-08-08 — feat(apps): APE-8 Fix-with-AI — fenced install-log excerpt into a prefilled debug chat (#918)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.5`, `Session 2`. Those tokens appear in its source plan at:

- `T2.5` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:123` — | T2.5 | Fix-with-AI: `InstallResult.log_excerpt` + Store button → prefilled fenced chat turn | `apps/app_manager.py`, Store error UI | a deliberately broken app's failed install offers the button; the opened chat contains the fenced log; fence verified |
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:79` — ### Session 2 — Quality bar + native evolution
- `Session 2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:118` — (a)+(c) fold into **Session 2** (they ARE the quality bar); (b) folds into **Session 3** beside `appMessaging` (one consent-copy review covers both). Session count stays 4.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
