<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-2` — Derive `needs` from bindings; lift the sibling-only restriction

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #461 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1339 |

## Intent — what this is for

`PP-1` makes the disagreement visible; this deletes the second edge list. Fluxtion's "Graph Engineering Needs a Compiler" (2026-07-29) names the exact defect — an explicit orchestration definition that restates relationships already present in the components is "a second edge list", and the remedy is to declare structure once and derive coordination. The sibling-only restriction exists to protect a hand-maintained ordering list from being wrong; once ordering is derived, the dependency graph is already global and `_kahn_levels` already proves it acyclic, so reachability can be computed over the derived graph and the restriction becomes unnecessary rather than load-bearing. This is the single biggest expressiveness gap versus the peer graph runtimes: a diamond spanning two containers is inexpressible today.

## Done when

Ordering edges are DERIVED from bindings at definition time and consumed by the frontier; a hand-written `needs` survives only to express ordering that is not dataflow (a lock, an external side-effect sequence) and is checked against the derived set rather than trusted. `WF_UNKNOWN_NEEDS`' sibling-only rule is replaced by a global reachability computation over the derived graph, so a cross-container `needs` is honoured instead of refused, and a `branch` decline still marks exactly the unreachable derived targets SKIPPED (WF2-R18 preserved: a join must neither deadlock on an untaken leg nor fire early on a live one). Verified by a run whose diamond spans two containers completing correctly, and by the WF2-R18 join tests staying green. The `to_skip` reachability change is the risk surface and gets its own test matrix over decline-inside-parallel, decline-inside-foreach-body and decline-with-a-cross-container-reader.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-1`](PP-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- `needs` is derived from bindings rather than hand-declared, which is what makes PP-1's census finding survivable: zero templates declare `needs`, so a rule resting on hand declaration would have had nothing to check
- 🔑 LIFTING THE SIBLING-ONLY RESTRICTION IS THE REAL CHANGE — a dependency that could only be expressed between siblings forces authors to flatten a tree to say a true thing, and the flattening loses the grouping that made the template readable
- the bundled-template suite is 388/388 with the derivation live across 18 of 19 templates
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not a browser surface.

**Notes:** Deriving ordering from the dataflow that already exists is the plan's edges thesis in its cleanest form — the binding is the declaration, so the two cannot disagree.

## Recorded history

### Commits (2)

- `94711166d` 2026-08-15 — docs(roadmap): flip PP-2/PP-5/PP-10/PP-14 to done (batch cherry-pick lost their plans[] edit)
- `5c3153c8a` 2026-08-14 — feat(workflows): PP-2 derive ordering from bindings; lift sibling-only needs

### Named by atom id in the roadmap prose (13)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:85` — `PP-1` makes the disagreement a typed error. `PP-2` derives ordering from bindings and — because the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:138` — PP-1 ─→ PP-2, PP-3
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:168` — `PP-1`, `PP-2`, `PP-3`, `PP-11`, `PP-14` and `PP-15` are pure-logic and can land at any time.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:180` — Take the inference (`PP-2`), leave the codegen.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:804` — action-node contract (or `PP-2`, which will move these edges) should widen that allowlist in the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1040` — - **2026-08-14 — `PP-2` DONE.** Ordering is now DERIVED from bindings and consumed by the frontier;
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1047` — - **2026-08-14 — `PP-2` composition with `PP-11`.** Derived ordering and admission are ORTHOGONAL
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1052` — earlier PP-2 attempt threaded `wip: bool` through `tick`; that mechanism no longer exists after
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1054` — - **2026-08-14 — `PP-2` the "restriction is unnecessary" argument HELD against the post-`PP-11` code.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1060` — - **2026-08-14 — `PP-2` no existing schedule changed (the honest proof).** `PP-11`'s bundled
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1066` — - **2026-08-14 — `PP-2` the `to_skip` reachability change (the named risk surface).** The asymmetry:
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1076` — - **2026-08-14 — `PP-2` DEVIATION (plan step 3).** The plan asked for a warning on a `needs` ABSENT
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1084` — - **2026-08-14 — `PP-2` DISCOVERY.** A pure frontier cannot re-derive a per-item BRANCH decline whose

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
