<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2WOR-4` — Run workspace + worktree execution wiring + code-run cockpit

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-WORK-CONTAINERS](../plans/WORKFLOWS-V2-WORK-CONTAINERS.md) (`WF2WOR`, plan status `done`) |
| **Atom table** | [`../atomic/WF2WOR.md`](../atomic/WF2WOR.md) |
| **Execution position** | #652 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##188,#191 |

## Intent — what this is for

§4.1 (workspace-provisioning block), §4.2 (folder contracts), Migration Order steps 2 & 5

## Done when

Success Criterion 7: plan_provisioning/pending_setup/plan_teardown are wired into the controller run-start path and executed (subprocess setup/teardown); PID-liveness lock files + preserved_workspace_path land on the run record; workspace_default_mode/workspace_teardown_on_expiry config defaults wire through all four points; a code-kind run provisions a worktree with preserve_patterns + idempotent setup, survives resume, tears down before deletion, and the cockpit diff panel + Apply Locally/Checkout Branch verbs work end-to-end

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2WOR-1`](WF2WOR-1.md) | intra-repo | `done` |

### Unblocks (2)

[`WF2WOR-12`](WF2WOR-12.md) (`done`), [`WF2WOR-5`](WF2WOR-5.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 66 (WF2WOR) — the archive extractor's path safety falsified and found unasserted (fixed in #2825); the Work board driven in a real browser

**Code evidence:**

- `workflows/worktrees.py` and `workflows/provisioning.py` both import `containers`, and the worktree suite is green inside the 555
- 🔑 PID-LIVENESS LOCK FILES RATHER THAN A FLAG: a lock whose holder is a dead process is a lock nobody can clear, and a liveness check is what stops a crashed run from stranding its workspace forever
- `preserved_workspace_path` on the run record is what makes a post-mortem possible — a teardown that always ran would delete the evidence of the failure that needed it
- 555 passed across the containers, needs-input, export, container-workspace, publish, filedrop, introspection, fan-out, worktree and memory-locality suites; the Work board and a project detail page driven on :10011

**Driven in the UI:** Not driven: needs a code run with a bound workspace.

**Notes:** Setup and teardown executing as real subprocesses (rather than as declared intent) is the clause worth re-checking after any controller refactor — a provisioning step that silently no-ops looks identical to one that succeeded.

## Recorded history

### Commits (1)

- `8b4644b61` 2026-08-09 — feat(workflows): WF2WOR-4 run workspace provisioning + teardown wiring + code-run cockpit (#967)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:21` — structurally unmet. **Criterion 7 is MET as of 2026-08-10 (WF2WOR-4):** `workspace`/`worktrees` now
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1198` — - **2026-08-10 — DONE — WF2WOR-4 (§4.1, criterion 7): the workspace block is now REAL, and both

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§4.1`, `§4.2`. Those tokens appear in its source plan at:

- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:36` — - **WORK-R3** — workspace-provisioning block {mode, preserve_patterns, setup, teardown} + locking/teardown bundle + durable-branch persistence → §4.1
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:149` — 1. **Isolation by default:** filesystem-touching parallel leaves each get an isolated worktree/scratch dir via the §4.1 workspace block (mode `scratch` unless the task declares `worktree`); a **compile-time lint warns when >1 concurrent worker holds Write to the same artifact/dir** (single-Write-…
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:229` — - **Work**: state-grouped rows — *Needs input* pinned first, then Working / Queued / Suspended / Ready for review / Done (the Jules/Claude-agents board anatomy + §5.2's truthful states). Rows = runs + legacy loops + tasks, with cheap-model one-line summaries, §1.5 claim badges, and §4.1 `origin: …
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:247` — Live node tree, journal timeline, checkpoint rail with fork buttons, gate cards showing the exact pending question with inline reply (the NeedsInputItem card, same component), plan artifact with comment-to-revise, spec editor (the graph is a VIEW over the spec file on disk — eject-hatch principle…
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:288` — | `loop/worktree.py` | **REUSE** as the worktree-mode backend of the §4.1 workspace block |
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:303` — 2. Workspace block (§4.1: worktree/in_place/scratch modes, preserve_patterns, setup/teardown idempotency, locking bundle, run-owned resource cleanup) + folder contracts (§4.2) — before any FE, since the batch compile depends on it.
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:377` — - **(c) Writes stay single-threaded.** A `parallel[stage]` leaf is **read/analysis-only by default**; a leaf that mutates shared state must be serialized by the engine. This aligns with §3's existing per-leaf `capability: research|mutating` field — which this amendment promotes from a field to an…
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:603` — tool-handler depth flag, the §4.1 workspace block, and the fact that the engine's node timeout is
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:51` — - **WORK-R18** — folder contracts (.folder.yaml, lifecycle incl. ttl_staging) → §4.2
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:76` — ├─ context/ + worktrees/  (existing project dirs, threaded into runs; folder contracts §4.2)
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:130` — 5. **Per-run file drop + outbox (R17).** Each run exposes: an **inbound file drop** (approval-gated multipart ingestion — size cap, atomic tmp+rename via `atomic_write_bytes`, SEL audit entry per file; explicit human approval showing what + size, unless the template declares auto-accept for speci…
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:303` — 2. Workspace block (§4.1: worktree/in_place/scratch modes, preserve_patterns, setup/teardown idempotency, locking bundle, run-owned resource cleanup) + folder contracts (§4.2) — before any FE, since the batch compile depends on it.
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1311` — - **DEVIATION — §4.2 folder contracts got no new wiring in this atom.** The atom's scope line names
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1312` — §4.2, but `parse_folder_contract`/`may_write`/`validate_frontmatter` are consumed by the FILE-WRITE
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1316` — inert-control shape in reverse. The `SETUP_MARKER_DIR` half of §4.2's contract vocabulary IS live

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
