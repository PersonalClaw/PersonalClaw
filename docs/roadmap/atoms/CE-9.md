<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE-9` — Ramp coordination: community bounty issues + channel scaffold registration + trigger-source forward note

| | |
|---|---|
| **Status** | `blocked` |
| **Plan** | [CHANNEL-EXPANSION](../plans/CHANNEL-EXPANSION.md) (`CE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CE.md`](../atomic/CE.md) |
| **Execution position** | #122 of 684 in dependency order |
| **Startability** | not startable |
| **Blocked reason** | OWNER-GATED on one clause; the other two are MET and evidenced in-tree (verified 2026-09-06, PR #2559 merged 00be7c0ea). CLAUSE 2 — the `channel` template registered with ECOSYSTEM-TOOLING's scaffold — MET, and satisfied BY CONSTRUCTION rather than by an edit: ET-1's type table is DERIVED from PROVIDER_TYPES plus the provider registry, so there was never a registration to make and no DISCOVERY note is warranted. Re-verified live from a clean tmp dir rather than taken from another PR's word: `personalclaw app new my-whatsapp --type channel` emits exactly six files, provider.py imports ChannelTransportProvider from personalclaw.sdk.channel and subclasses it, the generated tests pass 7/7, and `--list-types` prints 19 runtime-derived types carrying both `channel` and `trigger_source`. CLAUSE 3 — the coordination line, with no bespoke early event glue — MET BY #2559, and it was NOT met before: the only line that existed was future-tense in the substrate's own plan ('the unified TriggerService must accept'), while this plan's cross-refs said 'once ... exposes' and the channel guide told contributors 'until it lands'. The seam had in fact shipped. Verified against code rather than the substrate plan's log: `trigger_source` is in PROVIDER_TYPES AND has a registered TriggerSourceTypeHandler wired in get_provider_registry(), with tests/test_trigger_sources.py green at 33 passed including an end-to-end declared-source-fires-an-event-trigger case. THE NEGATIVE HALF HOLDS BUT FOR AN UNCOMFORTABLE REASON, so it is recorded both ways rather than as a pass: across PersonalClawApps, emit_event / SourceEvent / event_bus / register_source / dispatch_event / triggers.json are ALL zero hits and the four channel apps' inbound loops all terminate in personalclaw.sdk.channel.run_chat — so no bespoke glue shipped. But it holds because NOTHING exists at all: adoption is 0/4 and the only implementer anywhere is a test fixture. Recording only the true negative would have read as a discharged obligation, so #2559's COORDINATION block states both halves in a table with the apps half marked OUTSTANDING, machine-checked by tests/test_ce9_coordination.py. That gap is now atom CE-10 and issue #2557. SUPERSEDED 2026-09-07 -- READ THE SNAPSHOT ABOVE AS HISTORY, NOT AS CURRENT STATE: CE-10 is DONE and adoption is 4/4, not 0/4. All four shipped channel apps declare a trigger source at apps origin/main (slack_runtime/trigger_source.py, telegram_runtime/, discord_runtime/, email_runtime/), each with its own tests/test_trigger_source.py proving the source really FIRES, measured by the repo rail .github/scripts/check_trigger_source_adoption.py which reds in BOTH directions. So "ALL zero hits" and "the only implementer anywhere is a test fixture" were TRUE WHEN WRITTEN and are false now -- CE-10 is precisely what changed them, which is the system working, not a defect here. Recorded because a reader acting on the stale numbers would re-open a discharged obligation. ONE REAL DEFECT DID FALL OUT OF THIS, and it was downstream rather than here: docs/maintainers/app-bounty-wants-list.md row 7 still solicits a community bounty for exactly the trigger sources CE-10 shipped, so CE-10 closing never propagated to the document that cites the gap. That document is a DERIVED claim about apps-repo state with nothing linking it to the rail that would invalidate it. Being fixed separately; ET-7 is gated only on PUBLISHING, so an unfixed row 7 would have put a bounty for finished work in front of a stranger. CLAUSE 1 REMAINS OWNER-ONLY: docs/maintainers/community-bounty-drafts.md still marks the shared risk-policy paragraph PENDING OWNER APPROVAL, with all three drafts carrying a placeholder where it goes. Independently confirmed the labels do not exist yet — `gh label list` has neither `bounty` nor `community-tier`. Nothing was posted and no label was created. SMALLEST REMAINING UNIT: the owner approves or edits that paragraph, the three drafts are posted as issues labeled community-tier + bounty (creating both labels), and the approval is recorded in this plan's execution log. |

## Intent — what this is for

Sessions 7-8 — Ramp: T7.3 + Amendment T7.6; Design §S7-8; owner task 5 (risk-policy approval)

## Done when

WhatsApp/Signal/Matrix GitHub issues live and labeled community-tier with the risk-policy paragraph and guide+kit links (after owner approves the risk-policy paragraph); the `channel` template is registered with ECOSYSTEM-TOOLING's scaffold (or a DISCOVERY note filed if the scaffold has not landed); a coordination line into WORKFLOWS-V2-AUTOMATION-SUBSTRATE records the app-registered trigger-source forward obligation with no bespoke early event glue shipped

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CE-7`](CE-7.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2-AUTOMATION-SUBSTRATE:app-registered trigger source types` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (10)

- `5057798b1` 2026-09-07 — docs(roadmap): two reasons described a world that no longer exists
- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `281d693b1` 2026-09-06 — docs(roadmap): execute the 2026-08-28 cycle ruling, and mint the atom for a seam nobody adopted
- `00be7c0ea` 2026-09-06 — docs(channels): the trigger-source obligation is half discharged, so stop writing it as pending
- `daa3b999b` 2026-09-06 — docs(roadmap): flip SH-12 and DCU-4; stop the frontier claiming owner work is startable
- `d81bbb8cf` 2026-09-06 — docs(ecosystem): ship the app bounty wants-list, machine-checked
- `56f3dec86` 2026-09-05 — docs(community): CE-9 ramp coordination — bounty drafts + scaffold/seam evidence
- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `775f47b8d` 2026-08-28 — docs(roadmap): rule three cross-plan blockers, one of them a fail-open
- `a438b9feb` 2026-08-22 — docs(roadmap): reconcile three stub atom counts with dag.json

### Named by atom id in the roadmap prose (23)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:478` — - **2026-08-22 — DISCOVERY (owner decision needed): `CE-9` and `ET-7` are in a real dependency
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:482` — * `CE-9` deps include `EXT:ECOSYSTEM-TOOLING:channel scaffold template + bounty board` → resolves to
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:485` — channel bounties reference` → resolves back to `CE-9`.
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:486` — Both are `todo`, so this is a mutual wait, not a bookkeeping artifact: `CE-9` waits on `ET-7`'s bounty
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:487` — board while `ET-7` waits on `CE-9`'s channel wants-list. `CE-9` sits in OWNER PRIORITY area 2
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:491` — `CE-9`, which also consumes the bounty board — and that is roadmap *design*, which is owner-maintained
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:502` — ### OWNER RULING — the `CE-9` ⇄ `ET-7` deadlock is broken by cutting ONE edge. 2026-08-28
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:504` — A 2026-08-22 DISCOVERY in this plan recorded that `CE-9` and `ET-7` are in a real dependency cycle, so
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:507` — `CE-9` depends on `EXT:ECOSYSTEM-TOOLING:channel scaffold template + bounty board`. `ET-7` depends on
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:514` — **RULED: cut `CE-9`'s dependency on the bounty board.** The dependency is the wrong way round on the
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:516` — So the edge that survives is `CE-9` → `ET-7`: this plan publishes the channel wants-list and the scaffold
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:519` — Consequence: **`CE-9` becomes startable** on the scaffold half alone, and `ET-7` stays legitimately blocked
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:525` — This is `T7.6`'s deliverable and `CE-9`'s third clause. It replaces the plan's earlier phrasing, which
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:585` — guide), and `CE-9` (records this note). None of the three required any app to actually declare one, and
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:586` — `CE-9`'s clause asks only for the coordination line — so a shipped seam had no scheduled adopter, and the
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:593` — `CE-9`'s third clause is **MET** by this note plus the machine-checked negative. What is *not* met, and was
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:598` — `CE-9` ⇄ `ET-7` cycle sat in the graph for ten days and the dag's own derived block kept reporting it
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:599` — (filed as #2551). `CE-9`'s dep on `EXT:ECOSYSTEM-TOOLING:channel scaffold template + bounty board` and
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:603` — lists as a direct dep. The surviving edge is `ET-7` → `CE-9`, which is the ordering the ruling argued
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:612` — - **`CE-9` is `blocked`, not `todo`.** Two of its three clauses are met and evidenced in-tree (PR #2559);
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:617` — test fixture. `CE-9`'s "no bespoke event glue shipped" clause passes today for the wrong reason: not
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:384` — (`CA-9`, `CE-9`, `DCU-3`, `DL-7`, `EI-2`, `ET-7`, `LV-7`, `PEP-3`, `PEP-11`, `PUBL-10`,
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1044` — are owner-executed walkthroughs and post-launch convenience channels; `CE-9` waits on the owner

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `T7.3`, `T7.6`. Those tokens appear in its source plan at:

- `T7.3` `docs/roadmap/plans/CHANNEL-EXPANSION.md:160` — | T7.3 | Bounty scaffolding: issues for WhatsApp/Signal/Matrix (community tier, risk-policy paragraph, guide + kit links); `channel` template registered with ECOSYSTEM-TOOLING's scaffold (coordinate — file DISCOVERY if scaffold not landed yet) | GitHub issues, cross-plan note | issues live and la…
- `T7.3` `docs/roadmap/plans/CHANNEL-EXPANSION.md:169` — 5. **Approve the channel risk-policy paragraph** (official-APIs-only for first-party; community tier for unofficial) before T7.3 publishes it.
- `T7.3` `docs/roadmap/plans/CHANNEL-EXPANSION.md:484` — * `ET-7` deps include `EXT:CHANNEL-EXPANSION:channel wants-list (T7.3) + channel scaffold template the
- `T7.3` `docs/roadmap/plans/CHANNEL-EXPANSION.md:508` — `EXT:CHANNEL-EXPANSION:channel wants-list (T7.3) + channel scaffold template`. The scaffold half is
- `T7.3` `docs/roadmap/plans/CHANNEL-EXPANSION.md:511` — cycle is entirely between **the wants-list** (T7.3, this plan's deliverable) and **the bounty board**
- `T7.3` `docs/roadmap/plans/CHANNEL-EXPANSION.md:608` — `docs/maintainers/`. The label is now `EXT:CHANNEL-EXPANSION:channel wants-list (T7.3) the channel
- `T7.6` `docs/roadmap/plans/CHANNEL-EXPANSION.md:222` — | T7.6 | Trigger-source forward note: coordination line into WORKFLOWS-V2-AUTOMATION-SUBSTRATE (app-registered trigger source types) so each vendor app adds its trigger-source provider when that seam lands — no early hand-rolled event glue | this plan + substrate plan cross-refs | both plans refe…
- `T7.6` `docs/roadmap/plans/CHANNEL-EXPANSION.md:525` — This is `T7.6`'s deliverable and `CE-9`'s third clause. It replaces the plan's earlier phrasing, which

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
