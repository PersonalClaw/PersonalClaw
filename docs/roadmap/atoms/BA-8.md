<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-8` — Browser extension connector: loopback-only typed local contract, paired via COMPANION-APPS device-session machinery, shipped as an app bundle

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #86 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment 2026-07-29 (b) The connector is a browser extension talking to the local gateway + (e) egress still applies; task B2

## Done when

The extension connects to the gateway over loopback only (LOOPBACK_INTERNAL rail, no new listening surface) and is listed as a connected device using the shipped COMPANION-APPS device-session/pairing (§C1/C2 — consumed, not forked); it speaks a typed local contract (navigate/read-outline/click/type/close); ships as an app bundle in the apps repo with zero browser-vendor strings in core — task B2

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`BA-7`](BA-7.md) | intra-repo | `done` |

### Unblocks (1)

[`BA-9`](BA-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips
- `cf5b3f661` 2026-09-02 — feat(browse): add loopback user_browser connector route (BA-8)
- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:861` — connector IS a companion client — `BA-8` pairs it through the same device-session machinery that
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:908` — **What `BA-8` needs from this atom:** call `browse.target.register_connector(device_id=…,
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:912` — is its writer BY DESIGN, with no non-test caller until BA-8 lands. `cdp_url` is what the extension
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:915` — are the two sentences already shown to a user; BA-8 should add its own reason there rather than
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1154` — ### `BA-8` — extension connector (flipped `done` 2026-09-03)
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1156` — **2026-09-03 — DONE (`BA-8`).** Merged via core PR #2350 (commit cf5b3f661, "feat(browse): loopback user_browser connector route") + apps PR PersonalClawApps#58 (loopback browser-extension connector bundle). Loopback-only (`LOOPBACK_INTERNAL`) typed local contract (navigate/read-outline/click/typ…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
