<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AE-1` — collection field wired through model/provider/handlers/tools/api.ts (tolerant reads)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ARTIFACTS-EVOLUTION](../plans/ARTIFACTS-EVOLUTION.md) (`AE`, plan status `done`) |
| **Atom table** | [`../atomic/AE.md`](../atomic/AE.md) |
| **Execution position** | #11 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 T1.1; Contracts C1 (Model addition, clean break + tolerant reads)

## Done when

collection round-trips through REST + MCP tool; pre-collection artifact meta.json loads clean with collection=''

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`AE-5`](AE-5.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T1.1`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:110` — | T1.1 | `collection` field: model + provider list filter + PATCH allowlist + tools (`artifact_update` collection arg, `artifact_list` filter) + api.ts type; tolerant read test (old meta.json loads with `""`) | `src/personalclaw/artifacts/models.py`, `native.py`, `provider.py`, `handlers.py`, `mc…
- `T1.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:153` — - [2026-07-27][S1a] DONE (T1.1 + T1.2, backend): the store-hardening half of S1,
- `T1.1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:154` — shipped as an atomic backend sub-scope. **T1.1 collection field:** `Artifact.collection`
- `Session 1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:106` — ### Session 1 — Entity split + store hardening

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
