<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-15` — Widen the convergence core and wire `SupervisorPolicy` into it

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #619 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

`loop/tick.evaluate` is already the right shape — pure, restartable, exhaustive branch order, every input derived from persisted state, with a documented purity contract (I/O the decision implies happens in the adapter and its RESULT is fed back as `state.metric` next tick). It is consumed by exactly ONE kind. Widening it to the full decision set makes it the single brain both engines call. Two members are missing from `Action`: `ESCALATE(rung)` — `loop_middleware`'s five rungs have no representation, so the loops engine fails binary after two consecutive errors — and `REPLAN`, whose absence is the sharper gap (see `WF2LOO-16`).

## Done when

`Action` gains `ESCALATE(rung)` and `REPLAN`, `TickState` absorbs `loop_middleware.LoopState`'s counters (call fingerprints, fix fingerprints, failure classes, progress marks), and `evaluate` becomes the one convergence decision for both the `loop` node and the legacy loop kinds — driven by a `SupervisorPolicy` rather than by per-kind Python. Purity is preserved and re-proven: the same (cfg, state, now) yields the same `Decision`, and a restarted process re-derives it from persisted state alone. `REPLAN` queues a real mutation batch (`mutations.py`'s `insert`/`delete`/`move`/`run_from` applied at the controller's drain point) instead of the retry-with-a-hint it is today. Verified by the existing `evaluate` branch tests staying green plus new coverage for each added member, and by a run that re-derives its remaining steps from a judge critique.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-14`](PP-14.md) | intra-repo | `done` |
| [`PP-12`](PP-12.md) | intra-repo | `done` |

### Unblocks (1)

[`PP-16`](PP-16.md) (`todo`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- `tests/test_pp15_convergence_core.py` green inside the 266, and the widening is what gives PP-14's declaration somewhere to be honoured — a policy parsed and validated but never dispatched would be the inert-control class at the top of the engine
- 🔑 WIRING IT INTO THE CONVERGENCE CORE RATHER THAN BESIDE IT IS THE DECISION THAT MATTERS: a supervisor consulted on a parallel path would be a second convergence rule, which is the exact duplication this plan exists to end
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not driven: needs a supervised run.

**Notes:** PP-14 and PP-15 are one capability split at the right seam — declare and validate, then dispatch — so the declaration is reviewable before anything depends on its behaviour.

## Recorded history

### Commits (5)

- `95a00d2f9` 2026-09-05 — feat(workflows): PP-16 seam 4d — sparse per-run SupervisorPolicy overlay (RULING 2)
- `97c9e0249` 2026-08-18 — docs(roadmap): flip PP-15 done — its work landed, its status did not (#1654)
- `9d3a6b468` 2026-08-17 — refactor(workflows): PP-15 widen the convergence core and wire SupervisorPolicy
- `72f93d908` 2026-08-15 — feat(guardrails): AG-13 consolidate the fourteen autonomy knobs into one SupervisorPolicy
- `572db7ea5` 2026-08-14 — feat(workflows): PP-14 declare SupervisorPolicy, its parser and validator

### Named by atom id in the roadmap prose (11)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:993` — the caller census skips, and nothing in the engine constructs it; PP-15 wires convergence,
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1033` — consolidated matcher belongs to the wiring owners (PP-15/AG-11); doing it here would change what
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1059` — write-scope repoint AG-13 flagged as "PP-15/AG-11 territory" needs that consolidated matcher too, so
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:130` — (`WF2LOO-12`): a control with no caller must say so, railed in both drift directions. `PP-15` widens
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:144` — WF2LOO-16 ─→ PP-14 ─→ PP-15 ─────────────┴─→ PP-16
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:168` — `PP-1`, `PP-2`, `PP-3`, `PP-11`, `PP-14` and `PP-15` are pure-logic and can land at any time.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1106` — it); **direction 2** asserts the module still carries its `zero production callers` / `PP-15` marker
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1119` — user can observe a `SupervisorPolicy` until `PP-15` wires it into `evaluate`. The CHANGELOG is
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1120` — user-facing (the in-app Updates panel), so the entry belongs to `PP-15`.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1377` — - **DONE — `PP-15` widen the convergence core and wire `SupervisorPolicy` into it.** `Action` gains
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1519` — **Not a dependency problem.** `PP-16` is READY: every non-`EXT` dep (`PP-15`, `PP-5`, `PP-13`) is

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
