<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AE-2` — Server-backed list-before-save dedup: find_similar + refusal-with-hint + REST 409

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ARTIFACTS-EVOLUTION](../plans/ARTIFACTS-EVOLUTION.md) (`AE`, plan status `done`) |
| **Atom table** | [`../atomic/AE.md`](../atomic/AE.md) |
| **Execution position** | #12 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 T1.2; Contracts C2 (Save dedup, error envelope §2.2)

## Done when

saving same name twice without slug/force yields a hint (tool) / 409 similar_artifact_exists (REST); no -2 slug minted on disk; source_path dedup path untouched; SEL outcome=deduped

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.2`, `§2.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:111` — | T1.2 | Server-backed dedup: `find_similar` on the provider + the `artifact_save` refusal-with-hint (+ `force`) + REST 409 `similar_artifact_exists` (+ `?force=1`); `source_path` path untouched; SEL `outcome="deduped"` | `native.py`, `mcp_artifacts.py`, `handlers.py`, tests | saving "Sales Dashb…
- `T1.2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:153` — - [2026-07-27][S1a] DONE (T1.1 + T1.2, backend): the store-hardening half of S1,
- `T1.2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:160` — **T1.2 server-backed dedup:** `provider.find_similar(name, kind)` (by derived slug,
- `§2.2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:61` — ### C2 — Save dedup (provider + tool + REST; §2.2 error envelope)
- `Session 1` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:106` — ### Session 1 — Entity split + store hardening

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
