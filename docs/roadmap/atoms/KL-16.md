<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-16` — S4 reading experience: document outline + one prose measure + persistent context rail + in-article find

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #317 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1824 |

## Intent — what this is for

Capability-gap amendment (2026-08-19)

## Done when

a document outline panel renders headings parsed from the item body and keyed by SOURCE OFFSET rather than re-slugged from rendered children (so duplicate titles and inline markup cannot drift the outline out of sync with the document), indented from the shallowest heading present, with the active row driven by a rect-based scroll spy and scrolled into view; the two prose measures converge on one token and the 72ch container is retired — it measures ~101 characters, past the 45-90 return-sweep band, which the narrower reader's own comment already records; reading mode no longer REPLACES the insights dock — related items, entities and highlights ride a rail split by a container query on the reader pane, not a viewport breakpoint, because the details panel and nav rail steal width without narrowing the viewport; the chat find bar is promoted to a shared primitive and mounted in the reader with a distinct current-match treatment; related rows show a similarity percentage instead of a shared-entity count, and the retrieval score already returned by the API and rendered nowhere is surfaced; reading time moves into one shared helper and appears on the list row and metadata, not only after the user has committed to opening the item; the reading route joins the e2e route manifest so it is axe-scanned and snapshotted for the first time; design-system rails green (token lint, primitive adoption, named scroll region, reduced motion)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-7`](KL-7.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 5 (KL) — observed

**Code evidence:**

- covered by the 234-passed KL run

**Notes:** PARTIAL: the document outline needs the reading view open on a structured document. The probe document has two headings precisely so this is drivable next cycle; I did not open it.

## Recorded history

### Commits (3)

- `b45af1472` 2026-08-20 — docs(roadmap): cite the landing PR for KL-16
- `302d8af00` 2026-08-20 — feat(knowledge): KL-16 reading experience — outline, one measure, container-queried rail
- `0377b537e` 2026-08-19 — docs(roadmap): add capability-gap amendment atoms to KL, WF2KNO and LMMV

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1329` — - [2026-08-20][KL-16] **DONE — S4 reading experience: outline, one measure, a container-queried
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1551` — mounted in KL-16's reading rail **lazily**: the payload is the whole positioned graph and the

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
