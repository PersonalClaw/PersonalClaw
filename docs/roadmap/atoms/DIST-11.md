<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-11` — Owner clean-machine walkthroughs V1–V4 (wheel install, getting-started, container, per-kind self-update)

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #188 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`owner`) |
| **Blocked reason** | OWNER-ONLY (inventory correction 2026-09-06). Carried no blocked_reason, so the ready frontier counted it as available agent work — it never was. The atom's own title begins 'Owner', and all four walkthroughs are defined as clean-VM / never-seen-the-project runs: V1 a wheel install on a Node-absent machine, V2 getting-started followed verbatim by a first-time reader, V3 a container two-command bring-up whose state must survive a compose down/up, V4 all four self-update paths. Their whole evidentiary value is that the machine has no prior state and the operator no prior knowledge, which an agent working in this workspace cannot supply by construction: any environment I can reach has already seen the project. Not blocked on code, and nothing to build — it needs the owner to run them on clean machines and record the results in the Execution log. |

## Intent — what this is for

Session 1 V1, Session 2 V2, Session 3 V3, Session 4 V4; Owner task 2

## Done when

each clean-VM/never-seen-the-project walkthrough passes end to end (V1 wheel→onboarding→first chat Node-absent; V2 getting-started verbatim; V3 container two-commands→TLS dashboard→state survives compose down/up; V4 all four kind self-update paths) and is recorded in the Execution log

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DIST-1`](DIST-1.md) | intra-repo | `done` |
| [`DIST-5`](DIST-5.md) | intra-repo | `done` |
| [`DIST-8`](DIST-8.md) | intra-repo | `done` |
| [`DIST-9`](DIST-9.md) | intra-repo | `done` |
| [`DIST-10`](DIST-10.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unverifiable`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 19 (DIST) — wheel built and booted on this machine

**Code evidence:**

- dag.json records this atom as `todo`, and the plan's status is in_progress — the recorded state and the code agree here

**Notes:** The audit's second unverifiable, and for the same reason as MRI-5: the deliverable IS a person on a clean machine. Four walkthroughs (wheel install to onboarding to first chat with Node absent; getting-started verbatim; container two-commands to a TLS dashboard surviving compose down/up; per-kind self-update) each require a fresh VM and a human who has never seen the project. No configuration change I can make closes it, which is what separates unverifiable from partial. Worth noting the roadmap prose in the workspace CLAUDE.md summarises Distribution as done — the plan and dag.json are the more honest record, and they say two atoms remain.

## Recorded history

### Commits (1)

- `df456c8e2` 2026-09-06 — docs(roadmap): mark DIST-11 and CRE-7 owner-only — the frontier was over-reporting

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/DISTRIBUTION.md:391` — a flake committed blind would be a distribution claim nobody has run. Sibling area-1 atoms `DIST-11`
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1043` — not re-derive it. All four owner-priority areas hold nothing startable: `DIST-11`/`DIST-12`/`CRE-7`

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `Session 1`, `Session 2`, `Session 3`, `Session 4`. Those tokens appear in its source plan at:

- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:145` — ### Session 1 — Packaging correctness
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:213` — ### Session 1 — Packaging correctness
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:222` — ### Session 1 (cont.) — Cycle 2
- `Session 1` `docs/roadmap/plans/DISTRIBUTION.md:238` — ### Session 1 (cont.) — Cycle 3
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:156` — ### Session 2 — PyPI + bootstrap + client
- `Session 2` `docs/roadmap/plans/DISTRIBUTION.md:245` — ### Session 2 — Cycle 4
- `Session 3` `docs/roadmap/plans/DISTRIBUTION.md:166` — ### Session 3 — Containers
- `Session 3` `docs/roadmap/plans/DISTRIBUTION.md:255` — ### Session 3 — Cycle 6
- `Session 4` `docs/roadmap/plans/DISTRIBUTION.md:175` — ### Session 4 — Self-update generalization (class B, gate `update_kind_aware`)
- `Session 4` `docs/roadmap/plans/DISTRIBUTION.md:263` — ### Session 4 — Cycle 8 (self-update generalization — CLEAN BREAK)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
