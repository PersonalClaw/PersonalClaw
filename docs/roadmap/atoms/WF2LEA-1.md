<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2LEA-1` — Shared lifecycle machine: Capture (gate/hygiene/staging), Propose (queue+decision memory), Curate (usage/decay/curator), Inject (slot allocator)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-LEARNING-FLYWHEEL](../plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md) (`WF2LEA`, plan status `done`) |
| **Atom table** | [`../atomic/WF2LEA.md`](../atomic/WF2LEA.md) |
| **Execution position** | #586 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##163,#164,#165,#166 |

## Intent — what this is for

Migration steps 1,3,4a,4b (§2.1 Capture, §2.2 Propose, §2.3 Curate, §2.4 Inject); learning/ package: gate.py, hygiene.py, staging.py, proposals.py, usage.py, decay.py, curator.py, surfacing.py

## Done when

One LearningGate computes eligibility once per event; capture_hygiene fences untrusted; append-only staging log with FLUSH_* outcomes; learning/proposals.py has six kinds + fingerprints + rejected-exemplar store + 4-verdict resolve cascade + change manifests + per-run quota + SEL-audited accept; usage store + one decay kernel (3 profiles) + hardened curator wired to history.py consolidation tick; ranked slot allocator with per-entity profiles, sacrificial-slot truncation, authority preamble — all green (shipped)

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (7)

[`WF2LEA-10`](WF2LEA-10.md) (`done`), [`WF2LEA-12`](WF2LEA-12.md) (`done`), [`WF2LEA-2`](WF2LEA-2.md) (`done`), [`WF2LEA-3`](WF2LEA-3.md) (`done`), [`WF2LEA-4`](WF2LEA-4.md) (`done`), [`WF2LEA-5`](WF2LEA-5.md) (`done`), [`WF2LEA-7`](WF2LEA-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (28 line(s))

This atom's scope cites `§2.1`, `§2.2`, `§2.3`, `§2.4`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:45` — - **LEARN-R5** (capture hygiene: system-injection filter, grounding gate, session scoring, notability rules, pre-compaction flush) → §2.1
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:59` — - **LEARN-R19** (explicit staging tier with outcome records and cost metering) → §2.1
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:64` — 1. There is NO `LearningGate` class today. Eligibility is the free function `after_turn_review.should_review()` (:88), recomputed independently in BOTH `chat_runner._maybe_after_turn_review` (:158) and `_maybe_skill_ladder_review` (:217) — and `capture_preference_facet` runs UNGATED on every non-…
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:86` — - **Config:** all new knobs extend the existing `LearningConfig` dataclass (config/loader.py:934) and MUST be wired through the FOUR points (recon-verified gotcha): (a) dataclass field with `_meta(label, help)`; (b) `AppConfig.load()`'s explicit field-by-field mapping (loader.py:1638-1802 — omiss…
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:107` — **Voice aspects (LEARN-R20):** user-voiced statements classified into aspects (Directive, Preference, Habit, Belief, Goal) are stored as COMPLETE verbatim statements with an aspect tag — never decomposed into normalized facts — as a memory sub-kind under new `user.voice.<aspect>.<slug>` semantic …
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:466` — §2.1 unifications: `gate.py` (one eligibility decision per event, consumed by every
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:41` — - **LEARN-R1** (proposal decision memory: fingerprints, rejection exemplars, quota, resolve cascade, extract→decide) → §2.2
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:49` — - **LEARN-R9** (ratchet invariant, scaffolding-retirement proposal kind, expiry metadata, provenance-weighted lifecycle) → §2.2 / §1
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:56` — - **LEARN-R16** (change-manifest attribution: predict-then-verify, 5-way verdicts, HARMFUL auto-revert) → §2.2 / §3.1
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:135` — - **Extraction is two-phase (LEARN-R1 batch-5):** fact extraction and the ADD/UPDATE/DELETE decision are two DISTINCT steps with a candidate-gathering query between them (search existing entities relevant to each extracted item) so the decide pass always sees what already exists — preventing drif…
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:226` — - Declined promotions feed §2.2's rejection exemplars with escalating re-propose cooldowns.
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:439` — **Contract:** a `project_context_review` capability that (1) reads a conversation or run, (2) emits proposals of kind `project_instruction` / `project_file` / `project_skill` into the existing §2.2 queue with a rationale per item, (3) **writes nothing** until accepted, and (4) records the decisio…
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:448` — | E1.4 | `project_context_review`: read a conversation/run → typed proposals (`project_instruction`/`project_file`/`project_skill`) with per-item rationale into the §2.2 queue; nothing written without acceptance; decision recorded | the learning pipeline modules, tests | reviewing a conversation …
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:458` — - **Promotion noise.** Retroactive promotion could flood the proposal queue. §2.2's decision memory and §2.3's curator already bound this; the amendment deliberately reuses them rather than adding a second gate.
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:46` — - **LEARN-R6** (curator hardening: provenance scoping, demote-never-delete WAL, over-deletion guards, decay formulas, mode-scoped sweeps) → §2.3
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:282` — Auto-captured templates land session-scoped (the existing capture ladder is the landing zone), sweep with the session unless promoted (§2.3's multi-gate).
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:295` — - (d) Reproducible stage failures propose a **failure CAPSULE** (repro command + failure_signature + forbidden_success_modes + bounded evidence) instead of prose; later replays verify the lesson still applies — replay outcome is the lesson's decay signal (and the lessons-exempt usage-store gap fr…
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:419` — - Resource loads are **usage-recorded** like `skill_invoke`, so the curator (§2.3) can age unused resources.
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:458` — - **Promotion noise.** Retroactive promotion could flood the proposal queue. §2.2's decision memory and §2.3's curator already bound this; the amendment deliberately reuses them rather than adding a second gate.
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:537` — - **DISCOVERY — the scheduling gap in §2.3 was real and worse than stated.**
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:47` — - **LEARN-R7** (context budget as ranked slot allocator with authority doctrine and tiered rendering) → §2.4
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:52` — - **LEARN-R12** (invocation axis, precondition gates, glob/pack auto-attach, trigger-shaped description lint) → §1 / §2.4
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:209` — - **`surfacing_events` table:** the §2.4 engine logs every surfacing event (entity kind, matching arm, confidence, session/turn). **"Used" is derived MECHANICALLY** — skill body loaded after surfacing, template run started from a suggestion, run outcome success/failure, lesson referenced by after…
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:225` — - Only a compact snapshot injects into planning/recovery prompts — one budgeted slot in §2.4's allocator, never the full history.
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:299` — **Pending→resolved outcome lifecycle (LEARN-R18):** decision-producing workflow runs journal a `pending_outcome` entry {subject, metric, horizon, baseline} into the Run Ledger at decision time. A resolver (background one-shot on the curator tick, after the horizon elapses) measures ground truth a…
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:393` — **Provenance.** A gap analysis (2026-07-28/29) plus a code audit surfaced three items the owner approved for planning. **Two of the three are narrower than the framing implied**, and this amendment says so rather than restating the pitch — §2.4 already owns a more sophisticated version of one of …
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:403` — - **§2.4 of this plan already designs something stronger than that fixed three-level model:** persisted L0/L1/L2 tiers per entity, a *ranked slot allocator* that degrades tier before dropping items, L2 gated at ≥0.9·top_score with a hard cap of ~3 full-detail items, and an L0 catalog of unloaded …
- `§2.4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:456` — - **Duplicating §2.4.** The single largest risk in this amendment. §2.4's ranked slot allocator with L0/L1/L2 degradation is the authority for *how much of an entity* reaches a prompt; E1.1 adds only a **new, deeper tier for skill resources**. A task that reimplements tiering in `skills/` is out …

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
