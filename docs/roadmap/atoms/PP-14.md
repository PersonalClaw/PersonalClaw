<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-14` — `SupervisorPolicy`: the declaration, its parser and its validator

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #617 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1339 |

## Intent — what this is for

A loop is not a second engine — it is a graph shape plus a supervisor policy. The shape already exists (`loop` node kind, `LoopMode.{counted,until,until_dry,until_cancelled}`, `foreach` with `max_concurrency`). The POLICY has no home, which is why it got implemented twice: `loop/` carries marginal-value and reproduce-before-ship while `workflows/` carries the pre-tier, the proof precondition, the actor matrix and the five-rung ladder, and neither is complete. This atom lands the declaration ONLY, deliberately inert, with the honesty marker `WF2LOO-12` established as this program's convention — a control with no caller must SAY it has no caller.

## Done when

A `SupervisorPolicy` declaration (rubric, escalation ladder, failure mutations, dwell/metric gates, marginal-value band, judge model tier, reproduce-before-ship, write scope, budget, HITL posture) parses from a loop node's config with tolerant reads and a closed field set, validated at authoring time with typed `WF_*` codes. Deliberately NOT wired: the module docstring states that it has zero production callers and names `PP-15` as the wiring owner, and a two-directional rail fails both when a caller appears while the marker stays and when the marker is removed while callers are still zero. Proven able to fail by two probes.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2LOO-16`](WF2LOO-16.md) | intra-repo | `done` |

### Unblocks (2)

[`AG-13`](AG-13.md) (`done`), [`PP-15`](PP-15.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- `SupervisorPolicy` has a declaration, a parser and a validator, with live importers in `validator.py`, `store.py`, `service.py` and `controller.py` — four production readers, so it is not a declared-but-unread surface
- 🔑 THE ATOM'S SEQUENCING IS RIGHT AND THE ROADMAP SAYS WHY: this was unblocked by the verdict-vocabulary reconciliation, because 'adding a supervisor policy … first would have minted a FIFTH dialect'. A policy that has to name a verdict cannot land before the verdicts are one vocabulary
- declaration plus parser plus validator as one atom is the correct unit — a parser without a validator accepts a policy nobody can honour, and the ninth `test_pp16_supervisor_policy_dispatch` suite is green inside the 266
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not a browser surface.

**Notes:** This is the third of the plan's three unnamed nouns. Its value is that the domain intelligence becomes a declaration a reader can inspect, rather than pluggable Python nobody can enumerate.

## Recorded history

### Commits (4)

- `94711166d` 2026-08-15 — docs(roadmap): flip PP-2/PP-5/PP-10/PP-14 to done (batch cherry-pick lost their plans[] edit)
- `72f93d908` 2026-08-15 — feat(guardrails): AG-13 consolidate the fourteen autonomy knobs into one SupervisorPolicy
- `572db7ea5` 2026-08-14 — feat(workflows): PP-14 declare SupervisorPolicy, its parser and validator
- `6d2a7f0bc` 2026-08-14 — docs(roadmap): add Platform-Primitives plan + 20 atoms (rev 17)

### Named by atom id in the roadmap prose (16)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:988` — rule. This lands the ONE declaration — the SAME object PP-14 declared, `SupervisorPolicy`,
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:990` — are one declaration, not two. **Stacked on `feature-pp14-supervisor-policy`; PP-14's commit
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:992` — PP-14 honesty rail stays green (the consolidation lives inside `supervisor_policy.py`, which
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1049` — Two pre-existing `tests/test_guardrails_ladder.py` failures are inherited from the PP-14 base
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1004` — `workflows/judge_contract.py` — PP-14's un-named primitive), **http-error-envelope-helper
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1176` — asked the question at authoring time, which is the entire thesis of `PP-14`.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:70` — | Autonomy as one policy object | AUTONOMY-GUARDRAILS | **`AG-13`** (new atom there), depending on `PP-14` so the autonomy ceiling and the supervisor policy are ONE object rather than two competing declarations. The two-level `Ceiling ∩ Profile` model it composes is PLATFORM-HARDENING-FLOORS §5's…
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:121` — ## §5 One work-unit noun (`PP-14`…`PP-16`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:129` — `PP-14` lands the declaration deliberately inert, with the honesty marker this program established
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:144` — WF2LOO-16 ─→ PP-14 ─→ PP-15 ─────────────┴─→ PP-16
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:168` — `PP-1`, `PP-2`, `PP-3`, `PP-11`, `PP-14` and `PP-15` are pure-logic and can land at any time.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1091` — - **2026-08-14 — `PP-14` DONE.** `workflows/supervisor_policy.py` lands the ONE `SupervisorPolicy`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1100` — - **2026-08-14 — `PP-14` the honesty rail (the atom's centre).** A static AST **call/construction
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1110` — - **2026-08-14 — `PP-14` DECISION: tolerant reads vs the closed set.** The parser never raises —
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1118` — - **2026-08-14 — `PP-14` DECISION: no CHANGELOG entry.** The declaration is deliberately inert — no
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1121` — - **2026-08-14 — `PP-14` falsifications (all reverted from `cp` backups, probe markers grepped).**

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
