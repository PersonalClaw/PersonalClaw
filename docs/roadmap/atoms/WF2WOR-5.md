<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2WOR-5` — Batch subagent_run compile-cutover + tool-handler posture seam + agent roster

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-WORK-CONTAINERS](../plans/WORKFLOWS-V2-WORK-CONTAINERS.md) (`WF2WOR`, plan status `done`) |
| **Atom table** | [`../atomic/WF2WOR.md`](../atomic/WF2WOR.md) |
| **Execution position** | #657 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1075 |

## Intent — what this is for

§3 (Subagent tools vs stages; batch-compile hardening R2/R16), Migration Order step 6

## Done when

Success Criterion 2: mcp_subagents.subagent_run(tasks=[...]) routes through compile_batch (N>=2) producing a live widget that survives a gateway restart with individually-retryable branches; the __wf_depth tool-handler seam enforces capability classes, orchestration-tool denial and the secret-filtered leaf env; each branch runs in an isolated workspace, returns schema-validated typed output, and holds a lease (no double-execution); agent-roster slug-keyed catalog projection over config agents + drift check ship

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2WOR-4`](WF2WOR-4.md) | intra-repo | `done` |
| [`WF2WOR-8`](WF2WOR-8.md) | intra-repo | `done` |
| [`WF2WOR-9`](WF2WOR-9.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 66 (WF2WOR) — the archive extractor's path safety falsified and found unasserted (fixed in #2825); the Work board driven in a real browser

**Code evidence:**

- the fan-out, validate and persistence suites are green (166 across those plus worktrees and memory locality)
- 🔑 SURVIVING A GATEWAY RESTART IS THE CLAUSE THAT SEPARATES A WIDGET FROM A LIVE OBJECT, and it is asserted — a batch widget that vanished on restart would leave the user with N running children and no surface to reach them
- individually-retryable branches mean one failed leaf does not cost the other N-1 their work
- `__wf_depth` as a tool-handler seam is the recursion bound: without a depth marker a sub-agent that spawns sub-agents has no ceiling but the budget
- 555 passed across the containers, needs-input, export, container-workspace, publish, filedrop, introspection, fan-out, worktree and memory-locality suites; the Work board and a project detail page driven on :10011

**Driven in the UI:** Not driven: needs two or more real sub-agent tasks.

**Notes:** The compile-cutover at N>=2 is the right threshold — a single task routed through a batch compiler would pay the widget cost for nothing.

## Recorded history

### Commits (2)

- `a201eaa1a` 2026-08-11 — feat(workflows): WF2WOR-5 route batch subagent_run through the compiler (#1075)
- `c4662f1fd` 2026-08-09 — feat(workflows): WF2WOR-9 fan-out leaf contract + capability enforcement + measurement harness (#961)

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1318` — at dispatch), and a secret-filtered leaf env (`mcp_shared.leaf_env`, `WF2WOR-5` done). What is absent is the
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1181` — cutover is **WF2WOR-5**, which depends on this atom. An over-claimed VC is worse than a deferred one.
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1183` — - **C2.3 harness verdict: NOT YET RUN on real work — deferred to WF2WOR-5 with the call site.** The
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1191` — - **NOT DONE (out of scope, by row):** the `mcp_subagents.subagent_run` compile-cutover (WF2WOR-5), the
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1365` — - [2026-08-11][WF2WOR-5 / Success Criterion 2] CLOSED OUT — fourth of the owner's partial-atom queue. All four clauses closed; the earlier BLOCKED claim on the isolated workspace is WITHDRAWN as incorrect.

### Its plan's own record, via the scope references (20 line(s))

This atom's scope cites `§3`, `R2`, `R16`. Those tokens appear in its source plan at:

- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:35` — - **WORK-R2** — batch-compile hardening (isolation, dual depth, lineage env, typed leaf outputs, write-holder lint, recall-view, N-variant, capability classes) → §3
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:49` — - **WORK-R16** — agent roster as slug-keyed drift-checked catalog → §3 (orchestrator paragraph)
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:101` — 5. **Claim leases + wayfinder ledgers (R8).** (a) Claim-before-work: an executing session/agent takes an exclusive TTL'd lease (renew/release, ≤1h) on a task/run before starting; lease files ride the existing `concurrency.single_flight` flock convention (`~/.personalclaw/locks/`) with the lease r…
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:165` — The WorkflowRun container model gains a `workspace` block, threaded through §3's compile path and TASKS-SOPS materialization:
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:198` — The workspace block's `env` section may reference a **per-project encrypted secrets store**: values held in the OS keychain (macOS Keychain via `keyring`), never in committed config, run records, or journals. This *extends* the existing credential seam (loader.py `save_credential()` → `.env` 0600…
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:286` — | `subagent_run` batch | **COMPILE** to implicit run (N≥2) with the §3 hardening contract |
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:307` — 6. Batch `subagent_run` → implicit-run compile **with the §3 hardening contract** + leases + agent-roster catalog + drift check + orchestrator-skill text update (same commit).
- `§3` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:318` — | Cross-branch contamination / recursive orchestration blowup / credential leakage in batch runs | §3 hardening contract: isolation-by-default, dual depth enforcement, secret-filtered leaf env, single-Write-holder lint (R2/R19) |
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:35` — - **WORK-R2** — batch-compile hardening (isolation, dual depth, lineage env, typed leaf outputs, write-holder lint, recall-view, N-variant, capability classes) → §3
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:53` — - **WORK-R20** — optional container workspace mode + snapshot checkpoints (opt-in, deferred) → §4.4
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:145` — ### Batch-compile hardening contract (R2 — the isolation/typing/least-privilege bundle)
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:161` — ## 4. Run Workspace & Environment (NEW — R3/R18/R19/R20)
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:200` — ### 4.4 Optional container mode (R20 — opt-in, deferred to last)
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:284` — | `SubagentManager` + persistence | **KEEP** (the substrate); batch leaves gain the R2 hardening contract |
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:318` — | Cross-branch contamination / recursive orchestration blowup / credential leakage in batch runs | §3 hardening contract: isolation-by-default, dual depth enforcement, secret-filtered leaf env, single-Write-holder lint (R2/R19) |
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:330` — - **9 sessions** (was 4; R1-R20 roughly double the mechanism surface), interleaved with v2 Slices 4-5 rather than strictly after. Sessions map 1:1 to Migration Order steps 1-8, with step 9 (container mode) explicitly deferred and unbudgeted until the rest has landed.
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:49` — - **WORK-R16** — agent roster as slug-keyed drift-checked catalog → §3 (orchestrator paragraph)
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:157` — **orchestrator_skill: SEPARATE, confirmed.** Routing policy ≠ execution engine. One shared hook: its agent roster feeds `workflow_plan`'s per-stage `agent` selection — one catalog, two consumers. **Roster format (R16):** a slug-keyed JSON catalog file (slugs = stable filename stems, never display…
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:290` — | orchestrator_skill | **KEEP, SEPARATE**; roster = slug-keyed drift-checked catalog projection over config agents (R16) |
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:642` — comparison metrics, and the agent-roster projection + drift check (R16) — the roster is a

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
