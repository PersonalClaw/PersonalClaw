<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-14` — Make `on_overlap: queue` queue instead of starting a concurrent run + an exhaustiveness ratchet over OverlapPolicy

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #683 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1130 |

## Intent — what this is for

§2 trigger-origin starts (`on_overlap`, owned by the run-workflow provider re-added in Slice 3)

## Done when

overlap.decide branches on every OverlapPolicy member and RAISES on an unmapped one; a `queue` start with a prior in flight PERSISTS an unlaunched run (DRAFT + a marker on run.extra) and returns outcome: "queued" naming it, instead of launching beside the prior; overlap.drain starts it from the controller's terminal write and from the watchdog poll, single-flight and idempotent; a hand-made DRAFT with no marker is never launched; the queue is capped at one and a dropped start names the cap in its outcome and the log; enum:OverlapPolicy.QUEUE leaves inert-surface-baseline.json

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-3`](WV-3.md) | intra-repo | `done` |
| [`WV-4`](WV-4.md) | intra-repo | `done` |

### Unblocks (1)

[`WV-15`](WV-15.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `917ef60f8` 2026-08-12 — feat(workflows): WV-14 make `on_overlap: queue` queue instead of starting a concurrent run (#1130)

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:6071` — **Exhaustiveness ratchet (the WV-13/WV-14 pattern: a raising tail + an AST read of the function's source).** `TestTheDrainIsExhaustive` asserts an AST walk of `_drain_spool`'s source names every `DrainAction` member (measured exact: 4/4) and of `drain_decision`'s source every `Handling` member (3…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2.md:1299` — - [2026-08-12][WV-14] DONE — `on_overlap: queue` finally queues instead of starting a concurrent run, and `OverlapPolicy` has a ratchet that refuses to let the next member ship without a branch.
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2.md:1313` — **The finding, and it closes WV-14's own DISCOVERY.** That log recorded "`HOOK_STATUS_TO_OUTCOME` has no `launched` entry … one line, its own atom". Auditing all three tables against their real writers found it was not one line and not one table: **nine values across four writers** were live and …
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2.md:1314` — **The silent fallback is the defect, not the missing key.** Both fallbacks were individually defensible — `RAN if last_run` reads "we know it ran, we merely lack the verdict", and `FAILED` follows `FireRecord.from_dict`'s "a row this build cannot classify must not count as a success". Neither say…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2.md:1316` — **No new vocabulary member, deliberately — but `weight` moved.** All nine landed on existing `Outcome` members; WV-14 paid the full cost of adding one (three tables + `engine.dispatch_action` + the FE union) and nothing here needed it. What did change is honesty about weight: `LEDGER_WEIGHT_OUTCO…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2.md:1323` — **DEVIATION — none on scope.** Two premise corrections, both cosmetic: WV-14's DISCOVERY names the function `project_hook_fire`, which is `hook_to_record` on this branch, and the brief described `hooks.py:650` as `"launched" if result.outcome == "launched" else "ok"` where WV-14 had already widen…

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§2`, `Slice 3`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:16` — + cached badge (`workflowFold.ts`'s `cached?` is declared and read by nothing), and §2's
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:26` — - **WF2-R1** (effect ledger + idempotency + per-def dispatch lock w/ lease + on_overlap policy) → §2 Side Effects, §4 tool idempotency, §5 ledger
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:28` — - **WF2-R3** (engine-owned completion, verification ladder, required_artifacts, typed verdicts, fresh-judge) → §2 Engine-Owned Completion
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:29` — - **WF2-R4** (attempt records w/ mutation hints, circuit breaker, soft budgets, pre-charge + baseline check + amendments) → §2 Failure/Retry/Budgets, resolves Open Question 4
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:30` — - **WF2-R5** (degraded completion, typed error taxonomy, two-knob timeouts, foreach per-item policy + amendments/batch-5 timeout tests) → §1 Node Outcome Model, §2 Timeouts, §8 Slice 11
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:31` — - **WF2-R6** (per-iteration session reset w/ journaled handoffs, decision records, output offloading + amendments) → §1 stage fields, §2 Context Lifecycle
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:33` — - **WF2-R8** (capability-dispatched schema enforcement, output_contract for non-LLM nodes + amendments) → §2 Structured Output
- `§2` `docs/roadmap/plans/WORKFLOWS-V2.md:35` — - **WF2-R10** (sticky cancel intent, terminal-write ownership, protocol-violation blocked state, workflow_audit) → §2 Termination Robustness, §4 tools
- `Slice 3` `docs/roadmap/plans/WORKFLOWS-V2.md:631` — - `validation.py`: remove `_WORKFLOW_SCOPES`, all `WORKFLOW_*_SCHEMA`, workflow entries in `MCP_CORE_SCHEMAS`, `"run-workflow"` in `ALLOWED_HOOK_PROVIDERS` (re-added for the v2 run-workflow action in Slice 3)
- `Slice 3` `docs/roadmap/plans/WORKFLOWS-V2.md:711` — ### Slice 3: Side Effects + Scope + Termination + Secrets (est. 2 sessions)
- `Slice 3` `docs/roadmap/plans/WORKFLOWS-V2.md:1178` — refusal rather than a silent skip); the effect ledger (Slice 3); mid-flight mutation

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
