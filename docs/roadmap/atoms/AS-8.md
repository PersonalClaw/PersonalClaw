<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AS-8` — Mission Control preset: four attention lanes with inline resolution

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AMBIENT-SURFACES](../plans/AMBIENT-SURFACES.md) (`AS`, plan status `done`) |
| **Atom table** | [`../atomic/AS.md`](../atomic/AS.md) |
| **Execution position** | #77 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1932 |

## Intent — what this is for

Amendment 2026-07-26 round 2 (a) Dashboard-as-views, task A2-2 (Mission Control preset over the unified attention store).

## Done when

a locked "Mission Control" view renders four attention lanes (Needs-approval / Your-turn / Working / Idle) sourced from the unified attention store; approving from a lane resolves the approval via the existing /api/approvals actions; a pending question answered from a card (options as actionable buttons) unblocks its loop.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AS-1`](AS-1.md) | intra-repo | `done` |
| `EXT:INBOX-NOTIFICATIONS-UNIFICATION:S1-2 kind registry + inbox-as-attention-store` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `c4f29d301` 2026-08-23 — chore(roadmap): cite AAP-1, AAP-2, AAP-3 and AS-8 to the batch PR
- `121698e63` 2026-08-23 — feat(dashboard): AS-8 Mission Control preset with four attention lanes

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:698` — ## Execution log — AS-8 (Mission Control preset: four attention lanes with inline resolution)
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:700` — - [2026-08-23][AS-8] **DONE.** All three `done_when` clauses land in one commit: the locked
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:709` — - [2026-08-23][AS-8] 🔴 **DISCOVERY — the two halves named DIFFERENT tiles, and nothing could see
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:719` — - [2026-08-23][AS-8] 🔴 **DISCOVERY — a page component nothing routes to.** `missionControl.test.tsx`

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
