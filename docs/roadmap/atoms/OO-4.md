<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `OO-4` — Seed good-first-issues + run setup_labels.sh on both repos (T2.2)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [OSS-OPERATIONS](../plans/OSS-OPERATIONS.md) (`OO`, plan status `done`) |
| **Atom table** | [`../atomic/OO.md`](../atomic/OO.md) |
| **Execution position** | #381 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 — Contribution ramps (T2.2: file the Design good-first-issue list with context/file-pointers/done-when and label them) + completes V1's deferred GitHub-side label/render step

## Done when

>=8 good-first-issues live with the good-first-issue label; setup_labels.sh run on both repos (16 labels each); stale/already-fixed seeds verified and closed-with-evidence rather than filed (croniter DST, video kind, xfail list, --slack-only)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`OO-1`](OO-1.md) | intra-repo | `done` |
| [`OO-3`](OO-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/OSS-OPERATIONS.md:18` — (`setup_labels.sh` had never been run before T2.2); GitHub Discussions ENABLED on both repos with
- `T2.2` `docs/roadmap/plans/OSS-OPERATIONS.md:74` — | T2.2 | Seed good-first-issues: file the Design list (each: context, pointer to files, done-when — reuse plan task lines where they exist), label them | GitHub issues (executor via `gh issue create`) | ≥8 issues live with good-first-issue label |
- `T2.2` `docs/roadmap/plans/OSS-OPERATIONS.md:128` — skill-format doc, T2.2 good-first-issue seeds, T2.3 Discussions structuring + README links + welcome
- `T2.2` `docs/roadmap/plans/OSS-OPERATIONS.md:183` — - **[2026-07-31][T2.2] DONE — 9 good-first-issues live (bar was ≥8), and two of the plan's seeds were
- `Session 2` `docs/roadmap/plans/OSS-OPERATIONS.md:69` — ### Session 2 — Contribution ramps

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
