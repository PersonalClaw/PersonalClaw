<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CA-1` — S1 backend: unified pairing routes (C2) + Devices registry over REMOTE-USER-AUTH session rows

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COMPANION-APPS](../plans/COMPANION-APPS.md) (`CA`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CA.md`](../atomic/CA.md) |
| **Execution position** | #88 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1679 |

## Intent — what this is for

Session 1 T1.1; Contracts C2 (POST /api/devices/pair/start|complete, GET /api/devices, POST /api/devices/{id}/revoke), C1 device-session model; new dashboard/handlers/devices.py + server.py wiring; SEL device_pair_started/device_paired/device_revoked

## Done when

pair/start->complete yields a durable device session (a sessions.json row with device/issuer set, no new token type) that survives a gateway restart; code reuse rejected (device_pair_code_invalid/expired); revoke locks the device out on its next request across a restart; SEL event on each route

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:REMOTE-USER-AUTH:durable sessions.json store + enroll path (C1/C3) — already shipped 2026-07-30` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`CA-2`](CA-2.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 39 (CA) — a SECOND BROWSER paired end-to-end and was revoked, driven

**Code evidence:**

- 🔑 DRIVEN END TO END, not read: minted a real pairing code in the UI, opened the /pair link in a SEPARATE BROWSER SESSION, named the device, and it landed signed in at #/dashboard. Then revoked it and MEASURED sessions.json on disk — the device row is gone and the two non-device rows are untouched
- auth/pairing.py enumerates six bounding properties WITH reasons, and the single-use ordering is the correct one: 'the record is removed and persisted BEFORE the caller mints a session, so two simultaneous redemptions cannot both succeed'
- 🔑 THE PARTIAL-FAILURE PATH RETRACTS THE SESSION (devices.py:288-294): if the device block cannot be attached, the just-minted session is revoked and forgotten — 'an un-listed device session is the exact failure the registry exists to prevent'
- the two rejection results the contract requires are distinct (device_pair_code_invalid / expired) but BOTH count toward the per-IP lockout — 'the difference is what the user is told, not how much grinding they are allowed'
- restart durability and revoke-across-restart are pinned by clause-named tests: test_clause_2_the_device_session_survives_a_restart, test_clause_4_revoke_locks_the_device_out_across_a_restart, test_revoke_refuses_the_devices_next_http_request, test_a_revoked_device_stays_refused_across_a_restart
- SEL on each route via _audit(...) with denied outcomes recorded too
- test_device_pairing + test_companion_discovery + test_companion_single_pairing_mechanism + test_ca7_remote_wss_auth + test_mc2_device_session_consumption 133/133; desktop/test/connectMode.test.js 65/65

**Driven in the UI:** Pair start → complete → list → revoke, driven in two independent browser sessions; the revoke's effect confirmed on disk.

**Notes:** 🔑 ARCC WAS SUBSTANTIVE HERE FOR THE FIRST TIME IN SEVERAL CYCLES, and both objectives it returned are met. (1) Token revocation must be real AND SCOPED — 'a revoked refresh token can no longer be used … any other refresh tokens are unaffected'. Measured exactly that on disk: the revoked device's row vanished, the others did not. Its threat statement ('without revocation a compromised token cannot be invalidated, allowing persistent access until it naturally expires') is why session_store.py DISCARDS an old-shape row rather than upgrading it — 'a row with no issuer is a live session the registry can neither describe nor revoke, which is precisely the audit gap this record exists to close'. That is a clean break justified by the security property, not by convenience. (2) Bounded session duration — see CA-2's note, where the number is visible in the product.

## Recorded history

### Commits (4)

- `739890ce8` 2026-08-25 — test(devices): MC-2 device-session consumption rails at the HTTP boundary
- `c495fc13a` 2026-08-18 — chore(reference): re-render for the batch union (MRT-3 + TSE-4 on top of CA-1)
- `cca592812` 2026-08-18 — chore(roadmap): cite the landing batch #1679 on APE-1, ET-3, FM-2 and CA-1
- `94547b32c` 2026-08-18 — feat(companion): CA-1 unified pairing routes + Devices registry over session rows

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:336` — - [2026-08-18][CA-1] DONE: S1 backend — C2's four routes (`dashboard/handlers/devices.py`, new,
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:358` — - [2026-08-18][CA-1] DEVIATION: **revoke DELETES the row rather than flipping a `revoked` flag**
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:367` — - [2026-08-18][CA-1] DEVIATION: **`last_seen` is NOT shipped**, though C1/C2 and T1.2 list it.
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:375` — - [2026-08-18][CA-1] DEVIATION: the session row's **shape is a clean break** — a row went from a
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:384` — - [2026-08-18][CA-1] DEVIATION (fence): two small additions to `token_auth.py`, which CA-1's
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:391` — - [2026-08-18][CA-1] DISCOVERY (not fixed, blocks CA-2): the CA-5 rough edge above is **still
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:395` — origin exemption, so this is not fixable inside CA-1 by adding one — it needs
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:398` — Loopback and same-origin pairing work today, which is what CA-1's own done_when covers.
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:399` — - [2026-08-18][CA-1] DISCOVERY (not fixed, wants its own atom — this is the sharpest edge CA-1
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:409` — set from eviction, or size the cap to devices + browsers) and is outside CA-1's fence, since
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:411` — - [2026-08-18][CA-1] DISCOVERY (not fixed): a paired device holds an ORDINARY session, so it has
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:417` — - [2026-08-18][CA-1] DISCOVERY (not fixed): `issuer` has exactly two producers — `"pair"` (this
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:424` — - [2026-08-18][CA-1] Contract note: three C2 names were followed over the atom row's shorthand —
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:483` — The Devices panel, its four api-client methods and the `last_seen` writer `CA-1`'s note asked for are
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:571` — nor the pairing code appears in `security_events.jsonl`** in either form, so CA-1's assertion is
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:587` — self-pair also spends an eviction slot (CA-1's eviction discovery makes that expensive). Falsified
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:608` — tombstone.** CA-1's reasoning holds and the drive confirms its consequence is the one the clause
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:626` — address**, and that is the only reason CA-1's "blocks CA-2" note needed no code change.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.1`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/COMPANION-APPS.md:131` — Consequence for T1.1: `pair/complete` sets the session cookie on the response; a native shell
- `T1.1` `docs/roadmap/plans/COMPANION-APPS.md:189` — | T1.1 | Unified pairing routes (C2): `pair/start`, `pair/complete`, `GET /api/devices`, `revoke` — device sessions are REMOTE-USER-AUTH C1 rows with `device`/`issuer` set (no new token type); SEL on each | `dashboard/handlers/devices.py` (new), `server.py` wiring | pair start→complete yields a d…
- `T1.1` `docs/roadmap/plans/COMPANION-APPS.md:278` — exists yet (it is a rendering of the pairing routes T1.1/T1.2 own). Degradability is asserted
- `T1.1` `docs/roadmap/plans/COMPANION-APPS.md:333` — and the bare hostname, no LAN address). Reads from that origin pass. Left to T1.1 /
- `Session 1` `docs/roadmap/plans/COMPANION-APPS.md:185` — ### Session 1 — Connectivity contract + Devices registry

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
