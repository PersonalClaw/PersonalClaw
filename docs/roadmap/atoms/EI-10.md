<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EI-10` — Secrets vault UX + presence-only API + grant-to-sandbox toggles

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXECUTION-ISOLATION](../plans/EXECUTION-ISOLATION.md) (`EI`, plan status `done`) |
| **Atom table** | [`../atomic/EI.md`](../atomic/EI.md) |
| **Execution position** | #232 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§8 (NEW-27 remainder over WORK-R19): Settings→Secrets vault listing global + per-project secrets with presence-only display, inherit-from-host rows as a first-class type, consumer links; GET/POST/DELETE /api/secrets (write-only values); project hub Context tab links; per-secret grant-to-sandboxed-runs toggle populating SandboxSpec.env; §11 Session 7

## Done when

SC10: the vault lists global + per-project secrets with presence-only values, inherit-from-host rows rendered distinctly, and consumer links; a secret granted to sandboxed runs reaches a docker leaf's env while an ungranted sibling does not; no value is readable back through any API; project export ZIPs contain presence flags only

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EI-1`](EI-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `eabfce171` 2026-08-28 — feat(secrets): EI-10 secrets vault — presence-only API + Settings surface
- `a15d5bfba` 2026-08-28 — docs(roadmap): strike two EXT deps that pointed at work the atoms already own
- `18251518f` 2026-08-09 — feat(sandbox): EI-1 SandboxProvider seam + none provider + ResourceCeilings (#933)

### Named by atom id in the roadmap prose (13)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1309` — ### OWNER RULING — `EI-10`'s WORK-CONTAINERS dependency is STRUCK; the per-project rows are its own. 2026-08-28
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1311` — `EI-10` carries `EXT:WORK-CONTAINERS:WORK-R19 secrets store + per-project keychain backend`. Audited: **no
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1322` — **And `EI-10`'s own `done_when` already assigns it:** *"the vault lists global + per-project secrets with
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1325` — **RULED: strike the dep.** `EI-10` owns the per-project rows, layered over the keychain backend `SH-1`
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1332` — owner-deferred to `WF2WOR-12`, and `sandbox_providers/` contains no docker provider. So `EI-10` should be
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1348` — - [2026-08-28][EI-10] **Three of four `done_when` clauses shipped; the fourth is owner-deferred.**
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1358` — - [2026-08-28][EI-10] **DEVIATION — the grant-to-sandbox toggle is NOT shipped, and the atom stays
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1367` — - [2026-08-28][EI-10] **Dependency `EXT:WORK-CONTAINERS:WORK-R19` was struck by owner ruling and is
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1377` — - [2026-08-28][EI-10] **Consumer links are DERIVED, never indexed** — `consumers_for()` walks the
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1383` — - [2026-08-28][EI-10] **Export.** `plan_export` gained `secret_names=`, supplied by
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1389` — - [2026-08-28][EI-10] **Falsification.** Baseline `tests/test_secrets_vault.py` = **45 passed**.
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1402` — - [2026-08-28][EI-10] **DISCOVERY, not fixed here — a security control is over-broad by substring
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1425` — ## Execution log — EI-10 (secrets vault UX + presence-only API) — validated done

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `§8`, `§11`, `Session 7`, `R19`. Those tokens appear in its source plan at:

- `§8` `docs/roadmap/plans/EXECUTION-ISOLATION.md:42` — - **NEW-27** (per-project secrets store), honoring the overlap note: WORK-R19 (keychain-backed per-project secrets, secret-filtered leaf env) + WF2-R14/AUTO-R14 (`{{secret:KEY}}` templating) are **approved** — the REMAINDER is the standalone vault UX (§8): the settings surface, inherit-from-host …
- `§8` `docs/roadmap/plans/EXECUTION-ISOLATION.md:85` — env: dict[str, str]            # already secret-filtered by WORK-R19 (§8)
- `§8` `docs/roadmap/plans/EXECUTION-ISOLATION.md:207` — - **Zero credentials inside.** The host authorizes calls by construction (it spawned the exec channel — the same trust basis as `X-Internal-Secret` internal HTTP, messaging.py:75, but with no secret to leak because there is no network hop). Tool results entering the sandbox are just data; secrets…
- `§8` `docs/roadmap/plans/EXECUTION-ISOLATION.md:277` — | WORK-R3 workspace block / WORK-R20 container mode / WORK-R8 leases / WORK-R19 secrets store | **APPROVED elsewhere — consumed, not rebuilt.** §1.3 adds `sandbox:` beside the workspace block; WORK-R20 re-expresses as `sandbox: docker` capability; §3.1(5) reuses the R8 lease convention; §8 is UX-…
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:42` — - **NEW-27** (per-project secrets store), honoring the overlap note: WORK-R19 (keychain-backed per-project secrets, secret-filtered leaf env) + WF2-R14/AUTO-R14 (`{{secret:KEY}}` templating) are **approved** — the REMAINDER is the standalone vault UX (§8): the settings surface, inherit-from-host …
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:85` — env: dict[str, str]            # already secret-filtered by WORK-R19 (§8)
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:207` — - **Zero credentials inside.** The host authorizes calls by construction (it spawned the exec channel — the same trust basis as `X-Internal-Secret` internal HTTP, messaging.py:75, but with no secret to leak because there is no network hop). Tool results entering the sandbox are just data; secrets…
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:242` — ## 8. Secrets Vault UX (NEW-27 remainder over WORK-R19)
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:244` — The store, keychain backing, spawn-time resolution, secret-filtered leaf env, and `{{secret:KEY}}` templating are ALL approved (WORK-R19 in WORK-CONTAINERS §4.3; WF2-R14/AUTO-R14). This section builds ONLY the standalone UX those mechanisms lack:
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:246` — - **Settings → Secrets vault:** one surface listing secrets across scopes — global (the existing `.env` via `save_credential`, loader.py:255) and per-project (the WORK-R19 keychain namespaces) — with per-row: name, scope, **presence-only value display** (never readable back; re-enter to rotate), …
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:247` — - **Inherit-from-host rows as a first-class type:** an entry that names a key but omits the value inherits from the host environment at spawn (Air's pattern, already specified mechanically in WORK-R19) — the vault UX renders these distinctly ("inherited: set in your shell, never stored") so the u…
- `R19` `docs/roadmap/plans/EXECUTION-ISOLATION.md:249` — - **Sandbox integration:** the §1 `SandboxSpec.env` is populated ONLY from explicit vault grants (per WORK-R19's secret-filtered leaf env); the vault UI's per-secret "grant to sandboxed runs" toggle is the consent surface. `docker`/`lima` providers never see ungranted keys, and §5.2 guarantees gr…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
