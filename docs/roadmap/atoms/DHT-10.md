<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DHT-10` — Round-trip export: knowledge item / text artifact -> writer format (+ csv writer)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-HANDLING-TOOLS](../plans/DOCUMENT-HANDLING-TOOLS.md) (`DHT`, plan status `done`) |
| **Atom table** | [`../atomic/DHT.md`](../atomic/DHT.md) |
| **Execution position** | #177 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2 (the round-trip); T2.4

## Done when

a source argument on document_create resolves a knowledge-item id or a TEXT artifact slug to markdown and flows through the same writer path (no new endpoint); a binary artifact is refused; a knowledge item becomes a .docx artifact and a JSON/rows artifact becomes .csv/.xlsx; source name used as title only when body does not already open with an H1

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DHT-5`](DHT-5.md) | intra-repo | `done` |
| [`DHT-8`](DHT-8.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.4`. Those tokens appear in its source plan at:

- `T2.4` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:188` — | T2.4 | The round-trip: export an existing knowledge item or text artifact to any available writer format (a `csv` text-kind writer lands here too), reusing the tools from S1/S2 rather than a new endpoint | `mcp_artifacts.py` / the knowledge export path, tests | a knowledge item becomes a .docx …
- `T2.4` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:290` — - [2026-07-29][S2] **DONE (T2.1–T2.4).** pptx + pdf writers, `deck_create`, and the
- `T2.4` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:320` — **T2.4 reuses the writer path rather than adding an endpoint,** as the plan requires: a

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
