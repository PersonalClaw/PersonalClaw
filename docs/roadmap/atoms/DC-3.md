<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DC-3` — S3: Live audio — push-to-talk mic capture to STT

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-CAPABILITIES](../plans/DESKTOP-CAPABILITIES.md) (`DC`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DC.md`](../atomic/DC.md) |
| **Execution position** | #150 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, verified 2026-08-27 - CODE IS COMPLETE; what remains is an environment gate plus two owner tasks, so do NOT schedule an implementation slot. Shipped and live: makePushToTalk registered in main.js, the global_hotkey capability, a Settings control, the config field through its round-trip points, and an indicator with ONE writer where capture always wins, driven from real renderer state rather than 'we sent a press'. The composer path is real end to end and stops the tracks. UNVERIFIABLE-BY-CODE: that macOS delivers the chord (globalShortcut is stubbed) and that the real TCC prompt appears. TWO residuals worth recording: the done_when's wording says chunk upload but useMicRecorder calls rec.start() with NO timeslice, so it posts ONE blob - the wording is wrong, not the code; and '<=2s on faster-whisper local' is UNMEASURED. SMALLEST REMAINING UNIT: one dogfood pass on a real Mac, plus owner tasks 3 (mic-privacy pass) and 4 (approve the chord). Nothing to build. |

## Intent — what this is for

Session 3 — Live audio (T3.1-T3.3, V3)

## Done when

global-hotkey push-to-talk (bridge global_hotkey cap, chord configurable in Settings) captures only while held/toggled with an always-on capturing indicator; renderer getUserMedia (TCC via bridge grant) chunk-uploads to existing /api/stt/transcribe and a spoken sentence lands in the composer at cursor <=2s after release on faster-whisper local; system-audio probe returns unavailable with reason and docs/guides/desktop.md states mic-only; deny-mic path degrades with an actionable prompt and an already-registered-chord conflict surfaces cleanly. (Owner tasks 3/4: mic-privacy sanity pass + default chord.)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DC-2`](DC-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 52 (DC) — the Security panel driven in a browser tab; contextIsolation and the inverted updater rail both falsified

**Code evidence:**

- the code is complete and the pieces are named: `makePushToTalk` registered in `main.js`, the `global_hotkey` capability, a Settings control, the chord config through its round-trip points, and a capture indicator
- 🔑 THE INDICATOR HAS ONE WRITER AND CAPTURE ALWAYS WINS, driven from real renderer state rather than from 'we sent a press' — the difference between an indicator that reports the microphone and one that reports the intention to use it. An always-on capture indicator that could disagree with the microphone is worse than none
- the composer path is real end to end and stops the tracks afterwards, so a released key does not leave a live capture
- the system-audio probe returns unavailable WITH A REASON rather than silently offering nothing, and the guide states mic-only
- test_desktop_seam + test_desktop_install_kind 37/37; desktop node suite 358/358 across 75 suites (falsified: one contextIsolation flag flipped reds exactly 1 of 358; adding electron-updater to package.json reds exactly the inverted rail)

**Driven in the UI:** Not drivable here: `globalShortcut` is stubbed in the test environment, so neither that macOS delivers the chord nor that the real TCC prompt appears can be observed without the packaged app on a Mac. Two owner tasks remain (a mic-privacy sanity pass and the default chord).

**Notes:** Partial for hardware and consent-dialog reasons rather than for code. The honest recorded residual is worth keeping: the done_when says 'chunk upload' while the shipped recorder does something slightly different — a wording drift in the clause, not a gap in the feature, and it was written down rather than quietly satisfied.

## Recorded history

### Commits (2)

- `b7c7ad574` 2026-09-04 — docs(roadmap): flip MC-7/DC-3/DC-4/APE-6 + ES-8 decomposition
- `c70b3e3dc` 2026-08-16 — feat(desktop): DC-3 global push-to-talk mic capture into the composer

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:132` — **2026-09-04 — DC-3 validated done (impl pre-landed).** Push-to-talk mic capture to STT is code-complete and driven end to end in tests: `desktop/pushToTalk.js` (chord grammar + bind/conflict state machine), the `global_hotkey` capability, the Settings chord control, `voice.push_to_talk_chord` th…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:163` — - **REMAINING (V3 + owner tasks 3/4).** Electron was never launched, so **three legs are unexercised**: that macOS delivers the chord at all (`globalShortcut` is stubbed), the real TCC prompt, and a real microphone (jsdom implements neither `getUserMedia` nor `MediaRecorder`, so both are fakes). …
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:167` — - **DECISION (extend the existing tray; move its logic OUT of the glue).** T4.1 says "verify what `Tray` wiring already exists — extend, don't duplicate", and it did exist: DC-2/DC-3 left a static six-item menu built inline in `main.js` plus a `setTitle` capture indicator. So `createTray()` is de…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:168` — - **DECISION (ONE writer for the menu-bar title).** DC-3's `● Listening` indicator and DC-4's approvals badge want the same pixels, and two callers of `tray.setTitle()` would mean whichever fired last wins — a mic indicator silently erased by a count refresh is precisely the "indicator that lies"…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:208` — - **REMAINING (one unobservable leg + a genuinely unrunnable one).** Electron was never launched, so **"macOS draws a banner"** and **"clicking it fires the `click` handler"** are proven only against a recording double — the same class of remainder DC-2/DC-3/DC-4 each carry, and it pairs with DC-…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:220` — - **DESKTOP-ONLY GATING — two independent conditions, both existing patterns, no new gate invented.** The row lives inside the section that already renders `!ds.connected` as "Desktop app not connected" (Pattern A, the gateway seam), and the toggle additionally requires `desktopBridge()?.loginIte…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:222` — - **REMAINING (unobservable here, and it is the clause's own verb).** The `done_when` says the toggle "**survives reboot**". Nothing in this repo can observe that: it needs a signed `.app` bundle (DC-1), a real macOS login session, and a machine restart. Everything up to the OS boundary is driven…
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:594` — precedent `test_roadmap_atomic_status_sync.py` documents for `DC-3` ("implementation landed,

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `T3.1`, `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:100` — | T3.1 | Push-to-talk: global hotkey registration (bridge capability; default chord configurable in Settings) toggling capture state with tray/menu-bar indication | `desktop/capabilities.js`, settings wiring | hotkey captures only while held/toggled; visible indicator always on while capturing |
- `T3.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:155` — - **DEVIATION (T3.1 — the chord TOGGLES; it does not read key-release).** Electron's `globalShortcut` delivers one callback per press and exposes **no global key-up event**. A true "while physically held" global gesture therefore needs a native key-event tap — an accessibility-permission-class AP…
- `T3.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:156` — - **DONE T3.1** — Chord grammar + bind/conflict state machine over an injected `globalShortcut`; `bind()` releases ours FIRST so re-saving the same chord is not reported as a conflict with itself, and **conflict** ("another app owns it") is a separate result from **invalid**, because the two need…
- `T3.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:163` — - **REMAINING (V3 + owner tasks 3/4).** Electron was never launched, so **three legs are unexercised**: that macOS delivers the chord at all (`globalShortcut` is stubbed), the real TCC prompt, and a real microphone (jsdom implements neither `getUserMedia` nor `MediaRecorder`, so both are fakes). …
- `T3.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:102` — | T3.3 | Honest system-audio note: probe + documented deferral (macOS system audio requires screen-capture entitlement paths); `docs/guides/desktop.md` states mic-only for now | guide + probe stub returning `unavailable` with reason | doc + probe agree; no half-shipped system audio |
- `T3.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:158` — - **DONE T3.3** — `system_audio` joins the capability vocabulary specifically so "no, and here is why" is a state the panel and the gateway can READ rather than a silence. It probes `unavailable` with the Screen-Recording reason on EVERY platform, and that branch runs **before** the platform test…
- `T3.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:163` — - **REMAINING (V3 + owner tasks 3/4).** Electron was never launched, so **three legs are unexercised**: that macOS delivers the chord at all (`globalShortcut` is stubbed), the real TCC prompt, and a real microphone (jsdom implements neither `getUserMedia` nor `MediaRecorder`, so both are fakes). …
- `Session 3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:21` — > owner cares about — **mic/live audio capture** — is this plan's **Session 3** (unbuilt), not a
- `Session 3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:96` — ### Session 3 — Live audio (the owner's headline)
- `Session 3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:151` — ### Session 3 — Live audio (2026-08-16)
- `Session 3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:180` — - **DISCOVERY (the packaging rail that "earned its keep" was enforced by nobody).** Session 3 recorded that `desktop/test/packaging.test.js` caught a `pushToTalk.js` missing from `build.files` and that "nothing else in the suite would have noticed" — which was true twice over. Measured today: `gr…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
