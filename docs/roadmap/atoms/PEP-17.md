<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PEP-17` — Issue Radar app (suite 6/9)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PRODUCT-EXPERIENCE-PARITY](../plans/PRODUCT-EXPERIENCE-PARITY.md) (`PEP`, plan status `done`) |
| **Atom table** | [`../atomic/PEP.md`](../atomic/PEP.md) |
| **Execution position** | #434 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

GitHub/GitLab issue triage with AI-suggested labels + per-issue investigation notes kept locally. Pairs with Code Review; open-source-maintainer value.

## Done when

The issue-radar app ships as its own validated PersonalClawApps PR built to the app-creation contract (app.json, SDK-only imports, minimum permissions, test_provider.py/test_server.py, README, LICENSE), validated by adding it as a local Store source and driving it in the real UI; listed in the Store and recorded in ECOSYSTEM-TOOLING's exemplar list; a real repo's issues are triaged with suggested labels and locally persisted investigation notes.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:668` — - **2026-09-06 — `issue-radar` (`PEP-17`, suite 6/9) — a `tool` exemplar for REACHING A THIRD-PARTY
- **source plan** `docs/roadmap/plans/PRODUCT-EXPERIENCE-PARITY.md:969` — - **[2026-09-06] FLIP → `done`: `PEP-12`, `PEP-13`, `PEP-17`, `PEP-20`. The other five suite atoms stay

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
