<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PA-1` — Approval memory + ProactiveConfig foundation (Session 1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROACTIVE-ASSISTANT](../plans/PROACTIVE-ASSISTANT.md) (`PA`, plan status `done`) |
| **Atom table** | [`../atomic/PA.md`](../atomic/PA.md) |
| **Execution position** | #396 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1363 |

## Intent — what this is for

§1.4 Approval memory; Provider & Config Plug-in Map (`approval` MemoryKind, ProactiveConfig); §4 tools (triage_rules); Implementation Effort Session 1

## Done when

`user.approval.` prefix resolves to an `approval` MemoryKind in `_kind_from_key` and is excluded from `_NON_FACT_KEY_CLAUSE`; deterministic most-specific/deny-wins rule matcher, reply-grammar parser, and 24h/7d/30d suppression cooldowns are unit-tested pure functions; `ProactiveConfig` round-trips through the 5-point wiring (test_config_roundtrip green); `triage_rules` tool lists/adds/revokes rules with provenance

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`PA-2`](PA-2.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `a39cf457c` 2026-08-15 — feat(memory): PA-1 approval memory kind + deny-wins rule matcher

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:322` — resolve to `propose`) — the opposite direction from §1.3, and the same split PA-1 recorded for
- **source plan** `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:364` — PA-5. The pipeline's output is proposals plus a delivered digest; PA-1's `match_rules` is
- **source plan** `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:401` — by PA-1 (`config/loader.py:3753` + validation at `:3784` + the `_EDITABLE_CONFIG` PATCH
- **source plan** `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:505` — routes shipped with PA-1 and had **no frontend consumer at all**:
- **source plan** `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:814` — - [2026-09-02][PA-5] LANDED on main; atom flipped ✅, and this closes the PROACTIVE-ASSISTANT plan (6/6). `web/src/pages/inbox/TriageDigestCard.tsx` renders the digest with auto-done + `autonomyUndo`, tier badges, one-tap yes/no/always (Always surfaced when a pattern exists), the "what your machin…

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `§1.4`, `§4`, `Session 1`. Those tokens appear in its source plan at:

- `§1.4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:44` — - **Commitments are a MemoryKind, not a file**: `record_commitment` lives in memory_service.py L558-651 with hard-coded guardrails (opt-in `memory.proactive_commitments`, confidence ≥0.8, ≤3 active/day/agent, key `user.commitment.<md5-12>`), delivered by the heartbeat's `_deliver_due_commitments`…
- `§1.4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:45` — - **Memory semantic keys are constrained**: regex `^[a-z][a-z0-9_.]*[a-z0-9]$`, ≤100 chars, value ≤4096 B (vector_memory.py); kind is inferred from key prefix (`_kind_from_key`, memory_record.py L310); non-fact prefixes are excluded from ambient fact injection via `_NON_FACT_KEY_CLAUSE` (vector_m…
- `§1.4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:257` — | Memory/knowledge conflation creeping in | boundary stated per artifact (§1.4, §2 preamble); review checklist item: no code path writes the other store |
- `§1.4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:296` — 8. Nothing in the triage or journal code paths writes to the other store: grep-level audit shows knowledge writes only in §2 paths, memory writes only in §1.4 rules + §2.4 lessons.
- `§1.4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:363` — rule is consulted — §1.4's routing table and §1.6's budget floor are PA-3, the digest card is
- `§1.4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:609` — `error` key entirely (`help_reason`) — the grammar refusing with a help line is §1.4's documented
- `§4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:23` — - **NEW-26** — Proactive Triage flagship: scheduled digest-collect → classifier gate → tiered strict-JSON proposals → persistent approval memory → trivial-tier auto-execution → run-outcome ranking → §1 (all sub-sections), §4 tools, §5 FE, sessions 1-3
- `§4` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:24` — - **NEW-23** — Personal Decision Journal: horizon-triggered review, outcome capture, calibration record, LEARN-R18 pairing → §2, §4 tools, §5 FE, sessions 4-5
- `Session 1` `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:281` — - **Session 1 — Approval memory + config**: `user.approval.` prefix + `approval` kind + non-fact exclusion; deterministic rule matcher (most-specific, deny-wins); reply grammar parser (pure function, exhaustively unit-tested); suppression cooldowns; `ProactiveConfig` four-point wiring; `triage_ru…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
