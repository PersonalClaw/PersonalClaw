<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PCS-9` — V2 live-provider verification of cache telemetry (real Anthropic + OpenAI runs)

| | |
|---|---|
| **Status** | `blocked` |
| **Plan** | [PROMPT-CACHE-SUBSTRATE](../plans/PROMPT-CACHE-SUBSTRATE.md) (`PCS`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PCS.md`](../atomic/PCS.md) |
| **Execution position** | #417 of 684 in dependency order |
| **Startability** | not startable |
| **Blocked reason** | ENVIRONMENT-GATED (owner ruling 2026-08-28 split this from PCS-7's shipped numbers half). Cannot be closed from this environment: (a) no Bedrock inference-profile id resolves to a price row, so a real run renders 'saved unpriced' — needs _rates id normalisation (a repo-wide pricing change); (b) no OPENAI_API_KEY here (PCS-3 now supplies the OpenAI AUTOMATIC posture the run depends on). Both gates are credentials/environment, not code. AUDIT UPDATE 2026-09-05 (PR #2489 merged): the _rates id-normalisation code half landed — every Bedrock inference-profile shape (any region prefix; version pair before a trailing date stamp) resolves to its family price row, pinned with a vacuity partner. Two gates remain, both environment: the live Anthropic+OpenAI telemetry runs (no OPENAI_API_KEY here), and one data gap — claude-fable-5 has no price row (needs verified public pricing, never invented). |

## Intent — what this is for

Session 3 / V2; split from PCS-7 per OWNER RULING 2026-08-28

## Done when

Real multi-turn Anthropic + OpenAI runs report rising hit-rate / non-zero saved-USD (turn1 creation, turns2+ read), OpenAI reports vendor reads with no marker sent, an undeclared/Ollama model runs byte-identical with zeros, toggling config off stops the marker while ordering holds.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PCS-7`](PCS-7.md) | intra-repo | `done` |
| [`PCS-3`](PCS-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 43 (PCS) — vendor rail falsified against the real tree; toggle driven

**Code evidence:**

- MEASURED as genuinely environment-gated, not unfinished: the atom is the only one in the plan carrying status `blocked`, and its blocked_reason names a concrete, checkable obstacle rather than a vague one — no Bedrock inference-profile id resolves to a price row, so a real run would render 'saved unpriced' until the rate-id normalisation lands
- every mechanism V2 would exercise is confirmed by the other eight atoms and their 78 + 39 passing tests; what is missing is a live provider, credentials and a multi-turn run, none of which this environment can supply
- the split itself was an owner ruling (2026-08-28) separating the shipped-numbers half (PCS-7) from live verification, which is why PCS-7 is confirmable while this is not
- 78 across marker/wire-translation/cache-usage/single-store/pricing-savings/citation-rail; bedrock-models app 39/39

**Driven in the UI:** Cannot be driven here by construction — that is the atom's content.

**Notes:** Confirming a `blocked` atom means confirming the BLOCK, and this is the second time in the campaign that has been the right verdict (the first was a genuinely owner-gated todo). The distinction worth keeping: this reason names a specific missing capability with a named fix (price-row id normalisation), so a later reader can tell whether the block still holds. A blocked_reason that only said 'needs real credentials' would be unfalsifiable and would outlive its cause.

## Recorded history

### Commits (4)

- `daa3b999b` 2026-09-06 — docs(roadmap): flip SH-12 and DCU-4; stop the frontier claiming owner work is startable
- `b0d51b429` 2026-09-05 — docs(roadmap): flip DC-6 + HC-5 done; EI-6/PCS-9 audit annotations; HC-5 exec-log
- `d64a64dce` 2026-09-05 — fix(pricing): every Bedrock inference-profile shape resolves to its price row (PCS-9 code half)
- `7869b1341` 2026-09-02 — chore(roadmap): execute the owner-ruled PCS-7 split (numbers half done, PCS-9 minted)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:993` — is lifted verbatim into a newly minted **`PCS-9`** ("V2 live-provider verification of cache telemetry
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:997` — depends on). `PCS-9.deps = [PCS-7, PCS-3]`. The dag regenerates to **661 atoms, 0 dangling**; the

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `Session 3`. Those tokens appear in its source plan at:

- `Session 3` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:247` — ### Session 3 — The measurement surface (**the producer — F3; do T3.1 first if merged with S2**)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
