<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-6` — `workflow replay <run_id>`: recorded-response provider + trajectory diff

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #464 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1329 |

## Intent — what this is for

We have a resume CACHE, not a replay. `journal.lookup` serves only SUCCESS states and the journal file is never rewritten; `harness/replay.py` replays recorded MCP traces as an offline tool server — a dev-test facility, not a run replay. The controller reads wall clock live (`_wake_due_nodes`). Fluxtion's requirement is that replay be structural rather than retrofitted: recorded inputs re-run on the same processor version reproduce a decision path. We are ~80% there and never cash it in — `_store_prompt` already persists the fully-resolved prompt and its docstring already says "Required for trajectory replay (§5)".

## Done when

A `workflow replay <run_id>` verb re-drives `frontier()` against a provider that returns the run's own recorded responses, and diffs the resulting trajectory against the original, reporting the first divergent node. The nondeterminism envelope a run depended on is journaled so replay is possible at all: clock reads behind a seam, provider responses by `output_ref`, and the resolved prompt already stored. Divergence is a first-class outcome, not a failure — a template edit SHOULD diverge, and the verb's job is to say where. Verified by replaying a completed multi-node run to a byte-identical trajectory, and by an edited prompt diverging at exactly the edited node and nowhere earlier.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-4`](PP-4.md) | intra-repo | `done` |

### Unblocks (1)

[`PP-7`](PP-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `960f6a6cf` 2026-08-14 — feat(workflows): PP-6 workflow replay verb with recorded-response provider, clock seam, and trajectory diff

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:97` — (`PP-6`), trajectory signatures (`PP-7`), edge statistics (`PP-8`), consumer liveness (`PP-10`),
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:140` — ├─→ PP-6 ─→ PP-7                   │
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:647` — - **2026-08-14 — `PP-6` DONE.** `workflow replay <run_id>` (`personalclaw/workflows/replay.py` +
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:658` — - **2026-08-14 — `PP-6` DEVIATION.** Replay re-drives `frontier()` in a dedicated `ReplayDriver`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:667` — - **2026-08-14 — `PP-6` DISCOVERY.** (a) `_adaptive_duration` floors `duration_secs` with `int()`,
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:980` — - **2026-08-15 — `PP-7` DEVIATION (built new over PP-6's tuples, did NOT reuse `TrajectoryStep`).**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:981` — The territory said "reuse PP-6's `TrajectoryStep` where it fits, or directly from ordered

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `§5`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:70` — | Autonomy as one policy object | AUTONOMY-GUARDRAILS | **`AG-13`** (new atom there), depending on `PP-14` so the autonomy ceiling and the supervisor policy are ONE object rather than two competing declarations. The two-level `Ceiling ∩ Profile` model it composes is PLATFORM-HARDENING-FLOORS §5's…
- `§5` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:76` — and the work-unit noun — has no owner, and is §1–§5 below.
- `§5` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:121` — ## §5 One work-unit noun (`PP-14`…`PP-16`)
- `§5` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1578` — became `async` and now funnels through the same primitive, keeping only §5.2's substrate rule as

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
