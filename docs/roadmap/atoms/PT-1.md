<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PT-1` — S1: personality registry + identity behaviors + persona-snippet backend generalization

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PERSONALITY-THEMES](../plans/PERSONALITY-THEMES.md) (`PT`, plan status `done`) |
| **Atom table** | [`../atomic/PT.md`](../atomic/PT.md) |
| **Execution position** | #491 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design 'S1 — The personality registry + identity behaviors' and 'S1 — Persona snippet generalization'; Task breakdown Session 1 (T1.1 registry/types + phosphor scheme, T1.2 PersonalityProvider, T1.3 display-name offer flow, T1.4 persona generalization, V1); Contracts C1/C3/C4/C5

## Done when

personalities.ts registry + closed behavior block typechecks with personalclaw/retro-terminal/claw-arcade entries; new phosphor scheme passes schemeContrast.test.ts (AA both modes) without weakening it; PersonalityProvider swaps document.title/favicon/data-personality/wordmark and restores byte-identical defaults on deactivate (survives reload); rename picker is propose-don't-write (unchecked leaves agent.bot_name untouched, checked patches it); backend _PERSONA_THEMES closed set replaces the hardcoded lumon branch with lumon as entry #1 and rejects unknown theme values; personalityA11y.test.ts structural tests green; make lint + make test + web typecheck/vitest/build green (per 2026-07-28 execution log)

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (3)

[`PT-2`](PT-2.md) (`done`), [`PT-3`](PT-3.md) (`done`), [`PT-4`](PT-4.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 48 (PT) — both personalities driven through switch, reload and residue check

**Code evidence:**

- 🔑 DRIVEN THROUGH THE WHOLE IDENTITY SWAP AND MEASURED ON THE DOCUMENT, not on the picker. Before: title 'PersonalClaw', favicon '/claw.svg', data-personality 'personalclaw'. After switching to Retro Terminal: title 'TERM://PersonalClaw', favicon '/icons/personality-retro-terminal.svg', data-personality 'retro-terminal'
- 🔑 IT SURVIVES A FULL RELOAD: all three readings identical after reload, so the swap is persisted state rather than in-memory decoration
- 🔑 THE PROPOSE-DON'T-WRITE CLAUSE IS A REAL CONSENT GATE, driven: clicking a personality opens a dialog that (a) states what changes immediately and that it is reversible — 'The colors, wordmark, tab title, density, and motion dials change right away — pick another personality any time to change them back' — and (b) puts the CONFIG WRITE behind its own switch, NAMING THE KEY: 'Also rename the assistant to TERM … This writes `agent.bot_name`, which the assistant uses to refer to itself. Turn it off to keep the name you have.'
- the picker's own intro makes the same promise the dialog keeps: 'picking one never changes your saved configuration without asking'
- the phosphor scheme passes schemeContrast.test.ts inside the 251, i.e. AA in both modes without the contrast rail being relaxed to admit it
- personalityA11y 53/53 (falsified: a dangling shellElement id reds exactly one rail); design+personality suites 251/251

**Driven in the UI:** Switch, reload, and the on-document readings at each step.

**Notes:** 🪤 A FINDING COLLAPSED HERE, and the shape is new to this campaign: I clicked Retro Terminal, measured the document, and saw NOTHING had changed — which read as a dead picker. What I had actually hit was the consent dialog I had not yet confirmed. Eleventh collapsed finding, and the first where the apparent missing effect was a GATE I had not passed rather than a defect or a bad probe. Separating the immediate-and-reversible visual change from the persistent config write, with the key named in the sentence that asks, is the strongest form of propose-don't-write I have audited.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:179` — - [2026-08-16][S2/T2.1] Gate: web typecheck green · FULL `npm test --workspace web` **276 files / 2772 tests passed (91.8s)**, zero failures (+3 files / +43 tests over the parent) · `npm run build --workspace web` green (504 SPA assets, 0 audio) · `tests/test_roadmap_dag_derived.py` 9 passed and …
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:190` — - [2026-08-16][S2/T2.4] Gate: `npm run typecheck --workspace web` clean · FULL `npm test --workspace web` **293 files / 2996 tests passed**, zero failures — baseline measured first on the branch point (3c6f1374): **2931 tests**, so +65 with no regressions and no ratchet or baseline touched · `npm…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:276` — - [2026-08-21][amendment · atom `PT-7`] DONE (a "sound human" toggle, per chat and per agent). **New `natural_voice` control at TWO scopes**, resolving per-conversation over the agent default. (a) **The resolution order is stated exactly ONCE** — `natural_voice.NATURAL_VOICE_PRECEDENCE = ("conver…

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `T1.1`, `T1.2`, `T1.3`, `T1.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/PERSONALITY-THEMES.md:133` — | T1.1 | `personalities.ts` registry + types (C1) with the two proof entries stubbed (base schemes: a mono-green fork added to `SCHEMES` for retro-terminal; an existing scheme for claw-arcade) | `web/src/design/personalities.ts` (new), `web/src/design/schemes.ts` (one added scheme) | registry typ…
- `T1.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:134` — | T1.2 | `PersonalityProvider`: resolve + apply (title, favicon link swap, `data-personality` attr, wordmark label via context), persist `personality` in the `appearance` overrides, full default-restore on deactivate; standard-scheme path provably untouched | `web/src/app/personality.tsx` (new), …
- `T1.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:156` — - **Favicon/title residue** after a crash mid-personality (defaults restored on deactivate, but a hard reload under an active personality must re-apply, not leak) — the provider re-applies from the persisted override on mount and restores on `personality: undefined`; T1.2's restore test covers th…
- `T1.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:135` — | T1.3 | Display-name offer flow: activation dialog with an explicit "Also rename the assistant to <name>" checkbox → `api.patchConfig('agent.bot_name', …)`; deactivation offers the restore; never silent (propose-don't-write) | `web/src/pages/settings/DesignPanel.tsx` (Personality picker section …
- `T1.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:23` — the Settings → Design `PersonalityPicker` with a propose-don't-write rename consent, and the T1.4
- `T1.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:136` — | T1.4 | Persona generalization (C4): `_PERSONA_THEMES` closed set, `_maybe_inject_persona` generalized, validation widened, lumon becomes entry #1 (clean break — no special case remains); second bundled snippet for retro-terminal | `src/personalclaw/dashboard/chat_utils.py`, `src/personalclaw/da…
- `T1.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:165` — - [2026-07-28][S1] DONE (the personality registry + identity behaviors). (a) **`web/src/design/personalities.ts`** — `Personality` + a CLOSED, typed `behavior` block (`displayName`, `wordmarkLabel`, `faviconHref`, `personaSnippet`, `uiDensity`, `documentTitle`). Three constraints are the design: …
- `Session 1` `docs/roadmap/plans/PERSONALITY-THEMES.md:129` — ### Session 1 — Registry + identity layer + persona generalization

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
