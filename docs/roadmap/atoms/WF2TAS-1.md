<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2TAS-1` — Projection core: WorkflowTaskBinding + Task fields + auto-materialization

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-TASKS-SOPS](../plans/WORKFLOWS-V2-TASKS-SOPS.md) (`WF2TAS`, plan status `done`) |
| **Atom table** | [`../atomic/WF2TAS.md`](../atomic/WF2TAS.md) |
| **Execution position** | #623 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##195 |

## Intent — what this is for

§1 State Projection (R1), Projection Enforcement (R5-core), Task Body Contract (R12), New Field WorkflowTaskBinding, Materialization Flow

## Done when

tasks/models.py gains WorkflowTaskBinding, TaskStatus.SKIPPED and six projection fields; workflows/materialize.py owns the state→status table, fingerprint dedup, fan-out caps and the write-rejection rule; round-trip tests green (92 tests, S55)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:WORKFLOWS-V2:engine InstanceState taxonomy + SUCCESS_STATES/TERMINAL_STATES sets` | cross-plan (EXT) | **unresolved** |

### Unblocks (5)

[`WF2TAS-11`](WF2TAS-11.md) (`done`), [`WF2TAS-2`](WF2TAS-2.md) (`done`), [`WF2TAS-3`](WF2TAS-3.md) (`done`), [`WF2TAS-4`](WF2TAS-4.md) (`done`), [`WF2TAS-6`](WF2TAS-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (32 line(s))

This atom's scope cites `§1`, `R1`, `R5`, `R12`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:26` — - **TASK-R1** → §1 State Projection (typed block/status taxonomy + card previews)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:27` — - **TASK-R2** → §1 Verified Done (done_criterion, evidence, engine-owned transitions, diagnostics sweep, clean-exit template, granularity lint)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:30` — - **TASK-R5** → §1 Projection Enforcement (API-rejected writes, actor matrix, fingerprint dedup, fan-out caps, idempotent recompute)
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:37` — - **TASK-R12** → §1 Task Body Contract + §6 Context Bundles
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:42` — - **TASK-R17** → §1/§5 Cascade-Fail Propagation
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:258` — `requirements` preflight: a def whose requirements are unmet **fails at suggestion time naming the missing item** instead of surfacing a suggestion that dies mid-run. The preflight uses the real availability seam that already exists: provider bundles may export module-level `availability() -> (bo…
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:422` — - Stages carry `enabled: bool` — muted = skipped-but-visible; projects as `skipped` per §1 (not removed), letting users trim a checklist instance (e.g., mute the staging gate for a hotfix) without structural edits.
- `§1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:453` — | Manual block (external reason) | `gate{kind: approval, prompt: "Blocked: {reason}"}` — pauses projection per §1 |
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:26` — - **TASK-R1** → §1 State Projection (typed block/status taxonomy + card previews)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:35` — - **TASK-R10** → §5 Frontier/Next Projections, Evented Unblock, TTL'd Leases
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:36` — - **TASK-R11** → §2 Parameter Pre-Fill Contract + Requirements Preflight
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:37` — - **TASK-R12** → §1 Task Body Contract + §6 Context Bundles
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:38` — - **TASK-R13** → §4 Per-Stage Mute, Observed Tool Profiles, Step-Scoped Approval Memory
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:39` — - **TASK-R14** → §2 Composition-Direction Lint (checklist ⊃ SOP, acyclic refs)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:40` — - **TASK-R15** → §7 Surfacing UX (composer chips, validated deep-links)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:41` — - **TASK-R16** → §2 Blueprint Sessions (third surfacing mode)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:30` — - **TASK-R5** → §1 Projection Enforcement (API-rejected writes, actor matrix, fingerprint dedup, fan-out caps, idempotent recompute)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:103` — ### Projection Enforcement (R5)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:108` — - **Three-actor transition matrix** (CORE precedent, R5 batch-5): allowed transitions are per-ACTOR (engine / user / agent), covering standalone tasks too. The agent (via `task_update` tool) may only move tasks to propose states (`blocked(needs_input)`, review); claim states (`in_progress` by eng…
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:142` — fingerprint: str = "" # sha1(source_ref or title+body)[:16] — dedup key (R5)
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:555` — 1. **Per-file JSON, no transactions** — materialization dedup and lease CAS rely on flock + atomic_write discipline, not transactions. Mitigation: fingerprint-lookup-before-create, `single_flight` around lease acquire, projection-as-idempotent-recompute (R5) so any race resolves on the next rebuild.
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:566` — - **7 sessions** (after Workflows v2 Slices 0-4) — was 3; the approved high-priority set (R1/R2/R3/R4/R5/R6) adds ~1 session, the medium/low set adds ~3 more.
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:567` — - **Session 1 — Projection core:** `workflow_binding` + new Task fields (`blocked_kind`, `preview`, `done_criterion`, `evidence`, `attempts`, `fingerprint`) in tasks/models.py; `TaskStatus.SKIPPED`; auto-materialization with fingerprint dedup + fan-out caps; typed state projection table (R1, R5-c…
- `R5` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:568` — - **Session 2 — Verified done + enforcement:** engine-owned criterion execution (reuse `run_verify_command`), pass-state gating, completion records; three-actor transition matrix + managed-write rejection on the task façade; cascade-fail propagation + debounced notify; stuck-work diagnostics swee…
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:37` — - **TASK-R12** → §1 Task Body Contract + §6 Context Bundles
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:86` — `blocked_kind` is a field on the Task (`blocked_kind: str = ""`), not a status explosion — `TaskStatus` (tasks/models.py:19) gains exactly one new member, `SKIPPED`. FE `taskMeta` STATUSES (board columns) adds the mapping; per-surface **label mapping is configuration** — FE board / Slack / future…
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:119` — ### Task Body Contract (R12)
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:471` — **Recon correction:** `create-task` already exists as a core-native `ActionProvider` (`action_providers/`, ABC at base.py:50, registered idempotently by `_ensure_default_providers_registered()`, already present in `ALLOWED_HOOK_PROVIDERS` — src/personalclaw/validation.py). The workflow engine's `…
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:492` — ### Context Bundles for Externally-Sourced Tasks (R12)
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:494` — When `create-task` materializes from an external source (inbox item, issue-like entity), it injects a structured user-editable context bundle `{source_id, title, url, description, status}` and reuses source-side naming conventions for the task/run name. Bundle text originating outside PClaw is fe…
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:502` — - `TaskStatus.SKIPPED` + `blocked_kind` badge + `preview`/progress render on board cards; column/label mapping extends `taskMeta` STATUSES (per-surface label mapping is configuration, never a state fork — R12).
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-TASKS-SOPS.md:567` — - **Session 1 — Projection core:** `workflow_binding` + new Task fields (`blocked_kind`, `preview`, `done_criterion`, `evidence`, `attempts`, `fingerprint`) in tasks/models.py; `TaskStatus.SKIPPED`; auto-materialization with fingerprint dedup + fan-out caps; typed state projection table (R1, R5-c…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
