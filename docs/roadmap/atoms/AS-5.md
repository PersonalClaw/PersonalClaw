<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AS-5` — Widget action bridge: extract useWidgetActionBridge, route non-chat hosts, harden

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AMBIENT-SURFACES](../plans/AMBIENT-SURFACES.md) (`AS`, plan status `done`) |
| **Atom table** | [`../atomic/AS.md`](../atomic/AS.md) |
| **Execution position** | #74 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1511 |

## Intent — what this is for

Amendment 2026-07-26 round 1 (interactive in-chat widgets), tasks T5-A1 and T5-A2; §5.4's raw-HTML widget path (the leg §5.4 leaves in the iframe). Bridge exists un-extracted today (WidgetFrame.tsx→ne:widget-action→ChatPage.tsx).

## Done when

web/src/ui/widget/useWidgetActionBridge.ts extracted with ChatPage behavior byte-identical; clicking a [data-action] in an artifact-library preview (chatEmbeds) and in the tile band lands the [UI] turn in a chat session via the ne:launch-chat path; the child→parent wire contract (widget-height/widget-action/widget-error; reserved __edit_mode_* parent→child) is documented with a test fixture; isTrusted and e.source spoof fixtures produce no turn; oversized payloads clip at 16 KiB with an honest …truncated marker.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AS-1`](AS-1.md) | intra-repo | `done` |

### Unblocks (1)

[`AS-6`](AS-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `0ef627ee8` 2026-08-17 — feat(widgets): AS-3 artifact iteration — EDITMODE tweak rail + click-annotation corrections
- `39d6427bb` 2026-08-16 — feat(widgets): AS-5 widget action bridge extracted, non-chat hosts routed

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:463` — ### 2026-08-16 — `AS-5` widget action bridge (Amendment round 1, T5-A1 + T5-A2) — DONE
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:520` — Both cheap iteration paths landed on the channel AS-5 reserved. A tweak now costs one
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:567` — renders `kind:react` through `ReactWidgetFrame`, which by AS-5's ruling carries no
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:805` — `[UI]`-turn bridge (AS-5), `controller.resume` (whose docstring already advertised "the out-of-band

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `§5.4`. Those tokens appear in its source plan at:

- `§5.4` `docs/roadmap/plans/AMBIENT-SURFACES.md:29` — - **NEW-6 am.5** — annotate mode on visual artifacts (element-anchored correction directives) + widget trees whose events feed back into execution → §4, §5.4
- `§5.4` `docs/roadmap/plans/AMBIENT-SURFACES.md:295` — | Rendered genui as an injection surface | Controlled rendering: registry-restricted output, schema validation, drop-invalid, no raw HTML in the host tree; actions limited to declared vocabulary; tile-originated fires bound by the trigger's frozen capability set (§5.4) |
- `§5.4` `docs/roadmap/plans/AMBIENT-SURFACES.md:350` — **Interactive in-chat widgets with a round-trip event bridge — mostly ALREADY BUILT; formalize and harden it.** Code recon: the round-trip exists today end to end. `widgetSrcdoc.ts` builds the sandboxed iframe doc (`sandbox="allow-scripts"` off a blob/null origin, strict CSP `connect-src 'none'`,…
- `§5.4` `docs/roadmap/plans/AMBIENT-SURFACES.md:360` — Folded into **Session 5** (already owns §5.4 action routing — the genui and raw-HTML action paths land as one reviewed surface); session count stays ~6.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
