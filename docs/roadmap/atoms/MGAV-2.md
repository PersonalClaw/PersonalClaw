<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MGAV-2` — Graph recall arm + backlink boost + evidence tags in hybrid retrieval

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MEMORY-GRAPH-AND-VAULT](../plans/MEMORY-GRAPH-AND-VAULT.md) (`MGAV`, plan status `done`) |
| **Atom table** | [`../atomic/MGAV.md`](../atomic/MGAV.md) |
| **Execution position** | #346 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2.1 graph arm in memory retrieval, §2.2 retrieval provenance tags; Session 2 (partial)

## Done when

Third retrieval arm resolves query entities via the same matcher, admits+boosts backlinked records with a floor above stopword noise, graph hits increment recall_count/visit_count into the existing promote_by_heat math, recall_evidence tags each hit, and graph-off/empty degrades to today's vector+keyword behavior

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MGAV-1`](MGAV-1.md) | intra-repo | `done` |

### Unblocks (3)

[`MGAV-3`](MGAV-3.md) (`done`), [`MGAV-5`](MGAV-5.md) (`done`), [`MGAV-7`](MGAV-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (13 line(s))

This atom's scope cites `§2.1`, `§2.2`, `Session 2`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:282` — | `vector_memory.py` hybrid retrieval (0.6/0.4) | **EXTENDED** | gains the graph arm + backlink boost (§2.1); vec/kw math untouched |
- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:470` — ### 2026-07-28 — Session 2 (partial): the graph recall arm (§2.1–2.2) — DONE
- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:484` — **A REAL RANKING BUG, found by driving recall rather than by unit tests.** §2.1
- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:500` — **§2.1 step 4 needs no new code** — graph-arm hits flow through the existing
- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:730` — overlap pass (same key → shared dotted key namespace → keyword overlap → the §2.1 graph arm, zero
- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:1019` — (`pref.facet.release.freeze` = "Sparrow freezes in December"), so only §2.1's third arm can surface
- `§2.1` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:1023` — no-API-key posture, where recall is keyword + graph, which is what §2.1 degrades between.
- `§2.2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:505` — **§2.2 evidence tags** ship as `recall_evidence` → `{record: [entity names]}`,
- `Session 2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:13` — Session 2 COMPLETE (recall arm 2026-07-28 + push reflex 2026-07-30); **the §1.3 knowledge-side alias
- `Session 2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:332` — - **Session 2 — recall arm + push reflex:** graph arm + backlink boost in hybrid retrieval, evidence tags in `recall_with_provenance`, push-context reflex + volunteer-events table + per-arm stats, knowledge-pipeline alias pre-pass, restricted-session guards.
- `Session 2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:407` — **Deferred to Session 2 by design:** the graph *recall arm* — this session ships the
- `Session 2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:470` — ### 2026-07-28 — Session 2 (partial): the graph recall arm (§2.1–2.2) — DONE
- `Session 2` `docs/roadmap/plans/MEMORY-GRAPH-AND-VAULT.md:522` — ### 2026-07-30 — Session 2 (remainder): the push-context reflex (§3) — DONE

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
