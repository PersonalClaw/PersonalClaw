<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE2-1` — Retrieval hardening + per-model savings accounting + subagent projection

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CONTEXT-ECONOMY](../plans/CONTEXT-ECONOMY.md) (`CE2`, plan status `done`) |
| **Atom table** | [`../atomic/CE2.md`](../atomic/CE2.md) |
| **Execution position** | #124 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1 (§1.1 content-hash result-id markers, §1.2 tool_result_get line addressing, §1.3 per-model savings accounting) + §2.5a subagent transcripts/results; Session 1

## Done when

SC#1 (content-hash r_<sha> ids dedupe identical raw to one file; fetch_slice line_start/line_end) + SC#2 (buried subagent finding recovered via typed digest + raw_ref through project_and_retain) + SC#4 (Settings→Tools savings card from tokenjuice_savings.json, GET /api/tools/savings) met; log [2026-07-26][S1] DONE, gate green (7986 passed)

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`CE2-2`](CE2-2.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 25 (CE2) — DRIVEN in a real browser

**Code evidence:**

- agents/native/read_gate.py:110 keeps the raw_ref map as '(resolved path, digest the read observed)', which is the content-addressing the atom's r_<sha> ids describe
- the freshness property is STRONGER than the atom claims: :19-20 requires 'the sha256 of the file's *full* bytes at read time equals its sha256 now. This is stronger than any turn window: a read taken one tool call ago is' stale if the file changed
- raw_ref has live consumers across eight modules including bg_compress.py, context_compaction.py and agents/native/builtin_tools.py — the reference survives into the compression path, which is what makes CE2-3's tool-row dropping safe
- 386 tests pass across the projection / compression / codegraph / tool-group modules (1 unrelated skip)

**Notes:** Confirmed on the retrieval half. The digest-now-equals-digest-then check is the detail worth recording: content-hash dedupe alone would happily serve a slice of a file that has since changed, and a turn-window heuristic would only narrow the race. Comparing the live digest closes it outright. The per-model savings accounting and the subagent projection clause were not driven — both need real turns.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/CONTEXT-ECONOMY.md:409` — `docs/roadmap/atomic/dag.json` carries **CE2-1…CE2-7** for CONTEXT-ECONOMY; CE2-8 arrived with
- **source plan** `docs/roadmap/plans/CONTEXT-ECONOMY.md:510` — `docs/roadmap/atomic/dag.json` carries CE2-1…CE2-7 only; CE2-10 arrived with the rev-18
- **source plan** `docs/roadmap/plans/CONTEXT-ECONOMY.md:661` — `docs/roadmap/atomic/dag.json` carries CE2-1…CE2-7 only; CE2-8/9/10 arrived with the rev-18

### Its plan's own record, via the scope references (18 line(s))

This atom's scope cites `§1`, `§1.1`, `§1.2`, `§1.3`, `§2.5`, `Session 1`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:25` — - **NEW-16** (reversible context compression: compress-cache-retrieve loop, lossy compressed originals behind unguessable hash markers + retrieval tool with byte/line ranges; type-routed compressors — JSON crusher, AST-aware code, prose model — over subagent transcripts and tool outputs; builtin/…
- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:30` — - **LEARN-R12** (approved, WORKFLOWS-V2-LEARNING-FLYWHEEL §1/§2.4): the `model_invoked: bool` axis already removes command-like skills from the surfacing embeddings and INDEX — the SKILLS-side context-budget reclaim is that plan's job and is only referenced here. This plan owns the orthogonal TOO…
- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:109` — 2. **Deterministic markers**: with §1.1 content-hash ids, projecting the same raw twice yields byte-identical previews and recovery hints — a resumed/replayed transcript re-serializes identically.
- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:125` — - **Kill switch + budget:** `tools.bg_compress_enabled` (default ON, fail-safe parse per the AUTONOMY-GUARDRAILS §5 tenet — but note this flag is a *feature* flag, not a guard flag: missing parses as the DEFAULT, documented in `_meta`); per-tick LLM-call cap so a backlog of old sessions can't bur…
- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:217` — - **Session 1 — retrieval hardening + accounting (§1, §2.5a):** content-hash result ids with legacy-read compat; `fetch_slice` line ranges + tool schema/hint updates; savings meter + store + `GET /api/tools/savings` + Settings card; subagent-result path through `project_and_retain` (raw_ref in pa…
- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:288` — - [2026-07-26][S1] DONE: retrieval hardening + savings accounting + subagent projection (§1, §2.5a). (a) **Content-hash result ids** — `result_store._content_id` = `r_<sha256(raw)[:12]>` replaces the count-based `_next_id`; `store_result` dedupes identical raw to ONE file (idempotent, touches mti…
- `§1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:289` — - [2026-07-26][S1] DEVIATION: the plan lacks an executor-ready `| ID | Task |` table (it has a §7 prose "Implementation Effort"). Derived Session-1 tasks from §1/§2.5a and the Success Criteria; no scope guessing beyond what those name. Recorded here for auditability.
- `§1.1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:109` — 2. **Deterministic markers**: with §1.1 content-hash ids, projecting the same raw twice yields byte-identical previews and recovery hints — a resumed/replayed transcript re-serializes identically.
- `§1.3` `docs/roadmap/plans/CONTEXT-ECONOMY.md:125` — - **Kill switch + budget:** `tools.bg_compress_enabled` (default ON, fail-safe parse per the AUTONOMY-GUARDRAILS §5 tenet — but note this flag is a *feature* flag, not a guard flag: missing parses as the DEFAULT, documented in `_meta`); per-tick LLM-call cap so a backlog of old sessions can't bur…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:16` — **Wave:** 0-1 — the compressor library, retrieval hardening, savings accounting, background compression service, and tool-group lifecycle are all v2-INDEPENDENT (they extend shipped subsystems: TokenJuice, the native runtime toolset assembly, the consolidation cadence). The workflow-node consumer…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:31` — - **WF2-R6 / WF2-R13** (approved, WORKFLOWS-V2 §2 Context Lifecycle + Run Ledger): node output offloading to `runs/<id>/artifacts/` and crystallize-before-prune journal digests are the ENGINE's context lifecycle and stay speced there. This plan supplies the shared compressor library those mechani…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:95` — An LLM summarizer (`one_shot_completion(use_case="background")` — the `reasoning`-axis resolution path, never the native chat runtime) for long natural-language outputs, used ONLY where latency is already tolerable: the background compression service (§4) and the subagent-result path (§2.5). It i…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:217` — - **Session 1 — retrieval hardening + accounting (§1, §2.5a):** content-hash result ids with legacy-read compat; `fetch_slice` line ranges + tool schema/hint updates; savings meter + store + `GET /api/tools/savings` + Settings card; subagent-result path through `project_and_retain` (raw_ref in pa…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:225` — Sessions 1-3 (NEW-16) and 4-5 (NEW-22) are independent tracks; either alone is a Wave-0 win. Session 6 depends only on Session 2's tree-sitter install (or ships its own) and is otherwise independent. The engine consumers (§2.5b node outputs, §5.4 per-template groups) activate when WORKFLOWS-V2 Sl…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:288` — - [2026-07-26][S1] DONE: retrieval hardening + savings accounting + subagent projection (§1, §2.5a). (a) **Content-hash result ids** — `result_store._content_id` = `r_<sha256(raw)[:12]>` replaces the count-based `_next_id`; `store_result` dedupes identical raw to ONE file (idempotent, touches mti…
- `§2.5` `docs/roadmap/plans/CONTEXT-ECONOMY.md:289` — - [2026-07-26][S1] DEVIATION: the plan lacks an executor-ready `| ID | Task |` table (it has a §7 prose "Implementation Effort"). Derived Session-1 tasks from §1/§2.5a and the Success Criteria; no scope guessing beyond what those name. Recorded here for auditability.
- `Session 1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:99` — - **Subagent transcripts/results** (v2-independent, Session 1-2): `SubagentManager`'s result handling routes through `project_and_retain` instead of the blind 3000-char injection cap — the parent session receives a type-projected digest carrying a raw_ref, so "the subagent found it but the cap at…
- `Session 1` `docs/roadmap/plans/CONTEXT-ECONOMY.md:217` — - **Session 1 — retrieval hardening + accounting (§1, §2.5a):** content-hash result ids with legacy-read compat; `fetch_slice` line ranges + tool schema/hint updates; savings meter + store + `GET /api/tools/savings` + Settings card; subagent-result path through `project_and_retain` (raw_ref in pa…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
