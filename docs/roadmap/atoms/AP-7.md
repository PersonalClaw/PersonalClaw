<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AP-7` — Project-fingerprint auto-surfacing + pack update flow + pack store FE + validation sweep

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-PACKS](../plans/AGENT-PACKS.md) (`AP`, plan status `done`) |
| **Atom table** | [`../atomic/AP.md`](../atomic/AP.md) |
| **Execution position** | #40 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1529 |

## Intent — what this is for

§7 Project-Fingerprint Auto-Surfacing (amendment d, second half); §1 pack_owned update flow; §9 fingerprint_rejections store; pack store/detail FE. Session 6. Success criteria 7, 8.

## Done when

packs/fingerprint.py zero-LLM scanner over Project.workspace_dir matches declared fingerprints on project-create and on-demand only; a Terraform-shaped dir surfaces a propose-only pack card with confidence + the §3.1 inspect report; rejecting once is remembered per (project,pack) and never re-nags; packs.fingerprint_enabled=false stops scanning entirely; pack update overwrites only pack_owned components and skips computedHash-drifted user-edited copies with a visible drift note; pack store/detail FE ships and the export→wipe→import round-trip validation sweep on a second PERSONALCLAW_HOME passes.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AP-3`](AP-3.md) | intra-repo | `done` |
| [`AP-4`](AP-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (6)

- `9dfb3aba3` 2026-08-17 — chore(roadmap): cite the landing batch #1529 on AP-7 and CC-7
- `b74426f82` 2026-08-17 — chore(reference): re-render the agent reference for AP-7's pack routes
- `14d05a4cc` 2026-08-17 — feat(packs): AP-7 project-fingerprint auto-surfacing, pack_owned update flow, pack store FE
- `1afc282e2` 2026-08-15 — feat(packs): AP-5 external agent-format export
- `c991af28a` 2026-08-08 — feat(packs): AP-3 requirements resolution + connector catalog — configure/substitute/skip, setup-skill, PacksConfig (#916)
- `57e77db23` 2026-08-08 — feat(packs): AP-2 pack import core — inspect-without-write, journaled leaves-first commit, trust scan (#915)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:309` — ### 2026-08-16 — `AP-7` DONE (fingerprint auto-surfacing, pack_owned update flow, pack store FE)
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:381` — duplications are present on `origin/main`; only the `AP-7` row and section were edited here.
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:418` — AP-7's declared scope ("the export→wipe→import round-trip validation sweep on a second
- **source plan** `docs/roadmap/plans/AGENT-PACKS.md:457` — **Not done here (unchanged scope):** no pack store / detail / export FE — AP-7 owns it. AP-4's

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§7`, `§1`, `§9`, `Session 6`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/AGENT-PACKS.md:26` — - **NEW-12 amendment (d)** (inbound half: an external skill-catalog importer pulling external skill catalogs into the skill store; project-fingerprint auto-surfacing that scans workspace file patterns, confidence-scores a project type, and proposes the matching pack) → §6, §7, source an external …
- `§7` `docs/roadmap/plans/AGENT-PACKS.md:255` — | **LEARNING-FLYWHEEL** (approved) | Its `{source, computedHash}` lock on imported templates is the drift primitive pack updates rely on (§1 pack_owned); its proposal queue is the review surface the prompt-card importer (§4.3) and fingerprint proposals (§7) feed. Packs are "the sharing channel fo…
- `§7` `docs/roadmap/plans/AGENT-PACKS.md:273` — - **Session 6 — fingerprinting + polish (§7):** fingerprint scanner + pack-side fingerprint declarations + propose-only cards + rejection memory; pack update flow (pack_owned overwrite, drift skip); pack store/detail FE; as-a-user validation sweep (export→wipe→import round-trip on a second `PERSO…
- `§7` `docs/roadmap/plans/AGENT-PACKS.md:330` — **DEVIATION 1 — a third bundled pack, `infra-ops`.** §7's example is an `infra-ops` pack matching a
- `§7` `docs/roadmap/plans/AGENT-PACKS.md:345` — **DEVIATION 3 — the propose-only card ships on the pack store, not the project hub.** §7 says the
- `§7` `docs/roadmap/plans/AGENT-PACKS.md:363` — **DISCOVERY — confidence had to be given a definition.** §7's example rule carries
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:22` — - **NEW-12** (installable bundles of composed configurations: identity, config subset, skills, workflow templates, triggers, MCP connector declarations, app wiring; dependency-closure `.pclaw` export/import; fresh-id cross-ref rewriting; leaves-first import with rollback; referential-integrity li…
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:38` — - **Approved neighbors this plan must NOT duplicate:** **WORK-R15** (WORK-CONTAINERS §1.7) owns the *project* export/import contract — manifest ZIP, per-entity sha256, path-safety validation (reject `../`, absolute paths, symlinks, null bytes — the `snapshot.py:_data_filter` posture), `imported-N…
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:109` — 1. **Structural layer:** the exporter's store readers are an allowlist of the §1 component stores. `.env`, `.local_secret`, `sel_hmac.key`, `telemetry_salt`, `session_map.json`, `memory*.db`, `knowledge.db`, `sessions/`, `security_events.jsonl` are not merely excluded — they are **never opened** …
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:125` — → quarantine extract (WORK-R15 path-safety) → integrity recompute (§1)
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:231` — - **Snapshot/durability:** `~/.personalclaw/packs/` (installed-pack ledger + locks) and `connector_catalog.json` register in DURABILITY-AND-SYNC's inventory (that plan's §1 owns coverage; this plan just adds its stores to the inventory when both land — no private allowlist).
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:255` — | **LEARNING-FLYWHEEL** (approved) | Its `{source, computedHash}` lock on imported templates is the drift primitive pack updates rely on (§1 pack_owned); its proposal queue is the review surface the prompt-card importer (§4.3) and fingerprint proposals (§7) feed. Packs are "the sharing channel fo…
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:268` — - **Session 1 — format + export core (§1, §2.1-2.2):** `pack.json` schema + integer versioning; dependency-closure walker over the real stores; structural + content redaction layers (shared `packs/deny.py` with portability.py); export preview UI; golden-pack fixture tests.
- `§1` `docs/roadmap/plans/AGENT-PACKS.md:352` — **DISCOVERY — an update needs a lock the ledger never had.** §1 says "the skills-lock
- `Session 6` `docs/roadmap/plans/AGENT-PACKS.md:273` — - **Session 6 — fingerprinting + polish (§7):** fingerprint scanner + pack-side fingerprint declarations + propose-only cards + rejection memory; pack update flow (pack_owned overwrite, drift skip); pack store/detail FE; as-a-user validation sweep (export→wipe→import round-trip on a second `PERSO…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
