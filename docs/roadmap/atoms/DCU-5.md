<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DCU-5` — Approval-ladder integration for desktop drive

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-COMPUTER-USE](../plans/DESKTOP-COMPUTER-USE.md) (`DCU`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DCU.md`](../atomic/DCU.md) |
| **Execution position** | #157 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Wire the approval ladder into policy (§3.4) so an unattended profile may drive the desktop only with a creation-time grant, while interactive sessions receive the approval prompt.

## Done when

An unattended run without the grant refuses and notifies; an interactive run prompts; validated.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DCU-4`](DCU-4.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:approval ladder + unattended-profile grant` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `ef0e0cc8e` 2026-08-27 — feat(desktop): DCU-5 refuse an unattended desktop drive without the operator's standing grant

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:848` — READ, not restructured (DCU-5 is live in that area). The floor outranks a connected connector —
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:508` — it is `DCU-5`'s declared deliverable and `DCU-5` depends on `DCU-4`, so wiring it now would empty that
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:511` — carries from `X-Session-Key`. `DCU-5` should read `guardrails.policy.profile_for_session(...)` and refuse
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:761` — - [2026-08-27][DCU-5] DONE: the approval ladder now gates desktop drive at step **4b** of the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:809` — plus the SEL gate. `DCU-5` then added the approval-ladder refusal and closed a fail-open at the HTTP seam
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:827` — **The consequence matters for anyone reading deps:** `DCU-5`, `DCU-6` and `DCU-7` all list `DCU-4` and therefore

### Its plan's own record, via the scope references (0 line(s))

Its scope cites `§3.4`, but no line of its source plan carries any of them.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
