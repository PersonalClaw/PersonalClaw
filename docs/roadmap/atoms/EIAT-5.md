<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EIAT-5` — Triggers page inbox-event configuration UI (web/, S3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EMAIL-INBOX-AND-TRIGGERS](../plans/EMAIL-INBOX-AND-TRIGGERS.md) (`EIAT`, plan status `done`) |
| **Atom table** | [`../atomic/EIAT.md`](../atomic/EIAT.md) |
| **Execution position** | #239 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 — T3.2. Core web/ frontend only (separate repo/PR seam from EIAT-4's apps-repo work).

## Done when

An inbox trigger (source, address glob, sender glob, subject/body matcher) is creatable from the Triggers page with URL-backed state, and draft-by-default is clearly surfaced for send-capable actions; no primitive-adoption ratchet trips (uses Button / ui/forms); a11y (focus-visible, reduced-motion) passes; cd web && npm run typecheck && npm test && npm run build all green.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EIAT-1`](EIAT-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `43fa10e3e` 2026-08-09 — docs(roadmap): CE-4 Discord channel app done (apps#29) (#949)
- `54c2044e8` 2026-08-09 — feat(triggers): EIAT-5 data-event trigger create UI on the Triggers page (#945)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:268` — - **2026-08-09 — DONE: EIAT-5** (Triggers page inbox-event configuration UI, web/, S3). Core `web/`
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:313` — The user supplies the one `Data event → InboxAddress → invoke-agent ($value)` trigger (EIAT-5 shipped
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:412` — EIAT-5's DEVIATION), so adding it would ship an inert field; an "email-only trigger" needs the

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T3.2`, `Session 3`. Those tokens appear in its source plan at:

- `T3.2` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:196` — | T3.2 | Triggers page: inbox-event configuration (source, address glob, sender glob, subject/body matcher) using shell primitives + tokens; URL-backed state; draft-by-default clearly surfaced for send-capable actions | `web/src/pages/triggers/`, `web/src/lib/api.ts` | an inbox trigger is creatab…
- `T3.2` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:271` — - **T3.2 (create UI):** the Triggers create page (`web/src/pages/triggers/TriggerCreatePage.tsx`)
- `Session 3` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:191` — ### Session 3 — Prompt-bound addresses + the trigger UX

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
