<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-1` — Durable usage ledger module + durability/inventory registration

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #97 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S1; C1 — The ledger (usage_ledger.py, storage per §2.4); Session 1 T1.1

## Done when

TurnUsage rows round-trip through append-only JSONL at config_dir()/usage/turns.jsonl (atomic_write, 2x-cap trim); rollup groups by all five keys (model|source|agent|provider|day) and sets priced=False when any constituent row is unpriced; totals aggregate; record_turn is fail-open (logs at DEBUG, never raises into a turn); path registered in durability/inventory.py as secret=False,derived=True so audit_home() passes with it claimed AND fails without it (both proven); tests/test_usage_ledger.py green

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (4)

[`CATO-2`](CATO-2.md) (`done`), [`CATO-3`](CATO-3.md) (`done`), [`CATO-4`](CATO-4.md) (`done`), [`CATO-5`](CATO-5.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:161` — - **CATO-1 (T1.1) DONE — the ledger store.** Added `src/personalclaw/usage_ledger.py`: the
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:289` — **COST-AND-TOKEN-OBSERVABILITY is now fully shipped:** store (CATO-1) → 4 write-sites (CATO-2/3/4) →
- **source plan** `docs/roadmap/plans/DURABILITY-AND-SYNC.md:983` — personalclaw.manifest_reference` and amended into the DAS-6a commit; cascade-rebased CATO-1 + PCS-2.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.1`, `§2.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:131` — | T1.1 | `usage_ledger.py`: `TurnUsage`, `record_turn` (fail-open, never raises), `rollup`, `totals`; JSONL at `config_dir()/usage/turns.jsonl` with 2×-cap trim; register the path in `durability/inventory.py` (`secret=False`, `derived=True`) | `src/personalclaw/usage_ledger.py`, `src/personalclaw…
- `T1.1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:161` — - **CATO-1 (T1.1) DONE — the ledger store.** Added `src/personalclaw/usage_ledger.py`: the
- `T1.1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:170` — (passes WITH the entry, reports `usage/` unclaimed WITHOUT). Scope: T1.1 ONLY — the four write
- `§2.4` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:45` — ### C1 — The ledger (`usage_ledger.py`, new; storage per §2.4)
- `Session 1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:127` — ### Session 1 — The durable ledger

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
