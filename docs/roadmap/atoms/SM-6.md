<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-6` — Retention surface: editable auto-archive policy with live dry-run preview

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #537 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 (T3.1); Design §S3 (retention policy surface)

## Done when

Retention policy editable in ChatPanel's 'Context & lifecycle' section with a live dry_run preview of exactly what would move (not an estimate); purge deliberately NOT built (single-session DELETE keeps its own confirm; bulk delete stays excluded). Shipped 2026-07-30 — closed the S2 gap where auto_archive_days ran hourly with no UI.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SM-4`](SM-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T3.1`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:94` — | T3.1 | Retention surface: what auto-archives / never / when purgeable (conservative defaults; purge is explicit + confirmed, distinct from history JSONL rotation) | Settings + sidebar | retention policy editable; purge requires confirm + shows what's affected |
- `T3.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:244` — ### 2026-07-30 — Session 3 (T3.1, T3.2, T3.3 + V3): DONE. Plan COMPLETE.
- `T3.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:269` — - **T3.1 — retention surface.** `session.auto_archive_days` shipped in S2 wired through
- `T3.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:276` — - **Purge deliberately NOT built.** §T3.1 mentions a purge surface; S2 deliberately
- `Session 3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:13` — 2026-07-28; Session 2 T2.2/T2.3 (bulk ops + lifecycle + auto-archive) shipped 2026-07-29; Session 3
- `Session 3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:90` — ### Session 3 — Lifecycle + templates + export (Wave 3)
- `Session 3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:244` — ### 2026-07-30 — Session 3 (T3.1, T3.2, T3.3 + V3): DONE. Plan COMPLETE.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
