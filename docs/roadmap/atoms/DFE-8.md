<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DFE-8` — Decks: DeckModel/Slide layout+geometry+bullet levels + pptx parser + slide editor

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-FIDELITY-EDITOR](../plans/DOCUMENT-FIDELITY-EDITOR.md) (`DFE`, plan status `done`) |
| **Atom table** | [`../atomic/DFE.md`](../atomic/DFE.md) |
| **Execution position** | #167 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 4 T4.3–T4.4; DeckModel/Slide layout selection, per-shape geometry, bullet levels; parsers/pptx_parser.py; the deck editor (slide list + per-slide fields)

## Done when

bullet depth round-trips (the writer hardcodes level=0 today); a slide edit survives download/read-back. V4 gate (deck half) holds.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DFE-5`](DFE-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 13 (DFE) — DRIVEN in a real browser

**Code evidence:**

- src/personalclaw/documents/writers/pptx_writer.py:8 'A bullet's depth is written, not assumed. This writer used to pin level = 0'; :87 and :91 set frame.paragraphs[0].level and para.level from the model
- src/personalclaw/documents/pptx_parser.py (272 lines) + pptx_shapes.py invert layout/geometry/bullet levels
- web/src/ui/content/deckModelEdit.ts:14 mirrors MAX_BULLET_LEVEL from the server and its test asserts it 'offers exactly the depths PowerPoint can express, counted the way a person counts' (1-based for the user, 0-based in the file)
- web/src/ui/content/SlideDeck.tsx:20 records the geometry contract: a title dragged somewhere odd in PowerPoint 'has to be recoverable, and the editor says so'
- part of the 71-test sheets/decks run and the 137-test web run

**Driven in the UI:** Not drivable (no pptx artifact can be created — see DFE-1).

**Notes:** The 1-based-for-the-user vs 0-based-in-the-file split is the kind of detail that usually leaks into the UI; here it is a named constant mirrored from the server with a test that counts 'the way a person counts'.

## Recorded history

### Commits (3)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `175e2fc9b` 2026-08-27 — test(documents): bind the deck inline-run test to DFE-8's Bullet shape
- `40eaf5020` 2026-08-27 — feat(files): DFE-8 carry a bullet's depth through the deck round trip

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:819` — Flipping DFE-3 does NOT complete this plan — DFE-4..DFE-8 remain `todo`, so the plan status is unchanged.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1010` — honestly, but the grid / slide editors are `DFE-7`/`DFE-8`; page setup and paragraph layout
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1024` — Consequence: `DFE-6`, `DFE-7` and `DFE-8` all depended only on `DFE-5` and are now startable.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1175` — - **NOT in this atom, by scope:** the grid and slide editors are `DFE-7`/`DFE-8`; `PUT …/model`
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1304` — deck editor is `DFE-8`; the V4 gate's DECK half is therefore still open. Charts, pivot tables,
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1342` — ## Execution log — DFE-8 (decks: DeckModel/Slide layout+geometry+bullet levels + pptx parser + slide editor) — **DONE**
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1344` — - [2026-08-27][S4 · atom `DFE-8`] **DONE.** Both `done_when` clauses hold, each proved by re-parsing
- **source plan** `docs/roadmap/plans/PROACTIVE-ASSISTANT.md:780` — `documentEditorSlot` ×2 — DFE-8's live area — and four `LoadError`/empty-state timeouts), so only

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T4.3`, `T4.4`, `Session 4`. Those tokens appear in its source plan at:

- `T4.3` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:441` — | T4.3 | `DeckModel`/`Slide` gain layout selection, per-shape geometry, and bullet levels; pptx writer + parser | model, `pptx_writer.py`, `parsers/pptx_parser.py`, tests | bullet depth round-trips (the writer hardcodes `level = 0` today) |
- `T4.4` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:442` — | T4.4 | The deck editor (slide list + per-slide fields) | editor, tests | a slide edit survives download/read-back |
- `Session 4` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:435` — ### Session 4 — Sheets and decks (the same pattern, twice)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
