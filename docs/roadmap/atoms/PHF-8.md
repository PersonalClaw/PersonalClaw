<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-8` — Ceiling-intersect-profile guardrail wiring

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #445 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1213 |

## Intent — what this is for

Wire the SafetyProfile family, which shipped with zero non-test callers (a declared decision object nothing consults). Add a Ceiling loaded once at boot from an operator-owned path the agent process cannot weaken; resolve(ceiling, profile) returns the intersection under tightest-wins (the profile may only narrow). Implement four archetypes each with one compose function (ScopedRuleset, OrdinalControl, CapabilityGate, ScopedMap), dispatching on archetype not scope name so adding a scope is data, not engine code; matchers and ordinal scales live in enforcer-owned registries; an unknown matcher aborts governance boot fail-closed. Encode the path-matcher rule as table-driven tests: normalize only the queried item (expand ~ and $VAR, then abspath) and never run the pattern through normpath (normpath treats */** as ordinary segments and collapses an adjacent .. against them, e.g. /a/**/../b -> /a/b, silently dropping the ** and widening an allow or shrinking a deny); cover /a/**/../b, a .. traversal against an allow-prefix, and a relative item against an absolute deny. Wire profile_for_session into the three dispatch seams plus spawn so the guardrail success criterion holds in code, proven by driving a real unattended trigger rather than a constructed object; composes with PHF-1 (profile picks tools/egress, the sandbox ceilings pick blast radius). Owner decision required on the ceiling's trust-root path and how it is protected from the agent on a single-user machine; document what the layer does and does not buy.

## Done when

A profile cannot widen the ceiling (test per archetype); an unknown matcher aborts boot with a WHAT/WHY/FIX error; a normpath-on-pattern implementation reds the matcher tests; a real unattended trigger resolves through the headless profile with a live reader; a narrower profile bites and a widening attempt is refused, confirmed from logs/SEL.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PHF-1`](PHF-1.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:SafetyProfile-S5.2-wiring` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `acd3bbf81` 2026-08-15 — feat(guardrails): AG-11 default-deny research spawns, cron approval via profile_for_session, project trust gate
- `5b46fdcd4` 2026-08-13 — feat(guardrails): AG-6 earn autonomy per action type, lose it on one rejection (#1197)

### Named by atom id in the roadmap prose (14)

- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:425` — `session_key=""` classifies as ATTENDED and silently skips the SafetyProfile layer (the PHF-8
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1082` — ceiling-bounded via `ceiling_permits_approval` (PHF-8), NOT profile-routed (routing it through
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1083` — `profile_for_session` would let HEADLESS veto the trust toggle — the exact thing PHF-8's docstring
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:787` — through the same `unattended_dispatch_key` helper the trigger/hook seams use (PHF-8), at the one
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:649` — - [2026-08-12][PHF-8] DONE: §5's ceiling ∩ profile layer is built and WIRED. New
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:663` — - [2026-08-12][PHF-8] MEASURED, before writing a line: all three dispatch seams passed
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:671` — - [2026-08-12][PHF-8] V5 HOLDS — driven against a REAL gateway (`PERSONALCLAW_HOME=/tmp/phf8-home`,
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:685` — - [2026-08-12][PHF-8] DEVIATION: `HEADLESS.egress_tier` changed `"registry"` → `"all"` at the
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:697` — - [2026-08-12][PHF-8] DISCOVERY, two wired-but-wrong egress controls found while giving
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:708` — - [2026-08-12][PHF-8] DISCOVERY: `denylist._glob_match` was the wired-but-wrong path matcher the
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:714` — - [2026-08-12][PHF-8] DISCOVERY, left for its owning plan: the third dispatch seam
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:722` — - [2026-08-12][PHF-8] DEVIATION: `SafetyProfile` gained one field, `path_allowlist`, because the
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:729` — - [2026-08-12][PHF-8] OWNER RULING on the trust root the atom asked for: default
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1804` — `PHF-8` built the size rails and their docstrings predicted this exact arrival by name; it has now

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
