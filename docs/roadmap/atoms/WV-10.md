<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-10` — WF2-A3 — FE inspector drawer (run detail + widget node rows) + cached-badge rendering

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #679 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #898 |

## Intent — what this is for

Amendment (2026-07-26) WF2-A3

## Done when

a user can open any node in WorkflowRunDetail + WorkflowProgressCard and read its exact resolved prompt/inputs/output; cached nodes render a visually distinct badge (workflowFold.ts cached? finally read)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-9`](WV-9.md) | intra-repo | `done` |
| [`WV-7`](WV-7.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 27 (WV) — DRIVEN in a real browser

**Code evidence:**

- NodeInspectorDrawer.tsx exists with a test file, and :148-160 renders the badge in BOTH states with a title explaining each — 'cached' / 'Output served from the resume cache' vs 'fresh' / 'Freshly produced this run'. Rendering the negative case is the better choice: a badge only on cached nodes is ambiguous with a badge that failed to render
- the drawer reads `data.cached` from WV-9's inspect payload
- 5238 tests pass across 107 workflow suites (2 skips, both a template legitimately having no work loop)

**Driven in the UI:** Not driven: opening a node needs a run with a terminal node, and no run can start in this home (no model bound).

**Notes:** 🔴 THE CLAUSE'S PARENTHETICAL IS FALSE AND THAT IS THE FINDING. It claims 'workflowFold.ts cached? finally read'. It is not read: workflowFold.ts:43 declares cached? on the event envelope, nothing carries it into a node, WorkflowNodeState has no cached field, and the badge that does exist reads a DIFFERENT source (the inspect endpoint). So the drawer's badge is real and the fold's flag is inert — one claim of two. Recorded as partial rather than contradicted because the atom's main clause (a badge exists, visually distinct, on the inspect payload) holds; the sub-claim about the fold does not. Filed as an issue: at-a-glance cached-ness in the run detail node list and the chat progress card is the thing WF2-A1 was emitted for.

## Recorded history

### Commits (2)

- `b7d20511b` 2026-08-07 — feat(workflows): WV-10 node-inspector drawer — FE consumer of the /inspect endpoint (#898)
- `07ab10270` 2026-08-07 — feat(workflows): WV-9 node-inspect endpoint — reconstructability set, secrets redacted (#893)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
