<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `LMMV-4` — Session 3 — HF token cascade (3-source, whoami-validated) + per-provider real-inference selftest & health endpoints

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [LOCAL-MODEL-MANAGER-V2](../plans/LOCAL-MODEL-MANAGER-V2.md) (`LMMV`, plan status `in_progress`) |
| **Atom table** | [`../atomic/LMMV.md`](../atomic/LMMV.md) |
| **Execution position** | #323 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #920 |

## Intent — what this is for

§5 HF Token Cascade; §6 Per-Provider Real-Inference Selftest + Health; §4.3 gated pre-warn (token-status-aware); §9 whoami_ttl_s / selftest_timeout_s config fields

## Done when

local_models/hf_token.py (re-exported via sdk.credentials) resolves credential-store(.env) → HF_TOKEN/legacy env → ~/.cache/huggingface/token, first whoami-valid source wins via the net.fetch CONNECTOR egress chokepoint (cached ~whoami_ttl_s); GET /api/models/hf-token/status returns per-source {present,valid,username?,masked} with values never leaving the server unmasked (Success Criterion 4); a set/clear field writes source 1 and diarization-pyannote._hf_token() delegates to the cascade (its app-config field honored one release then migrated); GET /api/models/local/{provider}/health never 500s (uses ABC availability_detail()); POST .../selftest runs a real per-capability inference (bundled fixtures, single_flight-serialized, bounded timeout, user-click only) returning typed reasons so a pyannote-4-style contract break fails on API not file presence (Success Criterion 5); Test buttons render inline in ModelsPanel; SEL logs token set/clear; the gated pre-warn consumes cascade status server-side.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`LMMV-7`](LMMV-7.md) (`blocked`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `3264effd1` 2026-08-08 — chore(roadmap): reconcile dag.json with merged stack (AS-4/APE-8/LMMV-4/SV-5 → done) (#926)

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:1025` — providers and for nonexistent names — owned by LMMV-4, not this atom. `G175` one guarded-entry-point

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `§5`, `§6`, `§4.3`, `§9`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:32` — - **NEW-8** (core) — sidecar isolation → §3; resumable install jobs + child-reported memory → §3.2, §7; canonical server-side download progress with poll/cancel/.part atomic writes → §4; gated-repo error translation → §4.3; three-source HF token cascade with whoami validation → §5; per-provider r…
- `§5` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:46` — - **Token class** (pyannote's HF-token friction was the last cell of the matrix to close) → a **three-source token cascade with live `whoami` validation** (§5), and **gated-repo errors translated to the exact user action** (§4.3).
- `§5` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:186` — - The FE translation table turns each into ONE concrete action: gated → "This model requires a HuggingFace token and license acceptance. [Open model page] [Add token in Settings]" — deep-linking to the §5 token settings and the HF repo page. `LocalModel.gated` (field exists today) pre-warns *befo…
- `§6` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:32` — - **NEW-8** (core) — sidecar isolation → §3; resumable install jobs + child-reported memory → §3.2, §7; canonical server-side download progress with poll/cancel/.part atomic writes → §4; gated-repo error translation → §4.3; three-source HF token cascade with whoami validation → §5; per-provider r…
- `§6` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:47` — - **"Is it actually working?" class** (the matrix RUN column was proven manually) → **per-provider real-inference selftest endpoints** that automate it (§6).
- `§4.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:32` — - **NEW-8** (core) — sidecar isolation → §3; resumable install jobs + child-reported memory → §3.2, §7; canonical server-side download progress with poll/cancel/.part atomic writes → §4; gated-repo error translation → §4.3; three-source HF token cascade with whoami validation → §5; per-provider r…
- `§4.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:46` — - **Token class** (pyannote's HF-token friction was the last cell of the matrix to close) → a **three-source token cascade with live `whoami` validation** (§5), and **gated-repo errors translated to the exact user action** (§4.3).
- `§4.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:247` — - **Egress:** `whoami`, direct-URL weight fetches the runner drives, and any endpoint probing go through `net.fetch` with `egress_policy_for(CONNECTOR)`; HF-hub library fetches are provider-internal (unchanged), but their *failures* are classified at the runner boundary (§4.3).
- `§9` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:223` — - FE: a compact loaded-models section in Settings → Models (rows + Unload buttons + pressure bar), and — reusing the existing Dashboard bento surface — an optional "On this machine" tile answering "what is occupying my RAM right now". Pressure warning threshold configurable (§9 config).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
