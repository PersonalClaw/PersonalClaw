<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-1` — session_search.py: FTS5 cross-session index + incremental heartbeat reindex

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #532 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 (T1.1); Contracts §C1 (session_search.py)

## Done when

Indexing a chat makes it findable via search_sessions; temporary/incognito sessions are never indexed and restricted-session exclusion holds at index/reindex/read time (test); reindex_all runs on the heartbeat (tick 1 + tick 5). Shipped 2026-07-28, tests/test_session_search.py (56 cases). DEVIATION: sqlite_compat (plan 39) does not exist — used except sqlite3.OperationalError fallback; session-grained (not turn-grained) index because history rewrites the full JSONL per turn.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`SM-2`](SM-2.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.1`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:76` — | T1.1 | `session_search.py`: FTS5 index (via `sqlite_compat`), `index_turn` on turn-write (skip temporary/incognito), `search_sessions`, `reindex_all` on the heartbeat (mirror the existing FTS reindex) | `src/personalclaw/session_search.py`, turn-write hook, heartbeat | indexing a chat makes it …
- `T1.1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:119` — ### 2026-07-28 — Session 1 (T1.1, T1.2 + V1): DONE. T1.3 re-scoped — see below.
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:12` — **Status:** DONE — Session 1 (FTS5 cross-session search + match snippets) shipped
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:72` — ### Session 1 — Cross-session search + scalable sidebar
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:119` — ### 2026-07-28 — Session 1 (T1.1, T1.2 + V1): DONE. T1.3 re-scoped — see below.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
