<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `RUA-4` — Public-exposure hardening (Secure cookie / wss CSP / trusted-proxy headers) + TOTP at login + single-use remote enrollment codes + remote-access guide

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [REMOTE-USER-AUTH](../plans/REMOTE-USER-AUTH.md) (`RUA`, plan status `done`) |
| **Atom table** | [`../atomic/RUA.md`](../atomic/RUA.md) |
| **Execution position** | #510 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S4; Contracts C3 (enroll routes) + integration points (exposure boundary); Session 4 tasks T4.1-T4.4 + V4

## Done when

With dashboard.public_url set: session cookie carries Secure; HttpOnly; SameSite=Lax; wss://<host> allowed in WS CSP; X-Forwarded-Proto/-For trusted only from configured trusted_proxies (spoofed header from untrusted peer ignored, mutation-verified). require_totp refuses password-only login with auth_totp_required and accepts with a live code. auth enroll issues an 8-char single-use code (300s TTL, max 5 outstanding, SHA-256 at rest 0600, constant-time) redeemable once from a second device yielding a persistent issuer:enroll session; reuse rejected. docs/guides/remote-access.md walks tunnel->password->public_url->2FA->phone pairing with a 'what this does not protect you from' section. Storage decision recorded (credentials.json in snapshot set; signing_key/sessions.json/enroll_codes.json excluded). Validated across 4 boots; test_auth_exposure.py 51 cases; make test 9752.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`RUA-3`](RUA-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T4.1`, `T4.4`, `Session 4`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/REMOTE-USER-AUTH.md:231` — | T4.1 | When `dashboard.public_url` set: `Secure` cookie, `wss://<public-host>` in the WS CSP, `X-Forwarded-Proto`/`-For` trusted **only** from a configured trusted proxy | `token_auth.py`, `server.py` CSP, `origin.py` | over TLS the cookie carries `Secure`; WS connects via `wss://`; forwarded h…
- `T4.1` `docs/roadmap/plans/REMOTE-USER-AUTH.md:430` — an arbitrary victim. S4 (T4.1) introduces trusted-proxy handling.
- `T4.4` `docs/roadmap/plans/REMOTE-USER-AUTH.md:234` — | T4.4 | Docs: `docs/guides/remote-access.md` login section (tunnel + TLS termination + login), threat notes; coordinate with MOBILE-COMPANION's Tailscale walkthrough | guide | a reader exposes the dashboard via their tunnel and logs in from cell data following it verbatim (owner V4) |
- `T4.4` `docs/roadmap/plans/REMOTE-USER-AUTH.md:539` — **T4.4 guide** (`docs/guides/remote-access.md`, linked from the README) walks a reader from
- `Session 4` `docs/roadmap/plans/REMOTE-USER-AUTH.md:227` — ### Session 4 — Public-exposure hardening + 2FA + remote enrollment

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
