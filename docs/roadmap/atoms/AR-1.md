<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AR-1` — Un-defer the plan: ship the council precursor, re-confirm demand, and resolve the 6 design questions into contracts

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [AGENT-ROOMS](../plans/AGENT-ROOMS.md) (`AR`, plan status `todo`) |
| **Atom table** | [`../atomic/AR.md`](../atomic/AR.md) |
| **Execution position** | #54 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`ext`, `owner`) |
| **Blocked reason** | OWNER-GATED — and now on exactly one question, nothing else. The precursor SHIPPED: AR-9's council template merged 2026-09-06 (PR #2507), closing the deliverable WORKFLOWS-V2-LOOPS-EVOLUTION had promised and never shipped, which is what made this atom's original ext gate void in fact (audit 2026-09-05). Every other deferral condition in the plan header is satisfied: WORKFLOWS-V2 core slices own pipeline-shaped orchestration, and ACP-AGENT-PARITY, AUTONOMY-GUARDRAILS and INBOX-NOTIFICATIONS-UNIFICATION have all landed. What is left is owner task 1 verbatim: re-confirm that DELIBERATION — open-ended, human-refereed, non-terminating conversation — rather than fan-out is the capability still missing, now that fan-out is actually available to try. The plan pre-authorises the cheap answer: 'if the council template satisfies demand, rooms may stay deferred indefinitely — that outcome is acceptable and cheap to discover.' So a NO closes AR-1..AR-8 as satisfied-by-precursor (8 atoms, ~17% of what remains open), and a YES additionally needs the 6 design questions plus owner tasks 2-3 resolved into a Contracts section before AR-2 can start. |

## Intent — what this is for

The cheap precursor (build this first, on the engine); Design questions to resolve at un-deferral; Owner tasks (at un-deferral, not before)

## Done when

The LOOPS-EVOLUTION council template has shipped, the owner re-confirms deliberation (not fan-out) is the missing capability, and all 6 design questions (transcript persistence format, member context budget policy, rooms x memory writes, ACP capability gates, room-level SEL, UI surface) plus the 3 owner tasks (default round budget + pause UX copy, ACP membership eligibility) are resolved into a Contracts section and task table added to this plan.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AR-9`](AR-9.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:core slices own pipeline-shaped multi-agent orchestration; rooms are only the deliberative remainder` | cross-plan (EXT) | **unresolved** |

### Unblocks (2)

[`AR-2`](AR-2.md) (`todo`), [`AR-3`](AR-3.md) (`todo`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 60 (AR) — the council template falsified; the eight unbuilt atoms swept to confirm their todo status is accurate

**Code evidence:**

- 🔑 THIS IS THE ONE ATOM WITH A CLAUSE GENUINELY MET, and it is the clause the whole plan was waiting on: the precursor SHIPPED. `AR-9`'s council template is in the tree and its suite passes 16/16, which is what makes the un-deferral question answerable rather than speculative
- STATUS `todo` IS CORRECT and the block has narrowed to exactly one thing — an owner judgement about whether deliberation rather than fan-out is the missing capability. That is a product decision, not a missing seam
- the six design questions the atom lists (transcript format, member context budget, rooms-versus-memory, and the rest) are unresolved, which is consistent with the seven downstream atoms being unbuilt
- council template 16/16 (falsified: contaminating one member's prompt with another's output reds the independence rail and the ordering rail); negative sweep across core + web finds no room store, member model, speaker queue or room event family

**Driven in the UI:** Not applicable: the deliverable is a decision plus resolved contracts.

**Notes:** 🔑 THE PLAN'S SHAPE IS THE THING WORTH CREDITING. It spent one cheap atom building the precursor that makes the expensive decision answerable, then stopped and asked. That is the opposite of the usual failure, where a large plan is half-built before anyone checks whether the capability is wanted — and it is why the other eight atoms being unbuilt is a sign of discipline rather than of neglect.

## Recorded history

### Commits (6)

- `2f0dffd37` 2026-09-07 — feat(roadmap): rank the owner's queue by what each atom actually unblocks
- `e2744e16d` 2026-09-06 — docs(roadmap): flip AR-9 done — the council precursor shipped; AR-1 is now one owner question
- `cf45e2596` 2026-09-06 — docs(roadmap): flip WF2AUT-11 done — AUTOMATION-SUBSTRATE complete at 14/14
- `fc02ccac6` 2026-09-06 — docs(roadmap): mint AR-9 council precursor; AR-1's dead ext gate becomes an owner call
- `61fb873c6` 2026-09-06 — docs(roadmap): record the council template landing in the LOOPS-EVOLUTION exec log
- `dd876068e` 2026-09-05 — docs(roadmap): inventory-audit corrections — PEP-11 decomposed to PEP-12..20, ET-10 superseded, four stale annotations refreshed

### Named by atom id in the roadmap prose (12)

- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:56` — An audit of every non-done atom found this plan walled off by a gate that could not clear. `AR-1`'s
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:59` — templates, none of them council). Because the edge resolved to a *done* atom, the graph read AR-1 as
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:63` — The correction takes the first of the two options AR-1's own blocked_reason offered — mint the precursor
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:66` — homed here rather than by reopening a closed plan, and AR-1 depends on it.
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:68` — What is left on AR-1 is genuinely the owner's and nothing else: after AR-9 ships, re-confirm that
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:71` — demand, rooms stay deferred, and AR-1..AR-9 close as satisfied-by-precursor instead of leaving eight
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:88` — This closes the precursor half of `AR-1` and leaves owner task 1 standing alone. The question is now
- **source plan** `docs/roadmap/plans/AGENT-ROOMS.md:92` — `AR-1`..`AR-8` as satisfied-by-precursor rather than a failure to explain away.
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:812` — **2026-09-06 — SH-4's registry gate was its own deliverable; the credential-free half split out as SH-12.** `SH-4` carried an ext dep reading `EXT:ECOSYSTEM-TOOLING:registry.json listings record signer identity` — word for word its own second done-when clause, so the atom depended on a thing it w…
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:6130` — paper and void in fact, the same defect class the 2026-09-05 audit found on `AR-1` and `SH-4`. The
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:1916` — template-library candidate — and `AR-1`'s ext gate ("council fan-out/fan-in workflow template ships
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:1968` — untouched: the deliverable now exists on merit, and `AR-1` also turns on an owner demand

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
