<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SH-2` — credential_keychain gate (class B) + m_*_credentials_to_keychain migration (snapshot-backed, rollback restores .env) + Settings 'move to keychain' action

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SECURITY-HARDENING](../plans/SECURITY-HARDENING.md) (`SH`, plan status `in_progress`) |
| **Atom table** | [`../atomic/SH.md`](../atomic/SH.md) |
| **Execution position** | #512 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 T1.2/T1.3/V1; Contracts C1 migration m_*_credentials_to_keychain; owner task 1 (macOS Keychain validation)

## Done when

migration fixture (fake keyring) moves .env secrets to keychain and removes the keys, idempotent + verify passes; rollback restores .env; portability export still excludes secrets; Settings action runs the migration with a visible snapshot-confirm step; macOS migrate/rollback + headless .env-fallback validation both recorded

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SH-1`](SH-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 64 (SH) — the deny-before-approval ordering falsified (both rails red); the registry validator and the audit chain verify both run live

**Code evidence:**

- `config/credential_migration.py` ships with `tests/test_credential_migration.py`, inside the 511 green
- 🔑 THE SETTINGS SURFACE IS HONEST ABOUT WHAT THE TOGGLE DOES NOT DO, which is the half most such switches get wrong: 'Changes where NEW credentials are written. Secrets already in .env stay readable and stay put until you move them below'
- 🔴 AND THE MIGRATION BUTTON IS CORRECTLY DISABLED HERE — nothing to move and no usable keyring. An enabled 'Move to keychain' with zero movable secrets would be the inert-control class this campaign keeps finding
- the gate is a persisted config field rather than env-only, 'without it the env var would have to be re-exported for every process, and the persisted request the migration acts on would have nowhere to live'
- 511 passed / 2 xfailed across tests/security + the credential and denylist suites; 80/80 on the audit API and SEL; the registry validator run live; the security and audit Settings pages driven on :10011

**Driven in the UI:** Driven: the switch, its help text and the disabled 'Move to keychain' button were read off the live page.

**Notes:** Confirmed on the mechanism and the surface. The macOS migrate/rollback half of the clause was NOT driven — this host has no usable keyring, so the real keychain round-trip stays a fixture here. That is the one clause a future change could break invisibly.

## Recorded history

### Commits (5)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `b0af2f1d7` 2026-08-26 — feat(security): SH-2 consented snapshot-backed credential move to the OS keychain
- `df8cdb5e7` 2026-08-20 — docs(roadmap): SH-2 is unauthorized, not blocked — and a late amendment lands nowhere
- `e767f4bc2` 2026-08-15 — feat(security): SH-1 OS keychain credential backend with fail-closed .env fallback
- `e2486677f` 2026-08-07 — docs(roadmap): deliverables-only DAG — delete 4 non-deliverable docs, fold guidance into CONTRIBUTING/AGENTS (#897)

### Named by atom id in the roadmap prose (24)

- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:1029` — **`loader.py` is 5581 lines — 419 of headroom.** `PHF-15` and `SH-2` extracted code out of it in
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:888` — `>= 100`, and the two `mobile.*` fields cost +65, leaving 35. `SH-2` then extracted the credential
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:918` — `MC-8`, `SH-2` and `LV-4`. Landing it took seven conflicts, all additive except two that had to take
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:2007` — **The split, by domain and not by line count** (`credentials.py`/`SH-2` as the precedent — clean
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:139` — ### 2026-08-26 — SH-2 (T1.2 / T1.3 / V1) consented credential move to the keychain — DONE
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:141` — - **[SH-2] DONE:** `config/credential_migration.py` owns `credentials_to_keychain` —
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:149` — - **[SH-2] The invariant the plan's T1.2 note demands ("silently losing a user's stored
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:158` — - **[SH-2] FINDING (fixed here, found by the lying-backend fixture) — leaving the key in `.env`
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:166` — - **[SH-2] DEVIATION — re-scoped from "class-B gate + `m_*_credentials_to_keychain` migration" to
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:177` — - **[SH-2] DEVIATION — `config/loader.py` was split.** Measured on `origin/main`: 5900 lines
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:182` — (selector, keychain helpers, dotenv helpers, `save_credential`/`get_credential`, plus SH-2's new
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:187` — - **[SH-2] PREMISE CORRECTION — "snapshot-backed" cannot mean `personalclaw snapshot`.**
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:198` — - **[SH-2] FINDING (P3, no fix needed — verified, not assumed) — `cli.py::main()` calls
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:207` — - **[SH-2] V1 VALIDATION — macOS migrate/rollback, recorded.** Darwin 25.6.0, real `keyring`
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:226` — - **[SH-2] V1 VALIDATION — headless `.env` fallback, recorded.** Same driver with `keyring`
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:233` — - **[SH-2] Falsifications, each mutation grep-confirmed on the live line and restored from a file
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:258` — - **[SH-2] PRE-EXISTING RED, not mine:**
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:430` — keychain, and every credential still sitting in `.env` (SH-2's migration has not run yet) reads
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:450` — (SH-2) own the class-B `credential_keychain` gate, the consent migration and the Settings
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:451` — action. A config gate here would build that seam twice and force SH-2 to redefine it. So no
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:497` — diff is SH-1 `todo`→`done`, the SH ready-frontier moving to SH-2, and SH `done` 3→4 / `todo` 6→5.
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1058` — - [2026-08-20][frontier] **CORRECTION to the census above — `SH-2` is not blocked, it is unauthorized.**
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1060` — difference and hides an option. Verified: `SH-1` is `done`, `SH-2` has no unmet non-EXT dep, and
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1076` — Recorded so the option is visible: authorizing `SH-2` out of order is a **sixth** way to end the

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `T1.2`, `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/SECURITY-HARDENING.md:15` — **Depends on:** SECURITY-LEGIBILITY (the threat model that prioritizes this — its DISCOVERY entries seed candidate scope), CI-RELEASE-ENGINEERING (fuzz jobs, signing infra), ECOSYSTEM-TOOLING S2 (the registry signed manifests protect). The keychain slice (S1) is class B and reversible-migration-b…
- `T1.2` `docs/roadmap/plans/SECURITY-HARDENING.md:66` — | T1.2 | Credential migration `credentials_to_keychain` (moves `.env` secrets → keychain, removes the keys from `.env`; idempotent; snapshot-backed; rollback restores `.env`), triggered on explicit user consent | `src/personalclaw/config/loader.py` (+ a small migration helper) | migration fixture…
- `T1.2` `docs/roadmap/plans/SECURITY-HARDENING.md:139` — ### 2026-08-26 — SH-2 (T1.2 / T1.3 / V1) consented credential move to the keychain — DONE
- `T1.2` `docs/roadmap/plans/SECURITY-HARDENING.md:149` — - **[SH-2] The invariant the plan's T1.2 note demands ("silently losing a user's stored
- `T1.2` `docs/roadmap/plans/SECURITY-HARDENING.md:449` — 1. **No config field; the opt-in is `PERSONALCLAW_CREDENTIAL_BACKEND`.** T1.1's sibling T1.2/T1.3
- `T1.3` `docs/roadmap/plans/SECURITY-HARDENING.md:77` — | T1.3 | Settings → Security note: which backend is active + a "move to keychain" action (triggers the migration with the snapshot confirm) | security settings component | action runs the migration with a visible snapshot step; state reflects post-migration |
- `T1.3` `docs/roadmap/plans/SECURITY-HARDENING.md:139` — ### 2026-08-26 — SH-2 (T1.2 / T1.3 / V1) consented credential move to the keychain — DONE
- `T1.3` `docs/roadmap/plans/SECURITY-HARDENING.md:174` — real and is the persisted user opt-in — the plan asked for that in T1.3 and SH-1 explicitly
- `T1.3` `docs/roadmap/plans/SECURITY-HARDENING.md:449` — 1. **No config field; the opt-in is `PERSONALCLAW_CREDENTIAL_BACKEND`.** T1.1's sibling T1.2/T1.3
- `Session 1` `docs/roadmap/plans/SECURITY-HARDENING.md:61` — ### Session 1 — OS keychain credential storage

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
