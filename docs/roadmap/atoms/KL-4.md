<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-4` — S2 bulk operations: bulk_apply + /api/knowledge/bulk + multi-select bar

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #305 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 — T2.3 (bulk ops); Contracts C2 bulk_apply, C3 POST /api/knowledge/bulk

## Done when

bulk_apply + POST /api/knowledge/bulk with 7 reversible ops (collect/uncollect/read_state/favorite/archive/restore/pin), per-item best-effort reporting changed/unchanged/missing; 500-item cap; delete deliberately excluded; typed 400s for missing args and smart_collection_immutable; read_state/favorite stay non-touching through the bulk path; multi-select bar wired with a shared Checkbox primitive; HTTP-level test suite added

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-1`](KL-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:13` — COMPLETE** — T2.1 curation display/filters, T2.2 tags taxonomy, T2.3 bulk ops, all 2026-07-29.
- `T2.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:96` — | T2.3 | Bulk ops: `bulk_apply` + `POST /api/knowledge/bulk`; multi-select bar on the list (add-to-collection/tag/archive/read/delete) | store, handler, list page | select 10 → add to a collection in one action; SEL/audit sane |
- `T2.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:243` — - 2026-07-29 — **DONE (S2: T2.3 bulk ops).** `bulk_apply` + `POST /api/knowledge/bulk`
- `T2.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:366` — the owner re-scope ruling recorded in the T2.3 log above.
- `T2.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:468` — **S2 is now COMPLETE** (T2.1 + T2.2 + T2.3). Remaining in the plan: Session 3
- `Session 2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:12` — **Status:** IN PROGRESS — Session 1 (collections + item curation) shipped 2026-07-29; **Session 2
- `Session 2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:90` — ### Session 2 — Curation + taxonomy + bulk

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
