<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-5` — WF2 replay scenarios (workflow-journal-projection + rewind-during-stream) + baselines, gating the journal format

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #548 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #921 |

## Intent — what this is for

§2.3 (required scenarios 4-5: workflow-journal-projection, rewind-during-stream), §2.1 (workflow journal→SSE projection tap)

## Done when

workflow-journal-projection recorded and green with its checked-in baseline before any WF2 Slice 3+ consumer reads the journal; a format change breaking the event-fold law fails compare; a missing required scenario fails the run (Success Criterion #4)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SV-3`](SV-3.md) | intra-repo | `done` |
| [`SV-4`](SV-4.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:journal event format + engine/journal.py (Slices 1-2) to record against` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `3264effd1` 2026-08-08 — chore(roadmap): reconcile dag.json with merged stack (AS-4/APE-8/LMMV-4/SV-5 → done) (#926)
- `2727ac6c6` 2026-08-08 — test(harness): SV-6 workflow-run resume-audit — byte-equal frontier reconstruction (#925)
- `d2564203b` 2026-08-08 — test(harness): SV-5 WF2 replay scenarios (workflow-journal-projection + rewind-during-stream) + baselines (#921)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:17` — blocked** — the two replay scenarios (`workflow-journal-projection`, `rewind-during-stream`, SV-5,
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:283` — - [2026-08-08][SV-6] DONE: workflow-run half of resume-audit — byte-equal frontier reconstruction from the event-fold (§2.4, **Success Criterion #5 workflow half met**). `harness/resume_audit.py::audit_workflow_run(run_id)` (beside `audit_loop`) reconstructs a persisted run from DISK ALONE — a fr…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:285` — - [2026-08-08][SV-5] DONE: WF2 replay scenarios + baselines gating the journal format (§2.3 required scenarios 4-5, §2.1 workflow projection tap). The S4 BLOCKED gate had cleared (WF2 engine + `workflows/journal.py` exist). **No engine change was needed** — the §2.1 "no tap needed" claim held end…

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `§2.3`, `§2.1`. Those tokens appear in its source plan at:

- `§2.3` `docs/roadmap/plans/SELF-VERIFICATION.md:84` — - `run <task|--diff>` — executes the union: mapped profile commands (`fast` → targeted pytest subset; `web` → vitest; `replay` → §2.3 compare; `full` → the sharded suite, per the full-suite-native-segfault note) + the scanner over changed files.
- `§2.3` `docs/roadmap/plans/SELF-VERIFICATION.md:182` — Each WORKFLOWS-V2 wave/slice landing adds one entry under `harness/exemplars/<slice>/`: a standalone runnable spec exercising that slice's mechanism (e.g., Slice 2: a 3-node run with a failing `required_artifacts` gate), a smoke script (run + assert, ≤30s), and a rationale note (what the slice ad…
- `§2.3` `docs/roadmap/plans/SELF-VERIFICATION.md:228` — - **Session 4 — baselines + journal gate + riders (§2.3, §2.4, §4.1)** *(Wave 1, co-scheduled with WF2 Slice 2)*: baselines.json + hard/drift compare + missing-scenario-fails; record `workflow-journal-projection` + `rewind-during-stream` against the young engine and **gate the journal format on t…
- `§2.3` `docs/roadmap/plans/SELF-VERIFICATION.md:277` — - [2026-07-26][S3] DONE: Event-trace replay regression substrate (§2.1, §2.2, + §2.3 baselines). Recorder in CORE (`src/personalclaw/trace_recorder.py`) — env-gated by `PERSONALCLAW_TRACE_DIR`, zero overhead + no file I/O when off, redacts payloads at write via `security.redact`, one NDJSON line …
- `§2.3` `docs/roadmap/plans/SELF-VERIFICATION.md:282` — - [2026-07-26][S4] BLOCKED (E-none — expected dependency, not a defect): the Workflows-v2-gated deliverables are correctly NOT built this session because the engine + journal do not exist yet: (1) replay scenarios `workflow-journal-projection` + `rewind-during-stream` (§2.3 required set) — need W…
- `§2.3` `docs/roadmap/plans/SELF-VERIFICATION.md:285` — - [2026-08-08][SV-5] DONE: WF2 replay scenarios + baselines gating the journal format (§2.3 required scenarios 4-5, §2.1 workflow projection tap). The S4 BLOCKED gate had cleared (WF2 engine + `workflows/journal.py` exist). **No engine change was needed** — the §2.1 "no tap needed" claim held end…
- `§2.1` `docs/roadmap/plans/SELF-VERIFICATION.md:194` — - **MCP**: the Chrome DevTools MCP server binds through the existing MCP connector configuration (`mcp_client.py` / `mcp_discovery.py`); the record/replay wrapper (§2.1) wraps that client, changing no contract.
- `§2.1` `docs/roadmap/plans/SELF-VERIFICATION.md:227` — - **Session 3 — replay substrate (§2.1, §2.2)** *(Wave 1, co-scheduled with WF2 Slice 0-1)*: trace format + redaction; WS/SSE/inbox recorder taps behind `PERSONALCLAW_TRACE_DIR`; Python metrics fold; vitest replay driver through `coalesceReducers.ts`/`runFold.ts`; record + commit the first three …
- `§2.1` `docs/roadmap/plans/SELF-VERIFICATION.md:277` — - [2026-07-26][S3] DONE: Event-trace replay regression substrate (§2.1, §2.2, + §2.3 baselines). Recorder in CORE (`src/personalclaw/trace_recorder.py`) — env-gated by `PERSONALCLAW_TRACE_DIR`, zero overhead + no file I/O when off, redacts payloads at write via `security.redact`, one NDJSON line …
- `§2.1` `docs/roadmap/plans/SELF-VERIFICATION.md:285` — - [2026-08-08][SV-5] DONE: WF2 replay scenarios + baselines gating the journal format (§2.3 required scenarios 4-5, §2.1 workflow projection tap). The S4 BLOCKED gate had cleared (WF2 engine + `workflows/journal.py` exist). **No engine change was needed** — the §2.1 "no tap needed" claim held end…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
