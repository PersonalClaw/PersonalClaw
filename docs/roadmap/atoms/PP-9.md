<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-9` — Generalize the outcome record beyond decisions (`pending_outcome`/`outcome_resolved`)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #467 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1303 |

## Intent — what this is for

The platform records what it DID and not what LANDED. That single shape produces loop output nobody reads, unmeasured routing edges, and the whole declared-but-inert class this repo keeps rediscovering by AST audit rather than at runtime. The right mechanism already exists and is scoped to one feature: LEARN-R18's `pending_outcome` journals a bet at decision time with `{subject, metric, horizon_secs, baseline}` and `outcome_resolved` closes it, keeping `resolution: measured | inconclusive` distinct so an unmeasurable outcome decays faster than a measured one, linked by `pending_event_id` so a second curator tick is idempotent.

## Done when

The outcome pair becomes a general ledger facility any producer may open: a published artifact, an escalation, a proposal, a declared control. The horizon resolver stays idempotent via `pending_event_id` and still distinguishes measured from inconclusive. At least two non-decision producers are wired in the same change so the generalization is exercised rather than merely available (a `publish:` artifact and a gate escalation). Coordinated with PROACTIVE-ASSISTANT's `PA-4` decision journal so there is ONE outcome facility, not a decision-shaped one plus a general one.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-4`](PP-4.md) | intra-repo | `done` |

### Unblocks (1)

[`PP-10`](PP-10.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `2a720fc52` 2026-08-14 — feat(ledger): PP-10 surface a work unit whose output nobody reads
- `e0dd8701b` 2026-08-14 — feat(ledger): PP-9 open the outcome record to every producer
- `6d2a7f0bc` 2026-08-14 — docs(roadmap): add Platform-Primitives plan + 20 atoms (rev 17)

### Named by atom id in the roadmap prose (11)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:71` — | Outcome/consumption records | PROACTIVE-ASSISTANT `PA-4` | **Coordinated**: `PP-9` generalizes LEARN-R18's existing `pending_outcome`/`outcome_resolved` pair rather than adding a second outcome facility. `PA-4`'s decision journal must land on the same one. |
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:100` — ## §3 Outcomes — measure what landed, not what ran (`PP-9`, `PP-10`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:106` — unmeasurable outcome decays faster. `PP-9` generalizes it; `PP-10` uses it to surface a work unit whose
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:142` — ├─→ PP-9 ─→ PP-10                  │
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:153` — **The timing argument, which is the part that needs an owner decision.** `PP-4`, `PP-5`, `PP-9` and
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:806` — - **2026-08-14 — `PP-9` DONE.** The outcome pair is now a general facility: `ledger/outcomes.py`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:829` — - **2026-08-14 — DEVIATION (`PP-9`): the resolver is no longer inert without a vector store, and
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:839` — - **2026-08-14 — DISCOVERY (`PP-9`): "after the bet" must be FILE POSITION, not `seq`.** The
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:849` — - **2026-08-14 — DECISION (`PP-9`): only a DECISION's outcome files a lesson proposal.** Generalizing
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:855` — - **2026-08-14 — `PP-9` proof.** Falsified three times, each with the target line read first and
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1013` — `PP-9` opened the publish question against `SOURCE_MEMORY` with a semantic key

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `R18`. Those tokens appear in its source plan at:

- `R18` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:71` — | Outcome/consumption records | PROACTIVE-ASSISTANT `PA-4` | **Coordinated**: `PP-9` generalizes LEARN-R18's existing `pending_outcome`/`outcome_resolved` pair rather than adding a second outcome facility. `PA-4`'s decision journal must land on the same one. |
- `R18` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:104` — audit rather than at runtime. The mechanism already exists, scoped to one feature: LEARN-R18's
- `R18` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:193` — 2. A workflow diamond spanning two containers runs correctly, and WF2-R18's join tests stay green.
- `R18` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:830` — `resolved_at` is deleted.** LEARN-R18 returned an empty report unless a live vector store was
- `R18` `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1075` — early-fire direction), plus the WF2-R18 join tests staying green.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
