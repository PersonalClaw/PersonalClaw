<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-1` — Shared inbound access seam — widen plan-41 substrate to 5 surfaces + per-client identity

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #214 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1951 |

## Intent — what this is for

§1 (The Inbound Access Layer): §1.1 fail-closed enablement, §1.2 per-client identity/bindings, §1.3 hard caps, §1.4 framing wrapper, §1.5 audit; §10 stores; §11 ExternalAccessConfig 4-point wiring. Session 1.

## Done when

New ExternalAccessConfig top-level section wired through all 4 points (dataclass+_meta, load(), to_dict(), _EDITABLE_CONFIG runtime subset — tokens/public_url NOT PATCH-editable); per-surface tokens (openai/mcp/a2a/capture/bridge) via save_credential with ≥32-byte + not-dashboard-token refusal; `~/.personalclaw/inbound_clients.json` (0600, atomic_write) with label/token_hash/surfaces/agent/tools/scope/rate_overrides/disabled; constant-time client lookup with bindings-as-pins (arg override = 403 + SEL); per-client token-bucket (1rps/burst20/4 concurrent) + result caps + auto-disable-on-repeat-breach; layered kill switches (master + per-surface + per-client + AUTONOMY-GUARDRAILS incident check, fail-closed parse); single fence_untrusted response wrapper; inbound_audit.jsonl (2× trim) + SEL on security events; Settings→External Access skeleton; inbound_clients.json + sender_trust.json join export/snapshot sets; test_config_roundtrip passes.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:MCP-READONLY-INBOUND:inbound/ substrate (auth/caps/audit/mcp_http/tools) to widen` | cross-plan (EXT) | **unresolved** |
| `EXT:AUTONOMY-GUARDRAILS:incident.json flag + guard_flag pattern` | cross-plan (EXT) | **unresolved** |

### Unblocks (5)

[`EA-2`](EA-2.md) (`done`), [`EA-4`](EA-4.md) (`done`), [`EA-5`](EA-5.md) (`done`), [`EA-8`](EA-8.md) (`done`), [`EA-9`](EA-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `7cd1a7134` 2026-08-23 — chore(roadmap): cite EA-1 to the batch PR
- `7162b4330` 2026-08-23 — feat(inbound): EA-1 shared inbound access seam — ExternalAccessConfig + per-client identity
- `ecd3f06e8` 2026-08-20 — docs(roadmap): WF2KNO-9 BLOCKED — the egress chokepoint has no atom

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:312` — ## Execution log — EA-1 (§1 shared inbound access seam + §10 stores + §11 config wiring, Session 1)
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:314` — - [2026-08-23][EA-1] **DONE.** All acceptance clauses hold, with two deviations and two follow-ups recorded
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:320` — - [2026-08-23][EA-1] **This is a CLEAN BREAK, not an additive section — two user-visible breaks.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:326` — - [2026-08-23][EA-1] **All four layered kill switches were proved to DENY, individually.** Each was
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:333` — - [2026-08-23][EA-1] 🔴 **`_EDITABLE_CONFIG` is not the only write path, so the criterion's phrasing was
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:343` — - [2026-08-23][EA-1] 🔴 **A dict-comprehension in `load()` made the four-points harness structurally
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:350` — - [2026-08-23][EA-1] **Backend truth, frontend silence — closed.** The backend shipped `caps` (five
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:355` — - [2026-08-23][EA-1] **A second implementation of layer 3 was deleted, not wired.** `gate.client_problem`
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:359` — - [2026-08-23][EA-1] **DEVIATION — `sender_trust.json` does not exist as-built.** §10 names it; the trust
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:362` — - [2026-08-23][EA-1] **DEVIATION — `inbound_audit.jsonl` ships 0644, not 0600.** Left deliberately: the
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:366` — - [2026-08-23][EA-1] **`test_portability`: `inbound_audit` added to `_SNAPSHOT_COVERAGE_GAPS` with its
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:368` — - [2026-08-23][EA-1] **Seven flat `{"error": prose}` sites converted to `http_errors.json_error`** rather
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:371` — - [2026-08-23][EA-1] **A design ratchet resolved an identifier tree-wide and mis-attributed six unrelated
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:380` — gateway's startup/shutdown.** `EA-1` had already done the contract-owner work: the `bridge` surface
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:549` — `EA-1`; the `check_sender`/`TrustDecision` naming drift was not.
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1115` — EA-1 (loader.py:3944/5159) so no config round-trip work was needed.
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1209` — EA-1); `_prompt_of` sends the last user turn plus system messages rather than replaying the client's
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1036` — + a2a-call outbound provider"), which is `todo` behind `EA-1` inside External-Access — a capstone

### Its plan's own record, via the scope references (34 line(s))

This atom's scope cites `§1`, `§1.1`, `§1.2`, `§1.3`, `§1.4`, `§1.5`, `§10`, `§11`, `Session 1`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:26` — - **NEW-10** (hardened external access: OpenAI-compatible inbound HTTP API where `model` targets an agent + `/v1/audio/speech|transcriptions|voices` aliases; gateway-mounted MCP server exposing a curated read-only capability subset with per-client bindings + kill switch; sender-trust substrate — …
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:44` — Two backlog items, one seam. NEW-10 points capability **outward** (external clients drive PClaw); NEW-20 points capture **inward** (PClaw learns from external agents). Both need the same thing built once: an authenticated, fail-closed, rate-capped, SEL-audited, kill-switchable HTTP mount on the g…
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:58` — - **Kill switches, layered:** (a) `external_access.enabled` master toggle (config, PATCH-editable — flipping it unmounts within one config read); (b) per-surface `enabled` flags; (c) per-client `disabled` flag (§1.2); (d) AUTONOMY-GUARDRAILS' incident mode (`~/.personalclaw/incident.json`) is che…
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:88` — - `model: "personalclaw/<agent-name>"` selects an agent from config.json `agents{}` (agents are an EntitySeamHandler entity, not a provider — resolution goes through the existing agent-binding path, `resolve_agent_bindings` loader.py:2067, then chat dispatch). A client with an `agent` binding (§1…
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:117` — - **Per-client bindings** (§1.2) subset this table per client and can pin scope (e.g. `scope: {project: "p-1234"}` filters tasks/knowledge to one project — args cannot widen it). `tools/list` reflects exactly the client's subset.
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:118` — - Results framed per §1.4; hard caps per §1.3 (100 results / 2 MiB / broad-query rejection over large stores, a >10k-row guard adapted to memory/knowledge row counts).
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:119` — - **Kill switch:** `external_access.mcp.enabled` + the master + incident checks (§1.1). One PATCH flips it off; in-flight requests finish, new ones get 503.
- `§1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:128` — - **Transport:** loopback-only, ALWAYS (exempt from `allow_remote`); mounted on a **random ephemeral port** chosen at gateway startup (its own tiny aiohttp runner, because discoverability-by-port-scan is the threat the random port answers), bearer per §1.1 (`bridge` surface). **Discovery file** `…
- `§1.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:119` — - **Kill switch:** `external_access.mcp.enabled` + the master + incident checks (§1.1). One PATCH flips it off; in-flight requests finish, new ones get 503.
- `§1.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:128` — - **Transport:** loopback-only, ALWAYS (exempt from `allow_remote`); mounted on a **random ephemeral port** chosen at gateway startup (its own tiny aiohttp runner, because discoverability-by-port-scan is the threat the random port answers), bearer per §1.1 (`bridge` surface). **Discovery file** `…
- `§1.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:161` — - The external agent sets `OPENAI_BASE_URL=http://127.0.0.1:10000/capture/v1` (or `ANTHROPIC_BASE_URL=.../capture`) and `OPENAI_API_KEY=<capture-surface bearer>` — auth IS the §1.1 token, so misconfigured agents fail loud, not open. Loopback-only, always (capture never sets `allow_remote`).
- `§1.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:300` — - Everything else (auth via §1.1 surface bearer + §1.2 client bindings, headless profile, SpendMeter per-client budgets, `inbound:` stateless prefix) is already specified — no change.
- `§1.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:58` — - **Kill switches, layered:** (a) `external_access.enabled` master toggle (config, PATCH-editable — flipping it unmounts within one config read); (b) per-surface `enabled` flags; (c) per-client `disabled` flag (§1.2); (d) AUTONOMY-GUARDRAILS' incident mode (`~/.personalclaw/incident.json`) is che…
- `§1.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:88` — - `model: "personalclaw/<agent-name>"` selects an agent from config.json `agents{}` (agents are an EntitySeamHandler entity, not a provider — resolution goes through the existing agent-binding path, `resolve_agent_bindings` loader.py:2067, then chat dispatch). A client with an `agent` binding (§1…
- `§1.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:117` — - **Per-client bindings** (§1.2) subset this table per client and can pin scope (e.g. `scope: {project: "p-1234"}` filters tasks/knowledge to one project — args cannot widen it). `tools/list` reflects exactly the client's subset.
- `§1.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:148` — - **`channel_transports/trust.py`:** one store `~/.personalclaw/sender_trust.json` (atomic_write) — `{transport: {sender_id: {state: allowed|denied|pending, display, paired_at, source: pairing|owner_approve|manual}}}` + `check_sender(transport, sender_id) -> TrustDecision`, consulted by the gatew…
- `§1.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:300` — - Everything else (auth via §1.1 surface bearer + §1.2 client bindings, headless profile, SpendMeter per-client budgets, `inbound:` stateless prefix) is already specified — no change.
- `§1.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:118` — - Results framed per §1.4; hard caps per §1.3 (100 results / 2 MiB / broad-query rejection over large stores, a >10k-row guard adapted to memory/knowledge row counts).
- `§1.4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:118` — - Results framed per §1.4; hard caps per §1.3 (100 results / 2 MiB / broad-query rejection over large stores, a >10k-row guard adapted to memory/knowledge row counts).
- `§1.4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:139` — - **Inbound:** `GET /a2a/agent-card` serves an A2A agent card whose *skills* are the user's **published workflows** — a workflow template gains an `a2a_published: bool` flag (default false; publishing is a per-template user decision in the template detail UI). `POST /a2a/tasks` maps an A2A task o…
- `§1.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:131` — - The bridge's write actions call the same internal handlers the FE calls (triggers façade, tasks handlers) — no parallel mutation paths. Every action call is audit-lined per §1.5.
- `§1.5` `docs/roadmap/plans/EXTERNAL-ACCESS.md:363` — real home's `security_events.jsonl` and `notifications.jsonl` — the precedent §1.5 itself cites — are
- `§10` `docs/roadmap/plans/EXTERNAL-ACCESS.md:312` — ## Execution log — EA-1 (§1 shared inbound access seam + §10 stores + §11 config wiring, Session 1)
- `§10` `docs/roadmap/plans/EXTERNAL-ACCESS.md:359` — - [2026-08-23][EA-1] **DEVIATION — `sender_trust.json` does not exist as-built.** §10 names it; the trust
- `§10` `docs/roadmap/plans/EXTERNAL-ACCESS.md:367` — reason** (it is `derived=True`, and §10 excludes it) rather than silently widening coverage.
- `§10` `docs/roadmap/plans/EXTERNAL-ACCESS.md:545` — against a contract the ruling may redefine. Also worth correcting in plan text: §6/:148 and §10/:208
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:203` — | External-access config | `config.json` → `external_access` section | `ExternalAccessConfig` dataclass | four wiring points (§11) |
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:273` — | Silent config drop (four-wiring-points gotcha) | Explicit checklist in §11; schema reachability tests enforce `_meta`; tokens deliberately outside config.json entirely |
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:312` — ## Execution log — EA-1 (§1 shared inbound access seam + §10 stores + §11 config wiring, Session 1)
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:337` — consult `_EDITABLE_CONFIG` at all. That is §11's stated design (its alternative is "a deliberate
- `§11` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1404` — **Citation correction.** `validation.py:555` is cited in §5, §11 and the `done_when` row.
- `Session 1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:249` — - **Session 1 — the seam (§1):** `inbound/` module, fail-closed mounting, token lifecycle (`save_credential`, CLI), client registry + bindings, caps + token bucket, framing wrapper, kill switches + incident check, audit JSONL + SEL, `ExternalAccessConfig` through all four wiring points, Settings …
- `Session 1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:258` — Sessions 1-3 are NEW-10's core and ship value alone; 4-5 are NEW-20; 6 completes both. Session 4 can land before 2-3 if capture is wanted early (it depends only on Session 1). Session 7 depends on Session 1's headless-profile classification but can otherwise land any time after it.
- `Session 1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:312` — ## Execution log — EA-1 (§1 shared inbound access seam + §10 stores + §11 config wiring, Session 1)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
