<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CRE-4` — Release pipeline release.yml (S3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CI-RELEASE-ENGINEERING](../plans/CI-RELEASE-ENGINEERING.md) (`CRE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CRE.md`](../atomic/CRE.md) |
| **Execution position** | #134 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Sessions S3; Task breakdown Session 3 T3.1-T3.5/V3; Contracts C1 release.yml (build/pypi/images/notes/attest); C2 owner two-package rule

## Done when

release.yml on tag v* has build (npm build web → python -m build; wheel contains personalclaw/static/dist/index.html), pypi + pypi-client (Trusted Publishing, no token secrets, separate environment: release / release-client per unique PyPI publisher tuple), images (buildx linux/amd64+arm64 GHCR via GITHUB_TOKEN), notes (CHANGELOG section verbatim), attest (attest-build-provenance on wheel+images); YAML+C1 contract-valid and proven live via owner-approved rc tag

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:DISTRIBUTION:wheel bundles web/dist (packaging change is DISTRIBUTION S1)` | cross-plan (EXT) | **unresolved** |

### Unblocks (2)

[`CRE-5`](CRE-5.md) (`done`), [`CRE-7`](CRE-7.md) (`todo`)

### ⚠️ In a dependency cycle

This atom sits in a cross-plan cycle, so no execution order can satisfy it as declared — the cycle has to be broken by a scope decision:

- `CRE-4 → DIST-3 → DIST-1 → CRE-4`

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `2f0dffd37` 2026-09-07 — feat(roadmap): rank the owner's queue by what each atom actually unblocks
- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `281d693b1` 2026-09-06 — docs(roadmap): execute the 2026-08-28 cycle ruling, and mint the atom for a seam nobody adopted

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:494` — **The other cycle is harmless and needs nothing:** `CRE-4 → DIST-3 → DIST-1 → CRE-4` is formed the same

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T3.1`, `T3.5`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:121` — | T3.1 | `release.yml` skeleton on tag `v*`: job `build` (checkout → `npm ci && npm run build` in web/ → `python -m build` → upload wheel+sdist artifacts) | `.github/workflows/release.yml` | rc tag on a branch produces artifacts; wheel contains `personalclaw/static/dist/index.html` (unzip check s…
- `T3.5` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:125` — | T3.5 | Attestations: `actions/attest-build-provenance` on wheel + images | `release.yml` | `gh attestation verify` passes on the wheel |
- `Session 3` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:117` — ### Session 3 — Release pipeline

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
