<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-2` — Static boundary scanner + diff-aware run + same-PR rule + Makefile wiring

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #545 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§1.3 (7 seed scanner checks), §1.4 (same-PR rule), §7 Session 2

## Done when

harness run --diff forces the replay profile when touching web/src/pages/chat/; a config field missing from AppConfig.load() fails config-four-points with a WHAT/WHY/FIX message; clean-tree calibration guard passes (Success Criterion #2)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SV-1`](SV-1.md) | intra-repo | `done` |

### Unblocks (1)

[`SV-7`](SV-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `§1.3`, `§1.4`, `§7`, `Session 2`. Those tokens appear in its source plan at:

- `§1.3` `docs/roadmap/plans/SELF-VERIFICATION.md:66` — scanner.py     # static boundary scanner (§1.3)
- `§1.3` `docs/roadmap/plans/SELF-VERIFICATION.md:213` — | Two-weight run records / skip-rationale ledger rows; triage-first convention | AUTOMATION-SUBSTRATE §1.3 / WF2-R15 + UNIVERSAL-PLANNING impact-triage | §3.2 step 1 consumes the conventions |
- `§1.3` `docs/roadmap/plans/SELF-VERIFICATION.md:226` — - **Session 2 — scanner + diff-aware run (§1.3, §1.4)**: the seed check table; `run --diff` forcing profiles from touched areas; same-PR-rule warning; WHAT/WHY/FIX finding format; Makefile target wiring the harness into the standard test entrypoint.
- `§1.3` `docs/roadmap/plans/SELF-VERIFICATION.md:269` — - [2026-07-26][S1] DEVIATION: the plan's Overview/§1.3 cite `test_action_schema_executor_parity.py` as the proof-of-pattern for the allowlist↔registry invariant. Code recon shows that test guards a DIFFERENT invariant (executor `action_config` reads ⊆ its `app.json` `settingsSchema`). The allowli…
- `§1.3` `docs/roadmap/plans/SELF-VERIFICATION.md:274` — - [2026-07-26][S2] DONE: Boundary scanner + diff-aware run + same-PR rule (§1.3, §1.4). `harness/scanner.py` — 7 static checks (hook-provider-parity, sse-event-registered, config-four-points, app-sdk-boundary = ERROR; destructive-test-isolation, fence-at-ingestion, no-naive-transcript-cut = WARNI…
- `§1.3` `docs/roadmap/plans/SELF-VERIFICATION.md:276` — - [2026-07-26][S2] DISCOVERY (not fixed — heuristic residual): `destructive-test-isolation` warns on `tests/test_sdk_cli.py`, which calls `save_credential` on a test-double `ctx` (not the real store) — a false positive the heuristic can't distinguish. Left as an advisory WARNING (its designed rol…
- `§1.4` `docs/roadmap/plans/SELF-VERIFICATION.md:226` — - **Session 2 — scanner + diff-aware run (§1.3, §1.4)**: the seed check table; `run --diff` forcing profiles from touched areas; same-PR-rule warning; WHAT/WHY/FIX finding format; Makefile target wiring the harness into the standard test entrypoint.
- `§1.4` `docs/roadmap/plans/SELF-VERIFICATION.md:274` — - [2026-07-26][S2] DONE: Boundary scanner + diff-aware run + same-PR rule (§1.3, §1.4). `harness/scanner.py` — 7 static checks (hook-provider-parity, sse-event-registered, config-four-points, app-sdk-boundary = ERROR; destructive-test-isolation, fence-at-ingestion, no-naive-transcript-cut = WARNI…
- `Session 2` `docs/roadmap/plans/SELF-VERIFICATION.md:226` — - **Session 2 — scanner + diff-aware run (§1.3, §1.4)**: the seed check table; `run --diff` forcing profiles from touched areas; same-PR-rule warning; WHAT/WHY/FIX finding format; Makefile target wiring the harness into the standard test entrypoint.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
