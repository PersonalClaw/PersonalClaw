<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CRE-7` — Owner real-world provisioning remainder

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [CI-RELEASE-ENGINEERING](../plans/CI-RELEASE-ENGINEERING.md) (`CRE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CRE.md`](../atomic/CRE.md) |
| **Execution position** | #137 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`owner`) |
| **Blocked reason** | OWNER-ONLY (inventory correction 2026-09-06). Carried no blocked_reason, so the ready frontier counted it as available agent work. Its own done_when ends 'owner-executed, no agent code change' — the clauses are confirming GHCR packages are public after the first image push, confirming the coverage-badge home, and deciding whether main gets branch protection requiring ci.yml green. Each is a registry/settings action on the live org, not a repo change, and the last is a governance decision rather than a task. Nothing here is buildable; leaving it unmarked only made the frontier over-report. |

## Intent — what this is for

Owner tasks (real world) items 2/4/5; Status line 'Remaining are OWNER items only'

## Done when

GHCR packages confirmed public after first image push; coverage-badge home confirmed (shipped self-owned in full.yml); optional main branch protection decided (require ci.yml green) — owner-executed, no agent code change

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CRE-4`](CRE-4.md) | intra-repo | `done` |
| [`CRE-5`](CRE-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `c010dab70` 2026-09-07 — fix(tools): a pre-existing container is not deliverable evidence
- `df456c8e2` 2026-09-06 — docs(roadmap): mark DIST-11 and CRE-7 owner-only — the frontier was over-reporting
- `dd876068e` 2026-09-05 — docs(roadmap): inventory-audit corrections — PEP-11 decomposed to PEP-12..20, ET-10 superseded, four stale annotations refreshed

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/DISTRIBUTION.md:392` — (clean-machine walkthroughs V1–V4) and `CRE-7` (real-world provisioning remainder) are owner
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:117` — (confirm/make the packages public; the anonymous package page 404s) is covered verbatim by CRE-7's
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1043` — not re-derive it. All four owner-priority areas hold nothing startable: `DIST-11`/`DIST-12`/`CRE-7`

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
