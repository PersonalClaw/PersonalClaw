<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AS-1` — Composable home: dashboard-as-views registry + Overview preset + pinning + AmbientConfig

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AMBIENT-SURFACES](../plans/AMBIENT-SURFACES.md) (`AS`, plan status `done`) |
| **Atom table** | [`../atomic/AS.md`](../atomic/AS.md) |
| **Execution position** | #70 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #910 |

## Intent — what this is for

§1 The Composable Home — Pinned Tiles Band (§1.1 tile registry, §1.2 placement, §1.3 pinning, §1.4 tile rendering + data seam); Amendment 2026-07-26 round 2 (a) Dashboard-as-views, task A2-1; Provider & Config Plug-in Map (AmbientConfig 5-point wiring). Note: A2-1 supersedes §1.2's single-band placement with named views.

## Done when

dashboard_views.json store + /api/dashboard/views CRUD (presets read-only) exist; locked "Overview" preset renders byte-identical to today's dashboard when the registry is empty; artifact tiles are artifact:<slug> refs with size+order only (no x/y coordinates anywhere); pin-to-dashboard control on WidgetFrame saves-then-POSTs a tile; cached SWR paint via useCachedData; dashboard_tile_propose tool writes added_by:agent rows rendering with an accept/dismiss chip; AmbientConfig section wired through all 5 points (dataclass+_meta, load(), to_dict(), _EDITABLE_CONFIG PATCH allowlist, Settings control) and test_config_roundtrip passes.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (4)

[`AS-2`](AS-2.md) (`done`), [`AS-5`](AS-5.md) (`done`), [`AS-8`](AS-8.md) (`done`), [`AS-9`](AS-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `14529480c` 2026-08-08 — feat(ambient): AS-1 composable home — dashboard-as-views registry, Overview preset, pin + propose, AmbientConfig (#910)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:448` — - **DEVIATION — `dashboard_views.json` was never declared in the durability inventory.** AS-1
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:456` — - **NOTE — the `AS-1` row in `docs/roadmap/atomic/AS.md` still reads ⬜ while `dag.json` says
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:731` — - **GAP (AS-1's, surfaced here, not closed) — `nav_pinned` and `icon` have no frontend consumer.**
- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:734` — rather than absorbed: building that rail is AS-1 surface work, not this atom's `done_when`.

### Its plan's own record, via the scope references (13 line(s))

This atom's scope cites `§1`, `§1.1`, `§1.2`, `§1.3`, `§1.4`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:26` — - **NEW-6** — user-composable home + live artifacts + generative-UI layer → §1 (tile registry), §2 (chatless refresh, freshness chips, SWR paint), §5 (component registry, streaming renderer, `visualize`)
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:51` — - **The customizable dashboard grid was RETIRED — a clean break, documented in code.** `web/src/pages/dashboard/DashboardPage.tsx:24`: *"no bento boxes … The customizable grid + per-user layout persistence were retired (clean break); everyone gets this one content-first layout."* There is **no wi…
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:261` — | `DashboardPage.tsx` fixed layout | **KEPT** | The clean break holds. One additive Pinned band (§1.2); empty registry ⇒ identical page. No grid, no per-widget layout persistence, first-party widgets stay hard-imported |
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:263` — | `WidgetFrame.tsx` | **EXTENDED** | Gains pin-to-dashboard (§1.3), EDITMODE tweak rail (§3), annotate toggle (§4). Sandbox model unchanged |
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:350` — **Interactive in-chat widgets with a round-trip event bridge — mostly ALREADY BUILT; formalize and harden it.** Code recon: the round-trip exists today end to end. `widgetSrcdoc.ts` builds the sandboxed iframe doc (`sandbox="allow-scripts"` off a blob/null origin, strict CSP `connect-src 'none'`,…
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:369` — Two owner decisions. Code recon (2026-07-26): today's `DashboardPage.tsx` renders eight hard-imported widgets — HeroPulse (header strip), ActionCenter, ActiveWork, TasksWidget, Suggestions, Discover, ScheduleWidget ("Recent activity"), SystemHealth (docked rail); the §1 recon's nine-widget list i…
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:371` — **(a) Dashboard-as-views (deliberately supersedes §1.2's "one additive Pinned band" placement).** The dashboard becomes NAMED COMPOSABLE VIEWS over ONE widget registry. Owner sequencing ruling: **PRESETS-FIRST ON A COMPOSITION-READY REGISTRY** — the view model is designed for user composition fro…
- `§1` `docs/roadmap/plans/AMBIENT-SURFACES.md:373` — - **View model:** `~/.personalclaw/dashboard_views.json` — `{id, name, icon?, nav_pinned: bool, widgets: [{ref, size, order}]}`. `ref` = `core:<widget>` | `artifact:<slug>` — §1's artifact tiles become one widget family in the same registry (§1.1's `dashboard_tiles.json` folds into the active vie…
- `§1.1` `docs/roadmap/plans/AMBIENT-SURFACES.md:373` — - **View model:** `~/.personalclaw/dashboard_views.json` — `{id, name, icon?, nav_pinned: bool, widgets: [{ref, size, order}]}`. `ref` = `core:<widget>` | `artifact:<slug>` — §1's artifact tiles become one widget family in the same registry (§1.1's `dashboard_tiles.json` folds into the active vie…
- `§1.2` `docs/roadmap/plans/AMBIENT-SURFACES.md:261` — | `DashboardPage.tsx` fixed layout | **KEPT** | The clean break holds. One additive Pinned band (§1.2); empty registry ⇒ identical page. No grid, no per-widget layout persistence, first-party widgets stay hard-imported |
- `§1.2` `docs/roadmap/plans/AMBIENT-SURFACES.md:371` — **(a) Dashboard-as-views (deliberately supersedes §1.2's "one additive Pinned band" placement).** The dashboard becomes NAMED COMPOSABLE VIEWS over ONE widget registry. Owner sequencing ruling: **PRESETS-FIRST ON A COMPOSITION-READY REGISTRY** — the view model is designed for user composition fro…
- `§1.3` `docs/roadmap/plans/AMBIENT-SURFACES.md:263` — | `WidgetFrame.tsx` | **EXTENDED** | Gains pin-to-dashboard (§1.3), EDITMODE tweak rail (§3), annotate toggle (§4). Sandbox model unchanged |
- `§1.3` `docs/roadmap/plans/AMBIENT-SURFACES.md:373` — - **View model:** `~/.personalclaw/dashboard_views.json` — `{id, name, icon?, nav_pinned: bool, widgets: [{ref, size, order}]}`. `ref` = `core:<widget>` | `artifact:<slug>` — §1's artifact tiles become one widget family in the same registry (§1.1's `dashboard_tiles.json` folds into the active vie…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
