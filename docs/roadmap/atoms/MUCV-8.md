<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MUCV-8` — Settings chain editor + per-entry health dots + composer precedence explainer

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MODEL-USE-CASES-V2](../plans/MODEL-USE-CASES-V2.md) (`MUCV`, plan status `done`) |
| **Atom table** | [`../atomic/MUCV.md`](../atomic/MUCV.md) |
| **Execution position** | #377 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##58 |

## Intent — what this is for

S3 T3.1-T3.4 (Design S3; C4 health consumption)

## Done when

ModelsPanel shows a Chat-routing group with all 5 sub-category rows (fallback:'Chat' empty-state) plus an ordered chain editor for chat+sub-categories (default/fallback-n labels, keyboard-accessible reorder, remove, append-on-pick) persisting order via setActiveModel; per-entry breaker-health dots from api.modelsHealth() (closed->green/half_open->amber/open->red+retry tooltip, no dot when no health row); composer Auto hint reads 'Use-case chain' with the precedence explainer in the pill popover; web typecheck+vitest+build green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MUCV-1`](MUCV-1.md) | intra-repo | `done` |
| [`MUCV-2`](MUCV-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 44 (MUCV) — Chat-routing rows driven; a later atom's supersession recorded

**Code evidence:**

- 🔑 DRIVEN: Settings → Models shows a 'Chat routing' group with ALL FIVE sub-category rows, each carrying the fallback empty-state the clause names — 'uses your Chat chain' — so an unbound row reads as INHERITING rather than as unconfigured
- 🔑 DRIVEN, expanding a row: it states the axis's purpose in product language ('One-shot judgment calls — web-page extraction and other guarded single completions', matching the vocabulary comment), the chain's ordering semantics where the user edits them ('Fallback chain — first is the default, later entries take over on failure'), and an honest optional-ness line: 'Already uses your Chat chain by default — no dedicated Reasoning model is required. Add a backend with a chat-capable model to override.'
- 🔑 THE HEALTH DOT REFUSES TO GUESS (ModelsPanel.tsx:425-435): 'No health row (provider never called) renders nothing — ABSENCE OF DATA MUST NOT READ AS "HEALTHY"'. A green dot on an unprobed provider would tell the user a fallback works when nobody knows
- the open-state tooltip explains the CONSEQUENCE rather than the state — 'chain entries on this provider are skipped until it recovers' — which is exactly MUCV-1's breaker-aware walk surfaced to the user
- role="img" plus a label because 'the dot is the ONLY carrier of the breaker state … on a role-less span aria-label is a PROHIBITED attribute — the name is discarded', the same precedent this codebase cites for its other icon-only state carriers
- test_use_case_chains + test_can_resolve_use_case 40/40

**Driven in the UI:** Drove the group, all five rows and one expanded row. The chain editor's reorder/remove/append and the dots themselves need bound models plus a called provider, so those are confirmed in code rather than driven.

**Notes:** The optional-ness line is the best copy in the plan: it converts an empty row from 'you have work to do' into 'this inherits, and here is how to override it'. Combined with the row label, a user cannot mistake an unbound sub-category for a misconfiguration — which matters because there are now five of them and four are usually meant to stay empty.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T3.1`, `T3.4`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:131` — | T3.1 | Chat-routing group in the Models panel: rows for all 5 sub-categories (label/description/`fallback: 'chat'` empty-state) + `USE_CASE_ORDER` updated | `web/src/pages/settings/ModelsPanel.tsx`, `modelsPanel.test.ts` | every sub-category is bindable in the UI; unbound rows read "uses your C…
- `T3.1` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:155` — - [2026-07-27][S3] DONE: Settings UI + composer explainer (T3.1-T3.4). ModelsPanel: `USE_CASE_META/ORDER` now carry all 5 sub-categories under a "Chat routing" group (labels/descriptions/`fallback: 'Chat'` empty-state "uses your Chat chain"); `multi` semantics replaced by `chain` — chat + sub-cat…
- `T3.4` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:134` — | T3.4 | Composer precedence explainer: one line in the model-pill popover ("Overrides this session's chain; if it fails, the chain takes over"), and 'Auto' labeled as "use-case chain" | `web/src/ui/composer/` popover component | explainer visible; no behavior change to the pick path |
- `T3.4` `docs/roadmap/plans/MODEL-USE-CASES-V2.md:155` — - [2026-07-27][S3] DONE: Settings UI + composer explainer (T3.1-T3.4). ModelsPanel: `USE_CASE_META/ORDER` now carry all 5 sub-categories under a "Chat routing" group (labels/descriptions/`fallback: 'Chat'` empty-state "uses your Chat chain"); `multi` semantics replaced by `chain` — chat + sub-cat…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
