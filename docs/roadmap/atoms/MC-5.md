<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-5` — S3 push-to-approval milestone 1: web push + ntfy adapter + deep link to the approval

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #339 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, recorded 2026-08-27. NOTE for anyone reading the branch: the MC-5 branch's own copy of this field claimed clause 1 was waiting on an owner decision to split config/loader.py. That claim is FALSE and was never in main's dag.json. It came from the branch putting the ceiling holder at 5965 lines with only 35 of headroom against the >=100 rail - which is exactly the 'assert 35 >= 100' CI red it produced - and it needed no owner decision at all: SH-2's config/credentials.py extraction paid the headroom, and a rebase alone took the holder to 5646 with 354 of headroom (measured live through scripts.generate_structural_baseline.scan(), never read from the baseline JSON; watch-band headroom 179 either way). Against newer main it lands at ~316, so it is not marginal. Nothing was raised, widened or regenerated. WHAT REMAINS is device-gated, not code-gated: the content-free push has no way to be observed here without a real phone accepting a VAPID subscription. The code half is implemented and railed and the atom is NOT superseded - main has no VAPID/web-push at all (a case-insensitive grep for vapid|web.?push across src/ and web/src returns zero; main's push_* files are durability-sync, memory, and push-to-talk audio). ONE CROSS-PLAN INTEGRATION FAILURE was found and fixed on the way, worth recording because it is not a merge conflict and no rebase would have surfaced it: DC-5's rail nativeNotifications.test.tsx::'covers every source the backend can send' parses NotificationKind("<source>" out of the Python and requires every source to have a native route, so MC-5's new `approval` source reddened it. Routed approval -> 'chat' because pages/chat/ApprovalCard is where a DESKTOP user answers the decision, and companion sits deliberately outside App.tsx's ROUTABLE set. Also corrected while here: notification_rules.py's doc prose had main and this branch making CONTRADICTORY claims about which targets are live (main: native live / push reserved; this branch the reverse), so neither side alone was true post-merge. Composed a third version stating both are live and naming channel_dm as the one remaining inert target. |

## Intent — what this is for

Amendment §S3 Push-to-approval (T3.2 web push, T3.3 ntfy/UnifiedPush adapter, T3.4 SW deep link, V3) + C3 Push

## Done when

VAPID keypair generated via `personalclaw push init` (keys in credential store), per-device subscription endpoint, and a content-free {kind,item_id} sender are wired as plan-42's `push` target; ntfy topic-URL adapter is an alternative backend (config mobile.push_backend/ntfy_topic_url); a locked-phone push → tap opens #/companion?approval=<id> with the correct card focused → approve → the paused run proceeds, <30s on cell data (timed); payload inspection shows ids only.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MC-3`](MC-3.md) | intra-repo | `done` |
| [`MC-4`](MC-4.md) | intra-repo | `done` |
| [`MC-2`](MC-2.md) | intra-repo | `done` |

### Unblocks (1)

[`MC-9`](MC-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 57 (MC) — the companion route driven at a 390x844 phone viewport; the content-free push gate falsified

**Code evidence:**

- 🔑 THE PAYLOAD CONTRACT IS THE SECURITY PROMISE AND IT IS ENFORCED, NOT DOCUMENTED — the module says so: `PAYLOAD_KEYS = frozenset({'kind', 'item_id'})`, 'Asserted, not documented', with ONE constructor (`content_free_payload`) and ONE gate (`assert_content_free`) so the payload 'is a shape the module BUILDS rather than trusted to each caller's discipline'
- 🔑 THE REASON IS THE RIGHT ONE AND IT NAMES THE ADVERSARY: a payload carrying a tool's arguments 'would make every one of those hosts a [confidant]' — the push services and any relay in the path. Data minimisation as a design constraint rather than as advice
- 🔑 AND THE SHARPEST LINE IN THE PLAN EXPLAINS WHY THERE IS NO HUMAN-READABLE BODY: 'the alternative is composing a sentence, and a sentence is content.' A notification title cannot be content-free, so there is none
- 🔑 FALSIFIED: disabling the key-set check reddens FIVE tests, including `test_a_content_laden_payload_never_reaches_the_wire` and four gate cases (two extra-key, two missing-key). Restored; 46/46
- the gate checks the key set BOTH ways, with the reason given — 'a missing key is as much a contract break as an extra one, because a sender that forgot `item_id` would ship a push the phone cannot route'
- the VAPID keypair lives in the credential store rather than in config, which is this repo's stated contract for secrets
- device-session + push + relay + shell + companion-discovery + single-pairing suites 106/106; PWA symlink 10/10 (falsified: disabling the payload key check reds 5, including the never-reaches-the-wire test)

**Driven in the UI:** Not drivable: a locked-phone notification needs a real device and a real push service. The gate, the constructor and the wire test were exercised directly, which is where the security claim lives.

**Notes:** 🔑 THE BEST DATA-MINIMISATION DESIGN IN THE CAMPAIGN. Two properties together: the safe payload has exactly one constructor, and the unsafe payload raises at a gate every sender crosses. Either alone would be a convention; together they make 'a push never carries content' a property of the module.

## Recorded history

### Commits (4)

- `90aa345cd` 2026-09-05 — docs(roadmap): flip MC-9 + EA-7 done (EXTERNAL-ACCESS completes 9/9); PP-16 4d/4e exec-log
- `944615456` 2026-09-05 — feat(push): the relay backend — ids-only pings through a stateless push-relay (MC-9)
- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `eebb6eac2` 2026-08-26 — feat(mobile): MC-5 content-free push to the phone

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:200` — `mobile.push_backend` gains `relay`: `send_relay` (src/personalclaw/push.py) wraps MC-5's
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:211` — ENVIRONMENT LIMIT, named the way MC-5's locked-phone leg was: no real handset was driven — on-device
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:219` — **2026-09-04 — MC-6 validated done (evidence #2359).** Clause 2 — the SW per-kind sound + badge cue mapping — shipped: the `push` handler in `web/src/sw.ts` and `web/src/app/pushPolicy.ts`'s `notificationFor()` compose the notification from the payload `kind`; a `sound` key was added to `notifica…
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:221` — **2026-09-02 — MC-5 validated done (impl pre-landed).** Web-push + ntfy adapter live as plan-42's `push` target (`push.py`, `handlers/push.py`; `notification_rules.py` marks push LIVE as of MC-5). `tests/test_mc5_push_to_approval.py` 34/34.
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:288` — manifest/service worker is `MC-4`; push and `#/companion?approval=<id>` are `MC-5`; the
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:540` — gap.** The done-when's call site is `MC-5`'s: `web/src/sw.ts`'s `push` handler and
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:544` — handler and a second notification composer into the same file `MC-5` already edits — the duplicate
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:549` — `mode`/`targets`/`conditions`/`verify`; `MC-5` extended that module (`ensure_target`) without
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:858` — ### 2026-08-27 — `MC-5` (S3 T3.4 push half) — **PARTIAL**: the whole chain ships and is proven; the timed locked-phone leg is device-gated

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T3.2`, `T3.3`, `T3.4`. Those tokens appear in its source plan at:

- `T3.2` `docs/roadmap/plans/MOBILE-COMPANION.md:120` — | T3.2 | Web push: VAPID keypair generation (`personalclaw push init` CLI; keys in credential store), subscription endpoint + per-device storage, content-free payload sender wired as plan 42's `push` target | backend push module (new, small), CLI, rules-engine target registration | push arrives o…
- `T3.2` `docs/roadmap/plans/MOBILE-COMPANION.md:171` — - **S3 — Push-to-approval (milestone 1 completes here):** T3.1 (manifest/SW) + T3.2 (web push) + tap-through deep-link to the specific approval; V3 is the milestone validation.
- `T3.3` `docs/roadmap/plans/MOBILE-COMPANION.md:121` — | T3.3 | ntfy/UnifiedPush path: docs + a delivery adapter (POST to user-configured ntfy topic URL, content-free) as an alternative push target | adapter + `docs/guides/remote-access.md` section | self-hosted ntfy receives pings; tap-through opens companion (deep link) |
- `T3.3` `docs/roadmap/plans/MOBILE-COMPANION.md:137` — | T4.1 | Capacitor shell: wraps the served companion URL, native safe-areas, no forked UI. Connection state is COMPANION-APPS §C1's endpoint registry (`{active, endpoints[]}` per plan 54 S3/T3.3, already shipped in `web/src/lib/endpoints.ts`) — **not** a single stored gateway URL and not a second…
- `T3.3` `docs/roadmap/plans/MOBILE-COMPANION.md:138` — | T4.2 | QR pairing screen **and endpoint switching**, both consumed from plan 54: Settings → Devices → "Pair phone" renders a QR of COMPANION-APPS §C2's `{pairing_url, code}` → app scans → `pair/complete` → device session (§C1); the paired gateway then becomes a §C1 registry entry (`device_sessi…
- `T3.4` `docs/roadmap/plans/MOBILE-COMPANION.md:177` — | T3.4 | Push→approval deep link: SW notification click opens `#/companion?approval=<id>` scrolled/highlighted; payload stays content-free `{kind, item_id}` | `web/src/sw.ts`, companion route | locked-phone push → tap → correct card focused → approve → run proceeds; <30s on cell data, timed |
- `T3.4` `docs/roadmap/plans/MOBILE-COMPANION.md:858` — ### 2026-08-27 — `MC-5` (S3 T3.4 push half) — **PARTIAL**: the whole chain ships and is proven; the timed locked-phone leg is device-gated

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
