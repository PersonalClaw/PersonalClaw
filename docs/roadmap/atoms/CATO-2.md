<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-2` — Wire the chat turn-completion write site (reference attribution)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #98 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

C2 — Attribution (chat row, the reference implementation); Session 1 T1.2

## Done when

A real chat turn writes exactly one ledger row beside the existing stats.inc_cost_usd (chat_runner.py ~2730) with correct tokens; cost derived via pricing.estimate_cost ONLY when the provider reported none (vendor cost wins when present); an unpriced model writes priced=False, cost_usd=0.0

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-1`](CATO-1.md) | intra-repo | `done` |

### Unblocks (1)

[`CATO-6`](CATO-6.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 42 (CATO) — Usage panel driven, period round-trip survived a reload

**Code evidence:**

- MEASURED: chat_runner.py:666 defines _record_turn_usage and :4038 calls it on the turn-completion path, beside the existing stats write the clause points at
- 🔑 THE PRECEDENCE RULE IS EXPLICIT AND CORRECT (record_from_event's docstring): cost is derived via pricing.estimate_cost ONLY when the provider reported none — 'vendor cost wins when present'. An estimator that overrode a real vendor number would make every priced row a guess
- priced is False only when the model has no price row AND the provider reported no cost, and then cost_usd is 'an honest 0.0 the UI renders unpriced'
- estimate_if_missing=False exists as a caller-side opt-out, so a site that must not guess can say so rather than being silently estimated for
- test_usage_ledger + test_usage_routes + test_usage_reachable_purposes 57/57; test_anthropic_cache_usage 5/5; test_api_manifest_drift 8/8; web usage + turn-telemetry 30/30

**Driven in the UI:** Not driven: a real chat turn needs a bound model. The write site and its precedence rule are confirmed in code and by the 57 passing ledger/route tests.

**Notes:** The interesting choice is that every write site shares ONE seam (record_from_event) rather than each assembling a TurnUsage. That is what makes the vendor-cost-wins rule a single fact instead of five copies that could disagree — the same reasoning EIAT applied to its fence being applied exactly once.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:179` — - **CATO-2 DONE — the chat write-site (C2, 1 of 4).** A real dashboard chat turn now writes exactly
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:208` — the loop-worker + schedule-injection paths ALREADY route through `run_chat` (so CATO-2 covers them
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:215` — own EVENT_COMPLETE loop (`source="cli"`). Refactored CATO-2's `_record_turn_usage` and CATO-3's
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:289` — **COST-AND-TOKEN-OBSERVABILITY is now fully shipped:** store (CATO-1) → 4 write-sites (CATO-2/3/4) →

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:132` — | T1.2 | Wire the `chat` site beside the existing `stats.inc_cost_usd` (`chat_runner.py:2558`); derive cost only when the provider reported none; set `priced=False` only when unpriced AND unreported | `dashboard/chat_runner.py`, tests | a real chat turn writes exactly one row with correct tokens;…
- `Session 1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:127` — ### Session 1 — The durable ledger

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
