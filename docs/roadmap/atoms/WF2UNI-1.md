<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2UNI-1` — Matching + classification: no-LLM intent classifier + tiered match_template T1-T5 + routing-fixture CI gate

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-UNIVERSAL-PLANNING](../plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md) (`WF2UNI`, plan status `in_progress`) |
| **Atom table** | [`../atomic/WF2UNI.md`](../atomic/WF2UNI.md) |
| **Execution position** | #635 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##178 |

## Intent — what this is for

Implementation Effort Session 1; Architecture: Intent Classifier (UP-R12); Tiered Template Matcher (UP-R2); Planner Eval Harness (UP-R13.1)

## Done when

workflows/intent.py (4-dimension tuple + rigor routing) and workflows/matcher.py (T1-T5, reason strings, negatives, presets, failure-path degradation) are live from workflow_plan; tests/fixtures/planner_routing.json asserted structurally in CI at 100% deterministic-tier accuracy with zero LLM calls; 18 bundled templates annotated with typed match metadata on DefMetadata.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (4)

[`WF2UNI-11`](WF2UNI-11.md) (`done`), [`WF2UNI-12`](WF2UNI-12.md) (`done`), [`WF2UNI-2`](WF2UNI-2.md) (`done`), [`WF2UNI-9`](WF2UNI-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 21 (WF2UNI) — code + tests observed on this machine

**Code evidence:**

- workflows/intent.py and workflows/matcher.py both present, with the T1-T5 tiering and reason strings the atom names
- the classifier is deliberately LLM-FREE, which is what makes the routing decision cheap enough to run on every plan request
- 430 python tests pass across the planning/matching/grounding/revision/grill modules (1 unrelated skip), plus 176 web tests over 16 loops-page files

**Notes:** Confirmed on modules + tests. The no-LLM property matters beyond cost: it is also why the scratchpad intake (WF2UNI-9) can triage a line without spending a model call per jotted note.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (20 line(s))

This atom's scope cites `Session 1`, `R12`, `R2`, `R13`. Those tokens appear in its source plan at:

- `Session 1` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:531` — Session 1's "test on 10 diverse intents" becomes a permanent quality gate:
- `Session 1` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:592` — - **Session 1 — Matching + classification:** intent classifier (tuple + rigor routing); tiered `match_template()` T1-T5 with reason strings, negatives, lighter_path, presets, failure-path degradation; template metadata extensions; routing-fixture eval file in CI (UP-R13.1). *Also: delete dead cha…
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:39` — - **UP-R12** — Cheap heuristic intent classifier for rigor routing, hybrid composition, ranked alternatives → *Architecture: Intent Classifier*
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:74` — 1. **Intent classification** (no-LLM heuristic) — routes rigor and stakes (UP-R12).
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:82` — ### Intent Classifier (UP-R12)
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:109` — - **T3 — intent-shape pre-classification** constraining candidates by category (uses the UP-R12 tuple).
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:119` — **Hybrid composition + ranked alternatives (UP-R12):** if top templates score within 0.15, or the tuple is complex + high-uncertainty, compose 2-3 templates as subworkflows at penalized confidence instead of forcing an arbitrary winner. Plan review always shows ranked alternatives with dimension-…
- `R12` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:536` — 4. The intent classifier's **≥85% routing-accuracy target** on the fixture suite is the deployment bar (UP-R12).
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:29` — - **UP-R2** — Tiered deterministic-first template matcher, matching metadata, negatives, lighter_path, presets, router tie-breaker policy → *Architecture: Tiered Template Matcher* + *Template Structure*
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:49` — - The old surfacing system's operative numbers are `workflows.match_threshold = 0.62` (config, `config/loader.py:1052`) + keyword word-overlap fallback `0.7` (`workflows/surfacing.py`); 0.55 appears only as a comment referencing vector_memory's short-text threshold and was never a skills/workflow…
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:76` — 3. **Tiered template matching** — deterministic-first, embedding as tie-breaker (UP-R2).
- `R2` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:103` — ### Tiered Template Matcher (UP-R2)
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:40` — - **UP-R13** — Planner eval harness (routing fixtures in CI + grounding A/B + per-template eval specs) → *Planner Eval Harness*
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:529` — ## Planner Eval Harness (UP-R13)
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:592` — - **Session 1 — Matching + classification:** intent classifier (tuple + rigor routing); tiered `match_template()` T1-T5 with reason strings, negatives, lighter_path, presets, failure-path degradation; template metadata extensions; routing-fixture eval file in CI (UP-R13.1). *Also: delete dead cha…
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:593` — - **Session 2 — Grounded generation:** grounding bundle regeneration from live registries (node taxonomy, action providers, MCP tools, capability matrix); pattern-shape registry + slot-fill; schema-constrained `oneOf` emission; repair-not-regenerate; self-check; grounding A/B harness (UP-R13.2); …
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:597` — - **Session 6 — Grill + entry surfaces + template pipeline:** structured `rigor: deep` protocol (question rounds w/ recommended answers, facts-vs-decisions split across memory AND knowledge lookups, stress-test, Step-0 schema, prohibitions, SaveFn persistence, QuestionSlider widget); rigor:fast +…
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:612` — - **Deterministic tiers drift from template metadata.** Mitigation: the CI routing fixtures assert tier-exact mappings with no LLM (UP-R13.1); lint enforces `when_to_use` against step-summaries.
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:732` — - **A/B harness (UP-R13.2)** ships as the acceptance test, scored on first-try-valid: ungrounded
- `R13` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:988` — harness (UP-R13.2) needs the judge LEARNING-FLYWHEEL owns. `revise{step_ref, comment}` as a

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
