<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SH-5` — Adversarial corpus harness (archive/integrity-race/verdict-evasion/invisible-char/degenerate-manifest) against SkillScanner/install_guarded + scanned==installed race invariant + nightly CI job + published methodology doc

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SECURITY-HARDENING](../plans/SECURITY-HARDENING.md) (`SH`, plan status `in_progress`) |
| **Atom table** | [`../atomic/SH.md`](../atomic/SH.md) |
| **Execution position** | #514 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1396 |

## Intent — what this is for

Session 3 — Adversarial gate testing / T3.1, T3.2, T3.3, V3; Design S3; Contracts C3; owner task 4 (approve publishing corpus)

## Done when

each of the five attack classes has >=1 asserting test; a swap-after-scan race fixture proves scanned-bytes==installed-bytes holds; nightly job in full.yml runs the corpus; docs/security/scanner-testing.md lets an outsider reproduce; a deliberate scanner weakness on a branch turns the corpus red

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`SH-7`](SH-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `9e0061023` 2026-08-15 — test(security): SH-5 scanner attack corpus + swap-after-scan race

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:283` — no `harness/corpus/`. There is no `harness/corpus/` because SH-5's corpus never lived there:
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:287` — new class follows SH-5's real convention and `harness/` is untouched.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T3.1`, `T3.2`, `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/SECURITY-HARDENING.md:93` — | T3.1 | Corpus harness: hypothesis strategies + fixed malicious fixtures for the five attack classes (archive, integrity-race, verdict-evasion, invisible-char, degenerate-manifest) against `SkillScanner`/`install_guarded` | `tests/security/test_scanner_adversarial.py`, `tests/security/corpus/` |…
- `T3.2` `docs/roadmap/plans/SECURITY-HARDENING.md:94` — | T3.2 | Concurrency/integrity: a test forcing a swap-after-scan attempt proves the scanned-bytes==installed-bytes invariant holds under a race | scanner install path test | race fixture cannot land unscanned bytes |
- `T3.3` `docs/roadmap/plans/SECURITY-HARDENING.md:95` — | T3.3 | Nightly CI job + published methodology (`docs/security/scanner-testing.md`, corpus described, how to run) | `.github/workflows/full.yml`, doc | nightly runs the corpus; doc lets an outsider reproduce |
- `Session 3` `docs/roadmap/plans/SECURITY-HARDENING.md:89` — ### Session 3 — Adversarial gate testing
- `Session 3` `docs/roadmap/plans/SECURITY-HARDENING.md:129` — **Lands in:** extends **Session 3** (adversarial gate testing — the baseline-integrity fixtures are a sixth corpus class: `baseline-tamper`) plus one small task in Session 4's surface work. Count stays **4 sessions** (S3 grows by ~half a session, absorbed; recorded honestly here).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
