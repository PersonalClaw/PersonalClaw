<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-9` — Self-QA Companion core: commit-watch cron script, self-qa bundled template, self_qa config four-point wiring

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #552 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | OWNER RULING 2026-08-26: add --isolated (or a per-session --user-data-dir) to the chrome-devtools MCP server args. RULED YES — a shared ~/.cache/chrome-devtools-mcp/chrome-profile makes the tool unusable whenever any sibling session holds it, and several agents drive this repo concurrently by design, so per-session isolation is the only configuration consistent with how the project is developed. Clauses 1/2/4 are met and railed. Clause 3 (a scenario mutating state through the real UI via Chrome DevTools MCP) becomes runnable once that arg is in place — it is a validation run, not a code change. No owner input remains. |

## Intent — what this is for

§3.1 (interim commit-watch cron script + state file), §3.2 steps 1-5 (triage/scenario-gen/execute/evidence/file-findings nodes), §5 (self_qa config through all four wiring points)

## Done when

a real commit to the watched repo fires the companion within one cron interval; a test-only commit yields a ledger-only skip with a one-line rationale; a user-impacting commit generates a scenario that mutates state through the real UI via Chrome DevTools MCP; a failing scenario files one Inbox item + one Task (Success Criterion #6)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SV-3`](SV-3.md) | intra-repo | `done` |

### Unblocks (2)

[`SV-10`](SV-10.md) (`done`), [`SV-11`](SV-11.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 47 (SV) — the harness was RUN and falsified against itself

**Code evidence:**

- MEASURED: the config four points are wired (SelfQaConfig at loader.py:389 with its own docstring, PATCH allowlist entries at handlers/core.py:842-843), the action providers ship (action_providers/selfqa_triage_provider.py), and the trigger screen registers both selfqa-triage and selfqa-file-finding
- 🔑 THE PROVIDER IS DELIBERATELY REGISTERED UNGATED, and the reason is the run-start preflight (action_providers/registry.py:126-138): 'a provider … gated on agent.self_qa.enabled is one the run-start preflight CANNOT SEE'. A capability that appears only when a feature flag is on cannot be validated before the run starts
- the file-finding action opens 'a LOCAL pclaw/selfqa-<sha8> branch (NEVER PUSHED)', which is the never-merged constraint the sibling atom names, enforced at the action rather than by convention
- python -m harness validate 15/15 specs; harness replay 4 scenarios within baseline; harness scan 2 advisory warnings

**Driven in the UI:** Not driven: the clause's four behaviours all need a real commit to a watched repo plus a bound model plus Chrome DevTools MCP — a test-only commit yielding a ledger-only skip, a user-impacting commit generating a scenario that mutates state through the real UI, and a failing scenario filing one Inbox item.

**Notes:** The ungated-registration reasoning generalises: a provider registry that hides disabled capabilities makes a preflight that enumerates capabilities incomplete, so the flag has to gate the RUN rather than the REGISTRATION. That is the same distinction as membership-versus-effect in the feedback plan's suppression set.

## Recorded history

### Commits (9)

- `ffa491692` 2026-09-04 — docs(roadmap): flip SV-11, ES-14, MI-5 to done — SELF-VERIFICATION and MULTIMODAL-IO complete
- `aa9a2cb81` 2026-09-04 — feat(selfqa): SV-11 — the commit watch rebinds to the vcs trigger; the interim cron script retires
- `995e2ef80` 2026-09-02 — docs(roadmap): flip already-built Wave-GA atoms to done + safe reconciliations
- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `084a42175` 2026-08-25 — docs(roadmap): give SV-9 and ES-5 machine-readable blocked_reasons
- `3189a06f6` 2026-08-25 — test(selfqa): SV-9 rail the Self-QA panel rows to the nested config patch path
- `712a4233f` 2026-08-25 — docs(roadmap): record that the stage-node seam clears SV-9's clause-3 blocker
- `c856b06f3` 2026-08-24 — fix(workflows): a boolean branch selector could match neither of its own cases
- `0442120c6` 2026-08-23 — feat(selfqa): SV-9 Self-QA Companion core — triage, ledger skip records, findings, commit watcher

### Named by atom id in the roadmap prose (33)

- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:287` — - [2026-08-23][SV-9] DONE (clauses 1, 2, 4 met; clause 3 PARTIAL — see the note below): Self-QA Companion core (§3.1, §3.2 steps 1-5, §5). `src/personalclaw/selfqa/` — `triage.py` (deterministic path classifier → `user`/`test`/`none` + a one-line rationale that is never empty), `ledger.py` (`reco…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:288` — - [2026-08-23][SV-9] DEVIATION (inventory ratchets, found by the full gate): five committed inventories reject a new provider/template/config field until they are updated, and the first pass of this atom updated none of them. All five are now correct, and three further reds were purely downstream…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:289` — - [2026-08-23][SV-9] DISCOVERY (real vulnerability, FIXED here): the triage path had an **option-injection** hole. `triage_commit` passed its `sha` straight into `git -C <repo> show --name-only --pretty=format: <sha>`, and that value is model-reachable — the template binds `{{inputs.commits}}`, a…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:290` — - [2026-08-23][SV-9] DISCOVERY (pre-existing, NOT fixed — out of scope): `tests/test_guardrails_ladder.py` has intra-file state leakage that xdist's distribution normally masks. Run whole-file single-process (`-n0`), `test_a_provider_that_refuses_leaves_the_rung_ALONE` (`assert 200 == 400`) and `…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:291` — - [2026-08-23][SV-9] DEVIATION: §3.2 step 1 specifies triage as an **`infer` node (cheap tier)**; it ships as a deterministic path classifier behind an `action` node. Two reasons, both load-bearing. Cost: a path classifier is zero tokens, so the watcher can run on a 5-minute interval without a bu…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:292` — - [2026-08-23][SV-9] DEVIATION: §5 opens "**No new provider TYPE and no new action provider**", and this lands two action providers. §5's own next sentence states the rule for the case it contemplates ("if a later revision did add an action provider, it MUST be added to `ALLOWED_HOOK_PROVIDERS` o…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:293` — - [2026-08-23][SV-9] DEVIATION: §3.1 describes the watcher raising `Report` "which the job's `run-workflow` action turns into a QA workflow run with `{{inputs.commits}}`". No such hand-off exists — a script's `Report` payload does not flow into a second action's inputs. The script therefore start…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:294` — - [2026-08-23][SV-9] PARTIAL — clause 3 (`a user-impacting commit generates a scenario that mutates state through the real UI via Chrome DevTools MCP`) is **not observed end to end, and nothing in this change pretends otherwise**. Measured: the `chrome-devtools` MCP server is configured and reach…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:295` — - [2026-08-23][SV-9] DISCOVERY (citation defect, corrected in code comments — no behavior change): the SV-9 atom's `done_when` attributes "a failing scenario files one Inbox item + one Task" to **Success Criterion #6**, which does not contain that clause. #6 covers the watcher interval, the skip …
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:298` — ## Execution log — `SV-9` (Self-QA Companion core: §3.1 commit-watch, §3.2 steps 1-5, §5 config) — **PARTIAL**, atom stays `todo`
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:300` — - [2026-08-23][SV-9] **PARTIAL.** Clauses 1, 2 and 4 are MET and railed; clause 3 (a scenario mutating
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:307` — - [2026-08-23][SV-9] 🔴 **A REAL OPTION-INJECTION HOLE, proved by writing a file to an arbitrary path.**
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:318` — - [2026-08-23][SV-9] 🔴 **A REAL "exactly one" hole in the filing path.** `file_finding` posted the Inbox
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:329` — - [2026-08-23][SV-9] **Clause 3 is gated by session tool policy, not by a missing server.**
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:340` — - [2026-08-23][SV-9] **The atom mis-cites Success Criterion #6, and drops half of it.** SC #6 contains no
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:348` — - [2026-08-23][SV-9] **Five committed inventory ratchets were never updated by the first pass**, leaving
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:355` — - [2026-08-23][SV-9] **Three tests were asserting less than they claimed, and one fixture leaked.**
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:369` — The SV-9 entry above was right that nothing rendered a `step_skipped` row's `sha`/`impact`/`rationale` — but
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:406` — here (it is neither a surfacing defect nor SV-9's), but it means the "a repeatedly SKIPPED step is a failure
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:409` — - [2026-08-24][SV-9 clause 3] 🔴 **A REAL DEAD BRANCH, found by actually driving the template — and
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:441` — scope, not SV-9's. **Clause 3 cannot close until it lands.**
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:459` — - **[2026-08-25][`SV-9`] THE WF2 ENGINE SEAM IS FIXED — this atom's clause-3 blocker is cleared.** The
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:462` — a WF2 seam rather than SV-9's own work. That seam now has a consumer.
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:503` — **`SV-9` stays `todo`** — this clears its clause-3 blocker, it does not implement SV-9.
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:511` — - [2026-08-25][SV-9 clause 3] **Clause 3 stays PARTIAL — but the gate MOVED, and the new one is named.**
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:543` — - [2026-08-25][SV-9 clause 3] **The backend half of the drive WAS proved live, and is labelled as exactly
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:555` — - [2026-08-25][SV-9 clause 3] 🔴 **Railed instead: the quiet-revert the drive exists to catch was UNRAILED
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:556` — on SV-9's own FE surface.** `AgentDefaultsPanel.tsx:76-79` warns in a comment that reusing the flat
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:586` — **SV-9 stays `todo`.** Clauses 1/2/4 remain MET; clause 3 remains PARTIAL, now blocked on harness config.
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:588` — - [2026-08-26][SV-9] **OWNER RULING — add `--isolated` (or a per-session `--user-data-dir`) to the
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:599` — once the flag is in place. `SV-9` stays `todo` until that run is recorded; no owner input remains.
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:608` — ## Execution log — SV-9 (Self-QA Companion core: commit-watch cron, self-qa bundled template, config wiring)
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:610` — - [2026-09-02][SV-9] LANDED on main; atom flipped ✅. The companion ships whole — `selfqa/scripts/selfqa_commit_watch.py` fires within one cron interval on a real commit to the watched repo; `selfqa/triage.py` yields a ledger-only skip with a one-line rationale for a test-only commit; the `self-qa…

### Its plan's own record, via the scope references (18 line(s))

This atom's scope cites `§3.1`, `§3.2`, `§5`. Those tokens appear in its source plan at:

- `§3.1` `docs/roadmap/plans/SELF-VERIFICATION.md:212` — | `vcs` trigger preset (file kind, content-hash dedup, delta payload) | AUTOMATION-SUBSTRATE **AUTO-R12** (Wave 3) | §3.1 ships an interim cron-script watcher; **retire it** when AUTO-R12 lands (explicit retirement item) |
- `§3.1` `docs/roadmap/plans/SELF-VERIFICATION.md:217` — **Dependency edges:** §1 depends on nothing. §2 depends on nothing to build, but its `workflow-journal-projection` scenario is recorded against WF2 Slices 1-2 output as those land (co-scheduled, Wave 1). §3 depends on WF2 Slices 0-5; §3.3's Proof rendering depends on WORK-CONTAINERS (graceful wit…
- `§3.1` `docs/roadmap/plans/SELF-VERIFICATION.md:229` — - **Session 5 — Self-QA companion core (§3.1, §3.2)** *(Wave 2)*: commit-watch cron script + state file; the `self-qa` bundled template (triage prompt with skip-rationale contract, scenario-gen prompt encoding the deep-as-a-user method, execute stage with DevTools MCP binding, findings→Inbox/Task…
- `§3.1` `docs/roadmap/plans/SELF-VERIFICATION.md:287` — - [2026-08-23][SV-9] DONE (clauses 1, 2, 4 met; clause 3 PARTIAL — see the note below): Self-QA Companion core (§3.1, §3.2 steps 1-5, §5). `src/personalclaw/selfqa/` — `triage.py` (deterministic path classifier → `user`/`test`/`none` + a one-line rationale that is never empty), `ledger.py` (`reco…
- `§3.1` `docs/roadmap/plans/SELF-VERIFICATION.md:293` — - [2026-08-23][SV-9] DEVIATION: §3.1 describes the watcher raising `Report` "which the job's `run-workflow` action turns into a QA workflow run with `{{inputs.commits}}`". No such hand-off exists — a script's `Report` payload does not flow into a second action's inputs. The script therefore start…
- `§3.1` `docs/roadmap/plans/SELF-VERIFICATION.md:298` — ## Execution log — `SV-9` (Self-QA Companion core: §3.1 commit-watch, §3.2 steps 1-5, §5 config) — **PARTIAL**, atom stays `todo`
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:208` — | `required_artifacts` proof gates + engine-owned completion | WORKFLOWS-V2 **WF2-R3** | §3.2's evidence gate *uses* it verbatim; no parallel gate mechanism |
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:210` — | Failure capsules (repro + failure_signature + forbidden_success_modes) + capsule replay as lesson decay | LEARNING-FLYWHEEL **LEARN-R8** (§3.3d) | §3.2 step 5 is a capsule *producer*; lifecycle/replay stays in the flywheel |
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:213` — | Two-weight run records / skip-rationale ledger rows; triage-first convention | AUTOMATION-SUBSTRATE §1.3 / WF2-R15 + UNIVERSAL-PLANNING impact-triage | §3.2 step 1 consumes the conventions |
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:229` — - **Session 5 — Self-QA companion core (§3.1, §3.2)** *(Wave 2)*: commit-watch cron script + state file; the `self-qa` bundled template (triage prompt with skip-rationale contract, scenario-gen prompt encoding the deep-as-a-user method, execute stage with DevTools MCP binding, findings→Inbox/Task…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:230` — - **Session 6 — evidence + fix branch + validation (§3.3, §3.2 step 6)** *(Wave 2)*: ffmpeg capture + contact sheet + GIF + SHA256 manifest + Artifact registration + `required_artifacts` gate; optional fix-branch stage on the worktree machinery; end-to-end as-a-user validation — real commits, rea…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:287` — - [2026-08-23][SV-9] DONE (clauses 1, 2, 4 met; clause 3 PARTIAL — see the note below): Self-QA Companion core (§3.1, §3.2 steps 1-5, §5). `src/personalclaw/selfqa/` — `triage.py` (deterministic path classifier → `user`/`test`/`none` + a one-line rationale that is never empty), `ledger.py` (`reco…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:291` — - [2026-08-23][SV-9] DEVIATION: §3.2 step 1 specifies triage as an **`infer` node (cheap tier)**; it ships as a deterministic path classifier behind an `action` node. Two reasons, both load-bearing. Cost: a path classifier is zero tokens, so the watcher can run on a 5-minute interval without a bu…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:298` — ## Execution log — `SV-9` (Self-QA Companion core: §3.1 commit-watch, §3.2 steps 1-5, §5 config) — **PARTIAL**, atom stays `todo`
- `§5` `docs/roadmap/plans/SELF-VERIFICATION.md:130` — - **Vitest** (`web/src/harness/replay.test.ts`): feeds chat-stream traces through `coalesceReducers.ts` and run traces through `runFold.ts` (and the future workflow event-fold, which WF2 §5 mandates be pure and unit-locked exactly so this works) — asserting the fold's terminal state matches the t…
- `§5` `docs/roadmap/plans/SELF-VERIFICATION.md:287` — - [2026-08-23][SV-9] DONE (clauses 1, 2, 4 met; clause 3 PARTIAL — see the note below): Self-QA Companion core (§3.1, §3.2 steps 1-5, §5). `src/personalclaw/selfqa/` — `triage.py` (deterministic path classifier → `user`/`test`/`none` + a one-line rationale that is never empty), `ledger.py` (`reco…
- `§5` `docs/roadmap/plans/SELF-VERIFICATION.md:292` — - [2026-08-23][SV-9] DEVIATION: §5 opens "**No new provider TYPE and no new action provider**", and this lands two action providers. §5's own next sentence states the rule for the case it contemplates ("if a later revision did add an action provider, it MUST be added to `ALLOWED_HOOK_PROVIDERS` o…
- `§5` `docs/roadmap/plans/SELF-VERIFICATION.md:298` — ## Execution log — `SV-9` (Self-QA Companion core: §3.1 commit-watch, §3.2 steps 1-5, §5 config) — **PARTIAL**, atom stays `todo`

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
