<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CC-8` — Chat plan-mode entry + mid-task re-planning bound to the existing planning gate

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CHAT-CRAFT](../plans/CHAT-CRAFT.md) (`CC`, plan status `done`) |
| **Atom table** | [`../atomic/CC.md`](../atomic/CC.md) |
| **Execution position** | #112 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment (2026-07-29) (b) Plan Mode; F1.3, F1.4 + VF plan portion

## Done when

a composer affordance opens the EXISTING planning/session.py walkthrough as an editable Markdown artifact (no second state machine), approve/comment route through it, activation is manual-only (quick tasks unaffected), the no-execute guarantee is enforced by the existing plan task-mode gate (test asserts a mutating tool is denied while awaiting approval, not the prompt), and activating mid-turn parks the run awaiting approval without losing the transcript and resumes on approval

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 35 (CC) — DRIVEN in a real browser

**Code evidence:**

- 🔑 dashboard/chat_plan.py:1-13 'There is deliberately NO SECOND STATE MACHINE here.' The walkthrough state is planning.session's own PlanSession/PlanStep with the same submit→edit→comment/approve transitions the loop planning surface drives; this module does only the two things that model leaves to its owner — persisting for a chat owner, and recording the chat-side attachment, which it labels 'turn bookkeeping, NOT plan state'
- 🔑 :17-20 ACTIVATION IS MANUAL BY MECHANISM, NOT BY PROMISE: 'Nothing on the send path calls into this module to CREATE a session; activate is reachable only from the composer affordance's endpoint. The turn-end hook (maybe_submit_plan_draft) is a NO-OP for a chat with no session, so a quick task is untouched — NO HEURISTIC, NO AUTO-DETECTION'
- 🔑 :22-24 THE SIXTH INDEPENDENT ARRIVAL AT THIS CAMPAIGN'S CORE PRINCIPLE: 'The no-execute guarantee is THE TASK-MODE GATE, NOT A PROMPT.' Activation flips the session to task mode `plan`, and the canonical gate in personalclaw.task_modes 'which the native runtime consults in _guard_and_invoke BEFORE approval' is what denies a write
- MEASURED: :53 imports PlanSession, PlanStep, StepStatus from personalclaw.planning.session — the reuse is an import, not a reimplementation
- tests/test_chat_plan_mode.py passes inside the 77
- 300 frontend chat tests pass; 77 python tests across the rewind/followups/branch/plan-mode/SEL suites

**Driven in the UI:** The composer exposes 'Task mode: Agent' as the mode control; I could not hold its popover open across separate driver invocations to read the Plan entry, so the affordance is confirmed in code rather than clicked.

**Notes:** The no-execute sentence belongs beside the five others this campaign has collected — a prompt is not an enforcement mechanism (twice), making a lint advisory leaves it to author discipline, a bound a prompt asks for is one injected content can ask to skip, a caller that could name itself the user makes the gate decorative, and the hidden button is a convenience while the check is the control. Six modules, six independent arrivals, no shared citation. And the gate's POSITION matters as much as its existence: consulted before approval, so a plan-mode session cannot be talked into a write by an approval prompt.

## Recorded history

### Commits (3)

- `b9409e6c0` 2026-09-07 — docs(roadmap): WF2UNI-14 named the wrong blocker, and its scope would falsify a done atom
- `2c30f04b4` 2026-08-19 — docs(chat): CC-6 the chat-surface guide covers all nine mechanics (#1754)
- `c578f639d` 2026-08-19 — feat(chat): CC-8 chat plan-mode entry bound to the existing planning gate (#1740)

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:21` — F1.1/F1.2 (`CC-7`) and the chat entry to the planning walkthrough F1.3/F1.4 (`CC-8`) — all shipped.
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:662` — - **2026-08-19 — `CC-8` DONE (amendment (b), F1.3 + F1.4).** Chat plan-mode entry bound to the
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:727` — `feature-cc6-nine-mechanics`, one commit. This entry closes the gap the `CC-8` DISCOVERY above
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:872` — ### 2026-08-19 — `CC-8` driven as a user, and held at `todo` — **PARTIAL**
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:912` — execution follow the edited plan, activate mid-task) is **recorded unmet**, and `CC-8` stays `todo`
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:916` — ### 2026-08-19 — `CC-8` CLOSED (`todo` → `done`): the `VF` row is driven, and the drive found two defects
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:978` — "the seven things" while `CC-7` (Branch) and `CC-8` (chat plan-mode) had shipped, so it described
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:1020` — `CC-8` have all shipped since. The atomic-table rail added on 2026-08-19 couples `dag.json` to the

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
