<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-3` — Sidebar windowing — premise corrected + deferred pending measurement

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #534 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 (T1.3)

## Done when

Re-scoped: there is no chat sidebar (ChatPage documents 'No left sidebar'); the session list lives in ChatHistoryPage which already had a search box. Virtualization deferred as speculative until a real jank profile exists on a large fixture; no windowing code shipped. Recorded in the 2026-07-28 Execution log.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SM-2`](SM-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:718` — ### 2026-08-21 — DSC-13 DONE (the shared windowing primitive, adopted at five long-list surfaces — and SM-3's deferred measurement finally taken)
- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:720` — - **The measurement SM-3 asked for, taken first.** SM-3 deferred windowing "as speculative until a real jank profile exists on a large fixture". That profile now exists. Real gateway on **port 10777**, home **`/private/tmp/dsc13-wt/.dev-home`** (both asserted from its `PERSONALCLAW_READY` line be…

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:18` — INBOX-NOTIFICATIONS-UNIFICATION S1-S5 on 2026-07-30, verified live before building. T1.3's sidebar
- `T1.3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:78` — | T1.3 | Sidebar windowing for large lists (verify ListScaffold; add virtualization if absent) | sidebar component | 500-session fixture scrolls smoothly (no jank; measure) |
- `T1.3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:119` — ### 2026-07-28 — Session 1 (T1.1, T1.2 + V1): DONE. T1.3 re-scoped — see below.
- `T1.3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:149` — **T1.3 (sidebar windowing) — RE-SCOPED, and the premise was wrong.** The task says
- `T1.3` `docs/roadmap/plans/SESSION-MANAGEMENT.md:354` — **Chip placement.** `web/src/pages/chat/OrganizeChip.tsx`, above the composer beside the existing `RoutingChip`, using the shipped `Button`/`IconButton` primitives with an accessible name on dismiss. There is no `web/src/pages/sessions/` and no chat sidebar (T1.3's re-scope), so `ChatPage.tsx` is…
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:12` — **Status:** DONE — Session 1 (FTS5 cross-session search + match snippets) shipped
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:72` — ### Session 1 — Cross-session search + scalable sidebar
- `Session 1` `docs/roadmap/plans/SESSION-MANAGEMENT.md:119` — ### 2026-07-28 — Session 1 (T1.1, T1.2 + V1): DONE. T1.3 re-scoped — see below.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
