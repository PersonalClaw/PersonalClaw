<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PUBL-4` — Ship real dashboard screenshots plus SHOWCASE and reproducible CAPTURE doc

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PUBLICATION](../plans/PUBLICATION.md) (`PUBL`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PUBL.md`](../atomic/PUBL.md) |
| **Execution position** | #500 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 §6; Status line

## Done when

real captures ship as docs/screenshots/{light,dark}/* + SHOWCASE.md + reproducible docs/screenshots/CAPTURE.md (superseding the docs/assets/screenshot-dashboard.png placeholder); README screenshot references resolve

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 56 (PUBL) — almost every clause re-read from the live GitHub and PyPI APIs rather than from the tree

**Code evidence:**

- 🔑 I SWEPT EVERY SCREENSHOT REFERENCE RATHER THAN SPOT-CHECKING: 31 references across `README.md` and `SHOWCASE.md`, and all 31 resolve to files on disk. Both `docs/screenshots/light/` and `docs/screenshots/dark/` carry the same numbered set
- `SHOWCASE.md` and `docs/screenshots/CAPTURE.md` both ship, and the placeholder the atom supersedes is gone
- 🔑 CAPTURE IS REPRODUCIBLE BY A SCRIPT, NOT BY A RECIPE: `docs/screenshots/capture.mjs` sits beside the document. A prose capture procedure drifts from the product on the first redesign; an executable one is re-runnable, which is what makes the light/dark pairs consistent
- live API: both repos public with homepage set; org holds the 3 named repos; tags v0.1.0-v0.1.3 with 4 GitHub Releases (2 assets each); PyPI core 0.1.0-0.1.3; 31 of 31 screenshot references resolve

**Driven in the UI:** Not driven: the deliverables are committed images and two documents. The reference sweep is the check that matters, since a broken image path is the failure a reader actually meets.

**Notes:** 🪤 MY OWN ERROR, AND THE FIFTH IN THIS FAMILY THIS CYCLE-SET — this time a SYNTAX assumption rather than a path one. I looked for markdown image syntax and for a path pattern, found neither in the README, and briefly had the references as missing. They are HTML `<img src=...>` tags, which is the form you need for width control. Withdrawn, then replaced with the sweep above.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `§6`, `Session 1`. Those tokens appear in its source plan at:

- `Session 1` `docs/roadmap/plans/PUBLICATION.md:55` — ## Session 1 — Release (manual, ~1 session)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
