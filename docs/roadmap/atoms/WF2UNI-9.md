<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2UNI-9` — Watched-scratchpad intake (Success Criterion 9): periodic scan proposes plans into the needs-input inbox, never auto-executed, with source backlink + dedup

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-UNIVERSAL-PLANNING](../plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md) (`WF2UNI`, plan status `in_progress`) |
| **Atom table** | [`../atomic/WF2UNI.md`](../atomic/WF2UNI.md) |
| **Execution position** | #643 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Planner Entry Surfaces (UP-R18); Provider & Config Integration (watched scratchpad scan); Success Criteria 9

## Done when

a file-watch/interval automation trigger scans the configured planning.scratchpad_path, runs each actionable line through the triage gate + intent classifier, and lands a PROPOSED plan via InboxService with a backlink to the source line; dedup by content-hash + seen-line tracking; checked/struck lines ignored; planning.scratchpad_path wired through the 5-point config contract.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2UNI-1`](WF2UNI-1.md) | intra-repo | `done` |
| `EXT:AUTOMATION-SUBSTRATE:file-watch/interval trigger + run-workflow action` | cross-plan (EXT) | **unresolved** |
| `EXT:INBOX-NOTIFICATIONS-UNIFICATION:InboxService/emit_attention_item proposal surface` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `431bb63b4` 2026-08-11 — feat(planning): WF2UNI-9 watched-scratchpad intake (#1047)

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:1065` — - [2026-08-11][WF2UNI-9 / Success Criterion 9] DONE: a plan a user jots into a notes file now becomes reviewable work. `planning/scratchpad.py` parses the configured scratchpad, triages each line, and raises an inbox PROPOSAL with a backlink to the source line. It rides the EXISTING `_file_watch_…

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `R18`. Those tokens appear in its source plan at:

- `R18` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:45` — - **UP-R18** — Watched scratchpad intake → *Planner Entry Surfaces*
- `R18` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:379` — 3. **Watched scratchpad (UP-R18)** — a watched local "daily page"/scratchpad file (or designated inbox note) that a periodic scan converts from unstructured text (unchecked todos, jotted intents) into candidate planning inputs. Each detected actionable line runs through the triage gate + the inte…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
