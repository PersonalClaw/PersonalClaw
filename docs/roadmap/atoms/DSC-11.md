<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-11` — Deferred tail (environment-blocked): authenticated seeded per-route axe CI gate (T3.2/T3.4) + V3 full-app walkthrough

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #212 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1899 |

## Intent — what this is for

Session 3 T3.2/T3.4 (axe-per-route CI rail) + V3 validation; Status line 'known, honestly-recorded tail'; cy13/cy18 environment-blocked notes

## Done when

e2e/a11y.spec.ts mounted into ci.yml web/rails as a blocking gate against a seeded authenticated session, no serious/critical on any route; V3 full-app keyboard-only + reduced-motion + phone-viewport walkthrough passes in both themes — requires a seeded, authenticated, per-route CI harness not stood up in-plan.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-2`](DSC-2.md) | intra-repo | `done` |
| [`DSC-3`](DSC-3.md) | intra-repo | `done` |
| `EXT:CI-RELEASE-ENGINEERING:seeded authenticated per-route axe CI harness in ci.yml web/rails` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `12469c657` 2026-08-22 — test(web): drop the five KNOWN_UNANNOUNCED entries the focus-ring work cleared
- `3cb4f360b` 2026-08-22 — chore(roadmap): cite DSC-11 and MRT-3 to the batch PR, and close DESIGN-SYSTEM-CONSISTENCY
- `6e9a21793` 2026-08-22 — test(web): DSC-11 V3 walkthrough — keyboard-only, reduced-motion, phone viewport

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:978` — ### 2026-08-22 — `DSC-11` DONE: the V3 walkthrough, and three detector bugs it took to get right
- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:980` — **DONE.** `DSC-11`'s two clauses are now both met.

### Its plan's own record, via the scope references (24 line(s))

This atom's scope cites `T3.2`, `T3.4`, `Session 3`, `cy13`, `cy18`. Those tokens appear in its source plan at:

- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:17` — cost) — and `SH4.3` explicitly mounts this plan's `T3.2`/`T3.4` axe-per-route a11y rail on it. So
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:18` — T3.2/T3.4/V3 are **DEFERRED with a named owner**, not open-ended.
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:78` — | T3.2 | WCAG AA parity pass: axe over every route → fix serious/criticals; focus-visible + keyboard nav + reduced-motion alternatives everywhere | pages, `ui/` | axe clean on all routes; keyboard-only walkthrough of the app works |
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:630` — - **NEXT:** S3 interaction/responsive parity tail (T3.3) and the remaining `rawInput`/`rawButton` drawdown where a byte-identical idiom exists. Environment-blocked tail stays honestly undone: the **authenticated axe-per-route CI gate** (T3.2/T3.4) and the **V3 full-app walkthrough** need a seeded…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:635` — - **Faithfulness (what moves, what doesn't):** the danger tone renders `text-on-surface-low hover:text-danger` with **no fill** — byte-identical hover treatment to the raw buttons. The **reveal** (opacity-0 → group-hover) rides `className`, unchanged. Two deliberate, tracked deltas, both improvem…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:640` — - **NEXT:** continue the `rawButton` drawdown across the rest of the `RowActionButton` family (per-site faithfulness), and the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough). Chaining per the autonomous-lo…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:650` — - **NEXT:** the standalone danger-remove idiom is now fully converged; remaining `RowActionButton` work is the deferred dense-row and filled/labeled variants (each needs its own density/fill faithfulness decision) plus the neutral-toggle family. Then the S3 T3.3 dark/light + responsive parity tai…
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:17` — cost) — and `SH4.3` explicitly mounts this plan's `T3.2`/`T3.4` axe-per-route a11y rail on it. So
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:18` — T3.2/T3.4/V3 are **DEFERRED with a named owner**, not open-ended.
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:80` — | T3.4 | Ratchet: mount token-lint-strict + axe into plan 33 web/rails CI (block regressions) | `.github/workflows/ci.yml` (coordinate with plan 33) | a new hardcoded value or a11y regression turns CI red |
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:131` — ### 2026-07-22 — S2→S3 cycle 6 (T3.4 ratchet: primitive-adoption CI guard)
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:132` — - **DONE (C1 third rail / T3.4):** added `web/src/design/primitiveAdoption.test.ts` + `primitiveAdoption.baseline.json` — a bespoke-chrome ratchet asserting raw `<button>` (baseline 420), raw `<input>/<textarea>/<select>` (206), and ad-hoc dialogs (0) counts may only DECREASE. A NEW raw element t…
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:160` — - **DONE (C1/T3.4 — 4th rail):** added `countInlineFontWeights()` to the reporter and an `inlineFontWeight` cap (baseline 168) to `primitiveAdoption.test.ts` + baseline.json. The inline `font-variation-settings "wght"` count may only shrink — a new inline weight turns the CI web job red, locking …
- `T3.4` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:630` — - **NEXT:** S3 interaction/responsive parity tail (T3.3) and the remaining `rawInput`/`rawButton` drawdown where a byte-identical idiom exists. Environment-blocked tail stays honestly undone: the **authenticated axe-per-route CI gate** (T3.2/T3.4) and the **V3 full-app walkthrough** need a seeded…
- `Session 3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:73` — ### Session 3 — Interaction + a11y + responsive parity
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:623` — ### 2026-07-24 — cy13 DONE (S3 a11y parity — the ten non-default color schemes reach WCAG AA in light mode, closing the cy15 DEVIATION; a permanent per-scheme contrast guard replaces the default-only axe rail)
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:628` — - **Validation (drove the running dev gateway, not just the source):** bundled Playwright against `127.0.0.1:10000` — forced `.light`, applied each scheme's light color set to `document.documentElement` exactly as `appearance.tsx`'s `applyScheme` does (`root.style.setProperty` per accent var), re…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:669` — - **No ratchet delta (non-ratchet cycle, like cy13):** both blocks already used `<Button>` (no raw `<button>`), and `consistencyAudit.report.ts` tracks chrome/weights/color — **not** any empty-state or interaction-pattern metric — so `primitiveAdoption.baseline.json` stays `{ rawButton: 278, rawI…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:682` — - **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-…
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:676` — ### 2026-07-24 — cy18 DONE (assessment/validation cycle — the autonomous-completable convergence lanes are code-verified DRAINED; no source change, and manufacturing a marginal one would violate the soul guardrail)
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:688` — - **Why this became completable — cy18's Lane 1 and Lane 2 conclusions were right in July and are stale now.** cy18 recorded that an `ErrorState` primitive "would gain exactly **one** faithful adopter — premature abstraction", and that of the cockpit's 21 raw buttons "**Zero** byte-identical clus…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
