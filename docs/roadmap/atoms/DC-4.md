<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DC-4` — S4: Tray/menu-bar presence + login-item + graceful quit

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-CAPABILITIES](../plans/DESKTOP-CAPABILITIES.md) (`DC`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DC.md`](../atomic/DC.md) |
| **Execution position** | #151 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 4 — Presence + platforms, T4.1 (tray/menu companion) + T4.3 (login item + quit); coordinates with AMBIENT-SURFACES tile registry when available

## Done when

tray/menu-bar icon+menu shows pending-approvals count (click-through deep-links into the SPA), running loops, quick-capture note->inbox, open dashboard, quit; counts live-update over the loopback WS/API; login-item toggle (Settings via bridge) survives reboot; graceful gateway shutdown on quit leaves no orphan gateway (process table verified). AMBIENT-SURFACES menu-bar tiles render here only when that plan is available (non-blocking).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DC-2`](DC-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `b7c7ad574` 2026-09-04 — docs(roadmap): flip MC-7/DC-3/DC-4/APE-6 + ES-8 decomposition
- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `5e457f76b` 2026-08-27 — feat(desktop): DC-4 make Settings' "Open at login" a real control on the login-item bridge
- `6ef548568` 2026-08-25 — feat(desktop): DC-4 tray presence + login item + graceful quit
- `1428cfb7e` 2026-08-18 — feat(desktop): DC-4 tray presence, opt-in login item, graceful gateway quit (#1728)

### Named by atom id in the roadmap prose (11)

- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:130` — **2026-09-04 — DC-4 validated done (blocked_reason disproven on main).** The two code gaps the prior `blocked_reason` recorded are gone. (1) The login-item toggle is Settings-reachable: `web/src/pages/settings/SecurityPanel.tsx` renders `LoginItemRow`, and `web/src/lib/desktopBridge.ts` DOES decl…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:168` — - **DECISION (ONE writer for the menu-bar title).** DC-3's `● Listening` indicator and DC-4's approvals badge want the same pixels, and two callers of `tray.setTitle()` would mean whichever fired last wins — a mic indicator silently erased by a count refresh is precisely the "indicator that lies"…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:173` — - **DONE T4.3 (graceful quit)** — The pre-DC-4 path was `kill("SIGTERM"); gatewayProcess = null;` inside a **synchronous** `before-quit`: it never learned whether the child exited, and because the gateway is spawned `detached: false` with no process group of its own, a SIGTERM that landed slowly …
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:175` — - **DISCOVERY (tray availability is a WINDOW-REACHABILITY invariant, not a cosmetic).** `main.js` hides the window on close instead of closing it, and `window-all-closed` keeps macOS alive with no windows — both correct *only while a menu-bar item exists to bring the window back*. A tray that fai…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:177` — - **REMAINING (V4 + the two runtime clauses).** Electron was never launched, so **three claims are test-backed but unobserved at runtime**: that macOS draws the icon and title (only "the menu template we hand `buildFromTemplate` is correct" is proven), that the login item **survives a reboot** (t…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:208` — - **REMAINING (one unobservable leg + a genuinely unrunnable one).** Electron was never launched, so **"macOS draws a banner"** and **"clicking it fires the `click` handler"** are proven only against a recording double — the same class of remainder DC-2/DC-3/DC-4 each carry, and it pairs with DC-…
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:210` — ### Session 6 — `DC-4`'s "Settings via bridge" clause (2026-08-27)
- **source plan** `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:222` — - **REMAINING (unobservable here, and it is the clause's own verb).** The `done_when` says the toggle "**survives reboot**". Nothing in this repo can observe that: it needs a signed `.app` bundle (DC-1), a real macOS login session, and a machine restart. Everything up to the OS boundary is driven…
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:651` — as new scope rather than folded into a closed one. `DC-4` shipped a tray "Quick Capture Note…" row
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:655` — `settings`, so the flag was parsed and dropped. The DC-4 brief assumed the missing reader belonged
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:659` — proposal. Recording the gap in DC-4's reason would have been a backlog keyed on what was found.

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `T4.1`, `T4.3`, `Session 4`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:15` — **Depends on:** CI-RELEASE-ENGINEERING (build/signing in the pipeline), DISTRIBUTION (release artifacts + install-kind detection: the shell sets `PERSONALCLAW_INSTALL_KIND=desktop`). Coordinates with MULTIMODAL-IO (voice capture lands on this bridge; screen-context is its flagship consumer), AMBI…
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:109` — | T4.1 | Tray/menu-bar companion: icon + menu (pending approvals count with click-through, running loops, quick-capture note→inbox, open dashboard, quit); wired to gateway state over the loopback WS/API (verify what `Tray` wiring already exists in main.js — extend, don't duplicate) | `desktop/mai…
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:165` — ### Session 4 — Presence (T4.1 + T4.3, 2026-08-17)
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:167` — - **DECISION (extend the existing tray; move its logic OUT of the glue).** T4.1 says "verify what `Tray` wiring already exists — extend, don't duplicate", and it did exist: DC-2/DC-3 left a static six-item menu built inline in `main.js` plus a `setTitle` capture indicator. So `createTray()` is de…
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:170` — - **DEVIATION (quick capture deep-links; it does not write the note).** T4.1 names "quick-capture note→inbox", and **no endpoint exists that creates an inbox item from text**: every `/api/inbox` POST acts on an item that already exists (`seen`/`send`/`draft`/`restore`/`favorite`/`dismiss-all`), a…
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:172` — - **DONE T4.1** — Live menu: connection header, pending-approvals row (click-through to `#/chat`, where `ApprovalPrompt` renders — and **click-through at zero too**, because a row that disables itself the moment the count clears is a dead end), a running-loops submenu deep-linking `#/loops/<id>` …
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:177` — - **REMAINING (V4 + the two runtime clauses).** Electron was never launched, so **three claims are test-backed but unobserved at runtime**: that macOS draws the icon and title (only "the menu template we hand `buildFromTemplate` is correct" is proven), that the login item **survives a reboot** (t…
- `T4.1` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:215` — - **DECISION 2 (`?capture=1` is NOT in scope — and the earlier ownership citation was wrong).** The `done_when`'s clause is `tray/menu-bar icon+menu **shows** … quick-capture note->inbox`, and T4.1's own verification column is "counts live-update; click-throughs deep-link into the SPA". Measured:…
- `T4.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:111` — | T4.3 | Login item toggle (Settings, via bridge) + graceful gateway shutdown on quit (verify current quit path kills the child cleanly — PPID-reaping interplay) | `desktop/main.js`, settings | login-item survives reboot (manual check); no orphan gateway after quit (process table verified) |
- `T4.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:165` — ### Session 4 — Presence (T4.1 + T4.3, 2026-08-17)
- `T4.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:171` — - **DEVIATION (the login-item toggle ships on the bridge AND in the tray, not in Settings).** T4.3 says "Settings, via bridge". The bridge half is complete (`pclawDesktop.loginItem.get/set` over two new `IPC_CHANNELS`, handled by `registerLoginItemIpc`), but the Settings *control* lives in `web/s…
- `T4.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:173` — - **DONE T4.3 (graceful quit)** — The pre-DC-4 path was `kill("SIGTERM"); gatewayProcess = null;` inside a **synchronous** `before-quit`: it never learned whether the child exited, and because the gateway is spawned `detached: false` with no process group of its own, a SIGTERM that landed slowly …
- `T4.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:174` — - **DONE T4.3 (login item)** — Opt-in (construction performs **zero** writes — asserted), reversible by the same call with `false`, and idempotent: `set()` reads the current registration and skips a matching write, so enabling twice cannot mint a second entry (falsified: removing the guard reds w…
- `T4.3` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:177` — - **REMAINING (V4 + the two runtime clauses).** Electron was never launched, so **three claims are test-backed but unobserved at runtime**: that macOS draws the icon and title (only "the menu template we hand `buildFromTemplate` is correct" is proven), that the login item **survives a reboot** (t…
- `Session 4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:105` — ### Session 4 — Presence + platforms
- `Session 4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:165` — ### Session 4 — Presence (T4.1 + T4.3, 2026-08-17)
- `Session 4` `docs/roadmap/plans/DESKTOP-CAPABILITIES.md:184` — ### Session 4 remainder — the orphan clause, actually measured (2026-08-25)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
