<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-2` — OpenAI-compatible inbound dialect (/v1/*) — agent-as-model + audio aliases

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #215 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2086 |

## Intent — what this is for

§2 (Dialect 1): §2.1 /v1/chat/completions + /v1/models, §2.2 /v1/audio/{speech,transcriptions,voices} aliases, §2.3 headless-profile safety composition; Amendment 2026-07-26 T2-A1/T2-A2. Session 2 (the 'doorway').

## Done when

POST /v1/chat/completions maps model=personalclaw/<agent> AND bare <agent> to an agent via resolve_agent_bindings; SSE chat.completion.chunk translation with [DONE] + usage block, non-stream returns one completion; `user` field + X-PersonalClaw-Session header → inbound:<client_id>:<sha8> session (stateless unless persistent_sessions); inbound: added to _STATELESS_PREFIXES + guardrails headless classification; tool calls execute server-side (never surfaced as tool_calls deltas), needs-approval returns dashboard-pointer terminal message; unknown agent → 404 in OpenAI error shape with stable code; /v1/audio/* thin aliases over /api/voice/synthesize + /api/stt/transcribe via resolve_provider_for_use_case (tts-1/whisper-1 cosmetic, resolve_voice(name) seam); per-client SpendMeter budgets via ModelCallGuard; unmodified `openai` SDK holds multi-turn convo + curl /v1/audio/speech returns bound-TTS audio (Success Criteria 2); zero provider names in dialect code paths.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EA-1`](EA-1.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:headless SafetyProfile + SpendMeter (Budget scope=trigger) + ModelCallGuard` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `1bce47fde` 2026-08-26 — docs(roadmap): EA-2 done (PR #2086); EA-8 stays todo with its cross-repo clause
- `4e41be3ba` 2026-08-26 — docs(reference): regenerate the offline reference for the /v1 and /a2a routes
- `91802ead2` 2026-08-25 — feat(inbound): EA-2 OpenAI-compatible /v1 dialect

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:918` — `session._STATELESS_PREFIXES`.** EA-2's scope names the latter, but that list is the PROVIDER
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:922` — guardrails list answers only the first. **EA-2 should not re-add it to the session list without
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1107` — ## Execution log — `EA-2` (§2 Dialect 1: the OpenAI-compatible `/v1` doorway, Session 2)
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1109` — - [2026-08-25][EA-2] **DONE — `/v1/*` mounted.** New `inbound/openai_dialect.py` registered from
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1116` — - [2026-08-25][EA-2] **Closed:** dual `model` form (`personalclaw/<agent>` + bare) → agent; unknown agent
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1123` — - [2026-08-25][EA-2] **The unknown-agent 404 is the load-bearing line.** `resolve_agent_bindings`
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1128` — - [2026-08-25][EA-2] **DEVIATION — `inbound:` was NOT added to `session._STATELESS_PREFIXES`.** EA-9's
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1136` — - [2026-08-25][EA-2] 🔴 **A purge through a fresh `SessionMap()` would have been silently undone.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1142` — - [2026-08-25][EA-2] **Per-client budgets came free from the key shape, and that is fragile.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1147` — - [2026-08-25][EA-2] 🔴 **mypy caught a real defect: `stitch_wavs` is `async`.** The sentence-chunk
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1152` — - [2026-08-25][EA-2] **DEVIATION — `openai_error` DELEGATES to `http_errors.json_error`; admission reuses
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1161` — - [2026-08-25][EA-2] **DEVIATION — the turn runner is INJECTED, not imported.** `gate_report.py`'s
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1169` — - [2026-08-25][EA-2] **OWNER DECISION SURFACED — `_DYNAMIC_CODE_SITE_CEILING` 16 → 17.** `openai_error`
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1180` — - [2026-08-25][EA-2] **Zero provider names, with a rail that has been watched fail.** The rail greps the
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1189` — - [2026-08-25][EA-2] **Success Criteria 2 VERIFIED with the real client.** `openai` 3.0.0 is already an
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1198` — - [2026-08-25][EA-2] **Gate:** `make lint` exit 0 (black 2073 files, isort, flake8, **mypy 1018 source
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1206` — - [2026-08-25][EA-2] **Not built here (out of the atom's `done_when`):** `GET /v1/models` for a client
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:1213` — - [2026-08-26][EA-2] **Atom flipped `done` (PR #2086).** Landed through a combined batch with EA-8.

### Its plan's own record, via the scope references (29 line(s))

This atom's scope cites `§2`, `§2.1`, `§2.2`, `§2.3`, `Session 2`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:26` — - **NEW-10** (hardened external access: OpenAI-compatible inbound HTTP API where `model` targets an agent + `/v1/audio/speech|transcriptions|voices` aliases; gateway-mounted MCP server exposing a curated read-only capability subset with per-client bindings + kill switch; sender-trust substrate — …
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:42` — - The learning capture path NEW-20 feeds: LEARNING-FLYWHEEL's LearningGate + `capture_hygiene.py` + the R19 staging tier in `learning.db` + the unified proposal queue (that plan's §2.1-2.2, migration steps 1-3). The capture proxy is a **fourth capture cadence** feeding that machinery — it must no…
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:52` — New module `src/personalclaw/inbound/` mounted by the `dashboard/server.py` app factory. All five dialects (§2-§5, §7) register sub-routes on it and inherit the full discipline below; none of them may add a route outside it.
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:73` — - **Query-only with no path to writes** on the outward read surfaces (§3): the MCP server's tool table is a hand-curated allowlist of read-only operations; there is no generic tool passthrough to `_aggregated_call_tool` (which includes write tools) and **an inbound request can never trigger a mig…
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:74` — - Prompts *entering* agent sessions via §2 are the caller's own words on the user's own machine — they are NOT fenced as untrusted (they're the conversation), but the session is marked `origin=inbound` and rides the learning gate + headless profile (§2.3), so an external client can't mint standin…
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:90` — - SSE streaming translated from the internal event stream; non-stream waits and returns one completion. Tool-approval requests arising mid-run are **never** interactively surfaced to the HTTP caller — the run executes under the headless profile (§2.3) and a needs-approval state returns a terminal…
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:114` — | `speak(text, voice?)` | the §2.2 TTS path, returns audio bytes (capped) | the one "action" — side-effect-free generation |
- `§2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:193` — - **Safety composition:** headless runs are unattended by definition — the session key uses the `inbound:cli:` prefix and inherits the SAME headless SafetyProfile as §2.3 (read-only tool defaults; write grants only via an explicit `--allow` flag mirroring the client-record grant model, printed to…
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:42` — - The learning capture path NEW-20 feeds: LEARNING-FLYWHEEL's LearningGate + `capture_hygiene.py` + the R19 staging tier in `learning.db` + the unified proposal queue (that plan's §2.1-2.2, migration steps 1-3). The capture proxy is a **fourth capture cadence** feeding that machinery — it must no…
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:234` — | LEARNING-FLYWHEEL staging/queue/hygiene (its §2.1-2.2) | **EXTEND with a capture source** — NEW-20's suggested home honored; no parallel learning pipeline; replay evidence feeds LEARN-R2's manifests |
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:293` — **Standard-API doorway priority + contract sharpening.** Gap-analysis evidence: the OpenAI-compatible `/v1/chat/completions` endpoint (any off-the-shelf client talks to the assistant) is the highest-leverage single slice of this plan — every OpenAI-SDK tool, editor plugin, and phone client become…
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:295` — ### Contract sharpening (additive to §2.1/§2.3 — no design change)
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:298` — - **Streaming:** SSE `chat.completion.chunk` frames translated from the internal event stream; `[DONE]` sentinel; `usage` block on the final frame (from the ModelCallGuard's token counts) — clients budget off it. Non-stream waits and returns one `chat.completion`. Tool calls execute **server-side…
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:299` — - **Sessions:** stateless per request by default; `user` field → `inbound:<client_id>:<sha8(user)>` continuity (already §2.1); ADD a header escape hatch `X-PersonalClaw-Session: <name>` for clients that can't set `user` (maps to the same key derivation; only honored when the client record sets `p…
- `§2.1` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1144` — run scope and binds `safety_budget_for_inbound()`. Using §2.1's `inbound:<client_id>:<sha8>` therefore
- `§2.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:114` — | `speak(text, voice?)` | the §2.2 TTS path, returns audio bytes (capped) | the one "action" — side-effect-free generation |
- `§2.2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:297` — - **`model` naming:** accept BOTH `personalclaw/<agent>` and bare `<agent>` (clients with model-name dropdowns can't always send slashes); `GET /v1/models` returns `{id: "personalclaw/<agent>", ...}` rows only for agents the client's binding permits. Unknown agent → 404 with §2.2 envelope `{"erro…
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:74` — - Prompts *entering* agent sessions via §2 are the caller's own words on the user's own machine — they are NOT fenced as untrusted (they're the conversation), but the session is marked `origin=inbound` and rides the learning gate + headless profile (§2.3), so an external client can't mint standin…
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:90` — - SSE streaming translated from the internal event stream; non-stream waits and returns one completion. Tool-approval requests arising mid-run are **never** interactively surfaced to the HTTP caller — the run executes under the headless profile (§2.3) and a needs-approval state returns a terminal…
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:193` — - **Safety composition:** headless runs are unattended by definition — the session key uses the `inbound:cli:` prefix and inherits the SAME headless SafetyProfile as §2.3 (read-only tool defaults; write grants only via an explicit `--allow` flag mirroring the client-record grant model, printed to…
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:293` — **Standard-API doorway priority + contract sharpening.** Gap-analysis evidence: the OpenAI-compatible `/v1/chat/completions` endpoint (any off-the-shelf client talks to the assistant) is the highest-leverage single slice of this plan — every OpenAI-SDK tool, editor plugin, and phone client become…
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:295` — ### Contract sharpening (additive to §2.1/§2.3 — no design change)
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:298` — - **Streaming:** SSE `chat.completion.chunk` frames translated from the internal event stream; `[DONE]` sentinel; `usage` block on the final frame (from the ModelCallGuard's token counts) — clients budget off it. Non-stream waits and returns one `chat.completion`. Tool calls execute **server-side…
- `§2.3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:890` — §9.5 says the run "inherits the §2.3 headless SafetyProfile (read-only defaults)". `tool_grants` has
- `Session 2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:250` — - **Session 2 — OpenAI dialect (§2):** chat completions (agent-as-model, SSE, `user`→session mapping, `inbound:` prefix + headless classification), `/v1/models`, the three audio aliases over the existing STT/TTS routes, per-client budgets.
- `Session 2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:293` — **Standard-API doorway priority + contract sharpening.** Gap-analysis evidence: the OpenAI-compatible `/v1/chat/completions` endpoint (any off-the-shelf client talks to the assistant) is the highest-leverage single slice of this plan — every OpenAI-SDK tool, editor plugin, and phone client become…
- `Session 2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:304` — No new session; session count stays ~7. Session 2 gains the three sharpenings above as acceptance criteria. Add to Session 2's Done-when: an unmodified `openai` Python SDK client and one off-the-shelf chat app (e.g. any BYO-base-URL client) each hold a multi-turn conversation via `user` continuit…
- `Session 2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:308` — | T2-A1 | Model-name dual form + OpenAI-shaped error envelope (stable `code` preserved) + usage block on final SSE frame | `inbound/openai_dialect.py` (Session 2 module), tests | `openai` SDK `client.chat.completions.create(model="<agent>", stream=True)` works verbatim; error shapes parse in the …
- `Session 2` `docs/roadmap/plans/EXTERNAL-ACCESS.md:1107` — ## Execution log — `EA-2` (§2 Dialect 1: the OpenAI-compatible `/v1` doorway, Session 2)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
