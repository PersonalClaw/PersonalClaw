<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-11` — Indexing H1.4: sqlite-vec ANN index with cached probe, fail-soft to exact scan, Doctor line

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #312 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1286 |

## Intent — what this is for

Amendment task table — H1.4; Dependency ruling (sqlite-vec, not faiss) + Runtime-availability clause; Risks (silent recall regression)

## Done when

sqlite-vec>=0.1,<1 added to core dependencies with a WHY comment; an ANN index over chunk vectors lives inside the vector arm; a cached enable_load_extension probe fails soft to the existing exact scan with a one-time INFO log; Doctor reports the degraded capability line; a recall-tolerance test asserts ANN-vs-exact match within the stated tolerance and a force-disabled-extension test proves correct results still return via exact scan; faiss and the [embeddings] extra are untouched

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-10`](KL-10.md) | intra-repo | `done` |

### Unblocks (1)

[`KL-13`](KL-13.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `530826e22` 2026-08-14 — feat(knowledge): KL-12 backfill chunks for libraries ingested before chunking
- `2729ec17b` 2026-08-13 — feat(knowledge): KL-11 index chunk vectors with sqlite-vec, fail-soft to the exact scan

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:26` — and superseded by `KL-9`/`KL-10`/`KL-11`/`KL-12`.) Created 2026-07-18 (roadmap rev 10; owner ask: more
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:532` — **DISCOVERY — H1.4 is now required, not an optimization.** The exact scan reads every embedded CHUNK blob plus every item blob, so its row count goes from N to roughly N·(1 + content_chars/1500). Measured on a synthetic 300-item library with 5 chunks each at 384 dimensions: 300 rows → 6.4 ms/quer…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:537` — - [2026-08-13][KL-11] DONE: a `sqlite-vec` `vec0` ANN index over the chunk vectors, inside the vector arm (H1.4). `sqlite-vec>=0.1,<1` added to CORE `dependencies` with the WHY comment the Dependency ruling asks for; new `knowledge/vector_index.py` (`probe()` + `ChunkVectorIndex`); the arm in `re…
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:572` — vectors and KL-11 indexed them, but both only help items that HAVE chunks, and every item
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:598` — through `store.replace_chunks`, which is what keeps KL-11's ANN index in step. The items'
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:654` — recall@20 1.0000 (min 1.0000)**. Consistent with KL-11's own 40.13 → 2.23 ms at its 1,800-row
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:1224` — KL-11 + KL-14; unblocks KL-17. Built as four fenced parallel slices (store/pass · config · endpoint ·

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
