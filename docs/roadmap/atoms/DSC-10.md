<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-10` — S3 interaction-pattern standardization + a11y/dark-light/responsive parity + pattern gallery (T3.1/T3.2/T3.3, C2)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #211 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 T3.1/T3.2/T3.3 (autonomous-completable) + C2 pattern gallery; Execution log cy7, cy12, cy33, dynamic-axe (S2 cy15), cy13, cy17, cy18

## Done when

EmptyState/SlotEmptyState + confirm/loading/selection/error patterns converged and documented in docs/design/patterns.md; dynamic axe serious/critical violations cleared on default scheme; all 11 color schemes reach WCAG AA in both modes with permanent schemeContrast.test.ts guard (67 assertions); settings bento inert-switch bug fixed; phone-width 375px sweep shows 0 horizontal-overflow routes.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-2`](DSC-2.md) | intra-repo | `done` |
| [`DSC-8`](DSC-8.md) | intra-repo | `done` |

### Unblocks (1)

[`DSC-12`](DSC-12.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 10 (DSC) — observed

**Code evidence:**

- the design corpus carries the specific rails this atom implies — accentOnCanvas, countChipContrast, dimmedInkContrast, disabledDimLevel, colorScheme, ariaProhibitedAttr, controlNameFloor — and all pass
- part of the 841-passed design run

**Notes:** PARTIAL: interaction-pattern standardization plus dark/light a11y is half static-checkable and half visual. The contrast and name-floor rails are real and passing; the dark-light comparison itself belongs to the visual harness I did not run. I did drive the theme control ('Theme: dark — switch to light') across many surfaces without a broken render, which is weak corroboration.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (47 line(s))

This atom's scope cites `T3.1`, `T3.2`, `T3.3`, `Session 3`, `cy7`, `cy12`, `cy33`, `cy15`, `cy13`, `cy17`, `cy18`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:77` — | T3.1 | Standardize interaction patterns (selection/empty/loading/error/confirm) across pages via shared primitives; document each in `patterns.md` | pages, `docs/design/patterns.md` | each pattern has one implementation + one doc entry; pages conform |
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:137` — ### 2026-07-22 — S3 cycle 7 (T3.1: empty-state pattern clarity — rename + document)
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:139` — - **DONE (T3.1, zero-pixel refactor):** renamed the slot variant `EmptyState` → `SlotEmptyState` in `kit.tsx` and its 5 importers (Suggestions, TasksWidget, ScheduleWidget, ActiveWork, ActionCenter). Same component, same JSX output → zero visual change; resolves the name collision. Documented BOT…
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:165` — ### 2026-07-22 — S3 cycle 12 (T3.1/C2: document the remaining interaction patterns)
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:195` — - **DONE (T3.1 — empty-state consolidation):** folded the two remaining ad-hoc empty states (`ChatActivityPanel`, `NotificationsPage`) into the canonical `EmptyState` pattern.
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:664` — ### 2026-07-24 — cy17 DONE (S3 T3.1 interaction-pattern standardization — the last two hand-rolled page-empties converge onto the shared `EmptyState` primitive; owner-authorized under the full-authority grant)
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:665` — - **Why in-scope now:** rotates from the S2 raw-input/raw-button lanes (both drained of *byte-identical* clusters — cy16's Why + cy15's NEXT established the survivors all move pixels or need primitive growth first) to the **S3 T3.1** interaction-pattern lane, which is explicitly "standardize sele…
- `T3.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:667` — - **Faithfulness (what moves, what doesn't):** LoopsListPage = **pixel-identical** (the only pre-existing delta, hint `max-w-[400px]` vs the primitive's `[420px]`, is a 20px-wider wrap — imperceptible, and the *correct* shared measure). CodeSection = the **T3.1-intended outlier normalization**: t…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:17` — cost) — and `SH4.3` explicitly mounts this plan's `T3.2`/`T3.4` axe-per-route a11y rail on it. So
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:18` — T3.2/T3.4/V3 are **DEFERRED with a named owner**, not open-ended.
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:78` — | T3.2 | WCAG AA parity pass: axe over every route → fix serious/criticals; focus-visible + keyboard nav + reduced-motion alternatives everywhere | pages, `ui/` | axe clean on all routes; keyboard-only walkthrough of the app works |
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:630` — - **NEXT:** S3 interaction/responsive parity tail (T3.3) and the remaining `rawInput`/`rawButton` drawdown where a byte-identical idiom exists. Environment-blocked tail stays honestly undone: the **authenticated axe-per-route CI gate** (T3.2/T3.4) and the **V3 full-app walkthrough** need a seeded…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:635` — - **Faithfulness (what moves, what doesn't):** the danger tone renders `text-on-surface-low hover:text-danger` with **no fill** — byte-identical hover treatment to the raw buttons. The **reveal** (opacity-0 → group-hover) rides `className`, unchanged. Two deliberate, tracked deltas, both improvem…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:640` — - **NEXT:** continue the `rawButton` drawdown across the rest of the `RowActionButton` family (per-site faithfulness), and the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough). Chaining per the autonomous-lo…
- `T3.2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:650` — - **NEXT:** the standalone danger-remove idiom is now fully converged; remaining `RowActionButton` work is the deferred dense-row and filled/labeled variants (each needs its own density/fill faithfulness decision) plus the neutral-toggle family. Then the S3 T3.3 dark/light + responsive parity tai…
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:79` — | T3.3 | Dark/light + responsive parity: every token used in both themes (no theme-only hardcodes); no horizontal body scroll at phone widths | pages, tokens | theme toggle on every page shows no broken surface; phone-width pass clean |
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:630` — - **NEXT:** S3 interaction/responsive parity tail (T3.3) and the remaining `rawInput`/`rawButton` drawdown where a byte-identical idiom exists. Environment-blocked tail stays honestly undone: the **authenticated axe-per-route CI gate** (T3.2/T3.4) and the **V3 full-app walkthrough** need a seeded…
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:640` — - **NEXT:** continue the `rawButton` drawdown across the rest of the `RowActionButton` family (per-site faithfulness), and the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough). Chaining per the autonomous-lo…
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:650` — - **NEXT:** the standalone danger-remove idiom is now fully converged; remaining `RowActionButton` work is the deferred dense-row and filled/labeled variants (each needs its own density/fill faithfulness decision) plus the neutral-toggle family. Then the S3 T3.3 dark/light + responsive parity tai…
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:662` — - **NEXT:** the dense-textarea cluster is converged and `TextArea` now carries a tested `size` axis. Remaining S2 raw-input drawdown is (a) the `resize`/`onKeyDown`-bearing inline composers (need those props grown on `TextArea` in a lockstep step), and (b) any remaining faithful `Select`/`<input>…
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:674` — - **NEXT:** the empty-state interaction pattern (T3.1) is fully converged + locked; only the deliberately-distinct load-error/not-found/filtered/slot variants remain (correctly NOT `EmptyState`). A future *error-state* primitive (patterns.md's open `[ ] Error state` item) could later fold the loa…
- `T3.3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:682` — - **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-…
- `Session 3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:73` — ### Session 3 — Interaction + a11y + responsive parity
- `cy7` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:552` — ### 2026-07-24 — cy7 DONE (G1 full `RowActionButton`/remove-X family — the 5-site multi-variant sweep onto `SquareIconButton` — batch cycle 7/9)
- `cy7` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy12` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:611` — ### 2026-07-24 — cy12 DONE (S2 raw-input migration wave 3 — the model-provider settings text field converges onto `TextInput`)
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:623` — ### 2026-07-24 — cy13 DONE (S3 a11y parity — the ten non-default color schemes reach WCAG AA in light mode, closing the cy15 DEVIATION; a permanent per-scheme contrast guard replaces the default-only axe rail)
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:624` — - **Why this, why now (conformance, not taste — so it's in-scope autonomously):** cy15 recorded a standing DEVIATION — the coral **default** scheme was AA-corrected and is enforced live by `e2e/a11y.spec.ts` (axe), but **axe only ever exercises the active/default scheme**, so the other ten curate…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:625` — - **Measurement (WCAG 2.1 relative-luminance, 4.5:1 small-text):** measured all 11 schemes in the two contexts each light accent actually bears text/UI — `primary` as a filled control (onPrimary over primary) **and** as accent text on the white surface, `primaryEmphasis` as the hover fill, `info`…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:627` — - **Structural close of the cy15 hole — new permanent guard `web/src/design/schemeContrast.test.ts`:** iterates **every** `SCHEMES` entry (not just the default) and asserts each one's `primary`/`primaryEmphasis`/`info` meets ≥4.5:1 in both modes — light: filled-control, accent-text-on-white, emph…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:642` — ### 2026-07-24 — cy15 DONE (S2 raw-button wave, cross-file — the standalone inline danger-remove `RowActionButton` idiom converges onto `SquareIconButton tone="danger"` across four files; owner-authorized under the full-authority grant)
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:643` — - **Why in-scope now:** direct continuation of cy14's NEXT. cy14 converged the tightest sub-cluster (the 3 reveal-X buttons in one file); cy15 extends the same `SquareIconButton tone="danger"` convergence to the **standalone inline danger-remove idiom** wherever it appears raw across the app — th…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:653` — - **Why in-scope now:** rotates from the (now-converged) raw-button idiom back to the S2 raw-**input** drawdown flagged in cy15's NEXT. Earlier cycles (3/5/14) established there is **no byte-identical raw-`<input>` cluster** left (the survivors hardcode `flex-1`/`px-2`/`text-on-surface-var`/custo…
- `cy15` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:662` — - **NEXT:** the dense-textarea cluster is converged and `TextArea` now carries a tested `size` axis. Remaining S2 raw-input drawdown is (a) the `resize`/`onKeyDown`-bearing inline composers (need those props grown on `TextArea` in a lockstep step), and (b) any remaining faithful `Select`/`<input>…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:623` — ### 2026-07-24 — cy13 DONE (S3 a11y parity — the ten non-default color schemes reach WCAG AA in light mode, closing the cy15 DEVIATION; a permanent per-scheme contrast guard replaces the default-only axe rail)
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:628` — - **Validation (drove the running dev gateway, not just the source):** bundled Playwright against `127.0.0.1:10000` — forced `.light`, applied each scheme's light color set to `document.documentElement` exactly as `appearance.tsx`'s `applyScheme` does (`root.style.setProperty` per accent var), re…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:669` — - **No ratchet delta (non-ratchet cycle, like cy13):** both blocks already used `<Button>` (no raw `<button>`), and `consistencyAudit.report.ts` tracks chrome/weights/color — **not** any empty-state or interaction-pattern metric — so `primitiveAdoption.baseline.json` stays `{ rawButton: 278, rawI…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:682` — - **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:664` — ### 2026-07-24 — cy17 DONE (S3 T3.1 interaction-pattern standardization — the last two hand-rolled page-empties converge onto the shared `EmptyState` primitive; owner-authorized under the full-authority grant)
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:672` — - **Docs:** `patterns.md` — replaced the line-163 "should adopt … as they're touched" TODO-note with a **cy17 convergence record** (both sites now on `EmptyState`, markup locked by the test) + an explicit "remaining centered blocks are load-error / not-found / filtered / slot — don't force them o…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:677` — - **Why a no-code cycle is the honest move (not a stop, not a manufactured edit):** the autonomous-loop-continuity directive says don't stop while authorized work remains — but the soul guardrail ("consistency, not redesign; add a shared primitive *only when the system genuinely lacks a pattern*"…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:682` — - **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-…
- `cy17` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:712` — - **Live validation — driven in a real browser, in BOTH themes, and cy17's theme caveat is now retired.** Served this worktree's frontend on Vite `:3100` proxied to a gateway on `:10000` with an ISOLATED `PERSONALCLAW_HOME` (deleted after) and `AUTH_MODE=none`, then drove it with the bundled Play…
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:676` — ### 2026-07-24 — cy18 DONE (assessment/validation cycle — the autonomous-completable convergence lanes are code-verified DRAINED; no source change, and manufacturing a marginal one would violate the soul guardrail)
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:688` — - **Why this became completable — cy18's Lane 1 and Lane 2 conclusions were right in July and are stale now.** cy18 recorded that an `ErrorState` primitive "would gain exactly **one** faithful adopter — premature abstraction", and that of the cockpit's 21 raw buttons "**Zero** byte-identical clus…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
