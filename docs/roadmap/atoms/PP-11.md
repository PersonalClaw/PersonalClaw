<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-11` — Extract `AdmissionPolicy` behind today's lanes (no behaviour change)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #469 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1304 |

## Intent — what this is for

Four independent schedulers answer "what may run now, given persisted state", sharing ZERO lines: `workflows/tick.frontier()` (typed lanes + per-container `max_concurrency` + WIP=1), `loop/tick.evaluate()` (dwell / min_findings / metric_pass / metric_hold / rollback), `workflows/pool.py`'s `frontier`/`next` (priority + blocking-count + overdue with TTL'd CAS leases — verified: `pool.py` imports only stdlib), and `triggers/` `tick_once`. The first three are the same projection with different admission rules, and each has a capability the others structurally cannot express. arXiv:2604.11378's contribution is precisely that an agent loop is "a single ready unit scheduler" and graph engines differ from it in degree, not kind — so the unification is semantically available, not aspirational.

## Done when

`frontier()`'s admission step becomes an ordered list of `AdmissionPolicy` objects, with today's lane caps, per-container `max_concurrency` and WIP=1 expressed as the first three policies and composed tightest-wins. `frontier()` stays PURE — no clock, no I/O — and its output is byte-identical for every existing spec: a golden-file test over the bundled templates' frontier decisions captures before and diffs after. `wip_held` and `deferred` keep their distinct meanings (a declared invariant being enforced vs lane pressure). No new policy is added here and no other scheduler is touched — this atom only makes the seam exist.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-4`](PP-4.md) | intra-repo | `done` |

### Unblocks (1)

[`PP-12`](PP-12.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- 🏅 THE SHARPEST REFACTOR DISCIPLINE IN THE CAMPAIGN, STATED AS A LOGICAL NECESSITY: `PP-11` introduced 'NOTHING NEW' — the three rules `frontier()` already applied, 'moved behind a named interface and composed explicitly'. And why: 'a refactor that also changed behaviour COULD NOT BE VERIFIED, because there is no oracle for "did the scheduler still decide the same thing"'
- 🔑 THE ORACLE'S PROVENANCE IS THE WHOLE PROOF: `tests/test_workflows_frontier_golden.py`, 'WHOSE FIXTURES WERE CAPTURED BEFORE A LINE OF THIS EXISTED'. Fixtures captured after the refactor would encode the new behaviour and prove nothing. 5/5 green, and its own guard notes a change that 'would silently shrink the captured decision set'
- 🔑 THE DIAGNOSIS IS THE PLAN'S ENTIRE THESIS IN ONE SENTENCE: four schedulers 'answered the same question … and shared ZERO lines … Each held a capability the others structurally could not express, SO EVERY NEW ADMISSION RULE LANDED WHEREVER ITS AUTHOR HAPPENED TO BE STANDING'
- 🔴 AND THE UNIFICATION IS SCOPED RATHER THAN TOTAL: `triggers/` 'stays out ON PURPOSE (it answers whether to START, on a wall clock)'. Refusing to merge the thing that is not the same question is what keeps the primitive honest
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not a browser surface.

**Notes:** 🏅 Capture the oracle first, then move nothing but the shape — the only order in which 'no behaviour change' is a checkable claim rather than an assurance.

## Recorded history

### Commits (4)

- `d69a20a66` 2026-08-14 — feat(workflows): PP-12 add Lease, Dwell and MetricGate admission policies
- `5c3153c8a` 2026-08-14 — feat(workflows): PP-2 derive ordering from bindings; lift sibling-only needs
- `7042fb288` 2026-08-14 — refactor(workflows): PP-11 compose admission from an ordered policy list
- `6d2a7f0bc` 2026-08-14 — docs(roadmap): add Platform-Primitives plan + 20 atoms (rev 17)

### Named by atom id in the roadmap prose (27)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:73` — | Pure frontier core | WORKFLOWS-V2 `WV-3` (done) | **Extended, not replaced**: `PP-11` puts a seam behind it with a byte-identical acceptance bar. |
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:110` — ## §4 One admission core, N admission policies (`PP-11`…`PP-13`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:117` — `PP-11` is seam-only with a byte-identical golden-file bar — a refactor that also changes behaviour
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:143` — └─→ PP-11 ─→ PP-12 ─→ PP-13 ───────┤
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:168` — `PP-1`, `PP-2`, `PP-3`, `PP-11`, `PP-14` and `PP-15` are pure-logic and can land at any time.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:186` — * **A fifth verdict vocabulary**, which is the default outcome of doing `PP-11` or `PP-16` before
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:197` — 5. `frontier()`'s decisions are byte-identical across the `PP-11` refactor, proven by golden file.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:867` — - **2026-08-14 — `PP-11` DONE.** `workflows/admission.py` now owns the answer to "may this start?":
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:879` — - **2026-08-14 — DESIGN (`PP-11`): one bucket shape covers both of today's admission questions.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:888` — - **2026-08-14 — DECISION (`PP-11`): tightest-wins, and a TIE goes to the named refusal.** Composing
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:901` — - **2026-08-14 — DISCOVERY (`PP-11`): the bundled templates alone cannot prove the seam.** The
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:914` — - **2026-08-14 — DEVIATION (`PP-11`): a fidelity bug the golden caught mid-refactor.** The first
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:923` — - **2026-08-14 — `PP-11` proof.** `tests/fixtures/frontier_golden/{bundled,policies}.jsonl` (460
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:940` — - **2026-08-14 — DISCOVERY (`PP-11`): the golden file cannot catch a composition-ORDER bug, and that
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:950` — - **2026-08-14 — DISCOVERY (`PP-11`): a bare `python script.py` in a git worktree imports the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:960` — - **2026-08-14 — DECISION (`PP-11`): no CHANGELOG entry.** The atom's acceptance bar IS
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1047` — - **2026-08-14 — `PP-2` composition with `PP-11`.** Derived ordering and admission are ORTHOGONAL
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1050` — are built once in `frontier()` and threaded down exactly as `PP-11`'s `policies` are — neither
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1053` — `PP-11`, so this is a re-expression on the current base, not a rebase.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1054` — - **2026-08-14 — `PP-2` the "restriction is unnecessary" argument HELD against the post-`PP-11` code.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1058` — nodes is honoured by re-derivation each tick. `PP-11` restructured admission but left the tree walk
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1060` — - **2026-08-14 — `PP-2` no existing schedule changed (the honest proof).** `PP-11`'s bundled
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1197` — - **2026-08-15 — `PP-8` DECISION: CHANGELOG entry added.** Unlike `PP-11`, this atom adds a
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1282` — **5085 passed, 4 skipped**. `PP-11`'s golden frontier file re-runs unchanged, with
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1316` — so `admission.py` stays inside `PP-11`'s purity rail — which still passes unweakened.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1324` — (`ef8497ed…`) at `854529a2` with `PYTHONPATH` pinned to the worktree, per `PP-11`'s finding that a
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1369` — `PP-11`'s golden frontier file re-runs unchanged. `test_roadmap_dag_derived` +

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
