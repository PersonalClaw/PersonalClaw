<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR2-6` — Third mid-turn policy `steer` + ACP capability gate + composer affordance

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-RESILIENCE](../plans/PLATFORM-RESILIENCE.md) (`PR2`, plan status `done`) |
| **Atom table** | [`../atomic/PR2.md`](../atomic/PR2.md) |
| **Execution position** | #485 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment (2026-07-26) mid-turn steering; Execution log Session 6 (S6.1–S6.3)

## Done when

steer joins the mid_turn_policy enum (5-point wiring + PATCH values + roundtrip); a native turn injects a mid-turn steer at the next tool boundary (drain called before the no-tool-call return, keyed on the namespaced session key); non-capable runtime falls back to queue loudly; add_steer keyed on a wired drain source (no cross-turn leak); composer steer control + steered strip; validated as a user on Bedrock.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PR2-3`](PR2-3.md) | intra-repo | `done` |

### Unblocks (1)

[`PR2-10`](PR2-10.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `Session 6`. Those tokens appear in its source plan at:

- `Session 6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:592` — **Lands in:** an added **Session 6** (the plan's 5 sessions are all DONE — extending S3 retroactively would falsify the log). Count 5 → **6 sessions**.
- `Session 6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:600` — ## Execution log — Session 6 (mid-turn steering)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
