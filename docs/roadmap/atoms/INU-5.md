<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `INU-5` — Digest + demotion (S5)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INBOX-NOTIFICATIONS-UNIFICATION](../plans/INBOX-NOTIFICATIONS-UNIFICATION.md) (`INU`, plan status `done`) |
| **Atom table** | [`../atomic/INU.md`](../atomic/INU.md) |
| **Execution position** | #297 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##115 |

## Intent — what this is for

Session 5 — Digest + demotion (Wave 2) (T5.1-T5.3, V5)

## Done when

notification-digest deterministic action provider registered as a silent system cron drains digest_queue.jsonl into one grouped digest inbox item (empty queue produces nothing); unread_count() derives from inbox PENDING and the notification log loses acked/unread semantics (becomes read-only delivery audit); CHANGELOG entry names the one-time badge reset with personalclaw snapshot advised

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`INU-1`](INU-1.md) | intra-repo | `done` |
| [`INU-2`](INU-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T5.1`, `T5.3`, `Session 5`. Those tokens appear in its source plan at:

- `T5.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:207` — | T5.1 | Digest cron: system cron registration (respects `--no-crons`), drain queue → grouped `digest` inbox item + delivery per rule targets (dashboard + channel_dm via existing `deliver_notification`/`deliver_cron_result` precedent — record which fits) | `notification_rules.py` digest builder, …
- `T5.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:484` — - 2026-07-30 — **DONE (Session 5: T5.1–T5.3). The plan is COMPLETE (S1–S5).**
- `T5.1` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:486` — **T5.1 — the digest.** `build_digest_body` groups by kind (the point of a digest is that "9
- `T5.3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:209` — | T5.3 | CHANGELOG entry naming the one accepted state break — the unread badge resets once because acked/unread semantics leave the notification log — with `personalclaw snapshot` advised in the release notes | `CHANGELOG.md` | entry present and specific about what a user observes; the old unrea…
- `T5.3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:309` — T5.3 for a gate flip + legacy-branch deletion. `src/personalclaw/lifecycle/` does not
- `T5.3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:484` — - 2026-07-30 — **DONE (Session 5: T5.1–T5.3). The plan is COMPLETE (S1–S5).**
- `T5.3` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:505` — **T5.3 — the accepted state break, in the CHANGELOG.** The badge resets once on upgrade.
- `Session 5` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:203` — ### Session 5 — Digest + demotion (Wave 2)
- `Session 5` `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:484` — - 2026-07-30 — **DONE (Session 5: T5.1–T5.3). The plan is COMPLETE (S1–S5).**

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
