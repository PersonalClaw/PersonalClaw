<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-10` — Docs-lint + plan-hygiene gate

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #447 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##880 |

## Intent — what this is for

Add scripts/docs_lint.py, make docs-lint, and a blocking CI job checking dead relative links, missing anchors, file.py:NNN citations whose file no longer exists, and code-fence language tags; ship at the measured population with a shrink-only allowlist rather than at zero. Extend it to plan hygiene in report-only mode: a plan whose Status: DONE header has unchecked task rows, or whose header contradicts its Execution log, is reported — the log and the code win over the header, so this ratchets rather than blocks.

## Done when

A dead link or a stale file:line citation reds CI; the current population is committed (not zero); the plan-hygiene checker reproduces the known stale-header audit findings on a seeded stale header.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `546949220` 2026-08-06 — test(hardening): PHF-10 docs-lint + plan-hygiene gate (shrink-only ratchet) (#880)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
