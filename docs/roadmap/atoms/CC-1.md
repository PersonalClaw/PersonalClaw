<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CC-1` — S1: true rewind (fork-and-swap under same slot) + queue interrupt-now

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CHAT-CRAFT](../plans/CHAT-CRAFT.md) (`CC`, plan status `done`) |
| **Atom table** | [`../atomic/CC.md`](../atomic/CC.md) |
| **Execution position** | #105 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #feature-chat-craft (2026-07-27) |

## Intent — what this is for

Session 1 (T1.1-T1.3, V1); Design S1; Contracts C1, C2

## Done when

editing any past user turn with rewind:true snapshots the discarded tail onto user_msg['rewound'] (cap 5, redacted), truncates, sessions.reset rebuilds provider context from the truncated transcript, chat_rewound WS + SEL chat.rewind fire, and the orphaned /interrupt is wired with a queue_promoted echo + 'Interrupt now' QueueStack action

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`CC-6`](CC-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:618` — not drift**: CC-1 landed 2026-07-27 and 0.1.3 released 2026-07-30, so the field shipped in
- **source plan** `docs/roadmap/plans/CHAT-CRAFT.md:786` — drift: `CC-1` landed 2026-07-27 and 0.1.3 shipped 2026-07-30. This commit changes no user-visible

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.1`, `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/CHAT-CRAFT.md:126` — | T1.1 | Rewind backend: extend `api_chat_session_edit_resend` with `rewind: true` — snapshot the discarded tail onto the edited user message (`rewound` list, cap 5, redacted like `_attach_variants`), truncate, persist off-thread, `sessions.reset(session_key)`, broadcast `chat_rewound`; 409 while…
- `T1.1` `docs/roadmap/plans/CHAT-CRAFT.md:196` — (`feature-chat-craft`), one conceptual commit. **S1 (T1.1-T1.3):** true rewind via
- `T1.3` `docs/roadmap/plans/CHAT-CRAFT.md:128` — | T1.3 | Interrupt-now: `api.interruptChat` + an "Interrupt now" action on `QueueStack` cards; backend `queue_promoted` WS echo; superseded partial keeps the existing `stop_event` card | `web/src/lib/api.ts`, `ChatPage.tsx` (`QueueStack` + WS case), `src/personalclaw/dashboard/chat_handlers.py` (…
- `T1.3` `docs/roadmap/plans/CHAT-CRAFT.md:196` — (`feature-chat-craft`), one conceptual commit. **S1 (T1.1-T1.3):** true rewind via
- `Session 1` `docs/roadmap/plans/CHAT-CRAFT.md:122` — ### Session 1 — True rewind + queue with manners

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
