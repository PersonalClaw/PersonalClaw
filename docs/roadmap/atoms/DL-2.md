<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DL-2` — S2: docs IA + llms.txt/llms-full.txt + landing page + sitemap/OG + release flip

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISCOVERABILITY-LAUNCH](../plans/DISCOVERABILITY-LAUNCH.md) (`DL`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DL.md`](../atomic/DL.md) |
| **Execution position** | #192 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #website #20 |

## Intent — what this is for

Session 2 — Docs site + machine-readable surface (T2.1-T2.4, V2)

## Done when

every core doc reachable <=2 clicks with link-check green; llms.txt + llms-full.txt serve at domain root as text/plain and regenerate per build; landing page copy matches Design (differentiator + zero-telemetry cards, threat-model/SECURITY footer); sitemap + OG meta ship; source manifest flipped to channel: released with core+apps pinned v0.1.3

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DL-1`](DL-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.1`, `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:68` — | T2.1 | Information architecture: Guides / Reference / Architecture / Security / Roadmap(link to repo) nav; getting-started is the docs landing | site repo config | every core doc reachable ≤2 clicks; internal links resolve (link-check step in site CI) |
- `T2.4` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:40` — - **Launch assets:** screenshot set (dashboard home, chat with a tool-approval brief, loop cockpit mid-run, knowledge answer with citations, Store consent surface showing declared permissions) + a 60-90s silent capture (chat→approval→loop→knowledge→artifact/widget produced — the Canvas counter). …
- `T2.4` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:71` — | T2.4 | Sitemap + OG/social meta + per-repo social-preview images (1280×640: name, tagline, coral identity per `web/DESIGN.md` palette) | site repo + image assets; upload via repo settings (owner task 5) | rich embeds render in a link-preview checker |
- `T2.4` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:106` — 5. **Upload social-preview images** to both repos (Settings → Social preview) when T2.4 produces them.
- `Session 2` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:64` — ### Session 2 — Docs site + machine-readable surface

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
