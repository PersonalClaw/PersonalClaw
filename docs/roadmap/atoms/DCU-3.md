<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DCU-3` — macOS accessibility driver (indexed AX tree)

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [DESKTOP-COMPUTER-USE](../plans/DESKTOP-COMPUTER-USE.md) (`DCU`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DCU.md`](../atomic/DCU.md) |
| **Execution position** | #155 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`ext`) |
| **Blocked reason** | PARTIAL. The dispatch-level half of the stale-index clause is CLOSED by PR #2572; the live half is honestly `unproven` on this host and the reason is a finding, not a ceiling. WHAT #2572 CLOSED, and it was closed nowhere before: the clause has TWO verbs — a stale index (past TTL or changed fingerprint) must REFUSE and must FORCE A RE-SNAPSHOT — and only `refuses` had ever been asserted. The nearest existing test wound the frozen clock BACKWARDS, which is not a re-snapshot, and the rest asserted only that the string `computer_snapshot` appears in the FIX line: a remedy nobody had executed. tests/test_computer_use_stale_index.py now executes it for both triggers through service.computer_dispatch, and the load-bearing assertion is not that the new id acts — it is that THE OLD ID KEEPS REFUSING WHILE THE NEW ONE ACTS, since a test checking only the new id would pass against a store that had quietly started honouring the abandoned index, which is the opposite of the clause. TWO VACUITY TRAPS CLOSED. (1) service._stale raises ERR_COMPUTER_USE_STALE_INDEX for THREE causes — an unknown/evicted id, a past-TTL id, and a changed fingerprint — and two of them are vacuous for this clause: a typo'd snapshot_id refuses with exactly the code a careless harness looks for, reporting a measured refusal for a measured nothing. Every leg now asserts the detail it expects AND asserts the other two are absent, with a test driving all three for real to hold the strings apart. (2) Driver spawns are the second discriminator and are COUNTED: the past-TTL check is decided before any window is walked, while the fingerprint check exists BECAUSE one was, so the harness requires ZERO driver operations for the TTL leg and exactly ['snapshot'] for the fingerprint leg — a TTL refusal that spawned a driver, or a fingerprint refusal that did not, is a different bug wearing the same code. WHY THE LIVE LEG IS `unproven` AND NOT SKIPPED OR GREEN: scripts/dcu3_stale_index_validate.py probes the grant first and exits 1 with status `unproven`, failed_clause `preflight`, attempts 0 — no app launched, no window walked, nothing touched on the operator's desktop. `False` there can only mean the OS said no, because macos_ffi raises FFIUnavailable when the frameworks fail to load, so a load failure cannot masquerade as a denial. PREMISE CORRECTION that supersedes what I wrote on 2026-09-07 and on DCU-4: the Accessibility grant is NOT a workstation property. macOS resolves it against the RESPONSIBLE PROCESS, and the same python binary in the same repo measured granted under com.amazon.kiro.crew at 09:51 and DENIED under dev.warp.Warp-Stable at 18:38. So this atom is not permission-gated in principle, but closing its live leg requires running the validator FROM a session whose responsible process holds the grant, and recording which one that was. TWO CEILINGS REMAIN, NOT ONE — the second was missing from this row until 2026-09-07. (1) THE LIVE STALE-INDEX RUN: run scripts/dcu3_stale_index_validate.py from a session whose responsible process holds Accessibility; it is one command and it reports its own verdict. (2) `computer_type` HAS NEVER SUCCEEDED LIVE. Clause 1 says typing into a field succeeds, and #2552 satisfied that leg with `computer_set_value` (macos_driver op_set_value), which is a DIFFERENT MECHANISM: op_type routes through ffi.focus + ffi.type_text to CGEventPostToPid, and that key-posting path has only ever been observed REFUSING (the secure-field leg). So half of clause 1 is unexercised end to end and was reading as met. One live computer_type success on a non-secure field closes it. RELATED, filed rather than fixed: #2569 — the AX permission refusal's FIX names the python executable and says 'not a terminal app', which is the WRONG TCC principal and is disproved by this repo's own passing run (the grant that made #2552 possible is held by an APP BUNDLE). An operator following that instruction grants something macOS never asks about, which is why the grant step above keeps failing. #2570 — a past-TTL refusal writes its SEL row with resources='' because step 3a raises before `app = snap.app`, so count-based audits stay green over a record that lost its target. |

## Intent — what this is for

macOS accessibility driver over ctypes FFI (macos_driver.py, macos_ffi.py, types.py) per §2/§3.2: element walk into an indexed AX tree with TTL + fingerprint; AXPress activation by element index (no pointer involved); type/set-value/scroll/named-action; a located coordinate path that posts to the target process (CGEventPostToPid) for canvas/custom-drawn UI; and the explicit real-cursor global warp behind its own named method and a distinct SEL tool_kind that auto never resolves onto. Element-targeted, non-pointer input is the default and the only thing on by default.

## Done when

With the enable on, snapshotting a TextEdit window then AXPress-ing a button by index and typing into a field succeeds without the pointer moving; a stale index (past TTL or changed fingerprint) refuses and forces a re-snapshot.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DCU-1`](DCU-1.md) | intra-repo | `done` |
| `EXT:SECURITY-HARDENING:OS-input-layer class-B/S review` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`DCU-4`](DCU-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (15)

- `232c26738` 2026-09-07 — docs(roadmap): record the #2673 dependency audit — one real defect, two cleared
- `12a7db62d` 2026-09-07 — docs(roadmap): two gates asserted only in prose become edges, and one claimed gate leaves a title
- `492807a82` 2026-09-07 — fix(computer-use): the AX refusal names the responsible process, and a past-TTL SEL row keeps its app
- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `f57f50341` 2026-09-06 — test(computer-use): DCU-3's stale-index half — dispatch level closed, live run unproven
- `daa3b999b` 2026-09-06 — docs(roadmap): flip SH-12 and DCU-4; stop the frontier claiming owner work is startable
- `a6acaa5e4` 2026-09-06 — docs(roadmap): SH-4's registry gate was its own deliverable; split SH-12 out
- `f063ef7fa` 2026-09-03 — docs(roadmap): flip MC-6b + EI-2 (evidence #2359, #2361)
- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips
- `a15d5bfba` 2026-08-28 — docs(roadmap): strike two EXT deps that pointed at work the atoms already own
- `775f47b8d` 2026-08-28 — docs(roadmap): rule three cross-plan blockers, one of them a fail-open
- `a1481aa68` 2026-08-27 — fix(evals): LV-7 reconcile the benchmark with main's error-code contract
- `95cec31ea` 2026-08-26 — feat(desktop): DCU-3 macOS accessibility driver over an indexed AX tree
- `f4aa88da0` 2026-08-25 — fix(tools): adjudicate an atom only on entries it is the subject of
- `8b0636277` 2026-08-15 — feat(computer-use): DCU-1 out-of-band keystone enable-state

### Named by atom id in the roadmap prose (37)

- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:190` — stdio shim and the in-gateway dispatch chain, and `DCU-3` the macOS driver. A test that literally
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:261` — The OS driver is `DCU-3` and is not touched here.
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:378` — remember. That is correct sequencing (`DCU-3` owns the driver, `DCU-4` owns `service.py`'s chain and the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:402` — that needs `DCU-3`.** Shipped `computer_use/service.py` (the in-gateway chain), `tools.py` (the seven-tool
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:413` — put"** needs `DCU-3`'s macOS AX driver, which does not exist on `main` (no `macos_driver.py`,
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:414` — `macos_ffi.py` or `types.py`). `DCU-4` declares `DCU-3` as a dep; the atom was taken anyway because
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:417` — `DCU-3`**, which should record it.
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:422` — when `DCU-3` lands, what changes is one importable module — not the containment story. Driven for real
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:484` — "the driver running as a ceilinged subprocess" needs a child to run in; `DCU-3`'s `macos_driver.py` is
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:526` — brief did not mention that `DCU-3` is unbuilt, which is what makes one `done_when` clause unreachable
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:586` — - [2026-08-26][DCU-3] PARTIAL: the macOS accessibility driver ships as `types.py` (the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:597` — - [2026-08-26][DCU-3] DECISION: **ctypes FFI, and therefore NO new dependency — not even an
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:615` — - [2026-08-26][DCU-3] DEVIATION: added `ERR_COMPUTER_USE_AX_PERMISSION` to `errors.ERROR_CODES`
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:628` — - [2026-08-26][DCU-3] DEVIATION: re-scoped two `DCU-4` tests whose premise this atom retires, in
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:632` — nothing, and `DCU-4`'s own docstring predicted the change ("when `DCU-3` lands, what changes is
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:639` — - [2026-08-26][DCU-3] DISCOVERY: the structural-duplication ratchet caught a real re-derivation and
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:652` — - [2026-08-26][DCU-3] DISCOVERY (falsification, including one that did NOT red): six mutations were
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:668` — - [2026-08-26][DCU-3] BLOCKED-BY-ENVIRONMENT (not an escalation — everything buildable is built):
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:703` — `_CHILD_CODES` addition is not a preference: `DCU-3` measured that `_run_driver` rewrites any
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:721` — property `DCU-3` wrote `test_importing_the_ffi_touches_no_framework` about). **Simulated:** the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:735` — for this platform' refusal"* and that *"`DCU-3` will ship"* the FFI. Both were made false by
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:736` — `DCU-3` landing, not by this atom, and the task line here is the Windows/Linux refusal — so it
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:795` — `_CHILD_CODES` — the inverse of `DCU-3`/`DCU-6`: their codes had to be added because only the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:800` — ### OWNER RULING — `DCU-3`'s SECURITY-HARDENING dependency is STRUCK. 2026-08-28
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:802` — `DCU-3` carries `EXT:SECURITY-HARDENING:OS-input-layer class-B/S review`. Audited: **nothing produces it.**
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:805` — which is how `DCU-3` came to look cross-plan-gated when it is not.
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:816` — **`DCU-3` is therefore HARDWARE-gated only**, and precisely: a human adds the **python binary running the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:853` — phrasing predates `DCU-3`'s substrate decision — this system reads accessibility trees, not
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:874` — still parked per `DCU-3`), real SEL rows read back off disk into the feed, and the real
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:931` — **WHAT THIS DOES NOT CLOSE.** `DCU-3`'s own row still owns the *stale-index* half of its
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:939` — - **2026-09-06 — `DCU-3`'s STALE-INDEX half: the dispatch-level clause is closed, the LIVE clause is
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:941` — `DCU-3`'s `done_when` ends *"a stale index (past TTL or changed fingerprint) refuses and forces a
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:1019` — **WHAT THIS DOES NOT CLOSE.** `DCU-3` should NOT flip. The dispatch-level clause is green and
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:384` — (`CA-9`, `CE-9`, `DCU-3`, `DL-7`, `EI-2`, `ET-7`, `LV-7`, `PEP-3`, `PEP-11`, `PUBL-10`,
- **source plan** `docs/roadmap/plans/ONBOARDING-UX.md:1151` — `**Status:**` stays `todo` — the same convention `DCU-3`/`DFE-6` already use.
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:810` — **2026-09-03 — SH-11 minted: owner supplies the Apple signing + notarization secrets and the minisign release key.** `SH-3` shipped the signing scheme + verifier with the trust store deliberately EMPTY — an agent must not generate the private key it would hand over — so the owner half was implici…
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:814` — Worse, the real blocker was absent from the graph. `SH-11`'s own title says it *gates DC-1/DCU-3/SH-4*, but `SH-11` was not among `SH-4`'s deps, so the one thing actually holding signing up — the owner's Apple Developer secrets and the production minisign key — was invisible to the frontier. `SH-…

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `§2`, `§3.2`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:478` — mcp-computer` stdio server.** The plan's §2 draws one, but this repo already has exactly one stdio MCP
- `§2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:480` — `_post`, which is precisely the thin shim §2 describes ("the same reasoning as PersonalClaw's other MCP
- `§2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:491` — element index** and so skips step 3 — §2 reserves that path for canvas UI that exposes no addressable
- `§3.2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:598` — optional extra.** §3.2 already specified ctypes and it is also the cheaper answer: every symbol

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
