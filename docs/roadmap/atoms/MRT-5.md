<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MRT-5` — Learned policy + cost-aware ordering + propose-don't-write proposals

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MODEL-ROUTING-TELEMETRY](../plans/MODEL-ROUTING-TELEMETRY.md) (`MRT`, plan status `done`) |
| **Atom table** | [`../atomic/MRT.md`](../atomic/MRT.md) |
| **Execution position** | #369 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2066 |

## Intent — what this is for

§4.2 LearnedPolicy (60/40 scoring, >=5-sample floor, EMA+hysteresis), §4.3/§5.2 cost-aware near-equal ordering + cloud_quality_margin, §6.3-6.4 proposals + cooldown + notification + SEL audit; Session 3 (§9)

## Done when

per-(use_case,query_class,ref) score=0.60*success_rate+0.40*feedback over the fold with feedback extracted from WF2 ledger/eval_judge (renormalizing onto success_rate + feedback_n:0 when absent); a genuine quality gap at n>=5 enqueues a routing proposal (skills/proposals.py pattern) with inspectable evidence (scores, n, latency/cost deltas, sample_audit_ids) WITHOUT editing routing_policy.json; accept updates the table with proposal_id basis + SEL entry, reject suppresses re-propose for reproposal_cooldown_days (SC #5); cost only reorders within-hysteresis near-equals with cloud needing to beat local by cloud_quality_margin; deleting routing_stats.json/routing_policy.json degrades to heuristic/off and nothing writes memory.db/knowledge.db (SC #8)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MRT-4`](MRT-4.md) | intra-repo | `done` |
| [`MRT-1`](MRT-1.md) | intra-repo | `done` |
| [`MRT-2`](MRT-2.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:step_completed ledger events for the feedback signal (soft; router functions from model_calls.jsonl alone; already built)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 29 (MRT) — DRIVEN in a real browser

**Code evidence:**

- 🔑 proposals.py:1-8 THE JUSTIFICATION IS A CONSENT ARGUMENT, NOT A UX ONE: routing_policy.json is the user's table, and 'a telemetry fold quietly rewriting lever 3 would mean THE MACHINE CHANGED WHICH PROVIDER SEES THE USER\'S CONTENT without anyone deciding to'. So a measured quality gap enqueues a proposal with its evidence and waits
- 🔑 PROPOSE-DON'T-WRITE IS ASSERTED ON BYTES, AND THE HARNESS PROVES ITSELF: tests/test_routing_proposals.py:4-7 records that 'Asserting "the table grew no new key" would pass for' a changed existing value, so the tests read the raw bytes before and after — and test_the_byte_harness_can_see_a_real_write 'proves the comparison is not vacuous'. A vacuity floor on a byte comparison
- accept is the only writer and is reached only from a human decision; reject writes no table at all, only a suppression
- the cooldown is durable for a stated reason: the store holds the queue AND the rejection ledger, 'because a cooldown that lived in memory would reset on every gateway restart and therefore would not be a cooldown'
- untrusted text is FENCED in the proposal record 'so a poisoned record can\'t direct a model that later renders it' — prompt injection considered on the render path of an evidence blob
- 🔑 THE SEL ORDERING IS ARGUED, AND IT DELIBERATELY SUBORDINATES THE AUDIT LINE TO THE USER: accept writes the table THEN logs one SEL row, and 'If the SEL write raises, the acceptance STANDS … raising would report a failure for a change that applied, and rolling back would throw away a decision a human made in order to protect an audit line' (proposals.py:396-400, same posture as policy._sel_policy_change)
- MEASURED: the degradation clause has named tests that pass — test_deleting_the_fold_degrades_to_the_heuristic and test_a_corrupt_fold_degrades_rather_than_raising (tests/test_routing_learned_wiring.py:109,121)
- DRIVEN: the 'Proposed routing changes' section renders with its guarantee stated to the user — 'Nothing proposed. When measurements show one of your models clearly beating another for a request kind, the change is proposed here — routing never rewrites your table on its own.'
- a deliberate NON-abstraction, stated: the shape is borrowed from skills.proposals but 'Nothing is imported from there — a skill proposal and a routing proposal share a posture, not a schema'
- 441 tests pass across the 15 routing suites; make lint clean

**Notes:** 🎯 THE ARCC CONTRAST IS THE INTERESTING PART OF THIS CYCLE. ARCC returned Chronicle (BSC-adjacent host telemetry), whose one transferable objective is that activity telemetry must be captured and preserved and its collection agent must not be disabled — the audit trail's existence is not optional. This plan agrees on capture (the ModelCallGuard seam is the only writer, and query_class is a first-class column rather than an extra) and then DELIBERATELY DIVERGES on precedence: if the SEL write fails, the user's accepted change stands and the audit failure is logged. That is the opposite ordering to a fleet-security posture, and it is right here for the reason the code gives — a central security team's trail is load-bearing evidence about someone else, while a single-user tool's audit line must not be able to discard its owner's own decision. Worth recording as a considered divergence rather than pretending alignment. The scoring floor (n>=5), the 60/40 weights and the cloud_quality_margin are present in learned.py; observing a proposal actually enqueue needs real measurements and therefore a bound model.

## Recorded history

### Commits (9)

- `2d8b263a6` 2026-08-26 — fix(roadmap): MRT plan status + re-point the MRT-5 real-data rail at what does not expire
- `e826fe45c` 2026-08-25 — docs(roadmap): MRT-5 done — the gap detector has a caller (PR #2066)
- `bfaa3ea49` 2026-08-25 — feat(routing): MRT-5 the gap detector that enqueues routing proposals
- `99c43fbda` 2026-08-25 — test(tools): re-read ES-7's deciding log entry after the section moved
- `f4aa88da0` 2026-08-25 — fix(tools): adjudicate an atom only on entries it is the subject of
- `606612848` 2026-08-25 — fix(tools): scope audit_landed_atoms verdicts to the atom being adjudicated
- `4022f8d1a` 2026-08-24 — docs(roadmap): MRT-5 wire-depth sweep of the wires nobody measured (discovery)
- `8af1009dd` 2026-08-24 — test(routing): MRT-5 audit — pin propose-don't-write on the raising path and the floor from below
- `ae0dc12c2` 2026-08-23 — feat(routing): MRT-5 learned policy, cost-aware ordering, propose-don't-write

### Named by atom id in the roadmap prose (23)

- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:755` — 1003 of 1017 overall not run**. This is the same finding as MRT-5's `policy::_sel_policy_change` in the
- **source plan** `docs/roadmap/plans/EVALUATION-SUBSTRATE.md:1459` — `decide_log`, **three have now reddened a PR for exactly this reason** — `MRT-5`'s (#2066), this
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:945` — - **2026-08-23 — `MRT-5` COMPLETE in code; the feedback SIGNAL is structurally zero today, and that is the
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:949` — `learned` is accepted and folded onto the heuristic here: the learned scoring stage is [MRT-5's]"* (39-41)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:950` — and *"The heuristic floor. `learned` lands here too **until MRT-5 scores the fold**"* (417). Three config
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1036` — - **2026-08-24 — `MRT-5` re-audited against `origin/main` (`9e0f727b`): the scoring/ordering two-thirds are
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1112` — consequence, not a number to default, so it is recorded rather than improvised (E2/E6). **Remaining MRT-5
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1168` — **MRT-5's propose-don't-write rail still holds** — `test_routing_proposals.py` is green including its
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1183` — MRT-5 derives **9** wires from 4 annotated modules, not the 6 the brief assumed remained; APE-3 derives
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1193` — `MRT-5 policy::set_order` → **RAILED** (4 red, the rails the entry above added), and
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1197` — **MRT-5's seven remaining wires: 3 RAILED, 3 UNRAILED, 1 REFUSED.**
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1245` — `set_mode` was the one MRT-5 UNRAILED verdict with a non-zero cut (10 files). Re-run at
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1247` — measurement. `set_pin` and `_sel_policy_change` already had `cut=0` on the first pass, so all three MRT-5
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1326` — ## Execution log — MRT-5's missing executor: the gap detector, its trigger point, and the review surface (2026-08-25)
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1410` — **Wire census: MRT-5 now derives 14 wires, not 9** — annotating `stats.py` with §6.3 pulled its three
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1467` — - [2026-08-25][MRT-5] ✅ **ATOM FLIPPED `done` (PR #2066).** Integration re-gated the branch on
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1477` — **Both unpushed MRT-5 branches were adjudicated STALE on blob identity** (`routing/learned.py`
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1486` — - [2026-08-26][MRT-5] **Integration follow-up: the plan's own status field, and a real-data rail
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1490` — 1. **`MODEL-ROUTING-TELEMETRY` read `in_progress` with 5/5 atoms `done`.** `MRT-5` is the
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1494` — which made it expire by construction: it held only while `MRT-5`'s newest entry happened to
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1505` — - [2026-08-26][MRT-5] 🔎 **DISCOVERY, deliberately NOT fixed here — `audit_landed_atoms` reads a
- **source plan** `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1506` — measurement as unmet scope.** Chasing red 2 surfaced why `MRT-5`'s flip entry classifies
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1136` — the true one. Its LANDED-AND-CLEAN sibling error on `MRT-5` is fixed in the same change.

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `§4.2`, `§4.3`, `§5.2`, `§6.3`, `§9`, `Session 3`. Those tokens appear in its source plan at:

- `§4.2` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:51` — | WF2 Run Ledger `step_completed` / `gate_criterion` / `step_failed` | WORKFLOWS-V2 (WF2-R13) | run-level *outcome* signals: did the node whose LLM call this was ultimately pass its gate / complete without retries — the "feedback" half of scoring (§4.2) |
- `§4.2` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:241` — - **Session 3 — learned stage + proposals (§4.2, §5.2, §6.3–6.4):** per-class 60/40 scoring over the fold with feedback signal extraction from ledger/judge; ≥5-sample floor + hysteresis; cost-aware near-equal ordering + cloud-quality margin; proposal enqueue/accept/reject + cooldown + notificatio…
- `§4.2` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:355` — `score` (§4.2 0.60·success + 0.40·feedback, but **collapsing onto success_rate when feedback_n=0**
- `§5.2` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:241` — - **Session 3 — learned stage + proposals (§4.2, §5.2, §6.3–6.4):** per-class 60/40 scoring over the fold with feedback signal extraction from ledger/judge; ≥5-sample floor + hysteresis; cost-aware near-equal ordering + cloud-quality margin; proposal enqueue/accept/reject + cooldown + notificatio…
- `§5.2` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1100` — **The unmet clauses need no invented numbers — §6.3/§5.2 specify all three.** `reproposal_cooldown_days`
- `§5.2` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1384` — reorder near-equals (§5.2), and a cost preference must not nag a user about their table. So
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:241` — - **Session 3 — learned stage + proposals (§4.2, §5.2, §6.3–6.4):** per-class 60/40 scoring over the fold with feedback signal extraction from ledger/judge; ≥5-sample floor + hysteresis; cost-aware near-equal ordering + cloud-quality margin; proposal enqueue/accept/reject + cooldown + notificatio…
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1029` — outside §6.3-6.4 and outside this fence.
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1079` — functions reachable from `tests/test_routing_proposals.py` alone**. §6.3 also asks for the queue to be
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1100` — **The unmet clauses need no invented numbers — §6.3/§5.2 specify all three.** `reproposal_cooldown_days`
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1103` — **⚠️ OWNER DECISION — the enqueuer's trigger point, which the plan never names.** §6.3 says a crossing
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1114` — §6.3 evidence payload), and the accept/reject/list API + Routing-tab badge. Both are startable
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1388` — **Evidence (§6.3) is built from the EXISTING read model, not a second derivation.**
- `§6.3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:1394` — mangle a ref into an untrusted-text block. **Documented deviation from the §6.3 example:** `n` is
- `Session 3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:241` — - **Session 3 — learned stage + proposals (§4.2, §5.2, §6.3–6.4):** per-class 60/40 scoring over the fold with feedback signal extraction from ledger/judge; ≥5-sample floor + hysteresis; cost-aware near-equal ordering + cloud-quality margin; proposal enqueue/accept/reject + cooldown + notificatio…
- `Session 3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:732` — opposite.** Session 3 concluded *"the turn ledger has no loop rows at all"* and *"NOTHING ties model
- `Session 3` `docs/roadmap/plans/MODEL-ROUTING-TELEMETRY.md:830` — Session 3's stated reason for deferring this clause — *"NOTHING ties model spend to a specific

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
