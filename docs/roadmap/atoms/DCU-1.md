<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DCU-1` — Keystone out-of-band enable-state

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-COMPUTER-USE](../plans/DESKTOP-COMPUTER-USE.md) (`DCU`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DCU.md`](../atomic/DCU.md) |
| **Execution position** | #153 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1410 |

## Intent — what this is for

New computer_use package with enable_state.py (§3.1): an out-of-band enable file that the agent process can neither read nor write (not a config field the agent can PATCH), plus enable_state.is_enabled() as the first check in the dispatch chain. No prompt, tool, or chat instruction can flip it — it is the single hardest gate and the whole capability is OFF until the operator turns it on out-of-band.

## Done when

With the enable file absent, every computer-use tool refuses with a WHAT/WHY/FIX message pointing to the out-of-band enable step; no tool or config path can flip the state.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`DCU-3`](DCU-3.md) (`todo`), [`DCU-4`](DCU-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `775f47b8d` 2026-08-28 — docs(roadmap): rule three cross-plan blockers, one of them a fail-open
- `8b0636277` 2026-08-15 — feat(computer-use): DCU-1 out-of-band keystone enable-state

### Named by atom id in the roadmap prose (21)

- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:166` — - [2026-08-15][DCU-1] DONE: keystone out-of-band enable-state shipped as
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:187` — - [2026-08-15][DCU-1] DISCOVERY: ⚠️ **the computer-use tool population is EMPTY on `main`, so the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:208` — - [2026-08-15][DCU-1] DEVIATION: added a live call site the atom's file list does not name —
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:221` — - [2026-08-15][DCU-1] DISCOVERY (falsification found a real defect, not a test gap): 8 mutations
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:237` — - [2026-08-15][DCU-1] Gate: `make lint` clean (black 1679 files, isort, flake8, mypy 866 sources, no
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:259` — Three clauses, all offline-testable, against `DCU-1`'s shipped keystone: a non-allowlisted app refuses,
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:263` — convenience.** `DCU-1` built the keystone so *"no tool or config path can flip the state"*. The app
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:311` — and a dropped write logs a **WARNING** (a deliberate step up from `DCU-1`'s `logger.debug` on its
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:327` — `DCU-1` registered its code; added, and verified by extracting the codes `policy.py` actually names and
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:332` — **A premise in the briefing was wrong, and the agent was right to override it.** I required `DCU-1`'s
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:333` — ratchet to pass untouched as the compatibility proof. It cannot: `DCU-1` deliberately chose `apps` as its
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:375` — `gateway.py:3869`, which imports `ensure_computer_use_boot` — `DCU-1`'s once-per-run boot row. So no
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:394` — `DCU-1`'s `test_every_computer_use_entry_point_guards_first` binds step 1 would have to choose
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:397` — rail has no dispatch to bind to yet. `DCU-1` left the same tension unresolved — its keystone rail
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:410` — exercisable over a real seven-tool population for the first time — `DCU-1` recorded that clause as
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:433` — **RESOLVING THE TENSION `DCU-1` AND `DCU-2` BOTH LEFT OPEN.** `DCU-1`'s keystone rail binds every
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:437` — entry point. So the entry-point population went 0 → **1**, not 0 → 7, and `DCU-1`'s ratchet stopped being
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:487` — tempting optimisation and it breaks `DCU-1`'s clause: `mcp_core._aggregated_call_tool` routes by asking
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:772` — pins it instead of trusting it. The grant is a fourth key in `DCU-1`'s out-of-band enable
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:807` — Meanwhile the substance that dep was reaching for **was delivered inside this plan**: `DCU-1` shipped the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1207` — `DCU-1` were the only two whose atom is still `todo` on `main` — the other 25 are stale branches whose

### Its plan's own record, via the scope references (0 line(s))

Its scope cites `§3.1`, but no line of its source plan carries any of them.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
