<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-3` — S2 approvals-first companion route + approve/reject wiring

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #337 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1250 |

## Intent — what this is for

Amendment §Session placement S2 — Approvals-first companion (T2.1r) + C2 Companion route API map

## Done when

On a phone viewport, `#/companion` renders full-context approval cards from GET /api/approvals (tool name, arguments, session/agent; plan-43 decision-brief when present, raw fallback until then — do not block on plan 43) and approve/reject round-trips against a dev gateway; all other sections stubbed behind S3.5; URL doctrine holds.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (4)

[`MC-4`](MC-4.md) (`done`), [`MC-5`](MC-5.md) (`done`), [`MC-6`](MC-6.md) (`done`), [`MC-7`](MC-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4ba0d863d` 2026-08-13 — feat(companion): MC-3 approve from a phone at #/companion (#1250)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:223` — ### 2026-08-13 — `MC-3` (S2 T2.1r + T2.2 approvals half) — **DONE**
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:457` — companion grows. `MC-3`'s "Not on the phone yet" stub list is **deleted**, not hidden behind a
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:461` — **RESOLVED ROUTE MAP (T2.2, the half `MC-3` deferred).** Every action is the owning surface's own
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:489` — dropped, so the fetched list is authoritative. This is `MC-3`'s trap generalized: its

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.1r`. Those tokens appear in its source plan at:

- `T2.1r` `docs/roadmap/plans/MOBILE-COMPANION.md:176` — | T2.1r | Rescope S2 to approvals-only: `#/companion` renders full-context approval cards from `GET /api/approvals`; approve/reject round-trip; other sections stubbed behind S3.5 | `web/src/pages/companion/`, router | phone viewport: a real pending approval renders with tool+args+session context …
- `T2.1r` `docs/roadmap/plans/MOBILE-COMPANION.md:223` — ### 2026-08-13 — `MC-3` (S2 T2.1r + T2.2 approvals half) — **DONE**

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
