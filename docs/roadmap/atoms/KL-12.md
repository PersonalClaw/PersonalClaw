<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-12` — Indexing H1.5: resumable batched chunk backfill + VH validation

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #313 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1286 |

## Intent — what this is for

Amendment task table — H1.5 (backfill) and VH (validation); Amendment Design (c) re-embed as a migration concern

## Done when

a resumable, batched, progress-reporting backfill (modeled on reembed_all + the ingest queue's recover_pending) chunks existing items; interrupting and restarting resumes without duplicating or skipping; mid-backfill search returns sensible results (degrades to whole-item vectors, never zero); VH holds — a deep-answer question on a 30+ page PDF/long markdown is retrieved and cited to the right section (failing on main first), search latency measured before/after ANN on a large seeded library, backfill interrupt/resume verified, full local gate green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-9`](KL-9.md) | intra-repo | `done` |
| [`KL-10`](KL-10.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `530826e22` 2026-08-14 — feat(knowledge): KL-12 backfill chunks for libraries ingested before chunking

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:26` — and superseded by `KL-9`/`KL-10`/`KL-11`/`KL-12`.) Created 2026-07-18 (roadmap rev 10; owner ask: more
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:565` — - 2026-08-13 — **DONE (`KL-12`: Amendment H1.5 + VH).** `knowledge/chunk_backfill.py`
- **source plan** `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:573` — ingested before H1.1 has none. Measured, not argued: on the KL-12 branch itself, a library

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
