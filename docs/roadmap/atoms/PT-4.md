<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PT-4` — S2: error-treatment variants on ErrorBoundary + IncidentBanner (skin-only)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PERSONALITY-THEMES](../plans/PERSONALITY-THEMES.md) (`PT`, plan status `done`) |
| **Atom table** | [`../atomic/PT.md`](../atomic/PT.md) |
| **Execution position** | #494 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1290 |

## Intent — what this is for

Design 'S2 ...' (errorTreatment); Task breakdown Session 2 T2.3

## Done when

ErrorBoundary fallback and IncidentBanner accept an optional visual variant id from the personality context (visual skin only: same copy, same role=alert, same actions, AA-checked); forced error under each personality renders its treatment; under a standard scheme both are pixel-identical to today

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PT-1`](PT-1.md) | intra-repo | `done` |

### Unblocks (2)

[`PT-5`](PT-5.md) (`done`), [`PT-6`](PT-6.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 48 (PT) — both personalities driven through switch, reload and residue check

**Code evidence:**

- 🔑 THE SKIN-ONLY GUARANTEE IS ENFORCED BY THE TYPE'S SHAPE, and the docstring says so: an errorTreatment is 'an id from the closed ERROR_TREATMENTS map. Presentation only: THE SHAPE OF A TREATMENT HAS NO ROOM FOR COPY, ACTIONS OR ROLES, so a personality CANNOT REWORD OR DISARM A FAILURE. Omitted = today's treatment, unchanged'
- the a11y suite carries the dangling-id rail for this map as well as for shell elements, so a treatment id that does not resolve fails
- personalityA11y 53/53 (falsified: a dangling shellElement id reds exactly one rail); design+personality suites 251/251

**Driven in the UI:** Not driven: the clause requires a FORCED error under each personality (an ErrorBoundary fallback and an IncidentBanner), which needs a deliberate crash or a real incident.

**Notes:** This is the highest-stakes of the three closed maps and the reasoning is the sharpest. A theme that could restyle an error is harmless; a theme that could reword one could make a failure read as success, and a theme that could change its actions could remove the recovery. Making copy, actions and role UNREPRESENTABLE in the treatment shape is why the skin can be handed to an identity at all.

## Recorded history

### Commits (2)

- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `db580f9c4` 2026-08-14 — feat(ui): PT-4 give personalities an error-surface skin

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:29` — `PersonalityShellElement` slot in `App.tsx`. T2.3 (error treatments, atom `PT-4`):
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:169` — - [2026-08-14][S2/T2.3 · atom `PT-4`] DONE (error-treatment variants — skin only). (a) **`web/src/design/errorTreatments.ts`** — a closed `ERROR_TREATMENTS` map (`terminal-frame`, `arcade-panel`), each entry two class strings plus a three-token `paint` triple. The skin-only guarantee is the TYPE:…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:181` — - [2026-08-16][S2/T2.2] **Measured defect, fixed here — `map[id] ?? null` was never closed.** The resolver's first draft copied `getErrorTreatment`'s shape and the closure test reddened immediately: a plain index reads the **prototype chain**, so `getShellElement('constructor')` returned `Object`…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:185` — - [2026-08-16][S2/T2.2] Gate: `npm run typecheck --workspace web` clean · FULL `npm test --workspace web` **278 files / 2771 tests passed** (baseline on the branch point measured first: 274 / 2743 — +4 files / +28 tests, zero regressions, no ratchet or baseline touched) · `npm run build --workspa…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:186` — - [2026-08-16][S2/T2.2] DISCOVERY (adjacent, deliberately left): PT-5's row asks `personalityA11y.test.ts` to go red on a dangling `shellElement`/`errorTreatment` id. PT-4 already added the treatment half, so this atom added the symmetric `shellElement` assertion — the registry's own structural g…

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:29` — `PersonalityShellElement` slot in `App.tsx`. T2.3 (error treatments, atom `PT-4`):
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:145` — | T2.3 | Error-treatment variants: `ErrorBoundary` fallback + `IncidentBanner` accept an optional visual variant from the personality context — skin only (same copy, `role="alert"`, actions) | `web/src/app/ErrorBoundary.tsx`, `web/src/app/IncidentBanner.tsx` | forced error under each personality …
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:169` — - [2026-08-14][S2/T2.3 · atom `PT-4`] DONE (error-treatment variants — skin only). (a) **`web/src/design/errorTreatments.ts`** — a closed `ERROR_TREATMENTS` map (`terminal-frame`, `arcade-panel`), each entry two class strings plus a three-token `paint` triple. The skin-only guarantee is the TYPE:…
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:170` — - [2026-08-14][S2/T2.3] Proofs. **Pixel-identical under a standard scheme** is asserted against markup CAPTURED FROM THE PRE-CHANGE TREE (323265b6) — the ErrorBoundary fallback's full `innerHTML` with lucide's `<svg>` children normalised away (every svg ATTRIBUTE, including the class a treatment …
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:171` — - [2026-08-14][S2/T2.3] DEVIATION: the closed map lives in a NEW `design/errorTreatments.ts`, not inside `personalities.ts` as contract C1 sketched. `personalities.ts` imports only the `ErrorTreatmentId` type, so the registry stays a registry and the treatment file carries its own AA rationale ne…
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:172` — - [2026-08-14][S2/T2.3] DISCOVERY (pre-existing, NOT fixed here — fixing it would break this atom's own pixel-identity clause): `IncidentBanner` paints `background: var(--color-error-container); color: var(--color-on-error-container)` and **neither token is defined anywhere** — not in `tokens.css…
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:173` — - [2026-08-14][S2/T2.3] DISCOVERY (adjacent gap, deliberately left): `ErrorBoundary`'s fallback has **no `role="alert"`** and uses a RAW `<button>`, where `IncidentBanner` has both the role and the `Button` primitive. The atom's "same `role=alert`" clause is satisfiable only for the banner. Both …
- `T2.3` `docs/roadmap/plans/PERSONALITY-THEMES.md:174` — - [2026-08-14][S2/T2.3] Gate: `make lint` rc 0 (black/isort/flake8/mypy, 811 source files) · web typecheck green · FULL `npx vitest run` **223 files / 2209 tests passed** (baseline on 323265b6 measured first: 220 / 2183 — so +3 files / +26 tests, zero regressions) · `npm run build` green, and all…
- `Session 2` `docs/roadmap/plans/PERSONALITY-THEMES.md:139` — ### Session 2 — Sound cues + shell element + error treatment + proofs polished

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
