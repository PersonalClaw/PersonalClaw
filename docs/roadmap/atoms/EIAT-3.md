<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EIAT-3` — mail-inbox send_reply over SMTP, draft-by-default (S2)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EMAIL-INBOX-AND-TRIGGERS](../plans/EMAIL-INBOX-AND-TRIGGERS.md) (`EIAT`, plan status `done`) |
| **Atom table** | [`../atomic/EIAT.md`](../atomic/EIAT.md) |
| **Execution position** | #237 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1656 |

## Intent — what this is for

Session 2 — T2.4; Contract C3 (Outbound). Guardrail 4 (draft-by-default outbound).

## Done when

A reply is composed but NOT sent while draft-mode is on; enabling sending delivers and threads correctly via In-Reply-To; the provider honors the platform's live-writes/dry-run posture (supports_dry_run). Owner-gated on the draft-by-default confirmation (Owner task 4).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EIAT-2`](EIAT-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 41 (EIAT) — Triggers page driven; a URL bug found and fixed (PR #2792)

**Code evidence:**

- MEASURED: smtp_client.py + outbound.py ship and their tests pass inside the 82; the provider declares supports_dry_run so it rides the platform's live-writes/dry-run posture rather than inventing a second switch
- draft-by-default is the shipped default, and the clause records it as owner-gated (Owner task 4) — a default that ships OFF for a send-capable surface is the right direction and matches the fail-closed discriminator this campaign keeps meeting
- mail-inbox app suite 82/82; core event_triggers 19/19 + trigger scoping/sources 56/56; item_kind seam 18/18; web triggers 194/194

**Driven in the UI:** Not driven: composing a reply and observing it NOT sent, then enabling sending and observing correct In-Reply-To threading, needs a live SMTP server plus a real received message to thread against.

**Notes:** Partial for the drive only. The threading clause is the half worth flagging for a future validation pass: In-Reply-To correctness is exactly the kind of claim that unit tests can assert structurally while a real mail client still renders the reply as a new thread.

## Recorded history

### Commits (7)

- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `b2578e62c` 2026-08-18 — chore(roadmap): cite the landing batch #1656 on EIAT-3, WF2LEA-10 and ET-1
- `c0178228a` 2026-08-17 — chore(roadmap): mark EIAT-3, WF2LEA-10 and ET-1 done with their findings
- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `398234478` 2026-08-12 — feat(inbox): EIAT-6 carry a source-declared item kind through to the inbox row (#1121)
- `51d517d93` 2026-08-11 — docs(roadmap): EIAT-4 done (apps#34); EIAT-3 BLOCKED-OWNER on the draft-by-default call (#1093)
- `54c2044e8` 2026-08-09 — feat(triggers): EIAT-5 data-event trigger create UI on the Triggers page (#945)

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:265` — (`_init_inbox` hardcodes the filesystem source) and the real-mailbox V2 validation. EIAT-3 (SMTP
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:295` — per-provider send/draft flag today (the real posture is owned by mail-inbox `send_reply`, EIAT-3,
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:297` — future `send-*`; when EIAT-3 lands a real posture the note should read it instead of the name.
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:328` — - **BLOCKED `EIAT-3`** (send_reply over SMTP, draft-by-default) — **owner-gated by its own done_when**,
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:380` — it belongs to `EIAT-3`'s reply path (a draft was sent at the source) — which is owner-gated on
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:417` — - **2026-08-17 — DONE (`EIAT-3`): `mail-inbox` send_reply over SMTP, draft-by-default** (PersonalClawApps#36).
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:429` — - **2026-08-17 — DISCOVERY (`EIAT-3`): three shipped halves have no reader on the core side.**
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:443` — - [2026-08-26][EIAT-3] **OWNER RULING on Owner task 4 — replies DEFAULT TO DRAFT. This ratifies what
- **source plan** `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:444` — already shipped, and clears a stale gate record.** `EIAT-3`'s `done_when` ended "Owner-gated on the

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.4` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:188` — | T2.4 | `send_reply` over SMTP, **draft-by-default**: honors the live-writes/dry-run posture; sending requires explicit enablement | `mail-inbox/provider.py`, tests | a reply is composed but not sent while draft-mode is on; enabling it sends and threads correctly (`In-Reply-To`) |
- `Session 2` `docs/roadmap/plans/EMAIL-INBOX-AND-TRIGGERS.md:181` — ### Session 2 — The mail inbox provider app (apps repo)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
