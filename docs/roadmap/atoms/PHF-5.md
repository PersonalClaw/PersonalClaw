<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-5` — Committed config-schema baseline + drift gate

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #442 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##875 |

## Intent — what this is for

Add scripts/generate_config_baseline.py that walks the existing _meta registry and emits a committed config-baseline.json (flat path list + type + default + sensitive flag), plus a CI job asserting regeneration is a no-op. This catches drift a round-trip test cannot see: a renamed key, a silently dropped _meta, or a field that stopped being written. Generator must be deterministic (byte-identical on re-run).

## Done when

Renaming a config field without regenerating reds CI naming the path; regeneration is byte-identical on re-run; adding a field without regenerating is caught.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`PHF-6`](PHF-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `546949220` 2026-08-06 — test(hardening): PHF-10 docs-lint + plan-hygiene gate (shrink-only ratchet) (#880)
- `3293c213f` 2026-08-06 — test(hardening): PHF-6 inert-surface baseline (writer/reader shrink-only ratchet) (#879)
- `19cac85b9` 2026-08-06 — test(config): PHF-5 committed config-schema baseline + drift gate (#875)

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1803` — - [2026-08-26][PHF-14] **NEW ATOM — the ceiling rail fired, and this is the payment.** `PHF-5` and
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1823` — `PHF-5`'s schema baseline at ZERO drift as the proof that no observable config key moved, and
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:2034` — non-identical. `tests/test_config_baseline.py` (PHF-5's schema baseline) green, and the generator

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
