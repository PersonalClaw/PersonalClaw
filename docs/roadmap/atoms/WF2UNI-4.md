<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2UNI-4` — Review + revision: typed merge-by-id patches + NO_UPDATE sentinel + TTL'd draft sketches + announce-block surface + plan-as-markdown

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-UNIVERSAL-PLANNING](../plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md) (`WF2UNI`, plan status `in_progress`) |
| **Atom table** | [`../atomic/WF2UNI.md`](../atomic/WF2UNI.md) |
| **Execution position** | #638 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##182 |

## Intent — what this is for

Implementation Effort Session 4; Plan Revision Gate (UP-R7); Plan Review UX (announce-block, ranked alternatives, inferred-chips, cost estimate)

## Done when

workflows/revision.py implements merge-by-id patch semantics (absent-preserved is structural; replace/add refusals deliberate), the NO_UPDATE sentinel, TTL'd sketches with a tombstone set, the announce-block review header applying the same lint exemptions, and a counts-only cost estimate; wired into both workflow_plan paths.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2UNI-3`](WF2UNI-3.md) | intra-repo | `done` |

### Unblocks (3)

[`WF2UNI-10`](WF2UNI-10.md) (`done`), [`WF2UNI-5`](WF2UNI-5.md) (`done`), [`WF2UNI-8`](WF2UNI-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `Session 4`, `R7`. Those tokens appear in its source plan at:

- `Session 4` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:595` — - **Session 4 — Review + revision:** streaming multi-view review (announce-block, ranked alternatives, inferred-chips, naming call); typed merge-by-id revision + NO_UPDATE sentinel + TTL'd sketches + plan-as-artifact; `revise{step_ref, comment}`; new SSE events into `RUN_LIFECYCLE`.
- `R7` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:34` — - **UP-R7** — Typed merge-by-id revision patches over TTL'd draft sketches, streaming multi-view render, NO_UPDATE sentinel, plan-as-markdown-artifact → *Plan Revision Gate*
- `R7` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:78` — 5. **Validation + review** — stage-contract lint (UP-R3), risk/autonomy annotation (UP-R4/R6), streaming multi-view review (UP-R7).
- `R7` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:332` — ### Plan Revision Gate (UP-R7 — mechanism for the walkthrough's best interaction)
- `R7` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:549` — | `planning/` module (runner.py, session.py) + `loop/plan_walkthrough.py` + `loop/*_plan_briefs.py` | **ABSORB → delete with loops** — `run_planner_pass` ≡ a `stage` node with `schema` (structured-output replaces the fragile `plan_steps.json`/`step_artifact.json` sentinel-file polling); the `Plan…
- `R7` `docs/roadmap/plans/WORKFLOWS-V2-UNIVERSAL-PLANNING.md:584` — 7. **Draft sketches:** TTL'd plan drafts with If-Match concurrency + atomic promote (UP-R7), prose view mirrored to an artifact.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
