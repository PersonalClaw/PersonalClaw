<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DFE-6` — Layout control: page setup, paragraph layout, headers/footers, page-geometry preview

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-FIDELITY-EDITOR](../plans/DOCUMENT-FIDELITY-EDITOR.md) (`DFE`, plan status `done`) |
| **Atom table** | [`../atomic/DFE.md`](../atomic/DFE.md) |
| **Execution position** | #165 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 T3.1–T3.4; PageSetup/ParagraphStyle model→docx writer→parser→editor controls; §6 (preview is a labelled approximation — no rasterizer)

## Done when

A4 landscape with 2cm margins round-trips correctly on python-docx read-back; alignment/space-before-after/line-spacing/indents/keep-with-next each round-trip and the editor's controls reflect the loaded document's real values (not defaults); a header round-trips and one the model can't represent is reported not dropped; the geometry preview reflects configured size/margins and is labelled an approximation. V3 gate holds.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DFE-5`](DFE-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 13 (DFE) — DRIVEN in a real browser

**Code evidence:**

- src/personalclaw/documents/writers/docx_writer.py:203 _apply_page + :240 _apply_header_footer + :261 _add_page_number_field; docx_parser.py inverts them
- tests/test_document_layout.py green in the 456-test run
- web/src/ui/content/DocumentLayout.tsx:9 states the §6 contract in the module docstring: 'The preview is a labelled APPROXIMATION. No rasterizer exists in this project…'
- web/src/ui/content/documentLayout.test.tsx:253 asserts it 'says it is an approximation, in words, beside the shape' — the label is a checked property, not a comment

**Driven in the UI:** Not drivable: the layout controls live inside the document editor, which cannot be reached (see DFE-1/#2748).

**Notes:** Every clause has an implementation and a test, including the honest-reporting one (a header the model cannot represent is reported rather than dropped) and the labelled-approximation one. Partial only because 'the editor's controls reflect the loaded document's real values (not defaults)' is a statement about what a user sees on a loaded document, and no document can be loaded in this build without an agent-generated artifact. Code and tests say it holds; I did not see it.

**Follow-ups filed:**

- issue #2748 (empty state points at a route that dead-ends for binary kinds)

## Recorded history

### Commits (3)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `b14289fda` 2026-08-26 — feat(files): DFE-6 layout control — page size, per-edge margins, paragraph indents, headers/footers
- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1011` — controls are `DFE-6`; and `PUT …/model` still refuses non-docx kinds (`_MODEL_KINDS`), which
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1024` — Consequence: `DFE-6`, `DFE-7` and `DFE-8` all depended only on `DFE-5` and are now startable.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1028` — ## Execution log — DFE-5 owner ratification + DFE-6 (layout control) — **DONE**
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1043` — - [2026-08-26][S3 · atom `DFE-6`] **DONE.** All four `done_when` clauses met (T3.1-T3.4).
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1210` — the "one lossy-edit contract, not two" reading of the tenets. Ruled against: `DFE-6` is editing
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1294` — - **For DFE-6's rebase.** No file `DFE-6` owns was restructured, but three shared files were touched
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1412` — components. If the divergence is still cosmetic once DFE-6 and DFE-7 are both on main, that is a
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1468` — `dashboard.document_editing` already gates this surface, as `DFE-6`/`DFE-7` both found) · every new
- **source plan** `docs/roadmap/plans/ONBOARDING-UX.md:1151` — `**Status:**` stays `todo` — the same convention `DCU-3`/`DFE-6` already use.

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `T3.1`, `T3.4`, `§6`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:429` — | T3.1 | Page setup end-to-end: size, orientation, margins through model → docx writer → parser → editor controls | model, `docx_writer.py`, `docx_parser.py`, editor, tests | A4 landscape with 2cm margins round-trips and is correct when read back by python-docx |
- `T3.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:873` — fire on every generated `.docx` until `PageSetup` grows per-edge margins — `T3.1`'s
- `T3.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1043` — - [2026-08-26][S3 · atom `DFE-6`] **DONE.** All four `done_when` clauses met (T3.1-T3.4).
- `T3.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1047` — `.docx` until `PageSetup` grows per-edge margins — `T3.1`'s territory". That was the real cost of
- `T3.4` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:432` — | T3.4 | Page-geometry preview: real margins and page-break positions, **labelled an approximation** (no rasterizer exists — see §6) | editor, tests | the preview reflects the configured page size/margins; the label states it is an approximation |
- `T3.4` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1043` — - [2026-08-26][S3 · atom `DFE-6`] **DONE.** All four `done_when` clauses met (T3.1-T3.4).
- `§6` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:31` — editing (see §7) or server-side page rasterization (see §6).
- `§6` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:144` — - **No LibreOffice, no rasterization, no CRDT** — verified absent (§6, §7). Server-side page
- `§6` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:176` — rasterization (§6); not a template or theme system (DOCUMENT-HANDLING-TOOLS deferred those
- `§6` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:432` — | T3.4 | Page-geometry preview: real margins and page-break positions, **labelled an approximation** (no rasterizer exists — see §6) | editor, tests | the preview reflects the configured page size/margins; the label states it is an approximation |
- `§6` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:914` — WYSIWYG, and §6's "the preview is a labelled approximation" already set that expectation.
- `Session 3` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:425` — ### Session 3 — Layout control

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
