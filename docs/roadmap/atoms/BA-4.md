<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-4` — Browser-session credential handoff (persistent per-site profiles, request_login needs-input flow, session-validity check)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #82 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§5 Browser-Session Credential Handoff (5.1 persistent profile per site, 5.2 request_login action, 5.3 session validity heuristic); Session 4 credential slice

## Done when

Form fill + submit on a real login page via credential handoff works end-to-end: the run parks on needs-input, the user authenticates in a headful window, the run resumes with the authenticated session, and a subsequent run reuses the persisted per-site profile without re-auth; the credentials-never-transit-the-agent invariant holds (LLM never sees password/2FA/token) — SC 3

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`BA-3`](BA-3.md) | intra-repo | `done` |

### Unblocks (2)

[`BA-5`](BA-5.md) (`done`), [`BA-6`](BA-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `995e2ef80` 2026-09-02 — docs(roadmap): flip already-built Wave-GA atoms to done + safe reconciliations
- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `6b06fcaff` 2026-08-28 — feat(browse): BA-4 credential handoff — a credential the agent structurally cannot read or write
- `0b40b3f41` 2026-08-26 — docs(roadmap): rule the nine owner decisions the frontier was parked on, and file PHF-14
- `3984eb0ed` 2026-08-24 — feat(browse): BA-2 real CDP transport + live-browser proof of the egress gate

### Named by atom id in the roadmap prose (12)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:577` — *persistent per-site profiles* to **BA-4** — so whoever writes the launcher decides both, and building
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:578` — it here would pre-empt BA-4's `done_when`. `transport.py` therefore takes a `webSocketDebuggerUrl` and
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:580` — scope on BA-4 (which already owns profiles) or a new atom, rather than leaving it between two atoms
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:612` — so whether BA-2 flips on `done_when` or waits for the launcher to move to BA-4/a new atom is an **owner
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:758` — nothing. Launching Chrome with a persistent per-site profile is BA-4's slice (§5.1) and §10's
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:790` — else — the Stealth Stack and the launcher are separate scope with their own atoms (`BA-4`…`BA-9`),
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:924` — - [2026-08-28][BA-4] **DONE — all four clauses addressed; the invariant is proven, the human-in-a-window
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:934` — edit, not a `BA-4` one.
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:936` — - **2026-09-02 — `BA-4` landed on `main` and flipped `done` in `dag.json`.** The code described above is merged (validated: `tests/test_browse_credential_handoff.py` 62/62 on `main`, matching the 62 tests recorded here); the 🟡 table mark and the dag status are updated in the same change that writ…
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:984` — ever survive in a link's QUERY: that half was closed before BA-4, and a screen there would have
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:987` — one real gap, and BA-4 adds a *rail* on the query half instead of duplicating it: a later session
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1036` — user authenticates in a headful window". **Core does not launch Chrome, and BA-4 does not change

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `§5`, `Session 4`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:356` — **The three make-or-break enrichments + the actuator pattern.** Design evidence: browse agents live or die on context cost (~20x reduction from compressed outlines + screenshots-as-paths; unusable without), on the user being able to *watch* an unattended browse, and on auth that never transits th…
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:381` — - **(c) Auth handoff honestly scoped** — §5 kept, sharpened: the user completes logins once in a visible window; session state (cookies/localStorage) is captured to the per-site profile (§5.1) with the profile-encryption key held in the credential store (`save_credential("BROWSE_PROFILE_KEY_<slug…
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:402` — **Scope boundary against §5 — read this before writing code.** This plan already owns a credential-handoff model, and it is a *good* one: per-site persistent Chrome profiles under `~/.personalclaw/browse/profiles/<site_slug>/` (§5.1), a `request_login` action that parks the run and opens a **head…
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:404` — | | **Gateway profile** (§5, already designed) | **User browser** (this amendment) |
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:417` — - **(a) A second execution target behind one selector.** The `browse` action provider gains a `target: "gateway" | "user_browser"` field (default `"gateway"` — the existing §5 path, so nothing changes for shipped behavior). `user_browser` requires the companion extension (below) to be connected; …
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:425` — 4. **No credentials are ever read, stored, or transmitted** — the extension drives an already-authenticated browser; it never touches a password field's value, a 2FA code, or a cookie jar. This is §5.2's invariant, restated for the new target and equally non-negotiable.
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:576` — browser instance lifecycle with the interactive MCP", which reads as *attach*, while §5.1 gives
- `§5` `docs/roadmap/plans/BROWSE-AUTOMATION.md:758` — nothing. Launching Chrome with a persistent per-site profile is BA-4's slice (§5.1) and §10's
- `Session 4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:322` — - **Session 4 — credential handoff + integration + validation:** persistent profile management; `request_login` needs-input flow; session validity check; WATCHED-SOURCES escalation wiring; deep-research template integration; as-a-user validation (browse a real site, fill a real form, authenticate…
- `Session 4` `docs/roadmap/plans/BROWSE-AUTOMATION.md:759` — Session 4.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
