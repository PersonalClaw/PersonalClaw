<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `IA2-5` — Reference resolver pair (inbox_item + loop_finding) end-to-end from real pages

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INVESTIGATE-ANYWHERE](../plans/INVESTIGATE-ANYWHERE.md) (`IA2`, plan status `done`) |
| **Atom table** | [`../atomic/IA2.md`](../atomic/IA2.md) |
| **Execution position** | #290 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

S1 T1.5 + V1 — reference pair + validation

## Done when

inbox_item (fenced body via inbox render path) and loop_finding (finding + cycle verdict) both investigable end-to-end; mounted on InboxDetail verdict row and LoopPeek finding header; V1 as-a-user pass (context chip, grounded answer, ask-mode mutation denied). DEVIATION: resolvers registered in investigate.py (not inbox_service/loop module) to avoid import-order coupling

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`IA2-1`](IA2-1.md) | intra-repo | `done` |
| [`IA2-4`](IA2-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T1.5`. Those tokens appear in its source plan at:

- `T1.5` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:107` — | T1.5 | First two resolvers as the reference pair: `inbox_item` (fenced body via the inbox render path) + `loop_finding` (finding + cycle verdict) | `investigate.py` registrations in `inbox_service.py`, loop module | both kinds investigable end-to-end from their real pages |
- `T1.5` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:133` — - [2026-07-27][S1] DONE: the primitive (T1.1-T1.5). **T1.1** `src/personalclaw/investigate.py`: `InvestigateContext` envelope + resolver registry (`register_investigate_resolver`/`resolve`; unknown KIND raises KeyError → the route's 400, unknown ENTITY returns None → 404), snapshot cap 8KB with a…
- `T1.5` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:134` — - [2026-07-27][S1] DEVIATION (registration locality): the plan's T1.5 places resolver registrations "in `inbox_service.py`, loop module"; they live in `investigate.py` instead — importing the module (which the route handler always does) guarantees registration with zero import-order coupling to i…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
