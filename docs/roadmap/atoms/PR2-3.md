<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PR2-3` — Mid-turn message handling — queue + cancel-and-replace + ActiveJobTracker

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-RESILIENCE](../plans/PLATFORM-RESILIENCE.md) (`PR2`, plan status `done`) |
| **Atom table** | [`../atomic/PR2.md`](../atomic/PR2.md) |
| **Execution position** | #482 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§6 Mid-Turn Message Handling — Prompt Queue + Cancel-and-Replace

## Done when

resilience/active_jobs.py tracker (register at turn start / clear at turn end); cancel_and_replace policy on an interactive channel soft-cancels via stop_turn(preserve_queue=True), broadcasts superseded, and queues the new message; queue policy delivers next turn; loop/cron/subagent sessions never cancelled (criterion #7); mid_turn_policy + cancel_replace_min_interval_secs round-trip.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PR2-2`](PR2-2.md) | intra-repo | `done` |

### Unblocks (1)

[`PR2-6`](PR2-6.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 8 (PR2) — observed

**Code evidence:**

- tests/test_mid_turn_steer.py passes (part of the 152-passed run)

**Notes:** PARTIAL: queue, cancel-and-replace and the ACP path are all MID-TURN behaviours — they need a turn in flight to interrupt, which needs a model. The three policies are exactly the kind of thing a suite proves better than a happy-path UI run, and it passes.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§6`. Those tokens appear in its source plan at:

- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:12` — **Status:** DONE — all 6 sessions landed (S1-S5 2026-07-25; S6 mid-turn steering 2026-07-29). S1 Doctor core (§1) · S2 no-model degraded contract (§5) · S3 mid-turn message handling (§6) · S4 confirm-gated fixes + trust simulators + crash capture (§2/§3.1/§3.2/§6.5) · S5 health-scored remediation…
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:14` — **Wave:** split — §1-§3 (doctor probes + read-only surface) and §5 (degraded contract) are Wave 0/1 invariants (every existing surface touches models; offline behavior must be designed before the engine multiplies unattended runs); §6 (mid-turn handling) is Wave 1, independent; §4 (remediation en…
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:15` — **Depends on:** nothing for §1-§3/§5/§6 (Wave-0-compatible). §4 depends on AUTONOMY-GUARDRAILS (SpendMeter + model-call audit for cost caps) and prefers AUTOMATION-SUBSTRATE (runs as an adaptive-cadence trigger once triggers.json exists; hangs off the heartbeat until then).
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:27` — - **NEW-29** (mid-turn message handling: prompt queue + optional cancel-and-replace per channel via a per-channel active-job tracker with cancellation propagation) → §6. Sources: `air-dev` (prompt queue for busy sessions), `localagi` (cancel-previous-on-new-message per conversation_id).
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:120` — **Overlap honored:** the *scoring machinery* and its evolution (per-arm confidence, slot allocator, surfacing_events measurement, near-miss ledger) are owned by the approved **LEARN-R4/R7/R15** (WORKFLOWS-V2-LEARNING-FLYWHEEL §2.4/§2.5) and the observability panel by **LEARN-R14** (§6 there). Thi…
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:246` — The composer already shows queued items with per-item cancel (`queue_push/pop/cancel`). Added: a small "will replace the current answer" hint when the active session's policy is cancel_and_replace and a turn is in flight (read from the tracker via the session snapshot the chat handlers already re…
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:289` — - **FE:** Doctor tab via the Settings SUBPAGES registry; degraded chip in the shell; new API methods land in `lib/api.ts` (one flat file — high merge-conflict surface, so this plan's endpoints ship in ONE api.ts patch per session); no new WS envelope types (§6.4).
- `§6` `docs/roadmap/plans/PLATFORM-RESILIENCE.md:303` — Sequencing: §1-§3 + §5 + §6 have no hard dependencies (Wave 0/1); §4 after AUTONOMY-GUARDRAILS Session 2 (SpendMeter) and ideally with AUTOMATION-SUBSTRATE's trigger store for the adaptive-cadence trigger form.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
