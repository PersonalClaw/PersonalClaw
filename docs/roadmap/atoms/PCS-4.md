<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PCS-4` — Anthropic EXPLICIT translation (cache_control on block-shaped system=) + rails test

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROMPT-CACHE-SUBSTRATE](../plans/PROMPT-CACHE-SUBSTRATE.md) (`PCS`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PCS.md`](../atomic/PCS.md) |
| **Execution position** | #412 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1286 |

## Intent — what this is for

Session 2 / T2.3+T2.5; §C4 (edge-only translation); soul guardrail 1

## Done when

Hinted span translated to cache_control {type: ephemeral} on its last block, including the block-shaped system= (list of text blocks) required by §C4; adapter declares EXPLICIT; an unhinted list produces today's byte-identical request kwargs; rails sweep asserts cache_control/ephemeral appear ONLY in llm/anthropic.py and fails on a temporarily injected violation.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PCS-1`](PCS-1.md) | intra-repo | `done` |
| [`PCS-3`](PCS-3.md) | intra-repo | `done` |

### Unblocks (2)

[`PCS-7`](PCS-7.md) (`done`), [`PCS-8`](PCS-8.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 43 (PCS) — vendor rail falsified against the real tree; toggle driven

**Code evidence:**

- 🔑 FALSIFIED AGAINST THE REAL TREE, which is this atom's own acceptance clause ('fails on a temporarily injected violation'). Wrote one core module containing a cache_control/ephemeral literal: the sweep FAILED naming it — 'vendor cache syntax escaped the provider edge into: [_pcs_probe.py] — translate at the adapter, keep core neutral' — AND the vacuity test caught it independently. Removed it; green again
- MEASURED: cache_control appears in exactly one core file (llm/anthropic.py), which is the allowlist's single entry
- 🔑 THE RAIL HAS A VACUITY FLOOR AND SAYS WHY: test_the_sweep_is_not_vacuous asserts the patterns still match the one permitted file, because 'a rename of Anthropic's marker would leave a rail that matches nothing and passes forever'
- the wire-translation suite covers the shapes that actually break: the marker lands on the LAST block not the first, a block-shaped system= is produced, a hinted span never mutates the caller's dicts, a hint on the volatile note is ignored, and neither the neutral hint key nor the generation marker ever reaches the wire
- 78 across marker/wire-translation/cache-usage/single-store/pricing-savings/citation-rail; bedrock-models app 39/39

**Notes:** 🔑 FOURTH SELF-FALSIFYING RAIL IN THIS CAMPAIGN (after the skills-suppression census, the provider-boundary residue sweep and the pairing-mechanism census), and the vacuity reasoning is stated in nearly the same words each time. That consistency is now strong enough to read as a house rule rather than four coincidences.

## Recorded history

### Commits (3)

- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `5acb2a503` 2026-08-14 — feat(llm): PCS-4 translate the neutral cache hint to Anthropic cache_control
- `ef63529bb` 2026-08-06 — feat(llm): PCS-3 neutral prompt-cache marker + native-loop middleware (§C1/§C4) (#885)

### Named by atom id in the roadmap prose (12)

- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:298` — - **PCS-4 DONE — the Anthropic EXPLICIT translation (§C4, T2.3 + T2.5).** `llm/anthropic.py` is now
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:310` — **Guardrail 2 held and was falsified in both directions:** an unhinted list produces the pre-PCS-4
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:315` — **Scope:** PCS-4 only. `prompt_cache_enabled` stays PCS-5's (no config field added here, so the
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:327` — - **PCS-4 DISCOVERY — the rails sweep must match vendor LITERALS, not the word "ephemeral".** The
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:337` — - **PCS-4 DISCOVERY — §C4's "the marker normally attaches to the last system block" is not what the
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:349` — - **PCS-4 DECISION (not a deviation) — block-shaped `system=` is ONE text block, not one per part.**
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:356` — - **PCS-4 DISCOVERY — PCS-3's "OpenAI adapter declares AUTOMATIC" clause is UNMET on `main`.**
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:360` — a defect, and it is PCS-3's row rather than PCS-4's. Left untouched here so the miss stays visible;
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:364` — - **PCS-4 — no live cache hit observed; the evidence here is kwargs-level.** The marker was verified
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:388` — **DISCOVERY — PCS-4 is NOT on `origin/main`.** The session brief stated PCS-4 had shipped as
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:390` — `cache_control` translation, no `CACHE_HINT_KEY` reader and no `EXPLICIT` declaration; the PCS-4
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:394` — see a hint. PCS-4 inherits the switch with zero further work when it merges.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.3`, `T2.5`, `Session 2`. Those tokens appear in its source plan at:

- `T2.3` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:243` — | T2.3 | Anthropic translation: hinted span → `cache_control: {"type": "ephemeral"}` on its last block, incl. the **block-shaped `system=`** required by §C4; declares `EXPLICIT`; test asserts the wire shape and that an unhinted list produces today's exact request kwargs | `llm/anthropic.py`, test…
- `T2.3` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:298` — - **PCS-4 DONE — the Anthropic EXPLICIT translation (§C4, T2.3 + T2.5).** `llm/anthropic.py` is now
- `T2.5` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:245` — | T2.5 | Rails test: `cache_control` / `"ephemeral"` appear ONLY in `llm/anthropic.py` — a sweep over `src/` fails on any other occurrence, mirroring the existing provider-boundary residue sweep. Prove it fails by temporarily introducing a violation | `tests/` (beside the existing rails sweep) | …
- `T2.5` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:298` — - **PCS-4 DONE — the Anthropic EXPLICIT translation (§C4, T2.3 + T2.5).** `llm/anthropic.py` is now
- `Session 2` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:237` — ### Session 2 — The neutral marker + provider adoption

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
