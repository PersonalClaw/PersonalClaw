<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DFE-4` — Binary artifact write path + model read/render endpoints

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-FIDELITY-EDITOR](../plans/DOCUMENT-FIDELITY-EDITOR.md) (`DFE`, plan status `done`) |
| **Atom table** | [`../atomic/DFE.md`](../atomic/DFE.md) |
| **Execution position** | #163 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2020 |

## Intent — what this is for

Session 2 T2.1–T2.2; §C3 (PUT /api/artifacts/{slug}/raw: BINARY_KINDS guard, MAX_BINARY_CONTENT_BYTES before buffering, mime/kind match, required If-Match→409, SEL audit); GET /api/artifacts/{slug}/model + a server-side re-render endpoint

## Done when

a stale If-Match is refused 409; an oversized body is refused before buffering; a mime/kind mismatch is refused; an accepted write bumps exactly one version and logs one SEL row with byte count; GET /model returns parsed model + loss report and the render round trip is fully server-side (browser never sees OOXML); offline route reference regenerated.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DFE-3`](DFE-3.md) | intra-repo | `done` |

### Unblocks (1)

[`DFE-5`](DFE-5.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `b14289fda` 2026-08-26 — feat(files): DFE-6 layout control — page size, per-edge margins, paragraph indents, headers/footers
- `827fcbdd1` 2026-08-25 — docs(roadmap): cite the batch PR on DFE-4
- `7061cc724` 2026-08-24 — chore(reference): re-render the offline index for the batch union
- `b77479180` 2026-08-24 — docs(roadmap): DFE-4 done — binary write path + model read/render endpoints
- `dba50faf8` 2026-08-24 — feat(artifacts): DFE-4 binary write path + document model read/render endpoints

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:819` — Flipping DFE-3 does NOT complete this plan — DFE-4..DFE-8 remain `todo`, so the plan status is unchanged.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:822` — ## Execution log — DFE-4 (binary write path + model read/render endpoints) — **DONE**
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:824` — - [2026-08-24][S2 · atom `DFE-4`] **DONE.** Three routes on one shared guard chain in
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1012` — is DFE-4's deliberate parser-gated design.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1045` — - **This atom closes a defect DFE-4 recorded and could not fix.** DFE-4's log states: "a freshly
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1176` — still refuses non-docx kinds (`_MODEL_KINDS`), which is DFE-4's deliberate parser-gated design.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1268` — - **DISCOVERY — the plan's `parsers/xlsx_parser.py` path is wrong, as DFE-4 already recorded for
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1479` — `web/src/ui/content/documentEditing.ts`'s `EDITORS` table + its comment. One `DFE-4` line changed:
- **source plan** `docs/roadmap/plans/PLATFORM-RESILIENCE.md:1149` — **Rebase conflicts (DFE-4 landed on `main` mid-run), resolved:** `http_errors.py` had two disjoint

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.1`, `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:417` — | T2.1 | §C3 `PUT /api/artifacts/{slug}/raw` with every guard incl. **required `If-Match`**; SEL audit; regenerate the offline route reference | `artifacts/handlers.py`, `dashboard/server.py`, `src/personalclaw/reference/`, tests | a stale `If-Match` is refused 409; an oversized body is refused b…
- `T2.2` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:418` — | T2.2 | A model read/render pair for the editor: `GET /api/artifacts/{slug}/model` (parsed model + loss report) and a render endpoint that re-renders a posted model through the existing writer | `artifacts/handlers.py`, tests | the round trip is server-side; the browser never sees OOXML |
- `Session 2` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:413` — ### Session 2 — The binary write path + the editing surface

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
