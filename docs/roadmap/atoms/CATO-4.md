<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-4` — Wire loop, cron, channel, and cli turn-completion attribution

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #100 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

C2 — Attribution (loop/cron/channel/cli rows); Session 1 T1.4

## Done when

Each of the loop-worker, schedule, channel, and CLI turn-completion paths writes a row with the correct source string; each source appears in rollup(group_by='source') after a real turn on that path; one attribution test per source

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-1`](CATO-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 42 (CATO) — Usage panel driven, period round-trip survived a reload

**Code evidence:**

- MEASURED all four write sites the clause enumerates, each through the shared seam: gateway.py:2170 and :3092 (the loop-worker and schedule paths), subagent.py:2235, cli_chat.py:63 — plus cli_run.py:492-495 reading totals back for the headless CLI's own report
- the source axis is a documented closed set on TurnUsage (:47): 'chat | loop | cron | subagent | channel | cli | background'
- constants.py:25 records that ledger rows are KEYED consistently with the session-key convention, so the source axis and the session axis cannot disagree about the same turn
- test_usage_ledger + test_usage_routes + test_usage_reachable_purposes 57/57; test_anthropic_cache_usage 5/5; test_api_manifest_drift 8/8; web usage + turn-telemetry 30/30

**Driven in the UI:** Not driven: each source needs a real turn on that path.

**Notes:** Two write sites live in gateway.py rather than in the loop and schedule modules themselves, which is worth recording for a future reader: the attribution rides the gateway's terminal-event handling, so a new unattended path gets a row by reaching that handler rather than by remembering to call the ledger.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:201` — the loop-cron / channel-cli sites (C2 remainder = CATO-4) and the surfaces (S2 = CATO-5..8) remain,
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:207` — - **CATO-4 DONE — the remaining C2 write-sites (background / channel / cron / cli).** Root finding:

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.4` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:134` — | T1.4 | Wire `loop`, `cron`, `channel`, `cli` turn-completion paths with the right `source`; one test per source proving attribution | the loop-worker, schedule, channel, and CLI turn paths, tests | each source appears in `rollup(group_by="source")` after a real turn on that path |
- `Session 1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:127` — ### Session 1 — The durable ledger

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
