<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AR2-4` — AgentsRoutingConfig 5-point wiring + suppression store (entity_settings/agent_routing.json) + dismiss/unmute/status routes

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AGENT-ROUTING](../plans/AGENT-ROUTING.md) (`AR2`, plan status `done`) |
| **Atom table** | [`../atomic/AR2.md`](../atomic/AR2.md) |
| **Execution position** | #65 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #feature-agent-routing |

## Intent — what this is for

Design S2 (suppression memory) + Config; Contracts C3, C4, C5; Task breakdown Session 1 T1.4

## Done when

test_config_roundtrip.py green; dismiss×1 → 24h cooldown honored; dismiss×3 → muted-until-unmute; corrupt store file fails OPEN (nothing suppressed + warn); routes registered before /api/agents/{name} using §2.2 error envelope

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AR2-2`](AR2-2.md) | intra-repo | `done` |

### Unblocks (3)

[`AR2-5`](AR2-5.md) (`done`), [`AR2-7`](AR2-7.md) (`done`), [`AR2-8`](AR2-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.4` `docs/roadmap/plans/AGENT-ROUTING.md:125` — | T1.4 | `AgentsRoutingConfig` 5-point wiring + `_EDITABLE_CONFIG` entries + suppression store + dismiss/unmute/status routes (§2.2 envelope) | `config/loader.py`, `dashboard/handlers/core.py`, `agents/routing.py`, `dashboard/server.py` | `test_config_roundtrip.py` green; dismiss×1 → 24h cooldown…
- `Session 1` `docs/roadmap/plans/AGENT-ROUTING.md:118` — ### Session 1 — Metadata + classifier + emission (backend)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
