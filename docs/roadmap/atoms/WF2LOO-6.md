<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2LOO-6` — Close inert judge/gate controls at the dispatcher: pre-tier + judge_samples + cross_model lint

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-LOOPS-EVOLUTION](../plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md) (`WF2LOO`, plan status `done`) |
| **Atom table** | [`../atomic/WF2LOO.md`](../atomic/WF2LOO.md) |
| **Execution position** | #605 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Judge Design: Deterministic tier before/beneath every judge (R8), Typed verdict contract judge_samples median (R2c), judge isolation contract (R1); Execution log S144/S145/S146

## Done when

dispatch_gate runs _judge_pretier_screen before the model call (opt-in via an evidence binding; existence gate defaults off) short-circuiting rule-solvable rejects with failure_class (S144); judge_samples aggregated with the documented median rule over the gate's own verify.Verdict enum, tokens summed over samples, floored to 1 / clamped at MAX_JUDGE_SAMPLES=5 (S145); WFL_UNENFORCEABLE_ISOLATION lint warns when a template declares cross_model with no enforcement seam + a test asserts no shipped template makes the claim (S146). Each verified load-bearing by removal; gate green.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2LOO-1`](WF2LOO-1.md) | intra-repo | `done` |
| [`WF2LOO-3`](WF2LOO-3.md) | intra-repo | `done` |

### Unblocks (2)

[`WF2LOO-11`](WF2LOO-11.md) (`done`), [`WF2LOO-12`](WF2LOO-12.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `R8`, `R1`. Those tokens appear in its source plan at:

- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:43` — - **LOOP-R8** — Judge Design: deterministic verification tier before and beneath every LLM judge
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:113` — judge:                      # SEMANTIC: what "done/good" means (R1, R2, R3, R8)
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:144` — fallback_check: command_exit_code         # (R8) artifact_exists | command_exit_code |
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:194` — ## Judge Design: Maker/Checker With Teeth (LOOP-R1, R2, R3, R8)
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:226` — ### Deterministic tier before and beneath every judge (R8)
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:798` — | Deterministic gates (`gates.py` tristate verify, audit screen) | **(R8)** Free rule tier before every judge + `fallback_check` beneath every judge + PASS-contradicts-deterministic auto-escalation + verification ladder ordering | Burning judge tokens on rule-solvable checks / 2025 |
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:830` — - **Session 1 — Judge contract + runtime_hints spec**: the two-group hints schema; `tools_posture: verify`; typed verdict enum + constrained decoding + parse-retry; judge isolation via SubagentManager + actor-transition + proof-attachment invariants; deterministic pre-tier + `fallback_check` (R1,…
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:36` — - **LOOP-R1** — Judge Design: Maker/Checker Teeth (judge stages become act-capable, isolated, adversarial; the headline fix)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:45` — - **LOOP-R10** — Migration Path: coexistence aliases, template metadata hygiene, cockpit live-follow key equivalence
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:46` — - **LOOP-R11** — Per-Kind Templates §§2/4/5: design divergence, test triage, TDD doctrine, diff-edit refine
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:47` — - **LOOP-R12** — Per-Kind Templates §6 (new): bundled `diagnose` template (4-layer failure taxonomy over the Run Ledger)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:48` — - **LOOP-R13** — Migration Checklist: reactive compaction with one-shot guard + proactive topic-segmented compression
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:49` — - **LOOP-R14** — Engine Behaviors: mid-run human steering channel (interrupt queue, judge-comment triage, per-project judge-prompt overrides)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:50` — - **LOOP-R15** — Per-Kind Templates §1 (monitor variant): bounded loop self-scheduling via automation-substrate triggers
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-LOOPS-EVOLUTION.md:83` — | `set_onetime_task` / `set_recurring_task` tools (R15) | New tool module registered in `mcp_core._TOOL_MODULES` + `tool_providers/registry.py` (same pattern as `mcp_workflows.py`); the triggers themselves are AUTOMATION-SUBSTRATE entities |

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
