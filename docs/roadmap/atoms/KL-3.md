<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-3` — S2 tags taxonomy: authoritative tags + item_tags tables, JSON backfill, hierarchy UI

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #304 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 — T2.2 (tags taxonomy); Design §S2; Risks (tag-migration reconciliation)

## Done when

tags moved into authoritative tags + item_tags (surrogate id, parent_id self-FK with ON DELETE SET NULL, computed counts), JSON tags column DROPPED; single-pass idempotent backfill guarded on column presence; FTS sources from items_fts_src view so rebuild preserves recall; cycle guard rejects self/multi-hop parenting; GET /api/knowledge/tag-tree + Tags view render the hierarchy with live counts; external list[str] tag contract unchanged

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`KL-1`](KL-1.md) | intra-repo | `done` |

### Unblocks (1)

[`KL-5`](KL-5.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 5 (KL) — DRIVEN in a real browser

**Code evidence:**

- covered by the 234-passed KL run

**Driven in the UI:** Typed 'audit-probe' into the note form's 'Add a tag, Enter' box and pressed Enter: it became a chip carrying its own 'Remove audit-probe' control (so tag entry AND removal are both reachable). After saving and RELOADING, the tag is still attached to the item and renders as a `button "audit-probe"` — a clickable affordance, which is the taxonomy's filter surface rather than a decorative label.

**Notes:** Tag entry sits inside the CREATE flow, which is the strongest place for it: an item cannot be filed untagged by accident of a two-step UI.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:13` — COMPLETE** — T2.1 curation display/filters, T2.2 tags taxonomy, T2.3 bulk ops, all 2026-07-29.
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:95` — | T2.2 | Tags taxonomy: `tags` table, JSON-tags→references migration step, hierarchy + usage counts, tag management UI | `knowledge/store.py`, migration, tag UI | existing tags appear with counts; nesting works; old JSON still readable during dual-path |
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:298` — **NOT done: T2.1's remainder and T2.2.** T2.1 is ~85% shipped by S1 — what's left is
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:304` — **T2.2 (tags taxonomy) is NOT startable as written:** its done-when requires a
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:314` — because an execution log is a record, not a wiki). T2.2 **shipped** later the same day:
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:365` — **T2.1 is now complete.** Remaining in S2: **T2.2 (tags taxonomy)**, which still needs
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:367` — - 2026-07-29 — **DONE (S2: T2.2 tags taxonomy).** Tags moved from a JSON column on
- `T2.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:468` — **S2 is now COMPLETE** (T2.1 + T2.2 + T2.3). Remaining in the plan: Session 3
- `Session 2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:12` — **Status:** IN PROGRESS — Session 1 (collections + item curation) shipped 2026-07-29; **Session 2
- `Session 2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:90` — ### Session 2 — Curation + taxonomy + bulk

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
