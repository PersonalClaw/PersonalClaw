<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `LMMV-2` — Session 1 — Catalog & contract: CapabilityMatrix, runtime-contract/license fields, declarative catalog.json loader + truncation detector, available payload + ModelsPanel chips

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [LOCAL-MODEL-MANAGER-V2](../plans/LOCAL-MODEL-MANAGER-V2.md) (`LMMV`, plan status `in_progress`) |
| **Atom table** | [`../atomic/LMMV.md`](../atomic/LMMV.md) |
| **Execution position** | #321 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #760 |

## Intent — what this is for

§2.1 Structured capability matrix on LocalModel; §2.2 Runtime-contract metadata + license surfacing; §2.3 Declarative model-card catalog with truncation detection

## Done when

CapabilityMatrix (optional, default None) plus runtime/runtime_contract/license/non_commercial/context_tokens/output_tokens/io_mime fields added additively to LocalModel; LocalModelProvider._models_from_catalog() reads app-owned catalog.json, filters platforms against host, and computes downloaded via the §4.4 probe; truncation detector flags on-disk <60% of size_mb (with config_only escape hatch) as integrity:truncated + Repair affordance; the fixed-catalog five migrated to catalog.json (dropping a new entry makes a model appear/download/bind/RUN, deprecated shows a chip without breaking bindings); GET /api/models/available serializes the new fields; ModelsPanel renders matrix/license/non-commercial/deprecation/integrity chips (Success Criteria 6 & 7).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`LMMV-1`](LMMV-1.md) | intra-repo | `done` |

### Unblocks (1)

[`LMMV-7`](LMMV-7.md) (`blocked`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 23 (LMMV) — code and tests observed on this machine

**Code evidence:**

- MEASURED: CapabilityMatrix AND runtime_contract both present in local_models/provider.py — the two symbols the clause names
- 309 tests pass across the local-model / fit / sidecar / download modules (1 unrelated skip)

**Notes:** STATUS MISMATCH resolved in favour of done: the plan's detail section says `todo` while its table says ✅ and dag.json says `done`. The code backs done, so the detail section is stale prose. Recorded in issue #2763 alongside its siblings rather than filed separately.

**Follow-ups filed:**

- issue #2763 (LMMV-4 recorded done with none of its artifacts present; three sibling status mismatches resolved)

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:625` — - 2026-08-05 — **DONE (LMMV-2 / Session 1 — Catalog & contract). The core mechanism, proven
- **source plan** `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:732` — same cross-repo boundary LMMV-2 recorded: `git ls-files apps/` is still 0 here, so no provider

### Its plan's own record, via the scope references (18 line(s))

This atom's scope cites `§2.1`, `§2.2`, `§2.3`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:33` — - **NEW-8 am.(a)** — per-model structured capability matrix → §2.1
- `§2.1` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:258` — - **No new inference use-cases** (`audio_qa`, joint-transcriber ABC) — the §2.1 matrix makes them *declarable*; adding the use-case axis is a separate plan.
- `§2.1` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:628` — Landed §2.1/§2.2/§2.3 as an additive contract on `local_models/provider.py`: a
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:32` — - **NEW-8** (core) — sidecar isolation → §3; resumable install jobs + child-reported memory → §3.2, §7; canonical server-side download progress with poll/cancel/.part atomic writes → §4; gated-repo error translation → §4.3; three-source HF token cascade with whoami validation → §5; per-provider r…
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:34` — - **NEW-8 am.(b)** — model-aware per-model context budgets → §2.2
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:60` — - **`capableModels` is a FRONTEND function** (`web/src/pages/settings/ModelsPanel.tsx:43`) filtering `GET /api/models/available` rows per use-case and synthesizing rows for bound-but-absent models (the phantom-binding fix). There is no backend symbol by that name — license/deprecation surfacing a…
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:326` — `origin/main` (`addbc332`, #1628) before writing anything: the §2.2 budget helper
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:421` — completion, which also validates §2.2's consumer as a user: it goes through
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:472` — **§2.2 the budget helper, and where the hardcoded constants actually were.** New
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:510` — `COMPACT_AT_FRACTION` and the head/tail protection are untouched. This plan's own §2.2
- `§2.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:560` — undeclared card (4096) and a hosted ref (4096) — i.e. the consumer half of §2.2 is
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:32` — - **NEW-8** (core) — sidecar isolation → §3; resumable install jobs + child-reported memory → §3.2, §7; canonical server-side download progress with poll/cancel/.part atomic writes → §4; gated-repo error translation → §4.3; three-source HF token cascade with whoami validation → §5; per-provider r…
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:35` — - **NEW-8 am.(c)** — declarative model-card data files (drop-a-file add/deprecate) → §2.3
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:45` — - **Detection/delete class** (ST checked only the `model.save()` layout and missed `models--…`; a destructive test deleted the user's real bound L6 model) → a **declarative catalog with expected sizes and a truncation/weights-missing detector**, plus one shared multi-layout probe helper (§2.3, §4…
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:102` — `LocalModel` gains runtime-contract fields (all optional, sourced from the model card §2.3):
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:173` — - **Byte-level progress where the fetch allows it:** providers may implement optional `download_model_ex(name, progress_cb)` reporting real byte counts; the existing `_dir_size(cache_root)` poller stays as the universal fallback (it is honest-but-coarse, and correct for HF snapshot downloads that…
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:191` — The delete-detection bug-class gets a single shared helper instead of per-provider guesses: `local_models/layouts.py` with `is_downloaded(cache_root, model) -> bool` and `delete_all_layouts(cache_root, model) -> list[Path]` probing EVERY layout a download can produce — the provider's own `save()`…
- `§2.3` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:628` — Landed §2.1/§2.2/§2.3 as an additive contract on `local_models/provider.py`: a

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
