<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-4` — Self-describing MCP control bridge — loopback FE semantic actions

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #217 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1978 |

## Intent — what this is for

§4 (Dialect 3): random-port loopback bridge, control_bridge.json discovery file, typed self-describing action registry, server-side requiresConfirmation flow. Session 3 (bridge half).

## Done when

Loopback-only (allow_remote-exempt) bridge on a random ephemeral port via its own aiohttp runner; control_bridge.json (0600, atomic_write, rewritten each boot, deleted on clean shutdown) carries {port,url,token_ref,schema_version,actions_digest}; action registry emits {name,params_schema,sideEffect,requiresConfirmation,description} for open_cockpit/read_transcript/list_automations/create_task/toggle_automation/run_trigger_dry/notify; confirm-flagged actions return {status:needs_confirmation,confirm_token} + DashboardState.notify, resolved by user in dashboard or `personalclaw inbound confirm <token>`; write actions call the same internal FE handlers (no parallel mutation path); no destructive actions in v1; every call audit-lined (Success Criterion 9).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EA-1`](EA-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `2b7cd1015` 2026-08-24 — docs(inbound): correct the fence's provenance rationale that EA-4 made stale
- `7b51f8806` 2026-08-24 — fix(inbound): route control-bridge user content through the ONE fence
- `928823912` 2026-08-24 — fix(inbound): EA-4 pin-filter the bridge catalogue and unblind the envelope census
- `2d7f5b6b6` 2026-08-24 — docs(roadmap): cite batch PR #1978 on DFE-2/EA-4 and de-orphan ACP-AGENT-PARITY
- `ee7c7a6cc` 2026-08-24 — feat(inbound): EA-4 self-describing loopback control bridge

### Named by atom id in the roadmap prose (13)

- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:377` — ## Execution log — `EA-4` (§4 Dialect 3: the self-describing control bridge)
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:379` — - [2026-08-24][EA-4] ✅ **DONE — `src/personalclaw/inbound/bridge.py`, its own runner, wired into the
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:384` — - [2026-08-24][EA-4] **Four decisions, each answering a specific failure rather than a preference.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:396` — - [2026-08-24][EA-4] **The v1 registry is exactly §4's seven**, self-described as
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:406` — - [2026-08-24][EA-4] **No parallel mutation path, pinned at the source level.** `create_task` goes
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:411` — - [2026-08-24][EA-4] 🔴 **Two shape errors caught by reading the code rather than by a green test.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:417` — - [2026-08-24][EA-4] **`personalclaw inbound confirm <token>` goes through the bridge, not around it.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:421` — - [2026-08-24][EA-4] 🔴 **My own test had a dead assertion, and the falsification is what exposed it.**
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:428` — - [2026-08-24][EA-4] **Falsified, each restored from a file copy.** Neutering the confirm branch
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:432` — - [2026-08-24][EA-4] **Not in this atom, deliberately:** the dashboard-side confirm affordance (a
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:439` — - [2026-08-24][EA-4] 🔴 **The self-describing catalogue leaked the actions the caller could not
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:454` — - [2026-08-24][EA-4] 🔴 **A rail defect, not a bridge defect: the wire-envelope census was blind to
- **source plan** `docs/roadmap/plans/EXTERNAL-ACCESS.md:469` — - [2026-08-24][EA-4] **Falsified, nine mutations, each restored from a file copy.** The decisive pair:

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§4`, `Session 3`. Those tokens appear in its source plan at:

- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:27` — - **NEW-10 amendment 1** (self-describing MCP control bridge over the FE's semantic actions: random-port bearer-token localhost bridge + discovery file, sideEffect labels + requiresConfirmation flags, so external agents drive PersonalClaw without DOM scraping) → §4.
- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:57` — - **Loopback by default.** The layer binds inside the existing gateway process (no second listener); non-loopback *peers* are rejected per-surface unless `external_access.<surface>.allow_remote` is explicitly true AND `external_access.public_url` is set — the exact-Host/Origin-match boundary ("th…
- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:73` — - **Query-only with no path to writes** on the outward read surfaces (§3): the MCP server's tool table is a hand-curated allowlist of read-only operations; there is no generic tool passthrough to `_aggregated_call_tool` (which includes write tools) and **an inbound request can never trigger a mig…
- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:251` — - **Session 3 — MCP server + control bridge (§3, §4):** streamable-HTTP MCP mount, the five curated tools with per-client subsetting + scope pins, result caps/framing; the loopback bridge (random port, discovery file, action registry with sideEffect/requiresConfirmation, server-side confirm flow).
- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:377` — ## Execution log — `EA-4` (§4 Dialect 3: the self-describing control bridge)
- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:396` — - [2026-08-24][EA-4] **The v1 registry is exactly §4's seven**, self-described as
- `§4` `docs/roadmap/plans/EXTERNAL-ACCESS.md:434` — everything it needs; §4's own wording offers the dashboard *or* the CLI, and the CLI path is
- `Session 3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:251` — - **Session 3 — MCP server + control bridge (§3, §4):** streamable-HTTP MCP mount, the five curated tools with per-client subsetting + scope pins, result caps/framing; the loopback bridge (random port, discovery file, action registry with sideEffect/requiresConfirmation, server-side confirm flow).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
