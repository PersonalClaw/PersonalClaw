<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DAS-4` — Backups settings frontend panel (Session 2c)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DURABILITY-AND-SYNC](../plans/DURABILITY-AND-SYNC.md) (`DAS`, plan status `done`) |
| **Atom table** | [`../atomic/DAS.md`](../atomic/DAS.md) |
| **Execution position** | #142 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§3 + Provider & Config Plug-in Map (write-path leg / _EDITABLE_CONFIG + FE); Implementation Order Session 2 (surface)

## Done when

five durability.* dot-paths added to _EDITABLE_CONFIG PATCH allowlist (snapshot_dir deliberately excluded, pinned by test); Settings → Backups panel consumes status/snapshots/run endpoints; five-point config round trip (incl. False surviving _guard_flag) validated in a real browser on an isolated dev home; retention caps bounded; web typecheck+vitest+build green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DAS-3`](DAS-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (16 line(s))

This atom's scope cites `§3`, `Session 2`. Those tokens appear in its source plan at:

- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:28` — - **NEW-4** — durable state backup + multi-machine sync core (JSONL shards + SHA manifest + validate; pull→merge-import→export-union→push; boot-started scheduled self-backup with rolling backups + restore endpoints; git-versioned memory snapshots; encrypted-intents crypto for machine exchange) → …
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:94` — - **`personalclaw backup validate`:** manifest well-formed → every shard exists → bytes/rows/sha match → every row parses → sqlite dumps re-import cleanly into a scratch DB. Non-zero exit for CI/cron use. This is also the restore-drill core (§3).
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:170` — - **Roots:** `config.json` (+ entity_settings), `skills/`, the memory markdown workspace (`workspace/memory`, `workspace/_ext/`, `plan_memory/`), `prompts/` + `prompt_snippets/`, and `projects/<id>/context/`. Each root is (or shares) a local git repo under the `state-history` umbrella (§3).
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:186` — - `GET /api/durability/archive` + `POST /api/durability/archive/{id}/restore` — the snapshot list/restore pair (§3) exposed as the archive browser: date, size, per-domain row counts (from the manifest), validate status from the last drill.
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:207` — | `snapshot.py` | **ABSORBED** (kept as the tar engine) | `VALID_COMPONENTS`/`CORE_FILES` re-derived from the inventory (§1); gains `everything`; sqlite-backup-API applied to ALL `*.db` discovered (fixes the live `knowledge.db` raw-copy hazard); WAL checkpoint, `_data_filter`, atomic tar rename, …
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:280` — - **Inventory-driven merge (extends §1/§3, no new mechanism):** `restore --mode merge` iterates `durability/inventory.py` entries and dispatches on the already-specified `merge` field — `union_by_id` (entity dirs: copy rows/files whose id is absent), `sqlite_attach_ignore` (memory/knowledge DBs —…
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:282` — - **Auto-detect default (sharpens :650):** restoring onto a non-empty home (any non-derived inventory entry present — not just `memory.db`) → propose merge, print the plan, require confirmation; `--replace` stays the explicit wipe path and keeps the `_is_gateway_running` refusal + `pre-restore-<t…
- `§3` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:302` — - [2026-07-28][S1] Remaining: S2 (shard exporter + manifest + `backup validate` + boot-started snapshot service + restore drills) and S3 (sync core + `git-sync`/`dir-sync`). §3's rolling retention tiers were NOT built this session — the existing `--keep N` remains; the tiered N-nightly/M-weekly/Y…
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:12` — **Status:** IN PROGRESS — Session 1 (inventory + audit) and Session 2 (2a shards / 2b service / 2c
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:237` — - **Session 2 — Shard exporter + manifest + validate + snapshot service.** §2 format (canonical JSONL, year shards, part-split, tombstone write-path hook, blob dir for knowledge files); `backup validate`; boot-started service with nightly tar + hourly dirty-entry shard export + hourly git commit …
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:287` — Folded into **Session 2** (which already owns the restore endpoints + `backup validate`); the merge-plan UI confirm lands with Session 5's archive browser. Session count stays 5.
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:303` — - [2026-07-28][S2a] DONE (§2 — the shard format half of Session 2): `durability/shards.py`. (a) **Deterministic export** — `export_shards()` writes canonical JSONL (sorted keys, compact separators, LF, UTF-8) one directory per inventory entry, rows sorted by id; entity dirs → `entities.jsonl`, si…
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:310` — ## Execution log — Session 2b (scheduled snapshot service)
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:390` — ## Execution log — Session 2c (the backups settings surface)
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:451` — ## Execution log — Session 2d (the declared merge with no executor)
- `Session 2` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:504` — ## Execution log — Session 2e (the restore side of the gap closure)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
