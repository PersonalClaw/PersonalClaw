<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MI-6` — Voice-clone engine spike: OmniVoice vs CosyVoice on fixtures, pick one, real zero-shot inference + resumable weights + LMM-V2 through-clone selftest

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MULTIMODAL-IO](../plans/MULTIMODAL-IO.md) (`MI`, plan status `done`) |
| **Atom table** | [`../atomic/MI.md`](../atomic/MI.md) |
| **Execution position** | #359 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

MI-2 §2.2 engine-selection remainder carved out of MI-2 (tracked informally as 'MI-2c'; minted as the next free numeric id MI-6 because the status-sync id regex is [A-Z0-9]+-\d+ and a letter suffix like MI-2c is unrepresentable). Run the OmniVoice-vs-CosyVoice bake-off on the voice fixtures, PICK ONE engine and commit the loser's rejection notes to the plan dir; stand up real zero-shot cloning inference in the apps/voice-clone-tts sidecar with resumable model-weight download; prove an LMM-V2 selftest that synthesizes THROUGH a clone reference.

## Done when

the OmniVoice-vs-CosyVoice bake-off is run on fixtures and ONE engine is chosen with the loser's rejection notes committed to the plan dir; the chosen engine performs real zero-shot cloning inference (not a stub) inside apps/voice-clone-tts with a resumable weight download that survives an interrupted fetch; an LMM-V2 selftest synthesizes a clone through a reference-audio fixture end to end; a sidecar killed mid-inference leaves the gateway up with a typed crash reason

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MI-2`](MI-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 32 (MI) — DRIVEN in a real browser

**Code evidence:**

- MEASURED: docs/roadmap/plans/MI-6-voice-engine-bakeoff.md exists — the loser's notes ARE committed to the plan dir as the clause requires — and it states the verdict up front: 'Verdict up front: ship k2-fsa OmniVoice. Reject FunAudioLLM CosyVoice. Weighted score 0.875 vs 0.662 over the kept criteria'. The scorecard is reproducible: 'it is pure, offline, and takes no model'
- tests/test_voice_engine_bakeoff.py passes 12/12 in this tree, so the scorecard's own assertions run here rather than resting on the document
- 🔑 REAL INFERENCE, NOT A STUB, AND THE ISOLATION IS THE POINT: worker.py:1 'Sidecar worker for Voice Clone TTS — REAL OmniVoice zero-shot inference (MI-6)'; :4 it runs in 'the app's own venv, NEVER by the gateway process: the engine is torch-heavy diffusion'; :109 'import omnivoice  # heavy: torch + the diffusion stack; ONLY ever imported here'. And :125 records that the return shapes were LIVE-VALIDATED ('Live-validated shapes, most specific first') — the mark of a path that has actually run
- RESUMABLE WEIGHTS: provider.py:186 'Two mechanisms compose: huggingface_hub.snapshot_download already resumes', and :210 keeps the evidence for the next attempt — 'partial files stay for the resume; never crashes the app' — with :213 telling the user what to do ('re-run download to resume')
- core treats an interrupted fetch honestly rather than as success: local_models/layouts.py:20 'a bare <name>.part, means an interrupted fetch. REPORTING THAT AS PRESENT IS WORSE THAN…' with PARTIAL_SUFFIXES covering .part/.tmp/.incomplete/.download
- the LMM-V2 through-clone selftest exists with its own suite: dashboard/handlers/doctor.py + tests/test_doctor_clone_selftest.py
- the typed crash reason exists: local_models/sidecar.py:85-97 namespaces it for the wire as sidecar_crashed:<reason> (e.g. sidecar_crashed:signal_11), and :38 states the isolation claim precisely — 'a sidecar is a CRASH AND DEPENDENCY boundary'
- the app's own MI-6 suite passes 16/16
- 266 tests pass across the voice + screen-context suites; test_voice_engine_bakeoff 12/12; the app's MI-6 suite 16/16

**Notes:** 🪤 THE DELIVERABLE DOCUMENT IS THE STALE ARTIFACT THIS TIME — a new variant of a pattern this campaign keeps meeting. MI-6-voice-engine-bakeoff.md says in its own words that 'The rest of MI-6 (real inference …, resumable weight download, the LMM-V2 through-clone selftest, and the sidecar-kill typed crash reason) is DEFERRED TO A FOLLOW-UP RUN'. Read alone that contradicts the `done` status. But it was written DURING the bake-off and the follow-ups it predicted then landed: the plan's execution log names them (PersonalClawApps #70 for inference + resumable download, Core #2425 / merge 2ec168d00 for the selftest + crash reason), and I confirmed all four in code rather than in the log. So the atom is done and its bake-off note is a snapshot of an earlier moment. Previous cycles found a stale PLAN HEADER and a stale MODULE DOCSTRING; this is a stale sub-deliverable, and the same rule resolves all three — the code wins, and a document describing a gap that was later closed is not a defect (the flywheel wiring rail's docstring explains at length why linting for exactly this would be wrong).

## Recorded history

### Commits (7)

- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `2ec168d00` 2026-09-04 — feat(doctor): MI-6 through-clone TTS selftest + SDK sidecar facade
- `5791bf64e` 2026-09-04 — feat(evals): MI-6 footprint cell measured — 3.27 GB, verdict unchanged at 0.875 vs 0.662
- `07f007baf` 2026-09-03 — docs(security): declare huggingface.co in the egress census
- `c68a4a3eb` 2026-09-03 — feat(evals): MI-6 voice-clone engine bake-off — pick OmniVoice over CosyVoice
- `53a7334ea` 2026-09-03 — docs(roadmap): flip MI-2, DL-8, BA-5 to done post-#2370 + fix EI-3 placeholder
- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips

### Named by atom id in the roadmap prose (10)

- **source plan** `docs/roadmap/plans/MI-6-voice-engine-bakeoff.md:1` — # MI-6 — Voice-clone engine bake-off: OmniVoice vs CosyVoice
- **source plan** `docs/roadmap/plans/MI-6-voice-engine-bakeoff.md:3` — **Atom:** `MI-6` (the "MI-2c" engine-selection remainder of MULTIMODAL-IO §2.2).
- **source plan** `docs/roadmap/plans/MI-6-voice-engine-bakeoff.md:6` — loser's evaluation notes land in the plan dir, not as a second app." The rest of MI-6
- **source plan** `docs/roadmap/plans/MI-6-voice-engine-bakeoff.md:138` — The rest of MI-6, for the follow-up implementation run (this run is the bake-off only):
- **source plan** `docs/roadmap/plans/MULTIMODAL-IO.md:307` — + LMM-V2 through-clone selftest) was split out as its own atom `MI-6` and remains `todo`.
- **source plan** `docs/roadmap/plans/MULTIMODAL-IO.md:422` — **2026-09-03 — MI-6 minted (the "MI-2c" engine-selection remainder).** The OmniVoice-vs-CosyVoice
- **source plan** `docs/roadmap/plans/MULTIMODAL-IO.md:424` — the MI-2 halves land. It takes the next FREE numeric id `MI-6` rather than a letter suffix, because
- **source plan** `docs/roadmap/plans/MULTIMODAL-IO.md:430` — PR PersonalClawApps#60 still open), so `MI-6` is not startable until MI-2 completes. `MI-2` itself
- **source plan** `docs/roadmap/plans/MULTIMODAL-IO.md:431` — is deliberately NOT flipped — the engine spike is its remainder and now lives in `MI-6`.
- **source plan** `docs/roadmap/plans/MULTIMODAL-IO.md:433` — ### `MI-6` — Voice-clone engine spike: OmniVoice chosen, real zero-shot inference + through-clone selftest — DONE (2026-09-04)

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `§2.2`. Those tokens appear in its source plan at:

- `§2.2` `docs/roadmap/plans/MULTIMODAL-IO.md:306` — §2.2 OmniVoice-vs-CosyVoice engine spike (pick-one + real zero-shot inference + resumable weights
- `§2.2` `docs/roadmap/plans/MULTIMODAL-IO.md:423` — engine spike is carved out of `MI-2` §2.2 into its own atom so it can be picked up on its own once
- `§2.2` `docs/roadmap/plans/MULTIMODAL-IO.md:435` — Shipped §2.2's remainder across three PRs. Core #2436 ran the OmniVoice-vs-CosyVoice bake-off on

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
