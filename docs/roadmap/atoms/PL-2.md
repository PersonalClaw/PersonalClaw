<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PL-2` — Platform-wide WHAT/WHY/FIX AgentError envelope + append-only code registry

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-LEGIBILITY](../plans/PLATFORM-LEGIBILITY.md) (`PL`, plan status `done`) |
| **Atom table** | [`../atomic/PL.md`](../atomic/PL.md) |
| **Execution position** | #454 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2 ("Platform-wide WHAT/WHY/FIX error envelope"), §2.1 envelope, §2.2 attach seams, §8 provider-fidelity wiring; §9 Session 2

## Done when

errors.py AgentError{code,what,why,fix,suggestions} attached at ToolResult/ActionResult/three dispatch seams/ProviderResolutionError/validation rejections; ERROR_CODES seeded (ERR_TOOL_ARG_INVALID/ERR_MODEL_UNRESOLVED/ERR_HOOK_PROVIDER_UNKNOWN/ERR_ACTION_PROVIDER_FAILED); test_error_codes_append_only.py proves no removal/reword; ALLOWED_HOOK_PROVIDERS rejection carries allowed set as suggestions; FE ToolCard renders WHAT/WHY/FIX rows; PL-1 drift test's declared-error-codes guard now non-vacuous

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PL-1`](PL-1.md) | intra-repo | `done` |

### Unblocks (2)

[`PL-3`](PL-3.md) (`done`), [`PL-4`](PL-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§2`, `§2.1`, `§2.2`, `§8`, `§9`, `Session 2`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:29` — - **NEW-13 core (d)** (platform-wide WHAT/WHY/FIX error envelope on every tool/gate/provider error returned into an LLM session) → §2. Source: `harness-engineering-course` (agent-oriented error messages, OpenAI Codex pattern; L10 "converts failures into self-correction"); `meta-astryx` (append-on…
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:44` — - **Error machinery is half-built:** `ToolResult` already carries `recovery_hints` (TokenJuice appends a `tool_result_get(result_id=…)` hint, `tool_providers/projection.py:project_and_retain`), and the FE `ToolSegment` type already has a `recoveryHints` field (`web/src/pages/chat/chatTypes.ts`). …
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:77` — - Every `error_code` referenced by a tool entry MUST exist in the §2 code registry.
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:189` — - **Action provider:** exactly one — `call-app-route` (§4.2) — added to `ALLOWED_HOOK_PROVIDERS` (`src/personalclaw/validation.py`) in the same commit that registers it, or hook create/update rejects it. Its rejection message is itself a §2 envelope with the allowed set as `suggestions`.
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:204` — - **Session 2 — error envelope (§2):** `errors.py` `AgentError` + append-only `ERROR_CODES` + no-redefinition test; attach at `ToolResult`/`ActionResult`/the three dispatch seams/`ProviderResolutionError`/validation rejections; FE tool-card WHAT/WHY/FIX rows.
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:219` — | App-declared routes lie (declared but dead, or undeclared but live) | The §4.2 declared-vs-live check at enable/update files a warning notification — dishonest manifests are visible, and `agentCallable` tools 404 loudly through the proxy with a §2 envelope |
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:290` — ### 2026-07-25 — S2 (§2) WHAT/WHY/FIX error envelope — DONE
- `§2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:293` — exactly the seams §2.2 names — no new dispatch layer:
- `§2.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:293` — exactly the seams §2.2 names — no new dispatch layer:
- `§2.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:301` — `ERR_ACTION_PROVIDER_FAILED`. The module docstring pins the §2.2-vs-AgentError
- `§2.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:337` — `ERR_UPPER_SNAKE` convention (regex), no collision with the §2.2 `lowercase_snake` space,
- `§2.2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:356` — **Boundary held.** No route handler emits `AgentError`; §2.2 route-error responses are
- `§8` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:225` — | Four-wiring-points silent config drop | Explicit checklist in §8; schema reachability tests enforce (a) |
- `§9` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `Session 2` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:204` — - **Session 2 — error envelope (§2):** `errors.py` `AgentError` + append-only `ERROR_CODES` + no-redefinition test; attach at `ToolResult`/`ActionResult`/the three dispatch seams/`ProviderResolutionError`/validation rejections; FE tool-card WHAT/WHY/FIX rows.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
