<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `KL-1` — S1 Collections primitive: schema, store API, routes + Knowledge-page rail

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [KNOWLEDGE-LIBRARY](../plans/KNOWLEDGE-LIBRARY.md) (`KL`, plan status `done`) |
| **Atom table** | [`../atomic/KL.md`](../atomic/KL.md) |
| **Execution position** | #302 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 — Collections (T1.1 schema, T1.2 store API C2, T1.3 routes C3 + collections rail); Design §S1; Contracts C1/C2/C3

## Done when

collections + collection_items tables and read_state/favorited item columns added via the store's own additive ladder (store-native, no lifecycle gate); create a manual shelf and a smart shelf, both resolve to the same item shape; rail deep-links via ?collection=; a new matching item appears on a smart shelf with no backfill; smart shelf refuses membership writes with typed smart_collection_immutable

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (6)

[`KL-2`](KL-2.md) (`done`), [`KL-3`](KL-3.md) (`done`), [`KL-4`](KL-4.md) (`done`), [`KL-5`](KL-5.md) (`done`), [`KL-7`](KL-7.md) (`done`), [`KL-8`](KL-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `T1.1`, `T1.2`, `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:85` — | T1.1 | Schema (collections, collection_items; the two item columns via the additive-column pattern at `store.py:276`) — **store-native, no gate and no `lifecycle/` file** (the original wording asked for both; see the S1 execution-log DEVIATION) | `knowledge/store.py`, tests | opening the store …
- `T1.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:119` — - 2026-07-29 — **DONE (S1: T1.1 + T1.2 + T1.3).** Collections (manual + smart), item
- `T1.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:121` — - **T1.1 store + migration.** Two tables (`collections`, `collection_items`) and two
- `T1.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:124` — - **DEVIATION (E1, premise mismatch) — the plan's `lifecycle/` tasks are STALE.** T1.1
- `T1.1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:178` — + read state + favorites, which is what T1.1-T1.3 name.
- `T1.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:86` — | T1.2 | Store API C2 collection methods + smart-collection resolution (via `retrieval.search`) | `knowledge/store.py`, tests | manual + smart collections resolve; item in N collections works |
- `T1.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:119` — - 2026-07-29 — **DONE (S1: T1.1 + T1.2 + T1.3).** Collections (manual + smart), item
- `T1.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:137` — - **T1.2 store API (C2).** `create_collection` / `list_collections` /
- `T1.2` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:176` — - **NOT taken:** T1.2's `bulk_apply` / `find_duplicates` / `merge_items` belong to
- `T1.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:87` — | T1.3 | HTTP routes C3 for collections; frontend collections rail on the Knowledge page (create/rename/reorder, per-collection view) | knowledge handlers, `web/src/pages/knowledge/` | create a collection, add items, view it; smart collection updates as items match |
- `T1.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:119` — - 2026-07-29 — **DONE (S1: T1.1 + T1.2 + T1.3).** Collections (manual + smart), item
- `T1.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:152` — - **T1.3 routes (C3) + UI.** 9 routes; literal `collections` paths registered before
- `T1.3` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:178` — + read state + favorites, which is what T1.1-T1.3 name.
- `Session 1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:12` — **Status:** IN PROGRESS — Session 1 (collections + item curation) shipped 2026-07-29; **Session 2
- `Session 1` `docs/roadmap/plans/KNOWLEDGE-LIBRARY.md:81` — ### Session 1 — Collections

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
