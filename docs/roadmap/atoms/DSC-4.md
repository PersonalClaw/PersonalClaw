<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-4` — Inline font-weight canonicalization: fvs()/withWeight() + .fw-* utilities + 156-site migration (T2.1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #205 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 T2.1 conformance; Execution log cy8–cy10, cy18–cy27 (INLINE FONT-WEIGHT SUB-THREAD COMPLETE)

## Done when

web/src/design/fontWeight.ts + tokens.css .fw-400..650 added; every migratable inline fontVariationSettings 'wght' site (156 across cycles 18–27, byte-identical) routes through fvs()/withWeight(); inlineFontWeight ratchet driven 168→7 (the 7 liveMarkdown CodeMirror-theme sites legitimately stay); visual 64/64, no baseline moved.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DSC-3`](DSC-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `T2.1`, `Session 2`, `cy8`, `cy10`, `cy18`, `cy27`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:68` — | T2.1 | Fix the top-ranked drift: hardcoded values → tokens; shrink the allowlist (each removed entry = a real fix) | the ranked pages, `tokenLint.allowlist.json` | allowlist materially smaller; those pages token-clean; visual diff shows no regression (screenshot compare) |
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:150` — - **DONE (T2.1-style conformance, byte-identical):** fully migrated `NotificationBell.tsx` (4 inline `wght` → `fvs()`/`withWeight()`) and `HeaderActions.tsx` (3) to the fvs() helper. Combined-style sites use `withWeight(existing, n)` (keeps the other props); pure sites use `fvs(n)`. Both files no…
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:208` — - **DONE (T2.1-style, byte-identical):** migrated all inline `font-variation-settings "wght"` in the two shared `ui/` primitives — `Markdown.tsx` (5: table-header/h2/h3/h4 → `fvs(500)`, strong → `fvs(600)`) and `PlanningWalkthrough.tsx` (4: three pure → `fvs(550/600)`, one combined color+weight →…
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:214` — - **DONE (T2.1-style, byte-identical):** migrated all inline `font-variation-settings "wght"` across 7 self-contained page files — KnowledgeListPage (5), WorkflowDetail (4, incl. a conditional `nested ? undefined : fvs(550)` + a color-combined `withWeight`), TasksListPage (3), DiagnosticsPanel (3…
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:220` — - **DONE (T2.1-style, byte-identical):** migrated every inline `font-variation-settings "wght"` across the entire `pages/settings/*` cluster — ProvidersPanel (2), SettingsPage (2), ModelBackends (2), settingsWidgets (2), PortabilityPanel (2), MemoryPanel (2), bento (2), plus 9 single-site panels …
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:224` — - **Pre-existing note:** the impeccable hook flagged 13 `design-system-font-size` literals in settingsWidgets — pre-existing size-ramp drift (T2.1 scope), untouched by this weight-only cycle; left visible, not silenced.
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:228` — - **DONE (T2.1-style, byte-identical):** migrated the inline `font-variation-settings "wght"` across the medium/small loops·code·agents files — LoopPlanReview (8), DesignCockpitPage (5: 4 pure + 1 combined `withWeight({ background, color }, 600)`), CodePlanReview (4), AgentsListPage (3), LoopsLis…
- `T2.1` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:236` — - **DONE (T2.1-style, byte-identical):** migrated the inline `font-variation-settings "wght"` across `pages/chat/*` + `ui/chat/*` — ChatActivityPanel (2), ApprovalCard (2: 1 pure + 1 combined), SessionSkillsReview (2), SdlcProgressCard (2: 1 pure + 1 combined multi-line), MessageUser (2: 1 multi-…
- `Session 2` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:64` — ### Session 2 — Token + primitive hardening
- `cy8` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:565` — ### 2026-07-24 — cy8 DONE (G5 capability-picker unification — hoist `CapRow` to a shared `ui/` component, rewrite the code-plan `CapChip` surface onto it — batch cycle 8/9)
- `cy8` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy10` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:588` — ### 2026-07-24 — cy10 DONE (S2 raw-input migration wave 1 — the search-field idiom converges onto `SearchField`)
- `cy10` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:601` — - **Context + scope (this cycle's idiom = the query-box-with-external-submit → `ui/forms` `TextInput`):** the exact three sites cy10's NEXT reserved. Each is a single-line text field paired with an external submit `Button` and **no magnifier** — the semantic opposite of `SearchField` (no leading-…
- `cy10` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:620` — - **Validation:** `tsc --noEmit` clean; `npm run build` clean; vitest **150/150** green incl. `primitiveAdoption` ratchet + `consistencyAudit`. The live section (`RemoteModelProviders`, reached via Providers → "Model providers" → "Add instance") does **not mount in this dev home** — no model-prov…
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:12` — **Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes …
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:676` — ### 2026-07-24 — cy18 DONE (assessment/validation cycle — the autonomous-completable convergence lanes are code-verified DRAINED; no source change, and manufacturing a marginal one would violate the soul guardrail)
- `cy18` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:688` — - **Why this became completable — cy18's Lane 1 and Lane 2 conclusions were right in July and are stale now.** cy18 recorded that an `ErrorState` primitive "would gain exactly **one** faithful adopter — premature abstraction", and that of the cockpit's 21 raw buttons "**Zero** byte-identical clus…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
