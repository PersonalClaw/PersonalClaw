<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DIST-8` — Containers: install-kind env in both Dockerfiles + container guide + README compose snippet & install matrix

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISTRIBUTION](../plans/DISTRIBUTION.md) (`DIST`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DIST.md`](../atomic/DIST.md) |
| **Execution position** | #185 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #b93c337, 843c3cd |

## Intent — what this is for

Design §D; Session 3 T3.1–T3.3

## Done when

PERSONALCLAW_INSTALL_KIND=container baked into Dockerfile.backend + Dockerfile.web; docs/guides/containers.md covers ports/volumes/.env/snapshot-backup/pull-update; README carries the 2-line compose snippet + §A matrix table

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:CI-RELEASE:multi-arch (amd64+arm64) images published from CI to ghcr` | cross-plan (EXT) | **unresolved** |

### Unblocks (2)

[`DIST-11`](DIST-11.md) (`todo`), [`DIST-9`](DIST-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 19 (DIST) — wheel built and booted on this machine

**Code evidence:**

- MEASURED: PERSONALCLAW_INSTALL_KIND=container is baked into BOTH deploy/docker/Dockerfile.backend and deploy/docker/Dockerfile.web
- docs/guides/containers.md exists

**Notes:** The install-kind bake is what makes DIST-9's detection honest in a container, where neither a git root nor a pip marker would classify correctly — so this atom is the precondition for the next one rather than a separate nicety.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `T3.1`, `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/DISTRIBUTION.md:132` — Asserts the built wheel contains `personalclaw/static/dist/index.html`, installs into a scratch venv with NO Node, boots `personalclaw gateway --test-mode`, GETs `/` (200, HTML) and `/api/healthz` (200). Exit 0 = contract met. Run in `release.yml` (plan 33 T3.1).
- `T3.1` `docs/roadmap/plans/DISTRIBUTION.md:153` — | T1.5 | Wheel proof: script/CI step that builds (`npm run build` → `python -m build`), installs into a scratch venv, asserts `personalclaw/static/dist/index.html` inside site-packages, boots `personalclaw gateway --test-mode` and GETs `/` + `/api/healthz` | create `scripts/verify_wheel.py` (invo…
- `T3.1` `docs/roadmap/plans/DISTRIBUTION.md:170` — | T3.1 | `PERSONALCLAW_INSTALL_KIND=container` env in both Dockerfiles | `deploy/docker/Dockerfile.backend`, `Dockerfile.web` | `docker exec … env` shows it |
- `T3.1` `docs/roadmap/plans/DISTRIBUTION.md:257` — - **DONE T3.1 (container install-kind env)** — Added `PERSONALCLAW_INSTALL_KIND=container` to both Dockerfiles: `deploy/docker/Dockerfile.backend` runtime `ENV` block (the gateway process that runs `detect_install_kind()` — S4 T4.1) and `deploy/docker/Dockerfile.web` (nginx front-end sets it too …
- `T3.1` `docs/roadmap/plans/DISTRIBUTION.md:261` — - S3 core-repo/docs tasks (T3.1 Dockerfile env, T3.2 container guide, T3.3 README) are done. V3 (owner clean-VM: two commands → dashboard via TLS → create session+memory → `compose down && up` → state intact) is an owner real-world validation step (Docker not runnable in this sandbox).
- `T3.1` `docs/roadmap/plans/DISTRIBUTION.md:301` — | T3.1 + T3.2 | `b93c337` | `PERSONALCLAW_INSTALL_KIND=container` in both Dockerfiles; `docs/guides/containers.md` |
- `T3.3` `docs/roadmap/plans/DISTRIBUTION.md:171` — | T3.2 | Container guide: ports, volumes, `.env` pattern, backup via `personalclaw snapshot` inside the container, update = pull+up | create `docs/guides/containers.md` | doc walkthrough matches T3.3 validation exactly |
- `T3.3` `docs/roadmap/plans/DISTRIBUTION.md:172` — | T3.3 | README 2-line compose snippet + install matrix table (§A) | `README.md` | snippet copy-pastes clean on a fresh VM |
- `T3.3` `docs/roadmap/plans/DISTRIBUTION.md:259` — - Remaining S3: T3.3 (README 2-line compose snippet + §A install matrix table) and V3 (owner clean-VM: two commands → dashboard reachable via TLS → create session+memory → `compose down && up` → state intact).
- `T3.3` `docs/roadmap/plans/DISTRIBUTION.md:260` — - **DONE T3.3 (README install matrix + compose snippet)** — Rewrote the README `## Quickstart`: leads with `uv tool install personalclaw && personalclaw setup` (recommended) + the `curl … | sh` bootstrap one-liner, then a §A **install matrix** table (uv tool / bootstrap / pipx / pip / Docker Comp…
- `T3.3` `docs/roadmap/plans/DISTRIBUTION.md:261` — - S3 core-repo/docs tasks (T3.1 Dockerfile env, T3.2 container guide, T3.3 README) are done. V3 (owner clean-VM: two commands → dashboard via TLS → create session+memory → `compose down && up` → state intact) is an owner real-world validation step (Docker not runnable in this sandbox).
- `T3.3` `docs/roadmap/plans/DISTRIBUTION.md:302` — | T3.3 | `843c3cd` | README install matrix + 2-line compose snippet |
- `Session 3` `docs/roadmap/plans/DISTRIBUTION.md:166` — ### Session 3 — Containers
- `Session 3` `docs/roadmap/plans/DISTRIBUTION.md:255` — ### Session 3 — Cycle 6

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
