<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WV-6` — Slices 6+7 — 19 chat tools + spec ingestion + [ACTIVE WORKFLOWS] injection + HTTP API + FE list/detail pages

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2](../plans/WORKFLOWS-V2.md) (`WV`, plan status `done`) |
| **Atom table** | [`../atomic/WV.md`](../atomic/WV.md) |
| **Execution position** | #675 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Slice 6 + Slice 7

## Done when

mcp_workflows.py all 19 tools wired into _AGGREGATED_CATEGORY_MODULES + validation schemas; strict-mode repromptable ingestion + dry-run + preflight + generated manifest w/ CI drift test; handlers.py REST routes registered + per-run SseRegistry; FE workflows pages + api.ts methods + nav entry

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WV-4`](WV-4.md) | intra-repo | `done` |
| [`WV-5`](WV-5.md) | intra-repo | `done` |

### Unblocks (2)

[`WV-7`](WV-7.md) (`done`), [`WV-9`](WV-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `Slice 6`, `Slice 7`. Those tokens appear in its source plan at:

- `Slice 6` `docs/roadmap/plans/WORKFLOWS-V2.md:627` — - `context.py`: remove `force_workflow_ids` param + workflow-surfacing block in `build_message` (the `[ACTIVE WORKFLOWS]` injection lands in the same spot in Slice 6)
- `Slice 6` `docs/roadmap/plans/WORKFLOWS-V2.md:732` — ### Slice 6: Chat Tools + Spec Ingestion + Prompt Injection (est. 3 sessions)
- `Slice 6` `docs/roadmap/plans/WORKFLOWS-V2.md:977` — `context.py` still accepts `force_workflow_ids` (documented as unused until Slice 6).
- `Slice 6` `docs/roadmap/plans/WORKFLOWS-V2.md:1006` — plus the vocabulary invariant, and names Slice 6 / the flywheel skill work as where an
- `Slice 6` `docs/roadmap/plans/WORKFLOWS-V2.md:1177` — consumes them until Slice 6/7); `subworkflow` execution (Slice 10 — it returns a typed
- `Slice 7` `docs/roadmap/plans/WORKFLOWS-V2.md:741` — ### Slice 7: HTTP API + FE List/Detail Pages (est. 2 sessions)
- `Slice 7` `docs/roadmap/plans/WORKFLOWS-V2.md:851` — **Lands in:** Slice 1 (`step_cached` emission — journal.py already computes the hit), Slice 7 (inspect endpoint + drawer), Slice 8 (widget badge), Slice 11 (regression pair). No new slice; honest estimate 31 → **32 sessions** (+1 spread across Slices 7/8).
- `Slice 7` `docs/roadmap/plans/WORKFLOWS-V2.md:984` — discriminant deliberately kept so Slice 7 can re-add one without re-threading it),

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
