<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PL-7` — Capability discovery — Discover hub (owner-reshaped from §6 tool-usage power-ups)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-LEGIBILITY](../plans/PLATFORM-LEGIBILITY.md) (`PL`, plan status `done`) |
| **Atom table** | [`../atomic/PL.md`](../atomic/PL.md) |
| **Execution position** | #459 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§6 ("Capability-discovery power-ups") as SUPERSEDED by the 2026-07-25 owner-directed reshape (Success Criterion #7 + the §6-reshaped DEVIATION log entry); §8 config wiring (legibility.discover_tips); §9 Session 5 (S5c, then reshaped)

## Done when

tool-usage power_ups.py/tool_usage.py DELETED clean-break; legibility/discover.py CATALOG of 10 hand-authored area tips with per-area engagement checks; GET /api/legibility/discover + POST .../dismiss; Discover.tsx dashboard rotating spotlight + DiscoverPage.tsx hub (command palette, no nav tile); dismiss persists forever AND auto-hides once area used; nothing auto-enabled; legibility.discover_tips kill switch wired through the four config points; test_discover.py green; reference regenerated

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PL-1`](PL-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `§6`, `§8`, `§9`, `Session 5`. Those tokens appear in its source plan at:

- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:14` — context provider). Created 2026-07-13 from research synthesis, promoted from backlog. §6 shipped
- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:30` — - **NEW-13 amendment (e)** (capability-discovery power-ups: usage-telemetry-driven interactive mini-lessons cycling one untouched capability at a time as a dashboard widget) → §6. Attacks the proven manifest-vs-UI discovery gap from the *user* side.
- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:49` — - **Usage telemetry exists for skills only:** `skills/usage.py:SkillUsageStore` (`.usage.json` sidecar). §6 adds the analogous per-tool counter as a by-product of the manifest.
- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:237` — 7. ~~The dashboard shows one untouched-capability power-up with a working "try it" deep link; dismissing it persists; disabling `legibility.power_ups` removes the widget; nothing is ever auto-enabled.~~ **SUPERSEDED by the owner-directed §6 reshape (2026-07-25).** The tool-derived power-up widget…
- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:527` — **S5c — capability-discovery power-ups (§6).** `src/personalclaw/legibility/tool_usage.py`
- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:587` — ### 2026-07-25 — §6 reshaped: tool-derived power-ups → curated Discover hub — DEVIATION (owner-directed)
- `§6` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:589` — **DEVIATION from §6 as written.** §6 shipped (S5c) a tool-usage-telemetry-driven "power-ups"
- `§8` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:225` — | Four-wiring-points silent config drop | Explicit checklist in §8; schema reachability tests enforce (a) |
- `§9` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:405` — **Eval gate PASSED (§3.2 / §9 Session 3 — the ship blocker).** Ran the with/without eval on
- `Session 5` `docs/roadmap/plans/PLATFORM-LEGIBILITY.md:207` — - **Session 5 — UI docs + power-ups + context provider (§5-§7):** `.doc.ts` objects for the `ui/` kit + `ui-docs.json` build step + `UiDocsToolProvider` (`ui_search`/`ui_get`) + FE drift test; `tool_usage.json` counter + power-ups endpoint/widget; `context_router.py` + marker-fenced adapters + in…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
