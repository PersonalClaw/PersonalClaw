<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PT-5` — S2: finish claw-arcade proof + extend personalityA11y.test.ts for the new closed maps

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PERSONALITY-THEMES](../plans/PERSONALITY-THEMES.md) (`PT`, plan status `done`) |
| **Atom table** | [`../atomic/PT.md`](../atomic/PT.md) |
| **Execution position** | #495 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1453 |

## Intent — what this is for

Design 'S2 ...' (both proofs) and 'A11y invariants'; Task breakdown Session 2 T2.4; Contract C5

## Done when

claw-arcade proof fleshed out (expressiveness preset via runtime dials, sparkle dot shape, coin-blip cue); personalityA11y.test.ts extended to go red on unknown base scheme, dangling shellElement/errorTreatment id, and any cue declared without the master-toggle gate; both proof personalities fully switchable

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PT-2`](PT-2.md) | intra-repo | `done` |
| [`PT-3`](PT-3.md) | intra-repo | `done` |
| [`PT-4`](PT-4.md) | intra-repo | `done` |

### Unblocks (1)

[`PT-6`](PT-6.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 48 (PT) — both personalities driven through switch, reload and residue check

**Code evidence:**

- 🔑 FALSIFIED THE RAIL THIS ATOM SHIPS: replaced one personality's shellElement with an id that resolves nowhere, and personalityA11y went red on EXACTLY ONE rail — 'shellElement — names a shellElement that resolves in its closed map', 1 failed / 52 passed. Reverted; 53/53
- 🔑 THE SUITE IS BIDIRECTIONAL AND SAYS SO: 'every rail has a falsifying fixture, and every fixture a rail' — no rail without a mutation proving it, no mutation without a rail
- 🔑 THE VACUITY FLOOR IN ITS CLEAREST FORM ANYWHERE IN THIS CAMPAIGN: 'the base fixture is itself CLEAN — or every mutation below proves nothing'. If the base already violated a rail, every mutation-makes-it-red assertion would be meaningless
- the closed-map coverage the clause asks for is present: unknown base scheme, dangling shellElement and errorTreatment ids, and cue voices reachable only through a cue point; plus 'an unknown or removed id falls back to the default, never a broken state'
- 🔑 THE DEFAULT IS PINNED AS THE NULL IDENTITY: 'the default carries no assistant rename' and 'the default declares no cue voice, no dial, no shell element, no treatment' — so today's experience cannot drift by someone adding a behavior to the default entry
- DRIVEN: claw-arcade is present and switchable in the picker with its own persona ('calls itself CLAW-1'), alongside retro-terminal
- personalityA11y 53/53 (falsified: a dangling shellElement id reds exactly one rail); design+personality suites 251/251

**Driven in the UI:** Both proof personalities present and switchable; the retro-terminal one driven end to end.

**Notes:** Sixth vacuity floor in this campaign and the best-articulated one. The two assertions together — every rail has a falsifying fixture, and the base fixture is clean — are what make a 53-test suite evidence rather than decoration.

## Recorded history

### Commits (1)

- `32bf078b7` 2026-08-16 — feat(web): PT-5 claw-arcade proof and personality closed-map rails

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:31` — `ErrorBoundary`/`IncidentBanner`. T2.4 (proofs + C5 rails, atom `PT-5`): `behavior.dials` +
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:175` — - [2026-08-16][S2/T2.1 · atom `PT-2`] DONE (sound cues — synthesised, default OFF). (a) **`web/src/design/soundCues.ts`** — three earcons built from oscillator + gain envelopes on the context's own timeline (`turn_complete` a rising major third, `approval_needed` two rising pulses, `error` a fall…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:186` — - [2026-08-16][S2/T2.2] DISCOVERY (adjacent, deliberately left): PT-5's row asks `personalityA11y.test.ts` to go red on a dangling `shellElement`/`errorTreatment` id. PT-4 already added the treatment half, so this atom added the symmetric `shellElement` assertion — the registry's own structural g…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:187` — - [2026-08-16][S2/T2.4 · atom `PT-5`] DONE (both proofs finished + the registry's structural rails). (a) **`behavior.dials`** is the expressiveness preset the plan's Integration-points line asked for ("`runtime` dials for expressiveness presets"), and it reaches `runtime` the long way ON PURPOSE:…

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:31` — `ErrorBoundary`/`IncidentBanner`. T2.4 (proofs + C5 rails, atom `PT-5`): `behavior.dials` +
- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:146` — | T2.4 | `personalityA11y.test.ts` (C5) + finish the `claw-arcade` proof (expressiveness preset via `runtime`, sparkle dot shape, coin-blip cue) | `web/src/design/personalityA11y.test.ts` (new), `personalities.ts` | structural test red on: unknown base scheme, dangling element id, ungated cue; bo…
- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:187` — - [2026-08-16][S2/T2.4 · atom `PT-5`] DONE (both proofs finished + the registry's structural rails). (a) **`behavior.dials`** is the expressiveness preset the plan's Integration-points line asked for ("`runtime` dials for expressiveness presets"), and it reaches `runtime` the long way ON PURPOSE:…
- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:188` — - [2026-08-16][S2/T2.4] Proofs. **`personalityA11y.test.ts` is now ten NAMED rails, each proven red by a fixture that breaks exactly it** — 53 tests where there were 11. The old file checked the real registry only, which cannot distinguish a working rail from one that can never fail; every check …
- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:189` — - [2026-08-16][S2/T2.4] Fourteen mutations against the live lines; **thirteen red, one deliberately reded nothing and that mutation was KEPT as the fix.** Red: a dangling `shellElement` id, a dangling `errorTreatment` id, an unknown `baseScheme` (each flagged with the offending id in the message)…
- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:190` — - [2026-08-16][S2/T2.4] Gate: `npm run typecheck --workspace web` clean · FULL `npm test --workspace web` **293 files / 2996 tests passed**, zero failures — baseline measured first on the branch point (3c6f1374): **2931 tests**, so +65 with no regressions and no ratchet or baseline touched · `npm…
- `T2.4` `docs/roadmap/plans/PERSONALITY-THEMES.md:191` — - [2026-08-16][S2/T2.4] DISCOVERY (adjacent, deliberately left): **DotGlow's reduced-motion branch had no test before this atom** — the `animate = … && !reduce` line at `DotGlow.tsx:142` has shipped since the component was written, is the reason the halftone backdrop is honest under reduced motio…
- `Session 2` `docs/roadmap/plans/PERSONALITY-THEMES.md:139` — ### Session 2 — Sound cues + shell element + error treatment + proofs polished

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
