<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CRE-2` — Red-test triage + core ci.yml/full.yml + README badges (S1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CI-RELEASE-ENGINEERING](../plans/CI-RELEASE-ENGINEERING.md) (`CRE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CRE.md`](../atomic/CRE.md) |
| **Execution position** | #132 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Sessions S1; Task breakdown Session 1 T1.2/T1.3/T1.4/V1; Red-test policy (C2); Contracts C1 (ci.yml jobs lint/test/web/rails)

## Done when

known-red groups fixed-or-xfail'd per C2 (xfail_strict untouched, one filed issue per annotated group #6/#7/#8); .github/workflows/ci.yml authored with jobs lint/test/web/rails + concurrency cancel-in-progress; full.yml matrix {3.12,3.13}×{ubuntu,macos} skeleton; README CI badge renders; job ids match C1; deliberate lint error + test failure turn the run red

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CRE-1`](CRE-1.md) | intra-repo | `done` |

### Unblocks (3)

[`CRE-3`](CRE-3.md) (`done`), [`CRE-5`](CRE-5.md) (`done`), [`CRE-6`](CRE-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `T1.2`, `T1.3`, `T1.4`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:102` — | T1.2 | For each group, timebox ½ day: root-cause and fix, else mark `@pytest.mark.xfail(reason="<hypothesis> — issue #<n>", strict=False)` or `skip` with reason; file one issue per annotated group (issue creation is an owner task if repos lack issues — leave titles+bodies in the Execution log) …
- `T1.2` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:221` — in `sdk/cli.py` and was never in `cli_setup` on main (637fd11). Queued for T1.2
- `T1.2` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:251` — faiss untyped optional C-ext). The `make test` triage (T1.2) is the remaining half of V1.
- `T1.3` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:103` — | T1.3 | Author `ci.yml`: jobs `lint` (uv-cached, `make lint`), `test` (ubuntu, py3.12, `PERSONALCLAW_HOME=$RUNNER_TEMP/pclaw-home python -m pytest`), `web` (`npm ci && npm run typecheck && npm test && npm run build` in `web/`), `rails` (placeholder step until plans 31/32 tests exist — runs them …
- `T1.3` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:263` — - [2026-07-20][S1 amendment — isolation root-caused, mitigations retired] Owner directed fixing the test isolation at the source rather than shipping the loadscope/reruns mitigations. Four root causes, all fixed in-code (no reruns, no masking); the earlier "xdist flake" xfails (#8/#10) are remove…
- `T1.4` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:104` — | T1.4 | Badges: CI (workflow badge) + license into both README headers | `README.md` (core; apps repo's in S2) | badges render on the repo page |
- `Session 1` `docs/roadmap/plans/CI-RELEASE-ENGINEERING.md:97` — ### Session 1 — Red-test triage + core CI

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
