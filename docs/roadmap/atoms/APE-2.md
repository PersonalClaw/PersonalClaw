<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `APE-2` — Platform event registry (app_events.py) + emit sites + declared-subscription WS filter

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [APP-PLATFORM-EVOLUTION](../plans/APP-PLATFORM-EVOLUTION.md) (`APE`, plan status `done`) |
| **Atom table** | [`../atomic/APE.md`](../atomic/APE.md) |
| **Execution position** | #42 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1699 |

## Intent — what this is for

Session 1 T1.2; Contract C4 (typed platform events registry)

## Done when

app_events.py registers session.created/knowledge.ingested/task.completed at their emit sites; a fixture app subscribed to task.completed receives it; an unsubscribed app never does; SEL clean

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`APE-1`](APE-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `fc1a220d5` 2026-08-18 — chore(roadmap): cite the landing batch #1699 on APE-2, PEP-10 and TSE-5
- `c85c7e11d` 2026-08-18 — chore(roadmap): regenerate the dag derived block for three atom flips
- `bc051a9de` 2026-08-18 — feat(apps): APE-2 platform event registry + emit sites + declared-subscription dispatch
- `42302ce15` 2026-08-18 — feat(apps): APE-1 backgroundTasks + eventSubscriptions manifest grants

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:286` — - [2026-08-18][S1 · atom `APE-2`] **DONE** — `apps/app_events.py` registers the three platform events, each
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:367` — (that is `APE-3`) and nothing delivers a platform event (`APE-2`), so the consent surface gets a third,
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:373` — has a live call site; an accessor with none is an enforcement point that enforces nothing, so `APE-2`/`APE-3`
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:376` — gateway's WS allowlist (`test_event_subscriptions_do_not_widen_the_ws_event_allowlist`), or `APE-2` would
- **source plan** `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:599` — it joins the bullets, exactly the move `APE-2` made for `eventSubscriptions`, and the box is **deleted**
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:6023` — registry) stays with `APE-1`/`APE-2`, this atom's EXT dep. What lands here is the type being declarable,

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.2` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:75` — | T1.2 | Platform event registry `app_events.py` + register the first events (`session.created`, `knowledge.ingested`, `task.completed`) at their emit sites; WS filter delivers only declared events | `src/personalclaw/app_events.py`, the 3 emit sites, WS filter | a fixture app subscribed to `task…
- `Session 1` `docs/roadmap/plans/APP-PLATFORM-EVOLUTION.md:70` — ### Session 1 — Background + event capabilities

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
