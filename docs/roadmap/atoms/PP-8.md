<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-8` — Edge-decision statistics: per-`branch`/`gate` distribution + dead-case detection

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #466 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1340 |

## Intent — what this is for

"Put the judgment in the edges — and instrument them" is the one graph-engineering practice we have no answer to. `branch` (binding-decided) and `gate`/`judge` (model-decided) are journaled individually and never aggregated, so a selector that has taken one case 47/47 times, or a case no run has ever reached, is invisible. `introspection.gate_stats` (said-no statistics with a sample-gated fake-check badge) is the closest existing surface and covers gates only.

## Done when

A ledger projection reports, per `branch` and per judge gate, the case/verdict distribution across a template's run history, and flags a case never taken and a selector whose distribution is degenerate. Rendered on the existing introspection surface rather than a new one. Sample-gated like `gate_stats` — a distribution over three runs is not a finding, and reporting it as one is how a legible surface stops being read.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-4`](PP-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `ec2e4293d` 2026-08-15 — feat(workflows): PP-8 edge-decision statistics — per-branch/gate distribution + dead-case detection

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:90` — ## §2 The ledger as a platform primitive (`PP-4`…`PP-8`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:97` — (`PP-6`), trajectory signatures (`PP-7`), edge statistics (`PP-8`), consumer liveness (`PP-10`),
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:141` — ├─→ PP-8                           │
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:993` — - **2026-08-15 — `PP-7` DISCOVERY (expected additive conflict with `PP-8`).** `PP-8` (PR #1332, not
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:996` — `gate_stats`/`run_stats`, exactly as `PP-8` did — so when both merge, `introspection.py` conflicts
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1174` — - **2026-08-15 — `PP-8` DONE.** `introspection.edge_stats(runs)` projects, across a template's
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1184` — - **2026-08-15 — `PP-8` DISCOVERY: the `branch` case selection is NOT recorded as a dedicated
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1197` — - **2026-08-15 — `PP-8` DECISION: CHANGELOG entry added.** Unlike `PP-11`, this atom adds a
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1289` — since nothing user-observable changed. Plan header's "N of 16 shipped" left alone, per `PP-8`.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
