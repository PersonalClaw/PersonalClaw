<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-1` — S1 remote-access story: Tailscale-first guide + doctor reachability probe

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #335 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##870 |

## Intent — what this is for

Session 1 — Remote access story (T1.1 remote-access guide, T1.2 doctor probe, V1)

## Done when

A reader on cell data reaches their dashboard via tailnet following docs/guides/remote-access.md verbatim; `doctor` detects the tailscale interface and prints the phone-ready tokenized URL, and warns when the bind host exposes beyond loopback without auth (tailnet + misconfig fixtures pass).

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `ebea10d2d` 2026-08-06 — feat(doctor): MC-1 remote-access tailnet guide + reachability probe (#870)

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T1.1`, `T1.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/MOBILE-COMPANION.md:101` — | T1.1 | `docs/guides/remote-access.md`: Tailscale walkthrough (install, join, `personalclaw token` URL, auth-mode notes), Cloudflare Tunnel alt, anti-patterns section (why not port-forward; what `bypass_local_networks` does and when NOT to set it) | new guide | a reader reaches their dashboard f…
- `T1.1` `docs/roadmap/plans/MOBILE-COMPANION.md:138` — | T4.2 | QR pairing screen **and endpoint switching**, both consumed from plan 54: Settings → Devices → "Pair phone" renders a QR of COMPANION-APPS §C2's `{pairing_url, code}` → app scans → `pair/complete` → device session (§C1); the paired gateway then becomes a §C1 registry entry (`device_sessi…
- `T1.2` `docs/roadmap/plans/MOBILE-COMPANION.md:102` — | T1.2 | `doctor` reachability: detect tailscale interface/hostname, print the phone-ready tokenized URL; warn when bind host exposes beyond loopback without auth | `cli_doctor.py` | tailnet fixture prints URL; misconfig fixture warns |
- `T1.2` `docs/roadmap/plans/MOBILE-COMPANION.md:112` — | T2.4 | Devices list: consumed from COMPANION-APPS T1.2 (Settings → Devices — name, kind, minted, last-seen, issuer + revoke). This plan builds **no second list**; it links to that one from the companion surface | companion nav/link only | a revoke performed in plan 54's panel kills the device s…
- `T1.2` `docs/roadmap/plans/MOBILE-COMPANION.md:138` — | T4.2 | QR pairing screen **and endpoint switching**, both consumed from plan 54: Settings → Devices → "Pair phone" renders a QR of COMPANION-APPS §C2's `{pairing_url, code}` → app scans → `pair/complete` → device session (§C1); the paired gateway then becomes a §C1 registry entry (`device_sessi…
- `Session 1` `docs/roadmap/plans/MOBILE-COMPANION.md:97` — ### Session 1 — Remote access story

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
