<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `FS-2` — FE affordance: FeedbackThumbs primitive + mounts on inbox judgment blocks and loop finding rows (S2 / T2.1-T2.3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [FEEDBACK-SIGNAL](../plans/FEEDBACK-SIGNAL.md) (`FS`, plan status `done`) |
| **Atom table** | [`../atomic/FS.md`](../atomic/FS.md) |
| **Execution position** | #276 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 (T2.1-T2.3); Design 'The FE affordance (S2)'; web/src/ui/FeedbackThumbs.tsx, InboxDetail.tsx, LoopsListPage/LoopPeek

## Done when

quiet 👍/👎 pair with aria-pressed/filled-state, reduced-motion honored, skippable 'why' popover; thumb -> POST -> filled state -> re-thumb supersedes; verdict hydrates on reopen via GET /api/feedback/target; mounted on inbox classification/draft (+digest folded into detail per S2 deviation) and loop finding header. Log: [2026-07-27][S2] DONE.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`FS-1`](FS-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.1`, `T2.3`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/FEEDBACK-SIGNAL.md:155` — | T2.1 | `FeedbackThumbs` primitive (ui/): quiet 👍/👎 pair, hover/focus-reveal desktop + compact touch, filled-state reflection, reversible, 👎 opens optional one-line "why" popover (skippable); `prefers-reduced-motion` honored; token-lint clean | `web/src/ui/FeedbackThumbs.tsx` (new + `.doc.ts`), …
- `T2.1` `docs/roadmap/plans/FEEDBACK-SIGNAL.md:187` — - [2026-07-27][S2] DONE: FE affordance (T2.1-T2.3). **T2.1** `web/src/ui/FeedbackThumbs.tsx` (+`.doc.ts`, counted in ui-docs: 72 components): quiet pair, aria-pressed + filled-state reflection, hydrates from GET target on mount, optimistic + reversible, 👎 opens a skippable one-line "why" popover …
- `T2.3` `docs/roadmap/plans/FEEDBACK-SIGNAL.md:157` — | T2.3 | Mount on loop finding rows (`LoopsListPage.tsx` latest-finding rows + peek panel) with `("loop_judge", kind)` producers | `web/src/pages/loops/LoopsListPage.tsx` | a finding can be thumbed from list + peek; state consistent between the two |
- `T2.3` `docs/roadmap/plans/FEEDBACK-SIGNAL.md:187` — - [2026-07-27][S2] DONE: FE affordance (T2.1-T2.3). **T2.1** `web/src/ui/FeedbackThumbs.tsx` (+`.doc.ts`, counted in ui-docs: 72 components): quiet pair, aria-pressed + filled-state reflection, hydrates from GET target on mount, optimistic + reversible, 👎 opens a skippable one-line "why" popover …
- `Session 2` `docs/roadmap/plans/FEEDBACK-SIGNAL.md:151` — ### Session 2 — FE affordance on core surfaces + app SDK path

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
