<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-15` — AppConfig.load() must be a pure read — and the real-home rail must be able to see a metadata-preserving writer

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #452 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Two defects found together on 2026-08-27 while root-causing a CI-only real-home-rail failure, where the second is what hid the first. (1) AppConfig.load() IS NOT A PURE READ: when needs_migration is true it does shutil.copy2(path, path.with_suffix('.json.bak')) then cfg.save() (config/loader.py, inside load()'s try, best-effort with a warning on failure). So ANY caller that merely reads config mutates the user's config.json and drops a .bak beside it — including a module imported for an unrelated reason. That delivery mechanism was fixed separately (mcp_core's module-level _API constant became a call-time _api_base(), the 4th instance of the import-time-constant shape tests/conftest.py::_isolate_real_home_writers documents as beyond a fixture's reach), but the impurity itself remains and any future reader re-arms it. (2) THE REAL-HOME RAIL UNDER-REPORTS AGAINST copy2: the walk detects changes by mtime-since-session-start, and shutil.copy2 preserves the SOURCE mtime, so the .bak the migration writes looks older than the session and is invisible. That is why CI reported '1 entries changed' when two things changed, and it is a general blind spot for any metadata-preserving writer, not a quirk of this one. Both are invisible on a developer machine: the write only fires when config.json exists AND is pre-migration, and a developer's config is already migrated, so the rail is green locally however broken the code is. NOT in scope: deleting the migration itself (it is load-bearing for real upgrades) or redesigning the rail's single-walk performance choice.

## Done when

AppConfig.load() performs NO write under any input — proven by a fresh-interpreter subprocess rail (the shape tests/test_import_time_config_writes.py establishes) that seeds a genuinely pre-migration config.json, calls load(), and asserts (mtime_ns, contents) unchanged AND no .bak created, with a vacuity leg that observes the write when the migration is invoked through its new explicit entry point; the migration still runs for real upgrades through that explicit call from a startup path that a test can point at tmp_path, with a regression proving a pre-migration config IS migrated when the gateway boots and IS NOT when a library reader imports; every existing load() caller audited by a RUNTIME import sweep with a stated count (mypy cannot see a stranded first-party import) and none left depending on the write-back side effect; the real-home rail additionally detects a metadata-preserving write — a planted shutil.copy2 of a tracked file is REPORTED, proven by a probe that reds before the fix and passes after, with ALLOWED_RESIDUE still frozenset() and tests/real_home_guard.py's cost documented; and config/loader.py ends at or below its starting line count, since the 6000-line ceiling rail leaves it 100 lines of headroom (see PHF-14).

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 68 (PHF) — the proxy-signature replay window falsified; the plan that turns this campaign's own defect class into a ratcheted CI gate

**Code evidence:**

- `tests/test_import_time_config_writes.py` is green inside the 76, and the shape is the load-bearing part
- 🔑 A FRESH-INTERPRETER SUBPROCESS RAIL IS THE ONLY WAY TO PROVE 'NO WRITE UNDER ANY INPUT': in-process, a previous test's import has already run the module's side effects, so the write it is looking for has happened before the assertion exists. This campaign has found several assertions that could not fail; this one is designed so it can
- 🔴 AND THE SECOND HALF IS THE SUBTLER REQUIREMENT: the real-home rail must be able to SEE a metadata-preserving writer. A writer that preserves mtime and permissions is invisible to the obvious check, so the rail is built to catch the write that tried not to be noticed
- a pure `load()` is what lets every other atom in this plan read config freely — a loader that wrote on read would make config access a mutation nobody expected
- 76+87+18 = 181 passed across the inert-baseline, config-schema, import-time-write, aggregate-gate, ceiling, env-allowlist, config-roundtrip and app-backend-proxy suites; the replay window falsified and restored 18/18; loader.py measured at 4431 lines

**Driven in the UI:** Not a browser surface.

**Notes:** 🏅 'The real-home rail must be able to see a metadata-preserving writer' is the most sophisticated statement of the campaign's central class in the catalogue: not just 'assert the thing', but 'assert it in a way the plausible evasion cannot slip past'.

## Recorded history

### Commits (3)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `eb09d9321` 2026-08-26 — fix(config): PHF-15 make AppConfig.load() a pure read
- `919738607` 2026-08-26 — docs(roadmap): file PHF-15 for AppConfig.load()'s impurity, and record DL-6's ten unreleased claims

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:1029` — **`loader.py` is 5581 lines — 419 of headroom.** `PHF-15` and `SH-2` extracted code out of it in
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1827` — - [2026-08-27][PHF-15] **NEW ATOM — a read that mutates the user's config, and the rail that could not
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1859` — ## Execution log — `PHF-15` (`AppConfig.load()` is a pure read; the real-home rail sees a metadata-preserving write) — 2026-08-26 — DONE
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1938` — with no anchor raises. This fragility predated PHF-15 — any rename of `load()` would have triggered it.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
