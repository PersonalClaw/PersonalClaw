<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AAP-5` — §2.2 Approval-gate coverage — host is permission authority (gap 2, the safety hole)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ACP-AGENT-PARITY](../plans/ACP-AGENT-PARITY.md) (`AAP`, plan status `done`) |
| **Atom table** | [`../atomic/AAP.md`](../atomic/AAP.md) |
| **Execution position** | #5 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1937 |

## Intent — what this is for

Phase 2 §2.2 Approval-gate coverage — gap 2 (most-restrictive mode forwarding + always-ask so tools hit chat_runner.py:1771 gate; extend CC config isolation to bundled default)

## Done when

With task-mode=Ask a file write via any ACP provider yields a host approval card or block (never a silent write); deny-list rejects a denied command at the prompt and blocking PreToolUse fires pre-exec on every permission-surfaced ACP tool; residual not-gateable set enumerated per provider (SC #3).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AAP-1`](AAP-1.md) | intra-repo | `done` |
| [`AAP-2`](AAP-2.md) | intra-repo | `done` |
| [`AAP-3`](AAP-3.md) | intra-repo | `done` |

### Unblocks (2)

[`AAP-10`](AAP-10.md) (`done`), [`AAP-9`](AAP-9.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 46 (AAP) — the plan's own parity doc contradicts two of its atoms

**Code evidence:**

- 🔑 THE RESIDUAL SET IS CODE, NOT PROSE: acp/permission_authority.py:270 defines NOT_GATEABLE per provider as typed data (ProviderCoverage / NotGateable / ResidualState), each entry carrying its MEASUREMENT provenance ('AAP-3 sweep (K13, K15) + AAP-5 live re-drive 2026-08-18: one turn, 6 tool calls, 1 gated, 5 ungated') and its reason — kiro's todo_list 'emits a tool_call frame and a SEL invoked row but never a session/request_permission, so NO host gate — deny-list, task-mode, PreToolUse — can run for it'
- 🔑 THE TEST SUITE DRIVES CALL SITES, NOT PREDICATES, and says so: 'the mode clamp is asserted on AcpClient (THE CHOKEPOINT EVERY MODE PATH CROSSES), and the gate behaviours are asserted by running run_chat over a synthetic ACP event stream — the same harness test_dashboard_approval.py uses'
- 🔑 A VACUITY FLOOR ON A SECURITY CONTROL: 'Each restriction gets its INVERSE FLOOR so it reads as a requirement rather than an always-refuse.' A gate that refused everything would pass a refuse-only suite — this is the fifth instance of that discipline in the campaign and the most consequential place for it
- the clamp's own tests cover the ways it would leak: every declared auto-approve mode clamped, spelling variants refused, an UNKNOWN mode 'clamped not assumed safe', and unattended named as 'the ONLY declared escape'
- ResidualState distinguishes a residual that has been 'blessed — DECLARED vs EXCUSED', so an accepted gap and a merely-observed one are different records
- 178 across permission-authority/breaker/project-stamping/tool-card/unattended/cwd-containment; +39 bundles/dialect/slash/set-mode

**Driven in the UI:** Not driven end to end: 'a file write via any ACP provider' needs an authenticated provider. The gate path is exercised through the real chat runner over a synthetic ACP stream, which is the closest thing available here.

**Notes:** 🔑 ARCC'S ONE TRANSFERABLE OBJECTIVE LANDS EXACTLY ON THIS ATOM. Its search keyed on 'third party' and returned Amazon's third-party programmes rather than an authorization pattern — recorded as checked-and-adjacent — but the IAM third-party guidance carries the idea that matters: when an external party genuinely needs privileges you cannot constrain, the answer is to DOCUMENT AND REVIEW the residual rather than to imply it is constrained. That is precisely this clause's 'residual not-gateable set enumerated per provider', and here it is implemented as typed data with per-entry provenance instead of a paragraph. The gate half needs a provider; the honesty half is complete and checkable.

## Recorded history

### Commits (3)

- `549ae09b9` 2026-08-23 — chore(roadmap): cite AAP-5 to the batch PR
- `af7de1c0e` 2026-08-23 — fix(acp): AAP-5 make the host the permission authority, for real this time
- `566964628` 2026-08-18 — feat(acp): AAP-5 make the host the permission authority

### Named by atom id in the roadmap prose (36)

- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:114` — - **Acceptance:** with task-mode=Ask, a file write via any ACP provider produces a host approval card (or is blocked) — never a silent write; the deny-list rejects a denied command at the permission prompt with the standard denial message; PreToolUse blocking hooks fire pre-execution on every per…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:518` — gets silent execution with no host card. This is the measurement `AAP-5` needs: the fix is to
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1114` — Where the claude bundle ships an opt-in hardening flag that `AAP-5` can promote to a default, the
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1122` — (`C19`). So `AAP-5`'s "make the isolated config the default for host-managed sessions" has
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1125` — neither declares nor gates. Owner: agent app bundle + core seam, `AAP-5`.
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1606` — `AAP-4`/`AAP-5`/`AAP-6`) — read this before sequencing Phase 2.** `G3` (no `personalclaw-core`
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1615` — by `AAP-5` after this column was written, so this sweep did not re-measure it.
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1653` — environment and its own `~/.kiro` tree, so — as with codex — `AAP-5` has nothing to flip and the
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1944` — this follow-up ran on `origin/main` @ `252c944f`, which contains `AAP-4`, `AAP-5` and `AAP-6`. So a
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1963` — | `K28` | the cwd-confinement re-measurement, asserted **inside** the spawned CLI: S1 bound to `acp:kiro-cli` with `workspace_dir=…/.dev-home/ws`, then "Run exactly one shell command: `pwd`" | **`PWD=/private/tmp/aap3n-wt/.dev-home/ws` — `G1`'s escape does NOT reproduce at this tip.** The first s…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2231` — ## Phase 2 results — §2.2 Approval-gate coverage (atom `AAP-5`)
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2245` — **every** `session/set_*` site discarded that future — so a hard `-32602` was invisible for AAP-5's
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2382` — claude-code has a sixth (`auto`) that AAP-5's canonical five never named. Corrected with both measured
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2635` — operator's settings auto-approve nothing, so `AAP-5`'s job is to make that coverage structural
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2654` — operator's real `~/.codex` — `AAP-5` has no lever to flip here and must build one (`G17`).
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2712` — - 2026-08-18 — `AAP-5` **PARTIAL** (§2.2, gap 2 — the safety hole). Made the HOST the permission
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2805` — - 2026-08-18 — `AAP-6` **PARTIAL** (§2.3, gaps 3 and 5). Written against `AAP-5` and shipped FLAT on
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2806` — `main`: `AAP-5`'s PR reads `CLOSED` rather than merged, but all seven of its files are byte-identical
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2814` — `bypassPermissions` was clamped straight back). Resolving the tension with `AAP-5` is the whole
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2838` — `AAP-1`/`AAP-2`/`AAP-4`/`AAP-5`).** A `cron:`-keyed unattended session forwarded
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3053` — surface, `AAP-5`'s permission authority + kiro config seeding, and `AAP-6`'s unattended fail-fast
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3082` — carries the 2026-08-19 non-reproduction warning; and `AAP-4`/`AAP-5`/`AAP-6`'s host compensation is
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3324` — Seven commits in `b01cb76e..e25a6ffa` touch the ACP surface: `56696462` (`AAP-5` host permission
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3348` — and +203 in `dashboard/chat_runner.py`. `AAP-1`'s own log says "`AAP-5`'s job is to make that coverage
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3363` — *consumer* churned — `dashboard/chat_runner.py:3713` and `:3812` are inside `AAP-5`/`AAP-6`'s diffs —
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4577` — ## Execution log — `AAP-5` (§2.2 Approval-gate coverage)
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4579` — - [2026-08-23][AAP-5] **DONE.** All four acceptance clauses hold, three measured during Phase 1 and the
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4583` — - [2026-08-23][AAP-5] 🔴 **THE SAFETY HOLE'S CAUSE WAS NOT WHAT §2.2 ASSUMED: host-authority mode
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4594` — - [2026-08-23][AAP-5] 🔴 **The obvious fix to the residual registry would have been a SAFETY REGRESSION,
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4601` — - [2026-08-23][AAP-5] **The third state is a dataclass FIELD, not a property, and that was a cross-branch
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4607` — - [2026-08-23][AAP-5] **A test was asserting the false claim — the third such instance this session.**
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4611` — - [2026-08-23][AAP-5] **`G89` fixed without touching `chat_runner.py`** — a distinct per-fire
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4617` — - [2026-08-23][AAP-5] **§2.2's "RENDERS" requirement was unmet and falsely claimed met** — the paragraph
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4622` — - [2026-08-23][AAP-5] **Acceptance measured, and one arm recorded as VACUOUS rather than claimed.** codex
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4898` — - [2026-08-24][AAP-4] **Observation for `AAP-5`, filed not fixed:** claude-code's turns emitted
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4901` — "residual not-gateable set" rather than a regression of this atom — `AAP-5` is `done` and enumerates it.

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§2.2`. Those tokens appear in its source plan at:

- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:139` — - **Plan-mode for kiro — owner: upstream CLI limitation, documented + host-compensated.** The default dialect has no mode axis (`set_mode_request` → None). No shadow mechanism: the host task-mode gate already blocks non-plan mutations at the permission prompt (§2.2 makes that coverage real), and …
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:619` — The kind is still **not** fed to `task_mode_denies` — §2.2's recorded choice, now pinned by a
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2211` — ### Recipe corrections for whoever drives §2.2 next
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2231` — ## Phase 2 results — §2.2 Approval-gate coverage (atom `AAP-5`)
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2234` — `O96`-`O135`, findings `G120`-`G135`. **The safety hole was real, and its cause was not what §2.2
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2242` — params`, and stayed in the self-approving mode §2.2 exists to leave** (`O107`/`O108`).
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2313` — ### §2.2's "RENDERS" requirement was unmet — and falsely claimed met
- `§2.2` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2712` — - 2026-08-18 — `AAP-5` **PARTIAL** (§2.2, gap 2 — the safety hole). Made the HOST the permission

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
