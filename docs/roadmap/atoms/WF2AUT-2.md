<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2AUT-2` — Clock-engine cutover: crash-safe scheduler + wakeup dispatch + missed-fire/catch_up + boot sweep + ScheduleService deleted

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-AUTOMATION-SUBSTRATE](../plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md) (`WF2AUT`, plan status `done`) |
| **Atom table** | [`../atomic/WF2AUT.md`](../atomic/WF2AUT.md) |
| **Execution position** | #561 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##250-#543 |

## Intent — what this is for

§7 step 3; §3.1 crash-safe discipline; §3.2 inbox+wakeup dispatch; §3.4 missed fires; crit 7 & 12 (system-scheduler handoff); AUTO-R1/R16/R8; §2 schedule.py ABSORBED

## Done when

tick is the sole clock engine (S100 cutover); persist-before-execute + exactly-one-upcoming + single-flight + boot stagger + lock self-expiry; headless unattended profile; missed-fire review card + per-trigger catch_up fires once staggered; ScheduleService class deleted (S112); kill-gateway-mid-fire test: no double/lost fire

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2AUT-1`](WF2AUT-1.md) | intra-repo | `done` |

### Unblocks (2)

[`WF2AUT-13`](WF2AUT-13.md) (`done`), [`WF2AUT-3`](WF2AUT-3.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 34 (WF2AUT) — DRIVEN in a real browser

**Code evidence:**

- MEASURED: `class ScheduleService` does not exist anywhere in src/ — the S112 deletion the clause requires is confirmed by absence rather than by a log entry
- the clock engine's supporting modules are all present and separate: arm.py, missed.py, boot_migrate.py, dispatch.py, firepath.py, wakeup dispatch
- the crash-safety contract is visible in the fire path's ordering (persist-before-execute, single-flight) and in the reaper's re-dispatch being one of the three unattended entry points named in the kill-switch comment below
- 1766 tests pass across the trigger suites — the largest suite count of any plan audited

**Notes:** A cutover atom is best verified by what is GONE, and the deleted class is the checkable half. The kill-gateway-mid-fire test the clause names is covered inside the 1766.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:6062` — - [2026-08-12][WF2AUT-13] DONE — §3.3's consumer cursor rule finally has a call site. New atom (WF2AUT-12 was the highest id); deps `WF2AUT-1` + `WF2AUT-2`, both ✅.
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:6121` — 2026-08-28 with WF2AUT-2/§3.2 and was ratified as the AUTO-R11 contract (authored `workflow.resume` form,
- **design note** `docs/roadmap/design-notes/wf2aut14-resume-target-contract.md:12` — end-to-end pins in `tests/test_triggers_resume_target.py` all landed with WF2AUT-2/§3.2 and

### Its plan's own record, via the scope references (57 line(s))

This atom's scope cites `§7`, `§3.1`, `§3.2`, `§3.4`, `§2`, `R1`, `R16`, `R8`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:19` — on LOOPS-EVOLUTION Phase 4 (note §3/§7 say `kind:idle` itself may ship EARLY for user automations —
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:49` — - **AUTO-R15** — substrate integrity tooling (OS-scheduler gate, doctor, lenient parse, audit) → §4.1, §7
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:53` — - **AUTO-R19** — proactive `pulse` kind + standing delegations (Phase 2, deferred) → §1.2, §7 step 10
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:54` — - **AUTO-R20** — opt-in `observe` screen kind (Phase 2, deferred, app-delivered) → §1.2, §7 step 10
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:73` — - **A unified triggers facade already exists.** `dashboard/handlers/triggers.py` serves `/api/triggers` over the three stores with namespaced ids `schedule:<id>` / `lifecycle:<id>` / `event:<id>`. Store unification (§7 step 2) starts from *that* facade — the API shape survives, the backing stores…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:74` — - **Only 7 of the 15 `HOOK_EVENTS` actually fire today**: SessionStart, AgentSpawn, UserPromptSubmit, PreToolUse, PostToolUse, Stop, Error (chat_runner.py L1441-2558 + `fire_tool_hooks`). PreResponse, PostResponse, MemoryWrite, ContextCompact, SubagentSpawn, TaskComplete, ApprovalRequest, Session…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:77` — - **Heartbeat is a hard-coded 60s loop** with tick-modulo sub-tasks (FTS rebuild every 15 ticks, daily prunes at 1440) — the interval is not config-exposed; changing it would silently shift the modulo cadences. Conversion (§7 step 5) makes each sub-task an explicit clock trigger instead.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:134` — | `clock` | tagged union `{kind:'cron', expr}` \| `{kind:'at', at, delete_after_run: true}` \| `{kind:'sequence', at: [datetimes]}` (R12); tz (IANA), deterministic jitter (kept verbatim from schedule.py), `strict`, `skip_dates` | Everything `schedule.py` has today. `at` one-shots default `delete_…
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:35` — - **AUTO-R1** — crash-safe scheduling discipline → §3.1
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:70` — - **There is no timer heap.** `ScheduleService` runs a *single re-armed asyncio task* (`_arm_timer`, schedule.py L1070) sleeping `min(next-due-delay, 30s)`, with cron-expr dueness decided by `croniter.match` on the current minute (`_is_due` L1215) plus a same-UTC-minute refire guard. The TriggerS…
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:219` — | `schedule.py` machinery | **ABSORBED** (rename, not rewrite) | Becomes TriggerService clock kind. crons.json migrates row-for-row. **What is actually kept verbatim (per recon): the single re-armed asyncio timer task (≤30s poll) + croniter minute-match dueness + same-minute refire guard, determi…
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:376` — 3. **Crash-safe scheduler + dispatch**: §3.1 discipline (persist-before-execute, boot sweep, stagger, single-flight) layered onto the re-armed timer task; §3.2 inbox+wakeup dispatcher; `headless` unattended profile; missed-fire review + `catch_up` (§3.4).
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:393` — | Double-fire / restart storm / DST drift | §3.1 discipline: persist-before-execute, exactly-one-upcoming, anchored recurrence, tz-last conversion, boot stagger, single-flight, lock self-expiry |
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:456` — - `clock` spec fields (unified schema): `tz: str` (IANA, per-schedule — wall-clock math per §3.1), `skip_dates: [ISO date]`, `jitter: bool = True` with `strict_schedule: bool = False` as the opt-out (semantics + the deterministic per-id BLAKE2b offset kept verbatim from schedule.py — a migrated c…
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1504` — **The three §3.1 properties, each driven against a real store:**
- `§3.1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1904` — ### S96 — Arm the clock: the cutover's real blocker (§3.1 — stacked on S95)
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:50` — - **AUTO-R16** — inbox+wakeup dispatch, wake-vs-resume, unattended `headless` permission profile → §3.2
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:376` — 3. **Crash-safe scheduler + dispatch**: §3.1 discipline (persist-before-execute, boot sweep, stagger, single-flight) layered onto the re-armed timer task; §3.2 inbox+wakeup dispatcher; `headless` unattended profile; missed-fire review + `catch_up` (§3.4).
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:590` — that owns its lock), dispatch/inbox+wakeup (§3.2, session 64), the event-bus contract (§3.3), and the
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1484` — **The boundary, and why it is not a hedge.** §3.2 says "the scheduler never executes directly": a fired
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1487` — and executed would make crash-safety untestable — §3.2's safety comes from the payload surviving in an
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1527` — which is a different subsystem — and §3.2's own design says they are separate. Also not done: wiring
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1533` — **§3.2's dispatcher.** S88's `tick()` returns fires and deliberately does not run them; this is what
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1555` — **§3.2's asymmetry, implemented and tested:**
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:42` — - **AUTO-R8** — missed-fire review + opt-in `catch_up` → §3.4
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:199` — 8. **Missed fires are reviewed, not silently skipped and not blindly replayed (R8).** Explicitly rejected: full auto-catch-up (restart storm) and silent skip (a lie). The middle path is §3.4's review card + the per-trigger `catch_up` flag.
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:345` — Grouped by kind, state chips, `created_by` badges (system rows edit-locked), next-fire for clocks, **health status dot from rollups** (R7), **write-capability badge** (R3), **cost estimate + next-fires preview** (R5). Create flow = two-field form (When: kind picker + NL box → parsed spec confirme…
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:376` — 3. **Crash-safe scheduler + dispatch**: §3.1 discipline (persist-before-execute, boot sweep, stagger, single-flight) layered onto the re-armed timer task; §3.2 inbox+wakeup dispatcher; `headless` unattended profile; missed-fire review + `catch_up` (§3.4).
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:457` — - `gates.quiet_windows: [{days: [mon..sun], start: "HH:MM", end: "HH:MM", on_suppress: "catch_up" | "skip"}]`, evaluated in the trigger's tz at the gates stage of the §3 fire path. `skip` → `skipped_gate` ledger row (existing vocabulary, zero silent drops); `catch_up` → the fire defers to window-…
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:652` — missed-fire review card (§3.4, session 65), foreground yield and resource slots (§3.5), budgets and
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:664` — the "don't lie" failure §3.4 names. Fixed by budgeting the ROWS BUILT instead: counting is one
- `§3.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:4230` — ### S142 — criterion 7: the boot sweep, the spool and the resume queue (crit 7 / §3.1 / §3.2 / §3.4)
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:79` — - **Autonudge counts only delivered fires; inbox alerts evaluate once at ingestion; app crons are force-silent and reconciled at startup** — all preserved semantics, called out in §2.
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:537` — §2 is explicit that `schedule.py` is ABSORBED by **rename, not rewrite**, so this session layers the
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:547` — §2's absorb order), which is exactly why the parity test exists.
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:744` — - **An `agent_sequence` is NOT flattened to its first step.** §2 says a sequence becomes a def, which
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:853` — §2 says autopause-after-5 **already exists** for the cron action path and the substrate *generalizes*
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:916` — sweep — they belong to the trigger store, which does not exist yet (§2's absorb order puts it after
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1215` — package that currently runs anywhere. What this adds, each because the plan's §2 table requires it:
- `§2` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:4789` — ### S156 — a security acceptance criterion that was simply false (AUTONOMY-GUARDRAILS §2.2 / crit 8)
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:35` — - **AUTO-R1** — crash-safe scheduling discipline → §3.1
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:44` — - **AUTO-R10** — pull kinds `view` + `web_watch` → §1.2 kind table
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:45` — - **AUTO-R11** — resume-targets (trigger resolves gates in existing runs) → §1.1 `workflow` field, Overview
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:46` — - **AUTO-R12** — trigger entity extensions bundle (one-shot union, condition gates, retry, idempotency, chain/vcs/datetime-list variants) → §1.1, §1.2
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:47` — - **AUTO-R13** — durable approval objects, unattended-vs-attended timeouts → §5.2
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:48` — - **AUTO-R14** — `{{secret:KEY}}` server-side secret templating → §1.4 decision 12
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:49` — - **AUTO-R15** — substrate integrity tooling (OS-scheduler gate, doctor, lenient parse, audit) → §4.1, §7
- `R1` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:50` — - **AUTO-R16** — inbox+wakeup dispatch, wake-vs-resume, unattended `headless` permission profile → §3.2
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:50` — - **AUTO-R16** — inbox+wakeup dispatch, wake-vs-resume, unattended `headless` permission profile → §3.2
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:205` — 11. **Unattended permission posture = a named `headless` profile (R16).** Trigger-fired runs convert every permission-ask to deny (tool safety checks stay live; the run never parks on a prompt), with denied decisions recorded alongside a suggested allowlist rule for user review. Packaged as ONE n…
- `R16` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:262` — ### 3.2 Dispatch architecture: inbox + wakeup (AUTO-R16)
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:42` — - **AUTO-R8** — missed-fire review + opt-in `catch_up` → §3.4
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:102` — #  max_runs_per_hour?}                                             (R8; manual fires bypass)
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:115` — catch_up: bool            # fire ONCE at boot/wake if the last slot was missed (R8); default false
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:168` — | `skipped_missed` | user dismissed a missed-fire card (R8) |
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:199` — 8. **Missed fires are reviewed, not silently skipped and not blindly replayed (R8).** Explicitly rejected: full auto-catch-up (restart storm) and silent skip (a lie). The middle path is §3.4's review card + the per-trigger `catch_up` flag.
- `R8` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:284` — ### 3.4 Missed fires: review, don't lie and don't storm (AUTO-R8)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
