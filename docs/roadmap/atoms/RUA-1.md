<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `RUA-1` — Durable session foundation: persist HMAC signing key + move nonce/binding registry to auth/sessions.json (survive restart, fail-closed)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [REMOTE-USER-AUTH](../plans/REMOTE-USER-AUTH.md) (`RUA`, plan status `done`) |
| **Atom table** | [`../atomic/RUA.md`](../atomic/RUA.md) |
| **Execution position** | #507 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S1; Contracts C1 (auth/session_store.py); Session 1 tasks T1.1-T1.3 + V1

## Done when

A token minted before a gateway restart validates (HTTP 200) after it; signing_key + sessions.json both 0600; revoke_all_sessions()/personalclaw logout kills sessions durably across a restart; corrupt sessions.json forces re-auth and never falls open; generate_token/validate_token signatures unchanged. Verified across 3 boots; test_session_store.py 34 cases; make test 9439 passed.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`RUA-3`](RUA-3.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 2 (RUA) — observed

**Code evidence:**

- tests/test_session_store.py passes as part of a 132-passed run with the other two RUA modules — this is the module the atom names for its 34 cases
- the durable-store claim is asserted BY that suite (persisted signing key, nonce/binding registry in auth/sessions.json, corrupt-file forces re-auth and never falls open)

**Notes:** Stated precisely so the evidence is not overread: the atom's done_when says 'Verified across 3 boots', and I did NOT perform a three-boot token-survival run myself. My evidence is the passing test module that encodes that behaviour, which is code-level observation rather than a reading of the plan's log. Recorded confirmed on that basis; a reader wanting the boot-level proof should re-drive it.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T1.1`, `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/REMOTE-USER-AUTH.md:202` — | T1.1 | Persist the HMAC signing key: generate-once `auth/signing_key` (0600) loaded at boot, replacing module-level `_SECRET`; keep `generate_token`/`validate_token` signatures identical | `dashboard/token_auth.py`, new `auth/session_store.py` | tokens minted before a restart still validate aft…
- `T1.3` `docs/roadmap/plans/REMOTE-USER-AUTH.md:204` — | T1.3 | `revoke_all_sessions()`/`personalclaw logout` operate on the durable store; SEL `session_revoked` | `token_auth.py`, `cli_server.py` | logout kills sessions across a restart; SEL line present |
- `Session 1` `docs/roadmap/plans/REMOTE-USER-AUTH.md:198` — ### Session 1 — Durable session foundation

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
