<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AAP-2` — Phase 1 validation — codex end-to-end sweep

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ACP-AGENT-PARITY](../plans/ACP-AGENT-PARITY.md) (`AAP`, plan status `done`) |
| **Atom table** | [`../atomic/AAP.md`](../atomic/AAP.md) |
| **Execution position** | #2 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1932 |

## Intent — what this is for

Phase 1 — VALIDATION (§6 12-step checklist, codex column)

## Done when

Checked-in verified matrix column for codex with every audit cell CONFIRMED or DIVERGED at runtime (zero UNKNOWN cells: compaction, slash commands, context-%, subagent inject-back resolved) and findings added to the severity-ranked gap inventory.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (7)

[`AAP-10`](AAP-10.md) (`done`), [`AAP-4`](AAP-4.md) (`done`), [`AAP-5`](AAP-5.md) (`done`), [`AAP-6`](AAP-6.md) (`done`), [`AAP-7`](AAP-7.md) (`done`), [`AAP-8`](AAP-8.md) (`done`), [`AAP-9`](AAP-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `05c38590b` 2026-08-23 — fix(roadmap): de-duplicate AAP.md's atom table after the union resolution
- `c4f29d301` 2026-08-23 — chore(roadmap): cite AAP-1, AAP-2, AAP-3 and AS-8 to the batch PR
- `4fd5a7ecc` 2026-08-23 — docs(acp): AAP-2 close the codex validation column, completing Phase 1
- `8352ca5fc` 2026-08-17 — docs(acp): AAP-2 codex end-to-end validation sweep

### Named by atom id in the roadmap prose (56)

- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:413` — ABSENT. So that row is now **DIVERGED**, and the same re-drive is owed to `AAP-2`'s `C14`, whose
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:814` — ## Phase 1 results — codex verified matrix (atom `AAP-2`)
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1212` — `measurement="AAP-2 sweep — residual set measured EMPTY", entries=()`
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1328` — · **Host:** this repo at AAP-2's tip (`aa2610dc`), isolated
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1331` — `PERSONALCLAW_ACP_NO_PROVISION=1` (a no-op for a native binary, set for symmetry with `AAP-2`).
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1346` — **Method.** Same shape as `AAP-1`/`AAP-2`: the `kiro-cli-agent` bundle was installed from the
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1355` — log level never emitted the `cwd=`/`pool_cwd=` lines `AAP-2` read, so the in-CLI assertion was the
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1358` — **Marks.** Same vocabulary as `AAP-1`/`AAP-2`: `CONFIRMED` — the runtime matches the audit's
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1365` — Ledger ids are `K…` so they never collide with `AAP-1`'s `O…` or `AAP-2`'s `C…`.
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1374` — | `K5` | the `pwd` call's own frames, captured on `/api/ws` and resolved with `POST …/{S1}/approve {action:"approved"}` | **`pwd` raised an approval card** — `risk: "safe"`, `is_read_only: "1"`, and the turn **blocked** until approved (codex auto-resolved its reads and its six `cat` calls with no…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1377` — | `K8` | S2 turn 2 (`"Reply with exactly: SEQ2"`) immediately after `K7`, then re-count the trees | **the process IS reused across a session's own turns** — no new tree appeared (`2883 17853 19719` before and after) and the reply came back on the existing one. This **differs from codex**, where `…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1378` — | `K9` | `ls $PERSONALCLAW_HOME/session_pid_*.txt` with three sessions live and three CLI trees running | **only ONE pid file exists** — `session_pid_2883.txt` → `dashboard:chat-1-1787030505` (S1). S2 and S3 have live processes and no pid file at all, where `AAP-2` measured one file per session o…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1381` — | `K12` | S1, task mode `agent`: "read `…/scratch/probe.txt`, create `…/scratch/written.txt` = `HELLO`, `rm …/scratch/doomed.txt`", `/api/ws` captured, every card resolved `approved` by a poller | all three ran and the state changed as asked (`written.txt` created, `doomed.txt` gone). **Every one…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1382` — | `K13` | the same turn's eight `tool_call` frames vs its `approval` frames vs its SEL rows | **`AAP-1`'s `G2` (contingent gate coverage) is now MEASURED, not hypothetical.** kiro's native `todo_list` tool fired four times in that turn — `Creating task list: …`, `Completing #1`, `#2`, `#3` — each…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1383` — | `K14` | S3 with `POST /api/chat/task-mode {mode:"ask"}` set before the turn, then "use your write tool to create `…/scratch/ask-mode-probe.txt`" | **the write was blocked and the file never appeared** — and kiro handles it far better than codex did. The user is told why, inline, in the tool lin…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1385` — | `K16` | S2 turn: a correction ("No - stop doing that. Always answer me in exactly one sentence from now on, never more.") | two `learned` activity events: **`Learned: never more`** and `Learned: User correction to honor: …` — the identical bad extraction `AAP-1` and `AAP-2` measured, byte-for-b…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1389` — | `K20` | restarted the gateway with the same env, then inspected every session, then sent one turn | **all four sessions came back with `acp_provider: null`, `reasoning_effort: null`, `workspace_dir: null`, `mode: null` and (this sweep's addition) `task_mode: null`**, while `model` survived — th…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1390` — | `K21` | after that restart: `POST …/{s}/acp-agent` on each of the three pre-restart sessions, and on a brand-new one | the new session bound fine; **all three pre-restart sessions returned `{"error": "not found"}`** — even though `GET /api/chat/sessions` lists them. They become re-bindable only…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1393` — | `K24` | the control for `K23`: the same session key after a gateway restart (fresh CLI process), `task_mode` set to `agent`, then "quote the line starting `## Task mode:`" | `"## Task mode: Agent\nYou are in AGENT mode -- full execution…"` — **correct.** So the framing is assembled correctly pe…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1407` — | Task-mode framing (Agent/Ask/Plan/Build suffix) | WIRED | **DIVERGED** | the block IS injected and its VALUE goes stale on a reused process | `K23` — the framing quoted verbatim reads `## Task mode: Plan` on a session the API reports as `task_mode: agent`; `K24` — the same session on a fresh pr…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1428` — | SEL audit of every executed tool + effective risk | WIRED | CONFIRMED | WIRED — and better named than on codex, with one internal contradiction | `K12`, `K13`, `K15` — hash-chained `tool_invocation` rows for **every** executed tool including the ungated ones, each carrying the real operation ti…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1480` — | Reasoning effort per turn | WIRED | **DIVERGED** | the axis does not exist on kiro, yet the host accepts, persists and echoes a value | `K2` — `supported_efforts: []` on all 27 agents; `K10` — a bind with `reasoning_effort: "low"` returns `{"ok": true, …, "reasoning_effort": "low"}`. This is `A…
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1581` — ### Gap inventory — severity-ranked (kiro-cli findings, continuing `AAP-2`'s numbering)
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1593` — `Learned: never more` extraction — `K16`). `AAP-2`'s `G17` (no config-isolation lever) also
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1625` — `AAP-2` concluded "across the whole sweep no ACP tool executed without passing the host gate";
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1676` — provider error, then re-bind", and nothing surfaces it. This is why `AAP-2` was able to re-bind
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1743` — '…'` → `kind: read` and auto-approved (`AAP-2`'s `C5`, `C10`). One provider's users approve every
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1761` — - **`AAP-2`'s `G21` is confirmed on the provider §2.6 named it for.** `supported_efforts: []` on all
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1785` — blocker.** `AAP-2` filed `G26` because codex refuses ("I can't provide hidden context or
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1908` — **↳ RE-CLASSIFIED 2026-08-23 — this is NOT cosmetic. `AAP-2`'s sweep found the security
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:1934` — files `AAP-2` declined to touch, and outside this atom's fence. Each is filed with its exact wire
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2639` — - 2026-08-17 — `AAP-2` **PARTIAL**. Ran the audit §6 checklist against `acp:codex` (adapter
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2674` — measured confirmation of `AAP-2`'s `G21` on the provider §2.6 named it for. Zero incidental fixes —
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2680` — turns where the read, write and `rm` were carded. `AAP-2` concluded "no ACP tool executed without
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2757` — this machine), so their "residual set EMPTY" entries carry `AAP-1`/`AAP-2`'s measurement, not a
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2838` — `AAP-1`/`AAP-2`/`AAP-4`/`AAP-5`).** A `cron:`-keyed unattended session forwarded
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2946` — Picked `AAP-3` as the highest-unblock ready atom (it and `AAP-1`/`AAP-2` each gate the same seven
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2953` — `AAP-1`/`AAP-2` on nothing.** In a fresh isolated home (a copy of the dev home), `GET
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:2967` — So `AAP-1` and `AAP-2` are **not** environment-blocked; their residual cells are fixture and
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3087` — sweeps (`AAP-1`/`AAP-2`/`AAP-3`), all still `todo`. See the 2026-08-19 discovery entry above for the
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3273` — | 595, 603, 604, 628 | **codex (`AAP-2`)**, prior column — a different provider's atom |
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:3279` — | 1355, 1369, 1397 | prose — the `AAP-1` / `AAP-2` / `AAP-3` execution-log entries |
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4293` — exact signature `O12`, `AAP-2`'s `C14` and `AAP-3`'s `K17` each used to conclude ABSENT. The rows
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4326` — - [2026-08-23][AAP-1] **Recipe notes worth more than the marks, for whoever drives `AAP-2`/`AAP-3`.**
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4348` — than claiming the mutation. `AAP-2` and `AAP-3` keep their own residual cells; only the claude-code
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4399` — - **STILL UNVERIFIED / owed elsewhere.** `AAP-2`'s `C14` is still owed the M5d re-drive on this recipe.
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4402` — `G78` is **partly retracted** by `K94`. And this column's closure says nothing about `AAP-2`, whose 20
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4404` — ## Execution log — `AAP-2` (Phase 1 validation, codex end-to-end sweep)
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4406` — - [2026-08-23][AAP-2] **DONE — and with it all three Phase 1 columns.** The codex column's **20
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4411` — - [2026-08-23][AAP-2] 🔴 **`C14` corrected, and dated: `Procedural-outcome capture (M5d)` → DIVERGED,
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4417` — - [2026-08-23][AAP-2] 🔴 **The three providers fail this cell in three different ways, which is why a
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4424` — - [2026-08-23][AAP-2] 🔴 **`G88`: the registry's "measured EMPTY" claim is now falsified for TWO of
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4431` — - [2026-08-23][AAP-2] 🔴 **`G93`: `cron:` unattendedness is lost across a restart, silently.** Verified
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4437` — - [2026-08-23][AAP-2] **Three claude-code recipes did NOT port, which is the transferable lesson.**
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4445` — - [2026-08-23][AAP-2] **A mechanism correction to `AAP-1`'s `G56`.** Killed pre-text, codex **recovers**
- **source plan** `docs/roadmap/plans/ACP-AGENT-PARITY.md:4449` — - [2026-08-23][AAP-2] **Two stale premises in this column disproved:** the auto-nudge cell was recorded

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§6`. Those tokens appear in its source plan at:

- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:48` — **Method:** the campaign validation doctrine — real UI via Chrome DevTools MCP, every cycle **mutates state → inspects persistence → verifies consumers** (no shallow render-checks). One provider per session, in order **claude-code → codex → kiro-cli**, each driving the whole platform end to end. …
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:150` — **Phase 2 effort: ~6 sessions** — (a) MCP reachability 1.5 (two prongs × three CLIs); (b) approval-gate coverage 1; (c) unattended + loop guards 1; (d) resume 0.5; (e) learning + fidelity 1; (f) dialect closure + project stamping + parity doc 1. Each fix closes with a re-run of the relevant §6 ch…
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:181` — | Parity doc rots as CLIs/adapters update | It's generated from the checked-in verified matrices; re-running the §6 checklist is the documented refresh procedure, and each entry names the CLI/adapter version it was verified against |
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:212` — through the audit §6 checklist over the same HTTP/SSE + WebSocket surfaces the dashboard
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:827` — (`POST /api/chat/sessions/{s}/acp-agent`) and driven through the audit §6 checklist over the
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:1349` — (`POST /api/chat/sessions/{s}/acp-agent`) and driven through the audit §6 checklist over the same
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2626` — - 2026-08-17 — `AAP-1` **PARTIAL**. Ran the audit §6 checklist against `acp:claude-code`
- `§6` `docs/roadmap/plans/ACP-AGENT-PARITY.md:2639` — - 2026-08-17 — `AAP-2` **PARTIAL**. Ran the audit §6 checklist against `acp:codex` (adapter

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
