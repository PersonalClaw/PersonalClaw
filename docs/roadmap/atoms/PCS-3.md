<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PCS-3` — Neutral cache-marker module + middleware wiring + OpenAI AUTOMATIC

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROMPT-CACHE-SUBSTRATE](../plans/PROMPT-CACHE-SUBSTRATE.md) (`PCS`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PCS.md`](../atomic/PCS.md) |
| **Execution position** | #411 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##885 |

## Intent — what this is for

Session 2 / T2.1+T2.2; §C1, §C4 (capability field), §C5 (generation bump)

## Done when

llm/prompt_cache.py ships PromptCache enum, CACHE_HINT_KEY, mark_cacheable_prefix (NONE/AUTOMATIC return the input object unchanged, EXPLICIT hints exactly one non-tool message via shallow copy, never mutates caller dicts, vendor-string grep returns zero); ProviderCapability.prompt_cache defaults NONE; OpenAI adapter declares AUTOMATIC and translates nothing; middleware called in the native loop before complete() with a loop-owned _cache_generation bumped on compaction (runtime.py:1292) and agent-definition change, DEBUG-logged; every undeclared provider's message list is byte-identical to today.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PCS-1`](PCS-1.md) | intra-repo | `done` |

### Unblocks (4)

[`PCS-4`](PCS-4.md) (`done`), [`PCS-5`](PCS-5.md) (`done`), [`PCS-8`](PCS-8.md) (`done`), [`PCS-9`](PCS-9.md) (`blocked`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 43 (PCS) — vendor rail falsified against the real tree; toggle driven

**Code evidence:**

- MEASURED: llm/prompt_cache.py ships PromptCache (NONE/AUTOMATIC/EXPLICIT), CACHE_HINT_KEY and the marker helper, and the module is verifiably vendor-free — test_the_neutral_marker_module_stays_vendor_free asserts no vendor pattern and no 'ephemeral' string appears in it
- 🔑 THE PROVIDER-BOUNDARY CLAIM IS STRUCTURAL, NOT ASPIRATIONAL (:5-7): 'this module deliberately contains ZERO wire strings and no provider SDK import, so importing it never violates Property 11 (Provider SDK Lazy Import)'. The neutrality is what makes the module safe to import anywhere
- 🔑 THE GRADED SHAPE IS ARGUED BY PRECEDENT RATHER THAN INVENTED (:9-12): it 'mirrors StructuredOutput' because 'a per-request marker is a GRADED behavior (some families need one, some need none), so it rides on a provider as its own value rather than a boolean flag'. NONE is called 'the correct, safe default for any provider that has not opted in'
- AUTOMATIC's docstring names its own dependency: the stable-prefix ordering from PCS-1 'is what makes this work; there is nothing for the loop to mark'
- 78 across marker/wire-translation/cache-usage/single-store/pricing-savings/citation-rail; bedrock-models app 39/39

**Notes:** Reusing an existing graded-capability shape rather than adding a boolean is the same discipline CATO showed by reusing the feedback module's cap convention. A boolean would have forced AUTOMATIC and EXPLICIT to be distinguished somewhere else, which is how a second dialect starts.

## Recorded history

### Commits (6)

- `7869b1341` 2026-09-02 — chore(roadmap): execute the owner-ruled PCS-7 split (numbers half done, PCS-9 minted)
- `67ca36b1d` 2026-08-28 — fix(llm): PCS-3's OpenAI AUTOMATIC clause was unmet under a done row
- `5acb2a503` 2026-08-14 — feat(llm): PCS-4 translate the neutral cache hint to Anthropic cache_control
- `17dc1a9f7` 2026-08-07 — feat(channels): CE-1 sender-trust core seam — the channel-trust contract for CE-2..9 (#895)
- `1c5b579bf` 2026-08-07 — feat(learning): WF2LEA-3 reroute /api/lessons onto memory.db, delete LessonStore (#894)
- `ef63529bb` 2026-08-06 — feat(llm): PCS-3 neutral prompt-cache marker + native-loop middleware (§C1/§C4) (#885)

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:300` — `CACHE_HINT_KEY` PCS-3 places and attaches `{"type": "ephemeral"}` to the **last content block** of
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:356` — - **PCS-4 DISCOVERY — PCS-3's "OpenAI adapter declares AUTOMATIC" clause is UNMET on `main`.**
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:360` — a defect, and it is PCS-3's row rather than PCS-4's. Left untouched here so the miss stays visible;
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:361` — it is a one-line fix whenever PCS-3 is revisited (PCS-7's V2 asserts "OpenAI reports vendor reads
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:393` — (PCS-3's middleware, which IS on main), so the mode is already collapsed before any adapter could
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:473` — - **PCS-8 DISCOVERY — PCS-3's unmet "OpenAI adapter declares AUTOMATIC" clause is still unmet, and
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:480` — Left untouched: the fix is one line in `llm/openai.py` and belongs to PCS-3's row, and
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:690` — `PromptCache.NONE` (PCS-3's row, recorded twice above). Not chased — paying real OpenAI spend for a
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:786` — `PCS-3`'s still-unmet "OpenAI adapter declares AUTOMATIC" clause. Neither was simulated. Paying
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:870` — - **2026-08-28 — PCS-3 CLAUSE CLOSED, and a TRACKING CORRECTION that matters more than the one-line fix:
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:871` — `PCS-3` has read `status: "done"` in `dag.json` (PR #885) while one of its own `done_when` clauses was
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:885` — `PCS-3`'s still-unmet clause that the OpenAI adapter declare AUTOMATIC (today `llm/openai.py` declares
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:887` — finding was correct, written down, and still could not move `PCS-3`'s row — because nothing reconciles a
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:893` — the driver's**; the dated correction sits in `PCS.md`'s `PCS-3` section body instead.
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:929` — **Byte-identity is now railed, because this change moves OpenAI OUT of "undeclared"** and `PCS-3`'s last
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:953` — and `PCS-3` still read `done`. Closing it makes the ratchet move DOWN: `enum` 9 → 8, total inert 240 → 239,
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:996` — and there is no `OPENAI_API_KEY` (PCS-3, now done, supplies the OpenAI AUTOMATIC posture the run
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:997` — depends on). `PCS-9.deps = [PCS-7, PCS-3]`. The dag regenerates to **661 atoms, 0 dangling**; the

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T2.1`, `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:241` — | T2.1 | `llm/prompt_cache.py`: `PromptCache`, `CACHE_HINT_KEY`, `mark_cacheable_prefix` (every C1 clause incl. shallow-copy-never-mutate); unit tests incl. **the byte-identical assertion** for `NONE`/`AUTOMATIC` (assert the returned list `is` the input and no dict gained a key) | `src/personalcl…
- `T2.2` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:242` — | T2.2 | `ProviderCapability.prompt_cache` (default `NONE`, docstring mirroring `structured_output`'s rationale); core OpenAI adapter declares `AUTOMATIC`; middleware wired into the native loop before `complete()` with a loop-owned `_cache_generation` bumped on compaction (`runtime.py:1292`) and …
- `T2.2` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:293` — measure-before-claiming gate remain PCS-1/T2.2/S3. No user surface → no CHANGELOG. **Gates:**
- `Session 2` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:237` — ### Session 2 — The neutral marker + provider adoption

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
