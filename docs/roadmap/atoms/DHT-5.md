<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DHT-5` — Agent tools: document_create + sheet_create + document_formats

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-HANDLING-TOOLS](../plans/DOCUMENT-HANDLING-TOOLS.md) (`DHT`, plan status `done`) |
| **Atom table** | [`../atomic/DHT.md`](../atomic/DHT.md) |
| **Execution position** | #172 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Contracts C3 (agent tools); T1.5

## Done when

tools in mcp_artifacts.py create real artifacts, project-scoped + SEL-audited on success/refused/error; re-running same name updates in place (no -2 slug, reusing artifact_save dedup); oversized output returns a typed refusal naming the real size before storing; reply carries slug+version+raw URL, never bytes/base64

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DHT-2`](DHT-2.md) | intra-repo | `done` |
| [`DHT-3`](DHT-3.md) | intra-repo | `done` |
| [`DHT-4`](DHT-4.md) | intra-repo | `done` |

### Unblocks (3)

[`DHT-10`](DHT-10.md) (`done`), [`DHT-6`](DHT-6.md) (`done`), [`DHT-7`](DHT-7.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `T1.5`. Those tokens appear in its source plan at:

- `T1.5` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:177` — | T1.5 | Agent tools `document_create` + `sheet_create` + `document_formats` in `mcp_artifacts.py`, following the module's project-scoping + `_audit` shape; size guard returns a typed refusal naming the real size before storing | `src/personalclaw/mcp_artifacts.py`, tests | tools create real arti…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
