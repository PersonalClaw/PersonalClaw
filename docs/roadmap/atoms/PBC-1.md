<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PBC-1` — Manifest seam fields: cli.setup, cli.doctor, loggerRoots (contract C1)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROVIDER-BOUNDARY-COMPLETION](../plans/PROVIDER-BOUNDARY-COMPLETION.md) (`PBC`, plan status `done`) |
| **Atom table** | [`../atomic/PBC.md`](../atomic/PBC.md) |
| **Execution position** | #402 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #2682251 |

## Intent — what this is for

S1 T1.1; Contracts C1 (Manifest fields); Design B/C/D field definitions

## Done when

AppManifest gains CliConfig(setup, doctor) + loggerRoots with to_dict/from_dict parity and unknown-field forward-compat; round-trip test passes; absent fields default empty; existing manifests still parse (test_app_manifest.py extended)

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`PBC-2`](PBC-2.md) (`done`), [`PBC-3`](PBC-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T1.1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:135` — | T1.1 | Manifest fields: add `cli.setup: str` (module:function), `cli.doctor: str`, `loggerRoots: list[str]` to the manifest dataclasses with to_dict/from_dict parity; unknown-field forward-compat untouched | `src/personalclaw/apps/manifest.py` + its tests | round-trip test for the three fields;…
- `T1.1` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:164` — - [2026-07-20][T1.1] DONE: `CliConfig` + `loggerRoots` on `AppManifest` (to_dict/from_dict parity, unknown-field preservation); `tests/test_app_manifest.py` extended. commit `2682251`.
- `T1.1` `docs/roadmap/plans/PROVIDER-BOUNDARY-COMPLETION.md:174` — - [2026-07-20][HANDOFF] Executed via a code-kind goal loop (052ae197) that produced T1.1/T1.2a/T1.4 across parallel git worktrees, then stalled on a worktree-merge collision (worker also committed to the base branch → divergent history). Per owner authorization, the maintainer took over: consolid…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
