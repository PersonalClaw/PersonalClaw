<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-6` — S3.5 rest of companion: loops/tasks/inbox/notifications sections + SW sound/badge mapping

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #340 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, verified 2026-08-27. Clause 1 is FULLY SHIPPED and gated (the MC.md glyph understates it; the honest mark is a partial, not an empty box): CompanionSections ships RunningLoops/Tasks/Inbox/Recent in the asserted order, MC-3's 'Not on the phone yet' stub list is DELETED rather than flagged off, useCompanionAction owns revert AND reconcile once for all four, and every endpoint has a real client. Loops are read-only-plus-actions deliberately because PP-16 is unifying Loops concurrently. Clause 2 is UNMET on three independent absences, any one decisive: web/src/sw.ts is 122 lines with install/activate/fetch and NO push handler at all; web/src/app/pushPolicy.ts - the clause's actual call site for notificationFor() - does not exist; and the `rules` field the clause reads FROM does not exist, because notification_rules.Rule is mode/targets/conditions/verify with no `sound` and no per-kind badge key. SMALLEST REMAINING UNIT: land MC-5 first (it owns the sw.ts push handler and pushPolicy.notificationFor), then add `sound` to Rule + _coerce_rule + rules_document() + the PUT guard + a control in NotificationRulesMatrix, naming a voice from the EXISTING closed set in web/src/design/soundCues.ts rather than minting a vocabulary. This is entity_settings state, so the config round-trip contract does not apply and loader.py does not grow. Platform constraint for whoever takes it: a service worker CANNOT play audio, so 'a distinct sound fires' means silent/vibrate on the notification plus a message to an open client that plays the voice. |

## Intent — what this is for

Amendment §S3.5 (T3.5.1) — former S2 T2.1/T2.2 breadth

## Done when

Companion adds Running-loops (pause/nudge/stop via loop_routes), tasks, inbox-resolve, and recent-notifications sections working per the original S2 done-whens; the SW maps a push payload's `kind` to per-kind sound/badge using plan-42's rules field (a distinct sound fires for a kind configured in the rules UI).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MC-3`](MC-3.md) | intra-repo | `done` |
| [`MC-4`](MC-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (6)

- `f063ef7fa` 2026-09-03 — docs(roadmap): flip MC-6b + EI-2 (evidence #2359, #2361)
- `53a7334ea` 2026-09-03 — docs(roadmap): flip MI-2, DL-8, BA-5 to done post-#2370 + fix EI-3 placeholder
- `998adf7be` 2026-09-03 — feat(notifications): per-kind sound + badge cue mapping to the service worker (MC-6b)
- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips
- `a15d5bfba` 2026-08-28 — docs(roadmap): strike two EXT deps that pointed at work the atoms already own
- `96516fbae` 2026-08-26 — feat(companion): MC-6 loops/tasks/inbox/notifications sections on the phone

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1346` — Two atoms this week were parked exactly this way (`MC-6`, which owes its own `sound` field, and this one).
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:219` — **2026-09-04 — MC-6 validated done (evidence #2359).** Clause 2 — the SW per-kind sound + badge cue mapping — shipped: the `push` handler in `web/src/sw.ts` and `web/src/app/pushPolicy.ts`'s `notificationFor()` compose the notification from the payload `kind`; a `sound` key was added to `notifica…
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:289` — loops/tasks/inbox/notifications sections are `MC-6`.
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:445` — and it belongs with the surface that adds the companion's other sections (`MC-6`). Recorded here so
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:451` — ### 2026-08-26 — `MC-6` (S3.5 T3.5.1) — **PARTIAL**: the four sections are DONE, the SW sound/badge mapping is not
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:917` — **Rebase note (2026-08-27).** This atom was authored against a base that predated `MC-6`, `MC-7`,
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:919` — `main`'s side: `MC-6` deleted `CompanionPage.tsx`'s "Not on the phone yet" stub list that this atom's
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:807` — Two atoms this week were parked exactly this way (`MC-6`, which owes its own `sound` field, and this one).

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `T3.5`, `T2.1`, `T2.2`. Those tokens appear in its source plan at:

- `T3.5` `docs/roadmap/plans/MOBILE-COMPANION.md:178` — | T3.5.1 | S3.5: loops/tasks/inbox/notifications sections (former T2.1/T2.2 scope) + SW per-kind sound/badge mapping reading plan 42's rules field | companion components, `sw.ts` | sections work per original S2 Done-whens; distinct sound fires for a kind configured in the rules UI |
- `T3.5` `docs/roadmap/plans/MOBILE-COMPANION.md:451` — ### 2026-08-26 — `MC-6` (S3.5 T3.5.1) — **PARTIAL**: the four sections are DONE, the SW sound/badge mapping is not
- `T2.1` `docs/roadmap/plans/MOBILE-COMPANION.md:109` — | T2.1 | Route + IA: `#/companion` with four stacked sections (Approvals, Running, Inbox, Recent) using existing shell primitives + `useIsMobile`; large touch targets; no sidebar | `web/src/pages/companion/` new components, router registration | renders on a phone viewport; URL doctrine holds |
- `T2.1` `docs/roadmap/plans/MOBILE-COMPANION.md:170` — - **S2 — Approvals-first companion:** T2.1 narrows to the Approvals section only (full-context cards); T2.2 narrows to the approve/reject wiring; T2.3/T2.4 (device sessions/devices list — already superseded to plan 54's contract) unchanged.
- `T2.1` `docs/roadmap/plans/MOBILE-COMPANION.md:176` — | T2.1r | Rescope S2 to approvals-only: `#/companion` renders full-context approval cards from `GET /api/approvals`; approve/reject round-trip; other sections stubbed behind S3.5 | `web/src/pages/companion/`, router | phone viewport: a real pending approval renders with tool+args+session context …
- `T2.1` `docs/roadmap/plans/MOBILE-COMPANION.md:178` — | T3.5.1 | S3.5: loops/tasks/inbox/notifications sections (former T2.1/T2.2 scope) + SW per-kind sound/badge mapping reading plan 42's rules field | companion components, `sw.ts` | sections work per original S2 Done-whens; distinct sound fires for a kind configured in the rules UI |
- `T2.1` `docs/roadmap/plans/MOBILE-COMPANION.md:223` — ### 2026-08-13 — `MC-3` (S2 T2.1r + T2.2 approvals half) — **DONE**
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:110` — | T2.2 | Map + wire the control endpoints: approvals (`.../approve`), loop pause/nudge/stop (locate the loops handlers' routes — record the route map in the Execution log), task state transitions, inbox resolve (plan 42 API) | companion components | every action round-trips against a dev gateway;…
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:170` — - **S2 — Approvals-first companion:** T2.1 narrows to the Approvals section only (full-context cards); T2.2 narrows to the approve/reject wiring; T2.3/T2.4 (device sessions/devices list — already superseded to plan 54's contract) unchanged.
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:178` — | T3.5.1 | S3.5: loops/tasks/inbox/notifications sections (former T2.1/T2.2 scope) + SW per-kind sound/badge mapping reading plan 42's rules field | companion components, `sw.ts` | sections work per original S2 Done-whens; distinct sound fires for a kind configured in the rules UI |
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:223` — ### 2026-08-13 — `MC-3` (S2 T2.1r + T2.2 approvals half) — **DONE**
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:236` — **RESOLVED ROUTE MAP (T2.2, approvals half) — and a correction to §C2.**
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:461` — **RESOLVED ROUTE MAP (T2.2, the half `MC-3` deferred).** Every action is the owning surface's own
- `T2.2` `docs/roadmap/plans/MOBILE-COMPANION.md:483` — apart.** T2.2 asks for "optimistic UI reverts on failure". `useCompanionAction` owns both halves

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
