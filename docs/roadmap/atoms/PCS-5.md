<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PCS-5` — prompt_cache_enabled config through all five wiring points + FE control

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PROMPT-CACHE-SUBSTRATE](../plans/PROMPT-CACHE-SUBSTRATE.md) (`PCS`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PCS.md`](../atomic/PCS.md) |
| **Execution position** | #413 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1286 |

## Intent — what this is for

Session 2 / T2.4; §C6 (config, §2.1 five-point wiring)

## Done when

prompt_cache_enabled (default True) wired through dataclass+_meta, load() mapping, to_dict(), the _EDITABLE_CONFIG PATCH allowlist (dashboard/handlers/core.py), and a Models-settings-panel frontend control; test_config_roundtrip.py green; PATCH round-trips; middleware treats disabled as NONE; explicit test asserts the §C2/§C3 ordering repairs are NOT gated by the toggle (no dual path).

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PCS-3`](PCS-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 43 (PCS) — vendor rail falsified against the real tree; toggle driven

**Code evidence:**

- 🔑 DRIVEN TO THREE POINTS: toggled 'Prompt caching' OFF in Settings → Models, a FULL PAGE RELOAD showed the switch unchecked, and config.json on disk carried agent.prompt_cache_enabled = False. Toggled back ON and confirmed True on disk, so the home is left as found
- all five wiring points present: the dataclass field, load(), to_dict(), the PATCH allowlist (handlers/core.py:831) and the real FE control (ModelsPanel.tsx:418)
- 🔑 THE NO-DUAL-PATH GUARANTEE IS SURFACED TO THE USER, not just tested: the toggle's hint ends 'what the model is shown, and in what order, is identical either way.' That is the clause's own 'ordering repairs are NOT gated by the toggle' claim, written where a user debugging a provider will read it
- the section hint frames the feature honestly — 'Providers without cache support are unaffected' — so a user on Ollama does not expect a change
- 78 across marker/wire-translation/cache-usage/single-store/pricing-savings/citation-rail; bedrock-models app 39/39

**Driven in the UI:** Toggle, reload, on-disk value, restored.

**Notes:** The hint's last clause is the useful part for a debugging user: it tells them turning caching off does NOT change what the model sees, so a behaviour difference they observe after toggling cannot be attributed to prompt content. A toggle that silently reordered the prompt would make every provider bug report ambiguous.

## Recorded history

### Commits (2)

- `5d82a01d9` 2026-08-14 — feat(llm): PCS-5 give prompt caching an off switch
- `5acb2a503` 2026-08-14 — feat(llm): PCS-4 translate the neutral cache hint to Anthropic cache_control

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:315` — **Scope:** PCS-4 only. `prompt_cache_enabled` stays PCS-5's (no config field added here, so the
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:318` — second turn onward starting with this change, so it is perceivable without PCS-5/PCS-7.
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:369` — - **PCS-5 DONE — the prompt-cache switch (T2.4 / §C6).** `agent.prompt_cache_enabled` (default
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:391` — commit exists on an unmerged branch and `dag.json` still carries `PCS-5`'s sibling as `todo` with
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:392` — no `pr`. PCS-5 was completable anyway and needed no change: the switch gates the marker PRODUCER
- **source plan** `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:960` — field (`agent.prompt_cache_enabled`) already exists from `PCS-5` and is untouched, so the five-point

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T2.4`, `§2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.4` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:186` — Provider-boundary note: vendor cache syntax in a *core* adapter is permitted **only** because `llm/anthropic.py` and `llm/openai.py` are already the two in-core protocol clients enumerated in `docs/architecture/provider-boundary.md`. No new exception is created, and **no vendor cache syntax may a…
- `T2.4` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:244` — | T2.4 | Config `prompt_cache_enabled` through all five §C6 points incl. the frontend control; middleware treats disabled as `NONE`; **assert the ordering repairs are NOT gated** by it | `config/loader.py`, `dashboard/handlers/core.py`, `web/src/pages/settings/`, tests | `test_config_roundtrip.py…
- `T2.4` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:369` — - **PCS-5 DONE — the prompt-cache switch (T2.4 / §C6).** `agent.prompt_cache_enabled` (default
- `§2.1` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:202` — ### C6 — Config (§2.1 five-point wiring — all five points required)
- `§2.1` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:370` — `True`) through all five §2.1 wiring points: the `AgentConfig` field + `_meta`
- `Session 2` `docs/roadmap/plans/PROMPT-CACHE-SUBSTRATE.md:237` — ### Session 2 — The neutral marker + provider adoption

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
