<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `OO-7` — README version-drift guard (version-consistency hardening)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [OSS-OPERATIONS](../plans/OSS-OPERATIONS.md) (`OO`, plan status `done`) |
| **Atom table** | [`../atomic/OO.md`](../atomic/OO.md) |
| **Execution position** | #384 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Execution log [2026-07-31][README version] deviation: fix the stale pre-1.0 banner and mechanically prevent recurrence via tests/test_version_consistency.py (extended 4 → 6 surfaces: README banner + acp/client.py CLIENT_VERSION)

## Done when

README banner shows the current release version; test_version_consistency.py guards the README banner and acp/client.py CLIENT_VERSION (both mutation-verified to fail naming the file to fix); runbook bump table lists all six surfaces

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** consolidation pass 1

**Code evidence:**

- tests/test_version_consistency.py exists and holds exactly 6 test functions, matching the execution log's claim that it grew 4 -> 6 surfaces
- the README pre-1.0 banner guard is present (asserts the banner version equals the core version, naming the file to fix on failure)
- the acp/client.py CLIENT_VERSION guard is present — the second surface the log says was found hardcoded at 0.1.2 during the 0.1.3 release
- all 6 pass: `pytest tests/test_version_consistency.py` -> 6 passed

**Notes:** Audited because it was the ONLY `done` atom in the whole catalog carrying zero recorded evidence — no commit names it, no prose names it by id, and its scope cites a bracketed log tag ([2026-07-31][README version]) rather than a task number, so no token pattern resolved it. The record turned out to be real and precise; the atom's status is accurate. This is the useful negative result: absence of an id mention is not absence of work.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
