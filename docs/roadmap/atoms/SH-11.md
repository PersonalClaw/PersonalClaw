<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SH-11` — Owner: supply Apple Developer signing + notarization secrets and the minisign release key to the CI `release` environment (gates DC-1/SH-4)

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [SECURITY-HARDENING](../plans/SECURITY-HARDENING.md) (`SH`, plan status `in_progress`) |
| **Atom table** | [`../atomic/SH.md`](../atomic/SH.md) |
| **Execution position** | #520 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`owner`) |
| **Blocked reason** | OWNER-ONLY (2026-09-03): supplying Apple Developer signing secrets and generating the production minisign key are owner actions an agent must never perform (SH-3 ships the trust store empty for exactly this reason). Gates DC-1, DCU-3, SH-4. Belongs on gated_frontier, never ready_frontier. |

## Intent — what this is for

Owner provisioning of release-signing credentials, minted as an explicit atom so the signing gate blocking the desktop/release-signing atoms is legible. SH-3 (signing scheme + verifier, done) ships the trust store EMPTY on purpose — an agent must not generate the private key it would hand over. This is the owner half: run scripts/sign_app.py gen-key --signer PersonalClaw, drop the .pub into src/personalclaw/trusted_keys/, and load the four Apple Developer signing secrets into the CI release environment. NOTE the task groups DCU-3 here; DCU-3's actual blocker is a macOS Accessibility/TCC grant (a different owner action) tracked in DCU-3's own annotation — this atom directly unblocks the SIGNING side of DC-1 and SH-4.

## Done when

the four Apple Developer signing secrets and the minisign signing key are present in the CI `release` environment and the PersonalClaw public key is in src/personalclaw/trusted_keys/; DC-1's signed+notarized mac build and SH-4's release-pipeline signing are unblocked on the credential side

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SH-3`](SH-3.md) | intra-repo | `done` |

### Unblocks (2)

[`DC-1`](DC-1.md) (`todo`), [`SH-4`](SH-4.md) (`todo`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (6)

- `76ebfd5a7` 2026-09-07 — docs(roadmap): DC-1's install-kind clause is met, now that #2696 has landed
- `c010dab70` 2026-09-07 — fix(tools): a pre-existing container is not deliverable evidence
- `12a7db62d` 2026-09-07 — docs(roadmap): two gates asserted only in prose become edges, and one claimed gate leaves a title
- `2f0dffd37` 2026-09-07 — feat(roadmap): rank the owner's queue by what each atom actually unblocks
- `a6acaa5e4` 2026-09-06 — docs(roadmap): SH-4's registry gate was its own deliverable; split SH-12 out
- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:810` — **2026-09-03 — SH-11 minted: owner supplies the Apple signing + notarization secrets and the minisign release key.** `SH-3` shipped the signing scheme + verifier with the trust store deliberately EMPTY — an agent must not generate the private key it would hand over — so the owner half was implici…
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:814` — Worse, the real blocker was absent from the graph. `SH-11`'s own title says it *gates DC-1/DCU-3/SH-4*, but `SH-11` was not among `SH-4`'s deps, so the one thing actually holding signing up — the owner's Apple Developer secrets and the production minisign key — was invisible to the frontier. `SH-…
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:818` — **2026-09-06 — `SH-12` built the signer field OPTIONAL, because a required one would have manufactured claims.** `app-registry.schema.json` gains `signer` and `validate_registry.py` records and echoes it per listing — a `Signer` column in the PR comment, `signer_state` + `signer` in the JSON repo…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
