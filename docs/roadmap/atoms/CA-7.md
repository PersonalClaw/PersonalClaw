<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CA-7` — S3 remote-endpoint auth path over wss (no new origin exemption)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COMPANION-APPS](../plans/COMPANION-APPS.md) (`CA`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CA.md`](../atomic/CA.md) |
| **Execution position** | #94 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | CORRECTION 2026-09-07: this atom is `done` and the residual recorded here NAMED AN ACTION THAT WOULD BE WRONG TO TAKE. It read: 'SMALLEST REMAINING UNIT: give endpointSocket its first consumer — one shipped shell dialling the active registry row's wss instead of the local backendUrl; it lands naturally with CA-8.' CA-8 has now landed (#2584) and deliberately did NOT do that, correctly. The desktop shell HOSTS THE SPA IN A WEBVIEW, so per the companion contract's own split the socket is opened by the PAGE, origin-relative, and the shell must not build a WebSocket URL at all. Making the shell dial `wss` natively would have contradicted the architecture in order to satisfy a note in this field. Verified on main: endpointSocketUrl still has ZERO production consumers — the only hits are its own definition in desktop/endpointRegistry.js, its differential tests, and a docs example. SO THE RESIDUAL IS RESTATED, NOT DELETED: what is unmet is REMOTENESS, not mechanism, and the client that would give endpointSocket a consumer is one that talks to the gateway DIRECTLY instead of hosting its UI — which no shipped shell does today and none is planned in this loop. The mechanism itself holds and is railed (api_ws -> _check_ws_origin -> _paired_device_session, and 'no new origin exemption' holding by ABSENCE with a byte-identical rail on build_allowed_origins). A future consumer is a new atom's work, not an unfinished corner of this one. WHY THIS MATTERED ENOUGH TO EDIT A DONE ATOM: a residual is read as an instruction. This one pointed the next reader at a change the contract forbids, and it was going to keep pointing there every time someone asked what CA-7 still owed. |

## Intent — what this is for

Session 3 T3.2 + V3; client helper + docs; native client presents its device session over wss:// per REMOTE-USER-AUTH S4; verify check_origin/CSP allow it with no new exemption

## Done when

a native client reaches a remote gateway over the owner's tunnel using its device session with no new origin exemption and no cloud middle tier in the path (client->owner-gateway only, Success Criterion 3); killing the tunnel mid-session reconnects/degrades gracefully

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CA-6`](CA-6.md) | intra-repo | `done` |

### Unblocks (1)

[`CA-8`](CA-8.md) (`todo`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 39 (CA) — a SECOND BROWSER paired end-to-end and was revoked, driven

**Code evidence:**

- the mechanism is railed where the atom says it is: api_ws → _check_ws_origin → the paired-device session path, with 'no new origin exemption' holding BY ABSENCE plus a byte-identical rail on build_allowed_origins
- tests/test_ca7_remote_wss_auth.py and test_ca7_wss_tunnel_e2e.py both green inside the 133
- MEASURED, and it confirms the atom's own correction: endpointSocketUrl has no production consumer in desktop/connectMode.js or desktop/main.js
- test_device_pairing + test_companion_discovery + test_companion_single_pairing_mechanism + test_ca7_remote_wss_auth + test_mc2_device_session_consumption 133/133; desktop/test/connectMode.test.js 65/65

**Notes:** 🔑 THE MOST INSTRUCTIVE ATOM IN THIS PLAN, and none of it is about code. Its residual field was EDITED on 2026-09-07 to withdraw an instruction, and the reasoning is the campaign's own lesson stated by the plan itself: the old residual told the next reader to 'give endpointSocket its first consumer — one shipped shell dialling the active registry row's wss', and doing that WOULD HAVE CONTRADICTED THE ARCHITECTURE, because the desktop shell hosts the SPA in a webview and therefore the socket is opened by the PAGE, origin-relative. CA-8 landed and correctly declined to do it. The correction's stated motive is exactly right: 'a residual is read as an instruction. This one pointed the next reader at a change the contract forbids, and it was going to keep pointing there every time someone asked what CA-7 still owed.' Editing a done atom to retract a wrong next-step is the same discipline as this audit's own withdrawals, applied to the plan record instead of to a finding.

## Recorded history

### Commits (4)

- `50b3671e3` 2026-09-07 — docs(roadmap): flip CE-10, and fix five records that were wrong rather than stale
- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `ea789fb48` 2026-08-25 — test(companion): CA-7 drive the shipped wss device admission over a real TLS tunnel
- `57102588d` 2026-08-24 — feat(companion): CA-7 admit a native client's device session over wss

### Named by atom id in the roadmap prose (14)

- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:416` — plus a middleware reader, i.e. its own atom (CA-7's remote-endpoint work is the natural home).
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:713` — - **DISCOVERY (not acted on) — `CA-7`'s and `CA-8`'s acceptance needs two live gateways.** This atom
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:721` — ## Execution log — CA-7 (S3 remote-endpoint auth path over `wss`)
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:723` — - **CA-7 PARTIAL — the atom stays `todo`.** The native-client half is built, tested and gated; the
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:725` — the same reason `CA-6`'s log already recorded against `CA-7`/`CA-8`.
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:805` — ## Execution log — CA-7 / V3 (the tunnel clauses, driven over a real TLS tunnel)
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:807` — - **CA-7 still PARTIAL, but the two clauses that were entirely unobserved are now observed —
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:812` — The dispatch said "implement T3.2". `git log --grep=CA-7` returns `57102588 feat(companion):
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:813` — CA-7 admit a native client's device session over wss`, and `_paired_device_session`,
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:815` — Re-implementing would have been a duplicate. The residual named in CA-7's own log — clauses
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:817` — - **SECOND FINDING — every existing CA-7 test authenticates with `?token=`, the one mechanism the
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:851` — here would have passed with the CA-7 device branch **deleted**, measuring the tunnel instead of
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:862` — `Origin`-less case — is asserted last and is the whole of CA-7's seam.
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:930` — of the `wss`/reconnect region so it union-resolves against the in-flight `CA-7` edit to the same

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T3.2`, `Session 3`. Those tokens appear in its source plan at:

- `T3.2` `docs/roadmap/plans/COMPANION-APPS.md:208` — | T3.2 | Remote-endpoint auth path: a native client presents its device session over `wss://` per REMOTE-USER-AUTH S4; verify origin/CSP allow it without a new exemption | client helper, docs | a native client reaches a remote gateway over the owner's tunnel using its device session (no new origi…
- `T3.2` `docs/roadmap/plans/COMPANION-APPS.md:726` — - **T3.2's verb is *verify*, and the verification FAILED — that is the finding.** The row says
- `T3.2` `docs/roadmap/plans/COMPANION-APPS.md:776` — - **Client helper + docs (T3.2's declared deliverables).** `endpointSocketUrl`/`endpointSocket` in
- `T3.2` `docs/roadmap/plans/COMPANION-APPS.md:812` — The dispatch said "implement T3.2". `git log --grep=CA-7` returns `57102588 feat(companion):
- `Session 3` `docs/roadmap/plans/COMPANION-APPS.md:203` — ### Session 3 — Endpoint switching + reconnection (client contract)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
