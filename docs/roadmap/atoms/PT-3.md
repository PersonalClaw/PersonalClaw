<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PT-3` — S2: shell-element closed registry + TerminalStrip scanline component mounted at App shell

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PERSONALITY-THEMES](../plans/PERSONALITY-THEMES.md) (`PT`, plan status `done`) |
| **Atom table** | [`../atomic/PT.md`](../atomic/PT.md) |
| **Execution position** | #493 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1445 |

## Intent — what this is for

Design 'S2 ...' (shellElement); Task breakdown Session 2 T2.2; Contract C1 (SHELL_ELEMENTS closed map)

## Done when

SHELL_ELEMENTS closed {id -> lazy component} map added to personalities.ts; web/src/ui/personality/TerminalStrip.tsx renders at the App-shell slot only under its personality (aria-hidden, pointer-events-none, static under reduced-motion following DotGlow discipline); axe a11y pass unchanged; reduced-motion renders a static frame

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PT-1`](PT-1.md) | intra-repo | `done` |

### Unblocks (2)

[`PT-5`](PT-5.md) (`done`), [`PT-6`](PT-6.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `32bf078b7` 2026-08-16 — feat(web): PT-5 claw-arcade proof and personality closed-map rails
- `acbef6d44` 2026-08-16 — feat(web): PT-3 shell-element registry and TerminalStrip

### Named by atom id in the roadmap prose (3)

- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:27` — synthesised earcons + the master toggle, default OFF. T2.2 (shell elements, atom `PT-3`):
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:180` — - [2026-08-16][S2/T2.2 · atom `PT-3`] DONE (shell-element closed registry + TerminalStrip). (a) **`design/personalities.ts`** — `ShellElementId` (a literal union) + `SHELL_ELEMENTS: Record<ShellElementId, LazyExoticComponent<ComponentType>>`, per contract C1, with `getShellElement` as the runtime…
- **source plan** `docs/roadmap/plans/PERSONALITY-THEMES.md:187` — - [2026-08-16][S2/T2.4 · atom `PT-5`] DONE (both proofs finished + the registry's structural rails). (a) **`behavior.dials`** is the expressiveness preset the plan's Integration-points line asked for ("`runtime` dials for expressiveness presets"), and it reaches `runtime` the long way ON PURPOSE:…

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:27` — synthesised earcons + the master toggle, default OFF. T2.2 (shell elements, atom `PT-3`):
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:37` — Status corrected 2026-08-04 by code audit; T2.2 line added and the whole S2 line rewritten
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:144` — | T2.2 | Shell-element closed registry + the retro-terminal scanline strip component (aria-hidden, pointer-events-none, static under reduced-motion — the `DotGlow` discipline), mounted at the App-shell slot | `web/src/design/personalities.ts` (SHELL_ELEMENTS), `web/src/ui/personality/TerminalStri…
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:180` — - [2026-08-16][S2/T2.2 · atom `PT-3`] DONE (shell-element closed registry + TerminalStrip). (a) **`design/personalities.ts`** — `ShellElementId` (a literal union) + `SHELL_ELEMENTS: Record<ShellElementId, LazyExoticComponent<ComponentType>>`, per contract C1, with `getShellElement` as the runtime…
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:181` — - [2026-08-16][S2/T2.2] **Measured defect, fixed here — `map[id] ?? null` was never closed.** The resolver's first draft copied `getErrorTreatment`'s shape and the closure test reddened immediately: a plain index reads the **prototype chain**, so `getShellElement('constructor')` returned `Object`…
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:182` — - [2026-08-16][S2/T2.2] Proofs. **The decorative contract is asserted by RENDERING every registry entry** (`design/shellElements.test.tsx`), not by reading source: `aria-hidden="true"`, `pointer-events-none`, a `data-shell-element` marker naming its own id, and zero tabbable descendants. The loop…
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:183` — - [2026-08-16][S2/T2.2] **Lazy verified in the BUILD, not just declared.** Two rails — every registry value must be a React lazy type, and no module outside the registry may reference `ui/personality/` statically (comments stripped, two vacuity floors). **Falsification found a real gap in that ra…
- `T2.2` `docs/roadmap/plans/PERSONALITY-THEMES.md:184` — - [2026-08-16][S2/T2.2] Fourteen mutations against the live lines. All ten that had to red did: drop `aria-hidden` (3 tests), drop `pointer-events-none` (3), remove the reduced-motion branch (3), mount under every personality (5), non-lazy registry entry (2), static import from `App.tsx` (1), rev…
- `Session 2` `docs/roadmap/plans/PERSONALITY-THEMES.md:139` — ### Session 2 — Sound cues + shell element + error treatment + proofs polished

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
