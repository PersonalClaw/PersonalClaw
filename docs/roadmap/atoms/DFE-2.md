<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DFE-2` — Style-carrying document model + from_markup runs + docx writer emits new fields

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-FIDELITY-EDITOR](../plans/DOCUMENT-FIDELITY-EDITOR.md) (`DFE`, plan status `done`) |
| **Atom table** | [`../atomic/DFE.md`](../atomic/DFE.md) |
| **Execution position** | #161 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1978 |

## Intent — what this is for

Session 1 T1.1–T1.3; §C1 (Run/ParagraphStyle/PageSetup/Cell + additive Block/DocumentModel fields); soul guardrails 1 (vendor-neutral) & 5 (writers stay pure)

## Done when

Every existing model test passes untouched; a runs-only Block answers .text and a cells-only table answers .rows via __post_init__ derivation; markdown inline formatting (**bold**/*italic*/`code`/[t](url)) round-trips into Runs and old _strip_inline behavior is gone (not left beside it); a bold run reads back bold via python-docx.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:DOCUMENT-HANDLING-TOOLS:owns model.py + the four writers this extends (DONE)` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`DFE-3`](DFE-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (5)

- `175e2fc9b` 2026-08-27 — test(documents): bind the deck inline-run test to DFE-8's Bullet shape
- `0feedfd8f` 2026-08-24 — test(documents): DFE-3 pin the SHIPPED model classes on the parse side
- `2d7f5b6b6` 2026-08-24 — docs(roadmap): cite batch PR #1978 on DFE-2/EA-4 and de-orphan ACP-AGENT-PARITY
- `f7b78eb7a` 2026-08-24 — test(documents): DFE-2 finish the clean break and bind the writer tests to the shipped model
- `1dc5f1b74` 2026-08-23 — feat(documents): DFE-2 style-carrying document model — formatting survives into the file

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:562` — - **2026-08-23 — `DFE-2` COMPLETE (all four clauses). Atom stays `todo` only because this code is
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:598` — renders **byte-identical `word/document.xml`** through a transcription of the pre-DFE-2 dispatch and the
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:616` — - **2026-08-24 — `DFE-2` VERIFIED MERGED, and the clean break finished where it had stopped short.**
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:619` — neither the documents package nor any file here), so `dag.json`'s `DFE-2: todo` is stale bookkeeping
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:630` — ``documents/from_markup.py``'s `_strip_inline` — and citing it for behaviour that DFE-2 inverted, telling
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:679` — is not — `dag.json`'s `DFE-2.done_when` is the complete four-clause sentence, and the brief quoted it
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:696` — (`git ls-files | grep -i docx` returns only the writer and the DFE-2 fidelity suite). The fixture
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:776` — This is **DFE-2's hazard on DFE-3's seam**. Added

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T1.1`, `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:406` — | T1.1 | §C1 model additions (`Run`, `ParagraphStyle`, `PageSetup`, `Cell`, the new `Block`/`DocumentModel` fields) with the `text`←`runs` and `rows`←`cells` derivation in `__post_init__` | `documents/model.py`, tests | every existing model test passes untouched; a runs-only Block still answers `…
- `T1.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:680` — verbatim. The clause list is exactly T1.1 + T1.2 + T1.3's "Done when" columns, and T1.4/T1.5 belong to
- `T1.3` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:408` — | T1.3 | Docx writer emits the new fields: runs, paragraph style, cell spans, image width/alt | `documents/writers/docx_writer.py`, tests | a bold run is bold in the output (asserted via python-docx read-back, not by trusting the call) |
- `T1.3` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:680` — verbatim. The clause list is exactly T1.1 + T1.2 + T1.3's "Done when" columns, and T1.4/T1.5 belong to
- `Session 1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:402` — ### Session 1 — The styled model + the docx parser

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
