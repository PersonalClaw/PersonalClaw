<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PUBL-5` — Verify main green and flip both repos to public visibility

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PUBLICATION](../plans/PUBLICATION.md) (`PUBL`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PUBL.md`](../atomic/PUBL.md) |
| **Execution position** | #501 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 1 §2

## Done when

main confirmed green per CI-RELEASE-ENGINEERING S1 red-test triage; PersonalClaw/PersonalClaw and PersonalClaw/PersonalClawApps flipped to public visibility

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PUBL-1`](PUBL-1.md) | intra-repo | `done` |
| `EXT:CI-RELEASE-ENGINEERING:green main / red-test triage (S1)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 56 (PUBL) — almost every clause re-read from the live GitHub and PyPI APIs rather than from the tree

**Code evidence:**

- 🔑 BOTH REPOSITORIES REPORT `visibility: public` FROM THE API — the single most externally checkable claim in the plan, and it observes as stated
- the green-main precondition is the CI-RELEASE-ENGINEERING gate, and the release history below is downstream evidence that it held: the v0.1.2 and v0.1.3 release runs both concluded success end to end
- live API: both repos public with homepage set; org holds the 3 named repos; tags v0.1.0-v0.1.3 with 4 GitHub Releases (2 assets each); PyPI core 0.1.0-0.1.3; 31 of 31 screenshot references resolve

**Driven in the UI:** Not a product surface.

**Notes:** ARCC was queried for pre-publication review guidance on flipping a private repository public, since that is a deliberate exposure change. It returned container-registry exposure material and nothing about secret review before open-sourcing a source repo, so this is recorded as checked-and-thin. The transferable concern — that a public repository is open to anyone — is the one `SECURITY-LEGIBILITY` and the credential-file guards elsewhere in this audit already address.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `§2`, `Session 1`. Those tokens appear in its source plan at:

- `Session 1` `docs/roadmap/plans/PUBLICATION.md:55` — ## Session 1 — Release (manual, ~1 session)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
