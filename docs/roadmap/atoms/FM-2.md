<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `FM-2` — Morph.tsx shared-element wrapper + wire one real card→detail morph

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [FLUID-MOTION](../plans/FLUID-MOTION.md) (`FM`, plan status `done`) |
| **Atom table** | [`../atomic/FM.md`](../atomic/FM.md) |
| **Execution position** | #269 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1679 |

## Intent — what this is for

Session 2 — Liquid morphing (T2.1); Contracts C2 (Morph)

## Done when

a list card visibly morphs into its detail/expanded view and back via framer layout/layoutId; reduced-motion path yields instant swap; no layout thrash

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`FM-1`](FM-1.md) | intra-repo | `done` |
| `EXT:KNOWLEDGE-LIBRARY:knowledge card→reading view surface for the demo morph (alternate surfaces — session rows→chat, loop cards→cockpit — usable if unavailable)` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`FM-4`](FM-4.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 59 (FM) — the app-wide reduced-motion census falsified; the bounciness dial driven through a reload

**Code evidence:**

- `Morph.tsx` ships under `web/src/ui/motion/` with its own `Morph.test.tsx` AND a dedicated `Morph.reducedMotion.test.tsx` — the gated path is a separate file from the behaviour, so an instant-swap regression cannot hide inside a passing behaviour suite
- the component suites pass 69/69 across nine files
- design motion suites 83/83 + ui/motion 69/69 = 152 green (falsified: bypassing ONE getter's gate reds 6, including the app-wide census and the type-level check)

**Driven in the UI:** Not driven: 'a list card VISIBLY morphs into its detail view and back' is a motion observation. A still snapshot cannot prove a transition, and this harness cannot record one.

**Notes:** Partial for the visible-morph clause, which is the atom's substance and is genuinely not decidable from a DOM snapshot. What IS decidable — that the reduced-motion path exists as its own tested file — is confirmed.

## Recorded history

### Commits (2)

- `cca592812` 2026-08-18 — chore(roadmap): cite the landing batch #1679 on APE-1, ET-3, FM-2 and CA-1
- `b9345bd45` 2026-08-18 — feat(motion): FM-2 Morph.tsx shared-element wrapper + artifact card->viewer morph

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:298` — `FM-2`'s `Morph.tsx`; this atom ships the crossfade. `FM-7` still owns the 60fps budget proof and
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:499` — ### 2026-08-18 — `FM-2` (S2: T2.1; contract §C2 `Morph`) — **DONE**
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:636` — **`FM-2`'s 157px defect — DIAGNOSED and ATTRIBUTED to the pixel, deliberately NOT fixed. Handed to a
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:647` — wrong animation" hazard `FM-2` recorded: the exiting grid never unmounted (7 `[data-morph]` nodes still

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T2.1`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/FLUID-MOTION.md:79` — | T2.1 | `Morph.tsx` shared-element wrapper (framer `layout`/`layoutId`) + reduced-motion path; wire one real morph (knowledge card → reading view, coordinate with plan 49) | `web/src/ui/motion/Morph.tsx`, a consuming page | the card visibly morphs into its detail view + back; reduced-motion → in…
- `T2.1` `docs/roadmap/plans/FLUID-MOTION.md:499` — ### 2026-08-18 — `FM-2` (S2: T2.1; contract §C2 `Morph`) — **DONE**
- `T2.1` `docs/roadmap/plans/FLUID-MOTION.md:508` — **DEVIATION — surface.** T2.1 names knowledge card → reading view; the atom's dep allows alternates.
- `Session 2` `docs/roadmap/plans/FLUID-MOTION.md:75` — ### Session 2 — Liquid morphing

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
