<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-13` — Retire `pool.py`'s private frontier onto the unified core

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #471 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1560 |

## Intent — what this is for

`WF2TAS-6` shipped the task pool's own `frontier`/`next` projections and `WF2TAS-10` its lease write path. With `Lease` available as an admission policy (`PP-12`), the pool's private projection is a second implementation of a solved problem. Clean break under the pre-1.0 banner: no dual path, no compatibility shim.

## Done when

`pool.frontier`/`pool.next` are DELETED and the Work board's ready projection is computed by the unified core with a `Lease` policy plus the pool's existing priority/blocking-count/overdue ordering expressed as a comparator. The lease decision functions survive (they are the policy's implementation); only the duplicate projection goes. Verified: the Work board renders an identical ready set before and after over a seeded fixture, leases still survive a gateway kill, and `dependency_failed` cascade plus burst coalescing still fire. Retiring a legacy path is never a pure deletion — the sweep names every caller before the delete lands.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-12`](PP-12.md) | intra-repo | `done` |

### Unblocks (1)

[`PP-16`](PP-16.md) (`todo`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- `pool.py`'s private frontier is retired onto the unified core, which is the third of the four schedulers folded in — the atom that makes PP-11's seam load-bearing rather than aspirational
- 🔑 A SEAM WITH ONE CONSUMER IS A WRAPPER; THIS IS THE ATOM THAT MAKES IT A PRIMITIVE. Extracting an interface and leaving the other implementations in place is how a codebase acquires a fourth way to answer the same question
- the frontier golden oracle still passes 5/5 after the retirement, which is exactly what it was captured for
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not a browser surface.

**Notes:** The golden fixtures earn their keep here rather than at PP-11: the refactor that introduced nothing is easy to believe, and this is the one that moved a real scheduler's decisions.

## Recorded history

### Commits (3)

- `870b6e649` 2026-08-17 — chore(roadmap): cite the landing batch #1560 on PP-13
- `ae38dddd6` 2026-08-17 — refactor(workflows): PP-13 retire pool.py's private frontier onto the unified core
- `7042fb288` 2026-08-14 — refactor(workflows): PP-11 compose admission from an ordered policy list

### Named by atom id in the roadmap prose (13)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:72` — | Task-pool leases | TASKS-SOPS `WF2TAS-6`/`-10` (done) | **Reused**: `PP-12` adopts the shipped compare-and-swap lease decision functions as an admission policy; `PP-13` retires only the duplicate projection. |
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:110` — ## §4 One admission core, N admission policies (`PP-11`…`PP-13`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:118` — cannot be verified. `PP-12` adds `Lease` and `Dwell`/`MetricGate` on the proven seam. `PP-13` retires
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:143` — └─→ PP-11 ─→ PP-12 ─→ PP-13 ───────┤
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:937` — `MetricGate` are `PP-12`, and `pool.py`'s private projection is `PP-13` — this atom only makes the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1291` — - [PP-13] **DONE — and the caller census is the finding: the retired projection had ZERO production
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1308` — - [PP-13] **What landed.** The ordering moved to `admission.rank_key` — a comparator, deliberately
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1322` — - [PP-13] **The equivalence proof, and its vacuity floor.** `tests/fixtures/pool_frontier_golden/`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1334` — - [PP-13] **DEVIATION — one behaviour is deliberately NOT preserved, and it is a bug fix.** The
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1342` — - [PP-13] **Falsified three times; the third exposed a defect in my own test.** (i) Dropping the
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1356` — - [PP-13] **A wrong expectation of mine, corrected against the shipped rule.** I first asserted
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1362` — - [PP-13] **Gate.** `make lint` clean (black 1752 files, isort, flake8, mypy 902 files, no issues).
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1519` — **Not a dependency problem.** `PP-16` is READY: every non-`EXT` dep (`PP-15`, `PP-5`, `PP-13`) is

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
