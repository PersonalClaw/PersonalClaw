<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-10` — Spend-ceiling price-key rail: reconcile price-table ids with catalog ids (T03)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #541 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Research draft T03. The daily spend ceiling is INERT: price-table model ids use dots while the catalog uses hyphens, so only 2/10 ids resolve and computed spend reads $0.00 forever.

## Done when

Every catalog model id resolves to a price-table entry (one canonical id form + a normalization shim at the single lookup seam); a rail test enumerates the live catalog against the price table and fails on any unresolvable id; daily spend renders a nonzero value for a session that used a priced model; regression test pins the dot/hyphen normalization.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `d64a64dce` 2026-09-05 — fix(pricing): every Bedrock inference-profile shape resolves to its price row (PCS-9 code half)
- `4663b68b6` 2026-09-02 — docs(roadmap): flip five Wave-B batch-2 atoms done (SM-10, KL-18, CC-9, AG-14, ES-12)
- `c51fc7793` 2026-09-02 — fix(pricing): price-key rail — canonicalize catalog ids at the lookup seam (SM-10)
- `c0d61a28d` 2026-09-02 — docs(roadmap): rev-18 ground-truth reconciliation — EXT-dep audit applied, verified flips, 8 new atoms

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:302` — floor via SM-10, >0-gated — an unreadable ledger never stops a loop) and `deadline_secs`
- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:113` — **2026-09-02 — SM-10 done (PR #2306).** Pricing key-canonicalization rail: usage-ledger pricing keys canonicalized at the catalog boundary so cost lookups resolve for every served model id form; `loop_spend()['dollars_est']` made trustworthy (AG-14's cost ceiling consumes it). Rail test enumerate…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
