<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CA-6` — S3 shared client contract doc + multi-gateway registry (amendment T3.3)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COMPANION-APPS](../plans/COMPANION-APPS.md) (`CA`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CA.md`](../atomic/CA.md) |
| **Execution position** | #93 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1990 |

## Intent — what this is for

Session 3 T3.1 + Amendment T3.3; docs/guides/companion-apps.md + optional minimal shared TS helper in web/src/lib/; C1 endpoint model, {active, endpoints[]} registry, per-endpoint state namespacing, switcher spec, verbatim no-hub / no-gateway-to-gateway rule; reuses (not re-invents) the platform-resilience degraded-connection contract

## Done when

contract is precise enough that desktop + mobile implement it without re-deciding; two paired gateways are switchable from one client with zero state bleed (distinct sessions/inbox/settings per endpoint id); the doc states the hub veto verbatim; degraded UI reuses the existing contract

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:PLATFORM-RESILIENCE:degraded-connection contract to reuse` | cross-plan (EXT) | **unresolved** |

### Unblocks (3)

[`CA-7`](CA-7.md) (`done`), [`CA-8`](CA-8.md) (`todo`), [`CA-9`](CA-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 39 (CA) — a SECOND BROWSER paired end-to-end and was revoked, driven

**Code evidence:**

- docs/guides/companion-apps.md is 542 lines and carries the hub veto VERBATIM as the clause requires (§'No hub, ever'): 'No hub in core, ever. No gateway-to-gateway anything.'
- the {active, endpoints[]} registry is real on the desktop side and IS consumed by production code — desktop/connectMode.js:73 imports it — with per-endpoint namespaced state
- the two-gateway switch with zero state bleed is proven by desktop/test/connectMode.test.js, 65/65 passing, including the isolation case
- test_device_pairing + test_companion_discovery + test_companion_single_pairing_mechanism + test_ca7_remote_wss_auth + test_mc2_device_session_consumption 133/133; desktop/test/connectMode.test.js 65/65

**Driven in the UI:** Not driven: a second gateway to switch to needs a second machine (the same gate as CA-8's V4).

**Notes:** 🔑 CENSUS EXTENDED BEYOND WHAT CA-7's CORRECTION EXAMINED. That correction states endpointSocketUrl has 'ZERO production consumers — the only hits are its own definition in desktop/endpointRegistry.js, its differential tests, and a docs example'. Accurate as far as it looked, but there is a SECOND parallel definition it did not name: web/src/lib/endpoints.ts, whose ONLY importer in the entire web tree is its own test — so the module is wholly unconsumed, not merely one helper inside it. That is not a defect: this atom's own scope commissioned an 'OPTIONAL minimal shared TS helper in web/src/lib/' as the reference implementation future clients build against, and CA-9 deliberately ships no per-platform code. The word doing the work in that scope line is 'optional'. Recorded because the residual is genuinely larger than the correction says: the S3 client contract is currently implemented twice, in two languages, with no production caller in either — which is a thing to know before a third copy appears.

## Recorded history

### Commits (5)

- `cc8831afe` 2026-08-24 — docs(roadmap): cite the batch PR on the three atoms this train lands
- `0b81d7bde` 2026-08-24 — docs(roadmap): CA-6 done — shared multi-gateway client contract + registry helper
- `0b11092e6` 2026-08-24 — docs(roadmap): CA-6 correct COMPANION-APPS' stale token_auth citations
- `c104e5924` 2026-08-24 — docs(companion): CA-6 specify the shared multi-gateway client contract
- `73745c68e` 2026-08-24 — feat(companion): CA-6 shared multi-gateway endpoint registry + per-endpoint storage namespacing

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:663` — ## Execution log — CA-6 (S3 shared client contract + multi-gateway registry)
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:665` — - **CA-6 DONE.** `docs/guides/companion-apps.md` gains `## The shared client contract` (+199 lines) and
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:725` — the same reason `CA-6`'s log already recorded against `CA-7`/`CA-8`.
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:787` — `:1040-1041`→`:1059-1061`). `CA-6` fixed the same class of drift rather than inheriting it; the
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:792` — (`desktop/main.js` still holds one `backendUrl` and no shell reads the registry, per `CA-6`).
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:917` — exact shape `CA-6`'s `{active, endpoints[]}` registry replaced. Fixed by pointing S4 at the
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:988` — redemption path under a contract that says there is one, and CA-6's log already flagged the
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:130` — > `web/src/lib/endpoints.ts` (plan 54 `CA-6`). This plan's S4 tasks *implement that list* on

### Its plan's own record, via the scope references (9 line(s))

This atom's scope cites `T3.1`, `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.1` `docs/roadmap/plans/COMPANION-APPS.md:207` — | T3.1 | Define + document the shared client contract (C1 endpoints, switch, reconnect using the platform-resilience degraded contract — not a new one) so both wrappers implement it identically | `docs/guides/companion-apps.md`, a small shared TS helper if one fits in `web/src/lib/` | the contrac…
- `T3.1` `docs/roadmap/plans/COMPANION-APPS.md:245` — | **Open:** whether the shared client contract needs a tiny published TS module vs prose | T3.1 decides based on how much desktop + mobile actually share; default to prose + a minimal helper, not a framework |
- `T3.1` `docs/roadmap/plans/COMPANION-APPS.md:259` — No new session. S3 T3.1's client contract is WHERE the multi-entry registry + active pointer are specified (it was going to specify the endpoint list anyway); S4 T4.1/T4.2 gain the switcher acceptance bar. One added task row in S3 for the isolation guarantee.
- `T3.1` `docs/roadmap/plans/COMPANION-APPS.md:263` — | T3.3 | Multi-gateway registry in the client contract: `{active, endpoints[]}` shape, per-endpoint state namespacing (caches/WS/prefs keyed by endpoint id), switcher behavior spec (re-point + reload), and the written no-hub/no-gateway-to-gateway rule | `docs/guides/companion-apps.md` (T3.1's doc…
- `T3.3` `docs/roadmap/plans/COMPANION-APPS.md:21` — T3.3 and T4.4. Also never formally inserted into the execution order (workspace `ROADMAP.md` §5).
- `T3.3` `docs/roadmap/plans/COMPANION-APPS.md:263` — | T3.3 | Multi-gateway registry in the client contract: `{active, endpoints[]}` shape, per-endpoint state namespacing (caches/WS/prefs keyed by endpoint id), switcher behavior spec (re-point + reload), and the written no-hub/no-gateway-to-gateway rule | `docs/guides/companion-apps.md` (T3.1's doc…
- `T3.3` `docs/roadmap/plans/COMPANION-APPS.md:669` — A literal reading of T3.3's "caches/WS/prefs keyed by endpoint id" is a refactor of every web-storage
- `T3.3` `docs/roadmap/plans/COMPANION-APPS.md:922` — per S3/T3.3), "**not** a single stored gateway URL and not a second registry shape"; T4.2 widened
- `Session 3` `docs/roadmap/plans/COMPANION-APPS.md:203` — ### Session 3 — Endpoint switching + reconnection (client contract)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
