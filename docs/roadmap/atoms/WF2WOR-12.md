<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2WOR-12` — Container workspace mode + snapshot-anchored fork (deferred, opt-in)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-WORK-CONTAINERS](../plans/WORKFLOWS-V2-WORK-CONTAINERS.md) (`WF2WOR`, plan status `done`) |
| **Atom table** | [`../atomic/WF2WOR.md`](../atomic/WF2WOR.md) |
| **Execution position** | #660 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§4.4 (optional container mode R20), Migration Order step 9 (explicitly deferred to last)

## Done when

mode:container joins the workspace enum with a typed environment manifest (image XOR build, user, mounts, capabilities) on Docker/containerd or Apple Virtualization (no hard Docker dependency); container snapshots between stages anchor fork-from-checkpoint to workspace state; strictly opt-in with in_place/worktree staying default and no remote/cloud deploy modes

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2WOR-4`](WF2WOR-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 66 (WF2WOR) — the archive extractor's path safety falsified and found unasserted (fixed in #2825); the Work board driven in a real browser

**Code evidence:**

- `container` joins the workspace mode enum (`workspace.py:97`) and `container_env.py` carries the typed manifest; `tests/test_container_workspace.py` green inside the 228
- 🔑 `image` XOR `build` IS CALLED LOAD-BEARING AND IT IS: 'both is ambiguous about which wins'. A manifest that accepted both would resolve differently per backend, which is the worst kind of environment bug — reproducible on one machine only
- 🔴 THREE BACKENDS AND NO HARD DOCKER DEPENDENCY, detected by CLI probe on purpose: an import-time `docker-py` dependency 'would tax every [install]' for a feature that is 'local-first and opt-in'. Docker, containerd via nerdctl, and Apple Virtualization
- the build budget is separated from the probe ceiling because 'a cold `docker build` legitimately takes minutes' — one timeout for both would have made the honest case look like a hang
- 555 passed across the containers, needs-input, export, container-workspace, publish, filedrop, introspection, fan-out, worktree and memory-locality suites; the Work board and a project detail page driven on :10011

**Driven in the UI:** Not driven: no container backend is running on this host.

**Notes:** Deferred and opt-in is the honest posture for a tier that needs a container runtime the user may not have — and the manifest being validated at save time rather than at run time means an author learns about a bad declaration before a run depends on it.

## Recorded history

### Commits (3)

- `ba1986f61` 2026-09-02 — docs(roadmap): flip five Wave-A atoms done; three plans complete; file WF2UNI-14
- `fdcd1a236` 2026-09-02 — feat(workflows): container workspace mode + snapshot-anchored fork (WF2WOR-12)
- `8b4644b61` 2026-08-09 — feat(workflows): WF2WOR-4 run workspace provisioning + teardown wiring + code-run cockpit (#967)

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1313` — (`WF2WOR-12`, deliberately deferred/opt-in). So R19 has no producer and never will under that plan.
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1332` — owner-deferred to `WF2WOR-12`, and `sandbox_providers/` contains no docker provider. So `EI-10` should be
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1362` — `Mode.CONTAINER` with a scratch dir plus *"§4.4 is owner-deferred to WF2WOR-12"*. So the clause
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1377` — `lima`, `limactl`, `colima` all absent — the same measurement that blocks `WF2WOR-12` and `EI-7`'s SC7).
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1048` — loop-drain dep; `WF2WOR-12` needs a container runtime and this host has none (`docker`, `podman`,
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:422` — - **[2026-09-02][WF2WOR-12] DONE — container workspace mode ships (PR #2304).**
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1319` — - **NOT DONE:** container mode (§4.4) remains owner-deferred to **WF2WOR-12** and degrades to an

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `§4.4`, `R20`. Those tokens appear in its source plan at:

- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:53` — - **WORK-R20** — optional container workspace mode + snapshot checkpoints (opt-in, deferred) → §4.4
- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:69` — │    ├─ checkpoints       (fork points — v2 Slice 2; container snapshots optionally anchor them §4.4)
- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:169` — mode: worktree | in_place | scratch | container   # container is opt-in, §4.4
- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:310` — 9. (Deferred, opt-in) container workspace mode + snapshot-anchored fork (§4.4).
- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:710` — mode (§4.4) is opt-in and explicitly deferred to last by the plan. The two new config defaults
- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:916` — diff panel and the two verb buttons are FE work on this contract. Container mode (§4.4) remains
- `§4.4` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1319` — - **NOT DONE:** container mode (§4.4) remains owner-deferred to **WF2WOR-12** and degrades to an
- `R20` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:53` — - **WORK-R20** — optional container workspace mode + snapshot checkpoints (opt-in, deferred) → §4.4
- `R20` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:161` — ## 4. Run Workspace & Environment (NEW — R3/R18/R19/R20)
- `R20` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:200` — ### 4.4 Optional container mode (R20 — opt-in, deferred to last)
- `R20` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:330` — - **9 sessions** (was 4; R1-R20 roughly double the mechanism surface), interleaved with v2 Slices 4-5 rather than strictly after. Sessions map 1:1 to Migration Order steps 1-8, with step 9 (container mode) explicitly deferred and unbudgeted until the rest has landed.
- `R20` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1148` — already maps the author-facing name `model` onto `model_tier` (WF2-R20d), so a `workflow_edit` op

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
