<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-11` — Retire the interim commit-watcher cron script when the AUTO-R12 vcs trigger lands

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #554 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | OWNER RULING 2026-08-27 (question 4 of 11). Q: file a vcs-preset atom under AUTOMATION-SUBSTRATE (no atom owns AUTO-R12 today) or descope SV-11 to its vacuously-passing conditional rule? RULED: FILE THE VCS-PRESET ATOM under AUTOMATION-SUBSTRATE; SV-11 stays open pointing at it. Reasoning: a conditional rule that passes vacuously IS an inert control -- a declared mechanism with no reachable trigger -- and this session has spent most of its time closing exactly that shape (capture_store.prune with no caller, UsageStore.record with no writer, surfacing_events with no table). Descoping SV-11 to the vacuous rule would ship a fifth one deliberately. The retirement this atom describes is real work that belongs to whoever owns AUTO-R12, so it gets an atom. |

## Intent — what this is for

§6 (explicit AUTO-R12 retirement row), Risk table (rule spec asserting the cron script is absent once the vcs trigger kind exists)

## Done when

the self-qa template rebinds to AUTOMATION-SUBSTRATE's vcs trigger preset, the bundled selfqa_commit_watch.py cron script is deleted, and a rule spec asserts its absence once the vcs trigger kind exists

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SV-9`](SV-9.md) | intra-repo | `done` |
| `EXT:AUTOMATION-SUBSTRATE:AUTO-R12 vcs trigger preset (file kind watching .git/refs/heads/*)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `ffa491692` 2026-09-04 — docs(roadmap): flip SV-11, ES-14, MI-5 to done — SELF-VERIFICATION and MULTIMODAL-IO complete
- `aa9a2cb81` 2026-09-04 — feat(selfqa): SV-11 — the commit watch rebinds to the vcs trigger; the interim cron script retires
- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done

### Named by atom id in the roadmap prose (6)

- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:601` — - **[2026-08-27] OWNER RULING — `SV-11`: file the vcs-preset atom under AUTOMATION-SUBSTRATE; `SV-11` stays open
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:602` — pointing at it.** The alternative was descoping `SV-11` to its conditional rule, which **passes vacuously** — and a
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:606` — today; the retirement `SV-11` describes is real work and gets an atom.
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:614` — - [2026-09-04][SV-10] LANDED on main; atom flipped ✅. Merged via core PR #2410 (merge commit 2905bb3e8). `selfqa/evidence.py` computes each bundle file's size and SHA256 from the bytes on disk into a schema-versioned `manifest.json` and `register_bundle` composes the run's proof into ONE content-…
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:616` — ## Execution log — SV-11 (Retire the interim commit-watcher cron script onto the AUTO-R12 vcs trigger)
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:618` — - [2026-09-04][SV-11] LANDED on main; atom flipped ✅ and the plan is COMPLETE — all 11 atoms done. Merged via core PR #2440 (merge commit aa9a2cb81). The bundled `self-qa` template rebinds its trigger to AUTOMATION-SUBSTRATE's vcs preset (file kind watching `.git/refs/heads/*`), the interim `self…

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§6`, `R12`. Those tokens appear in its source plan at:

- `§6` `docs/roadmap/plans/SELF-VERIFICATION.md:29` — - **NEW-7** (Self-QA Companion: commit trigger → per-commit user-impact triage with ledger-only skip records → deep as-a-user scenario generation (fault injection, restart/resume arcs, resource-growth assertions, real UI driving) → execution via Chrome DevTools MCP + terminal → findings to Inbox/…
- `§6` `docs/roadmap/plans/SELF-VERIFICATION.md:31` — - **NEW-17** (event-trace replay regression: record real event streams as JSONL scenarios, replay offline into metrics — duplicate_event_rate, fanout, order violations — gate against baselines with hard + drift thresholds; MCP traffic record/replay; fresh-session resumability audit ride the same …
- `§6` `docs/roadmap/plans/SELF-VERIFICATION.md:155` — There is **no vcs trigger today**, and the approved one (AUTOMATION-SUBSTRATE AUTO-R12: `file` kind with a `vcs` preset watching `.git/refs/heads/*`, content-hash dedup, changed-delta payload) is Wave 3. The Wave-2 companion therefore ships with the **existing** seam: a bundled zero-token cron sc…
- `§6` `docs/roadmap/plans/SELF-VERIFICATION.md:247` — | Interim commit watcher lingers after AUTO-R12 | explicit retirement row in §6 + a rule spec asserting the cron script is absent once the vcs trigger kind exists (the harness polices its own migration) |
- `§6` `docs/roadmap/plans/SELF-VERIFICATION.md:618` — - [2026-09-04][SV-11] LANDED on main; atom flipped ✅ and the plan is COMPLETE — all 11 atoms done. Merged via core PR #2440 (merge commit aa9a2cb81). The bundled `self-qa` template rebinds its trigger to AUTOMATION-SUBSTRATE's vcs preset (file kind watching `.git/refs/heads/*`), the interim `self…
- `R12` `docs/roadmap/plans/SELF-VERIFICATION.md:155` — There is **no vcs trigger today**, and the approved one (AUTOMATION-SUBSTRATE AUTO-R12: `file` kind with a `vcs` preset watching `.git/refs/heads/*`, content-hash dedup, changed-delta payload) is Wave 3. The Wave-2 companion therefore ships with the **existing** seam: a bundled zero-token cron sc…
- `R12` `docs/roadmap/plans/SELF-VERIFICATION.md:212` — | `vcs` trigger preset (file kind, content-hash dedup, delta payload) | AUTOMATION-SUBSTRATE **AUTO-R12** (Wave 3) | §3.1 ships an interim cron-script watcher; **retire it** when AUTO-R12 lands (explicit retirement item) |
- `R12` `docs/roadmap/plans/SELF-VERIFICATION.md:217` — **Dependency edges:** §1 depends on nothing. §2 depends on nothing to build, but its `workflow-journal-projection` scenario is recorded against WF2 Slices 1-2 output as those land (co-scheduled, Wave 1). §3 depends on WF2 Slices 0-5; §3.3's Proof rendering depends on WORK-CONTAINERS (graceful wit…
- `R12` `docs/roadmap/plans/SELF-VERIFICATION.md:247` — | Interim commit watcher lingers after AUTO-R12 | explicit retirement row in §6 + a rule spec asserting the cron script is absent once the vcs trigger kind exists (the harness polices its own migration) |
- `R12` `docs/roadmap/plans/SELF-VERIFICATION.md:605` — `surfacing_events` named with no table), so shipping a fifth one deliberately is not available. Nothing owns AUTO-R12
- `R12` `docs/roadmap/plans/SELF-VERIFICATION.md:616` — ## Execution log — SV-11 (Retire the interim commit-watcher cron script onto the AUTO-R12 vcs trigger)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
