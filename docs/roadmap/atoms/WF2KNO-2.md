<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2KNO-2` — The provider pair: knowledge_persist + knowledge_retrieve app, allowlist, native-provider retrieval seam

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS](../plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md) (`WF2KNO`, plan status `done`) |
| **Atom table** | [`../atomic/WF2KNO.md`](../atomic/WF2KNO.md) |
| **Execution position** | #575 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§2.1 knowledge_persist, §2.2 knowledge_retrieve, §2.3 Zero-Model Heuristic Extraction Floor, §5.1 Retrieval Stage at Workflow Start; Session 2

## Done when

apps/native/knowledge-actions/ ships both providers (idempotent upsert/append_evidence/ops, budgets, citation enforcement, degradation-ladder retrieve with create_safety/freshness/detail caps); both in ALLOWED_HOOK_PROVIDERS + action registry; NativeKnowledgeProvider.search() routes retrieval via knowledge_providers registry (search_all first caller; manifest factory repointed); persist→retrieve→confirm template validated live with a proven idempotent no-op on re-run

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2KNO-1`](WF2KNO-1.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:action-node dispatcher + wait node (Slice 1) and ALLOWED_HOOK_PROVIDERS seam` | cross-plan (EXT) | **unresolved** |

### Unblocks (5)

[`WF2KNO-11`](WF2KNO-11.md) (`done`), [`WF2KNO-4`](WF2KNO-4.md) (`done`), [`WF2KNO-5`](WF2KNO-5.md) (`done`), [`WF2KNO-6`](WF2KNO-6.md) (`done`), [`WF2KNO-7`](WF2KNO-7.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 6 (WF2KNO) — observed

**Code evidence:**

- the provider pair is present AND REGISTERED — knowledge_persist / knowledge_retrieve appear in src/personalclaw/action_providers/registry.py, not merely in a module nobody imports
- src/personalclaw/action_providers/knowledge_maintain_provider.py and src/personalclaw/knowledge/updates.py carry the implementation
- part of the 210-passed run

**Notes:** Registration is the load-bearing evidence: a provider module that exists but is absent from the registry would be an inert capability, which is exactly the failure class this roadmap tracks elsewhere. 🪤 I guessed the path as action_providers/knowledge_persist_provider.py and was WRONG — it lives in knowledge_maintain_provider.py. Third cycle running where my path guess was wrong and the atom right.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§2.1`, `§2.2`, `§2.3`, `§5.1`, `Session 2`. Those tokens appear in its source plan at:

- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:27` — - **KNOW-R1** — structured, idempotent, provenance-carrying `knowledge_persist` contract (§2.1)
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:39` — - **KNOW-R13** — human-gold provenance weighting + decision-shaped knowledge at gate events (§2.1, §4.4; memory-decay weighting re-pointed to LEARNING-FLYWHEEL)
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:45` — - **KNOW-R19** — `expires_at`/TTL field on `knowledge_persist` (§2.1); the environmental context-probe template family is re-pointed to WORKFLOWS-V2-AUTOMATION-SUBSTRATE (harness state is not knowledge)
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:57` — **ARTIFACTS = a third, separate registry.** `artifacts/registry.py` holds named, VERSIONED, LLM-generated content (widget/html/markdown/svg) under `~/.personalclaw/artifacts/`. It has no structural relationship to knowledge today — the `also_artifact` link proposed in rev 1 **does not exist**; th…
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:141` — - **Writes through the ONE queue:** `knowledge_persist` writes via `store.create_typed_item(...)` (or the idempotent update path, §2.1) + `ingest_queue.enqueue(item_id)` — the single enrichment path all creators use. Synthesized notes route through the Passthrough pipeline graph (they arrive alre…
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:186` — - `evidence: exact_title | keyword | vector | graph` + `create_safety: exists | probable | unknown` — so workflow nodes branch update-vs-create WITHOUT an LLM duplicate check (this is what §2.1's idempotent persist keys off);
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:215` — 2. Persisting an already-known claim appends a **mention** and re-aggregates confidence (§2.1) — `support_count` becomes a ranking signal.
- `§2.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:224` — **Contradiction check on persist:** (1) deterministic pre-LLM test — flag claims sharing subject+predicate with different objects, or subject+object with different predicates (zero-cost, enabled by §2.1's claims structure); (2) then retrieve semantically-near items (the existing hybrid retriever …
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:34` — - **KNOW-R8** — `knowledge_retrieve` upgrades: tiered modes, detail budgets, evidence contract, freshness, graph expansion (§2.2)
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:53` — **KNOWLEDGE = the user's personal items.** Documents, files, photos, notes, bookmarks, journals — one GLOBAL library in `~/.personalclaw/workspace/knowledge/knowledge.db` (`knowledge/store.py`, one item = one logical document; the `namespace` column was deliberately dropped — there is NO per-proj…
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:158` — | `expires_at` / `ttl` | string | Optional expiry for knowledge that goes stale on a known clock (price quotes, schedules). `knowledge_retrieve` demotes/filters past-expiry items via freshness metadata (§2.2d). |
- `§2.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:398` — `coverage_gap` journal events (zero-result retrieves, §2.2) become persist proposals for the periodic synthesizer — closing the accumulate→synthesize→persist feedback loop.
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:43` — - **KNOW-R17** — zero-model heuristic extraction floor (§2.3)
- `§2.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:157` — | `extraction` | string | `llm` / `heuristic` — provenance of the extraction tier (§2.3) |
- `Session 2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:540` — - **Session 2 — The provider pair:** `apps/native/knowledge-actions/` app; `knowledge_persist` (idempotent upsert/append_evidence/ops modes, budgets, citations enforcement, dual-artifact write, heuristic floor) + `knowledge_retrieve` (modes, detail budgets, evidence/create_safety, freshness, grap…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
