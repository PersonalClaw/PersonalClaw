<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-10` — Consumer-liveness detection: surface a work unit whose output nobody reads

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #468 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1339 |

## Intent — what this is for

The watchdogs measure PRODUCER health — findings count, turn wall-time, stagnation, consecutive errors. Nothing asks whether a loop's output was ever read. The field post-mortem this is drawn from (2026-08-11) is a fully autonomous pipeline that had been dead for two months with nobody noticing, and its author's diagnosis was structural rather than tooling: nobody owned acting on the output. Our equivalent is a monitor-kind run writing a deliverable on a cadence into an artifact nobody opens — we detect a STALLED one and never a POINTLESS one. The raw signal already exists (artifact events, inbox read state, `pinned_artifacts`).

## Done when

A dormancy sweep reports a work unit whose last N cycles produced output with no consumer touch, surfaced as a PROPOSAL to pause or retire it — never an automatic stop, because "nobody looked yet" and "nobody will ever look" are different facts and only the user knows which. Uses the `PP-9` outcome record with a consumption horizon rather than a new counter. Verified by driving a run whose artifact is never opened (sweep fires) and one whose artifact is opened (sweep stays silent) — the second half is the one that proves it is not a blanket nag.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PP-9`](PP-9.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `94711166d` 2026-08-15 — docs(roadmap): flip PP-2/PP-5/PP-10/PP-14 to done (batch cherry-pick lost their plans[] edit)
- `2a720fc52` 2026-08-14 — feat(ledger): PP-10 surface a work unit whose output nobody reads
- `e0dd8701b` 2026-08-14 — feat(ledger): PP-9 open the outcome record to every producer

### Named by atom id in the roadmap prose (10)

- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:97` — (`PP-6`), trajectory signatures (`PP-7`), edge statistics (`PP-8`), consumer liveness (`PP-10`),
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:100` — ## §3 Outcomes — measure what landed, not what ran (`PP-9`, `PP-10`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:106` — unmeasurable outcome decays faster. `PP-9` generalizes it; `PP-10` uses it to surface a work unit whose
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:142` — ├─→ PP-9 ─→ PP-10                  │
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:851` — artifact's outcome is inconclusive", which the user cannot act on and which `PP-10` is the atom
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1000` — - **2026-08-14 — DONE (`PP-10`): consumer-liveness detection.** `learning/consumer_liveness.py`
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1012` — - **2026-08-14 — DEVIATION (`PP-10`): a THIRD metric source, and the publish producer moved onto it.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1015` — handed the counter to `PP-10`. Rather than write that counter — a second store the atom exists to
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1023` — - **2026-08-14 — MEASURED (`PP-10`): the firing population before the control got teeth.** Per this
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1034` — - **2026-08-14 — DISCOVERY (`PP-10`): an un-versioned artifact edit leaves no timeline event.**

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
