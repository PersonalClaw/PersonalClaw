<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AE-5` — Grid toolbar: search + kind/source chips + collection picker/assign + sort, URL-query-backed

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [ARTIFACTS-EVOLUTION](../plans/ARTIFACTS-EVOLUTION.md) (`AE`, plan status `done`) |
| **Atom table** | [`../atomic/AE.md`](../atomic/AE.md) |
| **Execution position** | #15 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 2 T2.2

## Done when

filter state round-trips the URL (?q/?kind/?src/?col/?sort); assigning a collection from the card/detail menu persists via PATCH

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AE-4`](AE-4.md) | intra-repo | `done` |
| [`AE-1`](AE-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:120` — | T2.2 | Grid toolbar: search + kind/source chips + collection picker/assign + sort; URL-query-backed (shareable filter state) | `ArtifactsSection.tsx` | filter state round-trips the URL; assigning a collection from the card menu persists |
- `T2.2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:218` — from the registry replaces the image kind check. **T2.2 toolbar:** search + kind Segmented + source/collection Popover
- `Session 2` `docs/roadmap/plans/ARTIFACTS-EVOLUTION.md:115` — ### Session 2 — The library surface

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
