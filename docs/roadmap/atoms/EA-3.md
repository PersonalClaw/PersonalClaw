<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EA-3` — Curated read-only MCP server (/mcp) — INHERITED from plan 41

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXTERNAL-ACCESS](../plans/EXTERNAL-ACCESS.md) (`EA`, plan status `done`) |
| **Atom table** | [`../atomic/EA.md`](../atomic/EA.md) |
| **Execution position** | #216 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##75/#83/#116 |

## Intent — what this is for

§3 (Dialect 2): streamable-HTTP /mcp mount + 5 curated read-only tools (memory_recall, knowledge_search, tasks_list/task_get, speak, search_transcripts). Session 3 (MCP half).

## Done when

Shipped under sibling plan 41 MCP-READONLY-INBOUND (src/personalclaw/inbound/mcp_http.py + tools.py, mounted at dashboard/server.py:410, fail-closed per __init__.py). EA does NOT rebuild this; per-client subsetting/scope-pins ride EA-1's bindings. Catalogued so the DAG shows §3 satisfied externally.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (7 line(s))

This atom's scope cites `§3`, `Session 3`. Those tokens appear in its source plan at:

- `§3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:13` — as of 2026-08-04 — but **§3's extracted slice HAS shipped under plan 41 MCP-READONLY-INBOUND**
- `§3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:19` — **Amended 2026-07-18 (roadmap rev 9):** two extractions land EARLY — §3's read-only MCP inbound became
- `§3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:26` — - **NEW-10** (hardened external access: OpenAI-compatible inbound HTTP API where `model` targets an agent + `/v1/audio/speech|transcriptions|voices` aliases; gateway-mounted MCP server exposing a curated read-only capability subset with per-client bindings + kill switch; sender-trust substrate — …
- `§3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:73` — - **Query-only with no path to writes** on the outward read surfaces (§3): the MCP server's tool table is a hand-curated allowlist of read-only operations; there is no generic tool passthrough to `_aggregated_call_tool` (which includes write tools) and **an inbound request can never trigger a mig…
- `§3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:99` — Inbound-run agent turns are unattended work: `inbound:` sessions resolve through the **`headless` SafetyProfile by construction** (guardrails §3 keys profiles off session-key classes — this plan adds `inbound:` to that classification), meaning read-only tool defaults, creation-time write grants o…
- `§3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:251` — - **Session 3 — MCP server + control bridge (§3, §4):** streamable-HTTP MCP mount, the five curated tools with per-client subsetting + scope pins, result caps/framing; the loopback bridge (random port, discovery file, action registry with sideEffect/requiresConfirmation, server-side confirm flow).
- `Session 3` `docs/roadmap/plans/EXTERNAL-ACCESS.md:251` — - **Session 3 — MCP server + control bridge (§3, §4):** streamable-HTTP MCP mount, the five curated tools with per-client subsetting + scope pins, result caps/framing; the loopback bridge (random port, discovery file, action registry with sideEffect/requiresConfirmation, server-side confirm flow).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
