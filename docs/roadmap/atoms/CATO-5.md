<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CATO-5` — Usage read routes + offline agent-reference regen

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [COST-AND-TOKEN-OBSERVABILITY](../plans/COST-AND-TOKEN-OBSERVABILITY.md) (`CATO`, plan status `done`) |
| **Atom table** | [`../atomic/CATO.md`](../atomic/CATO.md) |
| **Execution position** | #101 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

C3 Integration points (read routes); §2.2 error envelope; Session 1 T1.5

## Done when

GET /api/usage/rollup?group_by=&since=&until= and GET /api/usage/totals return correct aggregates using the §2.2 {error:{code,message}} envelope; offline agent reference regenerated (python -m personalclaw.manifest_reference) and the reference drift test is green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CATO-1`](CATO-1.md) | intra-repo | `done` |

### Unblocks (2)

[`CATO-7`](CATO-7.md) (`done`), [`CATO-8`](CATO-8.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 42 (CATO) — Usage panel driven, period round-trip survived a reload

**Code evidence:**

- MEASURED: dashboard/handlers/usage.py serves both routes (:129 registers /api/usage/rollup) with the §2.2 envelope, and the rollup accepts group_by/since/until PLUS session — the session filter is what CATO-7 later consumes
- the offline agent reference is regenerated and its drift test is green (test_api_manifest_drift.py 8/8), which is the half of this clause that would otherwise rot silently
- test_usage_routes.py green inside the 57
- test_usage_ledger + test_usage_routes + test_usage_reachable_purposes 57/57; test_anthropic_cache_usage 5/5; test_api_manifest_drift 8/8; web usage + turn-telemetry 30/30

**Notes:** The `session=` parameter is not in this atom's own done_when but is what makes CATO-7 possible — a case where the route was built one clause wider than its own acceptance criterion, deliberately, because the consumer was already known.

## Recorded history

### Commits (1)

- `4c3b0cd9d` 2026-08-06 — docs(security): EI-11 stop overstating confinement, scope the guardrails + desktop claims (#750)

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:201` — the loop-cron / channel-cli sites (C2 remainder = CATO-4) and the surfaces (S2 = CATO-5..8) remain,
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:221` — (CATO-5) + surfaces (CATO-6..8) remain, so no user-visible readout yet → no CHANGELOG. **Gates:**
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:227` — - **CATO-5 DONE — the usage read API.** `dashboard/handlers/usage.py`: `GET /api/usage/rollup?group_by=&since=&until=`
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:258` — `session_key`; only the query fns lacked the filter) and threaded `?session=` through the CATO-5
- **source plan** `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:290` — read API (CATO-5) → turn / session / account surfaces (CATO-6/7/8). Every soul guardrail held (one

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T1.5`, `§2.2`, `Session 1`. Those tokens appear in its source plan at:

- `T1.5` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:135` — | T1.5 | Read routes: `GET /api/usage/rollup?group_by=&since=&until=` and `GET /api/usage/totals`; §2.2 error envelope; regenerate the offline agent reference (`python -m personalclaw.manifest_reference`) since routes changed — there is a drift test | `dashboard/handlers/`, `server.py`, `docs/ref…
- `§2.2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:135` — | T1.5 | Read routes: `GET /api/usage/rollup?group_by=&since=&until=` and `GET /api/usage/totals`; §2.2 error envelope; regenerate the offline agent reference (`python -m personalclaw.manifest_reference`) since routes changed — there is a drift test | `dashboard/handlers/`, `server.py`, `docs/ref…
- `§2.2` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:230` — in `server.py`. A bad `group_by` is a §2.2 `{error:{code,message}}` 400 (validated at the boundary,
- `Session 1` `docs/roadmap/plans/COST-AND-TOKEN-OBSERVABILITY.md:127` — ### Session 1 — The durable ledger

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
