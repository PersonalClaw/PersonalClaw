<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DHT-8` — reportlab core dependency + pdf writer (platypus flowables)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-HANDLING-TOOLS](../plans/DOCUMENT-HANDLING-TOOLS.md) (`DHT`, plan status `done`) |
| **Atom table** | [`../atomic/DHT.md`](../atomic/DHT.md) |
| **Execution position** | #175 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2 (pdf); Contracts C4b (dependency ruling); T2.2

## Done when

reportlab>=4,<5 added to core dependencies (with WHY comment) and NOT in personalclaw-backend.spec excludes; uv.lock re-locked same commit; pdf_writer.py renders DocumentModel and re-reads through the existing pdfplumber reader with headings/paragraphs/tables/lists intact; pdf unconditionally in available_formats(); bullets extract clean (not (cid:127))

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DHT-1`](DHT-1.md) | intra-repo | `done` |

### Unblocks (1)

[`DHT-10`](DHT-10.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 15 (DHT) — DRIVEN in a real browser

**Code evidence:**

- pyproject.toml:71 reportlab pinned in CORE dependencies with the WHY comment above it, including the spec clause explicitly: 'Deliberately NOT in personalclaw-backend.spec's excludes (unlike faiss/torch): a PDF writer that vanished in the desktop build would recreate that same problem'
- no reportlab entry in personalclaw-backend.spec (checked directly) — the excludes clause holds
- uv.lock carries reportlab at 2303/2441/3242 with the same specifier, so the lock matches the manifest
- MEASURED: available_formats() returned pdf unconditionally in the validation home, and the pdf writer produced a real artifact from the same DocumentModel as the docx
- documents/writers/pdf_writer.py (187 lines) renders the model through platypus flowables

**Driven in the UI:** The generated .pdf artifact appears in the library and its detail resolves through the EXISTING pdf renderer rather than a duplicate type (DHT-9's clause), so the two atoms confirm each other on the same fixture.

**Notes:** One drift worth recording: the atom says reportlab>=4,<5 and the shipped pin is >=4,<6. That is a later widening after reportlab 5 shipped, not a missed clause — the intent (a bounded core pin, not excluded from the desktop build) holds. The reportlab comment is now cited BY the argon2id block at :108 as the precedent for a core dependency, which is a good sign the reasoning got reused rather than re-litigated.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T2.2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:186` — | T2.2 | Add `reportlab>=4,<5` to core `dependencies` per C4b (with the WHY comment, matching the neighbouring block's style) and confirm it is NOT in `personalclaw-backend.spec`'s excludes; write `pdf_writer.py` rendering the `DocumentModel` via platypus flowables; `pdf` is always in `available_…
- `T2.2` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:285` — **NOT done (S2, unchanged scope):** pptx + pdf writers (`reportlab` is S2's T2.2 and is
- `T2.2` `docs/roadmap/plans/DOCUMENT-HANDLING-TOOLS.md:293` — **T2.2 dependency, per the owner ruling:** `reportlab>=4,<5` added to core

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
