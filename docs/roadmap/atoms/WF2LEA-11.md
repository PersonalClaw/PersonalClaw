<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2LEA-11` — Amendment E1.3: retroactive completed-run/conversation → skill proposal (verify-then-build, no second queue)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-LEARNING-FLYWHEEL](../plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md) (`WF2LEA`, plan status `done`) |
| **Atom table** | [`../atomic/WF2LEA.md`](../atomic/WF2LEA.md) |
| **Execution position** | #596 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1086 |

## Intent — what this is for

Amendment (b) agent-authored + retroactively-promoted skills; task row E1.3

## Done when

After auditing skill_remember + proposals.py + §3.2 coverage, ONLY the missing retroactive path is added: a successful run/conversation can be promoted to a skill PROPOSAL feeding §3.2's existing queue; agent may propose unprompted but never writes; a rejected promotion is remembered in decision memory and does not re-surface

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2LEA-7`](WF2LEA-7.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 30 (WF2LEA) — DRIVEN in a real browser

**Code evidence:**

- MEASURED: skill_promotion.py has THREE non-self importer files — the clause's retroactive path exists and is wired
- the clause's 'no second queue' constraint is structural: promotion files into §3.2's existing proposal queue (learning/proposals.py), which is also where require_human sits, so a promoted skill inherits the human-only accept gate rather than needing its own
- tests/test_learning_promotion_wire.py passes
- 951 tests pass across the 30 learning suites

**Driven in the UI:** Not driven: promoting a successful run needs a completed run, which needs a bound model.

**Notes:** Partial for the drive. The reuse-the-queue decision is the load-bearing part and it is the right shape — a second queue would have needed its own accept gate, and the audited history of this codebase is that a second gate is where the weaker one ships.

## Recorded history

### Commits (1)

- `3f0845f9d` 2026-08-11 — feat(learning): WF2LEA-11 retroactive run→skill promotion — proposal only, never a write (#1086)

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:1344` — - **DONE `WF2LEA-11`** (Amendment E1.3 — retroactive completed-run/conversation → skill proposal).

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
