<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DL-5` — T3.2 remainder + T2.4 residual: launch media assets (60-90s demo GIF + social-preview images)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DISCOVERABILITY-LAUNCH](../plans/DISCOVERABILITY-LAUNCH.md) (`DL`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DL.md`](../atomic/DL.md) |
| **Execution position** | #195 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 3 — Launch assets (T3.2 GIF) + Session 2 T2.4 residual (social-preview images)

## Done when

60-90s silent capture (chat->approval->loop->knowledge->artifact) recorded on the seeded demo home and referenced from the site hero, with the click-path scripted; 1280x640 social-preview images produced per web/DESIGN.md palette ready for owner upload to both repos

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DL-4`](DL-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 58 (DL) — the demo seed booted on a fresh home and driven; the capability matrix read row by row

**Code evidence:**

- the media set is DECLARED rather than ad hoc: `marketing/screenshots.manifest.json` carries `$schema`, `schemaVersion`, `targetRelease`, `capture` and `shots`, with a `screenshots.schema.json` beside it
- 🔑 `targetRelease` IS THE DETAIL WORTH KEEPING — launch media pinned to a release is media you can tell is stale. An unversioned marketing screenshot set is the artifact most likely to still show last year's product
- seed suites 57/57; a fresh gateway booted with --seed demo-home on an isolated home and driven in a browser; capabilities matrix 20 rows (9 yes / 7 no / 4 partial); llms.txt 80 lines + llms-full.txt 5417

**Driven in the UI:** Not verifiable from here: a 60-90 second recording and 1280x640 social-preview images are binary artifacts whose CONTENT (the scripted click path, the palette) cannot be judged by reading a manifest, and the atom hands the social-preview upload to the owner.

**Notes:** Partial because the manifest proves the set is declared and pinned, not that the recording exists at the right length showing the right path. The schema-plus-manifest shape is the reproducibility half, and it is the half that survives.

## Recorded history

### Commits (5)

- `ca8b238aa` 2026-09-06 — docs(roadmap): flip DL-5 — the site hero references the tour, one click away on purpose
- `fbd192a73` 2026-09-06 — feat(launch): re-record the DL-5 demo — all five specified beats are now real
- `ecab7a975` 2026-09-06 — feat(launch): script the demo click-path and generate the 1280x640 repo cards
- `df456c8e2` 2026-09-06 — docs(roadmap): mark DIST-11 and CRE-7 owner-only — the frontier was over-reporting
- `e9d624be7` 2026-09-02 — docs(roadmap): flip ET-6 + DL-7 done (exemplar apps registry-listed; /compare matrix shipped)

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:659` — - **[2026-09-06] NOTE — the blocker behind `DL-5`'s three dropped beats is closed; `DL-5` itself NOT
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:676` — depended on a model (the fixture ships both). `DL-5` stays `todo` — its `done_when` requires the
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:687` — - **[2026-09-06] `DL-5` re-capture — all five specified beats are now real.** PR #2532 shipped the
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:706` — `--seed-local-model` binds chat and embedding and nothing local serves speech-to-text. `DL-5`
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:713` — - **2026-09-06 — `DL-5` flipped: the site hero now references the tour, and the reference is one
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:714` — click away on purpose.** The entry above left `DL-5` open on a single clause — the capture had to be
- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:742` — on `/product`. With `DL-5` closed, **every remaining `DL` atom is owner-side**: `DL-9`'s engineering

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T3.2`, `T2.4`, `Session 3`, `Session 2`. Those tokens appear in its source plan at:

- `T3.2` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:79` — | T3.2 | Capture the five screenshots + the 60-90s GIF/MP4 per Design list (owner performs capture if executor lacks a display; script the click-path either way as `docs/assets/capture-script.md`) | core repo: `docs/assets/*.png`, site repo hero media | README placeholder replaced; assets referen…
- `T2.4` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:40` — - **Launch assets:** screenshot set (dashboard home, chat with a tool-approval brief, loop cockpit mid-run, knowledge answer with citations, Store consent surface showing declared permissions) + a 60-90s silent capture (chat→approval→loop→knowledge→artifact/widget produced — the Canvas counter). …
- `T2.4` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:71` — | T2.4 | Sitemap + OG/social meta + per-repo social-preview images (1280×640: name, tagline, coral identity per `web/DESIGN.md` palette) | site repo + image assets; upload via repo settings (owner task 5) | rich embeds render in a link-preview checker |
- `T2.4` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:106` — 5. **Upload social-preview images** to both repos (Settings → Social preview) when T2.4 produces them.
- `Session 3` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:74` — ### Session 3 — Launch assets
- `Session 2` `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:64` — ### Session 2 — Docs site + machine-readable surface

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
