<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `FM-3` — LiquidShape.tsx fluid-blob shape morph (coral-tinted, expr()-scaled)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [FLUID-MOTION](../plans/FLUID-MOTION.md) (`FM`, plan status `done`) |
| **Atom table** | [`../atomic/FM.md`](../atomic/FM.md) |
| **Execution position** | #270 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1662 |

## Intent — what this is for

Session 2 — Liquid morphing (T2.2); Contracts C2 (LiquidShape)

## Done when

shape morphs smoothly between states (SVG-path vs canvas-metaball decided by measurement in T2.2), integrates visually with DotGlow/WavyProgress without clashing, and honors reduced-motion (instant) and expr() scaling

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`FM-1`](FM-1.md) | intra-repo | `done` |

### Unblocks (1)

[`FM-4`](FM-4.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `c76535f3f` 2026-08-18 — chore(roadmap): cite the landing batch #1662 on PUBL-8 and FM-3
- `4d1b7b44d` 2026-08-18 — feat(motion): FM-3 LiquidShape fluid-blob shape morph

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:401` — ### 2026-08-18 — `FM-3` (S2: T2.2; contract §C2 `LiquidShape`) — **DONE**
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:562` — ⚠️ **ADOPTION IS NOW UNOWNED.** `FM-3` deferred it to `FM-4` (line 488 above); `FM-4` declines it because
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:659` — `FM-3` deferred adoption to this atom, and adopting it means editing a product surface outside this
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:661` — No decorative motion was bolted onto a surface on executor taste, per `FM-3`'s own standing reason.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.2`, `Session 2`. Those tokens appear in its source plan at:

- `T2.2` `docs/roadmap/plans/FLUID-MOTION.md:80` — | T2.2 | `LiquidShape.tsx` fluid-blob morph (SVG path or canvas metaball, coral-tinted, `expr()`-scaled) + reduced-motion path | `web/src/ui/motion/LiquidShape.tsx` | shape morphs smoothly between states; integrates visually with `DotGlow`/`WavyProgress` (coherent, not clashing) |
- `T2.2` `docs/roadmap/plans/FLUID-MOTION.md:101` — - **Open:** canvas metaball vs SVG-path for `LiquidShape` — decide in T2.2 by measuring (canvas scales better for many shapes; SVG is simpler for one); no premature choice.
- `T2.2` `docs/roadmap/plans/FLUID-MOTION.md:401` — ### 2026-08-18 — `FM-3` (S2: T2.2; contract §C2 `LiquidShape`) — **DONE**
- `T2.2` `docs/roadmap/plans/FLUID-MOTION.md:412` — to be decided in T2.2 "by measuring … no premature choice". Three implementations of the same
- `Session 2` `docs/roadmap/plans/FLUID-MOTION.md:75` — ### Session 2 — Liquid morphing

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
