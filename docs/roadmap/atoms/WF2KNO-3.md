<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2KNO-3` — Long-run engine additions: until_cancelled + reaper, siblings/previous bindings, buffer-seal, adaptive clamp

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS](../plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md) (`WF2KNO`, plan status `done`) |
| **Atom table** | [`../atomic/WF2KNO.md`](../atomic/WF2KNO.md) |
| **Execution position** | #576 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§4.1 Item Identity + Persistent Seen-Set, §4.2 Bounded-Context Continuation, §4.3 Buffer-Seal, §8 Changes to WORKFLOWS-V2.md; Session 3

## Done when

workflows/longrun.py adds until_cancelled loop mode + reap_watchers, {{siblings.*}} (window/unseen/significant/full/hygiene) + {{previous.output}} + run-continuity state, buffer-seal wait condition, adaptive-delay clamp, journal ledger kinds, validators; a watcher run completes on accompanied-work-complete instead of hanging (validated live); 3 pre-existing loop-path defects fixed

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| `EXT:WORKFLOWS-V2:loop/parallel/wait engine + run journal (Slices 0-2)` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`WF2KNO-6`](WF2KNO-6.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 6 (WF2KNO) — observed

**Code evidence:**

- until_cancelled has THREE live consumers — workflows/controller.py, workflows/tick.py and workflows/validator.py — so the long-run mode is wired into the engine's dispatch, ordering and validation rather than sitting in one file
- tests/test_workflows_longrun.py passes (part of the 210-passed run)

**Notes:** PARTIAL: the reaper and sibling handling are time-and-lifecycle behaviours. Driving them means starting a long-run workflow and cancelling it, which needs a model provider to make the run do anything.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (12 line(s))

This atom's scope cites `§4.1`, `§4.2`, `§4.3`, `§8`, `Session 3`. Those tokens appear in its source plan at:

- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:29` — - **KNOW-R3** — item-identity contract + persistent seen-set + delta rule for `until_cancelled` (§4.1)
- `§4.1` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:285` — # window = synthesis_window; unseen = engine-maintained seen-set (§4.1)
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:30` — - **KNOW-R4** — bounded-context, diff-aware continuation semantics for long runs (§4.2)
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:40` — - **KNOW-R14** — `{{siblings.*}}` share-filter and convergence guard (§4.2)
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:306` — on_error: journal_and_continue   # persist failures are non-fatal (§4.2e)
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:360` — **Binding: `{{siblings.<id>.output}}`** — resolves to outputs emitted by a named sibling within the same `parallel` block, as a filtered, windowed view by default (§4.2). `| full` opts out of the significance filter; `| window(N)` bounds it; `| unseen` applies the persistent seen-set.
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:517` — 2. **New bindings:** `{{siblings.<id>.output}}` (filtered/windowed by default, §4.2) and `{{previous.output}}` (prior successful cycle/run). Binding filters: `window(N)`, `unseen`, `hygiene`, `fenced_sources`, `full`, `clamp`.
- `§4.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:519` — 4. **Run-continuity state** on the workflow def/trigger record (§4.2) — bounded rolling `{summary, recent_topics, recent_refs}` auto-injected into recurring runs.
- `§4.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:35` — - **KNOW-R9** — buffer-seal (volume-driven) synthesis trigger alongside wall-clock cadence (§4.3)
- `§4.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:280` — # or buffer-seal (§4.3): condition = buffer_full OR stale_timeout
- `§8` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:746` — `tests/test_knowledge_longrun_validation.py` for the §8 criteria. 12186 tests. This closes the
- `Session 3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:541` — - **Session 3 — Engine additions:** `until_cancelled` loop mode + persistent seen-set; `{{siblings.*}}` (filter/window/unseen) + `{{previous.output}}` + run-continuity state; buffer-seal wait condition; adaptive delay clamp; non-fatal persist handling; convergence guard.

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
