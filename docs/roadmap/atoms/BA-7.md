<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-7` — user_browser execution-target selector on the browse action config (default gateway, no silent fallback, unattended refusal)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #85 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment 2026-07-29 (a) A second execution target behind one selector + (d) rung cap interactive-only; task B1

## Done when

browse action config gains target: 'gateway'|'user_browser' (default 'gateway', byte-identical existing behavior under regression test); an unconnected user_browser task returns outcome='skip' with a typed actionable reason and NEVER silently falls back to the gateway profile; a scheduled/unattended/cron plan naming user_browser is refused at registration time with a typed error, not at run time — task B1

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`BA-3`](BA-3.md) | intra-repo | `done` |
| `EXT:AUTONOMY-GUARDRAILS:earned-autonomy ladder rung floor (never-unattended)` | cross-plan (EXT) | **unresolved** |

### Unblocks (1)

[`BA-8`](BA-8.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 14 (BA) — DRIVEN in a real browser

**Code evidence:**

- web/src/pages/settings/CompanionPanel.tsx:198-201 renders the Browser control section; SettingsPage.tsx:120 mounts CompanionPanel as { id: 'companion', label: 'Companion apps' }
- web/src/pages/settings/browserControlToggle.test.tsx asserts the PATH STRING the click sends, and says why: 'the switch renders, the user flips it, and nothing is written — because the panel PATCHes the wrong section prefix, or no path at all… the config round-trip's Python half cannot see this'
- src/personalclaw/browse/target.py + tests/test_browse_target.py green in the 426-passed run

**Driven in the UI:** Drove #/settings/companion. The 'Browser control' section reads 'Whether a browse task may drive your own browser, with the sites you are already signed in to', and its hint states all three done_when clauses in plain language: 'Off by default. When off, a task that asks for your browser is skipped with a reason — it is never switched to this machine's own browser profile, which has different logins. Scheduled and unattended runs can never use your browser at all.' Toggled 'Let tasks drive my browser' ON, then forced a FULL page load (new snapshot ref generation) — it came back checked. Persistence proved by reload.

**Notes:** The rare case where the UI copy states the atom's whole contract, including the two things 'a user cannot infer from a switch labelled with a verb' (its own test's words): skipped-not-switched, and never-for-a-scheduled-run. The frontend test asserting the PATCH path string rather than the rendered state is the right rail for this class of defect — a control bound to nothing looks identical to a working one.

## Recorded history

### Commits (5)

- `cf5b3f661` 2026-09-02 — feat(browse): add loopback user_browser connector route (BA-8)
- `995e2ef80` 2026-09-02 — docs(roadmap): flip already-built Wave-GA atoms to done + safe reconciliations
- `4023bf10a` 2026-09-02 — fix(triggers): validate specs at the door — the doctor now tells the truth
- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `3e684d8ac` 2026-08-27 — feat(browse): BA-7 select the browse execution target, refusing user_browser unattended

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:801` — - [2026-08-27][BA-7] **DONE — all three clauses met. Atom marked `🟡` (this code is not on `main`
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:931` — **Note on this plan's shape:** it has **no `## Execution log` heading**. `BA-2`, `BA-3` and `BA-7`

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
