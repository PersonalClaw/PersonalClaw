<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PHF-12` — Teach the inert-surface census whole-enum iteration — the false-red class its own docstring promised was impossible

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-HARDENING-FLOORS](../plans/PLATFORM-HARDENING-FLOORS.md) (`PHF`, plan status `done`) |
| **Atom table** | [`../atomic/PHF.md`](../atomic/PHF.md) |
| **Execution position** | #449 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1148 |

## Intent — what this is for

Corrects PHF-6's census (scripts/generate_inert_surface_baseline.py), whose enum detector documented its own error direction as "never a false red" and then produced one. Whole-enum iteration reaches every member by construction, so one iteration site now clears every member of the iterated class; resolution is import-aware so one of the seven Verdict enums cannot clear another's members. The generator states the detected shapes, the honest trade (more under-reporting bought for the elimination of this false-red class), and the KNOWN remaining false-red shape. Baseline regenerated by the tool, every delta accounted for against the code. The shrink-only forbidden-to-raise ratchet is untouched.

## Done when

whole-enum iteration clears every member of the iterated class, across for/async for, all four comprehension forms, and the list/tuple/set/frozenset/sorted/iter/reversed/enumerate family, matched bare (E) or module-qualified (mutations.OpKind) and resolved through the ITERATING file's own imports so one of the seven Verdict enums cannot clear another's members; the generator states the detected shapes, the honest trade, and value-lookup E(value) as the KNOWN remaining false-red shape; inert-surface-baseline.json regenerated BY THE TOOL (145 -> 140 total, enum 18 -> 13) with every departure verified reachable at a named iteration site and nothing entering; fixture-tree tests pin iteration-only NOT flagged, consumed-nowhere flagged, a same-named enum elsewhere NOT cleared, and a vacuity guard that the census still sees >50 enum classes and still reports members; the shrink-only ratchet is untouched and proven to still red on a real new inert member.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`PHF-6`](PHF-6.md) | intra-repo | `done` |

### Unblocks (1)

[`PHF-13`](PHF-13.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `917ef60f8` 2026-08-12 — feat(workflows): WV-14 make `on_overlap: queue` queue instead of starting a concurrent run (#1130)

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:491` — - [2026-08-12][PHF-12] DONE: the §3.2 census violated the invariant its own detector documents.
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:523` — - [2026-08-12][PHF-12] DISCOVERY: a SECOND, distinct false-red class remains and is deliberately
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:533` — rather than a widening of `PHF-12` (§3.2's stated risk is exactly "resist widening the definition
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:535` — - [2026-08-12][PHF-12] DEVIATION: `PHF-6`'s docstring instructs "do not 'fix' any surface this
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:543` — builds none of the missing writers. **`PHF-12`'s premise was wrong.** It recorded that
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:633` — `PHF-12`'s superseded claim is refused). PROBE, proving the pin can fail: the temporary widening
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:642` — - [2026-08-12][PHF-13] DEVIATION: the atom was scoped assuming `PHF-12`'s DISCOVERY was factually

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
