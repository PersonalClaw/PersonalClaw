<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2WOR-6` — Session-ownership run-start wiring + incognito enforcement at run start

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-WORK-CONTAINERS](../plans/WORKFLOWS-V2-WORK-CONTAINERS.md) (`WF2WOR`, plan status `done`) |
| **Atom table** | [`../atomic/WF2WOR.md`](../atomic/WF2WOR.md) |
| **Execution position** | #653 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##947 |

## Intent — what this is for

§5.1 (session ownership + incognito inheritance), Migration Order step 7

## Done when

Success Criterion 5: at run start the controller performs the session_restrictions mark and the JSONL memory_mode write, and mirrors a completion summary into the launching session; a blocking run launched from an incognito session writes nothing to knowledge/learning stores, verified durable after a gateway restart

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2WOR-1`](WF2WOR-1.md) | intra-repo | `done` |

### Unblocks (1)

[`WF2WOR-11`](WF2WOR-11.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `b3f933a81` 2026-08-09 — feat(workflows): WF2WOR-6 memory-mode inheritance wiring + blocking completion mirror (#947)

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1061` — - **2026-08-09 — DONE — WF2WOR-6 (§5.1, criterion 5): `workflows/ownership.py` wired into the run

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `§5.1`. Those tokens appear in its source plan at:

- `§5.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:501` — source registration and `session._app` keying (§5.1 — it touches `session_map.py`/`sync_bridge.py`,
- `§5.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:502` — which §5.1 itself scopes out); the overview's automatic revision on run completion (needs the
- `§5.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:777` — by §5.1 itself (it touches `session_map.py`/`sync_bridge.py`/Slack bridging); only new `workflow:`
- `§5.1` `docs/roadmap/plans/WORKFLOWS-V2-WORK-CONTAINERS.md:1061` — - **2026-08-09 — DONE — WF2WOR-6 (§5.1, criterion 5): `workflows/ownership.py` wired into the run

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
