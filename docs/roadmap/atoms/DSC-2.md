<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DSC-2` — Playwright visual-regression + axe a11y e2e harness with captured baselines (S2 prerequisite infra)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESIGN-SYSTEM-CONSISTENCY](../plans/DESIGN-SYSTEM-CONSISTENCY.md) (`DSC`, plan status `done`) |
| **Atom table** | [`../atomic/DSC.md`](../atomic/DSC.md) |
| **Execution position** | #203 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S1→S2 safety rail; Risks (screenshot-diff verification); Execution log cy3/cy13/cy14

## Done when

web/playwright.config.ts + e2e/{routes,helpers,visual,a11y}.spec.ts run 16 routes×2 themes; @playwright/test + @axe-core/playwright locked in package-lock; 32 Darwin visual baselines captured deterministic (2× zero-diff) against the tokenless dev gateway recipe; axe wcag2aa suite 32/32; blank-shell blocker root-caused (React-18 pin from main) and closed by rebase.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (6)

[`DSC-10`](DSC-10.md) (`done`), [`DSC-11`](DSC-11.md) (`done`), [`DSC-5`](DSC-5.md) (`done`), [`DSC-6`](DSC-6.md) (`done`), [`DSC-7`](DSC-7.md) (`done`), [`DSC-8`](DSC-8.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:744` — - **Visual baselines: MEASURED as pre-existing drift, deliberately NOT regenerated.** The DSC-2 harness reports **22 snapshots failing** — but they fail **byte-for-byte identically with and without this change** (control run on a pre-adoption build: `skills-dark` 25,068 = 25,068, `skills-light` 2…
- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:745` — - **Gate (from the repo root, venv on PATH).** `make lint` exit 0 (black 1,869 files, isort, flake8, mypy 943 sources) · `npm run typecheck:web` clean · **FULL `npm run test:web`: 458 files / 4,786 tests, 0 failures** · `npm run build --workspace web` clean · `make test` full Python suite · `npx …
- **source plan** `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:1117` — **No atom flipped** — `dag.json` untouched. This is a defect fix on `DSC-2`'s shipped deliverable
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1310` — a Linux runner even if it were wired. `DSC-2`'s visual-regression harness is a shipped mechanism with no

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `cy3`, `cy13`, `cy14`. Those tokens appear in its source plan at:

- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:485` — - **Discovery → the clean slice the triage exposed (verbatim component dedup, GraphZoomControls/cy38 class):** sweeping the page's helpers surfaced that its `Centered` layout wrapper (`function Centered({children}) { return <div className="flex h-full items-center justify-center">{children}</div>…
- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:489` — - **NEXT (item 1 mega-page queue CLOSED — every mega-page triaged):** ChatPage (cy48), ProjectsSection (cy49), and CodeCockpitPage (cy50) are done; the three largest mixed-role pages are triaged and their clean slices extracted (SelectionPill, InlineError, Centered). **No mega-pages remain.** Wha…
- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:516` — ### 2026-07-24 — cy3 DONE (G6 ghost Cancel/Done — the two surface-filled pill outliers collapse to Button variant=ghost — batch cycle 3/9)
- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:544` — 3. **Divergent-geometry clickable micro-pills — left on-token (per the carve-out).** The project-scope chips (`LoopCockpitPage:509`, `DesignCockpitPage:253`, `TasksListPage:515`) are **`h-5`/`h-6` pills sitting in `MetaPill` sibling rows**; `DagView:207` Approve is half of an **Approve/Deny pair*…
- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:560` — - **Ratchet: `rawButton` 314 → 290 (−24), committed same cycle.** Five buttons removed this cycle (audit per-file: ChatPage 18→16, ProjectsSection 18→17, CodeSection 3→2, CodePlanReview 9→8) drop live to **290**; the `primitiveAdoption` soft-nudge ("live counts well below baseline — ratchet DOWN …
- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:586` — - **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-p…
- `cy3` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:633` — - **Why in-scope now:** cy36's deferral list (plan §"Not this cycle") flagged the **`RowActionButton` family** as owner-gated because converging it "moves pixels" and its variants (toggle / danger-reveal / danger-always) seemed to need a new `tone`/`active` API. Two things resolve that: (1) the o…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:623` — ### 2026-07-24 — cy13 DONE (S3 a11y parity — the ten non-default color schemes reach WCAG AA in light mode, closing the cy15 DEVIATION; a permanent per-scheme contrast guard replaces the default-only axe rail)
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:628` — - **Validation (drove the running dev gateway, not just the source):** bundled Playwright against `127.0.0.1:10000` — forced `.light`, applied each scheme's light color set to `document.documentElement` exactly as `appearance.tsx`'s `applyScheme` does (`root.style.setProperty` per accent var), re…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:669` — - **No ratchet delta (non-ratchet cycle, like cy13):** both blocks already used `<Button>` (no raw `<button>`), and `consistencyAudit.report.ts` tracks chrome/weights/color — **not** any empty-state or interaction-pattern metric — so `primitiveAdoption.baseline.json` stays `{ rawButton: 278, rawI…
- `cy13` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:682` — - **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-…
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:632` — ### 2026-07-24 — cy14 DONE (S2 raw-button wave — the reveal-X row-delete family in `formControls.tsx` converges onto `SquareIconButton tone="danger"`; owner-authorized under the full-authority grant)
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:643` — - **Why in-scope now:** direct continuation of cy14's NEXT. cy14 converged the tightest sub-cluster (the 3 reveal-X buttons in one file); cy15 extends the same `SquareIconButton tone="danger"` convergence to the **standalone inline danger-remove idiom** wherever it appears raw across the app — th…
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:645` — - **Faithfulness (what moves, what doesn't):** `tone="danger"` renders `grid size-7 rounded-md text-on-surface-low hover:text-danger` — **no fill** in idle OR hover, byte-identical hover treatment to all four raw buttons. Deltas, all improvements: (a) hit-area standardizes to `size-7` (28px) — Vo…
- `cy14` `docs/roadmap/plans/DESIGN-SYSTEM-CONSISTENCY.md:677` — - **Why a no-code cycle is the honest move (not a stop, not a manufactured edit):** the autonomous-loop-continuity directive says don't stop while authorized work remains — but the soul guardrail ("consistency, not redesign; add a shared primitive *only when the system genuinely lacks a pattern*"…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
