<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `ET-8` — Registry surface on personalclaw.dev generated from `registry.json`

| | |
|---|---|
| **Status** | `todo` |
| **Plan** | [ECOSYSTEM-TOOLING](../plans/ECOSYSTEM-TOOLING.md) (`ET`, plan status `in_progress`) |
| **Atom table** | [`../atomic/ET.md`](../atomic/ET.md) |
| **Execution position** | #263 of 684 in dependency order |
| **Startability** | dependency-satisfied but **gated** (`owner`) |
| **Blocked reason** | PARTIAL 2026-08-26 (personalclaw.dev#31, rebase-merged 3cc45c89) - the pages, the cards, the per-app pages, the README fetch and the rebuild-picks-up-changes clause are all shipped and railed. TWO clauses remain, and the first is a PREMISE CORRECTION worth reading before anyone re-picks this. (1) THE REGISTRY DOES NOT EXIST AT THE PINNED CORE RELEASE. scratch/registry/app-registry.json 404s at v0.1.3 (2026-07-30); the registry tier landed on core main on 2026-08-18 (24f3f5c9, ET-3) and there is no newer tag. Verified independently with `git cat-file -e v0.1.3:scratch/registry/app-registry.json`. Reading core main instead would publish UNRELEASED core state as released state, so absence is recorded as a state (availability: 'absent-at-pin') and the page says so; the loader renders THREE states a card count cannot distinguish - absent-at-pin, empty, listed. So done_when's V4 ('a merged registry PR appears on the site after rebuild') is unreachable until the pin moves to a release containing the registry. What IS proven instead: changing the input and rebuilding changes the output. (2) 'card data matches the Store consent surface' is unverified against a live Store; what is asserted is that every consent value renders byte-equal and order-equal to its registry field, so it cannot disagree with any other reader of the same field. SECURITY SHAPE, since this is a pre-install trust surface: only the exact string 'clean' gets tone 'pass'; an unrecognised verdict presents as BLOCKING, never reassuring; 'no scan on record' renders as a distinct dashed treatment; permissions render permissions_declared verbatim and order-preserved. Anti-vacuity is explicit because the production registry is empty - a generator over it renders a clean zero-card page and every test passes for the wrong reason - so validate-registry-render.mjs fails if a registry with readable listings renders zero cards. OWNER-ONLY FIRST, THEN AGENT. CLEARS WHEN (corrected 2026-09-07, and the gate is SMALLER than this reason claimed): core cuts a release containing scratch/registry/app-registry.json, then re-point the pin — and V4 is measurable. The old wording also required 'a community registry PR merges'; that is already satisfied and was when this was written. FOUR listings are merged in core today (channel-null, inbox-github-notifications, watched-source-github, action-home-assistant), shipped by ET-6 which is done. A RELEASE ALONE SUFFICES. TWO further corrections. The path: this reason named scratch/registry/registry.json throughout, but fd93510f8 (2026-08-27 — the day after this was written) renamed it to app-registry.json, which is what scripts/sync-registry.mjs actually reads; the stale name is fixed above. And the GATE IS NOT ET-9: the registry data tier lives in CORE, which is where the site reads it, so the standalone github.com/PersonalClaw/registry repo matters only for the future community-PR flow the schema $id points at and is not on this atom's path. Measured: the site pins core v0.1.3 @ bc185c02 (2026-07-30); the registry tier landed on core main 2026-08-18 (24f3f5c9), confirmed NOT an ancestor of v0.1.3 and an ancestor of origin/main; gh release list still shows v0.1.3 as Latest. The live /registry page renders the honest third state verbatim: 'There is no registry file to read at PersonalClaw/PersonalClaw v0.1.3, so no listings are published here.' EXT DEP DROPPED AS SATISFIED: 'the site sync pipeline this registry surface extends' shipped in DL-1 (scripts/sync-docs.mjs + scripts/sync-sources.mjs), and scripts/sync-registry.mjs imports loadManifest, resolveSource and writeAtomically directly from ./sync-sources.mjs, so the extension is demonstrable rather than asserted. Caveat: DL-1 carries no evidence field at all, so this rests on code and live output only. |

## Intent — what this is for

Session 4 (T4.1, V4); Design — Registry surface (S4)

## Done when

static registry pages generated on personalclaw.dev from registry.json (cards: name/types/permissions/verdict/maintainer; per-app page with README fetch); declared permissions + last scan verdict visible pre-install; a rebuild picks up registry changes; V4: a merged registry PR appears on the site after rebuild and card data matches the Store consent surface

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`ET-3`](ET-3.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 61 (ET) — the scaffold type table run live; the registry sync run live, which reversed a finding

**Code evidence:**

- 🔑 THE PAGES ARE BUILT AND THEY ARE THE BEST PRE-INSTALL CONSENT SURFACE IN THIS CATALOGUE — 411 lines for the index and 303 for the per-app page, reading one normalizer, with a per-app README fetch and a verdict legend
- 🔑 PERMISSIONS ARE RENDERED VERBATIM, AND THE REASON IS THE SHARPEST SENTENCE HERE: they 'are never summarised, grouped, scored, or translated into a friendlier word. The reader is deciding whether to trust an app; A PARAPHRASE IS A CLAIM THE REGISTRY DID NOT MAKE'
- 🔑 AN ABSENT VERDICT IS NOT A PASSING ONE, and the rule is fail-closed: 'no scan on record' must be 'visibly distinct from "clean", never merely quieter', and 'ONLY the exact value "clean" is presented as passing; anything unrecognised is presented as blocking'. That is ARCC's graded-verdict requirement with the same polarity — anything not affirmatively safe is not treated as safe
- 🔑 NO TRUST SIGNAL IS INVENTED — 'Every field on the page is a registry field' — and an unreadable row is NOT rendered while the page states how many it dropped, so a parse failure is disclosed rather than silently shrinking the catalogue
- 🔑 ONE NORMALIZER, WITH THE REASON: the pages, the route-set contract and the render validator all read the same module, because 'a second parser would be a second place for a pre-install consent surface to disagree with itself'
- 🔴 BUT THE SURFACE RENDERS ZERO LISTINGS TODAY, DELIBERATELY. I ran the sync: `registry absent-at-pin, 0 listing(s) from PersonalClaw/PersonalClaw@bc185c02, 0 README(s) fetched`. The site pins core at the released version, and the registry file POSTDATES that release, so it 404s at the pin — and reading core `main` instead 'would publish unreleased core'
- app-scaffold + registry-validation suites 174/174; `app new --list-types` prints 19 provider types live; the site's registry sync run live returns `registry absent-at-pin, 0 listing(s)`

**Driven in the UI:** Not driven: the rendered pages are on the deployed site. The data path was exercised directly by running the sync script, which is what produced the finding below.

**Notes:** 🪤 A CONTRADICTION I WAS ONE STEP FROM FILING, AND THE SAVE IS THE POINT. Having read 700 lines of real pages, I had this atom as built-but-marked-todo — an understating status, which would have been the campaign's first contradiction in that direction. Then I ran the sync and found the surface renders nothing at the pinned release, by design, because the registry data postdates it. So `todo` is DEFENSIBLE: the pages exist, the catalogue they would show does not exist at the version the site publishes. The lesson is the campaign's recurring one in a new place — I read the RENDERER and concluded 'built', when the decisive fact was the DATA FLOW at the pin.

## Recorded history

### Commits (5)

- `3172b2f80` 2026-09-07 — fix(tools): an evidence line must say what kind of match produced it
- `c010dab70` 2026-09-07 — fix(tools): a pre-existing container is not deliverable evidence
- `ccb4541b8` 2026-09-06 — docs(roadmap): flip EI-6, and fix four gates that were wrong about why they were gated
- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips
- `908c7ed15` 2026-08-26 — docs(roadmap): ET-8 records the absent-at-pin registry and its two unmet clauses

### Named by atom id in the roadmap prose (5)

- **source plan** `docs/roadmap/plans/DISCOVERABILITY-LAUNCH.md:615` — Same class as `ET-8`'s premise correction. **No pin move is required** — nothing unreleased was
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:118` — done_when, and ET-10's claimed gating of ET-8 was a premise error: ET-8's registry surface reads
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:478` — - [2026-08-26][ET-8] **PARTIAL — stays `todo`, and the reason is a PREMISE CORRECTION, not a gap in
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:508` — so this is pre-existing, not ET-8's. And `validate:visual-baselines` reads **green locally with
- **source plan** `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:780` — publication path `ET-8`'s personalclaw.dev registry surface depends on. Owner action, marked

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T4.1`, `Session 4`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:95` — | T4.1 | Static registry pages on personalclaw.dev generated from `app-registry.json` (cards: name/types/permissions/verdict/maintainer; per-app page with README fetch) | site repo (plan 36's sync pipeline extension) | site lists registry; permissions + verdict visible pre-install; rebuild picks …
- `Session 4` `docs/roadmap/plans/ECOSYSTEM-TOOLING.md:91` — ### Session 4 — Registry surface (Wave 3)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
