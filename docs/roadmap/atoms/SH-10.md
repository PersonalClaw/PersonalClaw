<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SH-10` — Security panel: baseline denylist shown read-only with version + verified-hash indicator and 'N user additions'; anti-drift/anti-LLM-tamper (not anti-owner) limitation documented

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SECURITY-HARDENING](../plans/SECURITY-HARDENING.md) (`SH`, plan status `in_progress`) |
| **Atom table** | [`../atomic/SH.md`](../atomic/SH.md) |
| **Execution position** | #519 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1290 |

## Intent — what this is for

Amendment (2026-07-26) T4.4

## Done when

the /api/security/denied-commands payload + security settings page render the baseline-verified state; a tamper fixture flips the indicator; 'N user additions' shown; the anti-drift/anti-LLM-tamper-not-anti-owner threat model is documented in docs/security/

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SH-6`](SH-6.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `b52343f13` 2026-08-14 — test(settings): complete both Security panel mocks after DC-2 and SH-10 met
- `42449ac37` 2026-08-14 — feat(security): SH-10 show which command denylist is in force and whether it drifted

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:611` — ### 2026-08-14 — SH-10 (Amendment T4.4) the Security panel renders the verified baseline — DONE
- **source plan** `docs/roadmap/plans/SECURITY-HARDENING.md:736` — `allowed_commands`) are dropped at `load()` and change nothing. If SH-10's panel gains a

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T4.4`. Those tokens appear in its source plan at:

- `T4.4` `docs/roadmap/plans/SECURITY-HARDENING.md:135` — | T4.4 | Security panel: baseline shown read-only with version + verified-hash indicator and "N user additions"; docs state the anti-drift/anti-LLM-tamper (not anti-owner) threat model honestly | `dashboard/handlers/core.py` (`/api/security/denied-commands` payload), security settings page, `docs…
- `T4.4` `docs/roadmap/plans/SECURITY-HARDENING.md:611` — ### 2026-08-14 — SH-10 (Amendment T4.4) the Security panel renders the verified baseline — DONE

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
