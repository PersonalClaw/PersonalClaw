<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DAS-5` — Inventory-driven merge/restore/export sweep + restore endpoint (T2-M1..M3, Sessions 2d-2l)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DURABILITY-AND-SYNC](../plans/DURABILITY-AND-SYNC.md) (`DAS`, plan status `done`) |
| **Atom table** | [`../atomic/DAS.md`](../atomic/DAS.md) |
| **Execution position** | #143 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment 2026-07-26 (T2-M1/M2/M3); §1 merge field consumers; §6 export/import functions; §8 portability.py ABSORBED

## Done when

restore --mode merge dispatches all five StateEntry merge strategies (append_dedup incl. SEL key-gated fail-closed, sqlite_attach_ignore with FTS rebuild, union_by_id, lww_by_updated_at, replace_only skipped loudly); --components everything superset restores 19/19 files 0 lost; --dry-run prints per-entry merge plan matching the real merge; POST /api/durability/restore mirrors CLI with non-empty-home (home_is_populated) auto-detect + gateway-running refusal; create_export_zip/apply_import_zip made inventory-driven (no-secret asserted on bytes); triggers.json added to inventory; success criteria 1-2 and amendment gaps 2-3 met; full pytest -n4 green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DAS-1`](DAS-1.md) | intra-repo | `done` |
| [`DAS-3`](DAS-3.md) | intra-repo | `done` |

### Unblocks (2)

[`DAS-10`](DAS-10.md) (`done`), [`DAS-6`](DAS-6.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 12 (DAS) — observed

**Code evidence:**

- tests/test_durability_merge.py, test_durability_db_merge.py and test_durability_archive.py pass (part of the 561-passed run)
- tests/test_durability_convergence_e2e.py passes — an end-to-end convergence rail, which is the strongest shape available for a merge/restore claim

**Notes:** The convergence e2e module is why this is confirmed rather than partial: merge and restore are exactly where a unit test can pass while the composed behaviour diverges, and there is a rail for the composition.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§1`, `§6`, `§8`. Those tokens appear in its source plan at:

- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:28` — - **NEW-4** — durable state backup + multi-machine sync core (JSONL shards + SHA manifest + validate; pull→merge-import→export-union→push; boot-started scheduled self-backup with rolling backups + restore endpoints; git-versioned memory snapshots; encrypted-intents crypto for machine exchange) → …
- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:55` — - **`triggers.json` does not exist yet** — background stores today are `crons.json` / `hooks.json` / `event_triggers.json` / `autonudge.json`. The inventory (§1) covers what exists; when AUTOMATION-SUBSTRATE unifies them, the inventory line item swaps (that plan's step 9 already owns updating sna…
- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:207` — | `snapshot.py` | **ABSORBED** (kept as the tar engine) | `VALID_COMPONENTS`/`CORE_FILES` re-derived from the inventory (§1); gains `everything`; sqlite-backup-API applied to ALL `*.db` discovered (fixes the live `knowledge.db` raw-copy hazard); WAL checkpoint, `_data_filter`, atomic tar rename, …
- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:276` — **Merge-mode restore as a first-class, plan-printing citizen.** Sibling evidence: the restore people actually perform is "onto a machine that already has state" (new laptop half-set-up, partial loss, second machine) — and replace-mode restores there destroy the newer half. Recon: `snapshot.py::re…
- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:280` — - **Inventory-driven merge (extends §1/§3, no new mechanism):** `restore --mode merge` iterates `durability/inventory.py` entries and dispatches on the already-specified `merge` field — `union_by_id` (entity dirs: copy rows/files whose id is absent), `sqlite_attach_ignore` (memory/knowledge DBs —…
- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:454` — *"the §1 inventory's `merge` field is specified but merge-restore isn't listed as a
- `§1` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:622` — - [2026-08-05][S179] **DONE.** Closed §1's real gap: the manifest was incomplete, and the guard that
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:30` — - **NEW-4.b** — user-facing export/import/archive-restore endpoints (DSAR-shaped portability safety net) → §6
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:208` — | `portability.py` | **ABSORBED** | `EXPORT_EXCLUDE`/`EXCLUDE_DIRS` re-derived from `secret ∪ derived`; zip format gains the shard manifest (v3); endpoints grow into §6's surface; existing merge/replace + traversal validation kept |
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:240` — - **Session 5 — Time-travel + portability endpoints + FE.** §5 adaptive-debounce repos + rollback/revert/preview; §6 export/import/archive endpoints; Settings → Durability panel (status card, archive browser, sync config via the standard `/api/providers/...` routes, time-travel timeline, conflict…
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1471` — ## Execution log — DAS-10 (§6 DSAR endpoints) — **PARTIAL**
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1473` — - [2026-08-16][DAS-10] **PARTIAL — the atom stays `todo`.** The §6 *endpoint* half landed complete;
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1483` — buttons** (everything / knowledge / memory), §6's "separate buttons, not one blob".
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1490` — - [2026-08-16][DAS-10] 🔴 **MEASURED: the export leaked a `derived=True` store.** §6's rule is
- `§6` `docs/roadmap/plans/DURABILITY-AND-SYNC.md:1514` — - [2026-08-16][DAS-10] DEVIATION: **§6's "shard export as a zip" is NOT what shipped.** §6 and the

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
