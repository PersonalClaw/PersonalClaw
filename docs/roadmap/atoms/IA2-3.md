<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `IA2-3` — Turn-time injection _inject_investigate_context (first turn only, fenced once)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [INVESTIGATE-ANYWHERE](../plans/INVESTIGATE-ANYWHERE.md) (`IA2`, plan status `done`) |
| **Atom table** | [`../atomic/IA2.md`](../atomic/IA2.md) |
| **Execution position** | #288 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

S1 T1.3 / Contracts C3 — turn-time injection beside _inject_knowledge_content

## Done when

staged envelope reaches the model as fence_untrusted(snapshot, source=investigate:<kind>) + labelled preamble, joined to the attachments→knowledge→investigate chain at depth 0; second turn injects nothing; fence-break content in a snapshot is neutralised (test-proven); user-visible message untouched

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`IA2-1`](IA2-1.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T1.3`. Those tokens appear in its source plan at:

- `T1.3` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:105` — | T1.3 | Turn-time injection: `_inject_investigate_context` (first turn only, `fence_untrusted` with `source="investigate:<kind>"`, clears after inject) joined to the chain at `chat_runner.py:886-894`; test proves the fence markers wrap the snapshot and the user-visible message is untouched | `da…
- `T1.3` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:127` — - **Prompt-injection via entity content:** the whole reason the envelope is fenced + server-composed. The T1.3 fence-break test is the guard; any future resolver MUST route through the one injection point (grep gate: no resolver output reaches a prompt except via `_inject_investigate_context`).
- `T1.3` `docs/roadmap/plans/INVESTIGATE-ANYWHERE.md:133` — - [2026-07-27][S1] DONE: the primitive (T1.1-T1.5). **T1.1** `src/personalclaw/investigate.py`: `InvestigateContext` envelope + resolver registry (`register_investigate_resolver`/`resolve`; unknown KIND raises KeyError → the route's 400, unknown ENTITY returns None → 404), snapshot cap 8KB with a…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
