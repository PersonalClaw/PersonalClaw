<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WS-4` — feed-source (RSS/Atom/JSON/CSV + HN/GitHub presets) + cross-feed dedupe + raw-mode FeedItemGraph

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WATCHED-SOURCES](../plans/WATCHED-SOURCES.md) (`WS`, plan status `done`) |
| **Atom table** | [`../atomic/WS.md`](../atomic/WS.md) |
| **Execution position** | #664 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1402 |

## Intent — what this is for

§3.1 feed kinds + presets-as-recipes; §3.2 ETag/Last-Modified conditional-GET cursors; §3.3 guid composition + cross-feed canonical-URL dedupe; §6.3 raw enrichment via FeedItemGraph (no LLM nodes) + full variant fencing; §11 step 3

## Done when

Polling the same feed twice produces zero duplicate items and the same story arriving via HN Algolia AND RSS produces ONE item with both attributions (also_seen_in) (SC#3); a raw source's items reach FTS + vector search with zero LLM calls, asserted structurally that the raw graph contains no LLM nodes (SC#6)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WS-2`](WS-2.md) | intra-repo | `done` |

### Unblocks (1)

[`WS-9`](WS-9.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (1)

- `8c4f14189` 2026-08-15 — feat(sources): WS-4 cross-source dedup with both attributions

### Named by atom id in the roadmap prose (10)

- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:468` — **No CHANGELOG entry, deliberately, for the same reason WS-4 recorded:** `store.create_source` still
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:495` — - [WS-4] DONE: **§3 feed kinds + §3.3 cross-source identity + §6.3 raw mode.**
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:529` — create a watched source of ANY kind, so `WS-2`, `WS-4` and `WS-5` are all complete and the feature as a
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:536` — callers**, so WS-2's store, WS-3's five-detector web kind, WS-4's feeds and WS-5's directory
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:587` — `items.source_id` — WS-4 already documents that an item whose source row vanished degrades to raw,
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:688` — The other row's `0 new` is correct and is WS-4 working: repointed at the GitHub changelog it saw
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:695` — **CHANGELOG: yes.** WS-3 and WS-4 both withheld an entry because nobody could reach the feature.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:798` — brief said "WS-9 shipped the Sources UI"; `WS-9` is still `todo` and WS-4's own log records that
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:903` — zero non-test callers exactly as `WS-3` and `WS-4` recorded — so "bundled recipes surface in the
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1005` — **CHANGELOG: yes** — and this is the first WS atom that earns one. `WS-2`/`WS-3`/`WS-4`/`WS-5` all

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `§3.1`, `§3.2`, `§3.3`, `§6.3`, `§11`. Those tokens appear in its source plan at:

- `§3.3` `docs/roadmap/plans/WATCHED-SOURCES.md:29` — - **NEW-5** (core) — five-detector auto-source stack + declarative selector configs + preview → §2; outcome-driven escalating fetch under one budget at the `net.fetch` chokepoint → §2.3; guid-keyed novelty gating → §3.3; first-class watched feeds (HN Algolia, GitHub trending, RSS/CSV) with since-…
- `§3.3` `docs/roadmap/plans/WATCHED-SOURCES.md:79` — `SourceItem` is the normalized unit: `{guid, title, url, summary, content, published_at, author, media, extra}` — `guid` is mandatory (composed per §3.3 when the source doesn't supply one). Duck-typed detection (`is_knowledge_source_provider`, mirroring `local_models/registry.py:is_local_model_pr…
- `§3.3` `docs/roadmap/plans/WATCHED-SOURCES.md:111` — The **SourceEngine** (`knowledge/sources/engine.py`) is the missing poll loop the recon flagged (`detect_changes` has zero callers): a single asyncio loop in the gateway, sleep-until-next-due exactly like `ScheduleService._arm_timer` (single re-armed task, ≤30s cap — **there is no timer heap to e…
- `§3.3` `docs/roadmap/plans/WATCHED-SOURCES.md:261` — | KNOWLEDGE-SYNTHESIS monitoring templates | **FED by this plan** | Its accumulate legs stop being hypothetical: sources[] = WatchedSource refs; its item-identity contract = §3.3 guids |
- `§3.3` `docs/roadmap/plans/WATCHED-SOURCES.md:495` — - [WS-4] DONE: **§3 feed kinds + §3.3 cross-source identity + §6.3 raw mode.**
- `§3.3` `docs/roadmap/plans/WATCHED-SOURCES.md:912` — `packs/connectors.py` is NOT a half-present connector concept — it is AGENT-PACKS §3.3's
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:29` — - **NEW-5** (core) — five-detector auto-source stack + declarative selector configs + preview → §2; outcome-driven escalating fetch under one budget at the `net.fetch` chokepoint → §2.3; guid-keyed novelty gating → §3.3; first-class watched feeds (HN Algolia, GitHub trending, RSS/CSV) with since-…
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:102` — enrichment: str            # "full" | "raw"  — §6.3, raw is guaranteed no-LLM
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:495` — - [WS-4] DONE: **§3 feed kinds + §3.3 cross-source identity + §6.3 raw mode.**
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:510` — surviving item's `also_seen_in`. §6.3 raw mode is kept by ABSENCE in both halves: `FeedItemGraph` has
- `§6.3` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:321` — **~5 sessions**, mapping 1:1 onto §11:
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
