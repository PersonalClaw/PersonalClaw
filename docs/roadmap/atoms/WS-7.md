<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WS-7` — Streams: SourceItemIngested/SourcePollCompleted/SourceQueryMatched events + saved queries + digest handoff

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WATCHED-SOURCES](../plans/WATCHED-SOURCES.md) (`WS`, plan status `done`) |
| **Atom table** | [`../atomic/WS.md`](../atomic/WS.md) |
| **Execution position** | #667 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§6.1 provenance + events (bus or interim JSONL spool); §6.2 digest as background one-shot; §6.4 filters-as-streams SavedSourceQuery + SourceQueryMatched; §8 fence_untrusted at LLM/digest boundary; §11 step 5 (events half)

## Done when

Engine emits SourceItemIngested per new item + SourcePollCompleted per poll onto the substrate bus (interim spool until it lands); a saved source query matches new items with zero tokens and emits SourceQueryMatched, a subscribed Trigger fires, and the morning-digest template produces ONE knowledge item + one notification through notification_allowed() (SC#10); an injection payload in a scraped page cannot steer a digest run, fenced at the LLM boundary (SC#8)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WS-2`](WS-2.md) | intra-repo | `done` |
| `EXT:AUTOMATION-SUBSTRATE:event bus for SourceItemIngested/SourcePollCompleted + morning-digest template + web_watch source_id consumption (interim JSONL spool until bus lands)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (15)

- `82580fc88` 2026-09-07 — docs(roadmap): flip PEP-18 and PEP-19 — the UI validation they were blocked on is driven
- `ba6a0a09a` 2026-08-25 — fix(proactive): close the four full-suite ratchets PA-2 moved
- `03d175b00` 2026-08-25 — fix(roadmap): WATCHED-SOURCES is 9/9 — mark the plan done and rail the class
- `f4aa88da0` 2026-08-25 — fix(tools): adjudicate an atom only on entries it is the subject of
- `3dbceeefe` 2026-08-25 — docs(roadmap): flip WS-7 done — the digest now has a caller
- `ea947cd7c` 2026-08-25 — feat(sources): WS-7 give the morning digest a bundled clock trigger
- `7d99ffa6f` 2026-08-25 — test(tools): pin every deferral marker, not just the one WS-7 needed
- `606612848` 2026-08-25 — fix(tools): scope audit_landed_atoms verdicts to the atom being adjudicated
- `0e2af518a` 2026-08-25 — docs(roadmap): record WS-7 PARTIAL — its own log says nothing calls the digest
- `f5e1e9bdf` 2026-08-25 — fix(watched-sources): WS-7 an EMPTY completion is the degraded case, not a silent empty digest
- `2321821a6` 2026-08-24 — fix(watched-sources): WS-7 use the canonical atomic_write and pin test home isolation
- `69fbe49d5` 2026-08-24 — docs(roadmap): WS-7 execution log
- `aef8d9e3d` 2026-08-24 — feat(watched-sources): WS-7 morning digest handoff fenced at the LLM boundary
- `ce85254b3` 2026-08-24 — feat(watched-sources): WS-7 saved source queries emit SourceQueryMatched and fire a Trigger
- `b27236370` 2026-08-24 — feat(watched-sources): WS-7 source stream events + saved queries + digest handoff

### Named by atom id in the roadmap prose (10)

- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1012` — - **DONE — `WS-7` streams: `SourceItemIngested`/`SourcePollCompleted`/`SourceQueryMatched` + saved
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1116` — - **[2026-08-25][`WS-7`] CORRECTION — this atom was briefly flipped `done` and that was WRONG.** A
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1117` — roadmap tick read the `**DONE — \`WS-7\` streams: …**` opener of the entry above, verified the four
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1131` — matters.** It bucketed `WS-7` as LANDED-BUT-GATED — correct — but justified it as *"the log names an
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1135` — `later` clause), moving `WS-7` to `PARTIAL_OR_UNMET` — *"the log names an unmet clause"* — which is
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1138` — - **[2026-08-25][`WS-7`] DONE — the digest has a caller.** The correction entry above reverted a premature
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1149` — **The fence reasoning is intact, which is why a clock trigger and not a template.** WS-7 deliberately
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1179` — morning-digest **template**"; WS-7 itself recorded the DEVIATION to a callable for the fence reason above.
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1187` — off-limits. Also still open from WS-7's own log and out of scope here: `budget_spent`/`requests_used`
- **source plan** `docs/roadmap/plans/WATCHED-SOURCES.md:1190` — **CHANGELOG: yes, now.** WS-7's original "CHANGELOG: no — nothing here has a user-reachable entry point

### Its plan's own record, via the scope references (15 line(s))

This atom's scope cites `§6.1`, `§6.2`, `§6.4`, `§8`, `§11`. Those tokens appear in its source plan at:

- `§6.1` `docs/roadmap/plans/WATCHED-SOURCES.md:111` — The **SourceEngine** (`knowledge/sources/engine.py`) is the missing poll loop the recon flagged (`detect_changes` has zero callers): a single asyncio loop in the gateway, sleep-until-next-due exactly like `ScheduleService._arm_timer` (single re-armed task, ≤30s cap — **there is no timer heap to e…
- `§6.1` `docs/roadmap/plans/WATCHED-SOURCES.md:241` — - **Fencing:** scraped/feed content is untrusted. `sanitize_html` default-on at extraction; `fence_untrusted(text, source=f"source:{source_id}")` wraps content at every **LLM boundary** (pipeline enrichment nodes, digest synthesis, triage) — becoming a disciplined new call site alongside inbox_se…
- `§6.1` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under
- `§6.1` `docs/roadmap/plans/WATCHED-SOURCES.md:1028` — 🔴 **`budget_spent` reads 0 for every shipped provider, and that is deliberate.** §6.1 specifies
- `§6.1` `docs/roadmap/plans/WATCHED-SOURCES.md:1053` — §6.1 says payload content never participates in pattern matching. A matcher that stripped a fence to
- `§6.2` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under
- `§6.2` `docs/roadmap/plans/WATCHED-SOURCES.md:1015` — queries + the trigger bridge) and `source_digest.py` (the §6.2 digest). `source_engine.py` gains two
- `§6.4` `docs/roadmap/plans/WATCHED-SOURCES.md:29` — - **NEW-5** (core) — five-detector auto-source stack + declarative selector configs + preview → §2; outcome-driven escalating fetch under one budget at the `net.fetch` chokepoint → §2.3; guid-keyed novelty gating → §3.3; first-class watched feeds (HN Algolia, GitHub trending, RSS/CSV) with since-…
- `§6.4` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under
- `§6.4` `docs/roadmap/plans/WATCHED-SOURCES.md:1036` — 🔴 **§6.4's literal trigger shape does not exist and was NOT built.** The plan says a Trigger
- `§8` `docs/roadmap/plans/WATCHED-SOURCES.md:56` — - **Fencing is caller responsibility.** `net.fetch`/`web_fetch` output is NOT fenced at the fetch layer; today's `fence_untrusted` call sites are exactly inbox_service ×2, knowledge/insights.py, skills/proposals.py (+ one inline in after_turn_review). The source pipeline becomes a disciplined new…
- `§8` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:321` — **~5 sessions**, mapping 1:1 onto §11:
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:534` — - [WS-9] DONE: **§2.4 create-flow UI + §6.3 'no AI' chip + §12 health rollups + §11 step 5's UI
- `§11` `docs/roadmap/plans/WATCHED-SOURCES.md:1013` — queries + digest handoff (§6.1, §6.2, §6.4, §8, §11 step 5's events half).** Three new modules under

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
