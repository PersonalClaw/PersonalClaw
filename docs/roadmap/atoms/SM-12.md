<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SM-12` — MCP tool-name wire fidelity: preserve exact names across the chat turn boundary (T00)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SESSION-MANAGEMENT](../plans/SESSION-MANAGEMENT.md) (`SM`, plan status `done`) |
| **Atom table** | [`../atomic/SM.md`](../atomic/SM.md) |
| **Execution position** | #543 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Research draft T00 (homed here per rev-18). Tool names are mangled crossing the wire (sanitization/aliasing mismatch), breaking chat turns that reference MCP tools.

## Done when

A wire map documents every transform a tool name undergoes end-to-end; round-trip property test asserts name-in == name-out across the chat turn boundary for the full registered-tool census; any lossy transform is replaced by a reversible one; the failing turn shape from the draft reproduces green.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (3)

- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `62482defc` 2026-09-02 — fix(native): tool-name wire fidelity — census rail + loud collisions + wire map (SM-12)
- `c0d61a28d` 2026-09-02 — docs(roadmap): rev-18 ground-truth reconciliation — EXT-dep audit applied, verified flips, 8 new atoms

### Named by atom id in the roadmap prose (1)

- **source plan** `docs/roadmap/plans/SESSION-MANAGEMENT.md:115` — **2026-09-02 — SM-12 done (PR #2309).** Tool-name wire fidelity: `build_sanitized_index` pure function shared by runtime + census rail; loud collision warnings; wire map at `docs/architecture/tool-name-wire.md`; census rail asserts the shipped tool surface is collision-free and round-trips. 7 new…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
