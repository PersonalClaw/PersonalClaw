<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2KNO-5` — Contradiction pass + retrieval polish: contradiction.py, session_brief.py, fenced_sources, conflict UI

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS](../plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md) (`WF2KNO`, plan status `done`) |
| **Atom table** | [`../atomic/WF2KNO.md`](../atomic/WF2KNO.md) |
| **Execution position** | #578 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§3.2 Contradiction Flagging at Persist Time, §5.2 Provenance/Citation/Fencing, §5.3 Push-Based Session Brief; Session 5

## Done when

knowledge/contradiction.py + session_brief.py: persist-time deterministic conflict pass with typed-edge writes, fenced_sources binding filter, read-only conflict/relations routes, ConflictPanel Knowledge view, config knobs; a seeded same-subject+predicate conflict is flagged at persist with both claims retained under the precedence ladder; Session Brief injected into runs; split-brain knowledge_db_path fix; validated live

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2KNO-1`](WF2KNO-1.md) | intra-repo | `done` |
| [`WF2KNO-2`](WF2KNO-2.md) | intra-repo | `done` |

### Unblocks (2)

[`WF2KNO-10`](WF2KNO-10.md) (`done`), [`WF2KNO-6`](WF2KNO-6.md) (`done`)

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-08

**Checked by:** audit cycle 6 (WF2KNO) — observed

**Code evidence:**

- src/personalclaw/knowledge/contradiction.py exists on disk
- tests/test_knowledge_contradiction.py passes (part of the 210-passed run)

**Notes:** PARTIAL: a contradiction pass needs two claims that actually conflict AND a model to judge them. Neither is present.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (17 line(s))

This atom's scope cites `§3.2`, `§5.2`, `§5.3`, `Session 5`. Those tokens appear in its source plan at:

- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:32` — - **KNOW-R6** — contradiction flagging at persist time + typed item relations with edge provenance (§3.2)
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:190` — **Retrieval mechanics (KNOW-R8e + amendments):** always-include the matching `kind: overview` item when one exists; one-hop expansion over `confidence ≥ 0.7` typed relations (§3.2) with graph hits enriching but never displacing direct hits; explicit degradation ladder vector → FTS → substring wit…
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:239` — 2. **`knowledge-lint`** — semantic LLM pass cadenced by **mutation count** (`lint_every_n_persists`, default ~12 — not wall clock): contradictions (§3.2 report), staleness, gaps + suggested sources. Rule: health runs first ("linting an empty item wastes tokens").
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:340` — 1. **Lineage caps:** every synthesized item carries `parent_ids` (= `derived_from` relations, §3.2) back to its inputs; inputs get a `reflection_count` increment with eligibility `reflection_count < 3` for future passes — without this the watcher re-synthesizes its own output and drift compounds.
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:506` — "Ingest a 2-hour meeting transcript and populate knowledge + tasks in one pass", composed ONLY of existing node kinds: a cheap classifier stage examines the input and selects which extraction lenses apply (bias: when uncertain, include more lenses — the specialists do final filtering); a `paralle…
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:526` — - `knowledge/store.py` `_migrate`: new nullable `kind` column + indexed `logical_key` derived column + `last_verified`, `expires_at` columns; new `item_relations` table (§3.2). All additive; existing rows unaffected.
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:531` — - Knowledge UI: contradiction report surface + unresolved-wikilink affordance (§3.2, §3.4).
- `§3.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:704` — Contradiction-at-persist (§3.2) and typed-edge inference are session 38's.
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:36` — - **KNOW-R10** — retrieval provenance journaling, citation enforcement, untrusted-content fencing (§5.2)
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:154` — | `citations` | [string] | Ids into retrieval traces / source items (§5.2). The provider ENFORCES "no synthesized item without citations" for `kind: insight|report` unless `unsourced: true` is set explicitly. |
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:190` — **Retrieval mechanics (KNOW-R8e + amendments):** always-include the matching `kind: overview` item when one exists; one-hop expansion over `confidence ≥ 0.7` typed relations (§3.2) with graph hits enriching but never displacing direct hits; explicit degradation ladder vector → FTS → substring wit…
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:554` — - `security.fence_untrusted` / `sdk.security` (existing) for §5.2.
- `§5.2` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:562` — - **Prompt injection via retrieved knowledge** → mandatory `<untrusted_content>` fencing at interpolation + provider-enforced citations (§5.2).
- `§5.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:38` — - **KNOW-R12** — push-based retrieval: project Session Brief for workflow RUNS, `read_when` triggers, coverage-gap events (§5.3)
- `§5.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:61` — 2. Knowledge is **never ambiently injected into chat** (existing deliberate invariant — it enters chat only via the composer @-picker or agent `knowledge_search` tool). §5.3's Session Brief injects into **workflow runs**, not chat context, preserving this.
- `§5.3` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:171` — **Decision-shaped payloads (KNOW-R13):** `kind: decision` accepts `{question, options, choice, rationale, rejected_alternatives, constraints}` rendered into a canonical body — journals keep the "what", decisions keep the "why" that consolidation otherwise drops. A bundled decision-log pattern: ea…
- `Session 5` `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:543` — - **Session 5 — Contradiction + retrieval polish:** persist-time deterministic + fast-model conflict pass, typed-edge background inference (memoized, capped), contradiction report UI surface; Session Brief + coverage-gap loop; fencing filter (`fenced_sources`).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
