<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SV-10` — Self-QA evidence bundle capture + optional fix-branch + end-to-end validation

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SELF-VERIFICATION](../plans/SELF-VERIFICATION.md) (`SV`, plan status `done`) |
| **Atom table** | [`../atomic/SV.md`](../atomic/SV.md) |
| **Execution position** | #553 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

§3.3 (ffmpeg capture + contact sheet + GIF + SHA256 manifest + Artifact registration + required_artifacts gate), §3.2 step 6 (optional fix-branch stage), §7 Session 6 (as-a-user validation)

## Done when

a failing scenario produces screenshots+MP4+contact-sheet+GIF+logs under one SHA256'd manifest registered as a single Artifact, and the required_artifacts gate blocks completion when any declared proof is missing (Criterion #7); with fix_branch_enabled a confirmed finding yields a never-merged pclaw/selfqa-<sha8> branch linked in the Task (Criterion #8)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`SV-9`](SV-9.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 47 (SV) — the harness was RUN and falsified against itself

**Code evidence:**

- the clause's central mechanism is the one already confirmed elsewhere in this campaign: the required_artifacts gate blocks completion when a declared proof is missing, which is what stops an evidence bundle from being 'mostly there'
- the never-merged fix branch is enforced at the action (a LOCAL branch, never pushed) rather than by a reviewer noticing
- python -m harness validate 15/15 specs; harness replay 4 scenarios within baseline; harness scan 2 advisory warnings

**Driven in the UI:** Not driven: a failing scenario producing screenshots, an MP4, a contact sheet, a GIF and logs under one SHA256 manifest needs a real Self-QA fire with a bound model and a browser-driving MCP.

**Notes:** Partial for the drive. The SHA256 manifest registering the whole bundle as a SINGLE Artifact is the part that makes the gate meaningful: proof that arrives as five unrelated files can be partially missing without anything noticing, while one manifest either verifies or does not.

## Recorded history

### Commits (2)

- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `2905bb3e8` 2026-09-04 — feat(selfqa): add evidence-bundle and fix-branch mechanics (SV-10)

### Named by atom id in the roadmap prose (2)

- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:612` — ## Execution log — SV-10 (Self-QA evidence bundle capture + optional fix-branch + end-to-end validation)
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:614` — - [2026-09-04][SV-10] LANDED on main; atom flipped ✅. Merged via core PR #2410 (merge commit 2905bb3e8). `selfqa/evidence.py` computes each bundle file's size and SHA256 from the bytes on disk into a schema-versioned `manifest.json` and `register_bundle` composes the run's proof into ONE content-…

### Its plan's own record, via the scope references (14 line(s))

This atom's scope cites `§3.3`, `§3.2`, `§7`, `Session 6`. Those tokens appear in its source plan at:

- `§3.3` `docs/roadmap/plans/SELF-VERIFICATION.md:164` — 4. **`evidence` (`required_artifacts` gate)** — the node cannot complete until the declared proof globs exist (WF2-R3, engine-enforced, independent of agent self-report): `screenshots/*.png`, `recording.mp4`, `manifest.json`. §3.3 supplies the capture mechanics.
- `§3.3` `docs/roadmap/plans/SELF-VERIFICATION.md:209` — | Evidence bundle = Artifact manifest + Proof section + needs-input carrying bundles | WORK-CONTAINERS **WORK-R4** | §3.3 produces conforming bundles; rendering stays in WORK-CONTAINERS |
- `§3.3` `docs/roadmap/plans/SELF-VERIFICATION.md:210` — | Failure capsules (repro + failure_signature + forbidden_success_modes) + capsule replay as lesson decay | LEARNING-FLYWHEEL **LEARN-R8** (§3.3d) | §3.2 step 5 is a capsule *producer*; lifecycle/replay stays in the flywheel |
- `§3.3` `docs/roadmap/plans/SELF-VERIFICATION.md:217` — **Dependency edges:** §1 depends on nothing. §2 depends on nothing to build, but its `workflow-journal-projection` scenario is recorded against WF2 Slices 1-2 output as those land (co-scheduled, Wave 1). §3 depends on WF2 Slices 0-5; §3.3's Proof rendering depends on WORK-CONTAINERS (graceful wit…
- `§3.3` `docs/roadmap/plans/SELF-VERIFICATION.md:230` — - **Session 6 — evidence + fix branch + validation (§3.3, §3.2 step 6)** *(Wave 2)*: ffmpeg capture + contact sheet + GIF + SHA256 manifest + Artifact registration + `required_artifacts` gate; optional fix-branch stage on the worktree machinery; end-to-end as-a-user validation — real commits, rea…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:208` — | `required_artifacts` proof gates + engine-owned completion | WORKFLOWS-V2 **WF2-R3** | §3.2's evidence gate *uses* it verbatim; no parallel gate mechanism |
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:210` — | Failure capsules (repro + failure_signature + forbidden_success_modes) + capsule replay as lesson decay | LEARNING-FLYWHEEL **LEARN-R8** (§3.3d) | §3.2 step 5 is a capsule *producer*; lifecycle/replay stays in the flywheel |
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:213` — | Two-weight run records / skip-rationale ledger rows; triage-first convention | AUTOMATION-SUBSTRATE §1.3 / WF2-R15 + UNIVERSAL-PLANNING impact-triage | §3.2 step 1 consumes the conventions |
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:229` — - **Session 5 — Self-QA companion core (§3.1, §3.2)** *(Wave 2)*: commit-watch cron script + state file; the `self-qa` bundled template (triage prompt with skip-rationale contract, scenario-gen prompt encoding the deep-as-a-user method, execute stage with DevTools MCP binding, findings→Inbox/Task…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:230` — - **Session 6 — evidence + fix branch + validation (§3.3, §3.2 step 6)** *(Wave 2)*: ffmpeg capture + contact sheet + GIF + SHA256 manifest + Artifact registration + `required_artifacts` gate; optional fix-branch stage on the worktree machinery; end-to-end as-a-user validation — real commits, rea…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:287` — - [2026-08-23][SV-9] DONE (clauses 1, 2, 4 met; clause 3 PARTIAL — see the note below): Self-QA Companion core (§3.1, §3.2 steps 1-5, §5). `src/personalclaw/selfqa/` — `triage.py` (deterministic path classifier → `user`/`test`/`none` + a one-line rationale that is never empty), `ledger.py` (`reco…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:291` — - [2026-08-23][SV-9] DEVIATION: §3.2 step 1 specifies triage as an **`infer` node (cheap tier)**; it ships as a deterministic path classifier behind an `action` node. Two reasons, both load-bearing. Cost: a path classifier is zero tokens, so the watcher can run on a 5-minute interval without a bu…
- `§3.2` `docs/roadmap/plans/SELF-VERIFICATION.md:298` — ## Execution log — `SV-9` (Self-QA Companion core: §3.1 commit-watch, §3.2 steps 1-5, §5 config) — **PARTIAL**, atom stays `todo`
- `Session 6` `docs/roadmap/plans/SELF-VERIFICATION.md:230` — - **Session 6 — evidence + fix branch + validation (§3.3, §3.2 step 6)** *(Wave 2)*: ffmpeg capture + contact sheet + GIF + SHA256 manifest + Artifact registration + `required_artifacts` gate; optional fix-branch stage on the worktree machinery; end-to-end as-a-user validation — real commits, rea…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
