<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `CE-8` — Bring Slack to the full vendor-completeness pattern: register the inbox MessageSourceProvider, move non-seam UI behind app ui block, scrub core vendor-name residue

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [CHANNEL-EXPANSION](../plans/CHANNEL-EXPANSION.md) (`CE`, plan status `in_progress`) |
| **Atom table** | [`../atomic/CE.md`](../atomic/CE.md) |
| **Execution position** | #120 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##1009,#1032 (parts 2-4) + apps main (part 1 adapter) + unblocked by INU-8 #1090 |

## Intent — what this is for

Amendment 2026-07-26 — T7.4 (apps slack-channel + core inbox_providers docstrings)

## Done when

slack-channel app.json registers >=2 providers incl. an inbox MessageSourceProvider over the existing runtime client; Slack messages flow through the generic inbox source seam with no core slack string (inbox_providers docstring + native_source.py comment residue scrubbed); non-seam Slack surface lives behind the app's own ui block; boundary tests green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`CE-2`](CE-2.md) | intra-repo | `done` |

### Unblocks (2)

[`CE-10`](CE-10.md) (`done`), [`CE-7`](CE-7.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 50 (CE) — trust core read line by line; the adoption rail run and falsified; four app suites run

**Code evidence:**

- 🔑 THE MANIFEST CENSUS IS THREE PROVIDERS, NOT TWO: slack-channel declares the singular `provider` of type `channel` plus `providers: [inbox, trigger_source]`, so the '>=2 providers incl. an inbox MessageSourceProvider' clause is met with room to spare — and it is the ONLY one of the four with an inbox source, which is exactly what makes it the pattern the other three still owe
- 🔑 THE RESIDUE SCRUB IS OBSERVED, NOT CLAIMED: zero occurrences of the vendor name anywhere under core's inbox module, so the generic source seam carries no trace of the vendor that first drove it
- the inbox source is vendor-neutral by test rather than by intention: `test_source_name_is_the_vendor_neutral_key`, alongside checkpoint tests that pin non-redelivery, cursor advance on skipped messages, and checkpoint RETENTION when a channel errors — the failure mode where an error would silently skip messages forward
- `test_token_falls_back_to_the_shared_credential_store` — the vendor token resolves through the shared credential store rather than the app's own settings file, which is the repo's stated contract for secrets
- core trust 106/106 (channel_trust + api + pairing redemption + conformance kit); app suites telegram 150, discord 237, email 346, slack 566+1 xfailed = 1299+1; CE-9 coordination rails 13/13

**Driven in the UI:** Not driven: the inbox source needs a real workspace to poll. Validated at the manifest, the residue sweep and the app's own 566-test suite.

**Notes:** This atom is the one that makes CE-2's contradiction legible: Slack got the FULL vendor-completeness treatment (inbox source, trigger source, UI behind its own block, core residue scrubbed) while the trust-seam migration it depends on never landed. The vendor breadth advanced; the trust depth did not. Filed as issue #2803.

## Recorded history

### Commits (8)

- `9a8325805` 2026-09-06 — feat(ce10): the completeness advisory grows its trigger-source arm
- `281d693b1` 2026-09-06 — docs(roadmap): execute the 2026-08-28 cycle ruling, and mint the atom for a seam nobody adopted
- `ebeff5d29` 2026-08-11 — docs(roadmap): CE-8 done — part 1 unblocked by INU-8 and verified with the real app (#1095)
- `92420d541` 2026-08-11 — feat(inbox): INU-8 resolve app-contributed inbox sources + make the #47 guard bidirectional (#1090)
- `0a40c2ade` 2026-08-11 — docs(roadmap): add INU-8 — inbox provider-seam resolution, with design + implementation plan (#1089)
- `c70eb232f` 2026-08-10 — refactor(inbox): CE-8 scrub the last vendor names from the inbox source seam (#1032)
- `c9b44f774` 2026-08-10 — refactor(inbox): CE-8 partial — scrub vendor names from the inbox seam (#1009)
- `64aa3088e` 2026-08-09 — chore(roadmap): reconcile dag.json — CE-2/SV-6/WF2WOR-8 merged → done (#932)

### Named by atom id in the roadmap prose (17)

- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:369` — notification path until that lands. CE-7 follows CE-6 + CE-8.
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:370` — - [2026-08-10][CE-8 / T7.4] **BLOCKED (E1 — premise mismatch on the discovery seam).** Parts 2-4 of the done-when are done and shippable; part 1 ("registers an inbox `MessageSourceProvider` **over the existing runtime client**" that Slack messages actually **flow through**) cannot be met without …
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:373` — - **What would unblock it (owner decision — a contract change, not a fix inside CE-8).** Either (a) core teaches the inbox seam to resolve app-contributed `inbox` providers from the app registry via each manifest's `implementation` factory (the same resolution every other app provider type alread…
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:375` — - [2026-08-10][CE-8 / T7.4] **PARTIAL — parts 2-4 SHIPPED; part 1 stands BLOCKED (E1), unchanged.** A second pass re-derived the earlier BLOCKED finding from scratch and reached the same conclusion, so the blocker is confirmed twice by independent routes. What changed is the decision about the bu…
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:377` — **NOT a slack-only gap — `mail-inbox` has the same shape.** `mail-inbox/app.json` declares `{"type":"inbox","implementation":"mail_inbox_runtime.provider:create_provider"}` and is subject to the identical dead end, so EIAT-2's provider is equally unreachable. This is a seam defect affecting every…
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:379` — - **Core** (`refactor(inbox): CE-8 scrub the last vendor names from the inbox source seam`): the two sites the atom text names were ALREADY scrubbed by #1009 — a stale premise in the atom, recorded here. The real remaining defect was the site the atom did NOT name: `gateway.py:2169`'s comment ass…
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:380` — - **Apps** (`feat(slack): CE-8 register an inbox message source alongside the channel`): new `slack_runtime/inbox_source.py` implementing all six `MessageSourceProvider` members over the EXISTING `RealSlackClient` (an adapter, not a second client), importing core only via `personalclaw.sdk.inbox`…
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:394` — - **[2026-08-11][CE-8 / T7.4] DONE — part 1 unblocked and verified; the atom is complete.** The
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:398` — and gave `get_default_provider` an app-instance precedence step. This entry closes CE-8 rather than
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:412` — by the time CE-8 reads `done` on main, the seam it depends on is on main too. Verified against the
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:453` — `providers`), `slack-channel` is complete (CE-8), `mail-inbox` is not a channel. So the advisory
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:475` — *may* contribute sources, but none do today by decision") — CE-8 shipped one, and the doctrine now
- **source plan** `docs/roadmap/plans/CHANNEL-EXPANSION.md:572` — `_SampleSource`. `CE-8` brought Slack to "full vendor-completeness" as `channel` + `inbox`, which was the
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:542` — CE-8 and EIAT-2 both dead-ended on the same seam and no atom owned the fix; CE-8's own log states it
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:555` — **Two consumers unblocked on completion,** with app-side work already written and inert: CE-8 part 1
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:600` — **Hand-off:** CE-8 part 1 and EIAT-2 are now unblocked — their app-side adapters were already written
- **source plan** `docs/roadmap/plans/INBOX-NOTIFICATIONS-UNIFICATION.md:605` — CE-8/EIAT-2 — a follow-up row for whoever lands CE-8 part 1.

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `T7.4`. Those tokens appear in its source plan at:

- `T7.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:220` — | T7.4 | Slack to full pattern: add the `inbox` provider registration (`MessageSourceProvider` over the existing runtime client) to `providers[]`; move any non-seam Slack-specific surface behind the app's own `ui` block; scrub vendor-name residue from core seam comments | apps repo `slack-channel…
- `T7.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:370` — - [2026-08-10][CE-8 / T7.4] **BLOCKED (E1 — premise mismatch on the discovery seam).** Parts 2-4 of the done-when are done and shippable; part 1 ("registers an inbox `MessageSourceProvider` **over the existing runtime client**" that Slack messages actually **flow through**) cannot be met without …
- `T7.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:375` — - [2026-08-10][CE-8 / T7.4] **PARTIAL — parts 2-4 SHIPPED; part 1 stands BLOCKED (E1), unchanged.** A second pass re-derived the earlier BLOCKED finding from scratch and reached the same conclusion, so the blocker is confirmed twice by independent routes. What changed is the decision about the bu…
- `T7.4` `docs/roadmap/plans/CHANNEL-EXPANSION.md:394` — - **[2026-08-11][CE-8 / T7.4] DONE — part 1 unblocked and verified; the atom is complete.** The

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
