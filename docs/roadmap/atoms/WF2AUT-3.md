<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2AUT-3` — Records, health & safety: two-weight run records + typed outcomes + parking/autopause + budgets/caps/retention + capability allowlists + secrets

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-AUTOMATION-SUBSTRATE](../plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md) (`WF2AUT`, plan status `done`) |
| **Atom table** | [`../atomic/WF2AUT.md`](../atomic/WF2AUT.md) |
| **Execution position** | #562 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##250-#543 |

## Intent — what this is for

§7 steps 4 & 6; §1.3 record weights + typed outcome vocabulary + materiality; §3.6 budgets/triage; §3.7 health/typed-exits/parking; §1.4 decisions 7/11/12 (frozen capabilities, kill switch, PathGuard, {{secret:KEY}}); crit 3/8/11

## Done when

ledger-only vs full runs with materiality classification; every suppressed fire is a typed ledger row (zero silent drops under 24h storm); autopause after 5 true failures; budget/rate/spend caps metered + fair per-job retention; frozen action-set fence + global kill switch + PathGuard enforced; {{secret:KEY}} never resolved in store/journal/ledger

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2AUT-2`](WF2AUT-2.md) | intra-repo | `done` |

### Unblocks (3)

[`WF2AUT-11`](WF2AUT-11.md) (`done`), [`WF2AUT-5`](WF2AUT-5.md) (`done`), [`WF2AUT-9`](WF2AUT-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 34 (WF2AUT) — DRIVEN in a real browser

**Code evidence:**

- 🔑🔴 THE KILL SWITCH WAS MEASURED BROKEN BEFORE IT WAS FIXED, AND THIS IS THE WORST INSTANCE OF THIS CAMPAIGN'S CLASS. firepath.py:300-305: 'MEASURED: `personalclaw incident on` did NOT stop a clock trigger. The CLI calls it "Suspend/resume all unattended work" … but the unified engine — THE SOLE PATH THAT FIRES CLOCK TRIGGERS SINCE S100 — never read the flag. Driven before writing this: switch thrown, tick() still returned fires: [clock:nightly] with outcome=ran. So THE ONE CONTROL AN OPERATOR REACHES FOR DURING AN INCIDENT WAS THE ONE THAT KEPT RUNNING UNATTENDED WORK, WHILE REPORTING ITSELF ACTIVE'
- 🔑 ITS PLACEMENT IS ARGUED TWICE. Ordered FIRST, ahead of the injection screen, because 'a gate ordered after screen would make "is the payload clean" a precondition for honouring a kill switch'. And centralised rather than per-loop, because there are three unattended entry points (clock loop, file-watch poll, reaper re-dispatch) and 'only the file-watch one checked — a per-loop check is a control that must be re-added correctly at every future call site, WHICH IS PRECISELY HOW THIS GAP OPENED'
- it also declines to second-guess its dependency: incident_active() is deliberately fail-OPEN ('an unreadable flag file must not halt all automation on a filesystem hiccup') and this gate inherits that rather than hardening it locally
- 🔑🔴 PATHGUARD WAS ALSO MEASURED BROKEN FIRST, WITH A TABLE. pathguard.py:1-18: `paths` had been a first-class CAPABILITY_KEY since S69, fail-closed, and rendered as a fence in the UI — but capability_allows compared it with _matches_entry, 'STRING MATCHING, built for tool names like mcp__github__*. PATHS ARE NOT STRINGS FOR SECURITY PURPOSES.' Driven against the real function with allowlist ['/Users/me/notes/*']: it ALLOWED '/Users/me/notes/../../.ssh/id_rsa' and '/Users/me/notes/../.aws/credentials'
- the table also catches a near-miss that would bite a different allowlist: with ['/Users/me/notes'], denying '/Users/me/notesEVIL' was 'correct only by accident of this entry'
- the fence set is layered rather than single: handoff.py:39 names 'capability fence, PathGuard and the denylist' as the fail-closed controls together
- calendar.py:485/:517 states the inverse rule for a third-party gate — 'a broken third-party gate must not become a global kill switch' — so a provider raising degrades rather than halting everything
- 1766 tests pass across the trigger suites — the largest suite count of any plan audited

**Notes:** Two measured-before-fix defects in one atom, and both are the shape this audit keeps finding: a control that was present, fail-closed, UI-rendered, and did not do its job. PathGuard is the FOURTH independent occurrence of post-realpath containment in this codebase (the skill-resource reader, voice profiles, the uploads store, now this) — and the paths it was letting through are the exact two families this session's own constraints forbid reading, which is a fair measure of the stake.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (40 line(s))

This atom's scope cites `§7`, `§1.3`, `§3.6`, `§3.7`, `§1.4`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:19` — on LOOPS-EVOLUTION Phase 4 (note §3/§7 say `kind:idle` itself may ship EARLY for user automations —
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:49` — - **AUTO-R15** — substrate integrity tooling (OS-scheduler gate, doctor, lenient parse, audit) → §4.1, §7
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:53` — - **AUTO-R19** — proactive `pulse` kind + standing delegations (Phase 2, deferred) → §1.2, §7 step 10
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:54` — - **AUTO-R20** — opt-in `observe` screen kind (Phase 2, deferred, app-delivered) → §1.2, §7 step 10
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:73` — - **A unified triggers facade already exists.** `dashboard/handlers/triggers.py` serves `/api/triggers` over the three stores with namespaced ids `schedule:<id>` / `lifecycle:<id>` / `event:<id>`. Store unification (§7 step 2) starts from *that* facade — the API shape survives, the backing stores…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:74` — - **Only 7 of the 15 `HOOK_EVENTS` actually fire today**: SessionStart, AgentSpawn, UserPromptSubmit, PreToolUse, PostToolUse, Stop, Error (chat_runner.py L1441-2558 + `fire_tool_hooks`). PreResponse, PostResponse, MemoryWrite, ContextCompact, SubagentSpawn, TaskComplete, ApprovalRequest, Session…
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:77` — - **Heartbeat is a hard-coded 60s loop** with tick-modulo sub-tasks (FTS rebuild every 15 ticks, daily prunes at 1440) — the interval is not config-exposed; changing it would silently shift the modulo cadences. Conversion (§7 step 5) makes each sub-task an explicit clock trigger instead.
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:134` — | `clock` | tagged union `{kind:'cron', expr}` \| `{kind:'at', at, delete_after_run: true}` \| `{kind:'sequence', at: [datetimes]}` (R12); tz (IANA), deterministic jitter (kept verbatim from schedule.py), `strict`, `skip_dates` | Everything `schedule.py` has today. `at` one-shots default `delete_…
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:36` — - **AUTO-R2** — typed fire/run outcome vocabulary + overlap policy → §1.3, §5.2
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:182` — 3. **Two record weights + typed outcomes** — see §1.3. This replaces rev-1's bare "ledger-only fire" with the full outcome vocabulary; the flagship surface stays honest because nothing is dropped silently.
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:220` — | `schedule_history.py` ScheduleRun | **ABSORBED** | Its record shape (honest `launched≠succeeded`, dry-run replay, JSONL caps 100/job + index) becomes the ledger-only fire record, extended with the §1.3 typed-outcome vocabulary; full runs use the v2 run layout. `last_run_status()` semantics (UI …
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:243` — - Fire path (order matters): **injection screen (4a) → gates (debounce/quiet/cooldown/condition) → budget check pre-claim (R5, fail-closed) → overlap claim lock (R2) → yield/resource-slot check (R9) → fence payload → capability filter (R3) → resolve def / resume target → create run (full or ledge…
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:349` — Reverse-chron feed of runs/fires across all triggers with the §1.3 **typed outcome vocabulary** — every skip/defer/refusal visible with its one-line reason; no-op rows auto-archived out of the default view; All/Failed/Needs-input filters; needs-input pinned to top. Productive rows carry the "writ…
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:377` — 4. **Run-record integration**: two-weight records with the full §1.3 outcome vocabulary + materiality classification; typed exits + health rollups + parking (§3.7); generalized autopause; budget gates + background model tier (§3.6, minus triage).
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:598` — nothing anywhere recording that it did not run**. That is exactly the silent drop §1.3 bans, in
- `§1.3` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1681` — `skip_dates`, `timezone`, `strict_schedule`, `delete_after_run` — §1.3's quietly-losable class,
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:39` — - **AUTO-R5** — cost/budget gates, background model tier, fire→spawn triage → §1.2 gates, §3.6
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:377` — 4. **Run-record integration**: two-weight records with the full §1.3 outcome vocabulary + materiality classification; typed exits + health rollups + parking (§3.7); generalized autopause; budget gates + background model tier (§3.6, minus triage).
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:459` — - **Week-grid view:** the Automations page (§5.1) gains a Week tab — a 7×24 grid plotting each enabled clock trigger's fires for the coming week from the recurrence engine (which §3.6 already requires for the next-fires calendar preview — same computation, richer render). Quiet windows render as …
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:653` — triage (§3.6), and the migration (66, which must use S62's `LEGACY_FIELD_MAP`).
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:700` — service), foreground yield and resource slots (§3.5), budgets and triage (§3.6), and the migration
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1386` — budget returns `claim=None` and `"claim" not in passed`. An UNREADABLE budget refuses too — §3.6 is
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:3700` — checks" as fail-open, but §3.6 says *"the budget check is fail-closed — an unreadable budget is not an
- `§3.6` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:3701` — unlimited one"*, and the code follows §3.6. Those are two different questions wearing one word: the
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:41` — - **AUTO-R7** — health rollups, typed run exits, parking (no circuit breakers) → §1.1, §3.7
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:377` — 4. **Run-record integration**: two-weight records with the full §1.3 outcome vocabulary + materiality classification; typed exits + health rollups + parking (§3.7); generalized autopause; budget gates + background model tier (§3.6, minus triage).
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1756` — and the allowlist excludes every field §3.7's autopause thresholds on (`run_count`,
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:2177` — - **`tools.update`'s allowlist still protects the health fields** §3.7 autopauses on, so the API
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:4095` — ### S139 — criterion 3 was dead in three layers (§3.7) — DONE
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:4721` — ### S155 — three success statuses recorded as failures, and the no-op outcome nothing wrote (§1.3 / §3.7)
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:5014` — ### S159 — parking was a one-way door (§3.7 / decision 9)
- `§3.7` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:5021` — - **`ExitType.PARTIAL` has no producer.** §3.7 promises *"auto-refire on partial"*, and `PARTIAL` is
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:37` — - **AUTO-R3** — creation-time capability allowlist / write scopes → §1.4 decision 7
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:38` — - **AUTO-R4** — hardened `fence_untrusted` (screen, strip, provenance, typed extraction) → §1.4 decision 4
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:48` — - **AUTO-R14** — `{{secret:KEY}}` server-side secret templating → §1.4 decision 12
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:51` — - **AUTO-R17** — composite trigger shape, substrate recursion guard, IO-only hook doctrine → §1.2, §1.4 decisions 5/10
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:52` — - **AUTO-R18** — outbound delivery contract (stable event-id, statusUrl, destination formatting) + Run Now/Duplicate → §1.4 decision 13, §5.1
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:458` — - `gates.duty_gate: {provider: str, config: dict}` — a new provider seam `DutyGateProvider{name, async def on_duty(now, trigger_ctx) -> DutyVerdict{on_duty: bool, reason: str}}` in a `duty_gates/` registry (flat-dict, action_providers shape; app-contributed via `provider: {type: "duty_gate", …}` …
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1030` — an explicit `on_duty=False` suppresses. §1.4 classifies it fail-open because it calls OUT to a
- `§1.4` `docs/roadmap/plans/WORKFLOWS-V2-AUTOMATION-SUBSTRATE.md:1391` — **🔴 A DEFECT FOUND BY DRIVING, NOT READING: `evaluate_duty` IS ASYNC.** §1.4 makes the duty gate

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
