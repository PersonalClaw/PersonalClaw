<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `LMMV-1` — Shared multi-layout downloaded/delete probe + cleanup-candidate detection (local_models/layouts.py)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [LOCAL-MODEL-MANAGER-V2](../plans/LOCAL-MODEL-MANAGER-V2.md) (`LMMV`, plan status `in_progress`) |
| **Atom table** | [`../atomic/LMMV.md`](../atomic/LMMV.md) |
| **Execution position** | #320 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##120 |

## Intent — what this is for

§4.4 One multi-layout downloaded/delete probe; §4.2 cleanup-candidate detection helpers

## Done when

local_models/layouts.py exports is_downloaded / delete_all_layouts (greedy, all copies) / downloaded_layouts / candidate_paths / reclaimable_bytes / hf_repo_dirname probing save()/HF models-- snapshot/direct-file layouts, with partials excluded from 'downloaded'; tests/test_local_model_layouts.py (47 cases) green; is_downloaded wired into dashboard/model_downloads.py job runner as the asymmetric 'second opinion'. NOTE HALF-INERT: only is_downloaded has a production caller — delete/cleanup helpers remain unwired (finished by LMMV-3).

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`LMMV-2`](LMMV-2.md) (`done`), [`LMMV-3`](LMMV-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (11 line(s))

This atom's scope cites `§4.4`, `§4.2`. Those tokens appear in its source plan at:

- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:12` — **Status:** IN PROGRESS — one slice landed 2026-07-30 (§4.4 the shared multi-layout probe
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:45` — - **Detection/delete class** (ST checked only the `model.save()` layout and missed `models--…`; a destructive test deleted the user's real bound L6 model) → a **declarative catalog with expected sizes and a truncation/weights-missing detector**, plus one shared multi-layout probe helper (§2.3, §4…
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:121` — - **`ABC` default implementation:** `LocalModelProvider.list_models()` gains a protected helper `_models_from_catalog()` that reads the file, filters `platforms` against the host, maps entries to `LocalModel`, and computes `downloaded` via the shared multi-layout probe (§4.4). Providers with dyna…
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:152` — - Install is **resumable/idempotent**: each step existence-checks before doing work (venv exists → skip; package importable → skip; weights pass the §4.4 probe → skip), so a killed install re-runs from where it died.
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:572` — - 2026-07-30 — **PARTIAL (§4.4 the shared layout probe + §4.2 cleanup candidates).
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:575` — **DEVIATION — why §4.4 and not the plan's front half.** This plan is ~12 sections spanning a
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:577` — an HF token cascade, selftests and a memory widget. §4.4 was taken because it is (a) the one
- `§4.4` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:639` — empty/absent = all hosts), computes `downloaded` via the §4.4 probe, and flags
- `§4.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:13` — `local_models/layouts.py` + §4.2 cleanup-candidate detection, PR #120; DEVIATION recorded in the
- `§4.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:19` — frees" bug the log claims to fix is still live; §4.2's `cleanup-candidates`/`cleanup` endpoints and
- `§4.2` `docs/roadmap/plans/LOCAL-MODEL-MANAGER-V2.md:572` — - 2026-07-30 — **PARTIAL (§4.4 the shared layout probe + §4.2 cleanup candidates).

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
