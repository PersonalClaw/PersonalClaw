<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DFE-7` — Sheets: styled SheetModel + xlsx parser + grid editor (formulas stay formulas)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DOCUMENT-FIDELITY-EDITOR](../plans/DOCUMENT-FIDELITY-EDITOR.md) (`DFE`, plan status `done`) |
| **Atom table** | [`../atomic/DFE.md`](../atomic/DFE.md) |
| **Execution position** | #166 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Session 4 T4.1–T4.2; SheetModel per-cell format (font/fill/number-format/alignment), column widths, merges, formulas-as-formulas; parsers/xlsx_parser.py; the sheet grid editor over the model

## Done when

a formula stays a formula through the round trip (today "=SUM(A1)" is written as a string); a cell edit + a number format survive download/read-back. V4 gate (sheet half) holds.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DFE-5`](DFE-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 13 (DFE) — DRIVEN in a real browser

**Code evidence:**

- src/personalclaw/documents/writers/xlsx_writer.py:6 opens with the contract 'A formula is written as a formula, and a literal is written as a literal'; :86-93 writes cell.formula through the formula path and PINS a value-only cell back to the string type, with the reason inline — 'the model said value, not formula, and a label a user typed' should not become one
- src/personalclaw/documents/xlsx_parser.py (275 lines) reads per-cell number format, fill, alignment, column widths and merges; :184 explains why an ISO-looking string is NOT written back as a date ('the same sniffing' problem)
- web/src/ui/content/SheetGrid.tsx + sheetModelEdit.ts + sheetGridContract.test.tsx (one assertion notes Excel opens the wrong shape as #NAME?)
- 71 tests pass in tests/test_sheets.py + tests/test_decks.py

**Driven in the UI:** Not drivable (no xlsx artifact can be created — see DFE-1).

**Notes:** The done_when's premise is quoted in the writer's own docstring: today "=SUM(A1)" was written as a string. The fix is present with the inverse hazard handled too — openpyxl types anything starting with '=' as a formula, so a value-only cell has to be pinned BACK to a string, which is the half a naive fix would miss.

## Recorded history

### Commits (2)

- `9f679ca3d` 2026-08-28 — docs(roadmap): consolidate four tracking PRs into one — twelve flips, six rulings, INU-9
- `0af3a82e9` 2026-08-26 — feat(files): DFE-7 keep a spreadsheet formula a formula through the round trip

### Named by atom id in the roadmap prose (11)

- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1010` — honestly, but the grid / slide editors are `DFE-7`/`DFE-8`; page setup and paragraph layout
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1024` — Consequence: `DFE-6`, `DFE-7` and `DFE-8` all depended only on `DFE-5` and are now startable.
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1175` — - **NOT in this atom, by scope:** the grid and slide editors are `DFE-7`/`DFE-8`; `PUT …/model`
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1183` — ## Execution log — DFE-7 (sheets: styled SheetModel + xlsx parser + grid editor) — **DONE**
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1185` — - [2026-08-26][S4 · atom `DFE-7`] **DONE.** All three `done_when` clauses hold, each proved by
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1397` — DFE-7 gave: the editing surface renders ONE report for whatever it loaded. Each is registered in
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1412` — components. If the divergence is still cosmetic once DFE-6 and DFE-7 are both on main, that is a
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1425` — - **A primitive gap closed, exactly as DFE-7 closed Select's.** `TextInput` and `TextArea` had no
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1468` — `dashboard.document_editing` already gates this surface, as `DFE-6`/`DFE-7` both found) · every new
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1473` — Four `DFE-7` lines had to change, all of them because `DFE-7` deliberately wrote them to say pptx
- **source plan** `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:1501` — standing as `DFE-7`'s sheet half, which is why the row is `🟡` rather than `✅`.

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T4.1`, `T4.2`, `Session 4`. Those tokens appear in its source plan at:

- `T4.1` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:439` — | T4.1 | `SheetModel` gains per-cell format (font/fill/number-format/alignment), column widths, merges, and **formulas as formulas**; xlsx writer + a new xlsx parser | model, `xlsx_writer.py`, `parsers/xlsx_parser.py`, tests | a formula stays a formula through the round trip (today `"=SUM(A1)"` i…
- `T4.2` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:440` — | T4.2 | The sheet editor (grid) over the model | editor, tests | a cell edit + a number format survive download/read-back |
- `Session 4` `docs/roadmap/plans/DOCUMENT-FIDELITY-EDITOR.md:435` — ### Session 4 — Sheets and decks (the same pattern, twice)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
