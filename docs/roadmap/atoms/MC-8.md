<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `MC-8` — S4 QR pairing screen (renders COMPANION-APPS pairing routes)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [MOBILE-COMPANION](../plans/MOBILE-COMPANION.md) (`MC`, plan status `done`) |
| **Atom table** | [`../atomic/MC.md`](../atomic/MC.md) |
| **Execution position** | #342 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Sessions 4-6 — T4.2 QR pairing + C4 (SUPERSEDED — renders plan-54 §C2 /api/devices/pair/* rather than defining them)

## Done when

Settings > Devices > Pair phone renders a QR of {pairing_url, one-time code}; the shell scans and exchanges it for a device session end to end; code single-use (TTL 5min) verified. Pairing routes are consumed from plan 54, not defined here.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`MC-7`](MC-7.md) | intra-repo | `done` |
| [`MC-2`](MC-2.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `partial`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 57 (MC) — the companion route driven at a 390x844 phone viewport; the content-free push gate falsified

**Code evidence:**

- the pairing routes are CONSUMED rather than redefined, as the atom requires, and the pairing core was separately audited earlier in this campaign: single-use codes stored only as hashes, consumed before the sender is allowed, constant-time comparison, and a short TTL
- the code TTL surfaces to the caller as data rather than as prose: the start response carries `expires_at` and `expires_in` from `pairing.PAIR_CODE_TTL_SECS`
- `test_companion_single_pairing_mechanism.py` passes inside the 106 — a rail against a SECOND pairing mechanism appearing, which is the failure mode a QR screen invites
- device-session + push + relay + shell + companion-discovery + single-pairing suites 106/106; PWA symlink 10/10 (falsified: disabling the payload key check reds 5, including the never-reaches-the-wire test)

**Driven in the UI:** Not drivable end to end: scanning a QR and exchanging it for a device session needs a camera and a second device. The Devices surface the screen lives on was reached from the companion footer.

**Notes:** The single-mechanism rail is the thing worth keeping. A QR pairing screen is exactly where a project grows a parallel code path with weaker properties than the one it already had, and a test that reds when a second mechanism appears is the right shape of defence.

## Recorded history

### Commits (2)

- `4e2a0a68f` 2026-09-04 — docs(roadmap): flip MC-8, BA-9, SV-10, ES-13, ES-16, EI-4 and MI-6 done
- `cdbd885d3` 2026-08-26 — feat(companion): MC-8 render the pairing QR from the routes that already exist

### Named by atom id in the roadmap prose (9)

- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:231` — 1. **Confirm the supersession:** MOBILE-COMPANION's own device-token/QR design (C1/C4) is folded into this plan's unified pairing — approve that reconciliation (it removes duplication; the phone UI + push stay in MOBILE-COMPANION). **Executed by `CA-3` (2026-08-17); two folded shapes want a yes/n…
- **source plan** `docs/roadmap/plans/COMPANION-APPS.md:308` — spelling `pair_url` → `pairing_url`, matching MOBILE-COMPANION C4 and `MC-8`'s done_when (2 of
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:83` — > contract. What this plan builds is the screen (atom `MC-8`): render a QR of the
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:215` — **2026-09-04 — MC-8 validated done (owner-residual device gate).** The QR pairing screen is code-complete on main: the in-repo QR model-2 encoder `web/src/lib/qr.ts` (header names MC-8 as the atom that made the build-vs-install call) is RS-syndrome-verified by `web/src/lib/qr.test.ts`; `PairingQr…
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:563` — ### 2026-08-26 — `MC-8` (S4 T4.2, QR half) — **PARTIAL**: the screen ships and its payload is proven scannable; the shell leg is device-gated
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:568` — from `DevicesPanel.tsx`. So `MC-8` invented nothing. What was missing was exactly one thing, and
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:575` — That is an owner call, and `MC-8` is where it gets made. **The call: encode it in the repo, and
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:796` — **Finding for `MC-8`: `device_session_ref` is not fillable by the shell.** The field wants the nonce
- **source plan** `docs/roadmap/plans/MOBILE-COMPANION.md:918` — `MC-8`, `SH-2` and `LV-4`. Landing it took seven conflicts, all additive except two that had to take

### Its plan's own record, via the scope references (2 line(s))

This atom's scope cites `T4.2`. Those tokens appear in its source plan at:

- `T4.2` `docs/roadmap/plans/MOBILE-COMPANION.md:138` — | T4.2 | QR pairing screen **and endpoint switching**, both consumed from plan 54: Settings → Devices → "Pair phone" renders a QR of COMPANION-APPS §C2's `{pairing_url, code}` → app scans → `pair/complete` → device session (§C1); the paired gateway then becomes a §C1 registry entry (`device_sessi…
- `T4.2` `docs/roadmap/plans/MOBILE-COMPANION.md:563` — ### 2026-08-26 — `MC-8` (S4 T4.2, QR half) — **PARTIAL**: the screen ships and its payload is proven scannable; the shell leg is device-gated

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
