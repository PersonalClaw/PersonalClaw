<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `FS-6` — Re-add a live gated consumer of suppressed_producers() so suppression is enforced again

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [FEEDBACK-SIGNAL](../plans/FEEDBACK-SIGNAL.md) (`FS`, plan status `done`) |
| **Atom table** | [`../atomic/FS.md`](../atomic/FS.md) |
| **Execution position** | #280 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Status line 🔴 REMAINING; Contract C2 suppressed_producers(); Integration points 'workflows/surfacing.py + skills/surfacing.py consult suppressed_producers()'

## Done when

a runtime path withholds a suppressed producer: a producer below retire_threshold with n>=min_n stops surfacing at a live WF2 surfacing gate (or flywheel skill-surfacing eligibility filter), with a test proving suppression prevents surfacing. Today (verified 2026-08-04) no runtime path enforces it because WF2 Phase 1 deleted workflows.surfacing.eligible_workflows, T3.1's only gated consumer.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`FS-3`](FS-3.md) | intra-repo | `done` |
| `EXT:WORKFLOWS-V2:a surfacing eligibility gate to host the `in suppressed_producers()` membership check` | cross-plan (EXT) | **unresolved** |
| `EXT:LEARNING-FLYWHEEL:skill-level producer identity/surfacing gate (alternative consumer)` | cross-plan (EXT) | **unresolved** |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `917ef60f8` 2026-08-12 — feat(workflows): WV-14 make `on_overlap: queue` queue instead of starting a concurrent run (#1130)
- `cf302d452` 2026-08-08 — feat(skills): FS-6 enforce feedback suppression at the live skill-surfacing gate (#928)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/FEEDBACK-SIGNAL.md:207` — - [2026-08-08][FS-6] DONE: re-enforce feedback suppression at a LIVE surfacing gate (Contract C2). `suppressed_producers()` had had no live withholding consumer since WF2 Phase 1 deleted `workflows.surfacing.eligible_workflows` (its only gated consumer) — the control was inert. Wired the `in supp…
- **design note** `docs/roadmap/design-notes/fs6-suppression-consumer.md:1` — # FS-6 — a live gated consumer of `suppressed_producers()`
- **design note** `docs/roadmap/design-notes/fs6-suppression-consumer.md:22` — failure FS-6 exists to fix. The done-when requires *a runtime path WITHHOLDS*, so the host
- **design note** `docs/roadmap/design-notes/fs6-suppression-consumer.md:93` — (which documents the FS-6 gap and asks the next lander to "re-add an end-to-end case") is

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
