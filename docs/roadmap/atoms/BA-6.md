<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `BA-6` — Scheduled-actuator: persisted idempotent browse plans + WATCHED-SOURCES escalation tick + rung caps

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [BROWSE-AUTOMATION](../plans/BROWSE-AUTOMATION.md) (`BA`, plan status `done`) |
| **Atom table** | [`../atomic/BA.md`](../atomic/BA.md) |
| **Execution position** | #84 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Amendment 2026-07-26 (d) Scheduled-actuator pattern; §8.1 WATCHED-SOURCES headless-fetch escalation; §8.2 deep-research template consumption; task A3

## Done when

Persisted plans (browse/plans/<id>.json {goal,kind,cursor,notes}) execute as idempotent one-tick runs — killing the gateway mid-flow loses <=1 step and re-firing the same tick is a no-op at the same cursor; the WATCHED-SOURCES escalation chain falls through from web_fetch to exactly one browse tick that returns meaningful content for a JS-rendered page (SC 7); read-only plans graduate per the AUTONOMY-GUARDRAILS ladder while any SUBMIT-bearing plan registers floor=draft_only and cannot run unattended until promoted — task A3

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`BA-3`](BA-3.md) | intra-repo | `done` |
| [`BA-4`](BA-4.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `7e8321635` 2026-09-03 — docs(roadmap): phase-2 restructure -- gated frontier, new atoms, merged-atom flips
- `242a8b253` 2026-09-02 — feat(browse): persisted idempotent scheduled-actuator plans (BA-6)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:751` — produces no reversal handle. Not `draft_only`: that is BA-6's decision for a persisted browse
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:754` — reversal half BA-6 owns. Workflow action nodes are unaffected — `dispatch_action` does not consult
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1150` — ### `BA-6` — scheduled-actuator (flipped `done` 2026-09-03)
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:1152` — **2026-09-03 — DONE (`BA-6`).** Merged via core PR #2339 (commit 242a8b253, "feat(browse): persisted idempotent scheduled-actuator plans"). Persisted `browse/plans/<id>.json` idempotent one-tick runs (kill mid-flow loses <=1 step; re-fire is a no-op at the cursor), the WATCHED-SOURCES escalation …

### Its plan's own record, via the scope references (1 line(s))

This atom's scope cites `§8.1`, `§8.2`. Those tokens appear in its source plan at:

- `§8.1` `docs/roadmap/plans/BROWSE-AUTOMATION.md:382` — - **(d) Scheduled-actuator pattern** — browse runs as stateless, idempotent scheduled ticks against a persisted plan, not as one long-lived session: `browse/plans/<id>.json` `{goal, kind: watch_page|walk_flow, cursor, notes, max_steps_per_tick}` (atomic_write). "Watch this page" = one tick re-ext…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
