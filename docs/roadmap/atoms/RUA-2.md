<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `RUA-2` — Owner credential (argon2id) + auth config section + personalclaw auth CLI + deploy-env bootstrap seed

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [REMOTE-USER-AUTH](../plans/REMOTE-USER-AUTH.md) (`RUA`, plan status `done`) |
| **Atom table** | [`../atomic/RUA.md`](../atomic/RUA.md) |
| **Execution position** | #508 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design S2; Contracts C2 (auth/credentials.py), C4 (auth config section), C5 (CLI); Session 2 tasks T2.1-T2.4 + V2

## Done when

argon2id set_password/verify_password round-trips with constant-time (timing-equalized) unknown-user path and no hash in logs; auth section wired through all 5 config points (test_config_roundtrip green; login_enabled/require_totp/lockout knobs PATCH-editable, password never is); CLI auth set-password/enable/disable/status prompted no-echo, refused on non-TTY; PERSONALCLAW_LOGIN_USER/PASSWORD seeds credential once at first boot, no-ops on re-run, does not enable login. login_enabled defaults off. 115 new tests green.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (1)

[`RUA-3`](RUA-3.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `T2.1`, `T2.4`, `Session 2`. Those tokens appear in its source plan at:

- `T2.1` `docs/roadmap/plans/REMOTE-USER-AUTH.md:211` — | T2.1 | `auth/credentials.py`: argon2id `set_password`/`verify_password` (constant-time), `credentials.json` (0600); add `argon2-cffi` dep | `auth/credentials.py`, `pyproject.toml` | set→verify round-trips; wrong password rejected; hash never logged |
- `T2.4` `docs/roadmap/plans/REMOTE-USER-AUTH.md:214` — | T2.4 | Deploy bootstrap: `PERSONALCLAW_LOGIN_USER`+`PERSONALCLAW_LOGIN_PASSWORD` seed the credential once at first boot then clear from memory; documented for container/systemd | gateway boot path, `docs/guides/containers.md` | fresh home + env → credential seeded; env absent → no-op; re-run do…
- `T2.4` `docs/roadmap/plans/REMOTE-USER-AUTH.md:350` — **The rotation-safety property in the deploy seed (T2.4).** `bootstrap_from_env()` is a no-op
- `T2.4` `docs/roadmap/plans/REMOTE-USER-AUTH.md:373` — **Found and fixed a live doc bug in T2.4's path.** `docs/guides/containers.md` told headless
- `Session 2` `docs/roadmap/plans/REMOTE-USER-AUTH.md:207` — ### Session 2 — Owner credential + CLI + deploy bootstrap

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
