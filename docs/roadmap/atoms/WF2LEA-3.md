<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `WF2LEA-3` — Step 2: reroute /api/lessons consumers onto memory.db, then delete lessons.jsonl/LessonStore

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [WORKFLOWS-V2-LEARNING-FLYWHEEL](../plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md) (`WF2LEA`, plan status `done`) |
| **Atom table** | [`../atomic/WF2LEA.md`](../atomic/WF2LEA.md) |
| **Execution position** | #588 of 684 in dependency order |
| **Startability** | landed |
| **PR** | ##894 |

## Intent — what this is for

§7 step 2, §4 Disposition (lessons.jsonl LessonStore = REROUTE CONSUMERS), success criterion 6

## Done when

The three /api/lessons consumers (mcp_memory tools, dashboard backing in handlers/schedule.py, no-embedder write fallback in context.py) read/write memory.db lesson.* identically; residual JSONL imported; embedder-less writes verified; lessons.jsonl + LessonStore deleted from gateway.py/cli_server.py/cli_commands.py/context.py; criterion 6 regression test green

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`WF2LEA-1`](WF2LEA-1.md) | intra-repo | `done` |

### Unblocks (1)

[`WF2LEA-14`](WF2LEA-14.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 30 (WF2LEA) — DRIVEN in a real browser

**Code evidence:**

- MEASURED: LessonStore is gone — learning_report.py:18 records 'that class was deleted by' the step, and no definition survives in src/
- the surviving lessons.jsonl references are exactly the RESIDUAL IMPORT the clause requires, not a failed deletion: vector_memory.py:3193 'Migrate legacy markdown memory files and lessons.jsonl into vector memory' and handlers/memory.py:1026 a residual presence check
- 951 tests pass across the 30 learning suites

**Notes:** Read the surrounding code before judging: two live mentions of a file the atom says was deleted look like a contradiction until the clause's own 'residual JSONL imported' requirement is read next to them. A migration reader for a deleted store is the deletion done properly, not the deletion missed.

## Recorded history

### Commits (2)

- `c99892fbd` 2026-08-25 — test(learning): LV-3 rail the summary route registration; flip the atom done
- `1c5b579bf` 2026-08-07 — feat(learning): WF2LEA-3 reroute /api/lessons onto memory.db, delete LessonStore (#894)

### Named by atom id in the roadmap prose (4)

- **source plan** `docs/roadmap/plans/LEARNING-VISIBILITY.md:477` — exists.** It names `learn.py::LessonStore.load_all()`; **`LessonStore` was deleted by WF2LEA-3** and
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:1612` — path end to end: `WF2LEA-3` owns the three `/api/lessons` consumers, `WF2LEA-13` owns how a lesson
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:1691` — **DEVIATION (docs, not code): `WF2LEA-14` declares `WF2LEA-3` as its dependency, and that row's
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:1693` — parallel JSONL store (WF2LEA-3 retired it)"), `LessonStore` is gone, and `/api/lessons` reads and

### Its plan's own record, via the scope references (5 line(s))

This atom's scope cites `§7`, `§4`. Those tokens appear in its source plan at:

- `§7` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:596` — §7 criterion 7 has three clauses and all three are now measurements: per-arm surfaced-vs-used precision
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:641` — - **A mute is a full negative observation**, not a display flag — §7's clause says it must VISIBLY
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:863` — §7's criterion 1 has two halves: one inbox across all six kinds with provenance, evidence manifests and
- `§7` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:881` — - **There is deliberately NO trust parameter.** §7 says "under ANY trust mode", and a gate a mode could
- `§4` `docs/roadmap/plans/WORKFLOWS-V2-LEARNING-FLYWHEEL.md:1442` — §4's row explicitly authorizes dropping it "if not worth it". The evidence says it is not, and that

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
