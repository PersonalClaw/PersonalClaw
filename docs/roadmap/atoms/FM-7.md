<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `FM-7` — Motion budget proof: 60fps pass + reduced-motion/expressiveness=0 zero-motion CI guard

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [FLUID-MOTION](../plans/FLUID-MOTION.md) (`FM`, plan status `done`) |
| **Atom table** | [`../atomic/FM.md`](../atomic/FM.md) |
| **Execution position** | #274 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1829 |

## Intent — what this is for

Session 3 — budget proof (T3.3, V3)

## Done when

60fps verified on ChatPage + a cockpit with no jank; expressiveness=0 and prefers-reduced-motion both proven to yield instant/crossfade with zero springs; a reduced-motion assertion added to web CI where feasible; full-app motion pass holds at bounciness=1 (delightful) and bounciness=0 (calm)

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`FM-4`](FM-4.md) | intra-repo | `done` |
| [`FM-5`](FM-5.md) | intra-repo | `done` |
| [`FM-6`](FM-6.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (2)

- `ac9137423` 2026-08-20 — docs(roadmap): cite PR #1829 for FM-7
- `5cd0c266a` 2026-08-20 — fix(motion): FM-7 close the reduced-motion hole, then measure the budget

### Named by atom id in the roadmap prose (7)

- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:233` — guard and the 60fps pass are `FM-7`, which now inherits a working assertion instead of a comment.
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:298` — `FM-2`'s `Morph.tsx`; this atom ships the crossfade. `FM-7` still owns the 60fps budget proof and
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:395` — **Not in this atom, by scope:** `FM-7` owns the 60fps proof and the CI zero-motion guard, and now
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:492` — **Not in this atom, by scope:** `FM-7` owns the 60fps budget proof and the CI zero-motion guard — the
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:550` — vocabulary (and the 157px snapshot fix above); `FM-7` owns the 60fps proof and the CI zero-motion
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:756` — **`FM-7` is now unblocked** (its deps were `FM-4`, `FM-5`, `FM-6`) and enters the ready frontier.
- **source plan** `docs/roadmap/plans/FLUID-MOTION.md:758` — ### 2026-08-20 — `FM-7` (S3: motion budget proof) — **DONE**. The plan is complete (7/7).

### Its plan's own record, via the scope references (3 line(s))

This atom's scope cites `T3.3`, `Session 3`. Those tokens appear in its source plan at:

- `T3.3` `docs/roadmap/plans/FLUID-MOTION.md:90` — | T3.3 | Budget proof: a motion-performance pass (60fps on ChatPage + a cockpit; profile for jank) + assert `expressiveness=0` and reduced-motion both yield instant/crossfade with zero springs; add a reduced-motion assertion to web CI where feasible | perf profiling, a CI check | 60fps verified; …
- `T3.3` `docs/roadmap/plans/FLUID-MOTION.md:100` — - **Performance on the mega-pages** — Morph/layout animations can thrash; T3.3's budget proof is the gate; if a surface can't hit 60fps, it gets a simpler transition, not a dropped frame.
- `Session 3` `docs/roadmap/plans/FLUID-MOTION.md:84` — ### Session 3 — Route transitions + orchestration + budget proof (Wave 3)

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
