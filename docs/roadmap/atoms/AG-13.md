<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `AG-13` — Consolidate the fourteen autonomy knobs into one declarative policy (shared with SupervisorPolicy)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [AUTONOMY-GUARDRAILS](../plans/AUTONOMY-GUARDRAILS.md) (`AG`, plan status `done`) |
| **Atom table** | [`../atomic/AG.md`](../atomic/AG.md) |
| **Execution position** | #618 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1339 |

## Intent — what this is for

"How much freedom does this work have" is answered in fourteen places with no composition rule: `RunBudget`, `runtime_hints.execution.single_active_feature`, `require_hitl`, `gate_policy`'s risk-scoped auto-approval, the `confirmation` matrix plus per-stage mute, `autonomy.py`'s risk registry / floors / earned trust, `allowed_write_paths`, the `resilience` breaker config, `escalation_cfg.ladder`, loop `trust_ttl_secs`, loop `attended`, `max_cycles`, `idle_secs`, and `SafetyProfile`. You cannot answer what a given run is allowed to do without reading all fourteen. Three independent sources in the loop-engineering literature converge on the same requirement — risk-calibrated autonomy, trust graduated by leverage, and the ruling that the high-leverage decisions never leave — and all three need autonomy readable in ONE place to be tunable at all. The mechanism is already designed: PLATFORM-HARDENING-FLOORS §5's two-level `Ceiling ∩ Profile` with tightest-wins archetype dispatch. Depends on `PP-14` so this and `SupervisorPolicy` are ONE object rather than two competing declarations.

## Done when

One declarative policy composes the fourteen knobs tightest-wins under the `Ceiling ∩ Profile` model, and it is the SAME object `PP-14` declares — a run's supervisor policy and its autonomy ceiling are one declaration, not two. Every existing knob keeps its current effective value for every shipped template and bundled loop kind: a table maps each of the fourteen to its policy field and a test asserts the composed answer equals today's answer for a matrix of runs, so this is a consolidation and not a behaviour change. The path-matcher rule PLATFORM-HARDENING-FLOORS §5 lifted verbatim applies here (never `normpath` a PATTERN — `/a/**/../b` collapses to `/a/b` and silently widens an allow). `AG-11`'s deferred profile/trust behaviours are re-pointed at the consolidated object rather than executed twice.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`AG-7`](AG-7.md) | intra-repo | `done` |
| [`PP-14`](PP-14.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (4)

- `e768c58f7` 2026-08-18 — feat(loops): PP-16 one loop-status vocabulary + the Loop-to-WorkflowRun field map (#1705)
- `acd3bbf81` 2026-08-15 — feat(guardrails): AG-11 default-deny research spawns, cron approval via profile_for_session, project trust gate
- `72f93d908` 2026-08-15 — feat(guardrails): AG-13 consolidate the fourteen autonomy knobs into one SupervisorPolicy
- `6d2a7f0bc` 2026-08-14 — docs(roadmap): add Platform-Primitives plan + 20 atoms (rev 17)

### Named by atom id in the roadmap prose (12)

- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:985` — ### 2026-08-15 — Atom AG-13 (consolidate the fourteen autonomy knobs into one declarative policy) — DONE
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1028` — **DISCOVERY (not fixed here — a behaviour change AG-13 must not make):** `workflows/scope.py:149`
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1031` — lowers `**` to `*`). AG-13's consolidated read surface (`write_scope_allows`) uses the
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1048` — `feature-pp14-supervisor-policy`: **+7, zero removed** (19399 → 19406 — the seven AG-13 tests).
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1050` — (reproduced identically at commit `6de2130e` with no AG-13 change) and are NOT attributable here.
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1055` — AG-13's consolidated `SupervisorPolicy`** — that object ships in an unmerged stacked PR (#1333) and
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1057` — `main` today. AG-13's log line ("AG-11 re-pointed at the consolidated object") is aspirational; the
- **source plan** `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:1059` — write-scope repoint AG-13 flagged as "PP-15/AG-11 territory" needs that consolidated matcher too, so
- **source plan** `docs/roadmap/plans/BROWSE-AUTOMATION.md:520` — `AG-1`…`AG-13` are all `done` in `dag.json`, so Autonomy-Guardrails does not gate this.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:70` — | Autonomy as one policy object | AUTONOMY-GUARDRAILS | **`AG-13`** (new atom there), depending on `PP-14` so the autonomy ceiling and the supervisor policy are ONE object rather than two competing declarations. The two-level `Ceiling ∩ Profile` model it composes is PLATFORM-HARDENING-FLOORS §5's…
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:145` — └─→ AG-13
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:682` — re-homed to LOOPS-EVOLUTION (`WF2LOO-16`/`-17`/`-18`) and one to AUTONOMY-GUARDRAILS (`AG-13`)

### Its plan's own record, via the scope references (8 line(s))

This atom's scope cites `§5`. Those tokens appear in its source plan at:

- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:26` — 📐 **Before executing S5.2, read [PLATFORM-HARDENING-FLOORS](PLATFORM-HARDENING-FLOORS.md) §5.**
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:41` — - **NEW-1 amendment** (fail-safe guard-flag parsing tenet: missing/null/unknown parses ENABLED) → §5 (platform tenet).
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:261` — - **Session 3 — the floor (§1.2-§1.4, §5):** denylist + `check_action` at the three dispatch seams + `sdk.guardrails`; incident kill switch (flag + seam checks + endpoints + CLI); `DISABLE_LIVE_WRITES` honored + auto-set in conftest; `guard_flag` helper + safe-default schema test; SEL wiring thro…
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:481` — §1.3 incident kill switch, §1.4 DISABLE_LIVE_WRITES, §5 `guard_flag` (Session 3); §3
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:485` — ### 2026-07-25 — Session 3 (§1.2 denylist + §1.3 incident + §1.4 DISABLE_LIVE_WRITES + §5) — DONE
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:518` — the §5 schema test walks all dataclasses and fails the build if any guard-class field
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:605` — ### 2026-08-12 — Atom AG-6 / Session 5.1 (§5 rung-ladder core) — DONE
- `§5` `docs/roadmap/plans/AUTONOMY-GUARDRAILS.md:692` — **DEVIATION — the plan's third seam is `_fire_store_trigger`, not `_run_action_job`.** §5's

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
