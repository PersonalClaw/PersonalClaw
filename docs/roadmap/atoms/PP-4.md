<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `PP-4` — Extract the ledger as a platform primitive (`ledger/` package)

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [PLATFORM-PRIMITIVES](../plans/PLATFORM-PRIMITIVES.md) (`PP`, plan status `in_progress`) |
| **Atom table** | [`../atomic/PP.md`](../atomic/PP.md) |
| **Execution position** | #463 of 684 in dependency order |
| **Startability** | landed |
| **PR** | #1290 |

## Intent — what this is for

`workflows/journal.py` is the best observability surface in the tree — append-only, `redact()` on every write, deterministic `event_id`, monotonic `seq`, epoch stamping, 40+ typed kinds, one file read two ways (resume cache + Run Ledger) — and ONLY workflows have it. `loop/store.py` persists findings/verdicts/status with no event vocabulary; tasks carry status; triggers keep their own log. `learning/mining.py:15` derives from "the run's own journal", so the hill-climbing loop covers exactly one of four work-unit kinds. This atom moves the mechanism, not the semantics: no new fields, no new kinds, no behaviour change for workflow runs.

## Done when

A `personalclaw/ledger/` package owns the append/redact/stamp/spill machinery and the kind registry; `workflows/journal.py` becomes a thin workflow-flavoured facade over it with its ledger-kind set unchanged and `LEDGER_KINDS` still asserted by the existing drift test. Byte-identical output for a workflow run: a golden-file test captures a real run's `journal.jsonl` + `events.jsonl` before the move and diffs after, so "pure extraction" is proven rather than claimed. `MAX_INLINE_OUTPUT_BYTES` spilling, binary magic-prefix detection and the `result_omitted` stub move with it. No consumer outside workflows is added here — that is `PP-5`.

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (6)

[`PP-11`](PP-11.md) (`done`), [`PP-5`](PP-5.md) (`done`), [`PP-6`](PP-6.md) (`done`), [`PP-7`](PP-7.md) (`done`), [`PP-8`](PP-8.md) (`done`), [`PP-9`](PP-9.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 70 (PP, final plan) — the dependency-derivation census falsified, which proved its own docstring's claim that a strictness test alone would mean nothing

**Code evidence:**

- 🔑 I MEASURED THE EXTRACTION: `personalclaw/ledger/` is 1324 lines across seven focused modules — `kinds`, `writer`, `reader`, `redaction`, `hashing`, `outcomes`, `__init__` — and `workflows/journal.py` is now a 746-line facade rather than the owner
- 🔑 ARCC'S RETENTION REQUIREMENT LANDS HERE, AND ITS SUBSTANCE IS THE PART THAT TRANSFERS: the guidance is that an unset retention 'silently destroys the evidence an investigation depends on' and that the decision must be VISIBLE rather than defaulted. The ledger states one explicitly at the kind level — a `pending_outcome` with no matching `outcome_resolved` is 'the OPEN QUESTION RETENTION MUST NEVER EVICT' — and at the record level the deletion `reason` distinguishes `delete` from `retention`, so an eviction is never mistaken for a user's deletion
- one owner for kinds, redaction and hashing is what lets PP-6's replay and PP-7's signatures read the same bytes the writer wrote; two definitions of a kind would make a replay of an older run silently mean something else
- 266 passed across the ledger, outcome, priced-ledger and loop-emit suites
- 510 passed / 2 skipped across the PP15/PP16, ledger, admission, supervisor, trajectory, edge-decision and replay suites; bundled-template suite 388/388; frontier golden 5/5; the dependency derivation zeroed reds exactly the two census floors and nothing else

**Driven in the UI:** Not a browser surface: a storage primitive. Its consumer surface was driven in the SH cycle, where the audit page verified 350 events' hash chain live.

**Notes:** A facade rather than a deletion is the right clean-break shape for a module this many callers deep — the seam moves without a flag day.

## Recorded history

### Commits (4)

- `70f939eb6` 2026-08-15 — feat(loops): PP-5 emit the ledger and project findings/verdicts over it
- `80671444d` 2026-08-14 — docs(roadmap): correct 14 atom records the dashboard was reporting wrong
- `fee85e575` 2026-08-14 — refactor(ledger): PP-4 extract the ledger as a platform primitive
- `6d2a7f0bc` 2026-08-14 — docs(roadmap): add Platform-Primitives plan + 20 atoms (rev 17)

### Named by atom id in the roadmap prose (18)

- **source plan** `docs/roadmap/plans/AMBIENT-SURFACES.md:413` — - **The ledger becomes what PP-4 said it lacked: a SECOND PRODUCER.** `TileLedger(LedgerWriter)`
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1215` — `aborted`. Minting a `run_resumed` kind would have been the fifth-dialect defect PP-4 exists to
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:993` — (PP-4's extraction is clean and this pins it for one line). Deliberately picks directions
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:90` — ## §2 The ledger as a platform primitive (`PP-4`…`PP-8`)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:139` — PP-4 ─┬─→ PP-5 (+WF2LOO-16) ─────────────┐
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:148` — **Startable now:** `PP-1`, `PP-4`, `WF2LOO-16`, `WF2LOO-17`, `WF2LOO-18`.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:150` — **If only three land: `WF2LOO-16`, `PP-1`, `PP-4`.** Verdict, edges, ledger. Cheapest, they unblock
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:153` — **The timing argument, which is the part that needs an owner decision.** `PP-4`, `PP-5`, `PP-9` and
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:650` — nondeterminism envelope is complete: provider responses were already spilled by `output_ref` (PP-4)
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:686` — - **2026-08-14 — `PP-4` DONE.** `personalclaw/ledger/` now owns the mechanism: `kinds.py` (the 44
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:699` — - **2026-08-14 — DISCOVERY (`PP-4`): the seam is node identity, not "generic vs specific".** The
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:709` — - **2026-08-14 — DECISION (`PP-4`): the kind constants live in `ledger/kinds.py`, not the facade.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:717` — - **2026-08-14 — DISCOVERY (`PP-4`): the `result_omitted` stub is invisible to a journal diff.**
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:724` — - **2026-08-14 — `PP-4` proof.** Goldens captured on the branch point with `journal.py` unmodified
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:998` — touched. `PP-4`'s ledger-boundary rail stays green: this work lives in `workflows/`, which may import
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1019` — in `learning/`, not `ledger/` — reading a provider is I/O, and `PP-4`'s rail keeps `ledger/` pure.
- **source plan** `docs/roadmap/plans/PLATFORM-PRIMITIVES.md:1156` — `LEDGER_KINDS` (from PP-4) contains `step_completed` but NOT `step_started`, so
- **source plan** `docs/roadmap/plans/SELF-VERIFICATION.md:287` — - [2026-08-23][SV-9] DONE (clauses 1, 2, 4 met; clause 3 PARTIAL — see the note below): Self-QA Companion core (§3.1, §3.2 steps 1-5, §5). `src/personalclaw/selfqa/` — `triage.py` (deterministic path classifier → `user`/`test`/`none` + a one-line rationale that is never empty), `ledger.py` (`reco…

### Its plan's own record, via the scope references (0 line(s))

_Its scope names no task or section token to resolve._

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
