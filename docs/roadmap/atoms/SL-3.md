<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `SL-3` — docs/security/limitations.md — honest limitations in the architecture's own voice

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [SECURITY-LEGIBILITY](../plans/SECURITY-LEGIBILITY.md) (`SL`, plan status `done`) |
| **Atom table** | [`../atomic/SL.md`](../atomic/SL.md) |
| **Execution position** | #527 of 684 in dependency order |
| **Startability** | landed |

## Intent — what this is for

Design 'docs/security/threat-model.md' honest-limitations; Task breakdown Session 1 T1.3; Owner task 2

## Done when

New docs/security/limitations.md states both limitations with code citations (ACP-under-YOLO gates via system-prompt framing not rails, task_modes.py; app 'network' permission declaration-only, apps/permissions.py::can_use_network); owner sign-off received on the copy-sensitive wording

## Dependencies

_None — this atom has no declared dependency._

### Unblocks (2)

[`SL-4`](SL-4.md) (`done`), [`SL-5`](SL-5.md) (`done`)

## Verification

**Audit verdict:** `unaudited`

_No one has checked this atom's `done_when` against the code yet._ A verdict is recorded in [`verdicts.json`](verdicts.json), never by editing this file.

## Recorded history

### Commits (0)

_No commit names this atom._ For a `done` atom that is worth a second look: the work may have landed under a different id, or the status may be wrong.

### Named by atom id in the roadmap prose (0)

_No prose names this atom by id._ That is normal rather than suspicious: the source plans predate the decomposition and refer to their own work by task number, so the record is usually under the scope references below.

### Its plan's own record, via the scope references (6 line(s))

This atom's scope cites `T1.3`, `Session 1`. Those tokens appear in its source plan at:

- `T1.3` `docs/roadmap/plans/SECURITY-LEGIBILITY.md:52` — | T1.3 | Honest-limitations section drafted (ACP-YOLO framing-not-rails with `task_modes.py` citation; `network` declaration-only with the install-consent context; both in the security doc's own internal voice — quote it, don't soften it) | `docs/security/limitations.md` (new dir) | both limitati…
- `T1.3` `docs/roadmap/plans/SECURITY-LEGIBILITY.md:68` — 2. **Review and sign off the honest-limitations wording** (T1.3) and the response-expectation numbers in SECURITY.md — these are public commitments in your name.
- `T1.3` `docs/roadmap/plans/SECURITY-LEGIBILITY.md:78` — - [2026-07-21][staging] DONE: Staged plan 35. Read the plan, the roadmap session discipline, and both sanctioned sources (`docs/architecture/security.md`, `docs/research/learnings/security-and-guardrails.md`). Confirmed target files absent (no `SECURITY.md` either repo; no `docs/security/`). Ran …
- `T1.3` `docs/roadmap/plans/SECURITY-LEGIBILITY.md:83` — - [2026-07-22][T1.3] DONE: `docs/security/limitations.md` — both honest limitations in the architecture's own voice with code citations: (1) ACP-under-YOLO gates via system-prompt framing not rails (`task_modes.py:15-18` native hard-enforce vs ACP protocol path; quoted `security.md` Trust/YOLO se…
- `Session 1` `docs/roadmap/plans/SECURITY-LEGIBILITY.md:46` — ### Session 1 — Disclosure surface
- `Session 1` `docs/roadmap/plans/SECURITY-LEGIBILITY.md:84` — - [2026-07-22][V1] DONE: PVR enabled on BOTH repos via `gh api -X PUT .../private-vulnerability-reporting` (owner task 1 — confirmed `enabled:true` both). All internal links resolve (threat-model.md pending S2, lands same session); the `security.md#trust--yolo-state-trust_modepy` anchor slug comp…

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
