<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `DCU-4` — Thin stdio shim, in-gateway dispatch, tool surface + ceilinged spawn

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [DESKTOP-COMPUTER-USE](../plans/DESKTOP-COMPUTER-USE.md) (`DCU`, plan status `in_progress`) |
| **Atom table** | [`../atomic/DCU.md`](../atomic/DCU.md) |
| **Execution position** | #156 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | AUDIT 2026-09-07 (#2673): the DCU-3 edge is REAL AND LOAD-BEARING, and it is DISCHARGED IN SUBSTANCE — not mistaken, and not shortcut. DCU-4's done_when ("clicks an element by index end-to-end ... a real app driven by element index") is unsatisfiable without DCU-3's driver, and that driver has LANDED and DCU-4 EXECUTES it: computer_use/macos_driver.py (368 lines, all seven op_* handlers), macos_ffi.py (599 lines of real ctypes — walk_window:429, type_text:534, click_located:564), types.py:92 fingerprint_of. NO STUB AND NO LOCAL REIMPLEMENTATION: driver_host.py:54-70 resolve_driver "never invents a stand-in ... a stand-in that accepted an operation and did nothing is the simulated success floor 6 forbids", and no fallback stub exists on Darwin. The duplication an auditor would look for is explicitly REFUSED — service.py:164-171 keeps the fingerprint as "the driver's own summary ... never recomputed here", because a fingerprint derived from the stored elements "would compare a value against itself and always agree"; the dispatch passes it back (service.py:677-680) and macos_driver.py:195-217 refuses an ABSENT fingerprint rather than skipping the check. DCU-4's ceiling clause is real too (sandbox.py:1165-1199 prepends an rlimit shim, not a no-op). DCU-3 stays open only on owner/session actions — the live stale-index validator run and one live computer_type. DO NOT RE-RUN THIS AUDIT. |

## Intent — what this is for

The stdio MCP shim (cli.py) that resolves session identity and forwards while holding no OS handles; in-gateway dispatch running the full chain — enable / policy.check_app / index freshness+fingerprint / check_input_target / SEL gate / platform driver / re-snapshot+redact (service.py, §2); the tool surface registered in tools.py (computer_list_apps, computer_snapshot, computer_click, computer_type, computer_set_value, computer_scroll, computer_perform_action) with MCP registration; and the driver running as a ceilinged subprocess so a wedged/looping driver is kernel-bounded (§3.5). Includes end-to-end validation V1.

## Done when

The agent lists apps and clicks an element by index end-to-end; the shim holds no OS handles; the driver spawn carries the resource ceiling; V1 holds and is recorded — a real app driven by element index with the pointer staying put, a secure-field refusal, SEL records present, and an absent enable file blocking everything.

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`DCU-1`](DCU-1.md) | intra-repo | `done` |
| [`DCU-2`](DCU-2.md) | intra-repo | `done` |
| [`DCU-3`](DCU-3.md) | intra-repo | `todo` |

### Unblocks (3)

[`DCU-5`](DCU-5.md) (`done`), [`DCU-6`](DCU-6.md) (`done`), [`DCU-7`](DCU-7.md) (`done`)

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 67 (DCU) — the keystone's literal-true requirement falsified; the codebase names this campaign's own defect class in production code

**Code evidence:**

- 🏅 THE PRODUCTION CODE NAMES THIS CAMPAIGN'S CENTRAL DEFECT CLASS IN ITS OWN WORDS AND DESIGNS AGAINST IT. `Snapshot.fingerprint` is 'the driver's own summary of the tree it walked. It is stored and compared, NEVER RECOMPUTED HERE: the dispatch has no accessibility API and a fingerprint it derived from the STORED elements would compare a value against itself and always agree — A CHECK THAT CANNOT FAIL, WHICH IS THE DEFECT SHAPE THIS CODEBASE KEEPS FINDING'
- 🔑 NO STAND-IN DRIVER, EVER: `resolve_driver` 'never invents a stand-in … a stand-in that accepted an operation and did nothing is the SIMULATED SUCCESS floor 6 forbids'. An action that reports success without acting is the worst outcome available to a desktop driver
- 🔑 THE ORDER IS THE DELIVERABLE AND IT IS ASSERTED AT RUNTIME: seven steps, keystone first, and `test_the_chain_runs_every_screen_before_the_acting_driver_call` 'records the actual sequence at runtime and asserts it, rather than asserting that each step happened somewhere' — because 'a screen that runs after the driver has already pressed the button is NOT A SCREEN'
- 🔑 ONE DISPATCH ENTRY POINT RATHER THAN SEVEN TOOL FUNCTIONS, and the reason is a count: seven module-level functions 'would be seven keystone readers in a process that must not hold the decision at all'
- 🔑 ARCC'S REQUIREMENT LANDS ON STEP 7 ALMOST WORD FOR WORD. The guidance is that an application with no requirement to handle sensitive data must MASK it in both the prompt and the response, masking preferred so context survives. Here every driver-produced string passes through `redact_credentials`, 'this codebase's ONE definition of credential-shaped text', with the reason stated exactly right: 'A FIELD VALUE THE SYSTEM REDACTS ON THE WAY OUT OF A LOG IS ONE IT MUST NOT HAND TO A MODEL OUT OF A WINDOW'
- 🔴 AND THE NARROWING IS DISCLOSED RATHER THAN SILENT: `computer_list_apps` is narrowed to the operator's allowlist AND 'reports how many applications were WITHHELD', because 'an armed machine granting "drive TextEdit" should not thereby disclose the full list of what the operator has open; the count keeps the narrowing honest instead of pretending nothing was hidden'
- 418 passed / 1 skipped across the twelve computer-use suites; the keystone's literal-true check falsified (2 named cases red) and restored 42/42; the enable file confirmed ABSENT in the validation home

**Driven in the UI:** Not driven: behind the keystone.

**Notes:** The shim holding no OS handles is what makes the ceilinged spawn meaningful — a shim that held handles would keep the capability alive past the subprocess the ceiling applies to.

## Recorded history

### Commits (10)

- `232c26738` 2026-09-07 — docs(roadmap): record the #2673 dependency audit — one real defect, two cleared
- `492807a82` 2026-09-07 — fix(computer-use): the AX refusal names the responsible process, and a past-TTL SEL row keeps its app
- `ccb4541b8` 2026-09-06 — docs(roadmap): flip EI-6, and fix four gates that were wrong about why they were gated
- `daa3b999b` 2026-09-06 — docs(roadmap): flip SH-12 and DCU-4; stop the frontier claiming owner work is startable
- `6a019e3c3` 2026-09-06 — feat(computer-use): run DCU-4's V1 live and record it — the TCC gate was unprobed, not ungrantable
- `f4aa88da0` 2026-08-25 — fix(tools): adjudicate an atom only on entries it is the subject of
- `903cef2f1` 2026-08-25 — docs(roadmap): flip DCU-2 done — DCU-4 landing is the caller its gate was missing
- `05b2ca792` 2026-08-24 — feat(computer-use): DCU-4 thin stdio shim, in-gateway dispatch, tool surface + ceilinged spawn
- `8b4ca7b09` 2026-08-24 — test(computer-use): DCU-2 census the three screens' production call sites
- `8b0636277` 2026-08-15 — feat(computer-use): DCU-1 out-of-band keystone enable-state

### Named by atom id in the roadmap prose (26)

- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:189` — there was no `computer_use` package at all before this atom; `DCU-4` owns the tool surface, the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:195` — until `DCU-4`** — the clause "every computer-use tool", and the atom scope's "first check in the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:203` — loud and reds the moment `DCU-4` adds the first tool. Measured on a synthetic future tool: an
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:212` — defect, and the alternative (inventing a dispatch chain to have a caller) is worse — `DCU-4` owns
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:378` — remember. That is correct sequencing (`DCU-3` owns the driver, `DCU-4` owns `service.py`'s chain and the
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:379` — `computer_*` tools) but it means the `done_when`'s end-to-end phrasing is only demonstrable at `DCU-4`,
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:395` — `DCU-4`'s composition for it: a per-tool "must call `check_input_target`" rail contradicts `DCU-4`'s
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:399` — chain would satisfy only if the tools re-check it. Recorded for whoever takes `DCU-4`.
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:401` — - **2026-08-24 — `DCU-4` DONE (composition, tool surface, thin shim, ceilinged spawn) except the one clause
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:411` — *"armed but unexercised until `DCU-4`"*). *Not met, and it is a DEPENDENCY not a gap:* **"lists apps and
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:414` — `macos_ffi.py` or `types.py`). `DCU-4` declares `DCU-3` as a dep; the atom was taken anyway because
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:505` — **DEP NOTE — `EXT:AUTONOMY-GUARDRAILS` on `DCU-4`, answered concretely.** The **SEL-audit half is now
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:508` — it is `DCU-5`'s declared deliverable and `DCU-5` depends on `DCU-4`, so wiring it now would empty that
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:509` — atom. What `DCU-4` owes it instead is exactly one insertion point, and there is exactly one: in
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:559` — - **[2026-08-25][`DCU-2`] DONE — its gate was inertness, and `DCU-4` landing IS the caller.** The atom's
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:576` — at population ZERO and **flipped rather than deleted** by `DCU-4` — its docstring says why: *"a call
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:582` — yet ("a central-dispatch rail has no dispatch to bind to yet"). `DCU-4` supplied the dispatch, and
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:628` — - [2026-08-26][DCU-3] DEVIATION: re-scoped two `DCU-4` tests whose premise this atom retires, in
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:632` — nothing, and `DCU-4`'s own docstring predicted the change ("when `DCU-3` lands, what changes is
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:682` — `DRIVER_MODULES`'s Windows and Linux entries — which have named those modules since `DCU-4`
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:764` — insertion point `DCU-4`'s log reserved). **No new rung name was minted:** `guardrails/autonomy.py`
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:824` — - **[2026-08-27] `DCU-4` is NOT implementation-blocked — it is VALIDATION-gated, and the dep graph misleads.** Its
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:827` — **The consequence matters for anyone reading deps:** `DCU-5`, `DCU-6` and `DCU-7` all list `DCU-4` and therefore
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:883` — ## Execution log — `DCU-4` V1 (the live as-a-user validation) — **HOLDS 2026-09-06**
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:897` — because dispatching is the only entry point `DCU-4` shipped, and a validation that reached
- **source plan** `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:940` — `unproven`, and the grant premise `DCU-4` recorded is not a property of this workstation.**

### Its plan's own record, via the scope references (4 line(s))

This atom's scope cites `§2`, `§3.5`. Those tokens appear in its source plan at:

- `§2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:478` — mcp-computer` stdio server.** The plan's §2 draws one, but this repo already has exactly one stdio MCP
- `§2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:480` — `_post`, which is precisely the thin shim §2 describes ("the same reasoning as PersonalClaw's other MCP
- `§2` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:491` — element index** and so skips step 3 — §2 reserves that path for canvas UI that exposes no addressable
- `§3.5` `docs/roadmap/plans/DESKTOP-COMPUTER-USE.md:471` — stale-index refusal. Driver timeout 20s is the userspace half of §3.5 — a wedged child becomes a legible

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
