<!-- GENERATED by tools/gen_atom_records.py — do not edit. Every claim below is rendered from a committed input; edit that input and regenerate. -->

# `EI-7` — Second-opinion handoff + ProposerBackend + sandbox-internal tool gateway

| | |
|---|---|
| **Status** | `done` |
| **Plan** | [EXECUTION-ISOLATION](../plans/EXECUTION-ISOLATION.md) (`EI`, plan status `done`) |
| **Atom table** | [`../atomic/EI.md`](../atomic/EI.md) |
| **Execution position** | #229 of 684 in dependency order |
| **Startability** | landed |
| **Blocked reason** | PARTIAL, verified 2026-08-27 - nothing remains INSIDE this atom, so do NOT schedule it. The second-opinion clause HOLDS: select_target drops the origin runner via is_same_runner BEFORE any other filter, backends launch through sandbox_providers.registry.resolve_provider (never a bare subprocess), the re-diff uses content digests rather than `git diff` and is consumed as `accepted = bool(result.ok and result.diff_verified)`, with SEL rows and all four wiring points on main. CAVEAT: no stall detector fires it - the three declared consumers (loop watchdog 'stagnant' offer, gate on_stall: second_opinion, cockpit stalled banner) do not exist (grep on_stall returns two comments and zero implementation), so it is operator-wired, never automatic. The sandbox-interior clause is UNMET and structurally so: sandbox_providers/ has NO docker or lima provider, so there is no sandbox to be inside, and the shipped half is INERT - grep tool_gateway|pclaw_tool over src/ web/ returns ONE hit, a docstring cross-reference inside tool_gateway.py itself, and install_shim is called only from tests because SandboxHandle is a 3-method ABC with no copy_file_in to deliver the shim. What DOES hold host-side is the real refusal: _refusal enforces SafetyProfile.tool_grants via task_mode_denies. SMALLEST REMAINING UNIT: none here - dependency-blocked on EI-2/EI-4 plus a copy_file_in on SandboxHandle. |

## Intent — what this is for

§4.1 second-opinion action provider (registered + added to ALLOWED_HOOK_PROVIDERS) with handoff brief + fire-wait-verify; §4.2 ProposerBackend 4-method contract + subagent fallback backend; §5.2 pclaw-tool shim + loopback-free exec-channel tool gateway; §11 Session 6

## Done when

SC6: a stalled loop's "second opinion" fires a DIFFERENT cataloged runner one-shot inside the same sandbox class and is accepted only when the disk re-diff confirms the edits, SEL-audited. SC7: inside a docker/lima sandbox `pclaw-tool memory_recall` succeeds while ss/netstat shows zero listening sockets, no credential material exists in the sandbox, and a research-profile sandbox is refused write-class tools host-side

## Dependencies

| Depends on | Kind | Status |
|---|---|---|
| [`EI-1`](EI-1.md) | intra-repo | `done` |
| [`EI-5`](EI-5.md) | intra-repo | `done` |

### Unblocks (0)

_Nothing depends on this atom._

## Verification

**Audit verdict:** `confirmed`

**Checked on:** 2026-09-09

**Checked by:** audit cycle 65 (EI) — the spawn-site census falsified (an unmapped spawn named to file:line); the runner provenance rows driven in a real browser

**Code evidence:**

- `sandbox_providers/tool_gateway.py` and `pclaw_tool.py` ship, with `tests/test_sandbox_tool_gateway.py` green
- 🔑 THE SECOND-OPINION HANDOFF IS ACCEPTED ONLY ON EVIDENCE, WHICH IS THE WHOLE ATOM: a different cataloged runner's one-shot is 'accepted ONLY WHEN THE DISK RE-DIFF CONFIRMS THE EDITS', and it is SEL-audited. An accepted-because-it-claimed-success handoff would let a second model's report stand in for its work
- requiring a DIFFERENT runner is the point of a second opinion — the same runner twice is one opinion bought twice, the same reasoning AR-9's council template gives for its distinct roles
- 358 passed / 3 skipped across 17 sandbox, runner, vault, checkpoint and triage suites; 16/16 on the runner-health frontend suite; the Secrets vault and Agent-defaults pages driven on :10011

**Driven in the UI:** Not driven: needs a stalled loop and two bound runners.

**Notes:** The sandbox-internal tool gateway is what makes the second opinion possible without widening the sandbox — the inner runner reaches core tools through one mediated path rather than through a hole in the boundary.

## Recorded history

### Commits (2)

- `ea0e262d2` 2026-09-02 — docs(roadmap): flip 13 validated/landed atoms done; PLATFORM-HARDENING-FLOORS complete
- `4b4690603` 2026-08-25 — feat(isolation): EI-7 ProposerBackend contract + second-opinion handoff

### Named by atom id in the roadmap prose (8)

- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1110` — - [2026-08-25][EI-7] **PARTIAL — SC6 met, SC7 met host-side only.** §4.2's `ProposerBackend` had
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1414` — - **[2026-08-27] OWNER RULING — `EI-7`: DEVIATION accepted; the in-sandbox observation moves to `EI-2`'s
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1418` — inside one. `EI-7`'s own deliverable (`ProposerBackend` plus the different-cataloged-runner one-shot, accepted only
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1419` — when the disk re-diff confirms the edits, SEL-audited) is complete and green as PR **#2079**. Flip `EI-7` on merge.
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1421` — ## Execution log — EI-7 (second-opinion handoff + ProposerBackend + tool gateway) — validated done
- **source plan** `docs/roadmap/plans/EXECUTION-ISOLATION.md:1423` — **2026-09-02 — validated on main (impl pre-landed).** `proposer/__init__.py` (cites EI-7): SC6 second-opinion via a DIFFERENT cataloged runner, disk re-diff acceptance, SEL-audited. `sandbox_providers/pclaw_tool.py` + `tool_gateway.py`: SC7 socketless in-sandbox tool gateway. Suites green (propos…
- **source plan** `docs/roadmap/plans/PLATFORM-HARDENING-FLOORS.md:1377` — `lima`, `limactl`, `colima` all absent — the same measurement that blocks `WF2WOR-12` and `EI-7`'s SC7).
- **source plan** `docs/roadmap/plans/WORKFLOWS-V2-KNOWLEDGE-SYNTHESIS.md:1049` — `nerdctl`, `lima`, `limactl`, `colima` all absent — the same measurement blocks `EI-7`'s SC7);

### Its plan's own record, via the scope references (10 line(s))

This atom's scope cites `§4.1`, `§4.2`, `§5.2`, `§11`, `Session 6`. Those tokens appear in its source plan at:

- `§4.1` `docs/roadmap/plans/EXECUTION-ISOLATION.md:39` — - **NEW-14** (execution isolation & runner substrate: ephemeral workspace leases, per-run worktree isolation + diff panel, durable tmux sessions, BYO AgentRunner registry with second-opinion handoff, remote BYOI far edge) → §1-§5, honoring the overlap note: WORK-R3 (workspace-provisioning block),…
- `§4.1` `docs/roadmap/plans/EXECUTION-ISOLATION.md:113` — 1. **Subagents:** `SubagentManager.spawn` gains `sandbox: str = "none"`; the spawn path resolves the provider and runs the worker's process tree through `handle.exec`. `validate_cwd` + the existing approval ladder are unchanged — sandboxing is an *additional* layer, not a replacement for the allo…
- `§4.1` `docs/roadmap/plans/EXECUTION-ISOLATION.md:278` — | In-cockpit diff panel + reintegration verbs + localhost preview | approved in WORK-CONTAINERS §4.1 — §7's triage panel *extends* it |
- `§4.1` `docs/roadmap/plans/EXECUTION-ISOLATION.md:1172` — it):** the three §4.1 CONSUMERS. The loop watchdog's `stagnant` "second opinion" offer, the
- `§4.2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:40` — - **NEW-14 amendments (batch 2)** — (a) Lima VM tier (shell/file ops only, LLM host-side, cached availability probe, host↔guest path translation, explicit degradation) → §2; (b) pluggable SandboxProvider registry (bind-mount/isolated taxonomy + 6-method handle, none+docker built-ins, consumed by …
- `§4.2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:84` — egress_tier: str               # off|listed|registry|all — AUTONOMY-GUARDRAILS §4.2
- `§4.2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:1110` — - [2026-08-25][EI-7] **PARTIAL — SC6 met, SC7 met host-side only.** §4.2's `ProposerBackend` had
- `§5.2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:199` — - **Reattach-not-reap boot order:** the existing orphan recoveries (`reap_orphaned_loops`, subagent `_reconcile_orphans`) gain a pre-step — before tombstoning, probe `tmux -L personalclaw has-session -t <recomputed-name>`; alive → reattach + resume streaming (the run flips to WORK-R7's `suspended…
- `§5.2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:249` — - **Sandbox integration:** the §1 `SandboxSpec.env` is populated ONLY from explicit vault grants (per WORK-R19's secret-filtered leaf env); the vault UI's per-secret "grant to sandboxed runs" toggle is the consent surface. `docker`/`lima` providers never see ungranted keys, and §5.2 guarantees gr…
- `§5.2` `docs/roadmap/plans/EXECUTION-ISOLATION.md:1144` — `cleanup`) with no `copy_file_in`, so §5.2's "copied in via `copy_file_in`" has no method to call

---

_Rendered from `docs/roadmap/atomic/dag.json` (identity, status, scope, deps), `evidence.json` (git history), `verdicts.json` (audit verdicts) and the roadmap prose. Regenerate with `python3 tools/gen_atom_records.py`._
